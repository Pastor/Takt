# BuT — Язык программирования для конечных автоматов

**BuT** (Bus and Transitions) — предметно-ориентированный язык (DSL) для моделирования конечных автоматов и компонентных
систем, реализованный на Rust. Язык ориентирован на описание встраиваемых систем, управление состояниями и аппаратную
абстракцию с доступом к портам ввода-вывода.

---

## Содержание

1. [Архитектура проекта](#архитектура-проекта)
2. [Лексический анализ](#лексический-анализ)
3. [Синтаксис языка](#синтаксис-языка)
4. [Система типов](#система-типов)
5. [Семантика и модель выполнения](#семантика-и-модель-выполнения)
6. [Модульная система](#модульная-система)
7. [Встроенные функции](#встроенные-функции)
8. [Диагностика и ошибки](#диагностика-и-ошибки)
9. [Примеры программ](#примеры-программ)
10. [Тестирование](#тестирование)
11. [Оставшиеся проблемы и открытые задачи](#оставшиеся-проблемы-и-открытые-задачи)
12. [Реализованные улучшения](#реализованные-улучшения)

---

## Архитектура проекта

### Структура репозитория

```
BuT/
├── grammar/
│   ├── src/
│   │   ├── lib.rs                  — публичный API: parse, compile_to_c, unused/nondeterministic warnings
│   │   ├── diagnostics.rs          — система диагностических сообщений
│   │   ├── lsp.rs                  — LSP-утилиты: диагностика, hover, completion, goto_declaration (фича `lsp`)
│   │   ├── bin/
│   │   │   ├── butc.rs             — CLI-компилятор: butc compile --target c (FE5)
│   │   │   └── but_lsp.rs          — LSP-сервер: обработка запросов, синхронизация документов (PR7)
│   │   ├── generator/
│   │   │   ├── mod.rs              — трейт Generator, enum Language
│   │   │   ├── indent.rs           — утилиты форматирования отступов
│   │   │   └── c/
│   │   │       ├── mod.rs          — impl AsGenerator, get_typed_variable
│   │   │       ├── c_header.rs     — генерация .h: typedef struct, forward decl, топологическая сортировка
│   │   │       ├── c_source.rs     — генерация .c: тела функций, unroll_*, impl Generator (методы)
│   │   │       └── c_map.rs        — CMap: маппинг модели для навигации при кодогенерации
│   │   ├── parser/
│   │   │   ├── mod.rs              — точка входа парсера
│   │   │   ├── lexer.rs            — лексический анализатор (~980 строк)
│   │   │   └── ast.rs              — определения узлов AST (включая EnumDefine, FE1)
│   │   └── semantic/
│   │       ├── mod.rs              — определения семантических узлов (ModelNode, StateNode, ReferenceNode…)
│   │       ├── tree.rs             — построение семантического дерева (7 проходов)
│   │       ├── reference.rs        — разрешение условий `ref`-переходов
│   │       ├── expression.rs       — разрешение выражений
│   │       ├── statement.rs        — разрешение операторов
│   │       ├── type_node.rs        — система типов (TypeNode)
│   │       ├── type_inference.rs   — вывод типов (включая пользовательские псевдонимы, FE2)
│   │       ├── condition.rs        — разрешение именованных условий
│   │       ├── function.rs         — разрешение функций и параметров (включая [T;N], FE6)
│   │       ├── named_block.rs      — разрешение именованных блоков (enter/exit/always)
│   │       ├── import.rs           — импорт файлов и модульная система
│   │       ├── enum_node.rs        — узел перечисления EnumDefinitionNode (Ce4)
│   │       ├── builtin.rs          — реестр встроенных функций
│   │       ├── naming.rs           — утилиты разрешения имён
│   │       ├── unused.rs           — Ce13: диагностика неиспользуемых переменных (FE3)
│   │       ├── docs.rs             — привязка документационных `///`-комментариев
│   │       ├── index.rs            — SemanticIndex: поиск узлов по байтовому смещению (LSP)
│   │       └── validate.rs         — семантическая валидация (Ce14: детерминированность, NI6)
│   └── tests/
│       ├── data/lexer/             — тестовые файлы лексера
│       ├── data/parser/            — тестовые файлы парсера
│       ├── data/semantic/          — тестовые файлы семантики
│       ├── data/lsp/               — тестовые файлы LSP
│       └── lsp_tests.rs            — 33 интеграционных теста LSP (position, node_at_position, hover, goto_declaration)
├── changes/                        — патч-файлы для каждой реализованной фичи
├── examples/                       — примеры программ
├── extensions/                     — расширения IDE (Zed)
└── BuT.ebnf                        — грамматика в формате EBNF
```

### Конвейер компиляции

```
Исходный файл (.but)
        │
        ▼
┌─────────────────┐
│  Лексический    │  lexer.rs
│  анализатор     │  Токены с позиционной информацией
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Синтаксический │  LALRPOP (LR(1) парсер)
│  анализатор     │  Абстрактное синтаксическое дерево (AST)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Семантический  │  7 проходов: скелет → композиция → выражения → условия → блоки → функции → ref
│  анализатор     │  Семантическое дерево (ModelNode/StateNode/...)
└────────┬────────┘
         │
         ▼
   Семантическая модель + Диагностика
```

---

## Лексический анализ

Лексер написан вручную на Rust без использования генераторов. Он поддерживает:

- **Идентификаторы** — Unicode XID + символ `$`: `myVar`, `_x`, `$tmp`
- **Числа**:
    - Целые десятичные: `0`, `123`, `1_000_000`
    - Шестнадцатеричные: `0xFF`, `0x1A_BC`
    - Рациональные (float): `3.14`, `1e10`, `2.5E-3`
    - Адресные литералы: `0x00100000:5` (адрес:бит)
- **Строки**: одинарные и двойные кавычки, префикс `unicode`:
  ```
  "hello"    'world'    unicode"текст"    unicode'текст'
  ```
- **Комментарии**:
    - Однострочный: `// комментарий`
    - Документационный: `/// документация`
    - Блочный: `/* комментарий */` (в том числе многострочный; реализовано в C3)
- **Ключевые слова**: автоматически распознаются через статическую хеш-таблицу (PHF)

### Таблица ключевых слов

| Группа          | Ключевые слова                                             |
|-----------------|------------------------------------------------------------|
| Объявления      | `var`, `const`, `type`, `fn`, `extern`, `port`, `enum`     |
| Управление      | `if`, `else`, `loop`, `for`, `break`, `continue`, `return` |
| Состояния       | `model`, `state`, `start`, `ref`, `next`                   |
| Прочее          | `import`, `as`, `from`, `cond`, `formula`, `assembly`      |
| Литералы        | `true`, `false`                                            |
| Зарезервированы | `template`, `string`, `pragma`                             |

---

## Синтаксис языка

### Точка входа

Программа состоит из последовательности элементов верхнего уровня:

```
source_unit = { model_element } ;
```

### Импорт

```but
import "shared.but";
import "engine.but" as Motor;
import * as Utils from "lib.but";
import { foo, bar as baz } from "file.but";
```

### Определение типов

```but
type u8  = [bit;8];
type u16 = [bit;16];
type u32 = [bit;32];
type u64 = [bit;64];
```

### Переменные и константы

```but
var x;                   // без типа и значения
var x: u8;               // с явным типом
var x = 100;             // тип выводится из инициализатора
var x: u8 = 50;          // явный тип и значение

const MAX: u8 = 255;     // константа (инициализатор обязателен)
```

### Порты ввода-вывода

```but
port LED: u8   = 0x00100000;      // адрес памяти
port BTN: bit  = 0x00200000:0;    // адрес:номер_бита
```

### Функции

```but
extern fn debug(msg: u8);                        // внешняя функция

fn add(a: u8, b: u8) -> u8 {                    // локальная функция
    return a + b;
}

fn reset();                                       // прототип без тела
```

### Модели и состояния

```but
model TrafficLight {
    var timer: u8 = 0;

    start Red {                         // начальное состояние (ровно одно)
        enter  { timer = 0; }           // выполняется при входе в состояние
        always { timer = timer + 1; }  // выполняется каждый цикл
        exit   { }                      // выполняется при выходе из состояния
        ref Yellow: timer > 10;         // условный переход
    }

    state Yellow {
        next Green;                     // безусловный переход
    }

    state Green {
        ref Red: true;
    }
}
```

### Условия (cond)

```but
cond IsActive     = x = 1;           // = означает равенство, не присваивание!
cond InRange      = x > 0 & x < 100;
cond Either       = IsActive | InRange;
cond NotActive    = !IsActive;
```

### Операторы управления потоком

```but
// if — без скобок вокруг условия
if x > 0 {
    x = x - 1;
} else {
    x = 0;
}

// loop с условием (аналог while) — без скобок
loop x > 0 {
    x = x - 1;
}

// бесконечный loop (без условия)
loop {
    x = x + 1;
    if x >= 10 { break; }
}

// for — без скобок вокруг заголовка
for var i = 0; i < 10; i = i + 1 {
    // ...
}
```

### Выражения — таблица приоритетов

| Уровень | Оператор          | Описание                    | Ассоциативность |
|---------|-------------------|-----------------------------|-----------------|
| 14      | `=`               | присваивание                | правая          |
| 13      | `\|\|`            | логическое ИЛИ              | левая           |
| 12      | `&&`              | логическое И                | левая           |
| 11      | `==` `!=`         | равенство                   | левая           |
| 10      | `<` `>` `<=` `>=` | сравнение                   | левая           |
| 9       | `\|`              | побитовое ИЛИ               | левая           |
| 8       | `^`               | побитовое XOR               | левая           |
| 7       | `&`               | побитовое И                 | левая           |
| 6       | `<<` `>>`         | битовые сдвиги              | левая           |
| 5       | `+` `-`           | сложение, вычитание         | левая           |
| 4       | `*` `/` `%`       | умножение, деление, остаток | левая           |
| 3       | `**`              | возведение в степень        | правая          |
| 2       | `!` `~` `+` `-`   | унарные операторы           | —               |
| 1       | `num:bit`         | адресный литерал            | —               |

### Специальные конструкции выражений

```but
{1, 2, 3}                 // инициализатор массива
arr[5]                    // доступ по индексу
arr[2:6]                  // срез массива
arr[2:]                   // срез от позиции до конца
x.3                       // доступ к биту (member access)
x as u16                  // приведение типа
```

### Именованные блоки

```but
start S {
    enter  { /* выполнится один раз при входе */ }
    exit   { /* выполнится один раз при выходе */ }
    always { /* выполняется каждый цикл */ }
    always { /* оба блока always выполняются */ }
    custom_block { /* пользовательские блоки допустимы */ }
}
```

### Композиция моделей

```but
model Step1 { start S; }
model Step2 { start S; }
model Step3 { start S; }

start Sequential = Step1 + Step2;           // последовательная: Step1, затем Step2
state Parallel   = Step1 | Step2;           // параллельная: оба одновременно
state Combined   = (Step1 + Step2) | Step3; // комбинированная
```

### Формулы и ассемблерные вставки

```but
assembly "arm" {
    // платформо-зависимый код
}

formula "ltl" {
    always(active())
    eventually(done())
}
```

---

## Система типов

### Примитивные типы

| Тип     | Размер     | Значения                  | Описание                                    |
|---------|------------|---------------------------|---------------------------------------------|
| `bit`   | 1 бит      | `0`, `1`, `true`, `false` | Одиночный бит                               |
| `bool`  | 1 бит      | `true`, `false`           | Булев тип                                   |
| `float` | переменный | IEEE                      | Рациональное число                          |
| `unit`  | 0          | —                         | Пустой тип (возвращаемое значение процедур) |

### Составные типы

```but
[bit;8]          // массив из 8 бит (байт)
[u8;16]          // массив из 16 байт
```

### Вывод типов

Компилятор автоматически выводит тип из инициализатора:

| Инициализатор     | Выводимый тип |
|-------------------|---------------|
| `0` … `255`       | `[bit;8]`     |
| `256` … `65535`   | `[bit;16]`    |
| `65536` … `2³²−1` | `[bit;32]`    |
| `2³²` … `2⁶⁴−1`   | `[bit;64]`    |
| `true` / `false`  | `bool`        |
| `3.14`, `1e10`    | `float`       |

### Условия и выражения — разные операторы равенства

Важная особенность: в контексте **условий** (`cond`, `ref`) оператор `=` означает **проверку равенства**, а не
присваивание:

```but
cond IsZero = x = 0;        // x равен 0?
var x = 0;                  // присваивание 0 переменной x
```

В обычных **выражениях** `==` — это сравнение, `=` — присваивание.

---

## Семантика и модель выполнения

### Жизненный цикл конечного автомата

1. **Начальное состояние**: каждая модель с состояниями должна иметь ровно одно `start`-состояние
2. **Вход в состояние**: выполняются все блоки `enter` в порядке объявления
3. **Активное состояние**: выполняются все блоки `always` каждый цикл
4. **Проверка переходов**: вычисляются условия `ref`; при истинности — переход
5. **Выход из состояния**: выполняются все блоки `exit`

### Переходы

```but
ref NextState: condition;   // условный переход (condition — выражение или cond)
next NextState;             // безусловный переход (всегда срабатывает)
```

### Области видимости

- Переменные модели видны во всех вложенных состояниях
- Переменные состояния видны только в нём и вложенных блоках
- Разрешение имён: локальная область → родительская модель → прародительская и т.д.
- Затенение: локальное объявление скрывает родительское

### Семантические проходы анализатора

| Проход                          | Описание                                                                                                                                                                                   |
|---------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 0. Скелет                       | Извлекаются имена моделей/состояний, загружаются импорты                                                                                                                                   |
| 1. Композиция (`implements`)    | Разрешаются составные состояния (`= M1 + M2`, `= M1 \| M2`)                                                                                                                                |
| 2. Выражения переменных         | Разрешаются ссылки на переменные, вывод типов, порты                                                                                                                                       |
| 3. Именованные условия (`cond`) | Разрешаются объявления `cond` → `Condition`                                                                                                                                                |
| 4. Именованные блоки            | Разрешаются блоки `enter`/`exit`/`always`                                                                                                                                                  |
| 5. Функции                      | Разрешаются тела локальных и внешних функций                                                                                                                                               |
| 6. Условия переходов `ref`      | `Condition::Unresolved` заменяется разрешёнными условиями                                                                                                                                  |
| Валидация                       | Семантические проверки: Се1 (один start), bit-значения, Ce13 (неиспользуемые переменные), Ce14 (детерминированность), NI6 (типобезопасность enum), Ce11 (неявный bool), Ce5 (достижимость) |

### Ссылки на родителя в семантических узлах

Большинство семантических узлов (`VariableNode`, `ConditionNode`, `FunctionNode`, `NamedCodeBlock`) содержат поле
`upper: Option<Rc<RefCell<ModelNode>>>` — ссылку на родительскую модель. Это позволяет:

- получить контекст модели изнутри узла без передачи `model` как отдельного параметра;
- обходить объявленные переменные и условия соседних узлов без обратного прохода по дереву.

Сравнение методом `PartialEq` игнорирует поле `upper` (во избежание рекурсии).

---

## Модульная система

### Синтаксис импорта

```but
import "path/to/file.but";                        // весь файл
import "path/to/file.but" as Module;              // с переименованием
import * as Module from "path/to/file.but";       // все экспорты
import { TypeA, FnB as FnAlias } from "file.but"; // выборочный импорт
```

### Правила разрешения имён

- Файлы ищутся по списку путей поиска
- Имя модуля выводится из имени файла (camelCase: `my_file.but` → `MyFile`)
- Повторный импорт одного файла идемпотентен
- Выборочный импорт ищет имя в порядке: модели → типы → переменные → условия

---

## Встроенные функции

В компиляторе зарегистрированы следующие встроенные функции:

| Функция   | Сигнатура                                       | Назначение                                       |
|-----------|-------------------------------------------------|--------------------------------------------------|
| `debug`   | `debug(msg: BuiltinString) -> Unit`             | Вывод отладочного сообщения                      |
| `S`       | `S(model: BuiltinModel) -> BuiltinState`        | Получение начального состояния модели            |
| `min`     | `min(a: BuiltinNumeric, b: BuiltinNumeric) -> BuiltinNumeric` | Минимум двух числовых значений   |
| `max`     | `max(a: BuiltinNumeric, b: BuiltinNumeric) -> BuiltinNumeric` | Максимум двух числовых значений  |
| `abs`     | `abs(x: BuiltinNumeric) -> BuiltinNumeric`      | Абсолютное значение числа                        |
| `clamp`   | `clamp(x, lo, hi: BuiltinNumeric) -> BuiltinNumeric` | Ограничение значения в диапазон [lo, hi]    |

Тип `BuiltinNumeric` обозначает «любой числовой тип» (`bit`, `float`, `[bit;N]`).

Остальные функции требуют явного объявления (`fn` или `extern fn`).

---

## Диагностика и ошибки

### Структура диагностического сообщения

```
Уровень: Debug | Info | Warning | Error
Тип:     ParserError | SyntaxError | DeclarationError |
         TypeError | CastError | SematicError | Warning
Позиция: файл, строка, столбец (начало и конец)
Заметки: дополнительный контекст к ошибке
```

### Категории ошибок

| Категория          | Примеры                                       |
|--------------------|-----------------------------------------------|
| `ParserError`      | Неожиданный токен, неожиданный EOF            |
| `SyntaxError`      | Некорректная конструкция                      |
| `DeclarationError` | Неизвестный идентификатор, дублирование имени |
| `TypeError`        | Несоответствие типов, недопустимое приведение |
| `SematicError`     | Нарушение семантических правил                |
| `Warning`          | Потенциально проблемный код                   |

### Примеры проверок

- Ровно одно `start`-состояние в каждой модели
- Переменная типа `bit` принимает только `0`, `1`, `true`, `false`
- Все использованные переменные/функции/типы должны быть объявлены
- Индексы массивов проверяются при известном типе
- Ce13: предупреждение о неиспользуемых переменных
- Ce14/NI4: недетерминированные переходы (дубликаты и перекрытие интервалов условий)
- Ce15: размер массива не превышает `MAX_ARRAY_SIZE = 1024`
- Ce16: рекурсивные псевдонимы типов (`type A = [A; 8];`) — DFS-обнаружение
- Ce17: дублирующиеся поля структуры
- Ce18: неизвестный тип поля структуры

---

## Примеры программ

### Минимальный конечный автомат

```but
model Minimal {
    start Idle;
    state Active;
    state Done;
}
```

### Светофор с таймером

```but
type u8 = [bit;8];

model TrafficLight {
    var timer: u8 = 0;

    start Red {
        enter  { timer = 0; }
        always { timer = timer + 1; }
        ref Green: timer = 255;
    }

    state Green {
        enter  { timer = 0; }
        always { timer = timer + 1; }
        ref Yellow: timer = 100;
    }

    state Yellow {
        enter  { timer = 0; }
        always { timer = timer + 1; }
        ref Red: timer = 50;
    }
}

start Entry = TrafficLight;
```

### Работа с портами и внешними функциями

```but
type u8  = [bit;8];
type u32 = [bit;32];

extern fn debug(msg: u8);

port LED  : u8  = 0x00100000;
port BTN  : bit = 0x00200000:0;

fn toggle(val: u8) -> u8 {
    return ~val;
}

model ButtonLED {
    var state: u8 = 0;

    start Idle {
        ref Active: BTN = 1;
    }

    state Active {
        enter {
            state = toggle(state);
            debug(state);
        }
        next Idle;
    }
}

start M = ButtonLED;
```

### Композиция моделей

```but
model Init    { start S; }
model Startup { start S; }
model Run     { start S; }
model Halt    { start S; }

start System = (Init + Startup) + (Run | Halt);
```

### Выборочный импорт

```but
import { TrafficLight, u8 } from "common.but";
import "sensors.but" as Sensors;

model Main {
    start Running;
}
```

---

## Тестирование

### Покрытие тестовой базы

| Уровень   | Файлов | Категории                                                                   |
|-----------|--------|-----------------------------------------------------------------------------|
| Лексер    | ~15    | валидные токены, некорректные входы                                         |
| Парсер    | ~30    | синтаксические конструкции, ошибки восстановления                           |
| Семантика | ~80    | вывод типов, области видимости, переходы, импорты, разрешение условий `ref` |

Итого: более **1084 тестов** (lib + integration без LSP) / **1170 тестов** (с флагом `--features lsp`), включая 44 интеграционных теста LSP в `tests/lsp_tests.rs`.

### Стратегия тестирования

- Тесты автоматически обнаруживают `.but`-файлы в директориях `tests/data/`
- Файлы в `valid/` — должны компилироваться без ошибок
- Файлы в `invalid/` — должны производить диагностику

```bash
cargo test
```

---

## Недостатки и проблемные места

### 1. Перегрузка символа `=` в условиях и выражениях

В блоках `cond`/`ref` символ `=` означает **проверку равенства**, а в выражениях — **присваивание**. Это серьёзный
источник ошибок при переходе между контекстами:

```but
cond A = x = 0;    // второй = означает сравнение
always { x = 0; } // здесь = означает присваивание
```

**Предложение:** ввести `==` в контексте `cond`/`ref` (см. задачу С2).

### 2. Отсутствие шаблонов/обобщённых типов

Ключевое слово `template` зарезервировано, но не реализовано. Нет возможности написать параметрические типы или
функции — для встраиваемых систем это ведёт к дублированию кода при работе с разными размерами массивов.

### 3. Отсутствие стандартной библиотеки

Нет встроенного модуля с типовыми алгоритмами, коллекциями или утилитами для встраиваемых систем.

### 4. Предупреждения `clippy` (68 штук)

В кодовой базе накоплено 68 предупреждений clippy, в том числе:
- **21** — `this if statement can be collapsed` (вложенные `if` можно свернуть в `if ... && ...`)
- **13** — `using clone on type Location/u64 which implements Copy` (лишние клоны Copy-типов)
- **3** — `enclosing Ok and ? operator are unneeded` (лишняя обёртка `Ok(x?)`)
- **2** — `redundant field names in struct initialization` (лишние имена полей)

**Предложение:** провести рефакторинг, устранив все предупреждения clippy.

### 5. Встроенные функции: аргументы по-прежнему оборачиваются в скобки

Шаблоны расширения `min`/`max`/`abs`/`clamp` в `generate_function_call` используют
`({arg})` для каждого аргумента, что создаёт лишние скобки для простых выражений:

```c
// Текущий результат: min(value, 100) → (((value) < (100)) ? (value) : (100))
// Ожидаемый:                           (value < 100 ? value : 100)
```

**Предложение:** перейти от строковых шаблонов к прямому вызову `generate_expr` с учётом приоритетов.

---

## Предложения по улучшению

### Открытые синтаксические задачи

#### С2. Единый оператор сравнения в условиях

Ввести `==` в контексте `cond`/`ref` вместо перегрузки `=`:

```but
// Текущее (неоднозначно):
cond A = x = 0;

// Улучшенное:
cond A = x == 0;
```

### Открытые семантические задачи

#### Се7. Стандартная библиотека

Добавить встроенный модуль `std` или `core`:

```but
import std;

fn clamp(val: u8, lo: u8, hi: u8) -> u8;
fn min(a: u8, b: u8) -> u8;
fn max(a: u8, b: u8) -> u8;
fn abs(x: i8) -> u8;
```

---

## Оставшиеся проблемы и открытые задачи

### Известные проблемы

1. **Перегрузка `=` в условиях** — `cond A = x = 0;` неоднозначно; рекомендуется ввести `==` в контексте условий (С2)
2. **Нет шаблонов (generics)** — ключевое слово `template` зарезервировано, но не реализовано; дублирование кода
   при работе с разными размерами массивов неизбежно
3. **Отсутствие стандартной библиотеки** — нет встроенного модуля `std` или `core` с типовыми алгоритмами
   для встраиваемых систем (Ce7)
4. **68 предупреждений clippy** — накопленные предупреждения (22 `collapsible_if`, 13 `.clone()` на Copy-типах `Location`/`u64`, 3 `Ok(x?)` и др.); рефакторинг улучшит читаемость и производительность
5. **Встроенные функции: лишние скобки в аргументах** — шаблоны `min`/`max`/`abs`/`clamp`
   оборачивают аргументы в `({arg})` вместо использования `generate_expr` с приоритетами
6. **`panic!()` в семантическом анализе** — 19 вхождений `panic!()` в production-коде
   (`builtin.rs`, `expression.rs`, `extend.rs`, `function.rs`) — должны быть диагностическими ошибками;
   паника при ошибочных входных данных недопустима в компиляторе
7. **Незащищённые `unwrap()` в LSP и генераторе C** — `lsp.rs:1298`, `c_source.rs:201` и ряд других
   мест могут вызвать панику при неожиданных входных данных вместо возврата диагностики

### Открытые задачи

#### C2. Единый оператор сравнения в условиях

Ввести `==` в контексте `cond`/`ref` вместо перегрузки `=`:

```but
// Текущее (неоднозначно):
cond A = x = 0;

// Улучшенное:
cond A = x == 0;
```

#### Ce7. Стандартная библиотека

Добавить встроенный модуль `std` или `core` с типовыми функциями для встраиваемых систем:

```but
import std;
fn clamp(val: u8, lo: u8, hi: u8) -> u8;
fn min(a: u8, b: u8) -> u8;
fn max(a: u8, b: u8) -> u8;
```

#### Clippy: устранение предупреждений компилятора

Провести рефакторинг для устранения 68 предупреждений `cargo clippy`:
- Свернуть 21 вложенный `if` в `if ... && ...`
- Убрать 13 клонов Copy-типов (`Location`, `u64`, `StateNodeKind`)
- Устранить 3 лишних обёртки `Ok(x?)`
- Прочие мелкие улучшения качества кода

#### Встроенные функции: использование `generate_expr` с приоритетами

Заменить строковые шаблоны для `min`/`max`/`abs`/`clamp` на прямую генерацию
через `generate_expr` с корректными `min_prec`, чтобы устранить лишние `({arg})`:

#### Рефакторинг `panic!()` → диагностические ошибки

В `builtin.rs`, `expression.rs`, `extend.rs`, `function.rs` находится 19 вхождений `panic!()` в ветках
семантического анализа, которые срабатывают при неожиданных входных данных. Каждый `panic!()` должен
быть заменён на `Err(Diagnostic::error(loc, ...))` с передачей в конвейер диагностик вместо
аварийного завершения процесса.

Пример замены:
```rust
// Было (panic):
panic!("unexpected type in function argument");

// Стало (диагностика):
return Err(Diagnostic::error(loc, "Неожиданный тип аргумента функции".to_string()));
```

#### Очистка незащищённых `unwrap()` в LSP и генераторе C

`lsp.rs:1298` и `c_source.rs:201` содержат `unwrap()` на `Weak`-ссылках, которые могут привести к
панике при освобождённом объекте. Необходимо заменить на `.upgrade().ok_or_else(...)` или
`if let Some(...)` с корректной обработкой ошибки / ранним возвратом.

---

### Реализованные улучшения

Все патч-файлы расположены в директории `changes/` в формате `Changes-NN.patch`.

| Патч          | Фича                                                                          | Статус         |
|---------------|-------------------------------------------------------------------------------|----------------|
| Changes-01    | Исправление логических ошибок (4 бага)                                        | ✅ Исправлено  |
| Changes-02    | Исправление дополнительных ошибок компиляции                                  | ✅ Исправлено  |
| Changes-03    | Порядок полей в Parallel + enum state для Concatenation (B4, B5)              | ✅ Исправлено  |
| Changes-04    | Логические ошибки генератора C: typedef, forward decl, enum size (Changes-04) | ✅ Исправлено  |
| Changes-05    | Перенос функций генерации `.c` в `c_source.rs`                                | ✅ Реализовано |
| Changes-06    | Переименование патч-файлов к единому виду Changes-NN                          | ✅ Выполнено   |
| Changes-07    | Перечисления (Ce4) — семантический уровень                                    | ✅ Реализовано |
| Changes-08    | Проверка достижимости и полноты переходов (Ce5)                               | ✅ Реализовано |
| Changes-09    | Расширенный вывод типов (Ce6)                                                 | ✅ Реализовано |
| Changes-10    | Слабые ссылки SA8: `Weak<>` вместо `Rc<>` для обратных ссылок                | ✅ Исправлено  |
| Changes-11    | Блочные комментарии `/* */` в лексере (C3)                                    | ✅ Реализовано |
| Changes-12    | Тернарный оператор: семантика, вывод типов, тесты (C5)                        | ✅ Реализовано |
| Changes-13    | Расширение встроенных функций `min`, `max`, `abs` (C5-1)                      | ✅ Реализовано |
| Changes-14    | LSP hover отображает `///`-документацию (C6)                                  | ✅ Реализовано |
| Changes-15    | Тернарный оператор `? :` в парсере (C11)                                      | ✅ Реализовано |
| Changes-16    | Встроенные функции `min`, `max`, `abs`, `clamp` (NI2)                         | ✅ Реализовано |
| Changes-17    | Структурные типы `struct` (Ce17, Ce18) (NI3)                                  | ✅ Реализовано |
| Changes-18    | Интервальный анализ перекрытия условий (Ce14+) (NI4)                          | ✅ Реализовано |
| Changes-19    | Типобезопасные операции с `enum` (NI6)                                        | ✅ Реализовано |
| Changes-20    | LSP-сервер `but-lsp` (диагностика, дополнение, hover) (PR7)                  | ✅ Реализовано |
| Changes-21    | Безопасное именование файла в `compile_to_c` (V1V2)                           | ✅ Исправлено  |
| Changes-22    | Защита от обхода каталогов в импортах (V3)                                    | ✅ Исправлено  |
| Changes-23    | Ce15: ограничение размера массива `MAX_ARRAY_SIZE = 1024` (V4)                | ✅ Исправлено  |
| Changes-24    | Безопасные методы `try_start`/`try_end`/`try_range` (V5)                      | ✅ Исправлено  |
| Changes-25    | Безопасное разрешение имени модели в C-генераторе (V6)                        | ✅ Исправлено  |
| Changes-26    | Опечатка `"Cnt unrolled"` → `"Can't unroll"` (V7)                            | ✅ Исправлено  |
| Changes-27    | Ce16: обнаружение рекурсивных псевдонимов типов (DFS) (I5)                    | ✅ Реализовано |
| Changes-28    | Флаги `--verbose`/`--quiet` в `butc` (I6)                                    | ✅ Реализовано |
| Changes-29    | `goto_declaration_with_paths`: кросс-файловые переходы (I7)                   | ✅ Реализовано |
| Changes-30    | Индексация `LocalVar` в `enter`/`exit`/`always`-блоках (I8)                  | ✅ Реализовано |
| Changes-31    | Синтаксис `enum` в грамматике (Ce4+) (FE1)                                    | ✅ Реализовано |
| Changes-32    | Вывод типа из пользовательских псевдонимов (FE2)                              | ✅ Реализовано |
| Changes-33    | Диагностика неиспользуемых переменных (Ce13) (FE3)                            | ✅ Реализовано |
| Changes-34    | Проверка детерминированности переходов (Ce14) (FE4)                           | ✅ Реализовано |
| Changes-35    | CLI-компилятор `butc compile --target c` (FE5)                                | ✅ Реализовано |
| Changes-36    | Составные типы `[T;N]` в параметрах функций (FE6)                             | ✅ Реализовано |
| Changes-37    | Структурные типы `struct` — грамматика и семантика                            | ✅ Реализовано |
| Changes-38    | Полная генерация C-кода: `generate_stmt_expression` (43 ветки), `If`/`Loop`/`For` в `generate_code_block`, пример `comprehensive.but` (NI5) | ✅ Реализовано |
| Changes-39    | Исправление LSP-ошибки: параметры функции видны в `Expression`-ветке `statement.rs:100` (`params.clone()` вместо `vec![]`) (A1)            | ✅ Исправлено  |
| Changes-40    | Обновление README: удаление выполненных пунктов, актуализация документации                                                                 | ✅ Выполнено   |
| Changes-41-P01 | Минимизация скобок при генерации C: `expr_precedence`, `generate_expr` с `min_prec`, исправление `if`/`loop`/`return` | ✅ Реализовано |
| Changes-41-P02 | Тестовый файл `no_extra_parens.but` для генератора C                                                                  | ✅ Реализовано |
| Changes-42    | Актуализация README: удаление выполненных подпунктов «Предложения по улучшению»                                       | ✅ Выполнено   |
| Changes-43    | Исправление генерации имён полей вложенных моделей в функциях: `main->entry.field` вместо `main->controller.field`   | ✅ Исправлено  |
| Changes-44-P01 | Семантические координаты в диагностиках: `extract_name(loc)`, `check_import_cycle(loc)`, дублированное имя модели, `ref`-ссылки — `Location::Implicit` заменён реальными координатами (9 случаев) | ✅ Исправлено |
| Changes-44-P02 | LSP: заметки в диагностических сообщениях, `Condition::loc()` в AST, 4 новых интеграционных теста для координат диагностик, `rustfmt` 13 файлов | ✅ Реализовано |
| Changes-45    | Актуализация README: Changes-44 в таблице, новые открытые задачи (panic→diagnostic, unwrap cleanup)                  | ✅ Выполнено   |
| —             | SemanticIndex: поиск узла по LSP-позиции (LSP1)                               | ✅ Реализовано |
| —             | `textDocument/declaration`: переход к декларации (LSP2)                       | ✅ Реализовано |
| —             | `textDocument/documentSymbol`: структура документа (LSP3)                     | ✅ Реализовано |
| —             | `textDocument/semanticTokens/full`: семантическая подсветка (LSP4)            | ✅ Реализовано |
| —             | `ExpressionNode::Initializer` → C-инициализатор `{a, b, c}`                  | ✅ Реализовано |

#### Changes-01. Исправление логических ошибок — **Исправлено**

Четыре логические ошибки, обнаруженные при системном анализе кодовой базы:

**Баг 1 — `implement.rs`: отсутствующий разделитель `_` в именах составных состояний.**

`compact_implement` формировал имена типа `"EntrySequence"` вместо корректного `"Entry_Sequence"`.
В результате:
- состояния внутри составной последовательности получали неправильные имена;
- `test_unroll_implement_expressions` и `test_implement_to_model` падали.

```rust
// Было (неверно):
let prefix_name = format!("{}Sequence", prefix_name);
// Стало:
let prefix_name = format!("{}_Sequence", prefix_name);
```

**Баг 2 — `implement.rs`: паника при компактировании анонимной модели.**

При `import "file.but"` корневой модуль файла хранится как анонимная модель (`name = None`).
Если эта модель использовалась в выражении реализации `start Entry = Worker`, вызов `.unwrap()` на `name` приводил к панике.

```rust
// Было (паника для name = None):
let model_name = format!("{}{}", prefix_name, model.borrow().name.clone().unwrap());
// Стало (анонимная → только префикс):
let base = model.borrow().name.clone().unwrap_or_default();
let model_name = format!("{}{}", prefix_name, base);
```

**Баг 3 — `condition.rs`: лишнее клонирование перед `is_none()`.**

`function.clone().is_none()` создавало ненужный клон объекта только для проверки `None`.
После проверки `function` оставался недвинутым, что было семантически корректно, но запутанно.

```rust
// Было:
let function = if function.clone().is_none() {
// Стало:
let function = if function.is_none() {
```

**Баг 4 — `validate.rs`: асимметрия контекста для `Equal` и `NotEqual` в `validate_cond`.**

Для `ConditionNode::Equal(left, right)` правый операнд проверялся с контекстом левого (`Some(*left.clone())`),
что позволяло валидировать паттерн `S(Model) = ИмяСостояния`. Для `NotEqual` тот же контекст
не передавался, поэтому `S(Model) != НесуществующееСостояние` ошибочно пропускалось.

```rust
// Было:
ConditionNode::NotEqual(left, right) => {
    validate_cond(None, &left, model.clone())?;
    validate_cond(None, &right, model.clone())?;  // контекст теряется
}
// Стало:
ConditionNode::NotEqual(left, right) => {
    validate_cond(None, &left, model.clone())?;
    validate_cond(Some(*left.clone()), &right, model.clone())?;  // симметрично Equal
}
```

**Тесты:**

- Исправлены 2 падавших unit-теста в `semantic/implement.rs`
- Исправлены 3 устаревших интеграционных теста в `tests/semantic_tests.rs`
  (тесты `implement_add_composition_resolves`, `implement_parenthesized_add_resolves` проверяли
  `Implement::Sequence` вместо корректного `Implement::Model` после компактирования)

---

#### Changes-03. Порядок полей в Parallel + enum state для Concatenation — **Исправлено**

Два логических бага в генераторе C-кода (`generator/c/mod.rs`):

**B4 — Неправильный порядок полей в `StateExtend::Parallel`.**

`enum { ... } state;` генерировался первым в struct, тогда как поля данных (`eng0`, `eng1`) должны предшествовать управляющим полям:

```c
// Было (неверно):
struct {
    enum { ELEVATOR_MIDDLE_PARALLEL1_INIT, ... } state;
    ElevatorEngine engine0;
    ElevatorEngine engine1;
} middle_parallel1;

// Стало (верно):
struct {
    ElevatorEngine engine0;
    ElevatorEngine engine1;
    enum { ELEVATOR_MIDDLE_PARALLEL1_INIT, ... } state;
} middle_parallel1;
```

Исправлено в `build_extend_header` и `build_concat_item` (ветка `Parallel`): цикл полей перенесён перед генерацией enum.

**B5 — Отсутствие поля `{state}_state` для `StateExtend::Concatenation`.**

Для составных состояний (`state Mid = Eng + (Eng | Eng) + Eng`) не генерировалось поле для отслеживания активного под-элемента. Добавлена новая функция `build_concat_state_enum`:

```c
// Теперь генерируется:
enum {
    ELEVATOR_MIDDLE_INIT,
    ELEVATOR_MIDDLE_ENGINE0,
    ELEVATOR_MIDDLE_PARALLEL1,
    ELEVATOR_MIDDLE_ENGINE2,
    ELEVATOR_MIDDLE_ENGINE3,
    ELEVATOR_MIDDLE_ENGINE4
} middle_state;
```

Формат вариантов: Model → `{STATE}_{MODEL}{idx}`, Parallel/Concatenation → `{STATE}_PARALLEL{idx}`.

Дополнительно: добавлен параметр `unique_prefix: &str` в `build_parallel_item` для корректной генерации вложенных Parallel-блоков с полем `state`.

**Тесты (3 новых + 2 обновлённых):**

- `test_parallel_extend_numbering_starts_from_zero` — добавлена проверка: `eng0;` расположен перед `} state;` внутри parallel struct
- `test_concatenation_with_parallel_no_gaps` — добавлены проверки: порядок полей в parallel + наличие `mid_state;`
- `test_concatenation_generates_state_enum` — новый: проверяет `mid_state;` с вариантами `TEST_MID_INIT`, `TEST_MID_ENG0`, `TEST_MID_PARALLEL1`, `TEST_MID_ENG2`

**Обновлённые выходные файлы:** `grammar/.output/elevator.h`, `grammar/.output/extend_complex.h`.

---

#### FE1. Грамматический синтаксис для перечислений — **Реализовано**

Добавлена поддержка `enum` в LALRPOP-грамматику и лексер (токен `enum`):

```but
enum Direction { North, South, East, West }
enum Priority { Low = 0, Medium = 5, High = 10 }
```

Реализация: `ast.rs` (EnumDefine, EnumVariant), `lexer.rs` (Token::Enum), `grammar.lalrpop` (правила
EnumDefine/EnumVariant), `tree.rs` (построение EnumNode из AST).

#### FE2. Вывод типа из пользовательских псевдонимов — **Реализовано**

Добавлена функция `ast_type_to_node_ctx` с разрешением псевдонимов через `model.borrow().types`:

```but
type u8 = [bit;8];
fn get() -> u8 { return 0; }
var x = get();  // x: [bit;8] (разрешается через псевдоним u8)
```

#### FE3. Диагностика неиспользуемых переменных (Ce13) — **Реализовано**

Новый модуль `semantic/unused.rs`. Публичный API: `grammar::unused_variable_warnings(model)`.

```
Ce13: переменная 'tmp' объявлена, но нигде не используется
```

Порты и константы не предупреждаются — они составляют внешний интерфейс модели.

#### FE4. Проверка детерминированности переходов (Ce14) — **Реализовано**

Функция `check_nondeterministic_transitions` обнаруживает состояния с несколькими безусловными `ref`-переходами.
Публичный API: `grammar::nondeterministic_transition_warnings(model)`.

#### FE5. Интеграция генератора C в конвейер — **Реализовано**

CLI-компилятор `grammar/src/bin/butc.rs`:

```sh
butc compile --target c input.but -o output_dir
butc compile input.but            # генерирует файлы в каталог output/
butc compile -I /lib/but input.but -o output_dir  # с путями поиска импортов
```

Публичный API в `lib.rs`:
```rust
grammar::compile_to_c(filename, source, output_path, search_paths)
```
где `output_path` — путь к выходному **каталогу** (внутри создаются `.h`/`.c`-файлы).

#### FE6. Составные типы в параметрах функций — **Реализовано**

Добавлено правило `ParameterTypeExpr` в `grammar.lalrpop`, разрешающее запись `[T;N]` в параметрах:

```but
fn process(data: [bit;8]) -> bit { return 0; }
```

#### NI6. Типобезопасные операции с `enum` — **Реализовано**

Добавлены проверка допустимости числовых значений при присваивании переменным типа enum и разрешение имён вариантов как
выражений.
Публичный API: `grammar::enum_type_safety_errors(model)`.

#### PR7. LSP/языковой сервер `but-lsp` — **Реализовано**

Реализован полноценный Language Server Protocol сервер для языка BuT.

```sh
# Сборка LSP-сервера
cargo build --release --features lsp --bin but-lsp

# Запуск (обычно запускается редактором автоматически)
./target/release/but-lsp
```

**Поддерживаемые возможности LSP:**

| Возможность                                | Описание                                                                   |
|--------------------------------------------|----------------------------------------------------------------------------|
| `textDocument/publishDiagnostics`          | Ошибки и предупреждения парсера и семантики                                |
| `textDocument/completion`                  | 35 ключевых слов + идентификаторы из текущего документа                    |
| `textDocument/hover`                       | Тип переменной, сигнатура функции, варианты enum, `///`-документация       |
| `textDocument/declaration`                 | Переход к объявлению переменной, функции, состояния, `ref`-перехода (LSP2) |
| `textDocument/documentSymbol`              | Структура документа: модели, состояния, функции, типы                      |
| `textDocument/semanticTokens/full`         | Семантическая подсветка синтаксиса (10 типов токенов)                      |
| `textDocument/didOpen/didChange/didClose`  | Синхронизация документов (полный текст)                                    |

**Реализация:**

- `grammar/src/lsp.rs` — LSP-утилиты: диагностика, hover, completion, document symbols, semantic tokens, `goto_declaration`
- `grammar/src/bin/but_lsp.rs` — бинарный сервер на базе `lsp-server` + `lsp-types`
- Фича `lsp` в `Cargo.toml`: `cargo build --features lsp`

**Интеграция с Zed:**

```json
// settings.json Zed
{
  "lsp": {
    "but-lsp": {
      "binary": {
        "path": "/path/to/but-lsp"
      }
    }
  }
}
```

**Тесты:**

- 33 интеграционных теста в `tests/lsp_tests.rs`: конвертация позиций, `node_at_position`, hover, `goto_declaration`
- Файл примера: `examples/lsp_demo.but`

#### C5. Тернарный оператор — **Реализовано (частично)**

Семантика и вывод типов для `ConditionalOperator` полностью реализованы: `construct_expression`,
`type_inference`, `validate`, `unused`-анализ — все обрабатывают узел корректно.

**Ограничение:** токен `?` отсутствует в лексере (`lexer.rs`) и грамматике (`grammar.lalrpop`).
Для полной поддержки синтаксиса `cond ? then : else` необходимо:
1. Добавить `Token::Question` и `"?" => Token::Question` в лексер
2. Добавить правило `Precedence13.5` в `grammar.lalrpop` между уровнями приоритета

**Реализация:**

- `semantic/expression.rs`: `construct_expression` обрабатывает `ast::Expression::ConditionalOperator`
- `semantic/type_inference.rs`: поддержка `Expression::ConditionalOperator` (возвращает более широкий тип из ветвей)
- `semantic/validate.rs`, `semantic/unused.rs`: полный обход всех трёх ветвей

**Тесты:** 3 unit-теста в `semantic/expression.rs`, 2 файла примеров.

#### C6. LSP hover с документацией — **Реализовано**

`hover_info` в `lsp.rs` теперь отображает как сигнатуру идентификатора, так и привязанную к нему
`///`-документацию. Изменение: `construct_model` заменено на `construct_model_with_docs`.

**Реализация:**

- Функция `hover_info` вызывает `construct_model_with_docs` для привязки doc-комментариев
- После формирования сигнатуры вызывается `model.borrow().element_doc(word)` и документация
  добавляется к hover-ответу (через `\n\n`)
- Поддерживаются: переменные, функции, типы, `cond`, перечисления, состояния

**Тесты:** 3 unit-теста в `lsp.rs` (`test_hover_function_with_docs`, `test_hover_variable_with_docs`,
`test_hover_variable_without_docs`), 1 файл примера `tests/data/semantic/valid/lsp_hover_docs.but`.

#### LSP2. Переход к декларации (`textDocument/declaration`) — **Реализовано**

LSP-запрос `textDocument/declaration` позволяет редактору перейти к объявлению
идентификатора под курсором. Реализован для всех элементов языка BuT.

**Поддерживаемые виды переходов:**

| Курсор на…                     | Переход к…                                              |
|--------------------------------|---------------------------------------------------------|
| объявлению переменной/константы/порта | тому же месту (декларация = определение)         |
| объявлению функции (`fn`/`extern fn`) | тому же месту                                    |
| объявлению состояния (`state`/`start`/`end`) | тому же месту                             |
| `ref ИмяСостояния`            | строке `state ИмяСостояния;` в исходном тексте          |
| переменной в условии `ref S: x` | строке `var x: ...;` в исходном тексте               |
| функции в условии `ref S: f()` | строке `fn f(...)` в исходном тексте                  |

**Ограничения:**

- Использования переменных внутри тел `enter`/`exit`/`always`-блоков не индексируются
  (позиции использования в statement-узлах теряются при семантическом понижении).
- Кросс-файловые декларации (импортированные модели) не поддерживаются.

**Реализация:**

- `grammar/src/lsp.rs`: `goto_declaration(source, position) -> Option<Range>` — публичная функция;
  `declaration_range_of(node, source)` — разрешение по `SemanticNodeKind`
- `grammar/src/bin/but_lsp.rs`: объявлена capability `declaration_provider`,
  добавлен обработчик `GotoDeclaration::METHOD`

**Тесты:** 7 интеграционных тестов в `tests/lsp_tests.rs` группы `goto_declaration_*`:
самоссылка на декларацию, переход с `ref`-перехода, переход с переменной в условии, None для
пустого файла и позиции вне узлов.
#### C11. Тернарный оператор `? :` в парсере — **Реализовано**

Добавлен синтаксис тернарного оператора `cond ? then : else` в грамматику:

```but
var flag: bit = true;
var result: bit = flag ? true : false;          // простой тернарный
var nested: bit = a ? b ? true : false : false; // вложенный (правоассоциативный)
```

**Реализация:**
- `Token::Question` в `lexer.rs`
- Правила `Precedence14`/`LoopCondP14` в `grammar.lalrpop` с тернарным оператором
- LR(1)-конфликт между `number ":" number` (адресный литерал) и `:` тернарного оператора устранён
  через `Token::AddressLiteral` — лексер испускает `0xNNNN:bit` как единый токен
- Вспомогательная функция `parse_address_literal` в `parser/mod.rs`

**Тесты:** 8 тестов в `parser_tests.rs` (`ternary_simple_parses`, `ternary_nested_right_associative`,
`ternary_in_always_block` и контр-примеры).

#### NI2. Встроенные функции `min`, `max`, `abs`, `clamp` — **Реализовано**

Полный набор математических встроенных функций зарегистрирован в `builtin.rs`:

```but
var lo: bit = min(a, b);           // минимум двух значений
var hi: bit = max(a, b);           // максимум двух значений
var magnitude: bit = abs(a);       // абсолютное значение
var bounded: bit = clamp(a, b, a); // ограничение в диапазон [lo, hi]
```

**Реализация:**
- `TypeNode::BuiltinNumeric` в `type_node.rs` — тип для «любого числового значения»
- 4 записи в `BUILTIN_FUNCTIONS` (PHF) в `semantic/builtin.rs`
- Генератор C (`generator/c/mod.rs`) учитывает новый тип

#### NI3. Структурные типы `struct` — **Реализовано**

Полная поддержка структурных типов с именованными полями:

```but
struct Point { x: [bit;16], y: [bit;16] }
struct Flags { active: bit, ready: bit }

var pos: Point = 0;
var ctrl: Flags = 0;
```

**Реализация:**
- `Token::Struct`, правила `StructDefine`/`StructField` в грамматике
- `ast::StructDefine`, `ast::StructField`, `ast::ModelElement::Struct`, `ast::Type::Struct`
- `StructDefinitionNode` в `semantic/struct_node.rs`
- `TypeNode::Struct(String)` в `type_node.rs`
- `ModelNode::structs: HashMap<String, StructDefinitionNode>`, `search_struct(&str)`
- Генератор C: `struct NazvanieStruct var_name`

**Тесты:** 8 parser-тестов + 6 semantic-тестов.

#### NI4. Интервальный анализ перекрытия условий — **Реализовано**

`check_nondeterministic_transitions` расширена анализом NI4:

```but
start S {
    ref A: x = 3;    // NI4: перекрывается с x < 10 (3 < 10)
    ref B: x < 10;
}

start T {
    ref A: level < 10;   // NI4: перекрывается с level < 20
    ref B: level < 20;
}
```

**Алгоритм:**
1. Структурно идентичные условия → `NI4: ... гарантированное недетерминированное поведение`
2. Простые атомарные условия `var OP constant`: извлекаем `Constraint` (`Eq`, `Lt`, `Le`, `Gt`, `Ge`, `Ne`)
   и проверяем пересечение интервалов функцией `constraints_overlap`

**Реализация:** `semantic/validate.rs` — `Constraint`, `extract_simple_constraint`, `constraints_overlap`,
расширена `check_nondeterministic_model`.

**Тесты:** 5 тестов: дубликат условий, интервальное перекрытие, непересекающиеся условия, `x=3` vs `x<10`.

---

## Анализ проекта: ошибки, уязвимости и улучшения

Приоритеты: 🔴 Высокий · 🟡 Средний · 🟢 Низкий

### Исправленные ошибки

| #  | Приоритет | Местоположение | Описание |
|----|-----------|----------------|----------|
| B1 | 🔴 Высокий | `generator/c/mod.rs:630` | **Баг: `Subtract` генерировал `a[b]` вместо `a - b`**. Оператор вычитания транслировался в C-код как взятие по индексу: вместо `a - b` генерировалось `a[b]`. Исправлено: теперь генерируется `a - b`. |
| B2 | 🟡 Средний | `generator/c/mod.rs:582` | **Баг: `EnumVariant` в `unroll_cond` пропускал `_` между префиксом и именем варианта**. Генерировалось `ENUM_ROBOT_DIRECTIONnorth` вместо `ENUM_ROBOT_DIRECTION_NORTH`. Исправлено. |
| B3 | 🟢 Низкий | `parser/lexer.rs:765` | **Предупреждение: мёртвое начальное значение переменной `last`**. Инициализация `let mut last = self.input.len()` никогда не читалась: либо функция возвращалась раньше, либо значение перезаписывалось. Переписано через `break 'scan` — исключает неопределённость. |
| B4 | 🟡 Средний | `generator/c/mod.rs` | **Баг: `StateExtend::Parallel` генерировал `enum state` перед полями**. В struct параллельного состояния `enum { ... } state;` располагался первым, тогда как по C-конвенции данные должны предшествовать управляющим полям. Исправлено (Changes-03). |
| B5 | 🟡 Средний | `generator/c/mod.rs` | **Баг: `StateExtend::Concatenation` не генерировал поле `{state}_state`**. Для составных состояний отсутствовал enum для отслеживания активного под-элемента (например `middle_state`). Исправлено: добавлен `build_concat_state_enum` (Changes-03). |
| B6 | 🔴 Высокий | `generator/c/c_header.rs` | **Баг: недействительный `typedef struct`**. Генерировалось `typedef struct X { ... };` — синтаксически неверно в C (не создаёт псевдоним). Исправлено: закрывающая скобка теперь `} X;` (Changes-04). |
| B7 | 🟡 Средний | `generator/c/mod.rs` | **Баг: граничные значения при выборе типа для `enum`**. Использовались знаковые границы (`i8::MAX`, `i16::MAX`), вместо беззнаковых (`u8::MAX = 255`, `u16::MAX = 65535`). Значение 200 ошибочно выбирало `uint16_t` вместо `uint8_t`. Исправлено: используются `u8::MAX`, `u16::MAX`, `u32::MAX` (Changes-04). |
| B8 | 🟡 Средний | `generator/c/mod.rs` | **Баг: имя поля enum-переменной**. В ветке `TypeNode::Enum` для имени поля использовалось имя типа (`enum_name`), а не имя переменной (`name`). Генерировалось `uint8_t direction` вместо `uint8_t heading`. Исправлено (Changes-04). |
| B9 | 🟡 Средний | `generator/c/c_header.rs` | **Баг: структуры генерировались без топологической сортировки**. Если подструктура объявлялась после содержащей её родительской структуры, C-компилятор получал ссылку на неопределённый тип. Исправлено: DFS-топосортировка + блок forward declarations (Changes-04). |

### Потенциальные ошибки и уязвимости

| #  | Приоритет | Местоположение | Описание | Статус |
|----|-----------|----------------|----------|--------|
| V1 | 🔴 Высокий | `lib.rs` | **Паника при именовании файла**. `unwrap()` на `file_name()` и `to_str()` паниковали при пустой строке, пути с `/` в конце или непреобразуемом Unicode. | ✅ Исправлено: безопасная цепочка `.and_then()`, запасное имя `"Root"`. |
| V2 | 🔴 Высокий | `lib.rs` | **Паника при разборе имени файла**. `split(".")[0]` паниковал для строк без точки или `"."`. | ✅ Исправлено: `splitn(2, '.').next().unwrap_or(s)`. |
| V3 | 🟡 Средний | `semantic/import.rs` | **Обход каталогов через пути импорта**. `import "../../etc/passwd"` мог выйти за пределы рабочего каталога. | ✅ Исправлено: канонизация + проверка префикса `search_paths`. |
| V4 | 🟡 Средний | `semantic/validate.rs` | **Нет ограничения на размер массива**. `[bit; 9223372036854775807]` допустимо семантически → переполнение в генераторе C. | ✅ Исправлено: Ce15, максимум `MAX_ARRAY_SIZE = 1024`. |
| V5 | 🟡 Средний | `diagnostics.rs` | **Паника в методах `start()`/`end()`/`range()`** для не-файловых `Location` (`Builtin`, `Implicit`, `Codegen`). | ✅ Исправлено: добавлены безопасные методы `try_start()`/`try_end()`/`try_range() -> Option<_>`, инвариант задокументирован. |
| V6 | 🟢 Низкий | `generator/c/mod.rs` | **`unwrap()` при разрешении имени модели** в `get_upper_name` / `get_model_name_struct`. | ✅ Исправлено: `.unwrap_or_else(\|_\| "Unknown".to_string())`. |
| V7 | 🟢 Низкий | `generator/c/mod.rs` | **Опечатка в сообщении об ошибке**: `"Cnt unrolled"` вместо `"Can't unroll"`. | ✅ Исправлено. |

### Улучшения

| #  | Приоритет | Местоположение | Описание | Статус |
|----|-----------|----------------|----------|--------|
| I1 | 🔴 Высокий | `generator/c/c_source.rs` | **Неполная генерация тел функций**. `_tick` не содержит обработки переходов, `enter`/`exit`/`always`-блоков, записи портов. Сгенерированный C-код функционально неполон. | 🔲 Открыто |
| I2 | 🟡 Средний | `generator/c/c_source.rs` | **`todo!()` для вызовов функций** (`ConditionNode::Function`, `ExpressionNode::Function`). | 🔲 Открыто |
| I3 | 🟡 Средний | `generator/c/c_source.rs` | **`todo!()` для `ConditionNode::State`** — `S(Model)` в условиях генерирует панику. | 🔲 Открыто |
| I4 | 🟡 Средний | `generator/c/c_source.rs` | **`todo!()` для `ExpressionNode::Model`** — вложенная модель как значение вызывает панику. | 🔲 Открыто |
| I5 | 🟡 Средний | `semantic/validate.rs` | **Нет проверки рекурсивных псевдонимов типов**. `type A = [A; 8];` вызывал stack overflow. | ✅ Реализовано: Ce16, DFS-обход графа зависимостей псевдонимов. |
| I6 | 🟢 Низкий | `bin/butc.rs` | **Нет флага `--verbose` / `--quiet`**. | ✅ Реализовано: `--verbose` / `-v`, `--quiet` / `-q`; взаимоисключающие. |
| I7 | 🟢 Низкий | `lsp.rs` | **Нет поддержки кросс-файловых переходов `goto_declaration`**. | ✅ Реализовано: `goto_declaration_with_paths()` ищет в файлах импорта. |
| I8 | 🟢 Низкий | `semantic/index.rs` | **Statement-узлы не индексируются для LSP**. | ✅ Реализовано: `SemanticNodeKind::LocalVar`, индексируются `Let`/`Assign` внутри блоков. |

### Текущие проблемы и предложения

Результаты системного анализа проекта (Changes-39+). Приоритеты: 🔴 Высокий · 🟡 Средний · 🟢 Низкий

| ID | Приоритет | Модуль | Проблема | Предложение | Статус |
|----|-----------|--------|----------|-------------|--------|
| A1 | 🔴 Высокий | `semantic/statement.rs:100` | Параметры функции не передавались в `Expression`-ветку — `construct_expression` вызывался с `vec![]` вместо `params.clone()`, из-за чего `value > 100` или `out = value` в теле функции давали LSP-ошибку «идентификатор не найден» | Передавать `params.clone()` (исправлено в Changes-39) | ✅ Исправлено |
| A2 | 🟡 Средний | `semantic/validate.rs` (3130 строк) | Монолитный модуль, трудно поддерживать и тестировать изолированно | Разбить на `validate_refs.rs`, `validate_types.rs`, `validate_determinism.rs` | 🔲 Открыто |
| A3 | 🟡 Средний | `lsp.rs` (~90 KB) | LSP-сервер — монолитный файл; добавление новых capability затруднено | Выделить `lsp/hover.rs`, `lsp/completion.rs`, `lsp/diagnostics.rs` | 🔲 Открыто |
| A4 | 🟡 Средний | `generator/c/c_source.rs` (~58 KB) | Кодогенератор — монолитный файл | Разбить на `c_stmt.rs`, `c_expr.rs`, `c_func.rs` | 🔲 Открыто |
| A5 | 🟢 Низкий | Весь проект | `unwrap()` в критических местах — потенциальные паники в production | Заменить на `?` или `expect("сообщение")` с диагностическим текстом | 🔲 Открыто |
| A6 | 🔲 Открыто | — | C2: перегрузка `=` в условиях (`cond A = x = 0`) неоднозначна | Ввести `==` в грамматику для `cond`/`ref` | 🔲 Открыто |
| A7 | 🔲 Открыто | — | Ce7: нет стандартной библиотеки для встраиваемых систем | Создать модуль `std.but` с `clamp`, `min`, `max`, `abs` | 🔲 Открыто |
