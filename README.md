# BuT — Язык программирования для конечных автоматов

**BuT** (Bus and Transitions) — предметно-ориентированный язык (DSL) для моделирования конечных автоматов и компонентных
систем, реализованный на Rust. Язык ориентирован на описание встраиваемых систем, управление состояниями и аппаратную
абстракцию с доступом к портам ввода-вывода.

---

## Содержание

1. [Архитектура проекта](#архитектура-проекта)
2. [Лексический анализ](#лексический-анализ)
3. [Синтаксис языка](#синтаксис-языка)
4. [Система типов](#система-типов)
5. [Семантика и модель выполнения](#семантика-и-модель-выполнения)
6. [Модульная система](#модульная-система)
7. [Встроенные функции](#встроенные-функции)
8. [Диагностика и ошибки](#диагностика-и-ошибки)
9. [Примеры программ](#примеры-программ)
10. [Тестирование](#тестирование)
11. [Оставшиеся проблемы и открытые задачи](#оставшиеся-проблемы-и-открытые-задачи)
12. [Реализованные улучшения (FE1–FE6, NI6, C5, C6, LSP)](#реализованные-улучшения-fe1fe6-ni6-c5-c6)

---

## Архитектура проекта

### Структура репозитория

```
BuT/
├── grammar/
│   ├── src/
│   │   ├── lib.rs                  — публичный API: parse, compile_to_c, unused/nondeterministic warnings
│   │   ├── diagnostics.rs          — система диагностических сообщений
│   │   ├── lsp.rs                  — LSP-утилиты: диагностика, hover, completion, goto_declaration (фича `lsp`)
│   │   ├── bin/
│   │   │   ├── butc.rs             — CLI-компилятор: butc compile --target c (FE5)
│   │   │   └── but_lsp.rs          — LSP-сервер: обработка запросов, синхронизация документов (PR7)
│   │   ├── generator/
│   │   │   ├── mod.rs              — трейт Generator, enum Language
│   │   │   ├── indent.rs           — утилиты форматирования отступов
│   │   │   └── c/mod.rs            — генератор C-кода
│   │   ├── parser/
│   │   │   ├── mod.rs              — точка входа парсера
│   │   │   ├── lexer.rs            — лексический анализатор (~980 строк)
│   │   │   └── ast.rs              — определения узлов AST (включая EnumDefine, FE1)
│   │   └── semantic/
│   │       ├── mod.rs              — определения семантических узлов (ModelNode, StateNode, ReferenceNode…)
│   │       ├── tree.rs             — построение семантического дерева (7 проходов)
│   │       ├── reference.rs        — разрешение условий `ref`-переходов
│   │       ├── expression.rs       — разрешение выражений
│   │       ├── statement.rs        — разрешение операторов
│   │       ├── type_node.rs        — система типов (TypeNode)
│   │       ├── type_inference.rs   — вывод типов (включая пользовательские псевдонимы, FE2)
│   │       ├── condition.rs        — разрешение именованных условий
│   │       ├── function.rs         — разрешение функций и параметров (включая [T;N], FE6)
│   │       ├── named_block.rs      — разрешение именованных блоков (enter/exit/always)
│   │       ├── import.rs           — импорт файлов и модульная система
│   │       ├── enum_node.rs        — узел перечисления EnumDefinitionNode (Ce4)
│   │       ├── builtin.rs          — реестр встроенных функций
│   │       ├── naming.rs           — утилиты разрешения имён
│   │       ├── unused.rs           — Ce13: диагностика неиспользуемых переменных (FE3)
│   │       ├── docs.rs             — привязка документационных `///`-комментариев
│   │       ├── index.rs            — SemanticIndex: поиск узлов по байтовому смещению (LSP)
│   │       └── validate.rs         — семантическая валидация (Ce14: детерминированность, NI6)
│   └── tests/
│       ├── data/lexer/             — тестовые файлы лексера
│       ├── data/parser/            — тестовые файлы парсера
│       ├── data/semantic/          — тестовые файлы семантики
│       ├── data/lsp/               — тестовые файлы LSP
│       └── lsp_tests.rs            — 33 интеграционных теста LSP (position, node_at_position, hover, goto_declaration)
├── changes/                        — патч-файлы для каждой реализованной фичи
├── examples/                       — примеры программ
├── extensions/                     — расширения IDE (Zed)
└── BuT.ebnf                        — грамматика в формате EBNF
```

### Конвейер компиляции

```
Исходный файл (.but)
        │
        ▼
┌─────────────────┐
│  Лексический    │  lexer.rs
│  анализатор     │  Токены с позиционной информацией
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Синтаксический │  LALRPOP (LR(1) парсер)
│  анализатор     │  Абстрактное синтаксическое дерево (AST)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Семантический  │  7 проходов: скелет → композиция → выражения → условия → блоки → функции → ref
│  анализатор     │  Семантическое дерево (ModelNode/StateNode/...)
└────────┬────────┘
         │
         ▼
   Семантическая модель + Диагностика
```

---

## Лексический анализ

Лексер написан вручную на Rust без использования генераторов. Он поддерживает:

- **Идентификаторы** — Unicode XID + символ `$`: `myVar`, `_x`, `$tmp`
- **Числа**:
    - Целые десятичные: `0`, `123`, `1_000_000`
    - Шестнадцатеричные: `0xFF`, `0x1A_BC`
    - Рациональные (float): `3.14`, `1e10`, `2.5E-3`
    - Адресные литералы: `0x00100000:5` (адрес:бит)
- **Строки**: одинарные и двойные кавычки, префикс `unicode`:
  ```
  "hello"    'world'    unicode"текст"    unicode'текст'
  ```
- **Комментарии**:
    - Однострочный: `// комментарий`
    - Документационный: `/// документация`
    - Блочные комментарии (`/* */`) **не поддерживаются**
- **Ключевые слова**: автоматически распознаются через статическую хеш-таблицу (PHF)

### Таблица ключевых слов

| Группа          | Ключевые слова                                             |
|-----------------|------------------------------------------------------------|
| Объявления      | `var`, `const`, `type`, `fn`, `extern`, `port`, `enum`     |
| Управление      | `if`, `else`, `loop`, `for`, `break`, `continue`, `return` |
| Состояния       | `model`, `state`, `start`, `ref`, `next`                   |
| Прочее          | `import`, `as`, `from`, `cond`, `formula`, `assembly`      |
| Литералы        | `true`, `false`                                            |
| Зарезервированы | `template`, `string`, `pragma`                             |

---

## Синтаксис языка

### Точка входа

Программа состоит из последовательности элементов верхнего уровня:

```
source_unit = { model_element } ;
```

### Импорт

```but
import "shared.but";
import "engine.but" as Motor;
import * as Utils from "lib.but";
import { foo, bar as baz } from "file.but";
```

### Определение типов

```but
type u8  = [bit;8];
type u16 = [bit;16];
type u32 = [bit;32];
type u64 = [bit;64];
```

### Переменные и константы

```but
var x;                   // без типа и значения
var x: u8;               // с явным типом
var x = 100;             // тип выводится из инициализатора
var x: u8 = 50;          // явный тип и значение

const MAX: u8 = 255;     // константа (инициализатор обязателен)
```

### Порты ввода-вывода

```but
port LED: u8   = 0x00100000;      // адрес памяти
port BTN: bit  = 0x00200000:0;    // адрес:номер_бита
```

### Функции

```but
extern fn debug(msg: u8);                        // внешняя функция

fn add(a: u8, b: u8) -> u8 {                    // локальная функция
    return a + b;
}

fn reset();                                       // прототип без тела
```

### Модели и состояния

```but
model TrafficLight {
    var timer: u8 = 0;

    start Red {                         // начальное состояние (ровно одно)
        enter  { timer = 0; }           // выполняется при входе в состояние
        always { timer = timer + 1; }  // выполняется каждый цикл
        exit   { }                      // выполняется при выходе из состояния
        ref Yellow: timer > 10;         // условный переход
    }

    state Yellow {
        next Green;                     // безусловный переход
    }

    state Green {
        ref Red: true;
    }
}
```

### Условия (cond)

```but
cond IsActive     = x = 1;           // = означает равенство, не присваивание!
cond InRange      = x > 0 & x < 100;
cond Either       = IsActive | InRange;
cond NotActive    = !IsActive;
```

### Операторы управления потоком

```but
// if — без скобок вокруг условия
if x > 0 {
    x = x - 1;
} else {
    x = 0;
}

// loop с условием (аналог while) — без скобок
loop x > 0 {
    x = x - 1;
}

// бесконечный loop (без условия)
loop {
    x = x + 1;
    if x >= 10 { break; }
}

// for — без скобок вокруг заголовка
for var i = 0; i < 10; i = i + 1 {
    // ...
}
```

### Выражения — таблица приоритетов

| Уровень | Оператор          | Описание                    | Ассоциативность |
|---------|-------------------|-----------------------------|-----------------|
| 14      | `=`               | присваивание                | правая          |
| 13      | `\|\|`            | логическое ИЛИ              | левая           |
| 12      | `&&`              | логическое И                | левая           |
| 11      | `==` `!=`         | равенство                   | левая           |
| 10      | `<` `>` `<=` `>=` | сравнение                   | левая           |
| 9       | `\|`              | побитовое ИЛИ               | левая           |
| 8       | `^`               | побитовое XOR               | левая           |
| 7       | `&`               | побитовое И                 | левая           |
| 6       | `<<` `>>`         | битовые сдвиги              | левая           |
| 5       | `+` `-`           | сложение, вычитание         | левая           |
| 4       | `*` `/` `%`       | умножение, деление, остаток | левая           |
| 3       | `**`              | возведение в степень        | правая          |
| 2       | `!` `~` `+` `-`   | унарные операторы           | —               |
| 1       | `num:bit`         | адресный литерал            | —               |

### Специальные конструкции выражений

```but
{1, 2, 3}                 // инициализатор массива
arr[5]                    // доступ по индексу
arr[2:6]                  // срез массива
arr[2:]                   // срез от позиции до конца
x.3                       // доступ к биту (member access)
x as u16                  // приведение типа
```

### Именованные блоки

```but
start S {
    enter  { /* выполнится один раз при входе */ }
    exit   { /* выполнится один раз при выходе */ }
    always { /* выполняется каждый цикл */ }
    always { /* оба блока always выполняются */ }
    custom_block { /* пользовательские блоки допустимы */ }
}
```

### Композиция моделей

```but
model Step1 { start S; }
model Step2 { start S; }
model Step3 { start S; }

start Sequential = Step1 + Step2;           // последовательная: Step1, затем Step2
state Parallel   = Step1 | Step2;           // параллельная: оба одновременно
state Combined   = (Step1 + Step2) | Step3; // комбинированная
```

### Формулы и ассемблерные вставки

```but
assembly "arm" {
    // платформо-зависимый код
}

formula "ltl" {
    always(active())
    eventually(done())
}
```

---

## Система типов

### Примитивные типы

| Тип     | Размер     | Значения                  | Описание                                    |
|---------|------------|---------------------------|---------------------------------------------|
| `bit`   | 1 бит      | `0`, `1`, `true`, `false` | Одиночный бит                               |
| `bool`  | 1 бит      | `true`, `false`           | Булев тип                                   |
| `float` | переменный | IEEE                      | Рациональное число                          |
| `unit`  | 0          | —                         | Пустой тип (возвращаемое значение процедур) |

### Составные типы

```but
[bit;8]          // массив из 8 бит (байт)
[u8;16]          // массив из 16 байт
```

### Вывод типов

Компилятор автоматически выводит тип из инициализатора:

| Инициализатор     | Выводимый тип |
|-------------------|---------------|
| `0` … `255`       | `[bit;8]`     |
| `256` … `65535`   | `[bit;16]`    |
| `65536` … `2³²−1` | `[bit;32]`    |
| `2³²` … `2⁶⁴−1`   | `[bit;64]`    |
| `true` / `false`  | `bool`        |
| `3.14`, `1e10`    | `float`       |

### Условия и выражения — разные операторы равенства

Важная особенность: в контексте **условий** (`cond`, `ref`) оператор `=` означает **проверку равенства**, а не
присваивание:

```but
cond IsZero = x = 0;        // x равен 0?
var x = 0;                  // присваивание 0 переменной x
```

В обычных **выражениях** `==` — это сравнение, `=` — присваивание.

---

## Семантика и модель выполнения

### Жизненный цикл конечного автомата

1. **Начальное состояние**: каждая модель с состояниями должна иметь ровно одно `start`-состояние
2. **Вход в состояние**: выполняются все блоки `enter` в порядке объявления
3. **Активное состояние**: выполняются все блоки `always` каждый цикл
4. **Проверка переходов**: вычисляются условия `ref`; при истинности — переход
5. **Выход из состояния**: выполняются все блоки `exit`

### Переходы

```but
ref NextState: condition;   // условный переход (condition — выражение или cond)
next NextState;             // безусловный переход (всегда срабатывает)
```

### Области видимости

- Переменные модели видны во всех вложенных состояниях
- Переменные состояния видны только в нём и вложенных блоках
- Разрешение имён: локальная область → родительская модель → прародительская и т.д.
- Затенение: локальное объявление скрывает родительское

### Семантические проходы анализатора

| Проход                          | Описание                                                                                                                                                                                   |
|---------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 0. Скелет                       | Извлекаются имена моделей/состояний, загружаются импорты                                                                                                                                   |
| 1. Композиция (`implements`)    | Разрешаются составные состояния (`= M1 + M2`, `= M1 \| M2`)                                                                                                                                |
| 2. Выражения переменных         | Разрешаются ссылки на переменные, вывод типов, порты                                                                                                                                       |
| 3. Именованные условия (`cond`) | Разрешаются объявления `cond` → `Condition`                                                                                                                                                |
| 4. Именованные блоки            | Разрешаются блоки `enter`/`exit`/`always`                                                                                                                                                  |
| 5. Функции                      | Разрешаются тела локальных и внешних функций                                                                                                                                               |
| 6. Условия переходов `ref`      | `Condition::Unresolved` заменяется разрешёнными условиями                                                                                                                                  |
| Валидация                       | Семантические проверки: Се1 (один start), bit-значения, Ce13 (неиспользуемые переменные), Ce14 (детерминированность), NI6 (типобезопасность enum), Ce11 (неявный bool), Ce5 (достижимость) |

### Ссылки на родителя в семантических узлах

Большинство семантических узлов (`VariableNode`, `ConditionNode`, `FunctionNode`, `NamedCodeBlock`) содержат поле
`upper: Option<Rc<RefCell<ModelNode>>>` — ссылку на родительскую модель. Это позволяет:

- получить контекст модели изнутри узла без передачи `model` как отдельного параметра;
- обходить объявленные переменные и условия соседних узлов без обратного прохода по дереву.

Сравнение методом `PartialEq` игнорирует поле `upper` (во избежание рекурсии).

---

## Модульная система

### Синтаксис импорта

```but
import "path/to/file.but";                        // весь файл
import "path/to/file.but" as Module;              // с переименованием
import * as Module from "path/to/file.but";       // все экспорты
import { TypeA, FnB as FnAlias } from "file.but"; // выборочный импорт
```

### Правила разрешения имён

- Файлы ищутся по списку путей поиска
- Имя модуля выводится из имени файла (camelCase: `my_file.but` → `MyFile`)
- Повторный импорт одного файла идемпотентен
- Выборочный импорт ищет имя в порядке: модели → типы → переменные → условия

---

## Встроенные функции

В компиляторе зарегистрированы только две встроенные функции:

| Функция | Сигнатура                                | Назначение                            |
|---------|------------------------------------------|---------------------------------------|
| `debug` | `debug(msg: BuiltinString) -> Unit`      | Вывод отладочного сообщения           |
| `S`     | `S(model: BuiltinModel) -> BuiltinState` | Получение начального состояния модели |

Остальные функции требуют явного объявления (`fn` или `extern fn`).

---

## Диагностика и ошибки

### Структура диагностического сообщения

```
Уровень: Debug | Info | Warning | Error
Тип:     ParserError | SyntaxError | DeclarationError |
         TypeError | CastError | SematicError | Warning
Позиция: файл, строка, столбец (начало и конец)
Заметки: дополнительный контекст к ошибке
```

### Категории ошибок

| Категория          | Примеры                                       |
|--------------------|-----------------------------------------------|
| `ParserError`      | Неожиданный токен, неожиданный EOF            |
| `SyntaxError`      | Некорректная конструкция                      |
| `DeclarationError` | Неизвестный идентификатор, дублирование имени |
| `TypeError`        | Несоответствие типов, недопустимое приведение |
| `SematicError`     | Нарушение семантических правил                |
| `Warning`          | Потенциально проблемный код                   |

### Примеры проверок

- Ровно одно `start`-состояние в каждой модели
- Переменная типа `bit` принимает только `0`, `1`, `true`, `false`
- Все использованные переменные/функции/типы должны быть объявлены
- Индексы массивов проверяются при известном типе

---

## Примеры программ

### Минимальный конечный автомат

```but
model Minimal {
    start Idle;
    state Active;
    state Done;
}
```

### Светофор с таймером

```but
type u8 = [bit;8];

model TrafficLight {
    var timer: u8 = 0;

    start Red {
        enter  { timer = 0; }
        always { timer = timer + 1; }
        ref Green: timer = 255;
    }

    state Green {
        enter  { timer = 0; }
        always { timer = timer + 1; }
        ref Yellow: timer = 100;
    }

    state Yellow {
        enter  { timer = 0; }
        always { timer = timer + 1; }
        ref Red: timer = 50;
    }
}

start Entry = TrafficLight;
```

### Работа с портами и внешними функциями

```but
type u8  = [bit;8];
type u32 = [bit;32];

extern fn debug(msg: u8);

port LED  : u8  = 0x00100000;
port BTN  : bit = 0x00200000:0;

fn toggle(val: u8) -> u8 {
    return ~val;
}

model ButtonLED {
    var state: u8 = 0;

    start Idle {
        ref Active: BTN = 1;
    }

    state Active {
        enter {
            state = toggle(state);
            debug(state);
        }
        next Idle;
    }
}

start M = ButtonLED;
```

### Композиция моделей

```but
model Init    { start S; }
model Startup { start S; }
model Run     { start S; }
model Halt    { start S; }

start System = (Init + Startup) + (Run | Halt);
```

### Выборочный импорт

```but
import { TrafficLight, u8 } from "common.but";
import "sensors.but" as Sensors;

model Main {
    start Running;
}
```

---

## Тестирование

### Покрытие тестовой базы

| Уровень   | Файлов | Категории                                                                   |
|-----------|--------|-----------------------------------------------------------------------------|
| Лексер    | ~15    | валидные токены, некорректные входы                                         |
| Парсер    | ~30    | синтаксические конструкции, ошибки восстановления                           |
| Семантика | ~80    | вывод типов, области видимости, переходы, импорты, разрешение условий `ref` |

Итого: более 430 тестов (lib) + 33 интеграционных теста LSP в `tests/lsp_tests.rs` + прочие integration-тесты в `tests/`.

### Стратегия тестирования

- Тесты автоматически обнаруживают `.but`-файлы в директориях `tests/data/`
- Файлы в `valid/` — должны компилироваться без ошибок
- Файлы в `invalid/` — должны производить диагностику

```bash
cargo test
```

---

## Недостатки и проблемные места

### 1. ~~Непоследовательность синтаксиса управляющих конструкций~~ — **Исправлено (С1)**

Реализован вариант A улучшения С1: все управляющие конструкции используют единый стиль без скобок вокруг условия (как в
Rust/Go). `do-while` удалён из языка, добавлен `loop` с опциональным условием:

```but
if x > 0 { }                              // без скобок
loop x > 0 { }                            // loop с условием (аналог while)
loop { break; }                           // бесконечный loop
for var i = 0; i < 10; i = i + 1 { }     // без скобок
```

### 2. Перегрузка символа `=` в условиях и выражениях

В блоках `cond`/`ref` символ `=` означает **проверку равенства**, а в выражениях — **присваивание**. Это серьёзный
источник ошибок при переходе между контекстами:

```but
cond A = x = 0;    // второй = означает сравнение
always { x = 0; } // здесь = означает присваивание
```

### 3. Отсутствие блочных комментариев

Поддерживаются только однострочные `//` и `///`. Блочные комментарии `/* */` недоступны, что затрудняет временное
отключение больших фрагментов кода.

### 4. ~~Переменные запрещены внутри блоков операторов~~ — **Исправлено (С4)**

Реализована полноценная поддержка `var` и `const` внутри блоков операторов (`if`, `loop`, `for`,
именованных блоков `always`/`enter`/`exit`). Переменная видна в последующих операторах того же блока
и скрывается при выходе. Затенение переменных уровня модели поддерживается. `port` внутри блоков
не поддерживается — порты являются аппаратными маппингами и объявляются только на уровне модели.

```but
start S {
    always {
        var local: bit = false;
        local = true;           // OK: local видна в том же блоке
    }
    // local здесь недоступна
}
```

### 5. Минимальный набор встроенных функций

Только `debug` и `S`. Отсутствуют базовые математические функции (`min`, `max`, `abs`), битовые утилиты, работа с
таймерами.

### 6. ~~Отсутствие защиты от циклических импортов~~ — **Исправлено (Се1)**

Реализовано обнаружение циклических импортов. При обнаружении цикла возвращается диагностическая ошибка с полной
цепочкой:

```
Циклический импорт: /src/a.but → /src/b.but → /src/a.but
```

Все три формы импорта (`import "..."`, `import "..." as Name`, `import { A } from "..."`) защищены от циклов.

### 7. ~~Опечатка в имени директории тестов~~ — **Исправлено**

Директория переименована из `tests/data/sematic/` в `tests/data/semantic/`. Все ссылки в тестах
и документации обновлены.

### 8. ~~Активное использование `Rc<RefCell<T>>`~~ — **Частично исправлено (SA8)**

Обратные ссылки на родительскую модель (`upper`) во всех семантических узлах (переменные,
условия, функции, именованные блоки, состояния) преобразованы из `Rc<RefCell<ModelNode>>`
в `Weak<RefCell<ModelNode>>`. Это разрывает все циклы вида `Model → Node.upper → Rc<Model>`
и позволяет дереву корректно освобождаться при удалении корневого `Rc`.

Прямые ссылки (родитель → дочерняя модель через `models: HashMap`) по-прежнему используют
`Rc` — это правильно, поскольку они не образуют циклов. `Rc<RefCell<T>>` в целом остаётся
архитектурной особенностью; полный переход на арены или индексную адресацию возможен в
будущих версиях.

### 9. Отсутствие шаблонов/обобщённых типов

Ключевое слово `template` зарезервировано, но не реализовано. Нет возможности написать параметрические типы или
функции — для встраиваемых систем это ведёт к дублированию кода при работе с разными размерами массивов.

### 10. Нет пользовательских структурных типов

Система типов ограничена массивами фиксированного размера и псевдонимами. Нет аналога `struct` или `enum` с полями — для
встраиваемых систем это существенное ограничение.

### 11. ~~Безусловный переход `next` не проверяется на достижимость~~ — **Исправлено (Се5)**

Функция `transition_completeness_warnings` предупреждает, если состояние содержит `ref`-переходы
совместно с `next` (переходы `ref` в этом случае недостижимы после выполнения `next`).

### 12. ~~Нет проверки полноты переходов~~ — **Исправлено (Се5)**

Функция `transition_completeness_warnings` предупреждает:

- если в модели нет терминальных состояний (автомат не может завершить работу);
- если из конкретного состояния нет пути к терминальному состоянию (BFS по графу переходов).

### 13. ~~Документационные комментарии не включаются в модель~~ — **Исправлено (Се12)**

Реализована привязка `///`-комментариев к семантическому дереву через [`construct_model_with_docs`]. Поля
`ModelNode::doc` и `ModelNode::docs` содержат документацию модели и её элементов.

```but
/// Документация состояния.
start Idle {
    ref Active: btn = 1;
}
```

```rust
let (ast, comments) = parse(src, 0) ?;
let root = construct_model_with_docs( & ast, None, & [], & comments) ?;
root.borrow().element_doc("Idle"); // → ["Документация состояния."]
```

### 14. Отсутствие стандартной библиотеки

Нет встроенного модуля с типовыми алгоритмами, коллекциями или утилитами для встраиваемых систем.

---

## Предложения по улучшению

### Синтаксические улучшения

#### С1. Унификация синтаксиса условий — **Реализовано (вариант A)**

Реализован вариант A: все управляющие конструкции без скобок (стиль Rust/Go). `do-while` удалён, вместо него — `loop` с
опциональным условием.

```but
// Все конструкции — без скобок
if x > 0 { }
loop x > 0 { }        // loop с условием (аналог while)
loop { break; }       // бесконечный loop (аналог do-while с break)
for var i = 0; i < 10; i = i + 1 { }
```

#### С2. Единый оператор сравнения в условиях

Ввести `==` в контексте `cond`/`ref` вместо перегрузки `=`:

```but
// Текущее (неоднозначно):
cond A = x = 0;

// Улучшенное:
cond A = x == 0;
```

#### С4. Переменные внутри блоков операторов — **Реализовано**

```but
start S {
    always {
        var local: bit = false;    // видна только в этом блоке
        local = true;
        for var i: bit = false; i; i = false {
            local = i;             // i видна в условии, шаге и теле for
        }
    }
}
```

**Реализация:**

- Добавлено грамматическое правило `LocalVariableDefine` (без завершающего `";"` и без `port`).
  `SimpleStatement` использует `LocalVariableDefine` вместо `VariableDefine`.
- В семантическом анализе (`semantic/statement.rs`): инициализатор берётся из `def.initializer`
  внутри `VariableDefine`. Локальная переменная регистрируется в `model.variables` после разрешения
  оператора и удаляется при выходе из блока; при затенении исходное значение восстанавливается.
- `for var i: ...` поддерживается: переменная `i` видна в условии, шаге и теле цикла.
- `port` внутри блоков не поддерживается — порты суть аппаратные маппинги модельного уровня.

**Тесты:**

- 5 unit-тестов в `statement.rs`: разрешение, инициализатор, `const`, затенение, выход из области видимости
- 3 интеграционных теста в `semantic_tests.rs`
- 3 файла примеров: `local_var_in_block.but`, `local_var_in_for.but`, `local_var_nested.but`

#### С5. Тернарный оператор — **Реализовано**

```but
var x = condition ? value_true : value_false;
```

**Реализация:**

- AST: `Expression::ConditionalOperator(Location, Box<Expression>, Box<Expression>, Box<Expression>)` — уже существовал в `parser/ast.rs`.
- Семантика: `construct_expression` в `semantic/expression.rs` обрабатывает `ast::Expression::ConditionalOperator`,
  рекурсивно разрешая все три ветви (условие, then, else) в контексте модели.
- Вывод типов: `type_inference.rs` поддерживает `Expression::ConditionalOperator` — возвращает более широкий тип из then/else ветвей.
- Валидация: `validate.rs` обходит все три ветви при проверке семантики.
- **Ограничение**: синтаксис `? :` отсутствует в лексере/грамматике — узел `ConditionalOperator` может создаваться
  только программно (например, через formula-блоки или внутренние преобразования). Для полной поддержки необходимо
  добавить токен `?` в лексер и правило в `grammar.lalrpop`.

**Тесты:**

- 3 unit-теста в `semantic/expression.rs`: создание через `construct_expression`, переменная в условии, контрпример с несуществующим идентификатором
- 2 файла примеров: `tests/data/parser/valid/ternary_operator.but`, `tests/data/semantic/valid/ternary_operator.but`

#### С6.5. Поиск семантических узлов по позиции курсора (LSP) — **Реализовано**

Новый модуль `semantic/index.rs` реализует `SemanticIndex` — индекс семантических узлов,
позволяющий LSP-сервису находить точный узел дерева по координатам курсора (строка, символ)
без неоднозначного поиска по имени идентификатора.

**Принцип работы:**

1. `SemanticIndex::build(model)` обходит семантическое дерево и собирает записи
   `(start_byte, end_byte, SemanticNodeRef)` для всех объявлений.
2. Записи сортируются по `start_byte`.
3. `node_at_offset(offset)` перебирает записи и возвращает наиболее конкретный
   (с наименьшим диапазоном) узел, покрывающий смещение.

**Новые публичные функции в `lsp.rs`:**

- `position_to_offset(source, position)` — конвертирует LSP `Position` (строка + UTF-16
  символ) в байтовое смещение.
- `node_at_position(source, position, model)` — возвращает `SemanticNodeRef` (имя, вид,
  позиция) по LSP-позиции.

**Обновлённый `hover_info`:**

Работает в два этапа:
1. Направленный поиск через `node_at_position` — если курсор на объявлении, точно
   определяет вид элемента (`Variable`/`Const`/`Function`/`State`/`Enum`/…) и делает
   прямой lookup без перебора всех категорий.
2. Резервный поиск по имени — используется для hover на *использовании* элемента
   (а не на его объявлении).

```rust
// Поиск узла по LSP-позиции
use grammar::lsp::{node_at_position, position_to_offset};
use grammar::semantic::index::SemanticNodeKind;

let src = "var counter: bit = false; start S;";
let (ast, _) = parse(src, 0)?;
let model = construct_model(&ast, None, &[])?;

// Позиция (строка 0, символ 4) — 'c' в "counter"
let node = node_at_position(src, Position::new(0, 4), &model);
assert_eq!(node.unwrap().kind, SemanticNodeKind::Variable);
```

**Тесты:**

- 15 unit-тестов в `semantic/index.rs`: переменная, константа, порт, функция, extern fn,
  состояние, тип, условие, перечисление, модель, поиск за пределами, множественные элементы
- 33 интеграционных теста в `tests/lsp_tests.rs`: `position_to_offset`, `node_at_position`,
  hover с направленным поиском, `goto_declaration`
- 3 тестовых файла в `tests/data/lsp/`

#### С6. LSP сервер должен отображать документацию — **Реализовано**

**Реализация:**

- `hover_info` в `lsp.rs` теперь использует `construct_model_with_docs` вместо `construct_model`,
  что привязывает `///`-комментарии к семантическому дереву.
- После формирования сигнатуры функция вызывает `model.borrow().element_doc(word)` и,
  если документация непустая, добавляет её к hover-ответу через пустую строку.
- Формат: сначала блок кода с сигнатурой (` ```but ... ``` `), затем пустая строка, затем текст документации.
- Поддерживаются: переменные, функции, типы, условия (`cond`), перечисления, состояния.

```but
/// Обработка данных
/// data - данные для обработки
/// return - результат обработки
fn process(data: [bit;8]) -> bit { return 0; }
```

При наведении на функцию `process` hover отображает:

````
```but
fn process(data: [bit;8]) -> Bit
```

Обработка данных
data - данные для обработки
return - результат обработки
````

**Тесты:**

- 3 unit-теста в `lsp.rs`: hover с документацией функции, hover с документацией переменной, hover без документации (только сигнатура)
- 1 файл примера: `tests/data/semantic/valid/lsp_hover_docs.but`

---

### Семантические улучшения

#### Се1. Обнаружение циклических импортов — **Реализовано**

Реализована проверка на основе стека обрабатываемых файлов. При обнаружении цикла возвращается ошибка с полной цепочкой:

```
Циклический импорт: /src/a.but → /src/b.but → /src/a.but
```

Поддерживаются все формы импорта: `import "..."`, `import "..." as Name`, `import { A } from "..."`. Линейные цепочки (
без петли) работают корректно.

#### Се4. Перечисления — **Реализовано (семантический уровень)**

Добавлен `EnumNode` в семантическую модель. Перечисления создаются программно через API
(грамматический синтаксис `enum` отсутствует — это отдельная задача парсера):

```rust
use grammar::semantic::EnumNode;

let color = EnumNode::new("Color", & [("Red", None), ("Green", None), ("Blue", None)]);
// Red=0, Green=1, Blue=2 (автоинкремент)

let direction = EnumNode::new("Dir", & [("North", Some(0)), ("South", Some(180))]);
model.enums.insert("Color".to_string(), color);
let found = model.search_enum("Color"); // поиск по цепочке upper
```

**Реализация:**

- `EnumNode { name, variants: Vec<(String, i64)> }` в `grammar/src/semantic/mod.rs`
- `ModelNode::enums: HashMap<String, EnumNode>` — словарь перечислений модели
- `ModelNode::search_enum(name)` — поиск с обходом цепочки `upper`
- `TypeNode::Enum(String)` — тип переменной с именем перечисления
- Генератор C: `TypeNode::Enum` отображается в `uint32_t`

**Тесты:**

- 10 unit-тестов в `semantic_tests.rs`: автоинкремент, явные значения, поиск вариантов,
  `has_variant`, `search_enum`, `PartialEq` с полем `enums`
- 2 файла примеров: `ce4_enum_basic.but` (валидный), `ce4_unknown_variant.but`

#### Се5. Проверка достижимости и полноты переходов — **Реализовано**

Статический анализ:

- Предупреждение если из состояния нет перехода в терминальное состояние (ref или next) при этом состояние само по себе
  не является терминальным
- Предупреждение если в модели нет терминальных состояний (конечных состояний, состояний без переходов)
- Предупреждение о переходах совместно с `next` (недостижимый код)
- Ошибка если есть несколько `next` в одном состоянии (уже проверяется парсером)

```
Предупреждение: в модели нет терминальных состояний; автомат не может завершить работу
Предупреждение: состояние 'A' не имеет пути к терминальному состоянию
Предупреждение: состояние 'A' содержит ref-переходы совместно с next: переходы ref недостижимы после выполнения next
```

**Реализация:**

- Функция `check_transition_completeness(model)` в `grammar/src/semantic/validate.rs`
- Публичный API: `grammar::semantic::tree::transition_completeness_warnings`
- BFS-обход графа переходов для проверки достижимости терминальных состояний
- Рекурсивно обходит вложенные модели

**Тесты:**

- 15 unit-тестов и интеграционных тестов в `semantic_tests.rs`
- 5 файлов примеров: `ce5_terminal_states.but`, `ce5_no_warn_terminal.but` (валидные);
  `ce5_no_terminal.but`, `ce5_double_next.but`, `ce5_next_with_ref.but` (невалидные)

#### Се6. Расширенный двунаправленный вывод типов — **Реализовано**

Тип переменной выводится из возвращаемого типа функции, а не только из литерала инициализатора:

```but
fn getbool() -> bool { return true; }
var result = getbool();   // result: Bool — выводится из типа возврата getbool

fn get32() -> [bit;32] { return 0; }
var val = get32();        // val: [bit;32]
```

**Реализация:**

- Расширение `extract_type` в `grammar/src/semantic/type_inference.rs`
- Поддержка `FunctionNode::Unresolved` (AST-определение читается до разрешения функции)
- Расширение `ast_type_to_node` для псевдонимов встроенных типов (`bool`, `bit`, `float`, `unit`)
- Явно заданный тип не перезаписывается

**Тесты:**

- 7 unit-тестов и интеграционных тестов в `semantic_tests.rs`
- 2 файла примеров: `ce6_type_from_func.but`, `ce6_type_inference_chain.but`

#### Се7. Стандартная библиотека

Добавить встроенный модуль `std` или `core`:

```but
import std;

fn clamp(val: u8, lo: u8, hi: u8) -> u8;
fn min(a: u8, b: u8) -> u8;
fn max(a: u8, b: u8) -> u8;
fn abs(x: i8) -> u8;
```

#### Се11. Строгая проверка булевости условий переходов — **Реализовано**

Функция `implicit_bool_warnings(model)` проверяет все условия переходов (`ref`/`next`) и возвращает предупреждения
`Warning` для случаев, когда числовой тип используется без явного сравнения:

```but
ref Next: timer;          // Предупреждение Се11: числовой тип [bit;8] вместо bool
ref Next: timer != 0;     // Явно и безопасно — без предупреждения
```

**Реализация:**

- Модуль `grammar/src/semantic/validate.rs`
- Публичный API: `grammar::semantic::tree::implicit_bool_warnings`
- Условия `ref`-переходов разрешаются на этапе 6 конвейера (`Condition::Unresolved` → `Condition::*`), поэтому
  проверка Се11 работает прежде всего с разрешёнными условиями (`is_boolean_semantic_condition`), а
  `is_boolean_ast_condition` используется только как запасной путь, если условие не было разрешено
- «Булевыми» считаются: `bool`/`bit`-переменная, результат сравнения (`=`, `!=`, `<`, `>`, `<=`, `>=`), логическое НЕ
  (`!`), булев литерал, именованное условие (`cond`), вызов функции (тип неизвестен на этапе анализа), `Condition::None`
- Числовые условия: переменная типа `[bit;N]`, числовые литералы, арифметика (`+`, `-`), побитовые операции (`&`, `|`),
  элементы массива — порождают `Warning`
- Описание ошибочного выражения генерируется лениво (только при наличии нарушения)
- Рекурсивно обходит вложенные модели

**Тесты:**

- 58 unit-тестов в `validate.rs`: полное покрытие всех ветвей `is_boolean_ast_condition` и `ast_condition_summary`
- 30+ интеграционных тестов в `semantic_tests.rs`, в том числе тесты разрешения условий `ref` и тесты ссылок на
  родительскую модель (`upper`)
- 6 файлов примеров: `implicit_bool_warn.but`, `implicit_bool_numeric.but`, `implicit_bool_arithmetic.but`,
  `implicit_bool_named_cond.but`, `ref_cond_resolved.but` (валидный), `ref_cond_arithmetic.but` (вызывает Се11)

#### Се12. Документация как часть семантической модели — **Реализовано**

`///`-комментарии включены в семантическое дерево через новую функцию `construct_model_with_docs`. Каждый `ModelNode`
содержит:

- `doc: Vec<String>` — документация самой модели
- `docs: HashMap<String, Vec<String>>` — документация именованных элементов (состояний, переменных, функций, типов,
  условий)
- `own_doc()` / `element_doc(name)` — методы для удобного доступа

Алгоритм привязки основан на позициях в исходном тексте: `///`-комментарий достаётся тому элементу, ближайшим следующим
для которого он является. Поддерживаются многострочные блоки и вложенные модели.
---

## Оставшиеся проблемы и открытые задачи

### Известные проблемы

1. **Перегрузка `=` в условиях** — `cond A = x = 0;` неоднозначно; рекомендуется ввести `==` в контексте условий
2. **Отсутствие блочных комментариев** — `/* */` не поддерживаются; затруднено временное отключение блоков кода
3. **Минимальный набор встроенных функций** — только `debug` и `S`; отсутствуют `min`, `max`, `abs`, таймерные утилиты
4. **Нет шаблонов (generics)** — ключевое слово `template` зарезервировано, но не реализовано; дублирование кода
   неизбежно
5. **Нет структурных типов** — нет `struct`; невозможно сгруппировать связанные поля
6. **Отсутствие стандартной библиотеки** — нет встроенного модуля `std` или `core` (имеющийся `std.but` — тестовый)
7. ~~**Нет LSP/языкового сервера**~~ — **Исправлено (PR7)**: реализован `but-lsp` с поддержкой диагностики,
   автодополнения и hover
8. **Ce14 (FE4) проверяет только безусловные переходы** — пересечение условий (`a > 5` и `a > 3`) семантически не
   анализируется
9. **Генератор C неполон** — нет генерации тела функций перехода, обработчиков `enter`/`exit`/`always`
10. ~~**`enum` в системе типов ограничен**~~ — **Исправлено (NI6)**: добавлена проверка принадлежности числовых значений
    к вариантам перечисления; добавлено разрешение имён вариантов (например, `North`) в выражениях
11. **Тернарный оператор `? :` без парсерного синтаксиса** (C5 частично) — узел `ConditionalOperator` существует
    в AST и обрабатывается семантикой, но токен `?` отсутствует в лексере, поэтому оператор не может использоваться
    в BuT-коде напрямую. Требуется добавить `Token::Question` в `lexer.rs` и правило `Precedence` в `grammar.lalrpop`.

### Открытые задачи

#### NI1. Блочные комментарии

Добавить поддержку `/* */` и `/** */` в лексер:

```but
/* Временно отключённый блок кода */
var x = 42;
```

#### NI2. Расширение встроенных функций

Добавить в `builtin.rs`: `min(a, b)`, `max(a, b)`, `abs(x)`, `clamp(x, lo, hi)`.

#### NI3. Структурные типы

Ввести `struct` для группировки полей:

```but
struct Point { x: [bit;16], y: [bit;16] }
var p: Point = Point { x: 10, y: 20 };
```

#### NI4. Улучшение Ce14 — анализ перекрытия условий

Расширить `check_nondeterministic_transitions` для обнаружения пересечений условий на основе символьного анализа
интервалов.

#### NI5. Полная генерация C-кода

Завершить `generator/c/mod.rs`: генерация тел функций, FSM-диспетчера (`transition()`), обработчиков именованных
блоков (`on_enter`, `on_exit`, `always`).

#### ~~NI6. Типобезопасные операции с `enum`~~ — **Реализовано**

Реализована проверка: при присваивании переменной типа `Direction` числовым литералом, значение должно быть одним из
вариантов перечисления. Добавлено разрешение имён вариантов (например, `North`) как выражений.

```but
enum Direction { North, South, East, West }
var dir: Direction = 0;      // OK: 0 — значение варианта North
var dir2: Direction = North; // OK: вариант North разрешается в 0
var dir3: Direction = 99;    // NI6-ошибка: 99 не является вариантом Direction
```

**Реализация:**

- `ModelNode::search_enum_variant(variant_name)` в `semantic/mod.rs` — поиск варианта по имени среди всех доступных
  перечислений
- Разрешение идентификаторов-вариантов в `semantic/expression.rs` (шаг 4: имя → `Expression::Number(value)`)
- `validate_enum_values(model)` — проверка инициализаторов переменных типа enum в `validate_model`
- `check_enum_type_safety(model)` — расширенная проверка инициализаторов и присваиваний в именованных блоках (`always`,
  `enter`, `exit`)
- Публичный API: `grammar::enum_type_safety_errors(model)`

**Тесты:**

- 6 unit-тестов в `validate.rs`: корректная инициализация, некорректное значение, явные значения, несколько ошибок,
  не-enum тип, неизвестный тип
- 2 файла примеров: `ni6_enum_valid_assign.but` (валидный), `ni6_enum_invalid_assign.but` (невалидный)

---

### Реализованные улучшения (FE1–FE6, NI6, C5, C6, LSP)

| #   | Фича                                                        | Патч      | Статус        |
|-----|-------------------------------------------------------------|-----------|---------------|
| FE1 | Синтаксис `enum` в грамматике (Ce4+)                        | FE1.patch | ✅ Реализовано |
| FE2 | Вывод типа из пользовательских псевдонимов                  | FE2.patch | ✅ Реализовано |
| FE3 | Диагностика неиспользуемых переменных (Ce13)                | FE3.patch | ✅ Реализовано |
| FE4 | Проверка детерминированности переходов (Ce14)               | FE4.patch | ✅ Реализовано |
| FE5 | CLI-компилятор `butc compile --target c`                    | FE5.patch | ✅ Реализовано |
| FE6 | Составные типы `[T;N]` в параметрах функций                 | FE6.patch | ✅ Реализовано |
| NI6 | Типобезопасные операции с `enum`                            | NI6.patch | ✅ Реализовано |
| PR7 | LSP-сервер `but-lsp` (диагностика, дополнение, hover)       | PR7.patch | ✅ Реализовано |
| C5  | Тернарный оператор (семантика, вывод типов, тесты)          | C5.patch  | ✅ Реализовано (частично: без парсерного синтаксиса `? :`) |
| C6  | LSP hover отображает `///`-документацию                     | C6.patch  | ✅ Реализовано |
| LSP1 | SemanticIndex: поиск узла по LSP-позиции (строка, символ) | —         | ✅ Реализовано |
| LSP2 | `textDocument/declaration`: переход к декларации элемента  | —         | ✅ Реализовано |
| LSP3 | `textDocument/documentSymbol`: структура документа         | —         | ✅ Реализовано |
| LSP4 | `textDocument/semanticTokens/full`: семантическая подсветка | —         | ✅ Реализовано |

#### FE1. Грамматический синтаксис для перечислений — **Реализовано**

Добавлена поддержка `enum` в LALRPOP-грамматику и лексер (токен `enum`):

```but
enum Direction { North, South, East, West }
enum Priority { Low = 0, Medium = 5, High = 10 }
```

Реализация: `ast.rs` (EnumDefine, EnumVariant), `lexer.rs` (Token::Enum), `grammar.lalrpop` (правила
EnumDefine/EnumVariant), `tree.rs` (построение EnumNode из AST).

#### FE2. Вывод типа из пользовательских псевдонимов — **Реализовано**

Добавлена функция `ast_type_to_node_ctx` с разрешением псевдонимов через `model.borrow().types`:

```but
type u8 = [bit;8];
fn get() -> u8 { return 0; }
var x = get();  // x: [bit;8] (разрешается через псевдоним u8)
```

#### FE3. Диагностика неиспользуемых переменных (Ce13) — **Реализовано**

Новый модуль `semantic/unused.rs`. Публичный API: `grammar::unused_variable_warnings(model)`.

```
Ce13: переменная 'tmp' объявлена, но нигде не используется
```

Порты и константы не предупреждаются — они составляют внешний интерфейс модели.

#### FE4. Проверка детерминированности переходов (Ce14) — **Реализовано**

Функция `check_nondeterministic_transitions` обнаруживает состояния с несколькими безусловными `ref`-переходами.
Публичный API: `grammar::nondeterministic_transition_warnings(model)`.

#### FE5. Интеграция генератора C в конвейер — **Реализовано**

CLI-компилятор `grammar/src/bin/butc.rs`:

```sh
butc compile --target c input.but -o output.c
butc compile input.but   # выводит в output.c
```

Публичный API в `lib.rs`: `grammar::compile_to_c(source, output_path)`.

#### FE6. Составные типы в параметрах функций — **Реализовано**

Добавлено правило `ParameterTypeExpr` в `grammar.lalrpop`, разрешающее запись `[T;N]` в параметрах:

```but
fn process(data: [bit;8]) -> bit { return 0; }
```

#### NI6. Типобезопасные операции с `enum` — **Реализовано**

Добавлены проверка допустимости числовых значений при присваивании переменным типа enum и разрешение имён вариантов как
выражений.
Публичный API: `grammar::enum_type_safety_errors(model)`.

#### PR7. LSP/языковой сервер `but-lsp` — **Реализовано**

Реализован полноценный Language Server Protocol сервер для языка BuT.

```sh
# Сборка LSP-сервера
cargo build --release --features lsp --bin but-lsp

# Запуск (обычно запускается редактором автоматически)
./target/release/but-lsp
```

**Поддерживаемые возможности LSP:**

| Возможность                                | Описание                                                                   |
|--------------------------------------------|----------------------------------------------------------------------------|
| `textDocument/publishDiagnostics`          | Ошибки и предупреждения парсера и семантики                                |
| `textDocument/completion`                  | 35 ключевых слов + идентификаторы из текущего документа                    |
| `textDocument/hover`                       | Тип переменной, сигнатура функции, варианты enum, `///`-документация       |
| `textDocument/declaration`                 | Переход к объявлению переменной, функции, состояния, `ref`-перехода (LSP2) |
| `textDocument/documentSymbol`              | Структура документа: модели, состояния, функции, типы                      |
| `textDocument/semanticTokens/full`         | Семантическая подсветка синтаксиса (10 типов токенов)                      |
| `textDocument/didOpen/didChange/didClose`  | Синхронизация документов (полный текст)                                    |

**Реализация:**

- `grammar/src/lsp.rs` — LSP-утилиты: диагностика, hover, completion, document symbols, semantic tokens, `goto_declaration`
- `grammar/src/bin/but_lsp.rs` — бинарный сервер на базе `lsp-server` + `lsp-types`
- Фича `lsp` в `Cargo.toml`: `cargo build --features lsp`

**Интеграция с Zed:**

```json
// settings.json Zed
{
  "lsp": {
    "but-lsp": {
      "binary": {
        "path": "/path/to/but-lsp"
      }
    }
  }
}
```

**Тесты:**

- 33 интеграционных теста в `tests/lsp_tests.rs`: конвертация позиций, `node_at_position`, hover, `goto_declaration`
- Файл примера: `examples/lsp_demo.but`

#### C5. Тернарный оператор — **Реализовано (частично)**

Семантика и вывод типов для `ConditionalOperator` полностью реализованы: `construct_expression`,
`type_inference`, `validate`, `unused`-анализ — все обрабатывают узел корректно.

**Ограничение:** токен `?` отсутствует в лексере (`lexer.rs`) и грамматике (`grammar.lalrpop`).
Для полной поддержки синтаксиса `cond ? then : else` необходимо:
1. Добавить `Token::Question` и `"?" => Token::Question` в лексер
2. Добавить правило `Precedence13.5` в `grammar.lalrpop` между уровнями приоритета

**Реализация:**

- `semantic/expression.rs`: `construct_expression` обрабатывает `ast::Expression::ConditionalOperator`
- `semantic/type_inference.rs`: поддержка `Expression::ConditionalOperator` (возвращает более широкий тип из ветвей)
- `semantic/validate.rs`, `semantic/unused.rs`: полный обход всех трёх ветвей

**Тесты:** 3 unit-теста в `semantic/expression.rs`, 2 файла примеров.

#### C6. LSP hover с документацией — **Реализовано**

`hover_info` в `lsp.rs` теперь отображает как сигнатуру идентификатора, так и привязанную к нему
`///`-документацию. Изменение: `construct_model` заменено на `construct_model_with_docs`.

**Реализация:**

- Функция `hover_info` вызывает `construct_model_with_docs` для привязки doc-комментариев
- После формирования сигнатуры вызывается `model.borrow().element_doc(word)` и документация
  добавляется к hover-ответу (через `\n\n`)
- Поддерживаются: переменные, функции, типы, `cond`, перечисления, состояния

**Тесты:** 3 unit-теста в `lsp.rs` (`test_hover_function_with_docs`, `test_hover_variable_with_docs`,
`test_hover_variable_without_docs`), 1 файл примера `tests/data/semantic/valid/lsp_hover_docs.but`.

#### LSP2. Переход к декларации (`textDocument/declaration`) — **Реализовано**

LSP-запрос `textDocument/declaration` позволяет редактору перейти к объявлению
идентификатора под курсором. Реализован для всех элементов языка BuT.

**Поддерживаемые виды переходов:**

| Курсор на…                     | Переход к…                                              |
|--------------------------------|---------------------------------------------------------|
| объявлению переменной/константы/порта | тому же месту (декларация = определение)         |
| объявлению функции (`fn`/`extern fn`) | тому же месту                                    |
| объявлению состояния (`state`/`start`/`end`) | тому же месту                             |
| `ref ИмяСостояния`            | строке `state ИмяСостояния;` в исходном тексте          |
| переменной в условии `ref S: x` | строке `var x: ...;` в исходном тексте               |
| функции в условии `ref S: f()` | строке `fn f(...)` в исходном тексте                  |

**Ограничения:**

- Использования переменных внутри тел `enter`/`exit`/`always`-блоков не индексируются
  (позиции использования в statement-узлах теряются при семантическом понижении).
- Кросс-файловые декларации (импортированные модели) не поддерживаются.

**Реализация:**

- `grammar/src/lsp.rs`: `goto_declaration(source, position) -> Option<Range>` — публичная функция;
  `declaration_range_of(node, source)` — разрешение по `SemanticNodeKind`
- `grammar/src/bin/but_lsp.rs`: объявлена capability `declaration_provider`,
  добавлен обработчик `GotoDeclaration::METHOD`

**Тесты:** 7 интеграционных тестов в `tests/lsp_tests.rs` группы `goto_declaration_*`:
самоссылка на декларацию, переход с `ref`-перехода, переход с переменной в условии, None для
пустого файла и позиции вне узлов.