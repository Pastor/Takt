# FEATURES.md — витрина незакрытых фич

> Витрина **только незакрытых** фич (правила 10, 17, 21 в
> [docs/RULE.md](docs/RULE.md)). Информация о закрытых (`ГОТОВО`) и отменённых
> (`ОТМЕНА`) фичах здесь **не хранится** — она живёт в карточке фичи
> `docs/features/XXXX-*.md` (раздел «Итог (что сделано)» либо «Итог (отмена)»).
> Реестр всех карточек — [docs/features/README.md](docs/features/README.md).
>
> Файл содержит ровно **три блока**: (1) таблица последовательности разработки,
> (2) кандидаты в фичи, (3) процессный бэклог.

## 1. Последовательность разработки

Незакрытые фичи в порядке взятия в работу (правило 19: зависимости →
проработанность → приоритет → необходимость). Порядок, зависимости и статусы
пересматривает аналитик при каждом изменении состава/зависимостей фич.

Статусы: `СОЗДАНА`, `АРХИТЕКТУРА`, `АНАЛИЗ`, `РАЗРАБОТКА`, `ТЕСТИРОВАНИЕ`,
`ИСПРАВЛЕНИЕ`, а также особое состояние `ЗАБЛОКИРОВАНА` (пока не закрыта хотя бы
одна зависимость). Полностью выполненная (`ГОТОВО`) или отменённая (`ОТМЕНА`)
фича из таблицы удаляется.

Основная часть таблицы проработана до `РАЗРАБОТКА` (ADR + анализ + тест-план +
подзадачи разработки), поэтому критерий 2 правила 19 эти фичи не различает —
порядок задают зависимости и приоритет. Tier указан в карточке каждой фичи.

Положение **0046** (конец таблицы, `ЗАБЛОКИРОВАНА` зависимостью 0036) задано
критерием 4 в обход критерия 2. Обоснование — ниже.

> Фича **0048** «Детерминированная генерация кода» **закрыта** (`ГОТОВО`,
> 2026-07-16) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0048-deterministic-codegen.md), раздел «Итог (что
> сделано)». Она разблокировала codegen-снапшоты (требование 0026 и T1–T4
> тест-плана 0041) и дала детерминизм любой будущей цели (0045).

> Фича **0040** (полноценный PSI-парсер плагина IntelliJ) **отменена**
> 2026-07-15 решением заказчика и удалена из таблицы (правило 10) — причина и
> что делать вместо в её карточке, раздел
> [«Итог (отмена)»](docs/features/0040-intellij-psi-parser.md). Ни одна фича от
> неё не зависела, пересмотр зависимостей не потребовался.

| № | Фича | Зависит от | Tier | Статус |
|---|------|------------|------|--------|
| [0030](docs/features/0030-comprehensive-example-fix.md) | Исправление примера `comprehensive.lam` (недостижимый сценарий) | — | 2 | РАЗРАБОТКА |
| [0042](docs/features/0042-address-defines.md) | Инъекция define'ов для адресов (`--define`) | — | 2 | РАЗРАБОТКА |
| [0027](docs/features/0027-module-size-split.md) | Разделение переросших модулей (`validate.rs`, `lsp.rs`, `c_expr.rs`) | — | 2 | РАЗРАБОТКА |
| [0037](docs/features/0037-windows-test-failures.md) | Сбои тестов на Windows (пути include, ресурс viewport) | — | 2 | РАЗРАБОТКА |
| [0038](docs/features/0038-intellij-semantic-tokens.md) | Семантическая подсветка Lam в IntelliJ через `lam-lsp` | — | 2 | РАЗРАБОТКА |
| [0039](docs/features/0039-intellij-reformat.md) | Действие «Reformat Code» в плагине IntelliJ | — | 3 | РАЗРАБОТКА |
| [0043](docs/features/0043-address-map-export.md) | Экспорт карты адресов во внешний формат | — | 3 | РАЗРАБОТКА |
| [0034](docs/features/0034-sim-struct-types.md) | Структурные типы в симуляторе | — | 3 | РАЗРАБОТКА |
| [0036](docs/features/0036-sim-visibility.md) | Согласование видимости публичного API крейта `simulation` | — | 3 | РАЗРАБОТКА |
| [0046](docs/features/0046-build-warnings-cleanup.md) | Устранение всех предупреждений сборки (rustc + clippy) | 0036 | — | СОЗДАНА · **ЗАБЛОКИРОВАНА** |

### Обоснование порядка (правило 19)

> Фичи **0048**, **0033**, **0032**, **0031**, **0044** и **0035** **закрыты**
> (`ГОТОВО`, 2026-07-16) и удалены из таблицы (правило 10). 0033 задала контракт
> «вход не стоит такта» для будущих бэкендов (0041, 0045); 0032 свела значения
> симулятора к единому хранилищу (`ModelNodeContext`); 0031 разрешила композицию
> функций `f→g` (рекурсия — `SE-053`); 0044 ввела `invariant` и оживила
> Guard-формулы в симуляторе (`SIM-025`), сделав имя инварианта **атомом LTL**;
> 0035 сняла тихую потерю LTL в блоках (`SE-055`/`SE-056` через `ltl_warnings`).
> Итоги — в карточках фич.

- **0030 возглавляет очередь** — Tier 2; исправление примера `comprehensive.lam`.
  Прежде очередь возглавляли два бэкенда, **оба закрыты 2026-07-16**: 0045
  (SystemVerilog — оба гейта зелёные, сдвиг такта = 0 подтверждён потактовой
  сверкой) и 0050 (Rust `no_std` — весь корпус транслируется, при приёмке
  заведена недостававшая потактовая сверка). Итоги — в карточках
  [0045](docs/features/0045-sv-backend.md), [0050](docs/features/0050-rust-backend.md).
  ⚠️ 0030 поднялась не только по очереди: гейт цели `rust` **громко пропускает**
  `comprehensive.lam` (clippy прав: `temperature <= 0` при `u8` может значить
  только `== 0`), то есть дефект примера теперь виден каждому прогону предкоммита.
- **0038 → 0039.** 0039 формально не заблокирована — её ADR выбрал независимый
  путь (`lamc fmt --stdin`), — но оба механизма регистрируются через один EP
  `formattingService`, поэтому порядок обязателен. (Третья фича этой группы,
  0040, отменена — см. врезку выше.)
- **0045 и 0050 шли подряд** (обе — бэкенды, Tier 2, зависимостей нет).
  Соседство было выбрано по критерию 4 (необходимость) и по пересечению правок: обе
  добавляли вариант в `#[non_exhaustive] enum Language` и ветку в `generate()` —
  конфликт слияния там тривиален, но делать их подряд дешевле, чем вперемешку с
  чужими фичами. **0050 — наименее рискованная фича очереди:** её гейт
  (`rustc` + `clippy`) уже является зависимостью проекта, тогда как у 0041 арбитр
  `iec2c` собирается из исходников, а у 0045 требует `brew install verilator
  yosys`. Порядок между 0045 и 0050 при необходимости можно поменять: они
  независимы.
- **0045 каркаса не ждёт** — общий слой бэкендов **не заводится**: `Language` уже
  `#[non_exhaustive]`, трейт `Generator` из одного метода, `minimap::Map` и
  `Printer` общие. Закрытая 0041 это подтвердила на практике: `generator/st/`
  написан на существующем каркасе, не обнажив его дефицита, — `generator/sv/`
  пойдёт тем же путём. Ценный задел для 0045: `scripts/ensure-iec2c.sh` и форма
  гейта «внешний арбитр в `precheck.sh`» переносятся почти дословно.
- **0036 → 0046.** Широкая чистка предупреждений (~125) исключает
  `private_interfaces` (19 из них) — им занимается 0036, уже с принятым ADR.
  Прямой путь там оказался невозможен (`E0449`), а выбранная инкапсуляция ломает
  публичный API крейта; в общей чистке такое решение прошло бы мимоходом и без
  обоснования. Плюс 0027 → 0046: 0027 перекладывает `c_expr.rs` и `lsp.rs`, где
  живут крупнейшие группы clippy-долга.
- **0027 в середине, а не в начале.** Аналитик 0027 просил поднять её выше
  (перекладывает три файла целиком → конфликты слияния), но проверка показала,
  что пересечения с очередью нет: 0028 правит `c_model.rs`, 0029 —
  `generator/c/mod.rs`, 0035 — `semantic/statement.rs`, а 0027 делит
  `validate.rs`/`lsp.rs`/`c_expr.rs`. Конфликт возник бы только с работой по LSP
  (0038), которая идёт позже.

### Решения заказчика (2026-07-15)

Проработка вскрыла три развилки, которые аналитик решать не вправе. Все три
закрыты:

1. **Версия языка накапливается.** Пять фич (0029, 0031, 0033, 0042, 0044)
   независимо заявили `0.2.0 → 0.3.0`, что дало бы `0.7.0` без единого выпуска.
   Решено: подъём **один** — первая закрытая фича поднимает версию, остальные
   вносят изменения в уже поднятую. Зафиксировано в **правиле 22**
   ([docs/RULE.md](docs/RULE.md)). Обязанность каждой фичи обосновать в анализе,
   меняет ли она язык, сохраняется — накопление касается только номера.
   ⚠️ Исполнение решения затруднено тем, что константы `LANGUAGE_VERSION` в коде
   **нет** (версия живёт только в `README.md`) — кандидат заведён ниже.
   **Исполнено:** подъём `0.2.0` → `0.3.0` сделала закрытая **0029** как первая
   из пяти; фичи 0031, 0033, 0042, 0044 вносят изменения в **уже поднятую**
   версию и заново её не поднимают. Коммит подъёма помечен тегом **`v0.3.0`** —
   требование тега добавлено в правило 22 решением заказчика от 2026-07-15.
2. **0040 — `ОТМЕНА`.** Фича поглощается путём LSP4IJ (0038): четыре
   возможности из шести достаются оттуда несоизмеримо дешевле, а цена
   собственного PSI — вторая реализация грамматики рядом с LALRPOP. Удалена из
   таблицы; остаточная ценность (rename, `PsiReference` для `import`) **не
   потеряна** — вынесена кандидатом ниже.
3. **0029: `--float-width=64`, то есть 8 байт.** Совпадение с f64 симулятора
   важнее неизменности `c-hal`; ширина вещественного порта `c-hal` меняется с 4
   на 8 байт — это **ожидаемый** результат, а не регресс. Опция
   `--float-width=32` остаётся для платформ, где 8-байтное чтение недопустимо.

## 2. Кандидаты в фичи

Идеи, ещё не оформленные в карточку `docs/features/XXXX-*.md`. Прежний состав
блока (19 кандидатов) переведён в фичи 0026–0044 и живёт в таблице выше.
Ниже — находки, вскрытые **при этой проработке**: каждая подтверждена пробой или
чтением кода, и ни одна не входит в объём заведённых фич.

- **Общие переменные корня передаются под-модели по одной** (вскрыто закрытой
  0050). У `MovementController` (`stacker.lam`) их девять, поэтому такт получает
  10 параметров и упирается в порог `clippy::too_many_arguments` (7). Арность
  диктует **модель**, а не стиль, поэтому линт заглушён точечно на самом методе.
  Системное лечение — свернуть общие переменные в структуру `Shared` и
  передавать `&mut Shared`.

- **Расчёт ширины перечисления не вынесен в общий слой** (выявлено при закрытой
  0045 — **третий бэкенд подряд**, споткнувшийся об одно и то же). ST брал
  `USINT`, Rust — `#[repr(u8)]`, SV — `⌈log₂(вариантов)⌉`; все три предположения
  опровергнуты одним и тем же `enum Action { Idle = 670 }` из `elevator.lam:121`.
  Верный расчёт — **по диапазону значений** — сегодня реализован в каждом
  бэкенде отдельно (`rust_type::enum_repr`, `sv_type::enum_width`). Кандидат:
  вынести в `semantic/`, чтобы четвёртый бэкенд не наступил на то же.

- **Fixed-point Q(m.n) как тип языка** (вынесено закрытой 0045, ADR отверг
  вариант R-B). Цель `sv` запрещает `float` (`SV-003`): в синтезируемом RTL
  плавающей точки нет. Fixed-point дал бы вещественную арифметику в аппаратуре,
  но **язык формата не выражает**, и генератор был бы вынужден угадать (Q16.16),
  из-за чего сверка с симулятором (f64) проваливалась бы by design. То есть это
  расширение **языка**, а не бэкенда, — отдельная фича.

- **Цель `sv-mmio`: адреса портов → регистровый файл на шине** (вынесено закрытой
  0045, ADR вопрос 5, Option B). Сегодня карта адресов целью `sv` не
  потребляется: MMIO-адрес для RTL бессмыслен. Но осмысленна другая трактовка —
  породить регистровый файл на APB/AXI-Lite/Wishbone, где адрес задаёт смещение
  регистра. Это самостоятельная цель, а не флаг `sv`.

- **Порт `en` (clock enable) для цели `sv`** (вынесено закрытой 0045, ADR
  вопрос 1, Option C — признан «преждевременным, но не неверным»). Аддитивен
  поверх принятого решения: порт с умолчанием `1'b1`, дающий возможность
  тактировать модель реже тактового домена.

- **Предупреждение о делителе (`SV-009`)** (вынесено закрытой 0045, задача
  0045-06 — код зарезервирован, но не принят). `/` и `%` синтезируются в
  аппаратный делитель — крупный и медленный блок. Полезная подсказка или шум,
  решается пробой `yosys stat` на модели с делением; в корпусе деления нет.

- **Имена POU цели `st` не изолированы** (выявлено при закрытой 0041, обе находки
  подтверждены гейтом `iec2c`). Пространство имён `FUNCTION`/`FUNCTION_BLOCK` в
  IEC 61131-3 **плоское и общее со стандартной библиотекой**, а генератор `st`
  печатает имена как есть. Два следствия:
  (1) одноимённые `fn` разных моделей столкнутся — цель `c` префиксует именем
  модели (`Stacker_travel_time`), цель `st` — нет;
  (2) модель с именем `Concat`/`ABS`/`MIN`/`SEL` даёт `invalid function block
  name` — имя корневой модели берётся **из имени файла**, поэтому ловушка
  срабатывает на `concat.lam` (поймано на фикстуре 0041-07, файл переименован).
  Оба случая `iec2c` ловит **громко** («duplicate»/«invalid name»), тихого
  расхождения нет — поэтому кандидат, а не дефект. Лечение: префиксовать POU
  уникальным именем модели, как в цели `c`.

- **Булевы литералы в телах цели `st` печатаются числом** (выявлено при закрытой
  0041): `cmd_fork := 0;` вместо `cmd_fork := FALSE;`. MatIEC принимает `0`/`1`
  для `BOOL` (проверено пробой — вопреки ожиданию плана 0041-04), поэтому это не
  дефект компилируемости. Но **объявления** тот же генератор печатает канонично
  (`flag : BOOL := TRUE;`), то есть вывод сам с собой не согласован. Лечение:
  печатник выражений должен знать целевой тип присваивания — сейчас он его не
  получает (`st_expr::print_expression` типа назначения не видит).

- **Rename и `PsiReference` для `import` в плагине IntelliJ** (остаток отменённой
  фичи [0040](docs/features/0040-intellij-psi-parser.md); заводить **после**
  [0038](docs/features/0038-intellij-semantic-tokens.md)). Единственные две
  возможности из шести, которые путь LSP4IJ **не** закрывает: нативный rename как
  штатный рефакторинг IDEA и настоящий `PsiReference` для путей `import` с
  автообновлением при перемещении файлов (LSP даёт лишь `documentLink` и
  `WorkspaceEdit` без рефакторинга платформы). Требуют **структурного** PSI, но
  тонкого — только декларации и границы блоков (гибрид Option C из ADR 0040).
  Заводить только после 0038: тогда остаточный объём измеряется в живой
  проверке, а не оценивается умозрительно. Проработка 0040 (ADR, анализ,
  тест-план) сохранена и переиспользуется.

> ~~**Генерация недетерминирована — C *и* PlantUML**~~ (выявлено при 0026,
> уточнено при 0041-01) — **переведено в фичу
> [0048](docs/features/0048-deterministic-codegen.md)** «Детерминированная
> генерация кода» (2026-07-16, решение заказчика). Все заявки кандидата
> подтверждены пробами и уточнены в карточке; там же вскрыта третья причина
> (недетерминированная `topological_sort_models`, `c/mod.rs:836`) и опровергнута
> заявка про порядок полей структуры/вариантов пользовательского `enum` (они —
> `Vec`, то есть стабильны).

> ~~**LTL-верификация моделей: вход `lamc verify`**~~ — **сделано** фичей
> [0049](docs/features/0049-model-checking-ltl.md) (`ГОТОВО`, 2026-07-16;
> удалена из таблицы по правилу 10 — итог в её карточке). Ниже — ветви, которые
> она **не** покрыла и которые её конвейер разблокировал.

- **Верификация свойств над данными** (расширение закрытой 0049; ADR 0049,
  action item A7). Сегодня атом LTL — **имя состояния**, поэтому `G (temp <=
  100)` даёт честный `Verdict::Unsupported`, а не молчаливое «ложно». Атом-
  предикат над переменными (explicit-state или символьный MC) строится **поверх
  готового** конвейера `build_kripke → build_buchi(¬φ) → product → emptiness` —
  меняется разметка вершин, а не звенья. Сюда же остаточные ветви ADR:
  `X`-stutter, контрпример как трасса симулятора.

> ~~**Сужение области проверки `lamc verify --scope`**~~ — **сделано** фичей
> [0051](docs/features/0051-verify-scope.md) (`ГОТОВО`, 2026-07-16; удалена из
> таблицы по правилу 10 — итог в её карточке).

> ~~**Итеративные обходы в `verification/`**~~ — **сделано** фичей
> [0052](docs/features/0052-verify-iterative-traversal.md) (`ГОТОВО`, 2026-07-16;
> удалена из таблицы по правилу 10 — итог в её карточке). Объём был расширен
> заказчиком на **все** рекурсивные обходы: зонд опроверг постановку кандидата —
> самый низкий потолок был в **генерации** (общий для пяти целей), а не в
> `verification/`, и в release-сборке потолка верификации не нашли вовсе.

> ~~**Позиции диагностик из импортированных файлов**~~ — **сделано** фичей
> [0053](docs/features/0053-diagnostics-file-id.md) (`ГОТОВО`, 2026-07-16;
> удалена из таблицы по правилу 10 — итог в её карточке). Кандидат был
> сформулирован неверно: зонд показал, что `file_no ≡ 0` — дефект **латентный**
> (его не читал никто), а боль была в том, что `lamc` не печатал позиций
> **вовсе**. Объём расширен заказчиком до печати `файл:строка:колонка`.

> ~~**LSP однофайлов**~~ — **сделано** фичей
> [0055](docs/features/0055-lsp-multifile.md) (`ГОТОВО`, 2026-07-16; удалена из
> таблицы по правилу 10 — итог в её карточке). Зонд вскрыл, что кандидат винил не
> тот слой: импорт рядом с файлом не искал и сам `lamc` (только по `-I`), то есть
> редактор **наследовал** ядро. Лечение ушло в ядро — иначе редактор стал бы
> зелёным там, где сборка красная.

- **`goto_declaration` угадывает файл по имени модели** (вскрыто закрытой 0055,
  ADR 0055 A2). `goto_declaration_with_paths` (`lsp.rs`) перебирает `search_paths`
  и **угадывает** имя файла из имени модели (`to_snake_case`), переоткрывая и
  переразбирая его; её докблок честно признаёт: «если имя модели не совпадает с
  нормализованным именем файла, переход может не найти нужный файл». После 0053
  реестр (`FileTable`) даёт **точный** путь по `file_no` — угадывание можно
  выбросить целиком.

- **LSP не читает `initializationOptions`** (вскрыто закрытой 0055, A3).
  `lam_lsp.rs:66` берёт `(init_id, _init_params)` и параметры **игнорирует**,
  поэтому аналог `-I` в редакторе задать нечем: работает только каталог
  документа (фича 0055). Модель, чьи импорты лежат в общей библиотеке вне её
  каталога, в редакторе не соберётся, хотя `lamc -I lib` её собирает.

> ~~**`simulation` печатает диагностики без позиций**~~ — **сделано** фичей
> [0054](docs/features/0054-sim-diagnostics-positions.md) (`ГОТОВО`, 2026-07-16;
> удалена из таблицы по правилу 10 — итог в её карточке). Печать позиции при этом
> **вынесена** в общий слой `grammar::diagnostics::position_prefix`, а не
> скопирована: копия в двух бинарниках разошлась бы (прецедент — задача 0028-01).
> Симулятор терял больше `lamc`: не только позицию, но и код диагностики.

- **`Location::filename()` возвращает `"0"`, а не путь** (ADR закрытой 0053, A1).
  Метод даёт `format!("{}", file_no)` — то есть номер строкой, тогда как имя
  обещает путь; `parser_tests.rs:944` это закрепляет ассертом
  `loc.filename() == "0"`. После 0053 путь доступен честно
  (`Diagnostic::file`), поэтому метод — ловушка для следующего читателя.
  Лечение: переименовать/удалить, поправив тесты.

- **Скобочная форма `S(…)` отвергается семантикой** (выявлено при 0047, две
  пробы). `(S(Ping)) = Done` и `S((Ping)) = Done` дают **`SE-025`**
  «Неразрешённое условие», хотя `S(Ping) = Done` работает: спецслучай
  `validate.rs` ищет `Function(S, [Model])` **непосредственно**, и обёртка
  `ConditionNode::Parenthesis` его ломает — имя состояния остаётся неразрешённым.
  Ожидание «скобки ничего не меняют» опровергнуто пробой; заготовленный разворот
  скобок в генераторе C оказался бы **мёртвым кодом** и удалён. Лечение — в
  **семантике** (разворачивать `Parenthesis` перед сопоставлением контекста), а
  не в трансляции. Граница закреплена тестом
  `parenthesised_state_of_is_rejected_by_semantics`, который упадёт, когда
  семантика научится скобкам.

- **Эталонный `SRC` в `lib.rs` порождает C с 11 ошибками `cc`** (выявлено при
  0047). Модель-«всё сразу», на которой стоит `syntax_simple`, **никогда** не
  давала компилируемый C, и это не замечалось: тест проверял лишь факт `Ok`.
  Причины **не связаны** с состояниями: в порождённой структуре нет
  порт-колбэков `read_numeric`/`write_numeric`, а `const`-массив
  `MATRIX: u8 := {0,…}` печатается как скаляр (`CONST_..._MATRIX`), давая
  `expected ')'`. Проверено: строка перевода `S(Ping) = End` ошибок **не даёт**
  (ошибки — на соседних строках, обращения к портам). Из-за этого `syntax_simple`
  вынужден проверять **строку** в `.c`, а не компиляцию. Смежно с находкой 0034
  про структуры и с кандидатом про `always` на уровне модели.

- **Симулятор не исполняет массивы вовсе** (выявлено при 0029-04, две пробы;
  **блокирует** проверку T18 тест-плана 0029 — та признана неисполнимой).
  Дефект не в генераторе C (после 0029 он даёт корректный `uint8_t data[4]`), а
  в симуляторе:
  (1) `data[0] := 7;` → **`SIM-017`** «присваивание не в переменную пока не
  поддерживается симулятором» — записи в элемент массива **нет вовсе**;
  (2) `var data: [u8;4] := 0;` кладёт в переменную **скаляр** `Number(0)`, после
  чего чтение `data[0]` даёт **`SIM-010`** «переменная 'data' не является
  массивом» — то есть диагностика обманчива: массив объявлен, но стал скаляром.
  Следствие: сверка симулятора с C по массивам (расширение критерия A8 фичи
  0025) **недостижима**, пока это не починено. Ограничение закреплено тестом
  `a9_bit_and_array_conformance_gap` (`simulation/tests/conformance_c_tests.rs`),
  который **упадёт**, когда препятствие исчезнет. Смежно с кандидатом «Семантика
  `[bit;N]` расходится втрое» ниже: п. 2 — прямое следствие того, что скалярный
  инициализатор массива в языке не определён.

- **Конфликт кодов диагностик: `CC-014` занят дважды** (выявлено при реализации
  0029). Фича [0028](docs/features/0028-c-generator-stubs.md) держит
  **Accepted**-ADR на код `CC-014` («непереводимое условие перехода»), но
  разработка её не начата; реализованная 0029 заняла `CC-014`/`CC-015` под
  непредставимые в C типы, а затем `CC-016` (ширина порта) и `CC-017`
  (инициализатор массива). Обе фичи выбирали код **грепом свободного** и друг
  друга не увидели — то есть механизм выдачи кодов не защищает от коллизий.
  Ближайшее лечение: **0028 при реализации берёт `CC-018`** и далее (правка
  ADR 0028 и задачи `0028-01`). Системное: реестр кодов диагностик в одном месте
  (сейчас коды живут только в коде и в таблице `README.md`, пополняемой вручную).

- **Семантика `[bit;N]` расходится втрое** (выявлено при 0029). Генератор C
  трактует `[bit;8]` как **скаляр** `uint8_t`; симулятор (`coerce_array`) — как
  **массив из 8 значений**; корпус инициализирует его **скаляром**
  (`const MAX: Byte := 0xFF`), что `coerce_array` отверг бы. Три ответа
  противоречат друг другу. Это вопрос **семантики языка**, и 0029 не вправе его
  решать — поэтому её критерий сверки на `[bit;N]` сознательно не расширен.

- **`elevator_mini.lam` не исполняется вовсе** (выявлено при 0030).
  `SIM-009: переменная 'FloorSensor_F1_Bottom' не найдена`, хотя порт
  **объявлен** (строка 239, та же модель `Cabin`); семантика файл принимает,
  `lamc` компилирует. Порты под-модели композиции `Cabin | Motor` не попадают в
  среду вычисления — класс из предупреждения `CLAUDE.md` про «оба пути
  разрешения переменных». Дефект симулятора, не примера.

- **Дефекты генератора C по структурам** (выявлено при 0034, три пробы):
  (1) `var p: Point := {1,2};` → `model->p = {1, 2};` — **невалидный C**
  (`cc: error: expected expression`; нужен составной литерал `(Point){1,2}`);
  (2) `const c: Coord := {0,0,0};` (`extend_complex.lam`) **молча исчезает** из
  C — остаётся только `typedef`; (3) существование поля **не проверяется** —
  `p.NOSUCHFIELD` компилируется молча в невалидный `model->p.NOSUCHFIELD`
  (нужна диагностика в `grammar`).

> ~~**`{Root}_init` не инициализирует вложенные модели**~~ (выявлено при 0033) —
> **исправлено фичей [0033](docs/features/0033-init-tick-alignment.md)** (R6):
> инициализация вложенных вынесена в `_init`, чтение поля до первого `_tick`
> больше не UB (проба: `entry.state=0` вместо мусора).

> ~~**`SE-009` мёртв: дубликат `fn` принимается молча**~~ (выявлено при 0031) —
> **исправлено фичей [0031](docs/features/0031-fn-calls-fn.md)**: проверка
> дубликата перенесена в точку вставки функций (`tree.rs`), где `HashMap::insert`
> прежде молча перетирал; `fn f` дважды теперь даёт `SE-009`.

- **`lamc` не печатает предупреждения вообще** (выявлено при 0035).
  `unused_variable_warnings` и `nondeterministic_transition_warnings` — часть
  публичного API (`lib.rs`), но из CLI **не вызываются**: пользователь не видит
  ни Ce13, ни Ce14. Влияет на доставку любых новых предупреждений (SE-053/054
  из 0035 и 0042).

- **`semantic/unused.rs` не обходит `formulas`** (выявлено при 0035):
  переменная, используемая только в формуле, считается неиспользуемой.

- **Тело `always` на уровне модели не эмитится в C** (выявлено при 0035):
  проба `always { a := 1; }` вне состояния не дала в `tick` ничего.

- **Ключ карты адресов — голое имя порта** (выявлено при 0043; латентный дефект
  ядра [0020](docs/features/0020-port-address-decl.md)). `AddressResolution.map`
  — `HashMap<String, _>` по **голому** имени, тогда как C-генератор индексирует
  **квалифицированными** вариантами (проба на `elevator_mini`:
  `ELEVATOR_MINI_CABIN_…` при ключе `CabinButton_DC`). Одноимённые порты в разных
  под-моделях делят ключ — побеждает последний. В корпусе замаскировано ручной
  префиксацией имён авторами. Экспорт 0043 делает дефект видимым, но чинить его
  там нельзя — задевает `c-hal`.

- **Константы `LANGUAGE_VERSION` в коде нет** (выявлено при 0045). Версия языка
  живёт только в `README.md:1007`; рассинхрон между документацией и фактическим
  поведением компилятора ничем не ловится. Обостряется тем, что пять фич пула
  заявляют подъём версии.

- **`var q: u8;` без инициализатора → `SIM-009`** (выявлено при 0034
  контрольной пробой). Не структурный дефект — 0034 его не чинит.

- **Цель `sv-mmio`** (вынесено из 0045) — адрес порта из
  [0020](docs/features/0020-port-address-decl.md) → регистровый файл на шине.
  Ядро 0045 адрес игнорирует: MMIO для RTL — это шина, а не разыменование.

- **Режим «записать и продолжить» при нарушении инварианта** (вынесено из 0044).
  Ядро останавливает прогон (`TickResult::Failed`, SIM-008) — иначе сверка с C
  сравнивает несравнимое (`assert()` → `abort()`). Мягкий режим для отладки —
  отдельная фича.

- **Остальные 17 нарушителей лимита размера модуля** (вынесено из 0027; в её
  объём взяты только три). Крупнейший файл проекта — **`grammar/tests/`
  `semantic_tests.rs`, 4766 строк**, на 31% больше `validate.rs`, а правило
  распространяется на тесты явным текстом «вместе с тестами». Следом —
  `semantic/mod.rs` (1708). Суммарное превышение по 15 модулям `src` — 6255
  строк.

> ~~**Предупреждение сборки в `grammar`**: `field 'end' is never read`
> (`format/comments.rs:27`)~~ — **поглощено фичей
> [0046](docs/features/0046-build-warnings-cleanup.md)** (устранение всех ~125
> предупреждений сборки), заведённой параллельно 2026-07-15.

Остаточные пункты плагина (0022/0023): визуальная `runIde`-проверка в среде с
GUI; юнит-тест CT3 на произвольный неалфавитный символ → `BAD_CHARACTER`.
`verifyPlugin` **настроен и пройден** (фикс [0023-01](docs/fixes/0023-01-verifyplugin-descriptor.md)):
Compatible для IC 2024.3/2025.1/2025.2 — список IDE обновлять по мере выхода
новых релизов. Сборка по-прежнему под 2024.1.x/JDK 17.

> **Уточнение (0039).** Утверждение «собрать плагин в CI-среде нельзя»,
> кочевавшее по остаточным пунктам, **опровергнуто пробой**: `buildPlugin
> --offline` и `cleanTest test --offline` проходят успешно — 53 теста за ~8 с.
> GUI требует **только** `runIde`. Остаточным пунктом остаётся визуальная
> проверка, а не сборка.

## 3. Процессный бэклог

Улучшения процесса разработки, инфраструктуры и лучших практик.

- ~~Автоматизировать проверку целостности ссылок в Markdown (правило 14)~~ —
  **сделано** (2026-07-14): `scripts/check-links.py`, подключён в
  `scripts/precheck.sh`. Проверяет цель **относительно файла-источника**, поэтому
  ловит не только несуществующие пути, но и ссылки, ведущие «не туда» после
  переезда (класс `docs/docs/…` из правила 14). При заведении показал две находки:
  битую ссылку в самом `docs/RULE.md:138` (исправлена) и ссылку вида
  `[ADR 0019](0019-…)` в анализе 0025, резолвившуюся в *анализ* 0019 вместо ADR
  (исправлена).
  Остаётся: подключить в CI (`.github/workflows/ci.yml`).
- **CI не запускает `precheck.sh`** (выявлено при 0041, подтверждено при
  реализации 0041-06). Живая проверка порождённого C (cmake/ninja по
  `examples/`) в CI **не выполняется** — только `cargo build/check/test`. Именно
  поэтому дефект 0026 (порождённый C не компилируется для одиночной модели)
  дожил до своего исправления: все пять `examples/*.lam` имеют под-модели и
  попадали в рабочую ветку, а простейший класс моделей живой проверкой не
  покрывался вовсе. **Урок закрытой 0026:** дыра была не в коде, а в покрытии —
  и `precheck.sh` её по-прежнему не закрывает (там всё те же пять примеров). Шире уже записанного пункта про `check-links.py`: в CI не
  идёт **весь** предкоммит-гейт. **Подтверждено закрытой 0041:** ST-гейт (`iec2c`)
  работает и **включён в `precheck.sh`** (валит предкоммит на невалидном ST), но в
  CI не идёт — связывать не с чем. То есть предкоммит теперь проверяет живьём
  **две** цели (`c` и `st`), и обе проверки CI не видит. Инструмент CI-пригоден:
  MatIEC собирается из исходников (`scripts/ensure-iec2c.sh`), лицензия открытая,
  на `ubuntu-latest` bison 3.x (на macOS системный bison 2.3 сборку **валит** —
  нужен `brew` bison ≥ 2.4).
- **Windows отсутствует в матрице CI** (выявлено при 0037): `ci.yml` содержит
  единственный job `ubuntu`. Корневая причина класса «предсуществующие сбои на
  Windows»; введена в объём 0037 задачей 0037-03. Там же: CI не соблюдает
  `--test-threads=1` (при том что `CLAUDE.md` требует), `actions-rs/*`
  архивированы.
- **Правило о размере модуля живёт не в своде правил кода** (выявлено при 0027).
  Кандидат ссылался на «лимит ~1000 (`CLAUDE.md`, `docs/CODE.md`)», но в
  `docs/CODE.md` такого правила **нет** — единственный источник `CLAUDE.md:128`,
  то есть файл **живого контекста** (правило 9), а не свод правил кода
  (правило 23). Перенести в `docs/CODE.md`.
- **У фичи 0018 нет ADR** (выявлено при 0027): `docs/adr/0018-*` не существует —
  стадия 2 тогда не оформлялась. Ссылки «по образцу 0018» вводят в заблуждение.
- По мере проработки фич 0018–0020 заводить ADR/анализ/тест-планы через
  `scripts/new-feature.sh` (реестры уже готовы).
- **Правило в `docs/CODE.md`: запрет подстановочных веток (`_ =>`) при разборе
  семантических узлов** (`ExpressionNode`/`ConditionNode`/`StatementNode`) —
  вытекает из [ADR 0025](docs/adr/0025-simulator-expression-eval.md). Именно
  `_ => None` позволил двум вычислителям симулятора разойтись молча: добавление
  варианта не ломало сборку. Правило + линт
  `clippy::wildcard_enum_match_arm` переносят инвариант «все узлы обработаны» с
  дисциплины на компилятор. Сейчас такого правила в `docs/CODE.md` нет.
  **Реализовано точечно** в `simulation/src/eval/` задачей 0025-01 (проверено
  негативным тестом: линт валит сборку) — осталось обобщить как правило.
  **Подтверждение необходимости** (выявлено при 0034): рецидив уже случился —
  `unit/builder.rs::eval_expr` содержит **второй вычислитель** с `_ => None`,
  вне `eval/` и вне действия линта; он расходится с адаптером ровно на
  `ExpressionNode::Initializer`, и это механизм обманчивой диагностики SIM-009.
  Устраняется задачей 0034-04, но правило закрыло бы класс целиком.

- **Конфликт правил: `#[non_exhaustive]` против запрета `_ =>`** (выявлен при
  реализации 0025-01). `docs/CODE.md` **предписывает** помечать публичные
  перечисления `#[non_exhaustive]`, а этот атрибут существует **ровно для того**,
  чтобы добавление варианта **не** ломало сборку зависимых крейтов — то есть
  прямо отключает механизм [ADR 0025](docs/adr/0025-simulator-expression-eval.md).
  Сейчас конфликт узкий и безвредный: помечен только `TypeNode`
  (`grammar/src/semantic/type_node.rs:495`), из-за чего `eval::coerce_to_type`
  вынужден иметь одну ветку `_` (возвращает **ошибку**, не тихий `None`), а
  ключевые `ExpressionNode`/`ConditionNode`/`StatementNode` **не** помечены —
  гарантия для адаптеров цела. **Риск:** если кто-то применит правило CODE.md к
  `ExpressionNode`, механизм 0025 **тихо умрёт** (сборка пройдёт, адаптеры
  вернутся к «молча не обработано»). Нужно решение: либо явное исключение в
  `docs/CODE.md` для семантических узлов, либо нестабильный линт
  `non_exhaustive_omitted_patterns` (проект на nightly), либо отказ от
  `#[non_exhaustive]` для этих типов.
  **Уточнение (0036):** пометка уже публичных `Value`/`TickResult` механизм 0025
  **не сломала бы** (внутри крейта-объявителя атрибут не действует, а
  разбираются они только внутри `simulation`) — но обесценила бы внешний
  исчерпывающий разбор.
- **Дефект `scripts/new-feature.sh`: с `--register` новый ADR получает статус
  `Accepted`** ещё до того, как ADR написан (выявлено при заведении 0025 —
  правилось вручную). Скрипт должен проставлять `Draft`/`Proposed`, а `Accepted` —
  по факту принятия решения на стадии 2.
- **`scripts/new-feature.sh` не умеет добавлять артефакт **одной** поздней стадии**
  (выявлено при заведении тест-плана 0025). Стадии наступают по очереди, а
  генератор работает «всё сразу при заведении фичи»: `--with-dev` создаёт
  development **и** tests вместе, а повторный `--register` **дублирует** строки в
  реестрах уже заведённых стадий (`insert_row` вызывается безусловно). Тест-план
  0025 пришлось рендерить вручную (правило 17 допускает как запасной путь). Нужны
  флаги вида `--stage tests|dev|report` и идемпотентный `--register` (не вставлять
  строку, если она уже есть).
  **Подтверждено на масштабе** (заведение 0026–0045): генератор создаёт ровно
  **одну** подзадачу `XXXX-01`, тогда как аналитики декомпозировали работу на
  76 подзадач — реестр `docs/development/README.md` пришлось перестраивать
  скриптом. Плюс переименование подзадачи аналитиком оставляет в реестре битую
  ссылку (случай 0044-01). Нужен режим «пересобрать реестр по факту файлов».
