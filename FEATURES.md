# FEATURES.md — витрина незакрытых фич

> Витрина **только незакрытых** фич (правила 10, 17, 21 в
> [docs/RULE.md](docs/RULE.md)). Информация о закрытых (`ГОТОВО`) и отменённых
> (`ОТМЕНА`) фичах здесь **не хранится** — она живёт в карточке фичи
> `docs/features/XXXX-*.md` (раздел «Итог (что сделано)» либо «Итог (отмена)»).
> Реестр всех карточек — [docs/features/README.md](docs/features/README.md).
>
> Файл содержит ровно **три блока**: (1) таблица последовательности разработки,
> (2) кандидаты в фичи, (3) процессный бэклог.

## 1. Последовательность разработки

Незакрытые фичи в порядке взятия в работу (правило 19: зависимости →
проработанность → приоритет → необходимость). Порядок, зависимости и статусы
пересматривает аналитик при каждом изменении состава/зависимостей фич.

Статусы: `СОЗДАНА`, `АРХИТЕКТУРА`, `АНАЛИЗ`, `РАЗРАБОТКА`, `ТЕСТИРОВАНИЕ`,
`ИСПРАВЛЕНИЕ`, а также особое состояние `ЗАБЛОКИРОВАНА` (пока не закрыта хотя бы
одна зависимость). Полностью выполненная (`ГОТОВО`) или отменённая (`ОТМЕНА`)
фича из таблицы удаляется.

Таблица делится надвое **критерием 2 правила 19 (проработанность)**:

- **Голова (0038…0046)** — фичи, проработанные до `РАЗРАБОТКА` (ADR + анализ +
  тест-план + подзадачи; 0046 — до `АРХИТЕКТУРА`). Внутри головы критерий 2 их не
  различает, поэтому порядок задают зависимости и приоритет; Tier указан в
  карточке каждой. **Фичи хвоста переезжают сюда по мере проработки** — так в
  голову попали [0058](docs/features/0058-rust-tail-return-if-else.md),
  [0059](docs/features/0059-rust-shared-struct.md),
  [0060](docs/features/0060-enum-width-shared-layer.md),
  [0061](docs/features/0061-fixed-point-type.md),
  [0062](docs/features/0062-sv-mmio-target.md),
  [0063](docs/features/0063-sv-clock-enable.md),
  [0064](docs/features/0064-sv-divider-warning.md) и
  [0066](docs/features/0066-st-bool-literals.md) и
  [0068](docs/features/0068-verify-data-properties.md) и
  [0069](docs/features/0069-address-map-eval-split.md) (проработаны 2026-07-17;
  **0060 и 0065 закрыты 2026-07-18** — врезки ниже).
  0058, 0059, 0061, 0062, 0063, 0064, 0066, 0068 и 0069 — Tier 3, поэтому встали после прочих
  Tier 3. (Переоценены проработкой до **Tier 2** и уже **закрыты** две фичи: **0060**
  закрыла [0005-01](docs/fixes/0005-01-c-enum-signedness.md) — цель `c` теряла знак
  перечисления; **0065** — [0041-01](docs/fixes/0041-01-st-fn-dedup-silent.md).)
  ⚠️ **0062 РАЗБЛОКИРОВАНА (2026-07-20):** блокер
  [0020-01](docs/fixes/0020-01-port-bit-out-of-range.md) (Tier 1) **исправлен**
  фичей-владельцем [0098](docs/features/0098-port-bit-range-safe-hal.md) (заведена
  как отдельная — решение по «заводить ли фичу-владельца» принято в пользу «да»):
  `SE-060` (бит вне [0, 63]), дефолтный HAL читает по слову, содержащему бит,
  заведён гейт компиляции `c-hal`. Колонка «Зависит от» строки 0062 обновлена.
  ⚠️ **0061 — намеренно последняя из проработанных:** аналитик рекомендует брать её
  **после** дефектной части очереди. Это самая дорогая фича пула (язык + симулятор
  + четыре цели) при **нулевом спросе**: `grep float examples/*.lam` даёт **0**
  вхождений, то есть она добавляет возможность, а не чинит боль. Решение о моменте
  — за заказчиком.
- **Хвост (0088, 0089, 0092)** — фичи, заведённые 2026-07-17 переводом кандидатов
  и процессного бэклога (решение заказчика: «пока без проработки»). У них **нет ни
  ADR, ни анализа**, поэтому критерий 2 ставит их **ниже** любой проработанной
  фичи. По той же причине у них пусты колонки «Зависит от» и «Tier»: и то и
  другое проставляет аналитик на стадии анализа (правило 17).
  ⚠️ **0067 закрыта (`ГОТОВО`, 2026-07-23)** — первая из хвоста, взятая в работу и
  доведённая до конца; врезка-итог — ниже таблицы. Прежде она намеренно не
  прорабатывалась («заводить только после 0038, тогда остаточный объём измеряется
  в живой проверке»); 0038 закрыта, живая проба это подтвердила.

Порядок **внутри хвоста — по номеру**, и это не оценка: пока проработки нет,
критериям 1–3 различать фичи нечем. Первый же анализ вправе поднять любую из них
(критерий 4) — например, 0090 (CI не гоняет живые гейты) или 0081 (`lamc` не
печатает предупреждений вовсе) выглядят разблокирующими, но это **гипотеза**, а
не решение: проверять её должен аналитик.

Положение **0046** (`ЗАБЛОКИРОВАНА` зависимостью 0036) и положение **0037**
(самый хвост таблицы, наименьший приоритет — решение заказчика от 2026-07-17)
заданы критерием 4 в обход критерия 2. Обоснование — ниже.

> Фича **0048** «Детерминированная генерация кода» **закрыта** (`ГОТОВО`,
> 2026-07-16) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0048-deterministic-codegen.md), раздел «Итог (что
> сделано)». Она разблокировала codegen-снапшоты (требование 0026 и T1–T4
> тест-плана 0041) и дала детерминизм любой будущей цели (0045).

> Фича **0040** (полноценный PSI-парсер плагина IntelliJ) **отменена**
> 2026-07-15 решением заказчика и удалена из таблицы (правило 10) — причина и
> что делать вместо в её карточке, раздел
> [«Итог (отмена)»](docs/features/0040-intellij-psi-parser.md). Ни одна фича от
> неё не зависела, пересмотр зависимостей не потребовался.

> Фича **0039** «Действие Reformat Code в плагине IntelliJ» **закрыта**
> (`ГОТОВО`, 2026-07-19) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0039-intellij-reformat.md). Развилка разрешена
> заказчиком в пользу **Option B (LSP4IJ)**: после принятия LSP4IJ в фиче 0038
> реформат `.lam` (`Ctrl+Alt+L`) пришёл **бесплатно** от `lam-lsp`
> `textDocument/formatting` — production-кода не потребовалось, фича схлопнулась
> до приёмки (автотест A2 байт-в-байт на всём корпусе `examples/`). ADR
> обновлён (Option C → Option B, раздел «Обновление решения»); подзадачи Option C
> 0039-01/02/03 сняты как неактуальные. ⚠️ Урок: *когда посылка ADR (здесь —
> самодостаточность плагина) отменяется соседней фичей, развилку надо
> перерешать, а не достраивать снятый вариант.*

> Фича **0043** «Экспорт карты адресов во внешний формат» **закрыта**
> (`ГОТОВО`, 2026-07-19) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0043-address-map-export.md). Подкоманда
> `lamc address-map --emit map|json` выгружает фактически разрешённую карту
> (как `-t c-hal`): `map` замыкается через `--address-map` (круговой рейс
> байт-в-байт), `json` — версионированный контракт с генераторами HAL. CMSIS-SVD
> не поставляется (ADR). ⚠️ Логика вынесена в библиотеку (`address_map/export_cli.rs`):
> `bin/lamc.rs` пришпилен к baseline размера — в бинарнике тонкий диспетчер;
> baseline `lamc.rs` понижён 2039 → 2009 (переехал и `split_include_dirs`). Крейт
> `grammar` — минорный бамп 0.5.0 → 0.6.0 (язык не менялся, 0.2.0).

> Фича **0034** «Структурные типы в симуляторе» **закрыта** (`ГОТОВО`,
> 2026-07-19) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0034-sim-struct-types.md). Снят пробел T22 фичи 0025:
> `Value::Struct` (объявленный порядок полей), чтение/запись поля (точечно, с
> усечением по типу поля), инициализаторы, наблюдаемость; сверено с эталоном C
> (`cc`). Диагностики вместо тихой порчи (SIM-012/026–029); сравнение структур —
> `TypeMismatch` (C запрещает `==`). ⚠️ Осознанные отклонения: `builder.rs::eval_expr`
> **не удалён** (единый вычислитель — риск порт-регресса, кандидат в бэклог);
> загрузка структур `--load-state` не реализована. Крейт `simulation` — минорный
> бамп 0.2.0 → 0.3.0 (язык не менялся). Урок: *`deny(wildcard_enum_match_arm)` в
> `eval/` заставил компилятор перечислить все места под новый `Value::Struct` —
> механизм 0025 сработал.*

| № | Фича | Зависит от | Tier | Статус |
|---|------|------------|------|--------|
| [0088](docs/features/0088-module-size-remaining.md) | Нарушители лимита размера модуля — безопасная часть (12 файлов: тесты, генераторы, ast/lexer, viewport) | — | 3 | РАЗРАБОТКА |
| [0089](docs/features/0089-intellij-residual-checks.md) | Остаточные проверки плагина IntelliJ (0022/0023) | — | — | СОЗДАНА |
| [0092](docs/features/0092-adr-0018-retrofit.md) | У фичи 0018 нет ADR | — | — | СОЗДАНА |

> Фича **0067** «Rename и `PsiReference` для `import` в плагине IntelliJ»
> **закрыта** (`ГОТОВО`, 2026-07-23) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0067-intellij-rename-psi-import.md). **Tier 3:**
> аддитивная редакторная возможность, остаток отменённой 0040. Исполнена
> контрольная точка ADR 0040 «после закрытия 0038» **живой пробой** (headless
> `./gradlew test`): `PsiReference` не привязывается к листовому токену → нужен
> **композит**; R2/R4/R6 закрыты 0038, остаток — только R3+R5. Принят **Option B**
> [ADR 0067](docs/adr/0067-intellij-rename-psi-import.md) (хирургическое
> оборачивание одиночных токенов): в композиты уходят **только** строка-путь
> `import` (`IMPORT_PATH` → `FileReferenceSet` → **rename-on-move**) и
> идентификаторы деклараций/использований (`NAME_DECL`/`NAME_REF` → нативный
> **rename**); выражения/типы остаются плоскими листьями (грамматика не
> дублируется, единый источник форм — `LamSymbolScanner`). Антидивергенция —
> `LamPsiCorpusTest` (round-trip байт-в-байт по 195 `.lam`). `./gradlew test` — 80
> тестов зелены, `verifyPlugin` Compatible ×3 IDE, регресс 0022/0023 без правок.
> Язык не менялся; версия плагина `0.5.0 → 0.6.0`. Остаток — визуальный (`runIde`).

> Фича **0091** «Правило о размере модуля живёт не в своде правил кода» **закрыта**
> (`ГОТОВО`, 2026-07-22) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0091-module-size-rule-in-code-md.md). **Tier 3:**
> процесс/документация. Правило о размере модуля (правило **кода**) жило только в
> `CLAUDE.md` (живой контекст, правило 9), а не в своде `docs/CODE.md` (правило 23)
> — ссылка кандидата 0027 «см. `docs/CODE.md`» вводила в заблуждение. Option A
> [ADR 0091](docs/adr/0091-module-size-rule-in-code-md.md): формулировка правила
> перенесена в `docs/CODE.md` (секция «Размер модулей»); в `CLAUDE.md` — указатель
> + живой контекст (реестр-долг, храповик, разделения), число файлов
> актуализировано 22 → 18. Код/гейт/реестр не тронуты (правило уже проверяется
> машиной, 0027). Остаток нарушителей (18 файлов) — фича 0088.

> Фича **0093** «Правило о запрете `_ =>` в семантических узлах + конфликт с
> `#[non_exhaustive]`» **закрыта** (`ГОТОВО`, 2026-07-22) и удалена из таблицы
> (правило 10) — итог в её
> [карточке](docs/features/0093-wildcard-match-rule.md). **Tier 3:** стандарты кода
> + защитный гейт. Инвариант ADR 0025 (семантические узлы разбираются
> исчерпывающе, без `_ =>` — иначе вычислители расходятся молча) жил лишь в коде
> `eval/` и `CLAUDE.md`, а свод `docs/CODE.md` рекомендовал `#[non_exhaustive]` без
> исключения — атрибут, тихо отключающий этот инвариант. Option A
> [ADR 0093](docs/adr/0093-wildcard-match-rule.md): правило + явное исключение в
> `docs/CODE.md` + машинный гейт `scripts/check-exhaustive-nodes.sh`
> (в `precheck.sh`), падающий при пометке узла `#[non_exhaustive]` **или** снятии
> `deny` в `eval/` — оба пути «тихой смерти», проходящие сборку молча (прецедент
> ADR 0027/0077). Аддитивно: код не тронут, вывод/поведение неизменны. Рецидив
> `builder.rs::eval_expr` (второй вычислитель) — кандидат (владелец 0034).

> Фича **0094** «Доработка `scripts/new-feature.sh`» **закрыта** (`ГОТОВО`,
> 2026-07-22) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0094-new-feature-script-fixes.md). **Tier 3:** процесс/
> инфраструктура, но боль конкретная — генератор дублировал строку реестра при
> повторном `--register`, что обходилось **вручную ×3** за одну сессию. Option A
> [ADR 0094](docs/adr/0094-new-feature-script-fixes.md): идемпотентный `insert_row`
> по ключу-номеру (дубли исключены), ADR-заготовка `Draft` (не `Accepted`),
> `--stage NAME` (впервые `report`) + `--subtask NN`, `NF_ROOT` для тестируемости.
> Регресс-тест `scripts/test-new-feature.sh` в `precheck.sh` (поймал при разработке
> обрыв дефолтного пути из-за `[ … ] && …` под `set -e`). Пересборка реестра из
> файлов (Option B) отложена в кандидат (ненадёжна в POSIX sh). Язык/крейты не
> затронуты.

> Фича **0087** «Мягкий режим инвариантов симулятора (записать и продолжить)»
> **закрыта** (`ГОТОВО`, 2026-07-22) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0087-invariant-soft-mode.md). **Tier 3:** аддитивная
> отладочная возможность, продолжение [0044](docs/features/0044-sim-assert-invariant.md).
> Ядро 0044 **останавливает** прогон при нарушении инварианта (`SIM-025`, как
> `assert()`→`abort()` в C) — нужно для сверки. Мягкий режим (Option A
> [ADR 0087](docs/adr/0087-invariant-soft-mode.md)) **записывает и продолжает**:
> `Unit::tick_soft`/`tick_mode(soft)`, нарушение → поле `Node.invariant_violations`
> + продолжение такта (иначе ливлок); рекурсивный слив `take_invariant_violations`
> (зеркало `take_last_transitions`). Ошибка вычисления условия — `Failed` в **обоих**
> режимах (R4). CLI `--invariant-soft`, `RunResult::CompletedWithInvariantViolations`,
> ненулевой код. Жёсткий режим (умолчание) и C-конформность неизменны; мягкий в
> сверке с C намеренно не участвует. Аддитивно (правило 11): корпус байт-в-байт;
> язык не менялся; крейт `simulation` `0.5.0 → 0.6.0`.

> Фича **0084** «Ключ карты адресов — квалифицированный (модель + порт)»
> **закрыта** (`ГОТОВО`, 2026-07-22) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0084-address-map-qualified-key.md). **Tier 2:**
> латентный дефект корректности ядра [0020](docs/features/0020-port-address-decl.md)
> (выявлен при 0043). `AddressResolution.map` ключевалась голым именем порта, а
> `c-hal` индексировал квалифицированным вариантом → одноимённые порты разных
> под-моделей делили ключ, адрес первого терялся при вставке. В корпусе
> замаскировано (все адреса на корне). Option A
> [ADR 0084](docs/adr/0084-address-map-qualified-key.md): ключ карты
> квалифицирован (`qualified_port_key(model_unique, port)`), `ResolvedAddress`
> несёт голое `name`; продюсер (`model_unique_name`) и потребитель
> (`Name::unique()`) строят одну строку. c-hal/st-at — lookup по квалиф. ключу;
> sv-mmio/экспорт — по `resolved.name`. Публичные `.ld`/`json` остаются плоскими
> (дедуп по имени, круговой рейс R4 0043 сохранён; Option C отвергнута). Сторож —
> `address_collision_qualified_key_distinct_addresses` (0x10/0x20 против общего
> 0x20 до фикса). Аддитивно (правило 11): вывод корпуса всех целей байт-в-байт
> неизменен; язык не менялся; крейт `grammar` `0.10.0 → 0.11.0`.

> Фича **0085** «Константа версии языка в коде + гейт синхронизации с README»
> **закрыта** (`ГОТОВО`, 2026-07-22) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0085-language-version-constant.md). **Tier 3:**
> инфраструктура/процесс, но с конкретной ценностью «здесь и сейчас». Версия языка
> жила **только** в `README.md`, рассинхрон дока↔факт ничем не ловился. Option A
> [ADR 0085](docs/adr/0085-language-version-constant.md): единственный источник
> истины — `grammar::LANGUAGE_VERSION` (`grammar/src/version.rs`) + машинный гейт
> `scripts/check-language-version.sh` в `precheck.sh`/CI (сверка с канонической
> строкой README; отказ на расхождении/отсутствии/дубле якоря). **Гейт на первом
> прогоне поймал живой дефект 0078** (README `0.3.0` при фактической `0.4.0`) —
> строка приведена к `0.4.0` (не подъём версии, правило 22 — фиксация уже
> действующей). Прецедент машинных doc-гейтов — ADR 0027/0077. Аддитивно (правило
> 11): вывод целей и симулятора байт-в-байт неизменен; `lib.rs` удержан на baseline
> 1402 (ужат crate-doctest на 3 строки); крейт `grammar` `0.9.0 → 0.10.0`.
> Подкоманда `lamc version` (Option B) — **кандидат** (заблокирована baseline
> размера `lamc.rs`).

> Фича **0078** «Семантика `[bit;N]` расходится втрое» **закрыта** (`ГОТОВО`,
> 2026-07-22) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0078-bit-array-semantics.md). **Tier 2:** тип
> расходился впятеро (C-скаляр / rust-`[bool;8]` / st-`ARRAY OF BOOL` / sv-массив /
> симулятор-`Value::Array`) — симулятор врал про синтез. По решению заказчика
> ([ADR 0078](docs/adr/0078-bit-array-semantics.md), Option A) `[bit;N]` —
> **упакованный бит-вектор**: N ≤ 64 скаляр `round_up(N)` (`[bit;8]`≡`u8`), N > 64
> массив `uint64_t[⌈N/64⌉]` (sv — нативный `logic[N-1:0]`). Правило упаковки — в
> одном слое `semantic::bit_vector`; бит-доступ сверен с C. `CC-014` выведен (гейт
> реестра 0077 поймал рассинхрон → `RETIRED`). C неизменен на корпусе; rust/st/sv
> меняются лишь на `extend_complex`. Язык `0.3.0 → 0.4.0`, крейты `grammar`
> `0.8.0→0.9.0`, `simulation` `0.4.0→0.5.0`.

> Фича **0077** «Реестр кодов диагностик» **закрыта** (`ГОТОВО`, 2026-07-22) и
> удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0077-diagnostic-code-registry.md). **Tier 3:**
> инфраструктура; коллизия `CC-014` была латентной, боль — будущая. По
> [ADR 0077](docs/adr/0077-diagnostic-code-registry.md) (Option A) заведён **единый
> реестр** [`docs/diagnostics/`](docs/diagnostics/README.md) (171 код, 11 префиксов;
> пробелы AM/PU/ST закрыты) + **машинный гейт** `scripts/check-diagnostic-codes.sh`
> в `precheck.sh`/CI: незадокументированный код, протухшая запись, нарушение формата
> `XX-NNN`, дубль → сборка красная; печатает «следующий свободный» по префиксу.
> Прецедент — правило размера модуля (ADR 0027). Enum-типизация (Option B)
> отвергнута (≈330 мест, маргинальная польза). Аддитивно (`with_code` не тронут) →
> вывод целей и симулятора байт-в-байт неизменен; язык `0.3.0`, версии крейтов без
> изменений.

> Фича **0076** «Симулятор не исполняет массивы вовсе» **закрыта** (`ГОТОВО`,
> 2026-07-22) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0076-sim-arrays.md). **Tier 2:** симулятор врал про
> массивы — запись `data[i] := v` давала `SIM-017`, скаляр-init делал массив
> скаляром (`SIM-010` при чтении). По [ADR 0076](docs/adr/0076-sim-arrays.md)
> (Option A) реализован **единый механизм «места»** со структурами (0034): путь
> стал сегментным (`PlaceSegment{Field|Index}`), запись элемента исполняется, лист
> усекается по типу элемента (S9), список-init `{…}` приводится поэлементно.
> Границы/вид → `SIM-010`. Сверка с C потактовая
> (`array_element_matches_generated_c`), прежний сторож `a9_bit_and_array_conformance_gap`
> заменён. **Вне объёма (0078):** скаляр-init массива и `[bit;N]` (у них нет
> эталона — C scalar-init отвергает). Разблокирован T18 плана 0029. Корпус массивы
> не симулирует → вывод байт-в-байт неизменен; язык `0.3.0`, крейт `simulation`
> `0.3.0 → 0.4.0`.

> Фича **0037** «Сбои тестов на Windows» **закрыта** (`ГОТОВО с оговоркой`,
> 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0037-windows-test-failures.md), отчёт
> [0037](docs/reports/0037-windows-test-failures.md). **Tier 2:** дефект — **в
> тестах**, не в продукте. 0037-01: пять тестов `-I` в `lamc.rs` зашивали `:` как
> разделитель безусловным `#[test]` → падение на Windows (там `;`); параметризованы
> `const SEP = if cfg!(windows) {…}`. 0037-02: тест `viewport` писал в `/tmp`
> (нет на Windows) → паника; запись удалена, срез усечён по символам. 0037-03: job
> `windows` в CI (`cargo test`, `continue-on-error`) — **адаптирован** к
> precheck-барьеру после 0090. ⚠️ **Оговорка:** A8/A9/A11 (лог Windows, снятый
> `continue-on-error`) не проверены — Actions заблокирован по биллингу (прецедент
> 0090); закрыто по локальной проверке (darwin 2137 тестов зелены). Версия языка
> не поднята.

> Фича **0086** «`var q: u8;` без инициализатора → `SIM-009`» **закрыта**
> (`ГОТОВО`, 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0086-sim-var-without-initializer.md), отчёт
> [0086](docs/reports/0086-sim-var-without-initializer.md). **Tier 3:** объявленная
> переменная без инициализатора для симулятора не существовала (`SIM-009` при
> чтении). Продолжение 0034 (гэп 0034-04): политика «нет init → ноль по типу, как
> C default-init» распространена со структур на **все** типы — ветка `get_value`
> зовёт общий `default_field`, `default_struct` удалён. Скаляр без init → 0;
> регресса портов/констант нет; массивы — вне объёма (нерабочая 0076); кодоген
> байт-в-байт неизменен, версия языка не поднята.

> Фича **0072** «LSP не читает `initializationOptions`» **закрыта** (`ГОТОВО`,
> 2026-07-21) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0072-lsp-initialization-options.md), отчёт
> [0072](docs/reports/0072-lsp-initialization-options.md). **Tier 3:** `lam-lsp`
> игнорировал параметры клиента и передавал в ядро пустой список путей поиска
> (`&[]`), поэтому импорт из общей библиотеки вне каталога документа в редакторе
> не находился (боль из 0055 A3). Читается `initializationOptions.searchPaths`
> (аналог `-I`); разбор — в библиотеке `grammar::lsp::search_paths_from_options`
> (модуль `init_options.rs`, ради тестируемости), бинарник хранит пути в
> `ServerState` и прокидывает в `collect_diagnostics_at`/`goto_declaration_at`.
> Аддитивно (пустой список ≡ прежнее поведение); крейт `grammar` 0.7.0 → 0.8.0,
> версия языка не поднята. Ни одна фича от 0072 не зависела — порядок хвоста не
> изменился.

> Фича **0073** «`Location::filename()` возвращает номер, а не путь» **закрыта**
> (`ГОТОВО`, 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0073-location-filename-path.md), отчёт
> [0073](docs/reports/0073-location-filename-path.md). **Tier 3:** ложно названный
> `Location::filename()` (возвращал `file_no` строкой) **удалён** (Option A);
> честный `try_file_no()` уже был рядом, а «вернуть путь» невозможно — `Location`
> пути не знает (он в `FileTable`, 0053). Метод звали лишь два теста, дублировавших
> `try_file_no`; обе проверки убраны. API сужен без потребителей, вывод корпуса
> байт-в-байт неизменен, версия языка не поднята.

> Фича **0071** «Переход на имя состояния в условии (goto)» **закрыта** (`ГОТОВО`,
> 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0071-lsp-goto-state-name.md). **Tier 3:** `goto` на
> имени состояния в `S(Ping) = End` открывает декларацию; продолжение 0056. Два
> механизма: кросс-модельный `S(Ping)=End` (разбор на уровне `ConditionNode::Equal`
> в индексе) и внутримодельный `x = Done` (`ConditionNode::State` + use-site
> позиция). Вывод генераторов байт-в-байт неизменен; `index.rs` разделён на
> `index/{mod,collect}.rs`. Версия языка не поднята.

> Фича **0074** «Скобочная форма `S(…)` отвергается семантикой» **закрыта**
> (`ГОТОВО`, 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0074-parenthesised-state-of.md). **Tier 3:** скобки
> паттерна `S(Модель) = Состояние` (`(S(Ping)) = End`, `S((Ping)) = End`,
> `S(Ping) = (End)`) давали `SE-025`; стали **прозрачны** — вывод байт-в-байт
> равен бесскобочной форме. Option C ADR: канонизация в **единой воронке**
> `resolve_condition` (`strip_cond_parens`/`is_state_of`/`canonicalize_state_of`)
> чинит семантику, все генераторы и симулятор одной правкой — потребители
> получают каноничную форму (спецслучай `validate` не тронут). Сторож
> `parenthesised_state_of_is_rejected_by_semantics` инвертирован (граница ADR).
> **Проба нашла третью форму** (`S(Ping) = (End)`), не названную в карточке.
> Вывод корпуса неизменен; инвариант `resolve_state_references` цел; цели
> `rust`/`st`/`sv` паттерн `S(…)` по-прежнему не поддерживают (вне объёма).
> Версия языка не поднята.

> Фича **0083** «Тело `always` на уровне модели не эмитится» **закрыта**
> (`ГОТОВО`, 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0083-model-always-block-c.md). **Tier 2:** model-level
> `always` (вне состояния) молча терялся во **всех** целях **и** в симуляторе
> (`builder.rs` строил `executions: HashMap::new()`) — эталона не было, поэтому
> объём — выбор направления. **Заказчик выбрал реализацию** (Option B ADR): блок
> исполняется каждый такт до диспетчеризации состояния в симуляторе (эталон) и
> `c`/`rust`/`st`/`sv`. Помощники блоков вынесены в `c_blocks`/`rust_blocks`/
> `sv_blocks` (лимит размера; baseline `c_model`/`rust_model` понижены). Сверка:
> потактовая C↔симулятор + компиляция вывода rust/st/sv настоящими тулами (корпус
> конструкции не содержит). Вывод корпуса байт-в-байт неизменен; композитный
> `always` работал и раньше; `enter`/`exit` уровня модели вне объёма. Версия языка
> не поднята.

> Фича **0080** «Дефекты генератора C по структурам» **закрыта** (`ГОТОВО`,
> 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0080-c-struct-defects.md). **Tier 2:** три дефекта —
> (1) инициализатор структурной переменной → составной литерал `(Type){…}`;
> (2) используемая структурная константа → `static const`, а не `#define`
> (макрос давал `{…}.field`); (3) доступ к несуществующему полю → компайл-тайм
> **`SE-061`** (новый `member_access.rs`, было молча в невалидный C). Вывод
> корпуса байт-в-байт неизменен (корпус затронутых конструкций не содержит; в
> `extend_complex` структурная константа не используется). `SE-061` опережает
> рантайм `SIM-027` (тест 0034 перенастроен; механизм под юнит-тестами `eval/`).
> Массивные константы того же класса — вне объёма (0078). `generate_scalar_init`
> вынесен в `c_blocks` (лимит размера). Версия языка не поднята.

> Фича **0079** «`elevator_mini.lam` не исполняется: порты под-модели композиции»
> **закрыта** (`ГОТОВО`, 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0079-sim-composition-ports.md). **Tier 2:** порты
> внутри под-моделей композиции (`Cabin | Motor`) не драйвились —
> `extract_port_names` собирал их только с корня. **Симптом ≠ первопричина:**
> `SIM-009` замаскирован 0086 (порт без значения → 0), чтение уже работало
> (`get_value` на `Parallel` обходит ветви); дефект — порт **не перечислен для
> драйва**. Фикс: `PortNames::from_model` рекурсивна (вынесена в библиотеку ради
> тестируемости); `set_value`/`get_value` не трогались. Побочно починен матчинг
> модели в `run_simulations.sh` (имена с `_`). Driven-сценарий
> `elevator_mini_floor2.json` (`FloorSensor_F2_Bottom` → `current_floor=2`).
> `stacker` не задет (порты на корне); вывод корпуса неизменен; плоское
> пространство имён портов — вне объёма (0084). Версия языка не менялась.

> Фича **0061** «Fixed-point `q(m, n)` как тип языка» **закрыта** (`ГОТОВО`,
> 2026-07-18) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0061-fixed-point-type.md), отчёт
> [0061-05](docs/reports/0061-05-example-and-docs.md). **Tier 3 (переоценён с
> хвоста):** задачи 01–05. Q-арифметика (`+`/`−`/`*`/`/`) **побитово едина** у
> симулятора (`eval/fixed.rs`) и всех четырёх целей (`c`/`rust`/`st`/`sv`);
> ловушка C11 6.5.7p5 (сдвиг знакового) закрыта floor-делением `lam_q_floordiv`;
> детектор формата — общий слой `type_inference::extract_type` (`SE-059`);
> пример-регулятор `examples/regulator.lam` проходит все гейты корпуса. Закрыла
> отложенный R-B ADR 0045.

> Фича **0097** «Пример ПИД-регулятора на Lam» **закрыта** (`ГОТОВО`, 2026-07-19)
> и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0097-pid-regulator-example.md), отчёт
> [0097](docs/reports/0097-pid-regulator-example.md). **Tier 3, зависела от 0061.**
> Позиционный ПИД с anti-windup на `q(8,8)`, объект 1-го порядка; сходится **без
> входов** и завершается (контракт `examples_scenario_tests`). Конфликт имён с ФБ
> `INTEGRAL` IEC → `i_acc`/`meas`. Компилятор **не изменён** (пример на
> существующем языке). Позже переведён на прозрачный `float` фиксом 0097-01
> (флаги сборки задают представление).

> Фича **0070** «Инициализатор порта — это адрес, а не значение» **закрыта**
> (`ГОТОВО`, 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0070-port-initializer-address-role.md). **Tier 3:**
> `SE-035` ошибочно проверял инициализатор порта (это **адрес**, ADR 0020) как
> значение бита, отвергая `bit := 0xADDR` (тогда как `u8 := 0xADDR` проходил).
> Принята **Option A** (решение заказчика): `VariableNode::Port` выведен из-под
> `check_bit_variable_value` (`validate/enums.rs`) — проверка осталась для
> `var`/`const`. Устранена асимметрия u8/bit и ловушка «`bit := 0/1` — молча
> адрес». 5 тестов в новом `port_initializer_tests.rs` (не в замороженном
> `semantic_tests.rs`); README обновлён. Обратно совместимо, вывод корпуса
> байт-в-байт неизменен, версия языка **не** поднята (снятие ошибочной
> диагностики; прецедент 0098). Позиция бита у bit-порта (голый адрес → бит −1) —
> **кандидат** ниже.

> Фича **0090** «CI прогоняет весь `precheck.sh`» **закрыта** (`ГОТОВО` с
> оговоркой, 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0090-ci-precheck.md). **Tier 2:** барьер, который
> поймал бы два дефекта Tier 1 (0020-01, 0041-01), дожившие из-за необязательности
> арбитра в CI. Принята **Option A** ADR: CI прогоняет **весь** `precheck.sh` под
> строгим режимом (`PRECHECK_STRICT=1` + `require_tool`) — единый источник истины
> гейтов, дрейф двух реализаций исключён; `ci.yml` сведён к установке инструментов
> + одному вызову скрипта, архивные `actions-rs/*` уведены. Зависимостей нет
> (сняты 0041 — ST-гейт зелён, `iec2c` 8/8 — и инструментальная готовность).
> Устаревшие заметки про «невключённый ST-гейт» исправлены (CLAUDE.md/README/
> precheck.sh). ⚠️ **Оговорка:** A5/T2/T3/T11 (реальный Actions) **не проверены** —
> Actions заблокирован **биллингом аккаунта** (job не стартует; те же 3-секундные
> падения у 0062/0082); закрыта **по локальной проверке** (строгий `precheck.sh`
> `EXIT=0`) решением заказчика. Долг верификации — кандидат ниже. ⚠️ **Пересмотр
> (правило 19):** Option A увела из **0037** CI-модернизацию (`actions-rs/*`,
> `--test-threads=1`) — за 0037 остаётся **только матрица Windows** (задача
> 0037-03). Язык не менялся, версия не поднята.

> Фича **0062** «Цель `sv-mmio`: адреса портов → регистровый файл» **закрыта**
> (`ГОТОВО`, 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0062-sv-mmio-target.md). **Tier 3** (ничего не сломано,
> замыкает 0020). Принята **Option B** ADR: новая цель `-t sv-mmio`, парная к `sv`
> (как `c-hal` к `c`) — порт **с** адресом → бит регистрового файла на
> **шинно-агностичном** синхронном интерфейсе (`reg_addr`/`reg_wdata`/`reg_wen`/
> `reg_rdata`), порт **без** адреса → порт модуля. Направление принадлежит **биту**
> (правило 4): одно слово несёт биты обоих направлений; запись в `out`
> игнорируется, чтение `in` возвращает записанное (R5). Не угадываем (правило 6):
> `SE-060`/`SV-013`/`SV-014`. Блокер [0020-01](docs/fixes/0020-01-port-bit-out-of-range.md)
> снят фичей-владельцем [0098](docs/features/0098-port-bit-range-safe-hal.md) **до**
> взятия в работу. Ядро — `generator/sv/sv_mmio.rs` (переиспользует автомат `sv`);
> гейт `stacker` (verilator + yosys), **потактовая сверка через регистры**
> (`conformance_sv_mmio_tests`), цель `sv` байт-в-байт неизменна. ⚠️ `bin/lamc.rs`
> **ужат** дедупликацией веток (2009 → 1993); `compile_to_sv_mmio` вынесен в
> `compile_sv_mmio.rs`. Адаптеры APB/AXI-Lite/Wishbone — по-прежнему отдельные фичи
> по требованию (кандидат). Язык не менялся, версия не поднята.

> Фича **0082** «`unused.rs` обходит формулы» **закрыта** (`ГОТОВО`, 2026-07-20) и
> удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0082-unused-formulas.md). **Tier 2:** Ce13 (`SE-036`)
> давал **ложное** предупреждение на переменной, используемой только в LTL/Guard-
> формуле (`unused.rs` не обходил `ModelNode::formulas`/`StateNode::formulas`).
> Обострено 0081 (Ce13 теперь печатается пользователю). Option A ADR: обход формул
> наравне с прочими узлами (`collect_from_formula` + `collect_from_ltl`); реально
> мёртвая переменная предупреждается по-прежнему (негативный сторож). Язык не
> менялся, версия не поднята.

> Фича **0075** «Эталонная модель порождает компилируемый C» **закрыта** (`ГОТОВО`,
> 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0075-lib-src-reference-model.md). **Tier 2:** тест
> `syntax_simple` стоял на модели-«всё сразу» (`SRC`), которая никогда не давала
> компилируемый C (11 ошибок `cc`: `read_numeric` у `out`-порта с бит-доступом;
> const `[bit;8]` печатается массивом, бит-доступ — числом), и мог проверять лишь
> строку. Обе беды упираются в семантику `[bit;N]` ([0078](docs/features/0078-bit-array-semantics.md)).
> Option A ADR: эталон **разделён** — `parse_simple` держит полный `SRC` (покрытие
> парсера), компиляционная проверка переехала на `SYNTH_SRC`
> (`tests/reference_model_tests.rs`, `cc -c`), сохранив `S(Ping)=End`. `syntax_simple`
> удалён (`lib.rs` 1450→1405). Дефекты генератора — кандидаты (`read_numeric`
> локален; const `[bit;N]` — за 0078). Язык не менялся, версия не поднята.

> Фича **0081** «`lamc compile` печатает предупреждения» **закрыта** (`ГОТОВО`,
> 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0081-lamc-print-warnings.md). **Tier 2:** предупреждения
> публичного API (`unused_variable` = `SE-036`, `nondeterministic` = `SE-037/042`
> и прочие) из CLI **не вызывались вовсе** — доставка была частичной (два
> адрес-специфичных предупреждения, только для не-адрес-потребляющих целей). Option
> A ADR: единая точка `collect_model_warnings` + `print_compile_warning` (формат
> общий с ошибкой) для **всех** целей, `--quiet` уважается. Замер корпуса: одно
> реальное `SE-036` (`elevator.lam`, unused `action`), прочие категории молчат.
> Аналитик отмечал 0081 как разблокирующую доставку **любого** нового
> предупреждения — так и есть. Язык не менялся, версия не поднята.

> Фича **0098** «Проверка диапазона бита адреса порта и безопасный дефолтный HAL»
> **закрыта** (`ГОТОВО`, 2026-07-20) и в таблицу не заносилась (заведена как
> фича-владелец фикса [0020-01](docs/fixes/0020-01-port-bit-out-of-range.md) и
> закрыта в том же цикле, правило 10) — итог в её
> [карточке](docs/features/0098-port-bit-range-safe-hal.md). **Tier 1:** дефолтный
> HAL цели `c-hal` читал бит один байтом и сдвигал на `b.bit` → бит 8…31 молча
> ноль, бит ≥ 32 UB (`int >> 33`); воспроизводилось на `extend_complex.lam` (бит
> 33). Дожило потому, что `c-hal` **не компилировалась ни одним гейтом**. Принята
> Option C ADR 0098: диапазон бита **[0, 63]** (`SE-060`), ширина доступа HAL —
> минимальное слово, содержащее бит (UB исключён конструктивно), заведён гейт
> компиляции `c-hal`. HAL вынесен из `c_header.rs` в `c_hal.rs` (снят с лимита
> размера, долг модулей −1). Значенческий тест доказал устранение UB **исполнением**
> (`mmap`-эмуляция MMIO). Разблокировала **0062**. Язык не менялся, версия не
> поднята.

> Фича **0064** «Предупреждение о делителе (`SV-009`) в цели `sv`» **закрыта**
> (`ГОТОВО`, 2026-07-20) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0064-sv-divider-warning.md), раздел «Итог (что
> сделано)». Принята **Option B** ADR: `SV-009` — **предупреждение** на `/` и `%`
> **с переменным делителем**; делитель-константа и `*` — молча (обосновано замером
> `synth_ice40 -dsp`: переменное деление 558 LUT/0 DSP, умножение 17 LUT/3 DSP —
> дешевле сложения). Обнаружение — в `sv_expr::print_expression`; приёмник —
> `Fsm::warnings` через новое поле `Scope::warnings`. ⚠️ **У цели `sv` канала
> доставки не было вовсе** — фича завела `report` в `sv/mod.rs` по образцу
> `rust`/`st` **вместе с его дефектом** (`eprintln!` из библиотеки, `--quiet` не
> глушит — кандидат, вне объёма). Корпус свободен от деления → вывод всех целей
> побайтово прежний. Язык не менялся, версия не поднята.

> Фича **0063** «Порт `en` (clock enable) для цели `sv`» **закрыта** (`ГОТОВО`,
> 2026-07-19) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0063-sv-clock-enable.md), раздел «Итог (что сделано)».
> Пересмотрен отложенный **Option C** ADR 0045: модуль цели `sv` получает вход
> `en` с умолчанием `1'b1` (IEEE 1800 §23.2.2.4), гейтящий защёлкивание в
> `always_ff` (`end else if (en)`), но **не** сброс (правило 3 ADR). Аддитивность
> доказана **симуляцией** (`verilator --binary`, 4 экземпляра): неподключённый
> `en` побитово ≡ `en=1`, `en=0` замораживает, сброс при `en=0` срабатывает.
> Имя `en` зарезервировано (`SV-007`); корпус свободен. Язык не менялся, версия не
> поднята. ⚠️ Отклонение от буквы задачи: список чувствительности `always_ff`
> оставлен **синхронным** — правило 3 требует лишь структуру ветвей, а не
> `negedge rst_n`.

> Фича **0046** «Устранение всех предупреждений сборки (rustc + clippy)»
> **закрыта** (`ГОТОВО`, 2026-07-19) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0046-build-warnings-cleanup.md). Предупреждения обоих
> крейтов сведены к **нулю** (rustc 0, clippy 0) и закреплены `-D warnings` на
> шаге clippy в `precheck.sh` и CI (Option B ADR; CLI-уровень, **не** запрещённый
> `#![deny(warnings)]`). ⚠️ Инвентарь карточки (2026-07-15, ~125) устарел за 4 дня:
> свежий — **549 clippy**, из них **414 — один класс `result_large_err`** (clippy
> 0.1.99, `Diagnostic` = 136 Б > 128). Разрешено **ужатием `Location::Source`
> u64/usize → u32×3** (`Diagnostic` → 120 Б; решение заказчика, без `Box` и без
> крейт-`#[allow]`), каст локализован в аксессорах/хелпере `Location::source`.
> Остаток — `clippy --fix` + ручные `#[allow]` с обоснованием. Вывод генераторов
> байт-в-байт неизменен. Крейт `grammar` **0.6.0 → 0.7.0** (ломающая смена типов
> публичного `Location::Source`); язык не менялся. ⚠️ Уроки: *кэш clippy даёт
> ложный 0*; *`build --all-targets` не компилирует inline `#[cfg(test)]` lib*;
> *база карточки — снимок* (урок 0036 повторился).

> Фича **0036** «Согласование видимости публичного API крейта `simulation`»
> **закрыта** (`ГОТОВО`, 2026-07-19) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0036-sim-visibility.md). `Unit` инкапсулирован (ADR
> 0036, Option B): `pub enum Unit` → непрозрачный `pub struct Unit(UnitKind)` с
> приватным полем и приватным `pub(crate) enum UnitKind`; внутренние типы
> (`Context`/`Flow`/`Predicate`/`Guards`) честно остались `pub(crate)`.
> Предупреждений `private_interfaces` — **16 → 0** (10 из базы задачи опередила
> 0044); защёлка `#![deny(private_interfaces)]` **доказана пробой** (утечка →
> `error`). Публичный API не пополнился ни одним типом. Крейт `simulation` —
> `0.3.0 → 0.4.0` (слом формы публичного `Unit`); язык не менялся. **Разблокировала
> 0046** (снят `ЗАБЛОКИРОВАНА`). ⚠️ Лимит размера модуля вынудил вынести
> тест-модуль `unit/mod.rs` в `unit/tests.rs` без изменения утверждений (долг
> 21 → 20). ⚠️ Урок: *план задачи — снимок; объём сверяй с кодом на момент взятия
> в работу* (версия крейта, число предупреждений и `unused import` из базы
> устарели за 4 дня).

> Фича **0096** «Прозрачный `float` через глобальную Q-точность» **закрыта**
> (`ГОТОВО`, 2026-07-19) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0096-fixed-point-native-float.md). Автор пишет `float`,
> представление выбирают флаги `--float-as-q=m.n` / `--float-embedded`: цель `sv`
> синтезирует `float → q(m, n)` (**`SV-003` снят**), `c`/`rust`/`st` — native по
> умолчанию, целочисленный Q по второму флагу. Ядро — единая мутирующая
> трансформация `float → q(m, n)` над `ModelNode` (`semantic::lower_float`),
> переиспользующая Q-инфраструктуру 0061 и применяемая к цели И эталону-симулятору
> (сверка внутри режима). Симулятор двухрежимен **через ту же трансформацию** —
> `eval::fixed` не тронут (риск A-1 снят конструктивно). Версия языка не менялась
> (флаги — свойство генерации). ⚠️ Уроки: *«понижать литерал везде» скорректировано*
> (`Fixed + Number` в теле разошёлся бы с арифметикой симулятора — понижаются лишь
> инициализаторы); *идемпотентность требует гейта по исходному типу*; *двухрежимность
> — свойство подаваемой модели, а не двух веток вычислителя*.

> Фича **0038** «Семантическая подсветка Lam в IntelliJ через lam-lsp» **закрыта**
> (`ГОТОВО`, 2026-07-19) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0038-intellij-semantic-tokens.md). Плагин через LSP4IJ
> поднимает `lam-lsp` и красит идентификаторы по смыслу (единый источник —
> `grammar`). ⚠️ Реализация опровергла **две** премисы анализа: (1) сервер был
> готов не полностью — [фикс 0038-01](docs/fixes/0038-01-subtree-token-classification.md)
> (члены под-моделей не классифицировались; весь корпус вложен → подсветка врала
> бы); (2) LSP4IJ несовместим с 241 → **пересмотр драйвера 1 ADR**, платформа
> поднята до 2024.2.5/JDK 21 (Community цел, toolchain авто-провизионирует JDK).
> `verifyPlugin` — Compatible для IC 2024.3/2025.1/2025.2. Разблокировала **0067**
> (снят `ЗАБЛОКИРОВАНА`) и **0039** в LSP4IJ-варианте. Остатки (документированы, не
> блокируют): A11 (цвета — визуально только в `runIde`, как 0022/0023),
> уведомление R3 (кандидат-фикс). ⚠️ Урок: *«сервер готов» — проверяй зондом, а не
> по документам* (класс дефекта 0025).

### Обоснование порядка (правило 19)

> Фичи **0048**, **0033**, **0032**, **0031**, **0044** и **0035** **закрыты**
> (`ГОТОВО`, 2026-07-16) и удалены из таблицы (правило 10). 0033 задала контракт
> «вход не стоит такта» для будущих бэкендов (0041, 0045); 0032 свела значения
> симулятора к единому хранилищу (`ModelNodeContext`); 0031 разрешила композицию
> функций `f→g` (рекурсия — `SE-053`); 0044 ввела `invariant` и оживила
> Guard-формулы в симуляторе (`SIM-025`), сделав имя инварианта **атомом LTL**;
> 0035 сняла тихую потерю LTL в блоках (`SE-055`/`SE-056` через `ltl_warnings`).
> Итоги — в карточках фич.

> Фича **0056** «Кросс-файловый переход к декларации» **закрыта** (`ГОТОВО`,
> 2026-07-17) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0056-lsp-goto-exact-file.md). Переход в
> импортированный файл заработал в редакторе впервые; попутно **исправлен** фикс
> [0053-01](docs/fixes/0053-01-file-table-default-collision.md) (дефект закрытой
> 0053). Объём расширен решением заказчика: ссылки на модель получили позицию
> (`Extend::Model`, `ConditionNode::Model`) — без неё критерии A1/A2 были
> недостижимы. ⚠️ Урок: *«вернулся не тот узел» и «узла нет» — разные диагнозы*;
> зонд проработки показал первое и не проверил второго.

> Фича **0042** «Инъекция define'ов для адресов» **закрыта** (`ГОТОВО`,
> 2026-07-17) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0042-address-defines.md). Ядро (Tier 1) закрыло
> **тихий пропуск**: выражение адреса теперь вычисляется (символы и арифметика),
> а неудача названа (`SE-054`/`SE-055`) вместо вводящей в заблуждение `SE-052`.
> ⚠️ Рекомендация аналитика по очерёдности **0042 → 0043 → 0041** исполнена: оба
> соседа — потребители `AddressMap` — получат уже вычисленные адреса, не дублируя
> свёртку. Версия языка **не поднималась** (правило 22: 0.3.0 уже поднята 0029).

> Фича **0059** «Общие переменные корня → структура `Shared`» **закрыта**
> (`ГОТОВО`, 2026-07-18) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0059-rust-shared-struct.md). Общие переменные корня
> цели `rust` свёрнуты в `<Root>Shared` (новый модуль `generator/rust/rust_shared.rs`),
> передаются под-модели **одним** параметром `&mut Shared` — единственное
> `#[allow(clippy::too_many_arguments)]` в порождаемом коде исчезло, политика (а)
> ADR 0050 без исключений. Многоуровневость (`extend_complex`) — один
> `ExtendComplexShared` ретранслируется вниз. Корпус проходит гейт без заглушек;
> `conformance_rust` вердикта не изменил (основной критерий). Baseline храповика
> обновлён. Язык не менялся (правило 22). Побочная находка (5 `#[allow]` в самом
> генераторе) остаётся кандидатом.

> Фича **0068** «Верификация свойств над данными» **закрыта** (`ГОТОВО`,
> 2026-07-18, **консервативное ядро** — решение заказчика) и удалена из таблицы
> (правило 10) — итог в её
> [карточке](docs/features/0068-verify-data-properties.md). Атом LTL теперь не
> только имя состояния, но и **предикат над данными** (`cond`/булев `var`):
> `G Safe` (где `cond Safe = temp <= 100;`) даёт **вердикт**, а не `Unsupported`.
> Новый модуль `verification/data_kripke.rs` (абстракция **по формуле**, Option D):
> вершина = `(состояние, оценка отслеживаемых переменных)`; `build_buchi`/
> `product`/`emptiness` не тронуты. ⚠️ **Ход данных полностью недетерминирован**
> (максимальная сверх-аппроксимация): `Holds` надёжен, но свойство над меняющимися
> данными почти всегда даёт `Violated` (возможно, ложный) — доказуемы тавтологии и
> управляющие свойства. Прецизионное вычисление хода данных (полный Option D)
> **отложено** — тот же класс риска, что стоил проекта года. Направление ошибки —
> мутационный сторож (заморозка данных → ложный `Holds` → тест падает). `float`/
> порт/арифметика/потолок → честный `Unsupported`. Язык не менялся (правило 22:
> предикат — через существующий `cond`). Побочно: `G (temp<=100)` не выразимо
> напрямую (атом LTL — идентификатор), пишется через именованный `cond`.

> Фича **0069** «Разделение `address_map.rs` по темам» **закрыта** (`ГОТОВО`,
> 2026-07-18) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0069-address-map-eval-split.md).
> `grammar/src/address_map.rs` (1116 строк) заменён каталогом `address_map/`
> (`parse`/`env`/`eval`/`resolve` + `mod.rs`-реэкспорт, форма 0027) — все файлы
> под лимитом, пути импорта у потребителей не изменились. Перекладка: вывод
> **всех** целей побайтово прежний (опора на детерминизм 0048 — главный сторож),
> `apply_binary` осталась **одной** на оба матчера. Запись снята из реестра долга:
> 22 → **21** (превышение 12049 → 11920). Фикс
> [0020-01](docs/fixes/0020-01-port-bit-out-of-range.md) (Tier 1, живёт в этом же
> файле) **не** трогался — иначе дифф вывода перестал бы быть пустым. Язык не
> менялся (правило 22). ⚠️ Урок: *предвидение «при росте вынесем» (анализ 0042)
> оценило прирост вдвое оптимистичнее факта (150–200 против ~460) — мерить надо,
> а не полагаться на «ещё влезает».*

> Фича **0066** «Литералы по целевому типу в телах `st`» **закрыта** (`ГОТОВО`,
> 2026-07-18) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0066-st-bool-literals.md). Тела цели `st` печатают
> `BOOL`/перечисления по целевому типу (`coerce_to`, как в `rust`/`sv`):
> `cmd_fork := 0` → `FALSE`, `command := 2` → `Command_Stop` — вывод перестал
> противоречить сам себе. Дифф `st` — только литералы; `iec2c` зелёный; прочие цели
> побайтово прежние; `conformance_st` вердикта не изменил. Объём расширен против
> кандидата (лечится и `enum`, не только `BOOL`). Язык не менялся (правило 22).

> Фича **0058** «Цель `rust`: хвостовой `if/else` с `return`» **закрыта**
> (`ГОТОВО`, 2026-07-18) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0058-rust-tail-return-if-else.md). Хвостовой разворот
> `return` заходит в завершающий `if/else` (предикат `tail_foldable` + рекурсивный
> `print_tail` через `print_block`); `needless_return` на этой форме снят.
> `examples/generated/rust` побайтово неизменны (A2) — корпус сворачиваемого
> хвоста не содержит, правка аддитивна. Язык не менялся (правило 22). Побочная
> находка (`new_without_default` на модели без портов) остаётся кандидатом.

> Фича **0057** «Последовательная композиция (`+`) в цели SystemVerilog»
> **закрыта** (`ГОТОВО`, 2026-07-18) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0057-sv-sequential-composition.md). `SV-002` на `+`
> снят: регистр шага + `unique case` активного шага (Option A ADR 0057),
> композиция вынесена в новый `generator/sv/sv_compose.rs`. ⚠️ Два отклонения от
> проработки, найденных при разработке: **`extend_complex` не SV-транслируем**
> (`extern fn`, не `+`) — в `SV_TRANSLATABLE` не добавлен; **симулятор не
> исполняет композицию с `next`** — заведён [фикс
> 0057-01](docs/fixes/0057-01-sim-composition-next.md) (Tier 2), сверка ведётся с
> целью C (второй эталон ADR 0057), SV↔C совпадают потактово. Урок: *эталон,
> объявленный «и C, и симулятор», требует проверки, что они совпадают.*

> Фича **0060** «Расчёт ширины перечисления — в общий слой» **закрыта**
> (`ГОТОВО`, 2026-07-18) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0060-enum-width-shared-layer.md). Закрыт **Tier 1**
> [фикс 0005-01](docs/fixes/0005-01-c-enum-signedness.md): цель `c` теряла знак
> перечисления молча (`uint8_t` вместо `int8_t`, переход мёртв). Четыре
> независимых извлечения диапазона сведены в `semantic::enum_facts`; цели лишь
> отображают факт. Заведена сверка на отрицательном перечислении (гейт `c` шёл
> без `-Werror` и дефект принимал). ⚠️ Урок: *дублирование расчёта — механизм
> расхождения, а не стиль* (четыре копии дали четыре ответа на пустое
> перечисление и два — на знак).

> Фича **0065** «Изоляция пространства имён цели `st`» **закрыта** (`ГОТОВО`,
> 2026-07-18) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0065-st-namespace-isolation.md). Закрыт **Tier 1**
> [фикс 0041-01](docs/fixes/0041-01-st-fn-dedup-silent.md): цель `st` больше не
> склеивает одноимённые `fn` (префикс POU именем модели, как `c`). Введена
> `ST-014` (столкновение со стандартной библиотекой IEC — внятно вместо
> обманчивого `iec2c`). **Долг сверки отдан:** заведена потактовая
> `conformance_st_tests` — проработка допускала вынести её в отдельную фичу, но
> первопричиной нулевой трассы оказалась ноль-инициализация структуры MatIEC
> (одна строка), и сверка сделана в объёме 0065. ⚠️ Урок: *оценка «объём
> отдельной фичи» до диагноза первопричины — гипотеза* (спайк застревал на
> `EN`-семантике, а дело было в мусорном `FORCE`-бите неинициализированной
> памяти).

> Фича **0030** «Исправление примера `comprehensive.lam`» **закрыта**
> (`ГОТОВО`, 2026-07-17) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0030-comprehensive-example-fix.md). Пример проходит
> заявленный сценарий за 172 шага; заведён **корпусной гейт достижимости**
> (контракт либо исключение с причиной — третьего не дано). Объём был расширен
> решением заказчика на починку генератора `st`; пропуск `comprehensive` в гейте
> цели `rust` снят вместе с причиной. ⚠️ Урок для правок примеров:
> *осуществимость доказывается всеми гейтами корпуса, а не только симулятором* —
> проработка гоняла эскиз симулятором и целью `c` и потому не увидела двух
> латентных дефектов генераторов.

> Фича **0027** «Разделение переросших модулей» **закрыта** (`ГОТОВО`,
> 2026-07-17) и удалена из таблицы (правило 10) — итог в её
> [карточке](docs/features/0027-module-size-split.md). Правило о размере модуля
> стало **исполнимым**: храповик `scripts/check-module-size.sh` в `precheck.sh` и
> CI. Долг 16953 → 12049 строк, нарушителей 25 → 22 — остальные **заморожены**.
> ⚠️ Правя файл из реестра, готовься к отказу «вырос»: это замысел, а не помеха.

- **0037 — в самом хвосте, наименьший приоритет** (решение заказчика,
  2026-07-17). Фича проработана до `РАЗРАБОТКА` и зависимостей не имеет, то есть
  критерии 1–3 правила 19 её не удерживают внизу — положение задано **критерием 4
  (необходимость)** в обход критерия 2. Довод: разработка и все гейты
  (`precheck.sh`, CI) идут на Unix, Windows в матрице CI отсутствует вовсе
  (пункт процессного бэклога ниже), поэтому сбои на Windows никого сегодня не
  блокируют и ни одна фича очереди от 0037 не зависит. Следствие: 0037 берётся в
  работу **последней** — после 0046, хотя та `ЗАБЛОКИРОВАНА`.
  ⚠️ Решение **сильнее критерия 2**: после перевода кандидатов в фичи (0058–0094)
  0037 стоит и позади них — то есть позади фич **без ADR и анализа**, хотя сама
  проработана до `РАЗРАБОТКА`. Это и означает «наименьший приоритет»; иначе
  проработанность вернула бы её в голову таблицы при первом же пересмотре.

- **0038 → 0039 (обе закрыты).** Порядок оправдался: 0038 приняла LSP4IJ, и
  развилка 0039 (её ADR колебался между внешним `lamc fmt --stdin` и LSP-путём)
  разрешилась в пользу LSP4IJ — форматирование пришло бесплатно от `lam-lsp`,
  0039 схлопнулась до приёмки. Оба механизма регистрируются через один EP
  `formattingService`, поэтому очерёдность и была обязательна. (Третья фича этой
  группы, 0040, отменена — см. врезку выше.)
- **0045 и 0050 шли подряд** (обе — бэкенды, Tier 2, зависимостей нет).
  Соседство было выбрано по критерию 4 (необходимость) и по пересечению правок: обе
  добавляли вариант в `#[non_exhaustive] enum Language` и ветку в `generate()` —
  конфликт слияния там тривиален, но делать их подряд дешевле, чем вперемешку с
  чужими фичами. **0050 — наименее рискованная фича очереди:** её гейт
  (`rustc` + `clippy`) уже является зависимостью проекта, тогда как у 0041 арбитр
  `iec2c` собирается из исходников, а у 0045 требует `brew install verilator
  yosys`. Порядок между 0045 и 0050 при необходимости можно поменять: они
  независимы.
- **0045 каркаса не ждёт** — общий слой бэкендов **не заводится**: `Language` уже
  `#[non_exhaustive]`, трейт `Generator` из одного метода, `minimap::Map` и
  `Printer` общие. Закрытая 0041 это подтвердила на практике: `generator/st/`
  написан на существующем каркасе, не обнажив его дефицита, — `generator/sv/`
  пойдёт тем же путём. Ценный задел для 0045: `scripts/ensure-iec2c.sh` и форма
  гейта «внешний арбитр в `precheck.sh`» переносятся почти дословно.
- **0036 → 0046.** Широкая чистка предупреждений (~125) исключает
  `private_interfaces` (19 из них) — им занимается 0036, уже с принятым ADR.
  Прямой путь там оказался невозможен (`E0449`), а выбранная инкапсуляция ломает
  публичный API крейта; в общей чистке такое решение прошло бы мимоходом и без
  обоснования. Плюс 0027 → 0046: 0027 перекладывает `c_expr.rs` и `lsp.rs`, где
  живут крупнейшие группы clippy-долга.
- **0027 в середине, а не в начале.** Аналитик 0027 просил поднять её выше
  (перекладывает три файла целиком → конфликты слияния), но проверка показала,
  что пересечения с очередью нет: 0028 правит `c_model.rs`, 0029 —
  `generator/c/mod.rs`, 0035 — `semantic/statement.rs`, а 0027 делит
  `validate.rs`/`lsp.rs`/`c_expr.rs`. Конфликт возник бы только с работой по LSP
  (0038), которая идёт позже.

### Решения заказчика (2026-07-15)

Проработка вскрыла три развилки, которые аналитик решать не вправе. Все три
закрыты:

1. **Версия языка накапливается.** Пять фич (0029, 0031, 0033, 0042, 0044)
   независимо заявили `0.2.0 → 0.3.0`, что дало бы `0.7.0` без единого выпуска.
   Решено: подъём **один** — первая закрытая фича поднимает версию, остальные
   вносят изменения в уже поднятую. Зафиксировано в **правиле 22**
   ([docs/RULE.md](docs/RULE.md)). Обязанность каждой фичи обосновать в анализе,
   меняет ли она язык, сохраняется — накопление касается только номера.
   ⚠️ Исполнение решения затруднено тем, что константы `LANGUAGE_VERSION` в коде
   **нет** (версия живёт только в `README.md`) — кандидат заведён ниже.
   **Исполнено:** подъём `0.2.0` → `0.3.0` сделала закрытая **0029** как первая
   из пяти; фичи 0031, 0033, 0042, 0044 вносят изменения в **уже поднятую**
   версию и заново её не поднимают. Коммит подъёма помечен тегом **`v0.3.0`** —
   требование тега добавлено в правило 22 решением заказчика от 2026-07-15.
2. **0040 — `ОТМЕНА`.** Фича поглощается путём LSP4IJ (0038): четыре
   возможности из шести достаются оттуда несоизмеримо дешевле, а цена
   собственного PSI — вторая реализация грамматики рядом с LALRPOP. Удалена из
   таблицы; остаточная ценность (rename, `PsiReference` для `import`) **не
   потеряна** — вынесена кандидатом ниже.
3. **0029: `--float-width=64`, то есть 8 байт.** Совпадение с f64 симулятора
   важнее неизменности `c-hal`; ширина вещественного порта `c-hal` меняется с 4
   на 8 байт — это **ожидаемый** результат, а не регресс. Опция
   `--float-width=32` остаётся для платформ, где 8-байтное чтение недопустимо.

## 2. Кандидаты в фичи

Идеи, ещё не оформленные в карточку `docs/features/XXXX-*.md`.

> **Блок пуст: весь состав переведён в фичи 0058–0094** (2026-07-17, решение
> заказчика «завести фичи по кандидатам и процессному бэклогу, пока без
> проработки»). Это второй такой перевод: прежний состав (19 кандидатов) стал
> фичами 0026–0044.
>
> Находки **не потеряны** — текст каждого кандидата вместе с подтверждающими его
> пробами перенесён в раздел «Краткое описание» своей карточки; все 37 карточек
> заведены на **стадии 1** (`СОЗДАНА`), ADR и анализа у них **нет** (правило 17).
> Реестр — [docs/features/README.md](docs/features/README.md), очередь — в
> таблице выше.
>
> **Сведено при заведении** (объединялись только очевидные дубли — решение
> заказчика): «Цель `sv-mmio`» была записана **дважды** → фича
> [0062](docs/features/0062-sv-mmio-target.md); «Имена POU цели `st` не
> изолированы» и её подпункт про обычные переменные → фича
> [0065](docs/features/0065-st-namespace-isolation.md) (лечение одно);
> два дефекта `scripts/new-feature.sh` → фича
> [0094](docs/features/0094-new-feature-script-fixes.md); «подключить
> `check-links.py` в CI» поглощён пунктом «CI не запускает `precheck.sh`» → фича
> [0090](docs/features/0090-ci-precheck.md) (скрипт уже **часть** предкоммита);
> правило о запрете `_ =>` и конфликт с `#[non_exhaustive]` → фича
> [0093](docs/features/0093-wildcard-match-rule.md) (один инвариант).
>
> **Не переведены** (по одному на причину): пункт «Windows отсутствует в матрице
> CI» — остаётся в объёме фичи [0037](docs/features/0037-windows-test-failures.md)
> (задача 0037-03), заводить вторую фичу на ту же работу нельзя (решение
> заказчика); пункт «по мере проработки фич 0018–0020 заводить
> ADR/анализ/тест-планы» — **устарел** (все три `ГОТОВО`), его живой остаток —
> фича [0092](docs/features/0092-adr-0018-retrofit.md).
>
> Отсюда же **удалены зачёркнутые врезки** о кандидатах, которые уже стали
> закрытыми фичами (0048, 0049, 0051, 0052, 0053, 0055, 0056, а также фиксы 0031
> и 0033): информация о **сделанном** живёт только в карточке фичи (правила 10 и
> 21), а блок 2 — про **кандидатов**.

Новые находки записывать сюда: подтверждать пробой или чтением кода и указывать,
при какой работе вскрыта, — как делалось выше.

- **Второй вычислитель `unit/builder.rs::eval_expr` с `_ => None`** (подтверждено
  при закрытии [0093](docs/features/0093-wildcard-match-rule.md); граница ADR 0093).
  Вне модуля `eval/` и вне действия `#![deny(clippy::wildcard_enum_match_arm)]`
  живёт **второй** вычислитель над `ExpressionNode` с подстановочными ветками
  (`builder.rs:215/227/341/503`) — тот же класс, что дал 8 дефектов фичи 0025:
  добавление варианта не ломает сборку, вычислитель молча вернёт `None`. Второй
  вычислитель **намеренно не слит** с адаптерами (риск порт-регресса, 0080), но
  исчерпываемость можно навести и без слияния: item-level
  `#[deny(clippy::wildcard_enum_match_arm)]` на `eval_expr` заставит перечислить
  варианты явно (поведение то же — `_ => None` → явные варианты → `None`). ⚠️
  Территория фичи **0034** (задача 0034-04); гейт 0093 её **не** закрывает (он
  защищает пометку узла и `deny` в `eval/`, но не ловит `_ =>` в `builder.rs`).

- **`new-feature.sh --rebuild`: пересборка реестра из файлов** (выявлено при
  закрытии [0094](docs/features/0094-new-feature-script-fixes.md); Option B ADR
  0094, отложено). Реестры `docs/*/README.md` не пересобираются по факту файлов:
  переименование подзадачи аналитиком оставляет **битую ссылку**, а рассинхрон
  реестра с диском ничем не ловится (кроме `check-links.py` на битую ссылку).
  Нужен режим, реконструирующий строки реестра из markdown-файлов на диске.
  ⚠️ **Ненадёжно в POSIX sh:** извлечение заголовка/статуса/ссылок из markdown —
  хрупкий парсинг, свой формат строки у каждого реестра; риск испортить реестр.
  Кандидат на реализацию **вне** POSIX sh (напр. отдельный проверочный скрипт на
  python3, как `check-links.py`), либо как гейт «реестр ↔ файлы согласованы» без
  автопочинки. Повторяющуюся боль (дубли) 0094 уже сняла идемпотентностью — это
  остаточная, более редкая проблема.

- **Подкоманда `lamc version`** (выявлено при закрытии
  [0085](docs/features/0085-language-version-constant.md); Option B ADR 0085).
  Версия языка теперь есть в коде (`grammar::LANGUAGE_VERSION`), но `lamc` её **не
  печатает** — CLI-контракт был бы чище, чем гейт-греп исходника, и полезен
  пользователю. Заблокировано **baseline размера** `lamc.rs` (ровно 1993,
  `module-size-baseline.txt`): диспетч подкоманды растит файл, а расти ему нельзя
  (ADR 0027). Лечение — после появления запаса в `lamc.rs` или его рефакторинга
  (вынести часть логики в библиотеку, как `address-map` → `run_export_subcommand`),
  затем добавить `lamc version` (печать `LANGUAGE_VERSION` + `CARGO_PKG_VERSION`) и
  переключить гейт `check-language-version.sh` на сверку вывода CLI вместо грепа
  исходника.

- **Нет корпусного SV-примера на последовательную композицию `+`** (выявлено при
  закрытии [0057](docs/features/0057-sv-sequential-composition.md)). Единственный
  пример корпуса с `+` — `extend_complex.lam` — **не SV-транслируем** (`extern
  fn has_flag` → `SV-005`, struct, битовый доступ), поэтому `+` в цели `sv` **не
  покрыт гейтом `precheck.sh`** (`SV_TRANSLATABLE="stacker elevator_mini"`, обе на
  `|`). Гейт `yosys synth` на `+` прогнан **вручную** (проба 2026-07-18) и через
  `verilator` в потактовой сверке `conformance_sv_tests`, но в `precheck.sh` `+`
  на синтез **не подаётся**. Лечение: добавить маленький SV-транслируемый пример с
  `+` в `examples/` и в `SV_TRANSLATABLE`. ⚠️ Цена — рябь: новый пример требует
  контракта/исключения в `examples_scenario_tests`, канона `fmt`, генерации во все
  цели. Класс — «дыра в покрытии гейта», смежно с
  [0090](docs/features/0090-ci-precheck.md). ⚠️ Пока не закрыт [фикс
  0057-01](docs/fixes/0057-01-sim-composition-next.md) (симулятор не исполняет `+`
  с `next`), пример на `+` не пройдёт `examples_scenario_tests` без исключения.

- **Цель `c` не эмитит констант перечисления вовсе** (выявлено при проработке
  [0066](docs/features/0066-st-bool-literals.md), греп по порождённому `.h` —
  пусто). Поэтому `main->command = 2;` там **безальтернативно**: восстанавливать
  имя варианта нечем. В отличие от `st` (которая константы **объявляет** и тут же
  не использует), вывод `c` внутренне **согласован** — он просто беднее: читатель
  порождённого C не может узнать, что `2` — это `Stop`, не заглянув в `.lam`.
  Для сравнения: `rust` даёт `Command::Stop`, `sv` — `COMMAND_STOP`, обе
  восстанавливают вариант по целевому типу. Смежно с
  [0075](docs/features/0075-lib-src-reference-model.md) и
  [0080](docs/features/0080-c-struct-defects.md).

- **Потактовой сверки для цели `st` НЕ СУЩЕСТВУЕТ** (выявлено при проработке
  [0065](docs/features/0065-st-namespace-isolation.md)). Есть
  `conformance_c_tests`, `conformance_sv_tests`, `conformance_rust_tests` — `st`
  **нет**. То есть поведение порождённого ST **никогда не проверялось**: гейт
  `iec2c` доказывает, что вывод **компилируется**, но **не** что он **верен**.
  ⚠️ Это не теория — ровно так дожил [фикс 0041-01](docs/fixes/0041-01-st-fn-dedup-silent.md)
  (Tier 1): `iec2c` молча-неверный ST **принял** (rc=0). Правило проекта («заводить
  сверку вместе с бэкендом», уроки 0045/0050) для `st` **не исполнено** — это долг
  с фичи 0041. Трудность: `iec2c` **транслирует** ST в C (`POUS.c`/`POUS.h`), но
  runtime не прилагается — нужен драйвер, вызывающий функцию такта. Взято задачей
  [0065-03](docs/development/0065-03-st-conformance.md); ⚠️ если объём окажется
  несоизмерим, **выделять в отдельную фичу** — Tier 1 ждать не должен.

- **Предупреждения генераторов печатаются `eprintln!` ИЗ БИБЛИОТЕКИ и не
  возвращаются вызывающему** (выявлено при проработке
  [0064](docs/features/0064-sv-divider-warning.md), греп + чтение кода).
  `report` (`rust/mod.rs:202`, `st/mod.rs:293`) печатает диагностику **прямо в
  stderr из крейта `grammar`**, а `compile_to_sv`/`compile_to_rust` объявлены
  `Result<(), Diagnostic>` — предупреждений **не отдают** вовсе. Три следствия:
  (1) **`--quiet` их не глушит** (греп по `generator/`: слова `quiet` нет);
  (2) **LSP** получит их в свой stderr, а не покажет пользователю как диагностику;
  (3) **тест** может проверить факт предупреждения только перехватом stderr — то
  есть сторож хрупкий. Лечение: возвращать `Vec<Diagnostic>` из `compile_to_*`
  (как уже делает `compile_to_st_at` — «Возвращает `Ok(warnings)`»), а печать
  оставить бинарнику. ⚠️ Смежно с [0081](docs/features/0081-lamc-print-warnings.md),
  но это **другой канал**: 0081 про семантические Ce13/Ce14, которые CLI не
  вызывает; здесь — про целевые, которые библиотека печатает сама.

- **Цель `c-hal` не компилируется НИ ОДНИМ гейтом** (выявлено при проработке
  [0062](docs/features/0062-sv-mmio-target.md)). В `scripts/precheck.sh` она
  встречается **единственный раз** — в гейте **детерминизма** (строка 118: два
  прогона + `diff`): её вывод сверяют **сам с собой**, но компилятору **никогда не
  подают**. Каталога `c-hal` в `examples/generated/` нет вовсе (гейт `c` собирает
  только `examples/generated/c`). Именно это позволило дожить
  [фиксу 0020-01](docs/fixes/0020-01-port-bit-out-of-range.md) (Tier 1: UB в
  дефолтном HAL). ⚠️ Особенно обидно, что `c-hal` — **единственная цель, ради
  которой адреса вообще существуют**: то есть не проверяется ровно то, что
  уникально. Класс — «дыра **в покрытии**, а не в коде» (уроки 0026, 0005-01).
  Смежно с [0090](docs/features/0090-ci-precheck.md).

- **Адаптеры шин для цели `sv-mmio`** (вынесено проработкой
  [0062](docs/features/0062-sv-mmio-target.md), A-2 ADR). Ядро 0062 порождает
  **шинно-агностичный** регистровый файл — именно затем, чтобы не выбирать
  протокол без требования (возражение ADR 0045: «выбор произволен, а цена ошибки
  высока»). Адаптеры **APB**, **AXI-Lite**, **Wishbone** — по одной фиче на
  протокол, каждая **по требованию заказчика** (протокол диктует платформа: AXI-Lite
  — экосистема AMD/Xilinx, APB — ARM, Wishbone — открытые ядра). Адаптер тонкий
  (десятки строк) и платформенный — ему и место снаружи ядра.

- **Насыщение (saturation) для fixed-point `q(m,n)`** (вынесено проработкой
  [0061](docs/features/0061-fixed-point-type.md), правило 3 ADR). Ядро 0061
  фиксирует **wraparound** при `+`/`−` — как у целых типов языка. Но для
  регуляторов (типовой потребитель fixed-point) нужнее **насыщение**: перенос
  превращает «чуть перелетели уставку» в «прыгнули на другой край диапазона».
  Заводить **после** 0061 и **не молча**: правило 3 фиксирует wraparound **явно**
  именно затем, чтобы автор не принял умолчание за обещание.

- **Расхождение `>>` между целями непреодолимо в принципе** (вскрыто проработкой
  [0061](docs/features/0061-fixed-point-type.md), проба + чтение стандарта).
  В **C** сдвиг вправо знакового **отрицательного** — **implementation-defined**
  (C11 6.5.7p5; проба на clang даёт арифметический сдвиг — но это выбор
  компилятора, не гарантия). В **Rust** — **определён** как арифметический.
  В **ST** сдвига над числами **нет вовсе** (`SHL`/`SHR` только над битовыми
  строками, арифметика над ними запрещена, `<<` не существует). В **SV** —
  определён. Следствие: любая будущая фича, вводящая `<<`/`>>` **в язык Lam**,
  обязана задать семантику **нормативно** и не может «просто транслировать
  оператор» — четыре цели дадут четыре ответа, и три из них молча. Записано
  заранее, чтобы не открывать это в третий раз (первый — 0041 про биты в ST,
  второй — 0061).

- **Гейт цели `c` идёт без `-Werror` — сторож есть, но выключен** (выявлено при
  проработке [0060](docs/features/0060-enum-width-shared-layer.md)). Гейт
  (`cmake -DCMAKE_BUILD_TYPE=Debug -G Ninja` + `ninja`) принял порождённый C с
  **тождественно ложным** условием (`if (model->lv == -5)` при `uint8_t lv`),
  хотя `cc` это **видит** и предупреждает:
  `-Wtautological-constant-out-of-range-compare: comparison … is always false`.
  Именно так дефект [0005-01](docs/fixes/0005-01-c-enum-signedness.md) (Tier 1)
  дожил незамеченным. Класс — урок 0045 в чистом виде: «гейт доказывает, что вывод
  компилируется, но **не** что он верен», только здесь инструмент **уже** сказал
  правду, а гейт её проигнорировал. Лечение: `-Werror` (или хотя бы точечно
  `-Wtautological-*`, `-Wsign-conversion`) в сборке порождённого C. ⚠️ Прежде чем
  включать — прогнать: корпус может дать предупреждения, и тогда это отдельная
  работа. Смежно с [0090](docs/features/0090-ci-precheck.md) (CI не гоняет гейты
  вовсе).

- **Пустое перечисление `enum E { }` трактуется четырьмя целями по-разному**
  (выявлено при проработке [0060](docs/features/0060-enum-width-shared-layer.md),
  чтением кода). `c` → `uint8_t` (`unwrap_or_default`), `st` → `USINT`
  (`unwrap_or(0)`), `rust` → `u8` (`unwrap_or(0)`), `sv` → **`SV-004`** (ошибка).
  Один вход — четыре ответа, из них **три молчат**. Это вопрос **семантики языка**
  («допустимо ли перечисление без вариантов, и что значит переменная такого
  типа?»), а не кодогенерации, поэтому 0060 его сознательно **не решает** и лишь
  сохраняет сегодняшнее поведение дословно (прецедент: 0029 так же не решала
  семантику `[bit;N]`). Лечение — решение о семантике + единая диагностика
  (`SE-xxx`) вместо четырёх ответов. Смежно с
  [0078](docs/features/0078-bit-array-semantics.md) (тот же класс: язык не
  определил случай, цели догадались по-разному).

- **Сам генератор `rust` глушит `too_many_arguments` пять раз** (выявлено при
  проработке [0059](docs/features/0059-rust-shared-struct.md)).
  `rust_model.rs:389, 598, 695, 830, 1204` — `#[allow(clippy::too_many_arguments)]`
  на **собственных** функциях генератора (печатники получают по 8–10 аргументов).
  Политику (а) ADR 0050 это **не нарушает** (та — про порождаемый код), но
  воспроизводит ровно ту болезнь, которую 0059 лечит в выводе, — уровнем выше.
  В объём 0059 не взято: другой объём (код генератора, не вывод), смешение сделало
  бы дифф нечитаемым. Смежно с [0046](docs/features/0046-build-warnings-cleanup.md)
  (чистка предупреждений) и [0088](docs/features/0088-module-size-remaining.md)
  (`rust_model.rs` — 1361 строка, из реестра долга).

- **Цель `rust`: корневая модель БЕЗ портов не проходит гейт —
  `clippy::new_without_default`** (выявлено при проработке
  [0058](docs/features/0058-rust-tail-return-if-else.md), подтверждено
  контрольной пробой). Модель без единого порта даёт `pub fn new() -> Self` **без
  аргументов**, и `clippy -D warnings` требует `impl Default`. Контрольная проба:
  один добавленный порт → `pub fn new(hal: H)` → аргумент есть → линт **молчит**,
  гейт зелёный. То есть триггер — именно **отсутствие портов** у корневой модели.
  ⚠️ Корпус слеп к этому по **двум** причинам сразу: у **каждого** примера есть
  порты, а `new()` под-модели **приватен** (линт срабатывает только на публичных).
  Класс — ровно как у закрытой [0026](docs/features/0026-c-root-typedef.md): дыра
  **не в коде, а в покрытии**, и снова на **простейшем** классе моделей (одиночная
  модель без под-моделей и без портов). Лечение — решение того же рода, что
  принимала 0050 (политика (а): не эмитить то, на что линт ругается): эмитить
  `impl Default` рядом с `new()` либо не делать `new()` публичным. В объём 0058 не
  взято: другой механизм, другое лечение.

- **Дожать CI-верификацию [0090](docs/features/0090-ci-precheck.md) после снятия
  биллинг-блокировки** (вынесено закрытием 0090). Фича закрыта **по локальной
  проверке**: `ci.yml` переписан на единый вызов `precheck.sh` под строгим
  режимом, но **вживую в Actions не проверен** — job не стартует, аккаунт GitHub
  **заблокирован по биллингу** (`The job was not started because your account is
  locked due to a billing issue`; те же 3-секундные падения у прогонов 0062/0082).
  Непроверенными остались A5/T2/T3/T11 (тест-план 0090). Когда Actions
  разблокируют: (1) `gh run rerun` последнего прогона по `v2` (или новый push);
  (2) убедиться, что apt-версии `verilator`/`yosys` на `ubuntu-latest` тянут
  `--binary`/`--timing` тестбенчей и `synth` (единственный неснятый локально
  риск — расхождение версий инструментов); (3) при красном — точечно поправить
  установку инструментов в `ci.yml`. Класс — «долг верификации», не дефект кода.

- **Позиция бита у `bit`-порта с голым адресом** (вынесено закрытием
  [0070](docs/features/0070-port-initializer-address-role.md), смежно 0098). После
  0070 `in P: bit := 0x100;` валиден, но `c-hal` кладёт **бит −1** (нет позиции
  бита в слове) — для однобитного порта это неполно: HAL читает бит по позиции. Та
  же тема — ловушка «`bit := 0/1` на bit-порту — молча адрес 0/1, а не значение».
  Лечение (решение о семантике): либо умолчание бит 0, либо явная диагностика «у
  bit-порта адрес обязан иметь позицию `0xADDR:бит`». В объём 0070 не взято
  (Option A — минимум; отвергнутый Option B её включал, но ломал 3 регулятора
  корпуса `out ready: bit := 0`).

## 3. Процессный бэклог

Улучшения процесса разработки, инфраструктуры и лучших практик.

> **Блок пуст: весь состав переведён в фичи** (2026-07-17, то же решение
> заказчика). Процессные фичи проходят тот же жизненный цикл и попадают в ту же
> таблицу; их приоритет определяется по влиянию на разблокировку других (правило
> 19, критерий 3):
>
> - [0090](docs/features/0090-ci-precheck.md) — CI прогоняет весь `precheck.sh`
>   (живые гейты `c` и `st`, `check-links.py`);
> - [0091](docs/features/0091-module-size-rule-in-code-md.md) — правило о размере
>   модуля перенести в `docs/CODE.md`;
> - [0092](docs/features/0092-adr-0018-retrofit.md) — у фичи 0018 нет ADR;
> - [0093](docs/features/0093-wildcard-match-rule.md) — правило о запрете `_ =>`
>   + конфликт с `#[non_exhaustive]`;
> - [0094](docs/features/0094-new-feature-script-fixes.md) — доработка
>   `scripts/new-feature.sh`.
>
> Пункт про матрицу Windows остаётся за фичей 0037 (задача 0037-03), пункт про
> `check-links.py` в CI поглощён фичей 0090 — подробности во врезке блока 2.

Новые улучшения процесса записывать сюда до заведения фичи.
