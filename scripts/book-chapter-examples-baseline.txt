# Узаконенный долг гейта scripts/check-book-chapter-examples.py (фича 0521).
#
# Блоки глав, у которых обещание не проверяется прогоном по устройству самого
# примера. Список — ратчет: запись можно только удалять (дополнив пример до
# самодостаточного), добавлять — нет.
#
# ⚠️ Запись, которая начала проходить, роняет прогон: иначе долг растёт молча.
#
# Формат: путь#номер-блока-в-файле | причина
03-types/index.typ#10 | «var a: q(4, 4) sat := 7.0;» — ошибочная строка показана ЗАКОММЕНТИРОВАННОЙ (`// r := a + b; // SE-059`), в самом примере ошибки нет
05-expressions/index.typ#6 | «level := 1;» — перечень мест записи; объявления опущены намеренно, поэтому до обещанного SE-111 приходит SE-003
