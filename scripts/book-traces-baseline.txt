# Узаконенный долг гейта scripts/check-book-traces.py (фича 0522).
#
# Трассы, которые прогоном не воспроизводятся по устройству самого примера.
# Список — ратчет: запись можно только удалять, добавлять — нет.
#
# ⚠️ Запись, которая начала сходиться, роняет прогон: иначе долг растёт молча.
#
# Формат: путь#номер-трассы-в-файле | причина
12-time/index.typ#1 | выбег вентилятора — 3 минуты при 1 кГц, то есть 180 000 тактов; входы задаются сценарием, а прогон обрывается на его последнем шаге, поэтому файл пришлось бы завести на 180 000 записей (см. кандидата «прогон обрывается по длине сценария»)
