# Узаконенный долг гейта scripts/check-book-diagnostic-examples.py (фича 0520).
#
# Пары «пример → ожидаемый вывод», которые прогоном НЕ воспроизводятся, потому
# что пример намеренно не является программой. Список — ратчет: запись можно
# только удалять (дополнив пример до самодостаточного), добавлять — нет.
#
# ⚠️ Запись, которая начала воспроизводиться, гейт объявляет протухшей и роняет
# прогон: иначе долг растёт молча и перестаёт быть долгом.
#
# Формат: КЛЮЧ | причина
SE-062 | предел вложенности показан многоточием («несколько десятков вложенных скобок»): реальный вход — сотни знаков, и в книге он нечитаем
SY-005 | то же, что SE-062: сотни вложенных скобок заменены многоточием
