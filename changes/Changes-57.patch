diff --git a/README.md b/README.md
index d887fd3..8dae70c 100644
--- a/README.md
+++ b/README.md
@@ -810,11 +810,11 @@ LSP-сервер запускается редактором автоматич
    при работе с разными размерами массивов неизбежно
 3. **Отсутствие стандартной библиотеки** — нет встроенного модуля `std` или `core` с типовыми алгоритмами
    для встраиваемых систем (Ce7)
-4. **68 предупреждений clippy** — накопленные предупреждения (22 `collapsible_if`, 13 `.clone()` на Copy-типах `Location`/`u64`, 3 `Ok(x?)` и др.); рефакторинг улучшит читаемость и производительность
+4. **~100 предупреждений clippy** — накопленные предупреждения (`collapsible_if`, `.clone()` на Copy-типах `Location`/`u64`, `Ok(x?)` и др.); рефакторинг улучшит читаемость и производительность
 5. **Встроенные функции: лишние скобки в аргументах** — шаблоны `min`/`max`/`abs`/`clamp`
    оборачивают аргументы в `({arg})` вместо использования `generate_expr` с приоритетами
-6. **`panic!()` в семантическом анализе** — 19 вхождений `panic!()` в production-коде
-   (`builtin.rs`, `expression.rs`, `extend.rs`, `function.rs`) — должны быть диагностическими ошибками;
+6. **`panic!()` в семантическом анализе** — ~45 вхождений `panic!()` в production-коде
+   (`builtin.rs`, `expression.rs`, `extend.rs`, `function.rs`, `statement.rs` и др.) — должны быть диагностическими ошибками;
    паника при ошибочных входных данных недопустима в компиляторе
 7. **Незащищённые `unwrap()` в LSP и генераторе C** — `lsp.rs:1298`, `c_source.rs:201` и ряд других
    мест могут вызвать панику при неожиданных входных данных вместо возврата диагностики
@@ -851,10 +851,10 @@ fn max(a: u8, b: u8) -> u8;
 
 #### Clippy: устранение предупреждений компилятора
 
-Провести рефакторинг для устранения 68 предупреждений `cargo clippy`:
-- Свернуть 21 вложенный `if` в `if ... && ...`
-- Убрать 13 клонов Copy-типов (`Location`, `u64`, `StateNodeKind`)
-- Устранить 3 лишних обёртки `Ok(x?)`
+Провести рефакторинг для устранения ~100 предупреждений `cargo clippy`:
+- Свернуть вложенные `if` в `if ... && ...`
+- Убрать клоны Copy-типов (`Location`, `u64`, `StateNodeKind`)
+- Устранить лишние обёртки `Ok(x?)`
 - Прочие мелкие улучшения качества кода
 
 #### Встроенные функции: использование `generate_expr` с приоритетами
@@ -864,7 +864,7 @@ fn max(a: u8, b: u8) -> u8;
 
 #### Рефакторинг `panic!()` → диагностические ошибки
 
-В `builtin.rs`, `expression.rs`, `extend.rs`, `function.rs` находится 19 вхождений `panic!()` в ветках
+В `builtin.rs`, `expression.rs`, `extend.rs`, `function.rs`, `statement.rs` и других модулях находится ~45 вхождений `panic!()` в ветках
 семантического анализа, которые срабатывают при неожиданных входных данных. Каждый `panic!()` должен
 быть заменён на `Err(Diagnostic::error(loc, ...))` с передачей в конвейер диагностик вместо
 аварийного завершения процесса.
@@ -962,6 +962,8 @@ return Err(Diagnostic::error(loc, "Неожиданный тип аргумен
 | Changes-51-P01 | Кодогенератор C: исправление ошибок компиляции gcc — добавлен `resolve_simple_var_in_context` для корректного разрешения переменных (same-model/tick, root-from-nested, local-fn); `append = ", model"` для root-контекста; `const Root *main` в локальных функциях; prepend `main`/`model` как первый аргумент вызовов Local-функций в условиях | ✅ Исправлено |
 | Changes-51-P02 | Кодогенератор C: параметр `has_model: bool` пробросан через `generate_code_block`, `generate_stmt_expression`, `generate_expr`; обновлены тесты `test_init_parallel_generates_init_calls`, `test_init_concatenation_generates_first_init_only`, `test_tick_concatenation_generates_state_chain`, `test_submodel_variable_uses_state_field_name` | ✅ Исправлено |
 | Changes-52    | Кодогенератор C: единое терминальное состояние `_END` — все терминальные состояния получают явный переход в `_END`; `_is_done` всегда проверяет `_END`; удалён FIXME из auto-generated `_END`-кейса; 4 новых unit-теста | ✅ Реализовано |
+| Changes-56    | Кодогенератор C: вызов `extern` функций в `always`/`enter`/`exit`-блоках; исправлен порядок семантических этапов (функции разрешаются до именованных блоков); исправлено обращение к локальным переменным (`loc == Implicit` → по имени, не через `model->`); исправлен доступ к переменным родительской модели в локальных функциях (`model->field.var` вместо `main->field.var`); 3 новых интеграционных теста и 2 unit-теста | ✅ Реализовано |
+| —             | Скрипт `generate_examples.sh`: компиляция всех `.but` из `examples/` в `grammar/.output/` через `butc`; проверка gcc для всех примеров | ✅ Добавлено |
 | —             | SemanticIndex: поиск узла по LSP-позиции (LSP1)                               | ✅ Реализовано |
 | —             | `textDocument/declaration`: переход к декларации (LSP2)                       | ✅ Реализовано |
 | —             | `textDocument/documentSymbol`: структура документа (LSP3)                     | ✅ Реализовано |
@@ -1398,7 +1400,7 @@ start T {
 | #  | Приоритет | Местоположение | Описание | Статус |
 |----|-----------|----------------|----------|--------|
 | I1 | 🔴 Высокий | `generator/c/c_source.rs` | **Неполная генерация тел функций**. `_tick` не содержит обработки переходов, `enter`/`exit`/`always`-блоков, записи портов. Сгенерированный C-код функционально неполон. | 🔶 Частично: безусловные переходы для `Element::State` реализованы (Changes-50); `enter`/`exit` вызываются в правильном порядке; `always` генерируется до проверок; `break` добавлен в if-блоки. Условные переходы и запись портов — открыто. |
-| I2 | 🟡 Средний | `generator/c/c_source.rs` | **`todo!()` для вызовов функций** (`ConditionNode::Function`, `ExpressionNode::Function`). | 🔶 Частично: `ConditionNode::Function` для `Local`/`External` функций генерирует вызов в условиях перехода (Changes-50); неразрешённые функции возвращают `Err` вместо паники; `ExpressionNode::Function` — открыто. |
+| I2 | 🟡 Средний | `generator/c/c_source.rs` | **`todo!()` для вызовов функций** (`ConditionNode::Function`, `ExpressionNode::Function`). | ✅ Реализовано: `ConditionNode::Function` для `Local`/`External` (Changes-50); `ExpressionNode::Function` для `extern` и локальных функций в `always`/`enter`/`exit` (Changes-56); порядок семантических этапов исправлен. |
 | I3 | 🟡 Средний | `generator/c/c_source.rs` | **`todo!()` для `ConditionNode::State`** — `S(Model)` в условиях генерирует панику. | 🔶 Частично: паника устранена — возвращается `Err` и генерируется `//TODO` комментарий (Changes-50). |
 | I4 | 🟡 Средний | `generator/c/c_source.rs` | **`todo!()` для `ExpressionNode::Model`** — вложенная модель как значение вызывает панику. | 🔲 Открыто |
 | I5 | 🟡 Средний | `semantic/validate.rs` | **Нет проверки рекурсивных псевдонимов типов**. `type A = [A; 8];` вызывал stack overflow. | ✅ Реализовано: Ce16, DFS-обход графа зависимостей псевдонимов. |
@@ -1415,7 +1417,10 @@ start T {
 | A1 | 🔴 Высокий | `semantic/statement.rs:100` | Параметры функции не передавались в `Expression`-ветку — `construct_expression` вызывался с `vec![]` вместо `params.clone()`, из-за чего `value > 100` или `out = value` в теле функции давали LSP-ошибку «идентификатор не найден» | Передавать `params.clone()` (исправлено в Changes-39) | ✅ Исправлено |
 | A2 | 🟡 Средний | `semantic/validate.rs` (3130 строк) | Монолитный модуль, трудно поддерживать и тестировать изолированно | Разбить на `validate_refs.rs`, `validate_types.rs`, `validate_determinism.rs` | 🔲 Открыто |
 | A3 | 🟡 Средний | `lsp.rs` (~90 KB) | LSP-сервер — монолитный файл; добавление новых capability затруднено | Выделить `lsp/hover.rs`, `lsp/completion.rs`, `lsp/diagnostics.rs` | 🔲 Открыто |
-| A4 | 🟡 Средний | `generator/c/c_source.rs` (~58 KB) | Кодогенератор — монолитный файл | Разбить на `c_stmt.rs`, `c_expr.rs`, `c_func.rs` | 🔲 Открыто |
+| A4 | 🟡 Средний | `generator/c/` | Кодогенератор — был монолитным | Разбит на `c_expr.rs`, `c_model.rs`, `c_decl.rs`, `c_header.rs`, `c_source.rs`, `c_map.rs` | ✅ Выполнено |
 | A5 | 🟢 Низкий | Весь проект | `unwrap()` в критических местах — потенциальные паники в production | Заменить на `?` или `expect("сообщение")` с диагностическим текстом | 🔲 Открыто |
 | A6 | 🔲 Открыто | — | C2: перегрузка `=` в условиях (`cond A = x = 0`) неоднозначна | Ввести `==` в грамматику для `cond`/`ref` | 🔲 Открыто |
 | A7 | 🔲 Открыто | — | Ce7: нет стандартной библиотеки для встраиваемых систем | Создать модуль `std.but` с `clamp`, `min`, `max`, `abs` | 🔲 Открыто |
+| A8 | 🟡 Средний | `generator/c/c_model.rs:302` | Условные переходы для составных состояний (`StateExtend::Model`, `StateExtend::State`) генерируют `//TODO` комментарий вместо кода | Реализовать генерацию условных переходов или вернуть ошибку диагностики | 🔲 Открыто |
+| A9 | 🟡 Средний | `generator/c/c_model.rs:735` | Ветка обработки неизвестных элементов в tick-генераторе содержит `//FIXME: Пока не реализовано` | Реализовать обработку или вернуть `Err` с диагностикой | 🔲 Открыто |
+| A10 | 🟢 Низкий | `lsp_demo.but` | Пример `lsp_demo.but` объявляет `extern fn log(...)` — конфликт с C `log()` из `math.h` | Переименовать в `log_msg` или аналогичное | 🔲 Открыто |
