diff --git a/PLAN.md b/PLAN.md
index c2d17a1..f5aa05a 100644
--- a/PLAN.md
+++ b/PLAN.md
@@ -1,7 +1,9 @@
 # План
 
-1. Для генератора сишного кода и общего механизма работы примени следующие изменения. Для имен `enum` констант не надо к
-   полному имени в uppercase добавлять префикс с именем модели и типом порта. Пример: вместо
-   `ElevatorMini_BitPort_ELEVATOR_MINI_CABIN_FLOOR_BUTTON_F7` должно получиться `ELEVATOR_MINI_CABIN_FLOOR_BUTTON_F7`.
-   Придерживайся написанному в `AGENTS.md`
+1. Необходимо поменять работу с портами моделей. Следует разделить порты ввода и порты вывода. Добавить отдельный подтип
+   к уже имеющемуся классу портов. Сделать изменения в синтаксисе: убрать ключевое слово `port`, вместо него добавить
+   `in` и `out`. Где `in` - входные порты, `out` - выходные порты. Сделать семантическую проверку что в порты `in` не
+   могут быть записаны значения, а в `out` - только прочитаны. При генерации сишного кода также сделать разделение
+   `enum`, для имен перечислений `enum` добавить информацию о принадлежности к `in` или `out`. В функциях работы с
+   портами, также, передавать нужный `enum`. Придерживайся написанному в `AGENTS.md`
 
diff --git a/examples/elevator.but b/examples/elevator.but
index 0b44526..e53cb91 100644
--- a/examples/elevator.but
+++ b/examples/elevator.but
@@ -36,36 +36,36 @@ type u8 = [bit;8];  // 8-битный целый тип без знака
 
 // ══ ПОРТЫ ДАТЧИКОВ ПОЛОЖЕНИЯ КАБИНЫ (1 порт на этаж, 2 бита) ════════════════
 
-port sensors_1: u8 = 0x10000000;  // этаж 1: бит 0 = lo, бит 1 = hi
-port sensors_2: u8 = 0x10000001;  // этаж 2: бит 0 = lo, бит 1 = hi
-port sensors_3: u8 = 0x10000002;  // этаж 3: бит 0 = lo, бит 1 = hi
-port sensors_4: u8 = 0x10000003;  // этаж 4: бит 0 = lo, бит 1 = hi
-port sensors_5: u8 = 0x10000004;  // этаж 5: бит 0 = lo, бит 1 = hi
-port sensors_6: u8 = 0x10000005;  // этаж 6: бит 0 = lo, бит 1 = hi
-port sensors_7: u8 = 0x10000006;  // этаж 7: бит 0 = lo, бит 1 = hi
-port sensors_8: u8 = 0x10000007;  // этаж 8: бит 0 = lo, бит 1 = hi
-port sensors_9: u8 = 0x10000008;  // этаж 9: бит 0 = lo, бит 1 = hi
+in sensors_1: u8 = 0x10000000;  // этаж 1: бит 0 = lo, бит 1 = hi
+in sensors_2: u8 = 0x10000001;  // этаж 2: бит 0 = lo, бит 1 = hi
+in sensors_3: u8 = 0x10000002;  // этаж 3: бит 0 = lo, бит 1 = hi
+in sensors_4: u8 = 0x10000003;  // этаж 4: бит 0 = lo, бит 1 = hi
+in sensors_5: u8 = 0x10000004;  // этаж 5: бит 0 = lo, бит 1 = hi
+in sensors_6: u8 = 0x10000005;  // этаж 6: бит 0 = lo, бит 1 = hi
+in sensors_7: u8 = 0x10000006;  // этаж 7: бит 0 = lo, бит 1 = hi
+in sensors_8: u8 = 0x10000007;  // этаж 8: бит 0 = lo, бит 1 = hi
+in sensors_9: u8 = 0x10000008;  // этаж 9: бит 0 = lo, бит 1 = hi
 
 // ══ ПОРТ ДАТЧИКОВ КАБИНЫ (2 бита в одном регистре) ══════════════════════════
 
-port sensors_cab: u8 = 0x10000009;  // бит 0 = присутствие, бит 1 = перегрузка
+in sensors_cab: u8 = 0x10000009;  // бит 0 = присутствие, бит 1 = перегрузка
 
 // ══ ПОРТЫ КНОПОК ВЫЗОВА НА ЭТАЖАХ (1 порт на этаж, 2 бита) ════════════════
 
-port btns_floor_1: u8 = 0x20000000;  // этаж 1: бит 0 = вверх, бит 1 = вниз
-port btns_floor_2: u8 = 0x20000001;  // этаж 2: бит 0 = вверх, бит 1 = вниз
-port btns_floor_3: u8 = 0x20000002;  // этаж 3: бит 0 = вверх, бит 1 = вниз
-port btns_floor_4: u8 = 0x20000003;  // этаж 4: бит 0 = вверх, бит 1 = вниз
-port btns_floor_5: u8 = 0x20000004;  // этаж 5: бит 0 = вверх, бит 1 = вниз
-port btns_floor_6: u8 = 0x20000005;  // этаж 6: бит 0 = вверх, бит 1 = вниз
-port btns_floor_7: u8 = 0x20000006;  // этаж 7: бит 0 = вверх, бит 1 = вниз
-port btns_floor_8: u8 = 0x20000007;  // этаж 8: бит 0 = вверх, бит 1 = вниз
-port btns_floor_9: u8 = 0x20000008;  // этаж 9: бит 0 = вверх, бит 1 = вниз
+in btns_floor_1: u8 = 0x20000000;  // этаж 1: бит 0 = вверх, бит 1 = вниз
+in btns_floor_2: u8 = 0x20000001;  // этаж 2: бит 0 = вверх, бит 1 = вниз
+in btns_floor_3: u8 = 0x20000002;  // этаж 3: бит 0 = вверх, бит 1 = вниз
+in btns_floor_4: u8 = 0x20000003;  // этаж 4: бит 0 = вверх, бит 1 = вниз
+in btns_floor_5: u8 = 0x20000004;  // этаж 5: бит 0 = вверх, бит 1 = вниз
+in btns_floor_6: u8 = 0x20000005;  // этаж 6: бит 0 = вверх, бит 1 = вниз
+in btns_floor_7: u8 = 0x20000006;  // этаж 7: бит 0 = вверх, бит 1 = вниз
+in btns_floor_8: u8 = 0x20000007;  // этаж 8: бит 0 = вверх, бит 1 = вниз
+in btns_floor_9: u8 = 0x20000008;  // этаж 9: бит 0 = вверх, бит 1 = вниз
 
 // ══ ПОРТЫ КНОПОК В КАБИНЕ (9 кнопок = 2 байта) ══════════════════════════════
 
-port btns_cab_lo: u8 = 0x30000000;  // биты 0-7 → кнопки этажей 1-8
-port btns_cab_hi: u8 = 0x30000001;  // бит  0   → кнопка этажа 9
+in btns_cab_lo: u8 = 0x30000000;  // биты 0-7 → кнопки этажей 1-8
+in btns_cab_hi: u8 = 0x30000001;  // бит  0   → кнопка этажа 9
 
 // ══ ПЕРЕМЕННЫЕ СОСТОЯНИЯ ════════════════════════════════════════════════════
 
diff --git a/examples/elevator_mini.but b/examples/elevator_mini.but
index 8725d93..993e505 100644
--- a/examples/elevator_mini.but
+++ b/examples/elevator_mini.but
@@ -35,11 +35,11 @@ model Motor {
             ElevatorMotor_Stop = true;
         }
     }
-    port ElevatorMotor_Up     : bit = 0x20000100:0;
-    port ElevatorMotor_Down   : bit = 0x20000100:1;
-    port ElevatorMotor_Stop   : bit = 0x20000100:2;
-    port ElevatorMotor_SensorU: bit = 0x20000100:3;
-    port ElevatorMotor_SensorD: bit = 0x20000100:4;
+    out ElevatorMotor_Up     : bit = 0x20000100:0;
+    out ElevatorMotor_Down   : bit = 0x20000100:1;
+    out ElevatorMotor_Stop   : bit = 0x20000100:2;
+    in  ElevatorMotor_SensorU: bit = 0x20000100:3;
+    in  ElevatorMotor_SensorD: bit = 0x20000100:4;
 }
 
 model Cabin {
@@ -48,46 +48,46 @@ model Cabin {
             command = Stop;
         }
     }
-    port FloorButton_F1       : bit = 0x10000000:0;
-    port FloorButton_F2       : bit = 0x10000000:1;
-    port FloorButton_F3       : bit = 0x10000000:2;
-    port FloorButton_F4       : bit = 0x10000000:3;
-    port FloorButton_F5       : bit = 0x10000000:4;
-    port FloorButton_F6       : bit = 0x10000000:5;
-    port FloorButton_F7       : bit = 0x10000000:6;
-    port FloorButton_F8       : bit = 0x10000000:7;
-    port FloorButton_F9       : bit = 0x10000000:0;
+    in  FloorButton_F1       : bit = 0x10000000:0;
+    in  FloorButton_F2       : bit = 0x10000000:1;
+    in  FloorButton_F3       : bit = 0x10000000:2;
+    in  FloorButton_F4       : bit = 0x10000000:3;
+    in  FloorButton_F5       : bit = 0x10000000:4;
+    in  FloorButton_F6       : bit = 0x10000000:5;
+    in  FloorButton_F7       : bit = 0x10000000:6;
+    in  FloorButton_F8       : bit = 0x10000000:7;
+    in  FloorButton_F9       : bit = 0x10000000:0;
 
-    port FloorSensor_F1_Top   : bit = 0x10000100:0;
-    port FloorSensor_F1_Bottom: bit = 0x10000100:1;
-    port FloorSensor_F2_Top   : bit = 0x10000100:2;
-    port FloorSensor_F2_Bottom: bit = 0x10000100:3;
-    port FloorSensor_F3_Top   : bit = 0x10000100:4;
-    port FloorSensor_F3_Bottom: bit = 0x10000100:5;
-    port FloorSensor_F4_Top   : bit = 0x10000100:6;
-    port FloorSensor_F4_Bottom: bit = 0x10000100:7;
-    port FloorSensor_F5_Top   : bit = 0x10000100:8;
-    port FloorSensor_F5_Bottom: bit = 0x10000100:9;
-    port FloorSensor_F6_Top   : bit = 0x10000100:10;
-    port FloorSensor_F6_Bottom: bit = 0x10000100:11;
-    port FloorSensor_F7_Top   : bit = 0x10000100:12;
-    port FloorSensor_F7_Bottom: bit = 0x10000100:13;
-    port FloorSensor_F8_Top   : bit = 0x10000100:14;
-    port FloorSensor_F8_Bottom: bit = 0x10000100:15;
-    port FloorSensor_F9_Top   : bit = 0x10000100:16;
-    port FloorSensor_F9_Bottom: bit = 0x10000100:17;
+    in  FloorSensor_F1_Top   : bit = 0x10000100:0;
+    in  FloorSensor_F1_Bottom: bit = 0x10000100:1;
+    in  FloorSensor_F2_Top   : bit = 0x10000100:2;
+    in  FloorSensor_F2_Bottom: bit = 0x10000100:3;
+    in  FloorSensor_F3_Top   : bit = 0x10000100:4;
+    in  FloorSensor_F3_Bottom: bit = 0x10000100:5;
+    in  FloorSensor_F4_Top   : bit = 0x10000100:6;
+    in  FloorSensor_F4_Bottom: bit = 0x10000100:7;
+    in  FloorSensor_F5_Top   : bit = 0x10000100:8;
+    in  FloorSensor_F5_Bottom: bit = 0x10000100:9;
+    in  FloorSensor_F6_Top   : bit = 0x10000100:10;
+    in  FloorSensor_F6_Bottom: bit = 0x10000100:11;
+    in  FloorSensor_F7_Top   : bit = 0x10000100:12;
+    in  FloorSensor_F7_Bottom: bit = 0x10000100:13;
+    in  FloorSensor_F8_Top   : bit = 0x10000100:14;
+    in  FloorSensor_F8_Bottom: bit = 0x10000100:15;
+    in  FloorSensor_F9_Top   : bit = 0x10000100:16;
+    in  FloorSensor_F9_Bottom: bit = 0x10000100:17;
 
-    port CabinButton_F1    : bit = 0x20000000:0;
-    port CabinButton_F2    : bit = 0x20000000:1;
-    port CabinButton_F3    : bit = 0x20000000:2;
-    port CabinButton_F4    : bit = 0x20000000:3;
-    port CabinButton_F5    : bit = 0x20000000:4;
-    port CabinButton_F6    : bit = 0x20000000:5;
-    port CabinButton_F7    : bit = 0x20000000:6;
-    port CabinButton_F8    : bit = 0x20000000:7;
-    port CabinButton_F9    : bit = 0x20000000:8;
-    port CabinButton_DO    : bit = 0x20000000:9;
-    port CabinButton_DC    : bit = 0x20000000:10;
+    in  CabinButton_F1    : bit = 0x20000000:0;
+    in  CabinButton_F2    : bit = 0x20000000:1;
+    in  CabinButton_F3    : bit = 0x20000000:2;
+    in  CabinButton_F4    : bit = 0x20000000:3;
+    in  CabinButton_F5    : bit = 0x20000000:4;
+    in  CabinButton_F6    : bit = 0x20000000:5;
+    in  CabinButton_F7    : bit = 0x20000000:6;
+    in  CabinButton_F8    : bit = 0x20000000:7;
+    in  CabinButton_F9    : bit = 0x20000000:8;
+    in  CabinButton_DO    : bit = 0x20000000:9;
+    in  CabinButton_DC    : bit = 0x20000000:10;
 }
 
 start Main = Cabin | Motor;
diff --git a/examples/extend_complex.but b/examples/extend_complex.but
index 99bc70d..5b91bb6 100644
--- a/examples/extend_complex.but
+++ b/examples/extend_complex.but
@@ -12,9 +12,9 @@ const c: Coord = {0, 0, 0};
 enum Constant {
     X, Y, Z
 }
-port IDLE: bit = 0x44533932:1;
-port WORK: bit = 0x44533932:2;
-port WAIT: bit = 0x44533932:33;
+out IDLE: bit = 0x44533932:1;
+out WORK: bit = 0x44533932:2;
+in  WAIT: bit = 0x44533932:33;
 
 fn is_collected(c: Constant, v: u8) -> bool {
     if c == X && v == 1 {
diff --git a/examples/generated/c/elevator_main.c b/examples/generated/c/elevator_main.c
index 8e2a135..b027eaf 100644
--- a/examples/generated/c/elevator_main.c
+++ b/examples/generated/c/elevator_main.c
@@ -34,14 +34,7 @@ void scan_floor_buttons() {
 struct Ports {
 };
 
-static void port_write_numeric(Elevator_NumericPort port, int64_t val, void *userdata) {
-    struct Ports *ports = (struct Ports *) userdata;
-    (void) port;
-    (void) val;
-    (void) ports;
-}
-
-static int64_t port_read_numeric(Elevator_NumericPort port, void *userdata) {
+static int64_t port_read_numeric(Elevator_In_NumericPort port, void *userdata) {
     struct Ports *ports = (struct Ports *) userdata;
     (void) port;
     (void) ports;
@@ -52,7 +45,6 @@ int main(void) {
     struct Ports ports = {};
     Elevator elevator = {
             .userdata = &ports,
-            .write_numeric = port_write_numeric,
             .read_numeric  = port_read_numeric};
 
     Elevator_init(&elevator);
diff --git a/examples/generated/c/extend_complex_main.c b/examples/generated/c/extend_complex_main.c
index d4345dd..98272fd 100644
--- a/examples/generated/c/extend_complex_main.c
+++ b/examples/generated/c/extend_complex_main.c
@@ -7,14 +7,14 @@ bool has_flag(bool v) {
 struct Ports {
 };
 
-static void port_write_bit(ExtendComplex_BitPort port, bool val, void *userdata) {
+static void port_write_bit(ExtendComplex_Out_BitPort port, bool val, void *userdata) {
     struct Ports *ports = (struct Ports *) userdata;
     (void) port;
     (void) val;
     (void) ports;
 }
 
-static bool port_read_bit(ExtendComplex_BitPort port, void *userdata) {
+static bool port_read_bit(ExtendComplex_In_BitPort port, void *userdata) {
     struct Ports *ports = (struct Ports *) userdata;
     (void) port;
     (void) ports;
diff --git a/grammar/src/generator/c/c_header.rs b/grammar/src/generator/c/c_header.rs
index 8b84bac..99f8a94 100644
--- a/grammar/src/generator/c/c_header.rs
+++ b/grammar/src/generator/c/c_header.rs
@@ -7,9 +7,9 @@ use crate::generator::c::{
     get_typed_variable,
 };
 use crate::generator::indent::Printer;
-use crate::semantic::VariableNode;
 use crate::semantic::minimap::{Element, Name, StateExtend};
 use crate::semantic::naming::normalize_lowercase_snakecase;
+use crate::semantic::{PortDirection, VariableNode};
 use log::warn;
 
 /// Генерирует поля структуры C для extend состояния.
@@ -213,13 +213,14 @@ fn build_concat_state_enum(
     Ok(())
 }
 
-/// Собирает все порты из всех моделей, сгруппированные по [`PortClass`].
+/// Собирает все порты из всех моделей, сгруппированные по [`PortClass`] и [`PortDirection`].
 ///
-/// Возвращает словарь `PortClass → Vec<(model_name, port_name)>`.
+/// Возвращает словарь `(PortClass, PortDirection) → Vec<(model_name, port_name)>`.
 /// Порты отсортированы для детерминированной генерации.
 fn collect_ports_by_class(
     map: &CMap,
-) -> Result<std::collections::HashMap<PortClass, Vec<(Name, String)>>, Diagnostic> {
+) -> Result<std::collections::HashMap<(PortClass, PortDirection), Vec<(Name, String)>>, Diagnostic>
+{
     use std::collections::HashMap;
     let mut all_models = map.using_models();
     all_models.insert(
@@ -230,56 +231,70 @@ fn collect_ports_by_class(
             start: map.start().clone(),
         },
     );
-    let mut result: HashMap<PortClass, Vec<(Name, String)>> = HashMap::new();
+    let mut result: HashMap<(PortClass, PortDirection), Vec<(Name, String)>> = HashMap::new();
     for element in &all_models {
         let model_name = element.name();
         let model = map.raw_model_at(model_name.clone())?;
         let model_borrowed = model.borrow();
-        let mut ports: Vec<(Name, String, PortClass)> = model_borrowed
+        let mut ports: Vec<(Name, String, PortClass, PortDirection)> = model_borrowed
             .variables
             .values()
             .filter_map(|v| {
-                if let VariableNode::Port { name, ty, .. } = v {
-                    Some((model_name.clone(), name.clone(), PortClass::from_type(ty)))
+                if let VariableNode::Port {
+                    name,
+                    ty,
+                    direction,
+                    ..
+                } = v
+                {
+                    Some((
+                        model_name.clone(),
+                        name.clone(),
+                        PortClass::from_type(ty),
+                        *direction,
+                    ))
                 } else {
                     None
                 }
             })
             .collect();
-        ports.sort_by(|(_, a, _), (_, b, _)| a.cmp(b));
-        for (mname, pname, cls) in ports {
-            result.entry(cls).or_default().push((mname, pname));
+        ports.sort_by(|(_, a, _, _), (_, b, _, _)| a.cmp(b));
+        for (mname, pname, cls, dir) in ports {
+            result.entry((cls, dir)).or_default().push((mname, pname));
         }
     }
     Ok(result)
 }
 
-/// Генерирует тип-зависимые перечисления портов вида `{RootModel}_BitPort` и т. п.
+/// Генерирует тип-зависимые перечисления портов с разделением по направлению.
 ///
-/// Все порты одного типа объединяются в единый `typedef enum`.
+/// Для каждой комбинации `(PortClass, PortDirection)` генерируется отдельный `typedef enum`:
+/// `{Root}_In_BitPort`, `{Root}_Out_BitPort`, `{Root}_In_NumericPort` и т. п.
 /// Варианты именуются `{MODEL_UPPER}_{PORT_UPPER}` с последовательными значениями.
 /// Определения помещаются в заголовочный файл до struct-определений.
 fn generate_port_enums(printer: &mut Printer, map: &CMap) -> Result<(), Diagnostic> {
     let root_camelcase = map.root_name().unique_camelcase();
     let by_class = collect_ports_by_class(map)?;
     for cls in [PortClass::Bit, PortClass::Rational, PortClass::Numeric] {
-        let Some(ports) = by_class.get(&cls) else {
-            continue;
-        };
-        let type_name = cls.qualified_enum_name(&root_camelcase);
-        printer.print("typedef enum {").nl();
-        printer.up();
-        for (idx, (model_name, port_name)) in ports.iter().enumerate() {
-            let variant = format!(
-                "{}_{}",
-                model_name.unique_uppercase_snakecase(),
-                normalize_lowercase_snakecase(port_name.clone()).to_uppercase()
-            );
-            printer.ident(&format!("{} = {},", variant, idx)).nl();
+        for dir in [PortDirection::In, PortDirection::Out] {
+            let Some(ports) = by_class.get(&(cls, dir)) else {
+                continue;
+            };
+            let type_name = cls.qualified_enum_name_with_dir(&root_camelcase, dir);
+            printer.print("typedef enum {").nl();
+            printer.up();
+            for (idx, (model_name, port_name)) in ports.iter().enumerate() {
+                let variant = format!(
+                    "{}_{}",
+                    model_name.unique_uppercase_snakecase(),
+                    normalize_lowercase_snakecase(port_name.clone()).to_uppercase()
+                );
+                printer.ident(&format!("{} = {},", variant, idx)).nl();
+            }
+            printer.down();
+            printer.print(&format!("}} {};", type_name)).nl();
+            printer.nl();
         }
-        printer.down();
-        printer.print(&format!("}} {};", type_name)).nl();
-        printer.nl();
     }
     Ok(())
 }
@@ -368,55 +383,79 @@ fn generate_model_header(
     if main {
         let root_camelcase = map.root_name().unique_camelcase();
         let by_class = collect_ports_by_class(map)?;
-        let has_bit = by_class.contains_key(&PortClass::Bit);
-        let has_rational = by_class.contains_key(&PortClass::Rational);
-        let has_numeric = by_class.contains_key(&PortClass::Numeric);
-        if has_bit || has_rational || has_numeric {
+        let has_in_bit = by_class.contains_key(&(PortClass::Bit, PortDirection::In));
+        let has_out_bit = by_class.contains_key(&(PortClass::Bit, PortDirection::Out));
+        let has_in_rational = by_class.contains_key(&(PortClass::Rational, PortDirection::In));
+        let has_out_rational = by_class.contains_key(&(PortClass::Rational, PortDirection::Out));
+        let has_in_numeric = by_class.contains_key(&(PortClass::Numeric, PortDirection::In));
+        let has_out_numeric = by_class.contains_key(&(PortClass::Numeric, PortDirection::Out));
+        let has_any = has_in_bit
+            || has_out_bit
+            || has_in_rational
+            || has_out_rational
+            || has_in_numeric
+            || has_out_numeric;
+        if has_any {
             printer
                 .ident("/// NOTICE: Функции портов ввода вывода")
                 .nl();
             printer.ident("void  *userdata;").nl();
-            if has_bit {
-                let bit_type = PortClass::Bit.qualified_enum_name(&root_camelcase);
+            if has_out_bit {
+                let bit_out = PortClass::Bit
+                    .qualified_enum_name_with_dir(&root_camelcase, PortDirection::Out);
                 printer
                     .ident(&format!(
-                        "void  (*{write_bit})({bit_type} port, bool val, void *userdata);",
+                        "void  (*{write_bit})({bit_out} port, bool val, void *userdata);",
                         write_bit = FUNCTION_PORT_WRITE_BIT
                     ))
                     .nl();
+            }
+            if has_in_bit {
+                let bit_in =
+                    PortClass::Bit.qualified_enum_name_with_dir(&root_camelcase, PortDirection::In);
                 printer
                     .ident(&format!(
-                        "bool  (*{read_bit} )({bit_type} port, void *userdata);",
+                        "bool  (*{read_bit} )({bit_in} port, void *userdata);",
                         read_bit = FUNCTION_PORT_READ_BIT
                     ))
                     .nl();
             }
-            if has_rational {
-                let rat_type = PortClass::Rational.qualified_enum_name(&root_camelcase);
+            if has_out_rational {
+                let rat_out = PortClass::Rational
+                    .qualified_enum_name_with_dir(&root_camelcase, PortDirection::Out);
                 printer
                     .ident(&format!(
-                        "void  (*{write_float})({rat_type} port, float val, void *userdata);",
+                        "void  (*{write_float})({rat_out} port, float val, void *userdata);",
                         write_float = FUNCTION_PORT_WRITE_FLOAT
                     ))
                     .nl();
+            }
+            if has_in_rational {
+                let rat_in = PortClass::Rational
+                    .qualified_enum_name_with_dir(&root_camelcase, PortDirection::In);
                 printer
                     .ident(&format!(
-                        "float (*{read_float} )({rat_type} port, void *userdata);",
+                        "float (*{read_float} )({rat_in} port, void *userdata);",
                         read_float = FUNCTION_PORT_READ_FLOAT
                     ))
                     .nl();
             }
-            if has_numeric {
-                let num_type = PortClass::Numeric.qualified_enum_name(&root_camelcase);
+            if has_out_numeric {
+                let num_out = PortClass::Numeric
+                    .qualified_enum_name_with_dir(&root_camelcase, PortDirection::Out);
                 printer
                     .ident(&format!(
-                        "void    (*{write_numeric})({num_type} port, int64_t val, void *userdata);",
+                        "void    (*{write_numeric})({num_out} port, int64_t val, void *userdata);",
                         write_numeric = FUNCTION_PORT_WRITE_NUMERIC
                     ))
                     .nl();
+            }
+            if has_in_numeric {
+                let num_in = PortClass::Numeric
+                    .qualified_enum_name_with_dir(&root_camelcase, PortDirection::In);
                 printer
                     .ident(&format!(
-                        "int64_t (*{read_numeric} )({num_type} port, void *userdata);",
+                        "int64_t (*{read_numeric} )({num_in} port, void *userdata);",
                         read_numeric = FUNCTION_PORT_READ_NUMERIC
                     ))
                     .nl();
diff --git a/grammar/src/generator/c/c_source.rs b/grammar/src/generator/c/c_source.rs
index 30f9e2c..c591311 100644
--- a/grammar/src/generator/c/c_source.rs
+++ b/grammar/src/generator/c/c_source.rs
@@ -495,7 +495,7 @@ mod tests {
         let src = r#"
 type u8 = [bit;8];
 const LIMIT: u8 = 100;
-port SENSOR: u8 = 0x100000;
+in SENSOR: u8 = 0x100000;
 var v: u8 = 0;
 start Main { always { v = LIMIT; } }
         "#;
@@ -930,7 +930,7 @@ state End;";
     #[test]
     fn test_bit_access_port_read_in_condition() {
         let src =
-            "type u8 = [bit;8]; port BTN: u8 = 0x200000; start S { ref Done: BTN.0; } state Done;";
+            "type u8 = [bit;8]; in BTN: u8 = 0x200000; start S { ref Done: BTN.0; } state Done;";
         let code = generate_source_str(src);
         assert!(
             code.contains("(((*main->read_numeric)(ROOT_BTN, main->userdata) >> 0) & 1u)"),
@@ -963,7 +963,7 @@ state End;";
     /// Чтение бита порта в `always`: `x = BTN.0` → `(((*main->read_numeric)(ROOT_BTN, ...) >> 0) & 1u)`
     #[test]
     fn test_bit_access_port_read_in_always() {
-        let src = "type u8 = [bit;8]; port BTN: u8 = 0x200000; var x: u8 = 0; start S { always { x = BTN.0; } ref Done: true; } state Done;";
+        let src = "type u8 = [bit;8]; in BTN: u8 = 0x200000; var x: u8 = 0; start S { always { x = BTN.0; } ref Done: true; } state Done;";
         let code = generate_source_str(src);
         assert!(
             code.contains("(((*main->read_numeric)(ROOT_BTN, main->userdata) >> 0) & 1u)"),
@@ -974,7 +974,7 @@ state End;";
     /// Запись бита порта: `LED.7 = true` → read-modify-write через write_numeric
     #[test]
     fn test_bit_access_port_write_in_always() {
-        let src = "type u8 = [bit;8]; port LED: u8 = 0x100000; start S { always { LED.7 = true; } ref Done: true; } state Done;";
+        let src = "type u8 = [bit;8]; out LED: u8 = 0x100000; start S { always { LED.7 = true; } ref Done: true; } state Done;";
         let code = generate_source_str(src);
         assert!(
             code.contains("write_numeric)(ROOT_LED,")
diff --git a/grammar/src/generator/c/mod.rs b/grammar/src/generator/c/mod.rs
index 21299ba..8f4da63 100644
--- a/grammar/src/generator/c/mod.rs
+++ b/grammar/src/generator/c/mod.rs
@@ -43,6 +43,7 @@ use crate::generator::c::c_header::generate_header;
 use crate::generator::c::c_map::CMap;
 use crate::generator::c::c_source::generate_source;
 use crate::semantic::ModelNode;
+use crate::semantic::PortDirection;
 use crate::semantic::minimap::{Element, StateExtend};
 use crate::semantic::naming::normalize_lowercase_snakecase;
 use crate::semantic::type_node::TypeNode;
@@ -79,10 +80,18 @@ impl PortClass {
         }
     }
 
-    /// Полное имя C-перечисления с префиксом корневой модели.
-    /// Например: `ElevatorMini_BitPort`.
-    pub(super) fn qualified_enum_name(self, root_camelcase: &str) -> String {
-        format!("{}_{}", root_camelcase, self.enum_name())
+    /// Полное имя C-перечисления с направлением порта.
+    /// Например: `ElevatorMini_In_BitPort`, `ElevatorMini_Out_NumericPort`.
+    pub(super) fn qualified_enum_name_with_dir(
+        self,
+        root_camelcase: &str,
+        dir: PortDirection,
+    ) -> String {
+        let dir_str = match dir {
+            PortDirection::In => "In",
+            PortDirection::Out => "Out",
+        };
+        format!("{}_{}_{}", root_camelcase, dir_str, self.enum_name())
     }
 
     /// Определяет категорию по [`TypeNode`].
@@ -215,8 +224,8 @@ mod tests {
     const SRC: &str = r#"
 type u8 = [bit;8];
 
-port sensors_1: u8 = 0x100000000;
-port sensors_2: u8 = 0x200000000;
+in sensors_1: u8 = 0x100000000;
+in sensors_2: u8 = 0x200000000;
 cond AtFloor8 = sensors_1.0 & sensors_1.1;
 cond AtFloor9 = sensors_2.0 & sensors_2.1;
 
@@ -303,7 +312,7 @@ start Main = Robot;
 type u8 = [bit;8];
 const MATRIX: u8 = 0;
 const NUMB: u8 = 255;
-port SENSOR: u8 = 0x100000;
+in SENSOR: u8 = 0x100000;
 var v: u8 = 0;
 start Main { always { v = MATRIX; v = NUMB; } }
         "#;
diff --git a/grammar/src/grammar.lalrpop b/grammar/src/grammar.lalrpop
index 4527b96..ef50de6 100644
--- a/grammar/src/grammar.lalrpop
+++ b/grammar/src/grammar.lalrpop
@@ -141,9 +141,16 @@ VariableDefine: Box<VariableDefine> = {
             loc: Location::Source(file_no, l, r), typ, name, initializer: e
         })
     },
-    <l:@L> "port" <name:IdentifierOrError> <typ:(":" <Type>)?> <e:("=" <Expression>)?> <r:@R> ";" => {
+    <l:@L> "in" <name:IdentifierOrError> <typ:(":" <Type>)?> <e:("=" <Expression>)?> <r:@R> ";" => {
         Box::new(VariableDefine::Port {
-            loc: Location::Source(file_no, l, r), typ, name, initializer: e
+            loc: Location::Source(file_no, l, r), typ, name, initializer: e,
+            direction: PortDirection::In,
+        })
+    },
+    <l:@L> "out" <name:IdentifierOrError> <typ:(":" <Type>)?> <e:("=" <Expression>)?> <r:@R> ";" => {
+        Box::new(VariableDefine::Port {
+            loc: Location::Source(file_no, l, r), typ, name, initializer: e,
+            direction: PortDirection::Out,
         })
     },
 }
@@ -906,7 +913,8 @@ extern {
         "type" => Token::Type,
         "cond" => Token::Condition,
         "const" => Token::Constant,
-        "port" => Token::Port,
+        "in"   => Token::PortIn,
+        "out"  => Token::PortOut,
         "extern" => Token::Extern,
         "true" => Token::True,
         "false" => Token::False,
diff --git a/grammar/src/lib.rs b/grammar/src/lib.rs
index 3d9ff9e..64be23c 100644
--- a/grammar/src/lib.rs
+++ b/grammar/src/lib.rs
@@ -325,8 +325,8 @@ const MATRIX: u8 = { 0, 0, 0, 0, 0, 0, 0, 0 };
 const NUMB: u8 = 0xFF;
 cond  IsEmpty = it = 0;
 //Порт с указанием отображаемого адреса
-port  A : u8  = 0x00548835;
-port  B1: bit = 0x00648835:6;
+out   A : u8  = 0x00548835;
+in    B1: bit = 0x00648835:6;
 //Переменная
 var   it: [bit;64] = 0;
 
diff --git a/grammar/src/lsp.rs b/grammar/src/lsp.rs
index a25c6e1..327b9c3 100644
--- a/grammar/src/lsp.rs
+++ b/grammar/src/lsp.rs
@@ -1360,7 +1360,8 @@ pub fn semantic_tokens(source: &str) -> SemanticTokens {
             | Token::Start
             | Token::Variable
             | Token::Constant
-            | Token::Port
+            | Token::PortIn
+            | Token::PortOut
             | Token::Function
             | Token::Extern
             | Token::Enum
diff --git a/grammar/src/parser/ast.rs b/grammar/src/parser/ast.rs
index cfb3ab7..a6f82ef 100644
--- a/grammar/src/parser/ast.rs
+++ b/grammar/src/parser/ast.rs
@@ -174,7 +174,17 @@ pub enum Type {
     Unit,
 }
 
-/// Описание (`var` или `const` или `port`).
+/// Направление порта: входной (`in`) или выходной (`out`).
+#[derive(Debug, PartialEq, Eq, Clone, Copy, Hash)]
+#[cfg_attr(feature = "ast-serde", derive(Serialize, Deserialize))]
+pub enum PortDirection {
+    /// Входной порт — FSM только читает (значение поступает извне).
+    In,
+    /// Выходной порт — FSM только пишет (значение уходит наружу).
+    Out,
+}
+
+/// Описание (`var` или `const` или `in`/`out`-порта).
 #[derive(Debug, PartialEq, Eq, Clone)]
 #[cfg_attr(feature = "ast-serde", derive(Serialize, Deserialize))]
 pub enum VariableDefine {
@@ -189,7 +199,7 @@ pub enum VariableDefine {
         /// Инициализатор переменной
         initializer: Option<Expression>,
     },
-    /// Порт (неизменяемое значение), инициализируется адресом
+    /// Порт ввода-вывода, объявляется через `in` (входной) или `out` (выходной).
     Port {
         /// Местоположение в исходном тексте.
         loc: Location,
@@ -197,8 +207,10 @@ pub enum VariableDefine {
         typ: Option<Type>,
         /// Имя порта
         name: Option<Identifier>,
-        /// Инициализатор порта
+        /// Инициализатор порта (адрес, необязателен)
         initializer: Option<Expression>,
+        /// Направление порта
+        direction: PortDirection,
     },
     /// Константа (неизменяемое значение)
     Constant {
diff --git a/grammar/src/parser/lexer.rs b/grammar/src/parser/lexer.rs
index 70b31fb..f8dd239 100644
--- a/grammar/src/parser/lexer.rs
+++ b/grammar/src/parser/lexer.rs
@@ -168,8 +168,10 @@ pub enum Token<'input> {
 
     /// Ключевое слово `const`.
     Constant,
-    /// Ключевое слово `port`.
-    Port,
+    /// Ключевое слово `in` (входной порт).
+    PortIn,
+    /// Ключевое слово `out` (выходной порт).
+    PortOut,
 
     /// Стрелка `-->` (не используется в текущей грамматике).
     PeirceArrow,
@@ -287,7 +289,8 @@ impl<'input> fmt::Display for Token<'input> {
             Token::Reference => write!(f, "ref"),
             Token::Template => write!(f, "template"),
             Token::Condition => write!(f, "cond"),
-            Token::Port => write!(f, "port"),
+            Token::PortIn => write!(f, "in"),
+            Token::PortOut => write!(f, "out"),
             Token::Variable => write!(f, "var"),
             Token::Next => write!(f, "next"),
             Token::Extern => write!(f, "extern"),
@@ -442,7 +445,8 @@ static KEYWORDS: phf::Map<&'static str, Token> = phf_map! {
     "as"       => Token::As,
     "assembly" => Token::Assembly,
     "formula"  => Token::Formula,
-    "port"     => Token::Port,
+    "in"       => Token::PortIn,
+    "out"      => Token::PortOut,
     "model"    => Token::Model,
     "state"    => Token::State,
     "start"    => Token::Start,
diff --git a/grammar/src/semantic/expression.rs b/grammar/src/semantic/expression.rs
index b242c3e..bf4d5f4 100644
--- a/grammar/src/semantic/expression.rs
+++ b/grammar/src/semantic/expression.rs
@@ -1309,6 +1309,7 @@ mod tests {
             name: "p".into(),
             ty: TypeNode::Bit,
             expr: ExpressionNode::None,
+            direction: crate::semantic::PortDirection::In,
         };
         assert_eq!(var_type(&v), TypeNode::Bit);
     }
diff --git a/grammar/src/semantic/mod.rs b/grammar/src/semantic/mod.rs
index 463f3f0..aa60a9c 100644
--- a/grammar/src/semantic/mod.rs
+++ b/grammar/src/semantic/mod.rs
@@ -40,6 +40,7 @@ pub(crate) mod validate;
 
 use crate::diagnostics::Location;
 use crate::parser::ast;
+pub use crate::parser::ast::PortDirection;
 use crate::parser::ast::{Member, NamedArgument, ParameterList, Type};
 pub use crate::semantic::enum_node::EnumDefinitionNode;
 pub use crate::semantic::formula::Formula;
@@ -784,7 +785,7 @@ pub enum VariableNode {
         /// Инициализирующее выражение.
         expr: ExpressionNode,
     },
-    /// Порт, отображённый на аппаратный адрес.
+    /// Порт ввода-вывода, объявляется через `in` (входной) или `out` (выходной).
     Port {
         /// Родительская модель (слабая ссылка для предотвращения циклов Rc).
         upper: Option<Weak<RefCell<ModelNode>>>,
@@ -794,8 +795,10 @@ pub enum VariableNode {
         name: String,
         /// Тип переменной.
         ty: TypeNode,
-        /// Адрес порта.
+        /// Адрес порта (необязателен).
         expr: ExpressionNode,
+        /// Направление порта (входной / выходной).
+        direction: PortDirection,
     },
     /// Константа.
     Const {
diff --git a/grammar/src/semantic/statement.rs b/grammar/src/semantic/statement.rs
index 633fd0c..83c9acc 100644
--- a/grammar/src/semantic/statement.rs
+++ b/grammar/src/semantic/statement.rs
@@ -873,12 +873,12 @@ mod tests {
     #[test]
     fn function_param_visible_in_expression_statement() {
         use crate::semantic::FunctionDefinitionNode;
-        // `out = value` — оператор-выражение (присваивание), где `value` — параметр функции.
+        // `result = value` — оператор-выражение (присваивание), где `value` — параметр функции.
         // До исправления это приводило к ошибке LSP «Идентификатор 'value' не найден».
         let node = build(concat!(
             "type u8 = [bit;8]; ",
-            "var out: u8 = 0; ",
-            "fn clamp(value: u8) -> u8 { out = value; return value; } ",
+            "var result: u8 = 0; ",
+            "fn clamp(value: u8) -> u8 { result = value; return value; } ",
             "start S;"
         ));
         // Проверяем, что модель успешно построена и функция clamp присутствует
diff --git a/grammar/src/semantic/tree.rs b/grammar/src/semantic/tree.rs
index 7d7f699..86206c4 100644
--- a/grammar/src/semantic/tree.rs
+++ b/grammar/src/semantic/tree.rs
@@ -311,6 +311,7 @@ fn construct_model_stage0(
                     typ,
                     name,
                     initializer,
+                    direction,
                 } => {
                     let name = extract_name(name.clone(), loc)?;
                     let type_node = construct_type(typ, model_node.clone())?;
@@ -339,6 +340,7 @@ fn construct_model_stage0(
                             name: name.clone(),
                             ty: type_node,
                             expr,
+                            direction,
                         },
                     )
                 }
@@ -687,12 +689,14 @@ fn resolve_variable_expressions(
                 name: n,
                 ty,
                 expr: ExpressionNode::Unresolved(expr),
+                direction,
             } => VariableNode::Port {
                 upper,
                 loc,
                 name: n,
                 ty,
                 expr: construct_expression(expr, vec![], model.clone())?,
+                direction,
             },
             other => other,
         };
diff --git a/grammar/src/semantic/validate.rs b/grammar/src/semantic/validate.rs
index 2fb10e1..86dd04f 100644
--- a/grammar/src/semantic/validate.rs
+++ b/grammar/src/semantic/validate.rs
@@ -22,8 +22,8 @@ use crate::parser::{ast as ast_types, ast};
 use crate::semantic::condition::resolve_condition;
 use crate::semantic::type_node::TypeNode;
 use crate::semantic::{
-    ConditionNode, ExpressionNode, FunctionDefinitionNode, ModelNode, ReferenceNode, StateNode,
-    StateNodeKind, StatementNode, VariableNode,
+    ConditionNode, ExpressionNode, FunctionDefinitionNode, ModelNode, PortDirection, ReferenceNode,
+    StateNode, StateNodeKind, StatementNode, VariableNode,
 };
 use std::cell::RefCell;
 use std::collections::HashSet;
@@ -244,7 +244,22 @@ fn validate_cond(
         ConditionNode::Rational(_, _) => {}
         ConditionNode::String(_) => {}
         ConditionNode::Bool(_) => {}
-        ConditionNode::Variable(_var, _) => {}
+        ConditionNode::Variable(var_rc, _) => {
+            // Чтение из `out`-порта запрещено в условии (SE-027)
+            if let VariableNode::Port {
+                direction: PortDirection::Out,
+                name,
+                loc,
+                ..
+            } = &*var_rc.borrow()
+            {
+                return Err(Diagnostic::error(
+                    *loc,
+                    format!("Чтение из выходного порта '{}' запрещено", name),
+                )
+                .with_code("SE-027"));
+            }
+        }
         ConditionNode::Model(_model) => {}
         ConditionNode::State(_state) => {}
         ConditionNode::EnumVariant(_, _, _) => {}
@@ -306,11 +321,60 @@ fn validate_expression(
         | ExpressionNode::Equal(left, right)
         | ExpressionNode::NotEqual(left, right)
         | ExpressionNode::And(left, right)
-        | ExpressionNode::Or(left, right)
-        | ExpressionNode::Assign(left, right) => {
+        | ExpressionNode::Or(left, right) => {
             validate_expression(left, model.clone())?;
             validate_expression(right, model.clone())?;
         }
+        ExpressionNode::Assign(left, right) => {
+            // Запись в `in`-порт запрещена (SE-026)
+            let check_port = |expr: &ExpressionNode| {
+                if let ExpressionNode::Variable(v) = expr {
+                    return Some(v.clone());
+                }
+                if let ExpressionNode::BitAccess(inner, _) = expr {
+                    if let ExpressionNode::Variable(v) = inner.as_ref() {
+                        return Some(v.clone());
+                    }
+                }
+                None
+            };
+            if let Some(var_rc) = check_port(left) {
+                if let VariableNode::Port {
+                    direction: PortDirection::In,
+                    name,
+                    loc,
+                    ..
+                } = &*var_rc.borrow()
+                {
+                    return Err(Diagnostic::error(
+                        *loc,
+                        format!("Запись в входной порт '{}' запрещена", name),
+                    )
+                    .with_code("SE-026"));
+                }
+            }
+            // Для BitAccess как lvalue на out-порт не рекурсируем внутрь
+            // (это запись в порт, а не чтение — SE-027 здесь неприменим).
+            let is_out_port_bitaccess = if let ExpressionNode::BitAccess(inner, _) = left.as_ref() {
+                if let ExpressionNode::Variable(v) = inner.as_ref() {
+                    matches!(
+                        &*v.borrow(),
+                        VariableNode::Port {
+                            direction: PortDirection::Out,
+                            ..
+                        }
+                    )
+                } else {
+                    false
+                }
+            } else {
+                false
+            };
+            if !is_out_port_bitaccess {
+                validate_expression(left, model.clone())?;
+            }
+            validate_expression(right, model.clone())?;
+        }
         ExpressionNode::ConditionalOperator(left, right, other) => {
             validate_expression(left, model.clone())?;
             validate_expression(right, model.clone())?;
@@ -322,7 +386,22 @@ fn validate_expression(
         ExpressionNode::Type(_) => {}
         ExpressionNode::Address(_, _) => {}
         ExpressionNode::Bool(_) => {}
-        ExpressionNode::Variable(_var) => {}
+        ExpressionNode::Variable(var_rc) => {
+            // Чтение из `out`-порта запрещено (SE-027)
+            if let VariableNode::Port {
+                direction: PortDirection::Out,
+                name,
+                loc,
+                ..
+            } = &*var_rc.borrow()
+            {
+                return Err(Diagnostic::error(
+                    *loc,
+                    format!("Чтение из выходного порта '{}' запрещено", name),
+                )
+                .with_code("SE-027"));
+            }
+        }
         ExpressionNode::Model(_model) => {}
         ExpressionNode::Condition(cond) => {
             validate_cond(None, &cond.borrow().value, model.clone())?;
@@ -2374,6 +2453,7 @@ mod tests_ce4_declarations {
                 name: "p".to_string(),
                 ty: TypeNode::Enum("Dir".to_string()),
                 expr: ExpressionNode::Number(0),
+                direction: crate::semantic::PortDirection::In,
             };
             m.borrow_mut().variables.insert("p".to_string(), var);
             m
diff --git a/grammar/tests/data/parser/valid/bit_access.but b/grammar/tests/data/parser/valid/bit_access.but
index 9e5249e..177bbd9 100644
--- a/grammar/tests/data/parser/valid/bit_access.but
+++ b/grammar/tests/data/parser/valid/bit_access.but
@@ -8,9 +8,9 @@
 type u8 = [bit;8];
 
 /// Порт кнопок: 8 кнопок по одному биту каждая.
-port BTN : u8 = 0x00200000;
+in  BTN : u8 = 0x00200000;
 /// Порт светодиодов: 8 LED по одному биту каждый.
-port LED : u8 = 0x00100000;
+out LED : u8 = 0x00100000;
 
 model BitOps {
     /// Регистр флагов состояния.
diff --git a/grammar/tests/data/parser/valid/fsm_blinker.but b/grammar/tests/data/parser/valid/fsm_blinker.but
index 81ff4b0..1268eda 100644
--- a/grammar/tests/data/parser/valid/fsm_blinker.but
+++ b/grammar/tests/data/parser/valid/fsm_blinker.but
@@ -3,8 +3,8 @@
 
 type u8 = [bit;8];
 
-port LED:     u8  = 0x00100000;
-port BUTTON:  bit = 0x00200000:0;
+out LED:     u8  = 0x00100000;
+in  BUTTON:  bit = 0x00200000:0;
 
 var counter: u8 = 0;
 
diff --git a/grammar/tests/data/parser/valid/model_states.but b/grammar/tests/data/parser/valid/model_states.but
index e502401..c5ecffe 100644
--- a/grammar/tests/data/parser/valid/model_states.but
+++ b/grammar/tests/data/parser/valid/model_states.but
@@ -1,7 +1,7 @@
 // Модель с несколькими состояниями и переходами (позитивный тест парсера)
 type u8 = [bit;8];
 
-port Counter: u8 = 0x00100000;
+in  Counter: u8 = 0x00100000;
 
 var count: u8 = 0;
 
diff --git a/grammar/tests/data/parser/valid/named_blocks.but b/grammar/tests/data/parser/valid/named_blocks.but
index 8527afc..813349c 100644
--- a/grammar/tests/data/parser/valid/named_blocks.but
+++ b/grammar/tests/data/parser/valid/named_blocks.but
@@ -1,6 +1,6 @@
 // Именованные блоки кода (позитивный тест парсера)
 type u8 = [bit;8];
-port LED: u8 = 0x00100000;
+out LED: u8 = 0x00100000;
 
 model Blinker {
     start On {
diff --git a/grammar/tests/data/parser/valid/typed_variables.but b/grammar/tests/data/parser/valid/typed_variables.but
index ce4c727..d4c8900 100644
--- a/grammar/tests/data/parser/valid/typed_variables.but
+++ b/grammar/tests/data/parser/valid/typed_variables.but
@@ -14,9 +14,9 @@ const ONE:   Flag = true;
 const HALF:  Byte = 0x80;
 
 // ── Порты с адресами ─────────────────────────────────────────────────────────
-port INPUT:    Byte = 0x00100000;
-port OUTPUT:   Byte = 0x00200000;
-port FLAG_BIT: Flag = 0x00300000;
+in  INPUT:    Byte = 0x00100000;
+out OUTPUT:   Byte = 0x00200000;
+in  FLAG_BIT: Flag = 0x00300000;
 
 // ── Переменные ───────────────────────────────────────────────────────────────
 var x:   Byte = 0;
diff --git a/grammar/tests/data/semantic/invalid/named_block_undeclared_var.but b/grammar/tests/data/semantic/invalid/named_block_undeclared_var.but
index 4315b15..8afc7e7 100644
--- a/grammar/tests/data/semantic/invalid/named_block_undeclared_var.but
+++ b/grammar/tests/data/semantic/invalid/named_block_undeclared_var.but
@@ -4,6 +4,6 @@
 // В named blocks это НЕ проверяется (используется best-effort разрешение),
 // поэтому ошибка вызвана неверным объявлением порта на уровне модели.
 
-port BAD_PORT: bit;
+in BAD_PORT: bit;
 
 start S;
diff --git a/grammar/tests/data/semantic/valid/type_aliases.but b/grammar/tests/data/semantic/valid/type_aliases.but
index 6712061..1149370 100644
--- a/grammar/tests/data/semantic/valid/type_aliases.but
+++ b/grammar/tests/data/semantic/valid/type_aliases.but
@@ -9,7 +9,7 @@ type u8  = [bit; 8];
 type u16 = [bit; 16];
 
 var   counter: u8  = 0;
-port  STATUS:  u8  = 0x40000000;
+in    STATUS:  u8  = 0x40000000;
 
 start Idle {
     ref Done: true;
diff --git a/grammar/tests/lexer_tests.rs b/grammar/tests/lexer_tests.rs
index 437de65..a263b07 100644
--- a/grammar/tests/lexer_tests.rs
+++ b/grammar/tests/lexer_tests.rs
@@ -142,8 +142,8 @@ fn is_keyword_returns_true_for_keywords() {
 
     let keywords = [
         "break", "const", "continue", "else", "false", "for", "fn", "if", "import", "loop",
-        "return", "string", "true", "type", "as", "assembly", "formula", "port", "model", "state",
-        "start", "ref", "template", "cond", "var", "next", "extern",
+        "return", "string", "true", "type", "as", "assembly", "formula", "in", "out", "model",
+        "state", "start", "ref", "template", "cond", "var", "next", "extern",
     ];
     for kw in keywords {
         assert!(is_keyword(kw), "'{}' должно быть ключевым словом", kw);
@@ -522,7 +522,8 @@ fn token_display_keywords() {
         ("cond", Token::Condition),
         ("var", Token::Variable),
         ("const", Token::Constant),
-        ("port", Token::Port),
+        ("in", Token::PortIn),
+        ("out", Token::PortOut),
         ("fn", Token::Function),
         ("true", Token::True),
         ("false", Token::False),
@@ -1031,7 +1032,7 @@ fn complete_but_program_lexes_without_errors() {
 /// Пример полной BuT-программы
 type u8 = [bit;8];
 const MAX: u8 = 0xFF;
-port LED: u8 = 00100000;
+out LED: u8 = 00100000;
 var counter: u8 = 0;
 cond IsMax = counter = MAX;
 model Blinker {
diff --git a/grammar/tests/parser_tests.rs b/grammar/tests/parser_tests.rs
index 91f85ea..13395c6 100644
--- a/grammar/tests/parser_tests.rs
+++ b/grammar/tests/parser_tests.rs
@@ -378,7 +378,7 @@ fn parse_const_variable() {
 /// `port A: bit = 0x00548835:4`.
 #[test]
 fn parse_port_with_address() {
-    let root = must_parse("port A: bit = 0x00548835:4; model M { start S; }");
+    let root = must_parse("in A: bit = 0x00548835:4; model M { start S; }");
     let port = root.elements.iter().find_map(|e| {
         if let ModelElement::Variable(v) = e {
             Some(v.as_ref())
diff --git a/grammar/tests/semantic_tests.rs b/grammar/tests/semantic_tests.rs
index 62e7d84..9585d7d 100644
--- a/grammar/tests/semantic_tests.rs
+++ b/grammar/tests/semantic_tests.rs
@@ -113,7 +113,7 @@ fn search_var_finds_const() {
 /// `search_var` находит порт.
 #[test]
 fn search_var_finds_port() {
-    let node = build("type u8 = [bit;8]; port P: u8 = 0x00100000;");
+    let node = build("type u8 = [bit;8]; in P: u8 = 0x00100000;");
     assert!(node.search_var("P").is_some(), "Порт P должен быть найден");
     assert!(
         matches!(node.search_var("P").unwrap(), VariableNode::Port { .. }),
@@ -308,7 +308,7 @@ fn duplicate_nested_model_name_is_error() {
 /// Порт без явного типа — ошибка.
 #[test]
 fn port_without_type_is_error() {
-    let (ast, _) = parse("port P = 0x00100000;", 0).unwrap();
+    let (ast, _) = parse("in P = 0x00100000;", 0).unwrap();
     let result = construct_model(&ast, None, &[]);
     assert!(result.is_err(), "Порт без типа должен давать ошибку");
 }
@@ -316,7 +316,7 @@ fn port_without_type_is_error() {
 /// Порт с инициализатором не-адресом — адрес игнорируется, порт создаётся без адреса.
 #[test]
 fn port_with_non_address_initializer_is_valid() {
-    let (ast, _) = parse("type u8 = [bit;8]; port P: u8 = true; start S;", 0).unwrap();
+    let (ast, _) = parse("type u8 = [bit;8]; in P: u8 = true; start S;", 0).unwrap();
     let result = construct_model(&ast, None, &[]);
     assert!(
         result.is_ok(),
@@ -1333,8 +1333,8 @@ type u8 = [bit;8];
 const MATRIX: u8 = { 0, 0, 0, 0, 0, 0, 0, 0 };
 const NUMB: u8 = 0xFF;
 cond IsEmpty = it = 0;
-port A : u8  = 0x00548835;
-port B1: bit = 0x00648835:6;
+out A : u8  = 0x00548835;
+in  B1: bit = 0x00648835:6;
 var it: [bit;64] = 0;
 model Ping {
     start Start {
