diff --git a/README.md b/README.md
index e90968c..ef6483a 100644
--- a/README.md
+++ b/README.md
@@ -1171,18 +1171,9 @@ ref Next: timer != 0;     // Явно и безопасно — без пред
    при работе с разными размерами массивов неизбежно
 3. **Отсутствие стандартной библиотеки** — нет встроенного модуля `std` или `core` с типовыми алгоритмами
    для встраиваемых систем (Ce7)
-4. ~~**Генератор C неполон** — нет генерации тел функций перехода и обработчиков `enter`/`exit`/`always` (NI5)~~ — **Исправлено (Changes-38)**
 
 ### Открытые задачи
 
-#### ~~NI5. Полная генерация C-кода~~ — **Реализовано (Changes-38)**
-
-~~Завершить `generator/c/mod.rs`: генерация тел функций, FSM-диспетчера (`transition()`), обработчиков именованных
-блоков (`on_enter`, `on_exit`, `always`). Текущая реализация генерирует только заголовочные файлы с объявлениями.~~
-
-`generate_stmt_expression` полностью реализован (43 ветки), добавлены `If`/`Loop`/`For`/`Variable` в `generate_code_block`,
-реализованы вспомогательные функции `resolve_variable_c_expr` и `generate_function_call`. Добавлен пример `examples/comprehensive.but`.
-
 #### C2. Единый оператор сравнения в условиях
 
 Ввести `==` в контексте `cond`/`ref` вместо перегрузки `=`:
@@ -1252,6 +1243,7 @@ fn max(a: u8, b: u8) -> u8;
 | Changes-36    | Составные типы `[T;N]` в параметрах функций (FE6)                             | ✅ Реализовано |
 | Changes-37    | Структурные типы `struct` — грамматика и семантика                            | ✅ Реализовано |
 | Changes-38    | Полная генерация C-кода: `generate_stmt_expression` (43 ветки), `If`/`Loop`/`For` в `generate_code_block`, пример `comprehensive.but` (NI5) | ✅ Реализовано |
+| Changes-39    | Исправление LSP-ошибки: параметры функции видны в `Expression`-ветке `statement.rs:100` (`params.clone()` вместо `vec![]`) (A1)            | ✅ Исправлено  |
 | —             | SemanticIndex: поиск узла по LSP-позиции (LSP1)                               | ✅ Реализовано |
 | —             | `textDocument/declaration`: переход к декларации (LSP2)                       | ✅ Реализовано |
 | —             | `textDocument/documentSymbol`: структура документа (LSP3)                     | ✅ Реализовано |
@@ -1695,3 +1687,17 @@ start T {
 | I6 | 🟢 Низкий | `bin/butc.rs` | **Нет флага `--verbose` / `--quiet`**. | ✅ Реализовано: `--verbose` / `-v`, `--quiet` / `-q`; взаимоисключающие. |
 | I7 | 🟢 Низкий | `lsp.rs` | **Нет поддержки кросс-файловых переходов `goto_declaration`**. | ✅ Реализовано: `goto_declaration_with_paths()` ищет в файлах импорта. |
 | I8 | 🟢 Низкий | `semantic/index.rs` | **Statement-узлы не индексируются для LSP**. | ✅ Реализовано: `SemanticNodeKind::LocalVar`, индексируются `Let`/`Assign` внутри блоков. |
+
+### Текущие проблемы и предложения
+
+Результаты системного анализа проекта (Changes-39+). Приоритеты: 🔴 Высокий · 🟡 Средний · 🟢 Низкий
+
+| ID | Приоритет | Модуль | Проблема | Предложение | Статус |
+|----|-----------|--------|----------|-------------|--------|
+| A1 | 🔴 Высокий | `semantic/statement.rs:100` | Параметры функции не передавались в `Expression`-ветку — `construct_expression` вызывался с `vec![]` вместо `params.clone()`, из-за чего `value > 100` или `out = value` в теле функции давали LSP-ошибку «идентификатор не найден» | Передавать `params.clone()` (исправлено в Changes-39) | ✅ Исправлено |
+| A2 | 🟡 Средний | `semantic/validate.rs` (3130 строк) | Монолитный модуль, трудно поддерживать и тестировать изолированно | Разбить на `validate_refs.rs`, `validate_types.rs`, `validate_determinism.rs` | 🔲 Открыто |
+| A3 | 🟡 Средний | `lsp.rs` (~90 KB) | LSP-сервер — монолитный файл; добавление новых capability затруднено | Выделить `lsp/hover.rs`, `lsp/completion.rs`, `lsp/diagnostics.rs` | 🔲 Открыто |
+| A4 | 🟡 Средний | `generator/c/c_source.rs` (~58 KB) | Кодогенератор — монолитный файл | Разбить на `c_stmt.rs`, `c_expr.rs`, `c_func.rs` | 🔲 Открыто |
+| A5 | 🟢 Низкий | Весь проект | `unwrap()` в критических местах — потенциальные паники в production | Заменить на `?` или `expect("сообщение")` с диагностическим текстом | 🔲 Открыто |
+| A6 | 🔲 Открыто | — | C2: перегрузка `=` в условиях (`cond A = x = 0`) неоднозначна | Ввести `==` в грамматику для `cond`/`ref` | 🔲 Открыто |
+| A7 | 🔲 Открыто | — | Ce7: нет стандартной библиотеки для встраиваемых систем | Создать модуль `std.but` с `clamp`, `min`, `max`, `abs` | 🔲 Открыто |
