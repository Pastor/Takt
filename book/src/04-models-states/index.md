# Модели и состояния

> **Раздел в разработке.** Здесь будет описан(ы) `model`, `state`, стартовое состояние; составные состояния (последовательная `+` и параллельная `\|`); переходы `ref`.

<!-- В раздел помещается описание ЯЗЫКА: конструкции, семантика, примеры на
     Takt (в подкаталоге examples/) и, при необходимости, изображения (images/).
     Детали реализации инструментов и служебные сведения сюда не попадают. -->
