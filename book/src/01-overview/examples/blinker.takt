// Подсветка: лампа led горит, пока нажата кнопка btn.
model Lamp {
    in btn: bit;
    out led: bit;

    start Off {
        enter { led := 0; }
        ref On: btn = 1;
    }

    state On {
        enter { led := 1; }
        ref Off: btn = 0;
    }
}

start Main = Lamp;
