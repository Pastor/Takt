// Переключатель: кнопка btn переключает состояние, выход led его отражает.
model Blinker {
    in btn: bit;
    out led: bit;

    start state Off {
        enter { led := 0; }
        ref On: btn = 1;
    }

    state On {
        enter { led := 1; }
        ref Off: btn = 1;
    }
}
