# Порождённый код примера

Здесь приведён **полный** код, порождённый из модели контроллера кабины лифта
(раздел [«Практический пример»](../18-showcase/index.md)) для каждой цели
генерации. Один и тот же исходник `lift.takt` даёт все эти файлы **без правок** —
командой `taktc compile -t <цель> lift.takt -o out/`. Код воспроизводим побайтно
(генерация детерминирована), поэтому его можно сравнивать между версиями.

## Цель `c` — прошивка МК (порты через HAL-колбэки)

Заголовок `lift.h`:

```c
{{#include ../18-showcase/generated/c/lift.h}}
```

Реализация `lift.c`:

```c
{{#include ../18-showcase/generated/c/lift.c}}
```

## Цель `c-hal` — прошивка с прямым доступом к регистрам

Отличается от `c` слоем аппаратной абстракции: адреса портов (`0x50000010` и др.)
превращаются в доступ по памяти. Заголовок `lift.h`:

```c
{{#include ../18-showcase/generated/c-hal/lift.h}}
```

Реализация `lift.c`:

```c
{{#include ../18-showcase/generated/c-hal/lift.c}}
```

## Цель `rust` — `no_std` Rust

```rust
{{#include ../18-showcase/generated/rust/lift.rs}}
```

## Цель `st` — ПЛК, Structured Text (IEC 61131-3)

```pascal
{{#include ../18-showcase/generated/st/lift.st}}
```

## Цель `sv` — синтезируемый SystemVerilog (FPGA/ASIC)

```systemverilog
{{#include ../18-showcase/generated/sv/lift.sv}}
```

# Механизмы времени — что порождается

Раздел [«Механизмы времени»](../12-time/index.md) описывает конструкции; здесь —
код, который из них получается. Источник — пример вытяжного вентилятора того же
раздела (`fan.takt`): модель объявляет частоту `clock 1kHz` и выходит из состояния
выбега по `after 3m`.

Приведения `duration` ↔ число здесь не разбираются: единица перехода —
миллисекунда, и приведение сводится к обычной целочисленной арифметике.

## Цель `c`: выдержка — счётчик тактов

Когда частота тактирования известна, длительность **не** доживает до времени
выполнения: компилятор превращает её в число тактов, и в порождённом коде остаётся
целочисленное сравнение. Заголовок `fan.h`:

```c
{{#include ../12-time/generated/c/fan.h}}
```

Реализация `fan.c`:

```c
{{#include ../12-time/generated/c/fan.c}}
```

### Что здесь стоит рассмотреть

**Длительности в коде нет — есть число тактов.** Выдержка `after 3m` при частоте
1 кГц превратилась в сравнение:

```c
if (model->takt_dwell >= 180000) {
```

Три минуты — это 180 000 тактов по миллисекунде, и пересчёт сделан **на
компиляции**. Поэтому на устройстве не нужны ни часы, ни деление — только
инкремент и сравнение. По той же причине компилятор отказывается собирать модель,
у которой длительность не выражается целым числом тактов: округлять он не имеет
права.

**Разрядность счётчика выбрана по самой длинной выдержке.** В структуре модели
объявлено `uint32_t takt_dwell;` — 180 000 не помещается в 16 бит. Будь самая
долгая выдержка короче 256 тактов, хватило бы `uint8_t`: разрядность подбирается
под модель, а не берётся «с запасом».

**Счётчик обновляется один раз — в конце такта:**

```c
if ((unsigned)model->state != model->takt_prev_state) {
    model->takt_dwell = 1;
    model->takt_prev_state = (unsigned)model->state;
} else {
    model->takt_dwell++;
}
```

Смена состояния в этом такте означает вход в новое состояние, поэтому счётчик
становится единицей: к следующему такту с момента входа пройдёт ровно один такт.
Обновление стоит в **одном** месте, а не рядом с каждым присваиванием
`model->state`.

Значение, которое видят условия на такте, оставлено предыдущим тактом. Отсюда
свойство, заявленное в разделе: на такте входа в состояние выдержка равна нулю.

**Поля появляются только там, где нужны.** Ни `takt_dwell`, ни `takt_prev_state`
не эмитируются, если модель не использует `after`: код модели без времени в
точности такой же, каким был бы без этих механизмов.

### Отпечаток контракта частоты

Модель объявила `clock 1kHz`, и сборке передан совпадающий `--tick-hz=1000`
(иначе компиляция отказала бы — контракт частоты, `SE-069`/`SE-070`). В заголовок
цель вписывает объявленную частоту статическим утверждением:

```c
#define TAKT_REQUIRED_CLOCK_HZ 1000u
#ifndef TAKT_TICK_HZ
#define TAKT_TICK_HZ TAKT_REQUIRED_CLOCK_HZ
#endif
_Static_assert(TAKT_TICK_HZ == TAKT_REQUIRED_CLOCK_HZ,
    "частота тактирования не совпадает с объявленной моделью Takt");
```

По умолчанию `TAKT_TICK_HZ` равен объявленной частоте — код собирается без
настройки. Интегратор, задав `TAKT_TICK_HZ` реальной частотой своего тактового
источника, ловит несовпадение **на компиляции прошивки**: там, где выдержки уже
пересчитаны в такты, ошибка в частоте иначе прошла бы молча.

### Профиль «часы»: метка времени и разность

Если частота **не** объявлена (и не задана `--tick-hz`), время идёт по профилю
«часы»: выдержка меряется не тактами, а внешним источником. Цель добавляет в
корневую структуру колбэк

```c
uint64_t (*now_ms)(void *userdata);
```

и метку входа `takt_entry_ms`. На входе в состояние метка латчит текущее время
(`model->takt_entry_ms = now_ms(...)`), а условие сравнивает **разность**:

```c
if ((uint8_t)((uint8_t)main->now_ms(main->userdata) - model->takt_entry_ms) >= 5) {
```

Сравнение именно разностью, а не `t0 + D <= now`: беззнаковая обёртка счётчика
определена и нормирована, поэтому выдержка остаётся верной даже когда часы
переходят через край разрядности метки. Ширину метки выбирает компилятор — самую
узкую, вмещающую наибольшую выдержку модели.

Источник времени подставляет интегратор (как реализацию портов). Цель `c-hal`
даёт **дефолт** — библиотечные монотонные часы `clock_gettime(CLOCK_MONOTONIC)`,
связываемые автоматически. ⚠️ Дефолт тянет `<time.h>`, которого на голом железе
нет: там частоту объявляют (`clock`) и берут профиль «такты» либо пишут свой
`now_ms`.

## Остальные цели

Порождение времени для целей `rust`, `st` и `sv` описывается здесь по мере того,
как каждая из них его получает. Сейчас модель с временем на них не транслируется,
и компилятор говорит об этом прямо — вместо кода, считающего не то.
