struct Point { x: u8, y: u8 }
model Conf {
    var p: Point := {1, 2};
    var q: Point := {1, 2};
    var t: u8 := 0;
    start Idle {
        ref Done: p = q;
    }
    state Done {
        always { t := 1; }
    }
}
start Entry = Conf;
