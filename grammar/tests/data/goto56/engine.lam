// Цель перехода для КОНТРПРИМЕРА: импортируется под алиасом `Motor`.
// Модель называется Engine, файл — engine.lam, а в импортирующем файле имя
// модели — Motor. Угадывание искало бы motor.lam и не нашло бы ничего.
model Engine {
    var rpm: u8 := 0;

    start Stopped { ref Stopped; }
}
