// Контрпример: ссылка на необъявленную переменную в условии.
//
// Ошибка: 'ghost' не объявлена нигде в области видимости.
// Ожидаемая ошибка семантики: "Переменная или условие 'ghost' не найдены".

cond bad = ghost;
start S;
