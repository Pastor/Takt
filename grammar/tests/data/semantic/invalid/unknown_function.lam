// Контрпример: вызов необъявленной пользовательской функции в именованном блоке.
//
// Функция ghost не объявлена ни как локальная, ни как внешняя.
// Встроенных функций с таким именем также не существует.

var x: bit = 0;

start S {
    always {
        x = ghost();
    }
}
