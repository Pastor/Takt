// Файл a.but для проверки обнаружения циклических импортов.
//
// Образует цикл: a.but → b.but → a.but
// При обработке этого файла семантический анализатор должен
// вернуть ошибку «Циклический импорт».

import "cycle_b.but";

start Entry = CycleB { }
state Done;
