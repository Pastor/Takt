// Пример: простой конечный автомат (семантически корректный).
//
// Описывает автомат из двух состояний: Start → Finish.
// Переход происходит безусловно (без условия ref).

/// Стартовое состояние автомата.
start Start {
    ref Finish;
}

/// Завершающее состояние.
state Finish;
