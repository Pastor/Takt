// Ce6: Вывод типа переменной из возвращаемого типа функции.
//
// Функция getbool возвращает bool.
// Переменная result получает тот же тип без явного объявления.
// Ожидается: нет ошибок семантики.

fn getbool() -> bool {
    return true;
}

var result = getbool();
start S;
