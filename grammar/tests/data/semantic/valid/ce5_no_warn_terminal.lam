// Ce5: Все состояния достигают терминального, нет предупреждений.
//
// Цепочка: Start -> Middle -> End (терминальное).
// Ожидается: нет предупреждений о достижимости.

start Start {
    ref Middle: true;
}
state Middle {
    ref End: true;
}
state End;
