# Отчёт о тестировании фичи 0226: Канон именования: предупреждение в fmt и LSP

> Фича: [../features/0226-naming-convention-warning.md](../features/0226-naming-convention-warning.md) · тест-план: [../tests/0226-naming-convention-warning.md](../tests/0226-naming-convention-warning.md) · анализ: [../analyze/0226-naming-convention-warning.md](../analyze/0226-naming-convention-warning.md)

## Резюме

**Пройдено; фича готова к закрытию.** Все 16 проверок тест-плана зелены, все 8
критериев приёмки ADR выполнены, `./scripts/precheck.sh` — код возврата 0.

Прогоны: `cargo test --test style_naming_fmt_tests` — 9/9;
`cargo test --all-features --test style_naming_lsp_tests` — 5/5; юнит-тесты
`style/naming.rs` — 5/5; `taktc fmt --check examples/` — ноль предупреждений
(было 49).

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | `fmt --check`, неканоничное имя | ✅ | `probe.takt:2:9: Предупреждение [CS-001]: порт 'BadPort' … snake_case`, код 0 |
| T2 | `fmt` на месте | ✅ | отступ исправлен, имя осталось `BadPort` |
| T3 | неканоничный формат рядом с `CS-001` | ✅ | код 1 даёт формат; вердикт и предупреждение различимы |
| T4 | семантически некорректный файл | ✅ | предупреждение выдано; `compile` на том же файле — `SE-034`, код 1 |
| T5 | `--stdin` | ✅ | код и текст есть, префикса позиции нет, текст в stdout |
| T6 | 12 видов объявлений | ✅ | все 12 с верным видом, именем и ожидаемой формой |
| T7 | тот же состав в каноне | ✅ | ни одного предупреждения |
| T8 | вложенная модель | ✅ | ровно одно предупреждение — о `inner` |
| T9 | LSP: уровень и код | ✅ | `WARNING`, `CS-001`, `takt-lsp` |
| T10 | LSP: диапазон | ✅ | строка 1, колонка 8, длина 7 — имя целиком |
| T11 | LSP против `fmt` | ✅ | строки равны буквально |
| T12 | LSP на каноничном имени | ✅ | ни предупреждений, ни ошибок |
| T13 | LSP при ошибках | ✅ (зафиксировано как есть) | `CS-001` не показан; `fmt` на том же входе печатает — расхождение записано кандидатом |
| T14 | предикаты форм | ✅ | кириллица и имя без букв — по правилам ADR |
| T15 | корпус | ✅ | ноль предупреждений на `examples/**/*.takt` и примерах документа |
| T16 | пригодность проверок (мутации) | ✅ | 4 мутации, каждая валит ожидаемый тест — таблица в задаче 0226-03 |

## Результаты по функциональности

| Функциональность | Статус | Чем подтверждено |
|---|---|---|
| `taktc fmt` (файлы, `--check`, `--stdin`) | ✅ | T1–T8 прогоном бинаря |
| LSP | ✅ | T9–T13 под `--all-features` |
| `taktc compile` | — | не задета: заказчик назвал `fmt` и LSP |
| Цели генерации, симулятор | — | не задеты: канон на перевод и исполнение не влияет |
| Корпус `examples/` | ✅ | T15 + гейты предкоммита на перегенерированных снапшотах |
| Документ | ✅ | раздел «Стиль кода», приложение об ошибках, раздел о диагностиках; гейты ссылок и глифов |

## Выводы и дальнейшие шаги

Исправлений (`docs/fixes/0226-YY-*`) не потребовалось: дефектов в реализации
проверки прогон не выявил.

**Что показал эксперимент — три наблюдения, вышедшие за объём фичи:**

1. **Замер ADR был неполон.** Греп по формам именования назвал два файла и пять
   портов; готовый инструмент нашёл **49** нарушений на **трёх** файлах (в том
   числе `type u128`, которого грепом не увидеть). Урок общий: замер о форме
   имён надо делать инструментом, а не грепом, — и это возможно только после
   того, как инструмент написан, то есть порядок «сначала слой, потом корпус»
   оказался верным ещё и по этой причине.
2. **Редактор скрывает `CS-001`, пока в файле есть ошибки, а `fmt` — печатает.**
   Проверке семантика не нужна по построению, но политика LSP «сперва ошибки»
   старше фичи. Смена политики — решение заказчика, поэтому записано кандидатом
   в `FEATURES.md`; сегодняшнее поведение пришпилено тестом T13.
3. **`taktc compile` печатает предупреждения без позиции** (`Предупреждение
   [SE-036]: …`), хотя координата в диагностике есть: `compile_cli` не ставит
   путь, а `position_prefix` без пути отдаёт пустую строку. Два потребителя
   одного `format_warning` расходятся не форматом, а полнотой входа. Записано
   кандидатом — правка на строку, но меняет наблюдаемый вывод `compile` на всём
   корпусе.
