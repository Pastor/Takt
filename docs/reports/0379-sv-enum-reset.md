# Отчёт о тестировании фичи 0379: Сброс перечислимого регистра у цели sv

> Фича: [../features/0379-sv-enum-reset.md](../features/0379-sv-enum-reset.md) · тест-план: [../tests/0379-sv-enum-reset.md](../tests/0379-sv-enum-reset.md) · анализ: [../analyze/0379-sv-enum-reset.md](../analyze/0379-sv-enum-reset.md)

## Резюме

**Пройдено, фича готова к закрытию.** Мутация проверена и роняет сверку
значений; `precheck.sh` зелёный; вывод корпуса не изменился.

Замер после правки: `sv` и `sv-mmio` — `verilator принял · yosys синтезировал`
на обоих входах (переменная без инициализатора и порт перечислимого типа).

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Значение сброса — ноль | ✅ | `enum_reset_values_match_generated_sv`: `[[0, 0], [2, 1]]` |
| T2 | Линт принимает вывод | ✅ | `enum_reset_passes_the_lint_and_uses_the_right_form` |
| T3 | Форма без нулевого варианта | ✅ | `<= mode_e'(0);` |
| T4 | Форма с нулевым вариантом | ✅ | `<= PHASE_ZERO;` |
| T5 | Синтез | ✅ | проба: `yosys синтезировал` |
| T6 | Вывод корпуса | ✅ | `git diff examples/generated/` пуст |
| M1 | Мутация «первый вариант» | ✅ | `RTL=[[1, 0], …]` против `sim=[[0, 0], …]` |

## Результаты по функциональности

| Функциональность | Итог |
|---|---|
| цель `sv`/`sv-mmio` | ✅ линт и синтез приняли, значение прежнее |
| прочие цели, эталон | — не затронуты |

## Выводы и дальнейшие шаги

1. **Расходились две ветви одного правила:** «есть инициализатор» была права с
   0338, «инициализатора нет» печатала `'0` для любого типа.
2. **Линт мало, нужна сверка:** подстановка первого варианта прошла бы линт и
   изменила значение — это и показала мутация.
3. **Класс нашёл обходной замер** (проба порта, снимавшаяся ради другого
   вопроса).
4. Расхождение «ноль против первого варианта» с целью `rust` остаётся вопросом
   заказчика (кандидат 0353).
5. Исправлений не потребовалось.
