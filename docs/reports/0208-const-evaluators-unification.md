# Отчёт о тестировании фичи 0208: Три константных вычислителя компилятора живут порознь

> Фича: [../features/0208-const-evaluators-unification.md](../features/0208-const-evaluators-unification.md) · тест-план: [../tests/0208-const-evaluators-unification.md](../tests/0208-const-evaluators-unification.md) · анализ: [../analyze/0208-const-evaluators-unification.md](../analyze/0208-const-evaluators-unification.md)

## Резюме

**Пройдено. Фича готова к закрытию (`ГОТОВО`).** Восемь проверок зелёные,
критерии A1–A5 выполнены. Поведение не изменилось **ни в чём** — это и было
целью рефакторинга.

Окружение: macOS 25.5.0, толчейн `1.97.1`, `./scripts/precheck.sh` — код 0.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Одно значение в обеих позициях | ✅ | все десять операций совпали |
| T2 | Деление на ноль | ✅ | `SE-055` с прежним текстом у адреса |
| T3 | Граница сдвига | ✅ | 64 — отказ, 63 — значение |
| T4 | Сравнение в адресе | ✅ | отвергает матчер, список форм в тексте |
| T5 | Обёртка по 64 битам | ✅ | `i64::MAX + 1` → `i64::MIN` |
| T6 | Таблица сама по себе | ✅ | 6 юнит-тестов |
| T7 | Корпус | ✅ | без изменений |
| T8 | Предкоммит | ✅ | код 0 |

### Почему здесь нет мутации

⚠️ Тесты зелены и на версии **до** правки — и это не слабость, а свойство
задачи: фича ничего не исправляет, она убирает **дубль правил**. Мутация
показывает, что тест ловит дефект; здесь дефекта не было — было два текста
одного правила, уже разошедшихся формулировками.

Ценность сторожа — будущая: правило поправят в одной таблице, и T1–T3 назовут
разошедшуюся операцию. Сегодня же доказательством служит другое — **дубль
исчез**: таблица операций существует в единственном экземпляре, а вызывающие
переведены в тонких переводчиков.

## Результаты по функциональности

| Функциональность | Статус | Чем проверено |
|---|---|---|
| Выражение адреса (0042) | ✅ | T1–T5 + существующие `address_map_tests` |
| Общий вычислитель (0185/0192) | ✅ | T1–T3, T6 + тесты свёртки |
| Константная выдержка (0143) | — | вне объёма (другой домен) |
| Цели генерации, симулятор, документ | — | не задеты |

## Выводы и дальнейшие шаги

1. **Название карточки шире сделанного.** Вычислителей по-прежнему три; фича
   сняла **дубль правил**, а не их число. Итог карточки это оговаривает: третий
   вычислитель (выдержка) в объём не входит по существу — у него другой домен и
   другая семантика переполнения.
2. **Сессия дала три взгляда на один вопрос, и они не противоречат:**
   [0205](../features/0205-as-in-declaration-initializer.md) провела **границу**
   между вычислителями (приведение — право эталона),
   [0207](../features/0207-bitwise-not-unsigned-literal.md) добавила свёртке
   нормирование, 0208 свела **правила** в одно место. Общее правило —
   объединять следует правила, а не вычислители.
3. **Находка попутно, заведена кандидатом:** `const K := 1 << 8;` даёт `0` —
   тип выводится от левого операнда (`[bit;8]`), и нормирование 0207
   заворачивает 256 в ноль. Прежде это была ошибка `SE-089`. Поведение следует
   из принятых правил, но неочевидно.
4. Фиксов не потребовалось.
