# Отчёт о тестировании фичи 0333: раздел об ошибках программы

> Фича: [../features/0333-book-runtime-errors.md](../features/0333-book-runtime-errors.md) · тест-план: [../tests/0333-book-runtime-errors.md](../tests/0333-book-runtime-errors.md) · ADR: [../adr/0333-book-runtime-errors.md](../adr/0333-book-runtime-errors.md)

## Окружение

- macOS (darwin 25.5.0), Typst 0.15.1. Дата: 2026-08-20.

## Сводка прогонов

| Прогон | Результат |
|---|---|
| Эталон: `a / z` при `z = 0` | `SIM-001` |
| Эталон: `u as i8` при `u = 200` | `SIM-003` |
| Эталон: `w.70` при `w: [bit;8]` | `SIM-011` |
| `make -C book build` | документ собран |
| `check-book-glyphs.py`, `check-book-code-langs.py` | зелёные |
| `./scripts/precheck.sh` | код 0 |

## Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П4 | Все | ✅ |

## Что показал прогон

1. **Класс существует по замыслу, но был неописан.** Три разных диагностики
   останавливают прогон, и ни одна не имела объяснения, чем это отличается от
   прошивки.
2. **Оговорка про компиляцию нужна:** та же запись в объявлении отвергается
   `SE-121` — то есть строгость зависит не от инструмента, а от того, известно
   ли значение заранее.

## Дефекты

Не найдено.

## Редакторский слой (правило 29)

**Не требуется** — подтверждено: правится документ.

## Вердикт

**Готово.** Разница между прогоном и прошивкой описана и проверена прогоном.
