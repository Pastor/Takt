# Отчёт о тестировании фичи 0386: `SE-036` видит локальные объявления тел

> Фича: [../features/0386-unused-local-warning.md](../features/0386-unused-local-warning.md) · тест-план: [../tests/0386-unused-local-warning.md](../tests/0386-unused-local-warning.md) · анализ: [../analyze/0386-unused-local-warning.md](../analyze/0386-unused-local-warning.md)

## Резюме

**Пройдено, фича готова к закрытию.** Десять проверок тест-плана выполнены, все
три мутации ловятся, `./scripts/precheck.sh` зелёный, корпус новых
предупреждений не даёт.

## Окружение

macOS 25.5, `rustc` 1.97.1; прогоны — `cargo test --all-features`,
`taktc` по всем `examples/*.takt`, `precheck.sh`.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Неиспользуемая локальная | ✅ | `SE-036` про `spare` |
| T2 | Координата | ✅ | указывает на `var spare` (проверено смещением) |
| T3 | Используемая локальная | ✅ | молчит |
| T4 | Вложенный блок | ✅ | `deep` внутри `if` проверяется |
| T5 | Область видимости | ✅ | использование во вложенном блоке законно |
| T6 | Блок уровня модели | ✅ | `idle_only` |
| T7 | Тело функции | ✅ | `tmp` |
| T8 | Мутации мест обхода | ✅ | три из трёх роняют сторожа |
| T9 | Корпус | ✅ | новых предупреждений ноль |
| T10 | Знание одно | ✅ | `generator::local_stub` — обёртка |

## Результаты по функциональности

| Функциональность | Итог | Чем подтверждено |
|---|---|---|
| семантика | ✅ | пять тестов + мутации |
| CLI `taktc compile` | ✅ | предупреждение печатается с координатой |
| LSP | ✅ | доставка через общую точку `collect_model_warnings` (правок не потребовала) |
| цели генерации, эталон | ✅ | поведение не менялось; 28 мест правки — механические |

## Найденные дефекты

Фиксов не потребовалось.

## Выводы и дальнейшие шаги

**1. Первая редакция сторожа была недостаточной, и это показала мутация.**
Проверка покрывала одно место объявления из трёх: `always` фикстуры принадлежал
**состоянию**, поэтому мутация «снять обход блоков модели» её не роняла. Тест
дополнен входами уровня модели и функции; после этого ловятся все три мутации.
Урок тот же, что у 0383: сторож обязан быть чувствителен к каждому месту, а не
к классу вообще.

**2. Позиция узла оказалась частью предмета, а не побочной правкой.** Без неё
предупреждение печаталось бы с `1:1` — ложью вместо отсутствия координаты
(класс 0264); правка попутно сняла границу, которую 0264 сама и назвала.
Компилятор провёл по всем 28 потребителям узла — молча уехать было нечему.

**3. Ограничение размера модуля вмешалось в форму правки.** `semantic/mod.rs`
в узаконенном долге и расти не имеет права, поэтому объявление варианта ужато
до двух строк, а объяснение живёт у носителя признака. Это не косметика: гейт
отверг первую редакцию (+11 строк).

**4. Шума нет.** Прогон по всем примерам корпуса дал ноль новых предупреждений:
признак 0376 консервативен — запись считается использованием.

**5. Кандидаты.** Закрытие снимает кандидата «`SE-036` не видит локальных
объявлений тел». Соседний кандидат («локальная, записанная но не прочитанная»)
остаётся: признак по-прежнему считает запись использованием, и это осознанная
граница.

**Вердикт: ГОТОВО.**
