# Отчёт о тестировании фичи 0367: Массив структур у цели sv синтезируется

> Фича: [../features/0367-sv-struct-array.md](../features/0367-sv-struct-array.md) · тест-план: [../tests/0367-sv-struct-array.md](../tests/0367-sv-struct-array.md) · анализ: [../analyze/0367-sv-struct-array.md](../analyze/0367-sv-struct-array.md)

## Резюме

**Пройдено, фича готова к закрытию.** Массив структур переводится, вывод
проходит линт **и** синтез, значения совпадают с эталоном такт в такт.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Запись поля элемента | ✅ | `verilator принял · yosys синтезировал` |
| T2 | Агрегат целиком | ✅ | граница 0366 снята |
| T3 | Массив без инициализатора | ✅ | сброс по полям |
| T4 | Массив скаляров не затронут | ✅ | признак «структура внутри массива» |
| T5 | Сверка значений | ✅ | `(11,21,0), (12,22,0), (13,23,0)` |
| T6 | Проверка сброса значением | ✅ | `spare = 0` в обеих трассах |
| T7 | Синтез | ✅ | отдельный тест `struct_array_is_synthesizable` |
| T8 | Мутация «умолчание целиком» | ✅ | «Latch inferred for `cells_next[0].lo`» |
| T9 | Мутация «сброс целиком» | ✅ | «не синтезируется» |
| T10 | Контроль корпуса | ✅ | 11 примеров — ноль тревог |
| T11 | Регресс | ✅ | conformance 165, targets 391 |
| T12 | Вывод корпуса не изменился | ✅ | предкоммит без диффа |

## Результаты по функциональности

| Функциональность | Статус | Замечание |
|---|---|---|
| `sv_array::type_leaves` / `needs_leafwise` | ✅ | признак один на сброс и умолчание |
| `sv_fsm` (`Reg::leaves`) | ✅ | сброс и умолчание по полям |
| `sv_stmt` | ✅ | временная граница 0366 снята |

## Выводы и дальнейшие шаги

- **Класс видел один инструмент из трёх.** Линт цели и сверка значений идут
  через verilator, который обе неверные формы принимает; вердикт дал только
  `yosys synth` — поэтому у фичи отдельный тест синтеза.
- **Дефект был двухслойным:** починка сброса открыла защёлку на умолчании.
  Замер пришлось снимать дважды.
- **Граница, введённая 0366, снята той же неделей** — она и заводилась как
  временная, чтобы не заменять громкий отказ молчаливым несинтезом.
- Исправлений (`docs/fixes/`) не потребовалось.
