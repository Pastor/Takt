# Отчёт о тестировании фичи 0315: сторожа гейтов

> Фича: [../features/0315-gate-guards.md](../features/0315-gate-guards.md) · тест-план: [../tests/0315-gate-guards.md](../tests/0315-gate-guards.md) · ADR: [../adr/0315-gate-guards.md](../adr/0315-gate-guards.md)

## Окружение

- macOS (darwin 25.5.0), POSIX sh, Python 3.14. Дата: 2026-08-20.

## Замер (правило 30)

| Состояние гейта | До | После |
|---|---|---|
| внешний сторож | 8 | 11 |
| внутренняя самопроверка | 12 | 13 |
| **ни того, ни другого** | **4** | **0** |
| подсказка (код возврата не меняет) | 1 | 1 |

Четвёрка: `check-exhaustive-nodes.sh`, `check-language-version.sh`,
`check-repo-url.sh`, `check-links.py`.

## Сводка прогонов

| Прогон | Результат |
|---|---|
| `check-links.py --self-test` | ловушка взведена |
| `sh scripts/test-exhaustive-nodes.sh` | 4/4 |
| `sh scripts/test-language-version.sh` | 5/5 |
| `sh scripts/test-repo-url.sh` | 5/5 |
| `./scripts/precheck.sh` | код 0 |

## Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П4 | Ссылки и исчерпаемость | ✅ |
| П5…П8 | Версия языка и адрес | ✅ |
| П9…П10 | Здоровый вход, предкоммит | ✅ |

## Что показал прогон

1. **Список бэклога устарел.** Он называл пять гейтов; двое из них прикрытие
   уже получили. Замер снят заново — правило 30 сработало на процессной записи
   так же, как на дефектной.
2. **Якорь живого контекста строже, чем казалось.** Первая редакция сторожа
   версии писала в `CLAUDE.md` произвольную строку с номером — гейт отверг её:
   он требует форму «**сейчас `X.Y.Z`**» рядом с упоминанием константы. То есть
   сторож заодно проверил, что гейт **не** довольствуется любым числом.
3. **Проверка «атрибут в комментарии»** воспроизводит настоящую дыру фичи 0163
   — гейт искал подстроку где угодно в строке и находил её в док-комментарии.

## Дефекты

Дефектов гейтов не найдено: все четыре ведут себя как обещано, и теперь это
доказано, а не предполагается.

## Редакторский слой (правило 29)

**Не требуется** — подтверждено: код инструментов не менялся.

## Вердикт

**Готово.** Каждый гейт предкоммита либо имеет сторожа, либо проверяет себя сам.
