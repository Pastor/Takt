# Отчёт о тестировании фичи 0205: Приведение as не вычисляется в инициализаторе объявления

> Фича: [../features/0205-as-in-declaration-initializer.md](../features/0205-as-in-declaration-initializer.md) · тест-план: [../tests/0205-as-in-declaration-initializer.md](../tests/0205-as-in-declaration-initializer.md) · анализ: [../analyze/0205-as-in-declaration-initializer.md](../analyze/0205-as-in-declaration-initializer.md)

## Резюме

**Пройдено. Фича готова к закрытию (`ГОТОВО`).** Четырнадцать проверок зелёные,
критерии A1–A6 выполнены, дефектов не найдено.

Окружение: macOS 25.5.0, толчейн `1.97.1`, `./scripts/precheck.sh` — код 0.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Целое → широкое целое | ✅ | было `0`, стало `5` |
| T2 | Сужение с обёрткой | ✅ | было `0`, стало `44` — как `(uint8_t)300` в C |
| T3 | Целое → длительность | ✅ | было `SIM-006` в такте, стало `250ms` |
| T4 | Длительность → целое | ✅ | было `0`, стало `2000` |
| T5 | Булево → целое | ✅ | было `0`, стало `1` |
| T6 | Целое → `q(4, 4)` | ✅ | было `repr 0`, стало `repr 48` |
| T7 | Дробное → `q(8, 8)` | ✅ | было `repr 0`, стало `repr 384` |
| T8 | Приведение ссылки на константу | ✅ | форма из карточки: было `SIM-006`, стало `250ms` |
| T9 | Взаимные константы | ✅ | построение завершается, зацикливания нет |
| T10 | Ссылка без приведения | ✅ | `7` |
| T11 | Мутация | ✅ | краснеют 8 из 10 |
| T12 | Вывод корпуса | ✅ | `examples/generated/` без изменений |
| T13 | Гейт исчерпаемости | ✅ | `deny` на месте, новые ветви разобраны явно |
| T14 | Предкоммит | ✅ | код 0 |

### Что показала мутация (T11)

Со снятыми правками краснеют T1–T8. Зелёными остаются **осознанно**:

- **T9** (завершение) — до фичи рекурсии по ссылкам не было вовсе, ломаться
  было нечему; тест сторожит **введённый** предел;
- **T10** (ссылка без приведения) — эту форму и прежде спасала свёртка
  инициализатора (0192), которая превращает `var v := A;` в литерал ещё в
  семантике. Ветвь `Variable` здесь — floor: она нужна там, где свёртка не
  сработала, то есть под `as`.

## Результаты по функциональности

| Функциональность | Статус | Чем проверено |
|---|---|---|
| Симулятор (эталон) | ✅ | T1–T10 |
| Цель `c` | ✅ | сверка строки `_init` в каждой проверке + гейты предкоммита |
| Цели `rust`, `st`, `plantuml` | — | не задеты (правка внутри `takt-sim`), вывод корпуса неизменен |
| Цель `sv` | ⬜ | отвергает форму (`SV-002`) и до, и после фичи |
| LSP, форматтер (правило 29) | — | лексика, синтаксис и узлы АСД не менялись |

## Выводы и дальнейшие шаги

1. **Кандидат снова оказался неточен.** Запись гласила: «Отказ громкий (прогон
   останавливается), молчаливой потери нет». На деле громким он был лишь у двух
   форм из восьми; в шести эталон **молча** давал ноль при верном значении у
   цели. Это второй подряд случай (после 0204), когда перепроверка замера
   кандидата меняет картину, — правило «замеры кандидата подтверждает проба»
   работает.
2. **Отвергнутая альтернатива названа в ADR не для полноты.** Свёртка `as` в
   семантике выглядела естественной (приём 0143/0187/0192), но противоречит уже
   принятому решению того же вычислителя: `const_eval` **отказывается** считать
   дробную арифметику, потому что округление `q` задано эталоном. Приведение к
   `q(m, n)` — тот же случай, и две формы из восьми остались бы с нулём.
3. **Граница вынесена кандидатом:** цель `sv` отвергает выражение в
   инициализаторе (`SV-002`), в том числе константно вычислимое.
4. Фиксов не потребовалось.
