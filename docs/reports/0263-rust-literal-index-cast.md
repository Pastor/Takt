# Отчёт о тестировании фичи 0263: Приведение индекса к `usize` по нужде

> Фича: [../features/0263-rust-literal-index-cast.md](../features/0263-rust-literal-index-cast.md) · тест-план: [../tests/0263-rust-literal-index-cast.md](../tests/0263-rust-literal-index-cast.md) · анализ: [../analyze/0263-rust-literal-index-cast.md](../analyze/0263-rust-literal-index-cast.md)

## Резюме

**Пройдено; фича готова к закрытию (`ГОТОВО`).** Четыре проверки тест-плана
зелены, исправлений не потребовалось. Полный прогон: 3245 тестов, ноль
провалов; `./scripts/precheck.sh` зелён.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Литерал без приведения, переменная — с ним | ✅ | `self.arr[1]`, `self.arr[self.i as usize]` |
| T2 | Печатник условий | ✅ | правило одно на оба печатника (`subscript`) |
| T3 | Настоящий `clippy -D warnings` | ✅ | было две ошибки `unnecessary_cast`, стало ноль |
| T4 | Корпус и гейты | ✅ | снимки не изменились: в корпусе индексируют переменной |

## Замер (правило 30)

| Исходник | До | После |
|---|---|---|
| `arr[1] := 7;` | `self.arr[1 as usize] = 7;` → **error** `unnecessary_cast` | `self.arr[1] = 7;` |
| `seen := arr[1];` | то же | `self.seen = self.arr[1];` |
| `arr[i] := 9;` **(контроль)** | `self.arr[self.i as usize] = 9;`, принято | **не изменилось** |

## Результаты по функциональности

| Функциональность | Статус | Основание |
|---|---|---|
| Цель `rust` | ✅ | T1–T3 |
| Прочие цели, эталон, LSP, документ | — | не затронуты (обосновано в анализе) |

## Выводы и дальнейшие шаги

- Исправления не нужны; фича закрывается.
- Новых кандидатов замер не дал.
- **Кандидатов, называющих 0263, не осталось** (правило 28): запись блока 2
  переведена в эту карточку пакетно 2026-08-18.
