# Отчёт о тестировании фичи 0293: Структуры в целях `st`, `sv` и `rust`

> Фича: [../features/0293-struct-in-st-rust.md](../features/0293-struct-in-st-rust.md) · тест-план: [../tests/0293-struct-in-st-rust.md](../tests/0293-struct-in-st-rust.md) · анализ: [../analyze/0293-struct-in-st-rust.md](../analyze/0293-struct-in-st-rust.md)

## Резюме

**Пройдено, фича готова к закрытию.** Структуры переводятся всеми восемью
целями; у трёх исправленных — потактовые сверки с эталоном.

`./scripts/precheck.sh` — код 0; 3304 теста, 0 провалов.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | `sv`: объявление типа | ✅ | `typedef struct packed { logic [7:0] kp; logic [7:0] ki; } gains_t;` |
| T2 | `sv`: инструменты | ✅ | `verilator --lint-only -Wall` и `yosys synth` — код 0 |
| T3 | `sv`: трасса | ✅ | совпадает с эталоном |
| T4 | `st`: инициализатор | ✅ | `g : Gains := (kp := 2, ki := 3);`, `iec2c` принимает |
| T5 | `st`: трасса | ✅ | совпадает с эталоном (через `iec2c` + `cc`) |
| T6 | `rust`: перевод | ✅ | `clippy -D warnings` принимает корпус |
| T7 | `rust`: трасса | ✅ | совпадает с эталоном |
| T8 | Границы 0250 | ✅ | сторож обновлён: поле структуры теперь переводится, массив у `sv` — по-прежнему нет |
| T9 | Регресс | ✅ | `precheck.sh` код 0 |

## Замер 2026-08-19 и поправка к карточке

| Потребитель | Было | Класс |
|---|---|---|
| эталон, `c`, `c-hal`, `plantuml` | переводят | — |
| `st`, `st-at` | переводили всё, кроме **инициализатора** — терялся молча | `Tier 1` |
| `sv`, `sv-mmio` | ссылка на необъявленный тип, `verilator`: `Can't find typedef` при коде 0 | `Tier 2` |
| `rust` | `RS-011` — честный отказ | `Tier 3` |

⚠️ Карточка утверждала, что `st` отвечает `ST-011`. Сегодня цель структуры
**переводит** — условие перепроверено, а не унаследовано (урок 0161).

⚠️ **Проба с первого раза оказалась негодной дважды**, и обе ловушки назвал сам
`probe.sh`: имя файла совпало с именем типа (класс 0195) и висячая запятая в
объявлении полей.

## Класс жил в корпусе — и гейт его не видел

Дифф снапшотов: `examples/generated/st/pid_heater.st` объявлял
`loop_pid : PidState;` — то есть **коэффициенты регулятора** (`kp = 0.5`,
`ki = 0.0625`, `kd = 0.25`, границы выхода) до ПЛК не доезжали вовсе. Прошивка
считала с нулей. `iec2c` такой ST принимал, гейт был зелёным.

## Находки сверх ожидаемого

1. **Именованную форму агрегата SV отвергает yosys.** `'{kp: 2, ki: 3}`
   `verilator` принимает, `yosys` — `syntax error, unexpected ':'`. Принята
   позиционная конкатенация с **размерными** литералами (`{8'd2, 8'd3}`):
   безразмерные дают `WIDTHCONCAT`. Тот же урок, что у `assert … else` (0235):
   форма выбирается по тому, что принимают **оба** инструмента.
2. **`Eq` нельзя выводить для структуры с `float`.** `f64` его не реализует —
   вывод корпуса перестал бы компилироваться.
3. **Запись в поле требует `let mut`.** `rustc`: `E0594` на `pid_heater`.
   Случай не возникал, пока поля не переводились.

## Результаты по функциональности

| Функциональность | Статус | Комментарий |
|---|---|---|
| Цель `sv` / `sv-mmio` | ✅ | `sv_type::emit_structs`, `sv_const::struct_reset` |
| Цель `st` / `st-at` | ✅ | `st_decl::struct_init` |
| Цель `rust` | ✅ | `rust_decl::emit_structs`, `rust_struct::struct_literal`, доступ к полю |
| Цель `c` / `c-hal`, эталон | ✅ | не затронуты |
| Документ `book/` | ✅ | раздел о типах называет охват целей |
| LSP / плагины | — | язык не менялся (правило 29, ответ в анализе) |

## Выводы и дальнейшие шаги

Исправлений (`docs/fixes/0293-YY-*`) не потребовалось.

Вынесено кандидатом (правило 28): **массивы в цели `sv`** — по-прежнему
отвергаются (`SV-002`), и сторож границ 0250 это фиксирует. Класс тот же, что
был у структур: язык допускает, эталон исполняет, цель не умеет.
