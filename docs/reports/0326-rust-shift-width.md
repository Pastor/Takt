# Отчёт о тестировании фичи 0326: сдвиг на ширину типа

> Фича: [../features/0326-rust-shift-width.md](../features/0326-rust-shift-width.md) · тест-план: [../tests/0326-rust-shift-width.md](../tests/0326-rust-shift-width.md) · ADR: [../adr/0326-rust-shift-width.md](../adr/0326-rust-shift-width.md)

## Окружение

- macOS (darwin 25.5.0), Rust 1.97.1, verilator 5.050. Дата: 2026-08-20.

## Замер (правило 30)

| Потребитель | До | После |
|---|---|---|
| эталон, `c`, `c-hal`, `st`, `st-at`, `sv`, `sv-mmio` | `-1` и `0` | без изменений |
| **`rust`** | **вывод не компилируется** (`rustc`: overflow) | `-1` и `0`, собирается |

## Сводка прогонов

| Прогон | Результат |
|---|---|
| `cargo test --test targets rust_shift_width` | 3 passed |
| Ручной прогон `rustc` на порождённом модуле | `v=-1 w=0` |
| `cargo test --all-features` | провалов нет |
| `./scripts/precheck.sh` | код 0 |

## Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П4 | Форма, отсутствие прежней, контроль, граница | ✅ |
| П5…П6 | Прогон `rustc`, регрессия | ✅ |

## Что показал прогон

1. **Класс был назван заранее** — фичей 0325 как невошедший. Замер подтвердил
   его и сузил: расходится **одна** цель, и расходится невалидным выводом.
2. **Гейт цели `rust` этого не видит:** он гоняет только корпус, а сдвигов на
   ширину типа там нет.

## Дефекты

Дефектов, кроме предмета фичи, не найдено.

## Редакторский слой (правило 29)

**Не требуется** — подтверждено: язык и диагностики не менялись.

## Вердикт

**Готово.** Вывод собирается, значение совпадает с эталоном.
