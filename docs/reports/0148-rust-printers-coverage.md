# Отчёт о тестировании фичи 0148: Покрытие печатников цели rust тестами

> Фича: [../features/0148-rust-printers-coverage.md](../features/0148-rust-printers-coverage.md) · тест-план: [../tests/0148-rust-printers-coverage.md](../tests/0148-rust-printers-coverage.md) · анализ: [../analyze/0148-rust-printers-coverage.md](../analyze/0148-rust-printers-coverage.md)

## Резюме

**Пройдено; фича готова к закрытию (`ГОТОВО`).** 27 тестов закрывают ветви
печатников сверкой текста; покрытие `rust_cond.rs` выросло 23.0 → **64.6 %**.
Найдены **два** класса входа, на которых порождённый Rust не компилируется:
первый исправлен (фикс 0148-01), второй заведён с разбором (0148-02).

## Окружение

- Толчейн — пин `rust-toolchain.toml` (стабильный 1.97.1); `cargo-llvm-cov`
  доступен.
- Прогон: `cargo test --all-features -- --test-threads=1`,
  `./scripts/coverage.sh --files`, `./scripts/precheck.sh` (код выхода 0).

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Скобки у каждого бинарного узла | ✅ | инвариант ADR 0050 впервые получил свидетеля |
| T2–T8 | Условия: операторы, перечисление, массив, бит, вызовы | ✅ | 7 из 7 |
| T9 | `bit` с числом | ✅ | ⚠️ **до фикса** давало `bool == 1` → `E0308` |
| T10–T13 | `bit`: ноль, `!=`, булев литерал, литерал слева | ✅ | ⚠️ T12 **до фикса** валил `clippy::bool_comparison` |
| T14 | **Контрпример:** целое с числом | ✅ | сравнение осталось сравнением |
| T15, T16 | Голый `bit`; целое в позиции условия | ✅ | |
| T17–T20 | `if`/`else`, `while`, `loop`+`break`, `for`→`while` | ✅ | |
| T21 | `wrapping_*` без свёртки в `+=` | ✅ | подтверждает решение фичи 0127 |
| T22 | Умножение `q`: расширение и сдвиг | ✅ | |
| T23, T24 | Запись элемента массива; приведение `as` | ✅ | |
| T25 | Порты через HAL | ✅ | коэрция литерала при записи — образец для фикса 0148-01 |
| T26 | **Граница:** поле структуры | ✅ | `RS-011` |
| T27 | **Граница:** встроенная функция в условии | ✅ | `RS-020` — ⚠️ комментарий в коде обещает `RS-011` |
| T28 | **Мутация:** ветвь `bit` отключена | ✅ | пали ровно T9–T13, остальные 22 зелены |
| T29 | Корпус не изменился | ✅ | `git diff examples/generated/rust` пуст |
| T30 | Корпус зелёный | ✅ | `./scripts/precheck.sh` — код 0 |

## Замер покрытия (до → после)

| Модуль | Было | Стало | Δ |
|---|---|---|---|
| `rust_cond.rs` | 23.0 % | **64.6 %** | +41.6 |
| `rust_decl.rs` | 47.1 % | 68.6 % | +21.4 |
| `rust_stmt.rs` | 37.2 % | 57.7 % | +20.4 |
| `rust_expr.rs` | 42.8 % | 58.9 % | +16.1 |
| `rust_fixed.rs` | 51.7 % | 57.8 % | +6.1 |
| `rust_shared.rs` | 89.0 % | 94.8 % | +5.8 |
| `rust_live.rs` | 39.8 % | 39.8 % | **0.0** |

⚠️ Процент — следствие, а не цель (ADR 0138). Значима именно **нулевая** дельта
`rust_live.rs`: набор до этого слоя не доходит.

## Примеры и контрпримеры (правило 16)

Язык не менялся, языковых примеров в `book/` не заводилось.

- **примеры** — законные конструкции и их перевод (T1–T13, T15–T25);
- **контрпример** T14 — целое с числом: без него «починка», сводящая к операнду
  всё подряд, прошла бы T9–T13 и сломала обычные сравнения. Мутация T28 это
  подтвердила;
- **контрпримеры-границы** T26, T27 — непереводимое даёт **код диагностики**, а
  не тихо пропадает из вывода.

## Результаты по функциональности

| Функциональность | T1 | T9 | T21 | T26 |
|---|---|---|---|---|
| Цель `rust` | ✅ | ✅ | ✅ | ✅ |
| Цели `c`/`st`/`sv` | — | — | — | — |
| Симулятор | — | — | — | — |
| Язык (синтаксис/семантика) | — | — | — | — |

## Выводы и дальнейшие шаги

1. **Слепота стоила двух некомпилируемых классов входа.** Оба — простейшие
   записи: «проверить бит» (`btn = 1`) и «провод вход-выход» (`led := btn`).
   Гейт цели `rust` их не встречал, потому что гоняет **только корпус**, а
   корпус мал — ровно тот диагноз, ради которого фича заведена.
2. **Фикс 0148-01 исправлен здесь**: сравнение сведено к операнду тем же
   механизмом коэрции, которым уже пользуется присваивание (`o := 1` →
   `write_bit(..., true)`). Два пути одной идеи были разошедшимися.
3. **Фикс [0148-02](../fixes/0148-02-rust-port-to-port-borrow.md) заведён, не
   исправлен**: `hal.write_bit(P, hal.read_bit(Q))` даёт `E0499`. Лечение вводит
   **новый** механизм (временные переменные) и требует решений о именовании,
   порядке вычисления и способе распознавания — это ADR, а не правка на месте.
   ⚠️ Пока фикс открыт, простейшая проводка «вход на выход» целью `rust` не
   транслируется.
4. **Остаточный пробел:** `rust_live.rs` (39.8 %) не сдвинулся — записан
   находкой в `FEATURES.md`.
5. **Замечание к коду:** комментарий у ветви `FunctionDefinitionNode::Builtin` в
   `rust_cond.rs` обещает `RS-011`, а фактически выдаётся `RS-020` — условие до
   печатника не доходит. Тест пришпилил факт; расхождение отмечено у места.

Исправлений по самой 0148 не требуется.
