# Отчёт о тестировании фичи 0138: измерение покрытия тестами

> Фича: [../features/0138-coverage-measurement.md](../features/0138-coverage-measurement.md) · тест-план: [../tests/0138-coverage-measurement.md](../tests/0138-coverage-measurement.md) · анализ: [../analyze/0138-coverage-measurement.md](../analyze/0138-coverage-measurement.md)

## Резюме

**✅ ПРОЙДЕН.** Все 10 проверок тест-плана выполнены. Фича готова к закрытию.

| Команда | Результат |
|---|---|
| `ls codecov.yml` | отсутствует |
| `scripts/coverage.sh` | сводка печатается |
| `PATH` без инструмента | подсказка, код возврата 0 |
| `grep coverage scripts/precheck.sh` | пусто — не гейт |
| `./scripts/precheck.sh` | **`EXIT=0`**, 1 мин 47 с (прежнее время) |

## Результат замера

`cargo llvm-cov --workspace --features lsp` (`cargo-llvm-cov 0.8.7`, толчейн
`1.97.1`, однопоточно):

| Крейт | Покрытие строк |
|---|---|
| `takt-lang` | **79.22 %** (26 958 из 34 030) |
| `takt-sim` | **77.93 %** (4 674 из 5 998) |
| Всего | 79.02 % строк · 86.62 % функций · 76.66 % регионов |

## Главная находка

**`takt-lang/src/lsp/symbols.rs` — 0 % при 183 строках.** Возможность
`textDocument/documentSymbol` **объявлена** клиенту в `ServerCapabilities`, то
есть редактор её вызывает, — но `document_symbols` не упоминается **ни в одном**
тесте, и юнит-тестов в файле нет.

⚠️ Ноль процентов мог бы объясняться артефактом учёта, поэтому проверено
отдельно грепом: тестов действительно нет. Возможность работает вслепую.

## Прочие слепые зоны

| Файл | Покрытие | Чем объясняется |
|---|---|---|
| `bin/takt_lsp.rs` | 0 % (272 стр) | бинарник сервера тестами не покрывается; именно поэтому 0131 вынесла список возможностей в библиотеку — замер это подтверждает |
| `generator/rust/rust_cond.rs` | 25 % | у цели `rust` **один** тестовый файл, зовущий `compile_to_rust`; печатники проверяются гейтом сборки корпуса, а корпус мал |
| `generator/st/st_fixed.rs` | 31 % | Q-арифметика `st` |
| `generator/rust/rust_stmt.rs` | 41 % | печатник операторов `rust` |
| `semantic/lower_float.rs` | 42 % | трансформация `float → q` |
| `generator/rust/rust_live.rs` | 43 % | анализ мёртвых инициализаторов |
| `lsp/completion.rs` | 48 % | автодополнение: два теста на ключевые слова |
| `generator/c/c_expr/fixed.rs` | 49 % | Q-арифметика `c` |
| `address_map/eval.rs` | 55 % | вычислитель адресных выражений (0042) |
| `takt-sim/src/gif.rs` | 40 % | графика: визуальный вывод трудно проверять |

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Чужой конфиг удалён | ✅ | `codecov.yml` описывал пути `pest`, которых в репозитории нет |
| T2 | Ссылок нет | ✅ | грепом по README и `.github/` — пусто |
| T3 | Замер работает | ✅ | сводка по крейтам |
| T4 | Мягкий пропуск | ✅ | подсказка по установке, код 0 |
| T5 | Не гейт | ✅ | `precheck.sh` скрипт не вызывает |
| T6 | Фича `lsp` учтена | ✅ | без неё модули сервера выпали бы из измерения целиком |
| T7 | Слепые зоны названы | ✅ | таблица выше |
| T8 | Находка проверена отдельно | ✅ | греп подтвердил отсутствие тестов `document_symbols` |
| T9 | Кандидаты заведены | ✅ | две записи в блоке 2 |
| T10 | Предкоммит | ✅ | `EXIT=0`, время прежнее (1 мин 47 с) |

## Выводы и дальнейшие шаги

- Видимость настроенного покрытия убрана; вместо неё — воспроизводимый замер по
  требованию и **список слепых зон**.
- Главный результат — не 79 %, а обнаруженная возможность LSP без единого теста.
- Исправлений (`docs/fixes/0138-YY-*`) не потребовалось.
- Кандидаты, вынесенные из фичи: тесты для `documentSymbol` и покрытие цели
  `rust` (её печатники — самые слепые модули `takt-lang`).
- ⚠️ Непрерывного отслеживания нет: снижение покрытия никто не заметит. Это цена
  отказа от порога, принятая осознанно; цифры отчёта устареют, поэтому ценность
  — в списке зон, а не в числе.
