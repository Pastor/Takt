# Отчёт о тестировании фичи 0209: Внешний интерфейс модели

> Фича: [../features/0209-model-external-interface.md](../features/0209-model-external-interface.md) · тест-план: [../tests/0209-model-external-interface.md](../tests/0209-model-external-interface.md) · анализ: [../analyze/0209-model-external-interface.md](../analyze/0209-model-external-interface.md)

## Резюме

**Пройдено. Фича готова к закрытию (`ГОТОВО`).** Двенадцать проверок зелёные,
критерии A1–A10 выполнены.

Окружение: macOS 25.5.0, толчейн `1.97.1`, `./scripts/precheck.sh` — код 0.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Таблица по аргументу | ✅ | `v=9`, затем `v=8` — читается по адресу |
| T2 | Одно значение на такт | ✅ | `v=42` |
| T3 | Контр-пример: без стенда | ✅ | `SIM-019`, прогон остановлен |
| T4 | Контр-пример: стенд — свойство шага | ✅ | первый такт прошёл, второй отказал |
| T5 | Ошибка сценария названа | ✅ | имя функции в тексте |
| T6 | Агрегат принят семантикой | ✅ | было `SE-083` |
| T7 | Значения у эталона | ✅ | `prog=[9,8,7,6]`, `out_v=9` |
| T8 | Поэлементная печать в C | ✅ | `prog[0..3] = …`; формы `prog = {` нет |
| T9 | Контр-пример: неконстантный элемент | ✅ | `SE-083` |
| T10 | Границы целей | ✅ | `ST-017`, `SV-002` |
| T11 | Мутация (часть 2) | ✅ | краснеют 4 из 5 |
| T12 | Корпус и предкоммит | ✅ | без изменений, код 0 |

### Две находки, которых не было в карточке

1. **Снятие отказа семантики обнажило неготовность цели `c`.** Пока `SE-083`
   отвергал агрегат раньше, печатник до него не доходил; в тот же час, когда
   форма стала законной, цель начала печатать `p.prog = {9, 8, 7, 6};` — не
   выражение, а инициализатор объявления, который `cc` отвергает. Починено в той
   же задаче: печать поэлементная.
   ⚠️ Урок общий: снимая отказ, проверяй **всех** потребителей формы — отказ мог
   быть единственным, что их прикрывало.
2. **Стенд не доезжал до вложенных областей видимости.** При первой сборке
   подмена не срабатывала: тело блока и тело функции исполняются в своих
   контекстах, а такт идёт с `Unit` в роли контекста — умолчание трейта (`None`)
   в любом из трёх мест означает «стенда нет». Реализовано во всех трёх; тест
   гоняет вызов из тела `always`, то есть путь через все контексты.

## Результаты по функциональности

| Функциональность | Статус | Чем проверено |
|---|---|---|
| Симулятор (эталон) | ✅ | T1–T5, T7 |
| Цель `c` | ✅ | T8 + гейты предкоммита |
| Цель `rust` | ✅ | измерено: форму печатала верно и до правки |
| Цели `st`, `sv` | ⬜ | отказы пришпилены T10 |
| Формат сценария | ✅ | аддитивен: поле необязательно |
| Документ `book/` | ✅ | подраздел «Внешние функции в прогоне»; гейты 0133/0146 зелёные |

## Выводы и дальнейшие шаги

1. **Ограничение, записанное в живом контексте, снято наполовину.** Прежде
   примеры документа обязаны были брать значения датчиков входным портом, потому
   что `extern fn` со значением останавливал прогон. Теперь пример вправе читать
   внешнюю функцию — если сценарий задаёт значения.
2. **Таблица ищет по первому аргументу** — для функций с несколькими значащими
   аргументами этого мало. Названо границей в ADR; расширение — отдельная фича,
   если понадобится.
3. **Цели `st` и `sv` агрегатный аргумент не печатают.** Отказы пришпилены
   тестом: научатся — тест покраснеет и потребует снять запись.
4. Фиксов не потребовалось.
