# Отчёт о тестировании фичи 0055: Многофайловость LSP

> Фича: [../features/0055-lsp-multifile.md](../features/0055-lsp-multifile.md) · тест-план: [../tests/0055-lsp-multifile.md](../tests/0055-lsp-multifile.md) · ADR: [../adr/0055-lsp-multifile.md](../adr/0055-lsp-multifile.md) · анализ: [../analyze/0055-lsp-multifile.md](../analyze/0055-lsp-multifile.md)

## Резюме

**Пройдено. Фича готова к закрытию (`ГОТОВО`).**

`import` работает в редакторе и в `lamc` без настройки; ошибка из импортированного
файла показывается на строке `import` и называет настоящее место. Все 10 проверок
тест-плана пройдены; `precheck.sh` зелёный.

Версия языка не менялась (0.3.0), вывод генераторов неизменен.

**Зонд вскрыл, что кандидат винил не тот слой.** Кандидат назывался «LSP
однофайлов», и напрашивалось лечение внутри `lsp.rs`. Проба показала: **`lamc`
тоже** не ищет импорт рядом с импортирующим файлом — только по явному `-I`, даже
при запуске из его каталога:

```text
$ lamc compile -t c main53.lam -o o.c        # badsem.lam лежит рядом
main53.lam:1:1: [SE-013] Файл импорта не найден: «badsem.lam».
Пути поиска: (пути не заданы)
```

То есть редактор не был сломан — он **наследовал** поведение ядра. Почини мы
только LSP, редактор стал бы зелёным там, где сборка красная: тот самый класс
дефекта, который проект уже лечил (0028-01 — расхождение веток печати; 0054 —
расхождение форматов). Решением заказчика неявный путь заведён **в ядре**, и
редактор с компилятором согласованы по построению.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Импорт разрешается в редакторе | ✅ | прежде `import` **всегда** был ошибкой |
| T2 | **Пара к T1:** сборка с `-I` не изменилась | ✅ | снапшоты + гейт детерминизма 0048 зелены |
| T3 | `lamc` без `-I` находит импорт рядом | ✅ | проба: ошибка стала семантической, а не `SE-013` |
| T4 | Вложенный импорт без `-I` | ✅ | `top → mid → deep` → назван `deep.lam` |
| T5 | Чужая ошибка привязана к строке `import` | ✅ | `range.start.line == 0` |
| T6 | Текст называет настоящее место | ✅ | `в файле …lib_bad.lam:2:18: …` |
| T7 | Своя ошибка — на своём месте | ✅ | сужение не задело обычный путь |
| T8 | `lamc` печатает цепочку импорта | ✅ | две заметки на `top → mid → deep` |
| T9 | **Мутационная:** `file_no` снова отбрасывается | ✅ | краснеют T5 и T6 |
| T10 | Регрессий нет | ✅ | `precheck.sh` зелёный |

## Что изменилось для пользователя

```text
# в редакторе: было — на каждом import
[SE-013] Файл импорта не найден: «lib.lam». Пути поиска: (пути не заданы)
# стало — импорт разрешается; ошибка библиотеки видна на строке import:
в файле tests/data/lsp55/lib_bad.lam:2:18: Ссылка 'Nowhere' не найдена

# в lamc: цепочка импорта
deep.lam:2:18: Ошибка компиляции [SE-002]: Ссылка 'Missing' не найдена
  примечание: 'deep.lam' импортирован в 'mid.lam'
  примечание: 'mid.lam' импортирован в 'top.lam'
```

## Решения заказчика (2026-07-16)

Проработка вскрыла две развилки, которые аналитик решать не вправе:

1. **Откуда LSP берёт пути поиска** → «каталог файла — неявный путь **в ядре**».
   Вариант «только в LSP» отвергнут: он создал бы расхождение редактора и сборки.
2. **Как показывать чужую диагностику** → «на строке `import` + текст
   `в файле X:стр:кол`». Вариант «публиковать в URI чужого файла» отвергнут:
   требует снятия устаревших диагностик чужих URI — заметно больше состояния.

## Обратная совместимость (правило 11)

Неявный путь добавляется **в конец** списка, поэтому правка **строго аддитивна**:
где импорт разрешался раньше, найдётся тот же файл; меняется только исход,
который прежде был ошибкой. Цена — файл рядом с моделью шадоуится одноимённым из
`-I` (ADR 0055, A1). Обратный порядок («сначала моё») выглядит интуитивнее, но
молча сменил бы смысл существующих сборок, а выгода стилистическая.

## Ловушки, найденные на стадии

1. **Заметка была бесполезна в `lamc`.** Первая редакция писала «импортировано
   здесь: deep.lam», рассчитывая на позицию заметки. Но `lamc` печатает **только
   текст** заметки — пользователь видел два примечания без координат. Проба это
   показала сразу; сообщение стало самодостаточным («`X` импортирован в `Y`»).
   Тесты бы не поймали: они смотрят на LSP, где используется позиция, а не текст.
2. **Тесты LSP под `#[cfg(feature = "lsp")]` обычная сборка не видит.** Первая
   редакция добавила их без гейта: `cargo build` проходил, `precheck.sh`
   (`--all-features --all-targets`) падал. Та же ловушка, что в 0053 с литералами
   `Diagnostic` под `lsp` — второй раз подряд, и оба раза поймал предкоммит.

## Результаты по функциональности

| Функциональность | Затронута чем | Проверки | Статус |
|---|---|---|---|
| Разрешение импорта (проход 0) | неявный путь поиска | T1–T4, T10 + `semantic_tests.rs` | ✅ |
| Диагностики | заметки цепочки импорта | T8, `diagnostics_file_tests.rs` | ✅ |
| LSP | `collect_diagnostics_at`, `diagnostic_to_lsp` | T1, T5–T7, T9 | ✅ |
| CLI `lamc` | цепочка импорта в выводе | T3, T4, T8 | ✅ |
| Симулятор | наследует ядро | `cargo test -p simulation` | ✅ |
| Генерация (5 целей) | не затронута | снапшоты, гейт 0048 | ✅ |

## Окружение

- `rustc 1.99.0-nightly (daf2e5e18 2026-07-13)`, macOS (Darwin 25.5.0), ветка `v2`.
- `./scripts/precheck.sh` — зелёный целиком.

## Выводы и дальнейшие шаги

**Вердикт: готово.** Фича закрывается (`ГОТОВО`). Дефектов, требующих
исправления, стадия не оставила.

Урок: **«сломан инструмент X» стоит проверять на слое ниже.** Кандидат честно
описывал симптом (в редакторе импорт не работает), но лечение внутри `lsp.rs`
починило бы редактор и **создало** расхождение со сборкой. Это третий подряд
кандидат (0052, 0053, 0055), чья формулировка не пережила зонда.

Заведены **кандидаты в фичи** (правило 7), вынесенные за объём:

- **`goto_declaration` угадывает файл** по имени модели (`to_snake_case` +
  перебор путей), хотя реестр 0053 даёт точный путь (ADR 0055, A2).
- **LSP не читает `initializationOptions`** — аналог `-I` в редакторе задать
  нельзя, работает только каталог документа (A3).
