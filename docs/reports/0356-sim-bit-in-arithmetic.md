# Отчёт о тестировании 0356: Разряд в арифметике

- **Фича:** [../features/0356-sim-bit-in-arithmetic.md](../features/0356-sim-bit-in-arithmetic.md)
- **Тест-план:** [../tests/0356-sim-bit-in-arithmetic.md](../tests/0356-sim-bit-in-arithmetic.md)
- **Дата:** 2026-08-21

## Окружение

toolchain `1.97.1`, `cc` в PATH. Прогоны: `cargo test` (13 наборов зелёные),
`cargo test --test conformance conformance_bit_arith`, полный
`./scripts/precheck.sh`.

## Сверка с тест-планом

| № | Условие | Результат |
|---|---|---|
| П1, П2 | разряд читается числом | ✅ |
| П3 | `src.7 + 1` = 2 у эталона и цели `c` | ✅ |
| П4 | `ref Done: src.7 = 1;` срабатывает | ✅ |
| К1 | `Boolean + Number` — `TypeMismatch` | ✅ правило S8 в силе |
| К2 | `if src.7` работает | ✅ (`to_bool` принимает число) |
| С1 | трасса эталона = трасса цели `c` | ✅ `[2, 4, 6, 8, 8]` |

## Найденные дефекты

Фиксов не потребовалось. Одна поправка **в ходе** работы, важная по существу:

**Первая редакция правила меняла `as_num`** — то есть разрешала логическому
быть числом в арифметике **вообще**, отменяя S8. Упал юнит-тест правила
(`s8_bool_plus_int_is_error_not_silent_coercion`), и это верное поведение
сторожа: он охраняет решение 0025, а не строку кода. Правка перенесена к
**источнику** значения (`read_bit`), после чего S8 остался в силе и получил
контрпример рядом с изменённым тестом.

**Вторая поправка — коллизия имён фикстур.** Новая фикстура была названа
`conformance_bit_value.takt`, а так уже звалась фикстура 0335 (разряд в позиции
числового значения): запись перезаписала её, и упала **чужая** сверка ST
(`conformance_st_tests::bit_value`, «переменная 'high': неожиданное значение
None»). Поймал предкоммит. Своя переименована в `conformance_bit_arith.takt`,
чужая восстановлена из истории.

## Мутационная проверка

| Мутация | Кто упал | Что доказано |
|---|---|---|
| читать соседний разряд (`bit + 1`) | сверка С1, на предусловии | номер разряда сторожится значением, а не формой |

## Редакторский слой (правило 29)

**Правок не потребовалось, ответ зафиксирован:** лексика, синтаксис, семантика
и набор диагностик не затронуты.

## README и документ (правила 8, 24)

**Правок не потребовалось:** правило `bit` (значения 0/1) документом уже
описано, а внутреннее представление у эталона документ не описывает.
Наблюдаемое отличие одно — трассы `takt-sim` печатают разряд числом; примеры
документа этого не показывают.

## Вердикт

**ГОТОВО.** Условия тест-плана выполнены, сторож проверен мутацией, предкоммит
зелёный.
