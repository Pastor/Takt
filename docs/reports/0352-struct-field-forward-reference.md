# Отчёт о тестировании 0352: Видимость имени типа и цикл структур

- **Фича:** [../features/0352-struct-field-forward-reference.md](../features/0352-struct-field-forward-reference.md)
- **Тест-план:** [../tests/0352-struct-field-forward-reference.md](../tests/0352-struct-field-forward-reference.md)
- **Дата:** 2026-08-21

## Окружение

toolchain `1.97.1`; `cc`, `rustc`, `clippy-driver`, `iec2c`, `verilator`,
`yosys`, `typst` в PATH. Прогоны: `cargo test --test semantic
type_forward_reference_tests::`, `cargo test --test conformance
conformance_forward_type`, полный `./scripts/precheck.sh`.

## Сверка с тест-планом

| № | Условие | Результат |
|---|---|---|
| П1 | поле-структура ссылается вперёд | ✅ принято |
| П2 | поле-перечисление ссылается вперёд | ✅ принято |
| П3 | элемент массива ссылается вперёд | ✅ принято |
| П4 | псевдоним → структура ниже | ✅ принято |
| П5 | псевдоним → псевдоним ниже | ✅ принято |
| П6 | взаимный цикл структур | ✅ `SE-124` |
| П7 | самоссылка через массив | ✅ `SE-124` |
| П8 | цикл через псевдоним | ✅ `SE-124` |
| К1 | несуществующее имя | ✅ `SE-034` |
| К2 | дубль имени типа | ✅ `SE-108` |
| К3 | имя встроенного типа | ✅ `SE-107` |
| К4 | цепочка без цикла | ✅ принято |
| К5 | цикл псевдонимов | ✅ `SE-039` |
| С1 | трасса эталона = трасса цели `c` | ✅ `[3, 6, 9, 12, 12]` |

Итого: 14 тестов темы `semantic`, 1 сверка — зелёные; предкоммит зелёный.

## Найденные дефекты

Фиксов не потребовалось. Две находки **в ходе** разработки:

1. **Тест на пару псевдонимов не сторожил неподвижную точку.** Мутация «один
   виток вместо повторения» его **не роняла**: финальная попытка разрешить
   остаток служит вторым проходом. Добавлен тест на цепочку из **трёх**
   (`A → B → C`) — он мутацию ловит. Это ровно тот случай, когда сторож
   выглядит достаточным и не является им.
2. **Умолчание переменной у цели `c` — мусор стека.** Обнаружено при постройке
   сверки: харнесс читал 339 вместо 3. `Nodefault_init` ставит только `state`,
   переменные без инициализатора не обнуляются вовсе, тогда как эталон и семь
   остальных целей дают ноль. В объём фичи не входит (её предмет — видимость
   имени типа), вынесено кандидатом; в харнессе причина снята явным `= {0}` с
   комментарием — правило 30, «одна причина за раз».

## Мутационная проверка сторожей

| Мутация | Кто упал | Что доказано |
|---|---|---|
| обход `SE-124` не спускается в массив | `self_reference_through_array_is_rejected` | спуск в элемент не декоративен |
| один виток вместо неподвижной точки | `alias_chain_of_three_resolves` | повторение нужно; пара `A → B` его не сторожит |

## Редакторский слой (правило 29)

**Правок не потребовалось, ответ зафиксирован.** Новых токенов, форм объявления
и узлов АСД нет — `lsp/semantic_tokens.rs`, `lsp/keywords.rs::TAKT_KEYWORDS` и
`TaktSymbolScanner` плагинов не затронуты. **Новая диагностика `SE-124`
доезжает до редактора**: она рождается на стадии построения дерева
(`construct_model_stage0`), которую LSP зовёт через общий вход
`collect_compile_diagnostics`, а не печатается из библиотеки.

## Документ (правило 24) и версия языка (правило 22)

- Раздел `book/src/03-types/` дополнен абзацем о видимости имени типа с
  примером (структура ссылается на объявленную ниже, псевдоним — тоже) и
  примером `SE-124`; блок «Частые ошибки» получил запись о `SE-124`.
- Приложение «Ошибки и предупреждения» получило `SE-124` — гейт
  `check-book-diagnostics.py` зелёный (269 кодов сверены).
- Версия языка поднята `0.10.0` → `0.11.0` в трёх якорях (константа, README,
  живой контекст); гейт `check-language-version.sh` зелёный. Тег `v0.11.0`
  ставится на коммит с подъёмом (правило 22).

## Вердикт

**ГОТОВО.** Все условия тест-плана выполнены, сторожа проверены мутацией,
предкоммит зелёный.
