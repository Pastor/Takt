# Отчёт о тестировании фичи 0213: Цель c печатает лишний break после безусловного перехода

> Фича: [../features/0213-c-redundant-break.md](../features/0213-c-redundant-break.md) · тест-план: [../tests/0213-c-redundant-break.md](../tests/0213-c-redundant-break.md) · анализ: [../analyze/0213-c-redundant-break.md](../analyze/0213-c-redundant-break.md)

## Резюме

**Пройдено; фича готова к закрытию (`ГОТОВО`).** Безусловное ребро в цели `c`
завершает цепочку: собственного `break;` у него нет, рёбра за ним не печатаются.
Из корпуса ушли все **7** пар `break; break;`. Поведение тождественно —
подтверждено потактовой сверкой эталона с прошивкой и полным прогоном
`precheck.sh`. Оба сторожа проверены мутацией.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Корпус без двух `break;` подряд | ✅ | `corpus_has_no_two_breaks_in_a_row`: 11 примеров корпуса, ни одной пары (до правки — 7 в 6 файлах) |
| T2 | Ребро после безусловного не печатается | ✅ | `edge_after_unconditional_is_not_emitted`: в ветви `RUN` переход в `DONE`, `LATE` не упомянут, ровно один `break;` |
| T3 | Условное ребро сохраняет `break;` | ✅ | `conditional_edge_keeps_its_break`: два `break;` — внутри `if` и общий |
| T4 | Терминальное состояние уходит в `_END` | ✅ | `terminal_state_falls_to_end_with_single_break` |
| T5 | Поведение тождественно | ✅ | `unconditional_edge_trace_matches_generated_c`: `sim == C == [1, 0, 0, 0]` |
| T6 | Сторожа не декоративны | ✅ | см. таблицу мутаций ниже |
| T7 | Прочие потактовые сверки зелены | ✅ | полный `cargo test --all-features` в предкоммите |
| T8 | Вывод детерминирован | ✅ | гейт двух прогонов (`diff -r`) без отличий |
| T9 | Корпус и гейты целевых языков зелены | ✅ | `./scripts/precheck.sh` — код 0 |
| T10 | Документ показывает актуальный вывод | ✅ | `book/src/18-showcase/generated/c/lift.{c,h}` пересобран, цитата в `index.md` приведена в соответствие |

**Мутационная проверка (T6):**

| Мутация | Кто упал | Наблюдаемое |
|---|---|---|
| M1: полный откат (печать `break;` + продолжение обхода) | T1, T2 | список: `float_regulator.c:44`, `elevator_mini.c:232`, `pid_regulator.c:65`, `regulator.c:51`, `elevator.c:60`, `stacker.c:78,334` |
| M2: половинчатая правка (`break;` снят, обход **не** оборван) | T5 | `sim=[1, 0, 0, 0]` против `C=[1, 9, 9, 9]`; `cc -Wall -Werror` этот вывод принимает молча |

M1 не ловится сверкой трасс (поведение не меняется — код лишь недостижим), M2
не ловится грепом (пар `break;` не возникает). Сторожа дополняют друг друга, и
ни один из них не покрывает работу второго.

## Результаты по функциональности

| Функциональность | Статус | Чем подтверждено |
|---|---|---|
| Цель `c` — простые состояния | ✅ | T1–T5 |
| Цель `c` — композиции `+`/`\|` | ✅ | T1 и гейты корпуса: `batch_cycle` (`+`), `extend_complex`, `stacker` (`\|`); их `break;` печатается **внутри** `if`, ветвь не тронута |
| Цель `c-hal` | ✅ | гейт `cc -c` в предкоммите (то же ядро генерации) |
| Цели `st` / `rust` / `sv` / `sv-mmio` | ✅ | гейты `iec2c`, `rustc`/`clippy -D warnings` + прогон харнессов, verilator/yosys + тестбенчи |
| Симулятор (эталон) | ✅ | T5, T7 |
| Семантика, грамматика, форматтер | ✅ | T7 (правок не было) |
| Документ `book/` | ✅ | T10; гейты примеров, глифов и лексики документа зелены |

**Редакторский слой (правило 29): правки не потребовалось — обоснование.** Фича
не меняет лексику, синтаксис и семантику: новых токенов, узлов АСД, форм
объявления и диагностик нет. LSP (`takt-lang/src/lsp/`) работает с исходным
текстом и семантическим деревом, вывода цели `c` не касается; плагины
(`extensions/`) — тем более. Ответ зафиксирован также в анализе (стадия 3).

**Версия языка не поднимается** (правило 22): язык не изменился. Версия крейта
`takt-lang` — `0.32.0` → **`0.33.0`** (изменилось наблюдаемое поведение
библиотеки: форма вывода цели `c`).

## Выводы и дальнейшие шаги

1. **Корень был один, проявлений — два.** Кандидат называл только пару
   `break; break;`; замер стадии архитектуры вскрыл второе проявление — печать
   рёбер после безусловного — и главное: цель `c` отставала от трёх остальных,
   которые обход прекращают. Правка поэтому не «убрать лишнюю строку», а
   привести цель к общему контракту.
2. **Инструменты этот класс не видят.** `cc -Wall -Wextra -Wunreachable-code`
   молчит и о прежнем выводе, и о новом, и о выводе половинчатой правки —
   поэтому сторожа два и оба свои.
3. **Попутная находка (за объёмом фичи).** Снимок порождённого C в документе
   отставал от инструмента с фичи 0195 (имена портов без сегмента `PORT_`).
   Снимок цели `c` обновлён; то, что снимки `book/src/18-showcase/generated/`
   никем не сверяются (и прочие цели там не проверялись), вынесено **кандидатом**
   в `FEATURES.md`.
4. **Кандидат по языку.** Ребро после безусловного законно, но недостижимо
   всегда, и об этом молчат все потребители. Предложено предупреждение уровня
   семантики по образцу Ce14 — запись заведена в `FEATURES.md`.
5. **Исправлений (`docs/fixes/`) не потребовалось.**
