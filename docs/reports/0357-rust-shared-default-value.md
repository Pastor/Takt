# Отчёт о тестировании 0357: Умолчание общей переменной

- **Фича:** [../features/0357-rust-shared-default-value.md](../features/0357-rust-shared-default-value.md)
- **Тест-план:** [../tests/0357-rust-shared-default-value.md](../tests/0357-rust-shared-default-value.md)
- **Дата:** 2026-08-21

## Окружение

toolchain `1.97.1`. Прогоны: `cargo test` (13 наборов зелёные), прогон корпуса
целью `rust`, полный `./scripts/precheck.sh`.

## Сверка с тест-планом

| № | Условие | Результат |
|---|---|---|
| П1 | вывод не изменился | ✅ снапшоты корпуса не тронуты |
| П2 | ветвь недостижима (зонд) | ✅ ни одного срабатывания |
| П3 | вход через `from_calls` | ✅ значение пришло из `inits` |

## Найденные дефекты

Нет.

## Почему нового сторожа не заведено

Ветвь защитная: достижимого входа у неё нет, и тест на неё проверял бы не то,
что называет. Вместо теста в код записан **замер с датой** — чтобы следующая
фича не приняла ветвь за живую и не «дочинила» её обратно в строковую
константу.

⚠️ Это осознанное исключение из привычки «фича заводит сторожа»: сторож без
достижимого входа — декорация (тот же довод, что у ветви `DomainNotEnumerable`
в 0258).

## Редакторский слой (правило 29)

**Правок не потребовалось, ответ зафиксирован.**

## README и документ (правила 8, 24)

**Правок не потребовалось:** язык не менялся, наблюдаемый вывод — тоже.

## Вердикт

**ГОТОВО.** Знание об умолчании у цели `rust` живёт в одном месте; вывод
неизменен, предкоммит зелёный.
