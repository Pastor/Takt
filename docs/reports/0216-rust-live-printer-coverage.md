# Отчёт о тестировании фичи 0216: Печатник живости цели `rust`

> Фича: [../features/0216-rust-live-printer-coverage.md](../features/0216-rust-live-printer-coverage.md) · тест-план: [../tests/0216-rust-live-printer-coverage.md](../tests/0216-rust-live-printer-coverage.md) · ADR: [../adr/0216-rust-live-printer-coverage.md](../adr/0216-rust-live-printer-coverage.md)

## Окружение

- macOS (darwin 25.5.0), Rust 1.97.1 (пин `rust-toolchain.toml`),
  `clippy-driver` 1.97, `cargo-llvm-cov` 0.8.7.
- Дата прогона: 2026-08-20.

## Сводка прогонов

| Прогон | Результат |
|---|---|
| `cargo test --test targets rust_live_tests::` | 11 passed |
| `cargo test --all-features` | 14 наборов, провалов нет |
| `clippy -D warnings` на выводе фикстуры | код 0 |
| `./scripts/precheck.sh` | код 0 |

## Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П6 | Формы свёртки и отложенного объявления | ✅ |
| П7…П10 | Контрпримеры: значение остаётся живым | ✅ |
| П11 | Вывод принимается линтом | ✅ |
| П12 | Регрессия | ✅ |
| П13 | Корпус и гейт цели | ✅ |

## Что нашёл прогон

**Два дефекта цели `rust`**, оба — законный вход, невалидный по политике
линтов вывод, нулевой код возврата `taktc`:

1. **`match` с ветвью `_`** печатал мёртвое начальное значение
   (`unused_assignments`). Причина: анализ живости для `match` не давал
   вердикта никогда, а печатник разворачивает его в цепочку `if/else` — анализ
   и печать разошлись.
2. **`for` с присваивающим `init`** — то же самое на счётчике цикла.

Оба исправлены (задача 0216-02). Класс ровно тот, что дал фиксы 0148-01 и
0148-02, и карточка предупреждала о нём прямо.

**Контроль «замена одного отказа другим».** Признать перезапись было мало:
отложенная форма `let t: u8;` перед `if/else` — это
`clippy::needless_late_init`. Проверено пробой на чистом Rust **до** правки;
поэтому `match` сворачивается в выражение, а `for` — нет (там та же проба
показала, что отложенная форма принимается).

**Мутационные проверки** (все три прогнаны, каждая обязана валить сторожа):

| # | Мутация | Итог |
|---|---|---|
| М1 | из разбора `match` убрано требование ветви `_` | ПРОВАЛ: `open_match_keeps_initializer` и линт ✅ |
| М2 | `for` с `init` снова даёт `Unknown` | ПРОВАЛ: `for_init_kills_initializer` и линт ✅ |
| М3 | свёртка `match` отключена (отложенная форма) | ПРОВАЛ: линт — «unneeded late initialization» ✅ |

**Замер покрытия** (справочно, не цель — ADR 0138):

| Метрика | До | После |
|---|---|---|
| регионы | 39.84 % | **77.23 %** |
| строки | 42.62 % | **77.49 %** |
| функции | 17 из 33 | **35 из 37** |

Рост — следствие проверок по формам языка, а не самоцель: тесты писались от
поведения печатника, а не от непокрытых строк.

## Дефекты

Найдены и исправлены в рамках фичи (см. выше). Отдельных фиксов
(`docs/fixes/`) не заводилось: дефекты найдены и закрыты одной работой.

## Редакторский слой (правило 29)

**Не требуется** — подтверждено: правка внутри печатника цели, язык не
затронут.

## Вердикт

**Готово.** Поведение печатника закреплено одиннадцатью проверками (текст плюс
настоящий линт), два дефекта, ронявших сборку у пользователя, устранены.
