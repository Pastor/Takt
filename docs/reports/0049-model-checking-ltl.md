# Отчёт о тестировании фичи 0049: Верификация модели (Model Checking) на основе LTL

> Фича: [../features/0049-model-checking-ltl.md](../features/0049-model-checking-ltl.md) · тест-план: [../tests/0049-model-checking-ltl.md](../tests/0049-model-checking-ltl.md) · ADR: [../adr/0049-model-checking-ltl.md](../adr/0049-model-checking-ltl.md) · анализ: [../analyze/0049-model-checking-ltl.md](../analyze/0049-model-checking-ltl.md)

## Резюме

**Пройдено. Фича готова к закрытию (`ГОТОВО`).**

`lamc verify` даёт вердикт по LTL-свойствам управления: держится (доказательство
на всех прогонах абстракции) / нарушено (контрпример-лассо) / не проверено
(честный отказ на атоме-не-состоянии). Все 47 проверок тест-плана (T1–T47)
пройдены; `./scripts/precheck.sh` зелёный целиком; `cargo test --
--test-threads=1` — **1618 тестов** в 19 наборах, из них 53 по этой фиче.

Версия языка **не менялась** (0.3.0): фича не трогает грамматику — `lamc verify`
читает те же `: [LTL] φ;`, что 0035 уже разбирал. Вывод генераторов
`c`/`c-hal`/`st`/`st-at`/`plantuml`/`rust`/`sv` не изменился.

**Главный результат стадии — не «зелёные тесты», а два вскрытых дефекта**, оба в
том, ради чего фича существует (в вердикте), и оба найдены **не** прогоном
тестов:

1. **Дефект затравки 0010** ([фикс 0010-01](../fixes/0010-01-buchi-acceptance.md)):
   `build_buchi(F p)` принимал слово, где `p` не наступает. Прожил в зелёном CI с
   фичи 0010, потому что тесты `buchi.rs` проверяли **структуру** автомата, а не
   принимаемый **язык**, и потребителя у затравки не было. Без фикса первый же
   вердикт `lamc verify` был бы ложным на всём классе свойств живости.
2. **Недо-аппроксимация Крипке** (ревью стадии разработки): самопетля ставилась
   только состоянию без рёбер, поэтому прогон «ни один guard не сработал, стоим
   вечно» в модели отсутствовал → `F Done` на `start A { ref Done: x = 1; }`
   объявлялось **доказанным**. Ломалась ровно главная гарантия фичи —
   надёжность `Holds`.

Оба подтверждают принцип плана: **тесты на вердикт, а не на форму**. Тест-план
это закрепил (T12/T13 — известные теоремы, T40 — независимый оракул, T41/T47 —
мутационные пробы, доказывающие, что тесты **умеют краснеть**).

## Фактические результаты по проверкам

Прогон: `cargo test -p grammar -- --test-threads=1` + `./scripts/precheck.sh` +
пробы CLI. Все — на машине окружения (см. ниже).

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Вершины Крипке = состояния, рёбра = `ref` | ✅ | `kripke.rs::tests` |
| T2 | Guard ребра абстрагирован | ✅ | ребро есть, условие не читается |
| T3 | Тупик получает самопетлю | ✅ | частный случай `may_stutter` |
| T4 | Старт — состояние `start`, не первое по алфавиту | ✅ | `initial = Z` |
| T5 | Модель без `start` → `NoStartState` | ✅ | не молчаливое «держится» |
| T6 | Литералы извлекаются, темпоральные формулы — нет | ✅ | `product.rs::tests` |
| T7 | Произведение хранит согласованные достижимые пары | ✅ | пар над `B` нет |
| T8 | Нет принимающих — язык пуст | ✅ | `check.rs::tests` |
| T9 | Достижимый принимающий цикл найден | ✅ | лассо указывает на цикл |
| T10 | Недостижимый принимающий цикл — не контрпример | ✅ | |
| T11 | Принимающий тупик — не контрпример | ✅ | цикла нет |
| T12 | **Теорема**: `F p` не принимает слово без `p` | ✅ | **ловит 0010-01**; до фикса — краснел |
| T13 | **Теорема**: `G q`, `F q` принимают `q^ω` | ✅ | |
| T14 | `F Done` держится, если Done неизбежно | ✅ | `reachable.lam` |
| T15 | `F Done` нарушено, если Done недостижимо | ✅ | `unreachable.lam`, лассо обходит Done |
| T16 | `F Done` нарушено, если Done обходим | ✅ | цикл `A → B → A` |
| T17 | `G (Fault -> F Idle)` держится | ✅ | `holds.lam` |
| T18 | `G (Fault -> F Idle)` нарушено при залипании | ✅ | `fails.lam`, `Idle -> [ Fault ]*` |
| T19 | Атом-переменная → честный отказ | ✅ | `Unsupported(["temp"])`, не `Violated` |
| T20 | Неизвестный атом (опечатка) → отказ | ✅ | `Unsupported(["Dane"])` |
| T21 | Смешанная формула: только неподдержанные атомы | ✅ | известный атом не спасает |
| T22 | Формула без атомов проверяется | ✅ | `G true` → `Holds` |
| T23 | Проверка завершается на корпусе `examples/` | ✅ | зависаний нет; |K| конечна |
| T24 | Вердикт детерминирован | ✅ | 10 прогонов → один ответ |
| T25 | Крипке и произведение детерминированы | ✅ | `BTreeMap` (гейт 0048) |
| T26 | Свойство из строки разбирается грамматикой языка | ✅ | атомы длиннее 1 символа |
| T27 | Негодное свойство — ошибка, не паника | ✅ | `parse_ltl` бы паниковал |
| T28 | Список формул в `--property` отвергается | ✅ | молча взять первую = соврать |
| T29 | Формула вложенной модели — по её графу | ✅ | `model = "Engine"` |
| T30 | Модель без формул — пустой результат | ✅ | не «держится» |
| T31 | CLI: аргументы `verify` | ✅ | `-p`/`--property=`/`--trace`/`-I` |
| T32 | CLI: контрпримеры аргументов | ✅ | отказ, не молчаливый прогон |
| T33 | CLI: коды возврата | ✅ | проба: `holds.lam` → `0`, `fails.lam` → `1` |
| T34 | `--trace` печатает все три звена | ✅ | Крипке + автомат + произведение |
| T35 | Трасса: цикл сжат до периода, слово не изменено | ✅ | `[F,F]` → `[ F ]*`; непериодичный не тронут |
| T36 | README-паритет: `G (Open -> X Closing)` | ✅ | `Holds` / `Violated` |
| T37 | README-паритет: `G F Idle` | ✅ | `Holds` / `Violated` |
| T38 | Версия языка не изменена, вывод генераторов неизменен | ✅ | `grammar.lalrpop` не тронут; снапшоты зелены |
| T39 | Регрессий нет | ✅ | `precheck.sh` зелёный |
| T40 | **Независимый оракул**: `F X`/`G X` = прямой перебор | ✅ | 9 топологий, 44 вердикта, совпадение 44/44 |
| T41 | **Мутационная проба**: тесты ловят возврат 0010-01 | ✅ | `verify_tests.rs` краснеет (4); `buchi.rs::tests` 10/10 зелёные — **не ловят** |
| T42 | Формула состояния держится при гарантированном возврате | ✅ | `state_scope_holds.lam` (0049-06) |
| T43 | Формула состояния нарушена при залипании (**сторож области**) | ✅ | `state_scope_fails.lam`; прежняя семантика дала бы `Holds` |
| T44 | Печатается **проверенная** формула, а не авторская | ✅ | `G (Fault -> F Idle)`, не `F Idle` |
| T45 | Формула уровня модели областью не связана | ✅ | `fails.lam` неизменна |
| T46 | Формула в блоке состояния связана тем же состоянием | ✅ | `enter { : [LTL] F Idle; }` |
| T47 | **Мутационная проба области**: десахаризация отключена | ✅ | краснеют 3 из 5 (см. ниже) |

### Что показали мутационные пробы (T41, T47)

Проба — единственный способ отличить «тест зелёный, потому что кода нет дефекта»
от «тест зелёный всегда». Обе пробы временные, откачены после замера.

- **T41.** Возврат дефекта 0010-01 (`old.insert(η)` убран из ветки `Until`):
  `verify_tests.rs` краснеет — 4 теста, включая оракул T40. Тесты `buchi.rs`
  (10 шт., на структуру) остаются **зелёными**. Это и есть измеренная причина,
  по которой дефект прожил с фичи 0010: набор, проверяющий форму, дефект
  принятия не видит в принципе.
- **T47.** Отключение десахаризации области (`scoped_formula` → `match
  None::<String>`): краснеют **3 теста из 5**. Не краснеет `state_scope_holds` —
  и это правильный результат, а не пробел: на модели, где свойство держится, обе
  семантики дают `Holds`, отличить их может только **контрпример**. Отсюда
  вывод, зафиксированный в 0049-06: сторожем работает **парная** фикстура
  (пример + контрпример, правило 16), одиночный пример сторожем быть не может.

## Результаты по функциональности

| Функциональность | Затронута чем | Проверки | Статус |
|---|---|---|---|
| Автомат Бюхи (0010) | фикс [0010-01](../fixes/0010-01-buchi-acceptance.md): `Old` в `expand` | T12, T13, T41 | ✅ |
| Диагностики LTL (0035) | текст SE-055; общий сборщик `model_ltl_formulas` → `LtlSite` (0049-06) | `ltl_tests.rs` (SE-055/SE-056 на всех уровнях) | ✅ |
| Достижимость состояний (SE-046) | общий помощник `reachable_targets` | `semantic_tests.rs` | ✅ |
| Генерация C / ST / SV / Rust / PlantUML | не затронуты (вывод не меняется) | снапшоты, гейт детерминизма 0048, гейты `iec2c`/`rustc`/`verilator` | ✅ |
| CLI `compile` / `fmt` | добавлена ветка `verify` в `main()` | T31–T33, `precheck.sh` | ✅ |
| Симулятор | не затронут | `cargo test -p simulation` | ✅ |

Диагностики (0035) — единственная функциональность, задетая правкой 0049-06:
сборщик формул сменил тип возврата. Паритет сохранён намеренным решением —
**десахаризация живёт у верификатора, а не в сборщике**: диагностике нужна
авторская формула, иначе SE-056 ругался бы на атом `Fault`, которого автор не
писал. Проверено `ltl_tests.rs` (тексты и позиции предупреждений не изменились).

## Примеры и контрпримеры (правило 16)

Все — в `grammar/tests/data/verify/`, парами; отличие внутри пары минимально,
чтобы причина вердикта была видна глазом. Прогнаны пробой и попали в README
(раздел «Верификация модели (`lamc verify`)»).

| Фикстура | Свойство | Вердикт | Отличие от пары |
|---|---|---|---|
| `holds.lam` | `G (Fault -> F Idle)` | держится | — |
| `fails.lam` | `G (Fault -> F Idle)` | нарушено, `Idle -> [ Fault ]*` | самопетля `ref Fault` |
| `reachable.lam` | `F Done` | держится | — |
| `unreachable.lam` | `F Done` | нарушено | в `Done` не ведёт ни один `ref` |
| `atom_not_state.lam` | `G temp` | не проверено | атом — переменная |
| `state_scope_holds.lam` | `F Idle` в `Fault` → `G (Fault -> F Idle)` | держится | — |
| `state_scope_fails.lam` | то же | нарушено, `Idle -> [ Fault ]*` | самопетля `ref Fault` |

## Решения заказчика по открытым вопросам стадии (2026-07-16)

Три вопроса карточки фичи — неопределённости семантики, которых ADR не касался
(не дефекты реализации). Вынесены заказчику, решения приняты:

1. **Область формулы состояния** — вариант **«б»**: `: [LTL] φ;` в теле
   состояния `S` = сокращение для `G (S -> φ)`. **Потребовало доработки**: фича
   возвращена в `РАЗРАБОТКА` на задачу
   [0049-06](../development/0049-06-state-formula-scope.md), затем — этот отчёт.
   Обоснование выбора: вариант «а» (оставить) сохранял класс дефекта «формула
   молча не проверяет то, о чём написана» — на модели со стартом `Idle`
   формула `F Idle` в состоянии `Fault` держалась **тривиально**, пропуская
   вечное залипание в сбое.
2. **Импортированные модели** — вариант **«а»**: проверяются все модели файла,
   включая пришедшие через `import`; поведение задокументировано в README
   (раздел «Ограничения»). Сужение области — кандидат в фичи (`--scope`).
3. **Глубина рекурсии** — вариант **«а»**: обходы остаются рекурсивными (замер:
   3000 состояний — норма, 20000 — падение); для типичных моделей запаса
   достаточно. Ограничение задокументировано; итеративный обход — кандидат в
   фичи.

## Окружение

- `rustc 1.99.0-nightly (daf2e5e18 2026-07-13)`, macOS (Darwin 25.5.0), ветка `v2`.
- `cargo test -- --test-threads=1` — 1618 пройдено, 6 игнорировано, 19 наборов.
- `./scripts/precheck.sh` — зелёный (fmt, check, clippy, тесты, сборка примеров,
  гейт детерминизма 0048, гейты `c`/`iec2c`).
- Внешних зависимостей верификация не требует.

## Выводы и дальнейшие шаги

**Вердикт: готово.** Фича закрывается (`ГОТОВО`); карточка получает «Итог».

Что фича доказала сверх своего охвата — **гейт целевого инструмента не заменяет
проверки на смысл**: у 0045/0050 это показали компиляторы, принимавшие молча
неверный вывод, здесь — зелёный CI, годами не замечавший, что автомат Бюхи
принимает не тот язык. Общий у случаев не инструмент, а форма теста: проверялось
то, что легко проверить (структура, компилируемость), а не то, ради чего код
написан (язык, вердикт, поведение).

Дефектов, требующих исправления, стадия не оставила. Заведены **кандидаты в
фичи** (правило 7) — ограничения осознанные, задокументированные в README:

- сужение области проверки (`lamc verify --scope file|all`) — чтобы нарушенное
  свойство импортированной библиотеки не давало ненулевой код импортёру;
- итеративные обходы (`expand`, nested-DFS) — снять потолок ~3000 состояний;
- верификация свойств **над данными** (атом-предикат вместо атома-состояния) —
  строится поверх готового конвейера; сегодня даёт честный `Unsupported`.
