# Отчёт о тестировании фичи 0151: Накопление диагностик внутри отдельной проверки `validate`

> Фича: [../features/0151-diagnostics-batch-within-check.md](../features/0151-diagnostics-batch-within-check.md) · тест-план: [../tests/0151-diagnostics-batch-within-check.md](../tests/0151-diagnostics-batch-within-check.md) · анализ: [../analyze/0151-diagnostics-batch-within-check.md](../analyze/0151-diagnostics-batch-within-check.md)

## Резюме

**Пройдено; фича закрыта.** Все 11 проверок тест-плана зелены, 6 критериев
приёмки ADR выполнены, `./scripts/precheck.sh` — код возврата 0. Исправлений не
потребовалось.

Шесть измеренных классов перешли с одного сообщения на все; две границы
(стадии построения, выражение как элемент) остались на месте и пришпилены.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | два `ref` | ✅ | две `SE-025` |
| T2 | два `cond` | ✅ | две `SE-025` |
| T3 | две `bit`-переменные | ✅ | две `SE-035` |
| T4 | два массива | ✅ | две `SE-038` |
| T5 | два `address` | ✅ | две `SE-048` |
| T6 | два обращения к ячейке | ✅ | две `SE-099` |
| T7 | разные проверки | ✅ | свойство 0130 не потеряно |
| T8 | стадии построения | ✅ | по одному сообщению — решение 0152 |
| T9 | одна диагностика на элемент | ✅ | внутри выражения — первая |
| T10 | порядок | ✅ | по позиции |
| T11 | мутации | ✅ | обе валят ожидаемые тесты |

### Мутации

| Мутация | Упало | Ожидалось |
|---|---|---|
| ранний выход в `validate_bit_values` | тест класса + тест порядка | да |
| ранний выход в `validate_conditions` | тест именованных условий | да — **после** добавления теста |

## Результаты по функциональности

| Функциональность | Статус | Чем подтверждено |
|---|---|---|
| `taktc compile` | ✅ | T1–T10 |
| LSP | ✅ | тот же вход `validate_model_all`; набор тестов зелен |
| Стадии построения | ✅ | T8 |
| Цели, симулятор, корпус | — | не задеты |

## Выводы и дальнейшие шаги

**Кандидат оказался этой же фичей.** Запись блока 2 («два неразрешённых условия
дают одну диагностику») предполагала дыру обхода; проба показала ранний выход —
то есть ровно предмет 0151. Кандидат сложен в фичу, отдельной сущности не
заведено.

**Урок о тестах на код, а не на место.** Мутация `validate_conditions` сперва не
поймалась: `SE-025` рождается в **двух** местах — на ребре перехода и в
объявлении `cond`, — а тест покрывал одно. Общее правило: один код диагностики
не означает одно место эмиссии; заводя тест «по коду», проверь, сколько мест его
дают.

**Что осталось за фичей.** Две независимые ошибки **внутри одного выражения**
по-прежнему дают одну. Это граница принятого решения: отличить причину от
следствия внутри выражения — задача другого порядка сложности, и браться за неё
имеет смысл, если случай встретится на практике.
