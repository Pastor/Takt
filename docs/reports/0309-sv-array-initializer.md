# Отчёт о тестировании фичи 0309: массив с агрегатом в цели `sv`

> Фича: [../features/0309-sv-array-initializer.md](../features/0309-sv-array-initializer.md) · тест-план: [../tests/0309-sv-array-initializer.md](../tests/0309-sv-array-initializer.md) · ADR: [../adr/0309-sv-array-initializer.md](../adr/0309-sv-array-initializer.md)

## Окружение

- macOS (darwin 25.5.0), Rust 1.97.1, verilator 5.050, yosys. Дата: 2026-08-20.

## Замер (правило 30)

Вход: `var arr: [u8; 3] := {1, 2, 3};` с чтением `arr[1]` и записью `arr[0]`.

| Потребитель | До | После |
|---|---|---|
| эталон | `probe` 2, 4, 6 | без изменений |
| `c`, `c-hal`, `st`, `st-at`, `rust`, `plantuml` | переводят | без изменений |
| **`sv`, `sv-mmio`** | `SV-002` на объявлении | переводят, трасса совпадает |

**Контрольный шаг:** тот же вход **без** инициализатора цель переводила и до
правки — это и показало, что предмет узкий (агрегат в ветви сброса), а не
«массивы вообще».

## Сводка прогонов

| Прогон | Результат |
|---|---|
| `cargo test --test conformance conformance_sv_array` | 2 passed |
| `verilator --lint-only -Wall` на порождённом модуле | код 0 |
| `yosys synth` на нём же | код 0 |
| `cargo test --all-features` | провалов нет (после правки двух сторожей границ) |
| `./scripts/precheck.sh` | код 0 |

## Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П2 | Значения и отказ на размере | ✅ |
| П3…П4 | Сдвинутые границы | ✅ |
| П5 | Регрессия и предкоммит | ✅ |

## Что показал прогон

1. **Кандидат называл класс шире предмета.** «Массивы не переводит `sv`» —
   на деле не переводился один агрегат; всё остальное работало.
2. **Форму нельзя было выбрать рассуждением.** Конкатенация `{…}`, годная для
   `struct packed` (0293), для распакованного массива означает другое; `'{…}`
   проверено обоими инструментами до правки кода.
3. **Правка сдвинула две зафиксированные границы.** Оба сторожа пришпиливали
   отказ; переписаны на проверку значения — иначе они бы просто «позеленели» на
   любом выводе.

## Дефекты

Дефектов, кроме предмета фичи, не найдено.

## Редакторский слой (правило 29)

**Не требуется** — подтверждено: лексика, синтаксис и состав кодов не менялись.

## Вердикт

**Готово.** Все восемь целей переводят массив с агрегатом, и трасса RTL
совпадает с эталоном потактово.
