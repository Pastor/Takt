# Отчёт о тестировании фичи 0098: диапазон бита адреса и безопасный дефолтный HAL

> Фича: [../features/0098-port-bit-range-safe-hal.md](../features/0098-port-bit-range-safe-hal.md) · тест-план: [../tests/0098-port-bit-range-safe-hal.md](../tests/0098-port-bit-range-safe-hal.md) · ADR: [../adr/0098-port-bit-range-safe-hal.md](../adr/0098-port-bit-range-safe-hal.md) · закрывает [фикс 0020-01](../fixes/0020-01-port-bit-out-of-range.md)

- **Дата:** 2026-07-20
- **Окружение:** macOS (darwin 25.5.0), cargo nightly, `cc` (Apple clang).
- **Вердикт:** **готово.** Тесты `grammar` зелёные (`hal_bit_range_tests` 2,
  `generator::c` 98), гейт компиляции c-hal зелёный, `precheck.sh` зелёный. Фикс
  0020-01 закрыт.

## Сверка с критериями приёмки (ADR 0098)

| Критерий | Проверка | Результат |
|---|---|---|
| **A1** бит вне [0,63] → `SE-060` | `bit_in_range_ok_out_of_range_is_se060` | ✅ бит 63 генерируется, бит 64 → `SE-060`, текст называет `[0, 63]` |
| **A2** HAL без UB, проверено значением | `hal_reads_wide_bit_without_ub` (mmap) + `bit_word_width_by_bit_index` | ✅ бит 33 читается `uint64_t`, результат «1001»; границы слова 0→1…32→8 |
| **A3** c-hal компилируется гейтом | гейт c-hal в `precheck.sh` | ✅ 7 примеров `cc -c` OK; `elevator_mini` — пропуск SE-052 |
| **A4** `extend_complex` не правлен, цель `c` прежняя | детерминизм-гейт + отсутствие правок примера | ✅ бит 33 обоснован (uint64); вывод `c` байт-в-байт |

## Ключевые наблюдения

- **UB доказан устранённым исполнением, а не грепом.** Значенческий тест
  эмулирует MMIO через `mmap(MAP_FIXED)` по адресу порта (16 ТБ) и вызывает
  сгенерированный `read_bit`: при слове `1<<33` бит 33 = 1, бит 2 = 0; при
  `1<<2` — наоборот. Прежний код (`uint8_t >> 33`) равенства «1001» не даёт.
- **Замер платформы:** `mmap(MAP_FIXED)` macOS отвергает низкие адреса
  (0x10000000 → FAIL), но принимает высокие (0x100000000000 → OK) — отсюда выбор
  16-ТБ адреса; при неудаче тест печатает пропуск, а не падает.
- **Вынос HAL в `c_hal.rs`** снял `c_header.rs` с лимита размера (1334 → 955) и
  из реестра долга — долг модулей −1 (20 → 19 записей).

## Отклонения и ограничения

- **Per-port ширина ≠ per-word** (C-1 ADR): порты на одном адресе с разными
  битами получают разную ширину доступа. Каждый бит извлекается корректно;
  унификация — за Option B (язык выражает ширину слова).
- **Гейт — компиляция в объект**, не линковка (extern fn/main требуют драйверов).
- **Открытый вопрос фикса** разрешён на [0, 63] (уточнение: «ширина типа порта»
  отвергнута как несовместимая с битовой упаковкой).
