# Отчёт о тестировании фичи 0068: верификация свойств над данными

> Фича: [../features/0068-verify-data-properties.md](../features/0068-verify-data-properties.md) · тест-план: [../tests/0068-verify-data-properties.md](../tests/0068-verify-data-properties.md) · ADR: [../adr/0068-verify-data-properties.md](../adr/0068-verify-data-properties.md)

- **Дата:** 2026-07-18
- **Окружение:** macOS (darwin 25.5.0), cargo nightly, cmake/ninja/cc,
  verilator + yosys, MatIEC `iec2c`.
- **Вердикт:** **готово (консервативное ядро).** `lamc verify` даёт вердикт по
  предикатам над данными; `precheck.sh` зелёный целиком (2003 теста, exit 0);
  вывод целей не изменён.

## Сверка с критериями приёмки

| # | Критерий | Результат | Способ |
|---|---|---|---|
| A1 | Предикат над `u8` → вердикт; вершин = состояний × 256 | ✅ | тест `u8_predicate_yields_state_times_256_vertices` (2×256); корпус `comprehensive`/`Controller` → **1024** (4×256, `comprehensive_controller_u8_predicate_is_tracked`) |
| A2 | Три `u8` (> потолка) → `Unsupported` сразу | ✅ | `three_u8_exceeds_ceiling_unsupported`; потолок считается ДО построения |
| A3 | `float` в предикате → `Unsupported` | ✅ | `float_predicate_is_unsupported` |
| **A4** | **Гарантия «держится» сохранена** | ✅ | `tautology_over_domain_holds` (`G (temp<=255)` → Holds), `excluded_middle_holds`; тесты **на вердикт** |
| **A5** | **Направление ошибки: лишний прогон, не пропущенный** | ✅ | `abstracted_source_yields_extra_run_not_missed`; **мутация правила 3** (заморозка данных) валит 2 сторожа — проверено вручную |
| A6 | `bit`-атом → домен 2 | ✅ | `bit_variable_atom_domain_is_two` (3×2=6) |
| A7 | Вердикты 0049 не изменились | ✅ | 52 теста `verification` зелёные без правок; путь управления при пустой `labels` байт-в-байт |
| A8 | Завершается на корпусе; потолок до построения | ✅ | `precheck.sh` зелёный; тест трёх `u8` завершается мгновенно (потолок сработал до материализации) |
| A9 | Язык/версия не изменены | ✅ | предикат выражается существующим `cond`; грамматика LTL не тронута |

## Ключевой мутационный тест (A5)

Правило 3 («невычислимое → недетерминированно по всему домену») реализовано
полным недетерминизмом переходов по данным. Ручная мутация — заморозить данные
(`j2 := j` вместо перебора всех оценок) — валит ровно:

```
abstracted_source_yields_extra_run_not_missed ... FAILED
reachable_false_valuation_violates ... FAILED
```

То есть сторожа **взведены**: неверное направление ошибки (заморозка → ложный
`Holds`) обнаруживается. Урок 0056 соблюдён — сторож проверяет взведённость
ловушки, а не собственную удачу.

## Надёжность `Holds` (обоснование)

Реальный прогон присутствует в абстракции с **точными метками**: полный
недетерминизм переходов включает ход реальных данных, разметка вершины
вычисляется точно по её оценке. Позиция 0 трассы = init-значения переменных
(эталон — «чтение до первого `_tick`», R6 фичи 0033), поэтому начальная вершина
`(старт, init)` корректна. Следовательно, если абстракция говорит `Holds`, ни
один реальный прогон φ не нарушает.

## Границы (найдены при разработке, вне объёма ядра)

1. **`G (temp <= 100)` не выразимо напрямую** — атом LTL это
   `true|false|идентификатор`. Предикат пишется через `cond Safe = temp <= 100;`,
   затем `G Safe`. Синтаксис не менялся.
2. **Свойство над меняющимися данными → `Violated` (возможно, ложный).**
   Консервативное ядро доказывает лишь тавтологии над доменом и управляющие
   свойства. Полный Option D (прецизионный ход данных) отложен — риск
   `Holds`-несоответствия. Остаётся кандидатом.
3. **T9/T10 (самопетля по данным) не реализованы** — ядро **всегда** сохраняет
   петлю (безопасная сторона правила 6: снятие требует точного вычисления guard,
   которого в ядре нет).

## Найденные дефекты

Нет. Правка внутри `verification/` — вывод всех целей побайтово прежний
(`git diff examples/generated` пуст).
