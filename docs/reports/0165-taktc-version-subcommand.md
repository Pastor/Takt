# Отчёт о тестировании фичи 0165: `taktc version`

> Фича: [../features/0165-taktc-version-subcommand.md](../features/0165-taktc-version-subcommand.md) · тест-план: [../tests/0165-taktc-version-subcommand.md](../tests/0165-taktc-version-subcommand.md) · анализ: [../analyze/0165-taktc-version-subcommand.md](../analyze/0165-taktc-version-subcommand.md)

## Резюме

Все проверки пройдены; фича готова к закрытию. Инструмент отвечает о своей
версии по трём входам, вывод согласован с константами, гейт версии не тронут.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | `taktc version` | ✅ | две строки, код 0 |
| T2 | Три входа согласованы | ✅ | равенство выводов проверено |
| T3 | Версия языка из константы | ✅ | сверка с `LANGUAGE_VERSION` |
| T4 | Версия крейта из константы | ✅ | сверка с `CARGO_PKG_VERSION` |
| T5 | Версии различены | ✅ | «taktc …» и «язык Takt …» |
| T6 | Справка называет подкоманду | ✅ | — |
| T7 | Перечень команд в ошибке | ✅ | — |
| T8 | Гейт версии не изменён | ✅ | файл не правился, прогон OK |
| T9 | Документ | ✅ | показан **формат**, не числа |
| T10 | Предкоммит | ✅ | код возврата прочитан явно |

## Результаты по функциональности

| Функциональность | Статус |
|---|---|
| Библиотека `version.rs` | ✅ |
| CLI `taktc` | ✅ |
| Документ | ✅ |
| Язык, генераторы, симулятор, LSP | — |

## Выводы и дальнейшие шаги

1. **Входное условие карточки оказалось устаревшим — и это стоило проверить.**
   «Заблокировано лимитом размера модуля» было верно на момент регистрации
   (2026-07-28), но к моменту работы `taktc.rs` имел 216 строк запаса и в
   baseline не значился. Предварительный вынос кода, намеченный первым шагом,
   не понадобился. Урок общий: **условия карточки перепроверяются на стадии
   архитектуры**, иначе делается лишняя работа под верное когда-то основание.
2. **Пример в документе едва не устарел при рождении.** Первая редакция несла
   `taktc 0.38.0`; версия крейта растёт каждой фичей, а гейт версии `book/` не
   проверяет — значит расхождение жило бы молча. Показан формат.
3. **Гейт глифов отверг `⚠️`** (`U+26A0` вне шрифта Fira Code) — в книге принят
   другой приём (врезка). Ещё один случай, когда машина поймала то, чего глаз не
   заметил.
4. **Option D ADR (сверять вывод CLI гейтом) отклонён и остаётся отклонённым:**
   гейт версии дёшев и идёт до сборки; контракт вывода держит тест крейта.
5. **Исправлений (`docs/fixes/`) не требуется.**
