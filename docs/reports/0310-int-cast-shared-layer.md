# Отчёт о тестировании фичи 0310: общий носитель правила приведения

> Фича: [../features/0310-int-cast-shared-layer.md](../features/0310-int-cast-shared-layer.md) · тест-план: [../tests/0310-int-cast-shared-layer.md](../tests/0310-int-cast-shared-layer.md) · ADR: [../adr/0310-int-cast-shared-layer.md](../adr/0310-int-cast-shared-layer.md)

## Окружение

- macOS (darwin 25.5.0), Rust 1.97.1. Дата прогона: 2026-08-20.

## Замер (правило 30)

| Вход | До | После |
|---|---|---|
| `var v: u8 := 300 as u8;` | эталон `44`; `sv`, `sv-mmio` — `SV-002` | все девять — `44` |
| `Tuner(limit := 300 as u8)` | `SE-083` | `44` |
| `var v: i8 := 300 as i8;` | эталон `0` молча, `c`/`rust` `44`, `st` теряла инициализатор, `sv` — `SV-002` | все — `SE-121` |
| **контроль** `var v: u16 := 5 as u16;` | `5` у всех | без изменений |

## Сводка прогонов

| Прогон | Результат |
|---|---|
| `cargo test --test semantic int_cast` | 5 passed |
| Мутация: снять маску обёртки в носителе | тест краснеет ✔ |
| `cargo test --test conformance const_cast_init` | 3 passed |
| `cargo test --all-features` | провалов нет |
| `check-book-diagnostics.py`, `check-diagnostic-codes.sh` | 265 кодов сверены |
| `./scripts/precheck.sh` | код 0 |

## Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П5 | Обёртка, отказ, контроль, носитель, граница | ✅ |
| П6…П7 | Цель `sv` печатает и отвергает | ✅ |
| П8…П9 | Реестры, регрессия, предкоммит | ✅ |

## Что показал прогон

1. **Эталон расходился сам с собой.** `300 as i8` в теле — `SIM-003`, в
   инициализаторе — молчаливый ноль. Это нашлось только потому, что замер
   ставил один вход в два места.
2. **Мало вычислить — надо ещё донести.** Первая редакция вернула `SE-121`, и
   он растворился в свёртке, проглатывавшей любую ошибку вычислителя: проба
   по-прежнему давала ноль. Отсюда предикат `is_not_constant`.
3. **`SE-085` пришлось отнести к «не константе» по прогону.** Первая редакция
   сочла бюджет ошибкой записи, и цикл определений начал отвечать сообщением о
   пределе вместо `SE-072` — поймал тест `const_cycle_is_se072_not_hang`.
4. **Две зафиксированные границы сдвинулись** (0286 и ожидание теста 0205);
   обе переписаны на проверку значения, а не отказа.

## Дефекты

Дефектов, кроме предмета фичи, не найдено.

## Редакторский слой (правило 29)

**Не требуется** — подтверждено: лексика и синтаксис не менялись; новый код
`SE-121` доезжает до редактора общей точкой `collect_compile_diagnostics`.

## Вердикт

**Готово.** Целочисленное приведение считается одними формулами у компилятора и
эталона; знаковое переполнение отвергается вместо молчаливого расхождения.
