# Отчёт о тестировании фичи 0314: предупреждение о выброшенном вызове

> Фича: [../features/0314-c-dropped-builtin-warning.md](../features/0314-c-dropped-builtin-warning.md) · тест-план: [../tests/0314-c-dropped-builtin-warning.md](../tests/0314-c-dropped-builtin-warning.md) · ADR: [../adr/0314-c-dropped-builtin-warning.md](../adr/0314-c-dropped-builtin-warning.md)

## Окружение

- macOS (darwin 25.5.0), Rust 1.97.1. Дата: 2026-08-20.

## Замер (правило 30)

| Потребитель | До | После |
|---|---|---|
| эталон | печатает `debug: такт` | без изменений |
| `c`, `c-hal` | вызова в выводе нет, **молчание** | вывод тот же + `CC-024` |
| `st`, `st-at` | `ST-011` | без изменений |
| `rust` | `RS-011` | без изменений |
| `sv`, `sv-mmio` | `SV-002` | без изменений |

## Сводка прогонов

| Прогон | Результат |
|---|---|
| `cargo test --test targets c_builtin_dropped` | 3 passed |
| `taktc compile -t c` | предупреждение с позицией оператора |
| `taktc compile -t c --quiet` | молчит |
| `cargo test --all-features` | провалов нет |
| `check-book-diagnostics.py` | 267 кодов сверены |
| `./scripts/precheck.sh` | код 0 |

## Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П4 | Предупреждение, вывод, счёт, контроль | ✅ |
| П5…П7 | `--quiet`, реестры, регрессия | ✅ |

## Что показал прогон

1. **Позиция досталась бесплатно.** Носитель `site` (фича 0308) уже объявляет
   место оператора, и предупреждение получило координату без отдельной работы.
2. **Счёт пришлось проверять отдельно.** Первая редакция сторожа смотрела
   «есть ли предупреждение»; на двух вызовах она прошла бы и при одном.
3. **`--quiet` работает без правок:** канал 0168 глушит предупреждения целей
   там же, где и прочие, — ради этого печать из целей и была изъята.

## Дефекты

Дефектов, кроме предмета фичи, не найдено.

## Редакторский слой (правило 29)

**Не требуется** — подтверждено: лексика и синтаксис не менялись; предупреждения
целей в редактор не доезжают по устройству (их источник — генерация, а не
семантика).

## Вердикт

**Готово.** Вывод цели не изменился, а автор узнаёт, что вызов не поедет в
прошивку.
