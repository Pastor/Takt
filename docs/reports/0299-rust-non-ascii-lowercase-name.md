# Отчёт о тестировании фичи 0299: Регистр имени приводится по Unicode

> Фича: [../features/0299-rust-non-ascii-lowercase-name.md](../features/0299-rust-non-ascii-lowercase-name.md) · тест-план: [../tests/0299-rust-non-ascii-lowercase-name.md](../tests/0299-rust-non-ascii-lowercase-name.md) · анализ: [../analyze/0299-rust-non-ascii-lowercase-name.md](../analyze/0299-rust-non-ascii-lowercase-name.md)

## Резюме

**Пройдено; фича готова к закрытию (`ГОТОВО`).** Шесть проверок тест-плана
зелены, исправлений не потребовалось. Полный прогон: 3256 тестов, ноль
провалов; `./scripts/precheck.sh` зелён.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Не-ASCII имя поднимается | ✅ | `OutBitPort::Кнопка` |
| T2 | Контроль: верные имена | ✅ | `Кнопка`, `Button` не изменились |
| T3 | Коллизия регистра | ✅ | `RS-005`, тот же механизм, что для ASCII |
| T4 | Настоящий `clippy -D warnings` | ✅ | было `error` upper camel case, стало чисто |
| T5 | Нормализация как таковая | ✅ | в том числе смешанный алфавит `пуск_button` → `ПускButton` |
| T6 | Корпус и гейты | ✅ | снимки не изменились |

## Замер (правило 30)

| Вход | До | После |
|---|---|---|
| `out кнопка` | `кнопка,` → `clippy`: **error** | `Кнопка,` → принято |
| `out Кнопка` **(контроль)** | принято | **не изменилось** |
| `out button` **(контроль)** | `Button,` | **не изменилось** |
| прочие цели, эталон | `c` переводит, `st`/`sv` отказывают своими кодами | **не изменились** |

## Результаты по функциональности

| Функциональность | Статус | Основание |
|---|---|---|
| Слой имён `semantic::naming` | ✅ | T5 |
| Цель `rust` | ✅ | T1–T4 |
| Прочие цели, эталон | — | ответ на этот вход не изменился |
| LSP, документ | — | не применимо (обосновано в анализе) |

## Выводы и дальнейшие шаги

- Исправления не нужны; фича закрывается. Это **остаток работы 0200**: язык
  принял не-ASCII имена, а правило регистра осталось ASCII-только.
- Новых кандидатов замер не дал.
- **Кандидатов, называющих 0299, не осталось** (правило 28).
