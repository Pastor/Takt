# Отчёт о тестировании фичи 0162: Пропущенные метки версий языка и сторож правила 22

> Фича: [../features/0162-git-tag-v040.md](../features/0162-git-tag-v040.md) · тест-план: [../tests/0162-git-tag-v040.md](../tests/0162-git-tag-v040.md) · анализ: [../analyze/0162-git-tag-v040.md](../analyze/0162-git-tag-v040.md)

## Резюме

**Пройдено, фича готова к закрытию (`ГОТОВО`).** Десять проверок из одиннадцати
зелены, одиннадцатая (`T11`) — проверка предусловия, выполненная до правки.
Исправлений (`docs/fixes/0162-YY-*`) не потребовалось.

Главный результат — **не** тег из заголовка карточки, а то, что замер нашёл
**второй** пропуск, сделанный накануне (фича 0243, 2026-08-17). Один пропуск
можно счесть наследием давнего периода; два из восьми, с последним вчерашним, —
свойство правила без сторожа. Поэтому фича закрывается не двумя тегами, а
тегами **и** гейтом.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Разметка без разрывов | ✅ | `v0.3.0 v0.4.0 v0.5.0 v0.6.0 v0.7.0 v0.8.0 v0.9.0 v0.10.0` — восемь |
| T2 | Теги на измеренных коммитах | ✅ | `v0.4.0^{commit}` = `bba0115e`, `v0.10.0^{commit}` = `7d2174e0` |
| T3 | Теги аннотированные | ✅ | `git for-each-ref`: `objecttype = tag` у обоих |
| T4 | Пропуск тега пойман | ✅ | сообщение называет тег и печатает команды поиска коммита и создания тега |
| T5 | Верное состояние принято | ✅ | код 0 |
| T6 | Молчание в момент подъёма | ✅ | «ПРОПУСК: подъём 0.1.0 → 0.2.0 ещё не закоммичен», код 0, подсказка с `git tag -a v0.2.0` |
| T7 | Лёгкий тег отвергнут | ✅ | сообщение называет `objecttype` и команду замены |
| T8 | Тег вне истории `HEAD` отвергнут | ✅ | «указывает на коммит, не достижимый из HEAD» |
| T9 | Мягкий и строгий режимы различаются | ✅ | без флага код 0 и «ПРОПУСК»; при `PRECHECK_STRICT=1` — ошибка «нет ни одного тега» |
| T10 | Гейт в предкоммите | ✅ | `./scripts/precheck.sh` — код возврата 0, шаг виден в выводе |
| T11 | Публикуется новое имя | ✅ | `git ls-remote --tags origin` до правки: шесть тегов, `v0.4.0` и `v0.10.0` отсутствуют |

## Результаты по функциональности

| Функциональность | Как задета | Статус |
|---|---|---|
| Компилятор `taktc`, симулятор `takt-sim` | не задеты: правок кода нет | ✅ T10 |
| Метки истории git | два аннотированных тега | ✅ T1–T3, T11 |
| Гейты предкоммита | два новых шага | ✅ T4–T10 |
| CI (`.github/workflows/ci.yml`) | `fetch-depth: 0` у задания `ubuntu` | ⬜ **не проверено прогоном** — Actions заблокированы по биллингу (фича 0175); проверены отступы YAML и то, что задание `windows` правки не требует |

## Выводы и дальнейшие шаги

1. **Сторож нашёл дефект в собственном гейте.** Первая редакция проверки «тега
   нет» падала: в свежем temp-репозитории тегов нет **вовсе**, и срабатывала
   ветвь «клон без тегов». Различить «тег потерян» и «теги не выкачаны» нечем —
   это честная граница, но её нужно было **назвать**. Теперь она в заголовке
   гейта, а проверка ставит посторонний тег, чтобы проверять отсутствие **тега
   версии**, а не пустоту списка.
2. **Замер потребовал `^{commit}`.** У аннотированного тега `git rev-parse v0.5.0`
   отдаёт объект **тега**, а не коммит; наивное сравнение хешей дало ложное
   «тег указывает не туда» у всех шести существующих тегов и едва не увело
   вывод в «историю переписывали». Разыменование — часть замера, а не деталь.
3. **Гейт стережёт свежую версию, а не только прошлое.** После коммита подъёма
   версии в дереве и в `HEAD` совпадают, и тег становится обязателен — пропуск
   `v0.10.0` был бы пойман на первом же прогоне после фичи 0243.
4. **Исправлений не требуется.** Дефектов, заводящих `docs/fixes/0162-YY-*`,
   не найдено.
5. **Вынесено кандидатом** (правило 28): комментарий задания `windows` в
   `ci.yml` ссылается на однопоточный прогон «как требуют CLAUDE.md и
   precheck.sh», хотя фича 0190 сняла `--test-threads=1`; сам флаг там остался.
   Правка меняет поведение CI, проверить которое сейчас нечем, — потому вынесена,
   а не сделана попутно.
