# Отчёт о тестировании фичи 0284: Структура без полей

> Фича: [../features/0284-empty-struct-semantics.md](../features/0284-empty-struct-semantics.md) · тест-план: [../tests/0284-empty-struct-semantics.md](../tests/0284-empty-struct-semantics.md) · анализ: [../analyze/0284-empty-struct-semantics.md](../analyze/0284-empty-struct-semantics.md)

## Резюме

**Пройдено, фича готова к закрытию (`ГОТОВО`).** Восемь проверок тест-плана
зелены, исправлений (`docs/fixes/0284-YY-*`) не потребовалось.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Отказ на контрпримере | ✅ | `SE-115` у CLI и у эталона |
| T2 | Координата — объявление | ✅ | строка `struct Empty` |
| T3 | Текст называет правило и выход | ✅ | имя, «хотя бы одно поле», «удалите объявление» |
| T4 | Накопление | ✅ | две пустые структуры → две диагностики |
| T5 | Контроль: непустая законна | ✅ | диагностик нет |
| T6 | Граница: перечисление судит своё | ✅ | `SE-105`, `SE-115` отсутствует |
| T7 | Настоящие инструменты | ✅ | непустая проходит `cc -std=c11 -pedantic` и `iec2c`; пустая прежде давала `-Wgnu-empty-struct` и `no structure element declared` |
| T8 | Корпус цел | ✅ | `precheck.sh` — код 0, правок `examples/` не потребовалось |

## Результаты по функциональности

| Функциональность | Как задета | Статус |
|---|---|---|
| Семантика | проверка рядом с `SE-105` | ✅ T1–T6 |
| Эталон, цели `c`/`c-hal`/`st`/`st-at` | вход отвергается до генерации | ✅ T7, T8 |
| Цели `rust`, `sv` | **не задеты** — соседний класс | — |
| Грамматика | не тронута | ✅ T8 |
| Документ `book/` | раздел «Типы» + приложение | ✅ T8 |

## Выводы и дальнейшие шаги

1. **Карточка требовала замерить остальные цели перед решением — и это себя
   оправдало.** Замер показал, что `RS-014` у `rust` и висячая ссылка на
   необъявленный тип у `sv` **не про пустоту**: они воспроизводятся и на
   непустой структуре. Без контроля фича приписала бы себе чужой класс и
   «починила» бы его запретом, которого он не лечит.
2. **Две пробы из трёх были негодными — снова.** Первая не воспроизвела дефект
   у `rust` и `sv` вовсе: переменная была неиспользуемой, и штатный фильтр
   выбросил её из вывода (тот же капкан, что в 0300). Вторая дала ложный
   `redefinition of 'Pair'` в C: файл назывался `pair.takt`, и корневая модель
   столкнулась со `struct Pair` (известный класс 0195).
3. **Предкоммит покраснел на моём же тесте** — `clippy` отверг `line as usize`
   (`unnecessary_cast` под `-D warnings`). Правка однострочная; отмечено
   потому, что гейт сработал ровно как задуман: тесты идут под теми же линтами,
   что и код.
4. **Исправлений не требуется.** Дефектов, заводящих `docs/fixes/0284-YY-*`,
   не найдено.
5. **Кандидат актуализирован** (правило 28): запись «структуры не транслируются
   целями `st` и `rust` дальше объявления» расширена на цель **`sv`** — она
   печатает ссылку на необъявленный тип (`verilator`: `Can't find typedef`), и
   это воспроизводится на **любой** структуре, а не только на пустой.
