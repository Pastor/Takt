# Отчёт о тестировании фичи 0346: индексация параметра-массива

> Фича: [../features/0346-array-param-index.md](../features/0346-array-param-index.md) · тест-план: [../tests/0346-array-param-index.md](../tests/0346-array-param-index.md) · ADR: [../adr/0346-array-param-index.md](../adr/0346-array-param-index.md)

## Окружение

- macOS (darwin 25.5.0), clang. Дата: 2026-08-20.

## Сводка прогонов

| Прогон | Результат |
|---|---|
| Прежний вход: эталон и все цели | `SE-003` |
| Новый вход: `scripts/probe.sh` | эталон и все восемь целей исполняют |
| `cargo test --test semantic array_param_index` | 4 passed |
| `cargo test --test conformance array_param` | 1 passed |
| `cargo test` (весь) | зелёный |
| `./scripts/precheck.sh` | код 0 |

## Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П6 | Все | ✅ |

## Что показал прогон

1. **Ломался язык, а не цель.** Отказ из семантики выглядит «согласованным»:
   все потребители отвечают одинаково, и `probe.sh` честно пишет «поведение
   цели НЕ измерено» — именно эта строка и указала на предмет.
2. **Имя теста едва не подменило причину.** Функция `at` дала отказ парсера:
   `at` — ключевое слово (адрес порта). Тот же класс, что имя `ln` в фиче 0342.
3. **Один замер дал фичу и двух кандидатов.**

## Дефекты

Не найдено.

## Редакторский слой (правило 29)

**Не требуется** — подтверждено: лексика и синтаксис не изменились.

## Вердикт

**Готово.** Массив в параметре функции работоспособен; значения сверены.
