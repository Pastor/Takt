# Отчёт о тестировании 0354: Умолчание `duration` у эталона

- **Фича:** [../features/0354-sim-duration-default.md](../features/0354-sim-duration-default.md)
- **Тест-план:** [../tests/0354-sim-duration-default.md](../tests/0354-sim-duration-default.md)
- **Дата:** 2026-08-21

## Окружение

toolchain `1.97.1`, `cc` в PATH. Прогоны: `cargo test --test conformance
conformance_duration_default`, `cargo test` (весь набор), полный
`./scripts/precheck.sh`.

## Сверка с тест-планом

| № | Условие | Результат |
|---|---|---|
| С1 | трасса эталона = трасса цели `c` | ✅ `[1000, 2000, 3000, 4000, 5000]` |
| П1 | предусловие: трасса накапливающая | ✅ |

Весь набор `cargo test` — зелёный (3000+ тестов), предкоммит зелёный.

## Найденные дефекты

Фиксов не потребовалось. Две поправки **в ходе** работы:

1. **Проба измеряла чужую проверку.** Переменная звалась `step`, и цель `st`
   отказывала `ST-014` (имя зарезервировано в IEC) раньше предмета. Имя
   заменено на `tick_len`.
2. **Драйвер сверки не привязывал битовый колбэк** — запись из тела шла по
   нулевому указателю, и трасса цели приходила пустой (`[]`). Видно это было
   сразу: сверка сравнивала непустое с пустым, а не два разных значения.

## Мутационная проверка

| Мутация | Кто упал | Что доказано |
|---|---|---|
| возврат `Value::Number(0)` для `duration` | сверка, на **предусловии**: `[0, 0, 0, 0, 0]` | сторож видит не только отказ, но и потерю накопления |

## Редакторский слой (правило 29)

**Правок не потребовалось, ответ зафиксирован:** лексика, синтаксис, семантика
и набор диагностик не затронуты.

## README и документ (правила 8, 24)

**Правок не потребовалось:** правило умолчания уже определено восемью целями,
язык не менялся; README устройство вычислителя начальных значений симулятора не
описывает.

## Вердикт

**ГОТОВО.** Условие тест-плана выполнено, сторож проверен мутацией, предкоммит
зелёный.
