# Отчёт о тестировании фичи 0350: порт составного типа

> Фича: [../features/0350-port-composite-type.md](../features/0350-port-composite-type.md) · тест-план: [../tests/0350-port-composite-type.md](../tests/0350-port-composite-type.md) · ADR: [../adr/0350-port-composite-type.md](../adr/0350-port-composite-type.md)

## Окружение

- macOS (darwin 25.5.0), clang, verilator 5.050, yosys, MatIEC `iec2c`.
  Дата: 2026-08-20.

## Сводка прогонов

| Прогон | Результат |
|---|---|
| Прежний вывод `c` | `cc`: «passing 'Pair' … incompatible type 'int64_t'» |
| Прежний вывод `sv` | «Reference to 'pair_t' before declaration» |
| Проба: массив в шапке, сброс `'{default: '0}` | verilator ок, **yosys отвергает шапку** |
| Проба: массив в шапке, поэлементный сброс | то же |
| Новый вывод `sv` (порт-структура) | verilator и yosys чисты |
| `cargo test --test targets port_composite` | 3 passed |
| Снимки `book/` | совпадают |
| `./scripts/precheck.sh` | код 0 |

## Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П5 | Все | ✅ |

## Что показал прогон

1. **Несогласие было двусторонним:** две цели порождали невалидный вывод, а
   четыре отказывали — то есть «поведение языка» не существовало.
2. **Отказ бывает верным ответом.** Две формы сброса массива показали, что дело
   не в форме: шапку с распакованным массивом yosys не принимает вовсе.
3. **Правка порядка окупилась дважды:** типы, вынесенные из модуля, сделали
   выразимым порт-структуру — а тот же класс правился в 0347 внутри модуля.

## Дефекты

Не найдено.

## Редакторский слой (правило 29)

**Не требуется** — подтверждено: лексика и синтаксис не изменились.

## Вердикт

**Готово.** Порт составного типа либо переводится, либо отвергается с
причиной — молчаливо невалидного вывода нет.
