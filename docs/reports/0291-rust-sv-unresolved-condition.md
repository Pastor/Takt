# Отчёт о тестировании фичи 0291: Решение «ребро безусловно» — у одного носителя

> Фича: [../features/0291-rust-sv-unresolved-condition.md](../features/0291-rust-sv-unresolved-condition.md) · тест-план: [../tests/0291-rust-sv-unresolved-condition.md](../tests/0291-rust-sv-unresolved-condition.md) · анализ: [../analyze/0291-rust-sv-unresolved-condition.md](../analyze/0291-rust-sv-unresolved-condition.md)

## Резюме

**Пройдено, фича готова к закрытию (`ГОТОВО`).** Семь проверок тест-плана
зелены, исправлений (`docs/fixes/0291-YY-*`) не потребовалось.

Главных результатов два, и оба — поправки к тому, что фича о себе знала:
копий правила оказалось **восемь**, а не две, и дефект **недостижим**, то есть
`Tier 1` этой записи замером не подтверждён.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Отсутствие условия безусловно | ✅ | предикат отвечает `true` только на `None` |
| T2 | Неразрешённое — нет | ✅ | мутация «вернуть `Unresolved`» валит этот тест |
| T3 | Настоящее условие — нет | ✅ | сторож от обратной мутации |
| T4 | Обхода предиката нет | ✅ | на **первом** прогоне сторож нашёл 3 места сверх известных 2 и упал списком; после правки зелен |
| T5 | Недостижимость названа | ✅ | `ref Done: nosuchname;` → `SE-025` у всех целей |
| T6 | Корпус не изменился | ✅ | 3179 тестов, `precheck.sh` — код 0, правок `examples/` нет |
| T7 | Паттерн состояния не задет | ✅ | `ref Stop: S(Ping) = End;` — ребро условное: `rust` → `RS-020`, `sv` → `SV-002` |

## Результаты по функциональности

| Функциональность | Как задета | Статус |
|---|---|---|
| Цели `rust`, `sv` | ветвь `Unresolved` снята | ✅ T2, T4, T7 |
| Цели `c`, `st`, эталон, `kripke.rs`, Ce14 (×2) | вызов сведён к предикату | ✅ T4, T6 |
| LSP, плагины, документ | не задеты — язык не меняется | — |

## Выводы и дальнейшие шаги

1. **Сторож окупился на первом прогоне.** Кандидат называл два места; греп нашёл
   ещё три — `verification/kripke.rs` и два вхождения в
   `semantic/validate/nondeterminism.rs`. Они были **верны**, но оставались
   отдельными копиями одного правила: то есть ровно та схема, по которой `rust`
   и `sv` и разошлись с `c`.
2. **`Tier 1` не подтверждён замером — расстановка исправлена.** Уровень
   выводился из текста кандидата, утверждавшего «валидный вывод с другим
   автоматом». Прогон показал: вход отсекает `SE-025` до генерации, поведение
   действующих программ не затронуто. По шкале `FEATURES.md` это `Tier 4`.
   ⚠️ Урок общий: **уровень, выведенный из чужого утверждения, проверяется
   прогоном, как и всё остальное.** Расстановка 2026-08-18 делалась по текстам
   кандидатов и в этой части оказалась неточна; остальные записи `Tier 1` и `2`
   при взятии в работу подлежат той же перепроверке.
3. **Правку это не отменяет.** Речь о **ловушке**: ветвь молчаливой подмены
   автомата, которую от срабатывания отделяет **чужая** проверка (`SE-025`
   не давала обещания охранять генераторы). Цена снятия — предикат и восемь
   вызовов; цена срабатывания — молчаливо другой автомат в прошивке.
4. **Исправлений не требуется.** Дефектов, заводящих `docs/fixes/0291-YY-*`,
   не найдено.
5. **Кандидатов не заведено.** Граница сторожа (он видит только форму
   `matches!`) названа в его док-строке и в ADR; заводить под неё запись значило
   бы дублировать уже записанное.
