# Отчёт о тестировании фичи 0373: Локальная переменная структурного типа у цели sv

> Фича: [../features/0373-sv-struct-local.md](../features/0373-sv-struct-local.md) · тест-план: [../tests/0373-sv-struct-local.md](../tests/0373-sv-struct-local.md) · анализ: [../analyze/0373-sv-struct-local.md](../analyze/0373-sv-struct-local.md)

## Резюме

**Пройдено, фича готова к закрытию.** Все проверки тест-плана зелёные, три
мутации роняют ровно тех сторожей, ради которых заведены. Полный прогон —
3358 тестов, 0 провалов; `precheck.sh` зелёный; вывод корпуса не изменился.

Замер после правки (`probe.sh`, те же входы):

| Вход | было | стало |
|---|---|---|
| локальная структура, запись по полям | yosys: Latch inferred | `verilator принял · yosys синтезировал` |
| локальная структура, присваивание целиком | yosys: Latch inferred | `verilator принял · yosys синтезировал` |
| локальный массив структур | yosys: Latch inferred | `verilator принял · yosys синтезировал` |
| локальный массив скаляров (контроль) | принимался | принимается |
| локальное перечисление (контроль) | принималось | принимается |

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Модуль синтезируется | ✅ | `struct_local_is_synthesizable` |
| T2 | Значения совпадают с эталоном | ✅ | `struct_local_values_match_generated_sv`, трассы равны |
| T3 | Объявление предшествует операторам | ✅ | `struct_local_declaration_precedes_statements` |
| T4 | Умолчание целиком | ✅ | там же: `field = '0;`, `whole = '0;` |
| T5 | Умолчание по полям | ✅ | там же: `field.lo = 8'd0;`, `cells[1].hi = 8'd0;` |
| T6 | Контроль: массив скаляров | ✅ | проба принята обоими инструментами |
| T7 | Контроль: перечисление | ✅ | то же |
| T8 | Вывод корпуса не изменился | ✅ | `git diff examples/generated/` пуст |
| M1 | Мутация: не поднимать | ✅ | упали T1 и T3 |
| M2 | Мутация: умолчание только целиком | ✅ | упали T1 и T3 |
| M3 | Мутация: умолчание только по листьям | ✅ | упали T1 и T3 |

## Результаты по функциональности

| Функциональность | Итог |
|---|---|
| цель `sv`/`sv-mmio` | ✅ три формы записи синтезируются, значения верны |
| прочие цели и эталон | — не затронуты |

## Выводы и дальнейшие шаги

1. **Класс виден одному инструменту из трёх.** `taktc` возвращал ноль,
   verilator принимал, отвечал только yosys. Ровно ради этого гейт цели держит
   два инструмента (0045), а проба (0362) — оба.
2. **Форма выбирается прогоном, и мерить надо КОД ВОЗВРАТА.** Первая попытка
   считала `grep '^ERROR'` по выводу yosys, который печатает `путь:строка:
   ERROR: …`; наивный греп объявил годными три негодные формы. Ошибку показала
   проба, смотрящая на код возврата.
3. **Умолчаний два, и это не избыточность:** yosys считает незаданной ту
   «форму» сигнала, которой присваивает тело, — целую или полевую.
4. **Проба переписывалась дважды**, и обе ошибки вели к неверному выводу: имя
   локальной, совпадающее с именем типа (в IEC идентификаторы
   регистронезависимы), и частично прочитанная структура (`UNUSEDSIGNAL`). Оба
   класса вынесены кандидатами.
5. Корпус класс не покрывает — структурных локальных в `examples/` нет ни
   одной. Исправлений не потребовалось.
