# Отчёт о тестировании фичи 0335: разряд в позиции числового значения

> Фича: [../features/0335-bit-value-in-targets.md](../features/0335-bit-value-in-targets.md) · тест-план: [../tests/0335-bit-value-in-targets.md](../tests/0335-bit-value-in-targets.md) · ADR: [../adr/0335-bit-value-in-targets.md](../adr/0335-bit-value-in-targets.md)

## Окружение

- macOS (darwin 25.5.0), Rust 1.97.1, clang, MatIEC `iec2c`, verilator 5.050.
  Дата: 2026-08-20.

## Сводка прогонов

| Прогон | Результат |
|---|---|
| `scripts/probe.sh` на входе с разрядом | эталон `1`, все цели «OK» — **вердикт даёт не он** |
| `rustc` на прежнем выводе | `E0308` |
| `iec2c` на прежнем выводе | 2 ошибки «Incompatible data types» |
| `verilator -Wall` на прежнем выводе | `WIDTHEXPAND` |
| `cargo test --test targets bit_value` | 3 passed |
| `cargo test --test conformance bit_value` | 1 passed |
| Мутация «снять `BOOL_TO_`» | сверка ST падает на `iec2c` |
| `./scripts/precheck.sh` | код 0 |

## Сверка с тест-планом

| # | Условие | Итог |
|---|---|---|
| П1…П7 | Все | ✅ |

## Что показал прогон

1. **`probe.sh` этот класс не показывает.** Он сводит **коды возврата**, а все
   три цели возвращали ноль. Вердикт дал прогон **целевых** инструментов —
   напоминание, что скрипт замер собирает, но не судит (правило 30).
2. **Один корень, три симптома.** Разряд печатается булевым у трёх целей
   независимо, и все три ошиблись в одной позиции — это не совпадение, а
   свойство печатника выражений, не знающего позиции.
3. **Найдено попутно.** Проба ставилась под остаток от отрицательного; разряд
   в ней был вспомогательным. Соседний вход в пробе окупается.

## Дефекты

Не найдено.

## Редакторский слой (правило 29)

**Не требуется** — подтверждено: лексика и синтаксис не изменились.

## Вердикт

**Готово.** Разряд в позиции числа переводят все восемь целей, вывод
принимается всеми целевыми инструментами, значения сверены с эталоном.
