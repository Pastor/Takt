# Отчёт о тестировании фичи 0363: Индексация многомерного массива у цели st

> Фича: [../features/0363-st-multidim-subscript.md](../features/0363-st-multidim-subscript.md) · тест-план: [../tests/0363-st-multidim-subscript.md](../tests/0363-st-multidim-subscript.md) · анализ: [../analyze/0363-st-multidim-subscript.md](../analyze/0363-st-multidim-subscript.md)

## Резюме

**Пройдено, фича готова к закрытию.** Цель `st` переводит многомерный массив
и считает **те же значения**, что эталон, — доказано потактовой сверкой, а не
фактом компиляции. Дефектов оказалось три, и второй виден только сверкой.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Чтение элемента | ✅ | `iec2c принял · cc собрал` (было: «Number of subscripts/indexes does not match») |
| T2 | Запись элемента | ✅ | тот же печатник выражений |
| T3 | Переменный индекс | ✅ | `cells[i][j]` → `cells[i, j]` |
| T4 | Три измерения | ✅ | `cube[1][0][1]` → `cube[1, 0, 1]` |
| T5 | Массив бит-векторов | ✅ | `rows : ARRAY [0..1] OF USINT := [255, 3];` |
| T6 | Потактовая сверка значений | ✅ | трассы совпали: `[[1,12,3],[2,22,3],[3,32,3],…]` |
| T7 | Сверка ловит перестановку индексов | ✅ | мутация «снять `reverse`» → «такт 1 разошёлся» |
| T8 | Сверка ловит потерю строки | ✅ | мутация «вложенный инициализатор» → `other = 0` против `3` |
| T9 | Контрольный вход | ✅ | одномерный массив принимают все восемь целей и до, и после |
| T10 | Широкий бит-вектор в массиве | ✅ | `ST-011` — честный отказ, а не молчание |
| T11 | Регресс целей | ✅ | `cargo test --test targets` — 391 зелёный; `--test conformance` — 159 |
| T12 | Вывод корпуса не изменился | ✅ | предкоммит: снапшоты и гейт детерминизма без диффа |
| T13 | Документ собирается | ✅ | `make -C book build` |

## Результаты по функциональности

| Функциональность | Статус | Замечание |
|---|---|---|
| `st_expr` (индексация, два печатника) | ✅ | носители `expression_subscript_chain` / `condition_subscript_chain` |
| `st_decl` (инициализатор) | ✅ | `flat_array_items` — плоский список |
| `st_type` (размерности) | ✅ | спуск останавливается на упакованном бит-векторе |
| `book/src/03-types` | ✅ | абзац о вложенном массиве |
| Цель `c` | — | вне объёма: `CC-015` |

## Выводы и дальнейшие шаги

- **Замер и правка не совпали по объёму.** Кандидат-замер называл одну
  ошибку (`iec2c` отвергает индексацию); прогон нашёл три, и самая дорогая —
  **молчаливая**: `iec2c` принимает вложенный инициализатор и теряет вторую
  строку. Поймала её потактовая сверка; ни один инструмент цели этого не
  видит.
- **Третий дефект нашла смежная проба** (`[[bit; 8]; 2]`), а не предмет фичи:
  уплощение типа шло сквозь упакованный скаляр, то есть правило 0078
  нарушалось в цели, которая его же и применяет строкой выше.
- **Кандидат:** вложенный массив у цели `c` — `CC-015` при том, что
  `uint8_t grid[2][2]` в C представим; правка шире (объявитель, обнуление
  0353, агрегат 0330/0343), и её закрепляет юнит-тест
  `test_unrepresentable_element_maps_to_cc_015`, прямо фиксирующий отказ.
- Исправлений (`docs/fixes/`) не потребовалось.
