# Отчёт о тестировании фичи 0364: Вложенный массив у цели c

> Фича: [../features/0364-c-nested-array.md](../features/0364-c-nested-array.md) · тест-план: [../tests/0364-c-nested-array.md](../tests/0364-c-nested-array.md) · анализ: [../analyze/0364-c-nested-array.md](../analyze/0364-c-nested-array.md)

## Резюме

**Пройдено, фича готова к закрытию.** Цели `c`/`c-hal` переводят вложенный
массив, вывод принимает `cc -Wall -Wextra -Werror`, а значения совпадают с
эталоном такт в такт.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Объявление | ✅ | `uint8_t grid[2][2]` (было: `CC-015`) |
| T2 | Чтение элемента | ✅ | `cc принял` |
| T3 | Запись элемента | ✅ | `cc принял` |
| T4 | Переменный индекс | ✅ | `cc принял` |
| T5 | Три измерения | ✅ | `cube[1][0][1]` |
| T6 | Массив бит-векторов | ✅ | `uint8_t rows[2]` — правило 0078 соблюдено |
| T7 | Без инициализатора | ✅ | четыре присваивания нулей по элементам (0353) |
| T8 | Присваивание агрегата в теле | ✅ | листья: `model->grid[0][0] = 1;` … |
| T9 | Сверка значений | ✅ | `picked = 4`, `sum = 16`, `copied = 12` — совпали |
| T10 | Ловит зеркальные размерности | ✅ | мутация → «порождённый C не компилируется» |
| T11 | Ловит потерю рекурсии (инициализация) | ✅ | то же |
| T12 | Ловит потерю рекурсии (тело) | ✅ | то же |
| T13 | Юнит-тесты | ✅ | 1091 зелёный; тест-«закрепитель» снят, заведены два новых |
| T14 | Вывод корпуса не изменился | ✅ | предкоммит: снапшоты и гейт детерминизма без диффа |
| T15 | Контрольный вход | ✅ | одномерный массив принимают все восемь целей |

## Результаты по функциональности

| Функциональность | Статус | Замечание |
|---|---|---|
| Объявитель типа | ✅ | размерности в цикле, остановка на бит-векторе |
| Инициализация переменной | ✅ | `emit_element_init` — рекурсия по виду типа |
| Присваивание агрегата в теле | ✅ | `emit_aggregate` — то же правило, другой печатник |
| Сверка значений | ✅ | фикстура 2 × 3 плюс агрегат в теле |

## Выводы и дальнейшие шаги

- **Дефектов было три, и каждый прятался за предыдущим.** Снятие отказа
  открыло невалидную инициализацию, её починка — невалидное присваивание в
  теле. Замер, снятый один раз, показал бы только первый: проба
  повторялась после каждой правки.
- **Первая редакция фикстуры была декоративной.** Квадратная матрица и
  отсутствие агрегата в теле означали, что две мутации из трёх её не роняют.
  Годная фикстура — со **сторонами разной длины** и обеими позициями записи.
- **Два кандидата у цели `sv`**, найденные контрольными прогонами этой фичи
  (к вложенности отношения не имеют, воспроизводятся и на одномерном
  массиве): сброс массива без инициализатора печатается `'0` (verilator:
  «CONST is not an unpacked array»); переменный индекс даёт `WIDTHTRUNC`
  («requires 1 bit index, not 8 bits») — гейт цели считает предупреждение
  ошибкой.
- Присваивание вложенного агрегата у `st`/`sv` остаётся отказом (`ST-011`,
  `SV-002`) — вынесено кандидатом.
- Исправлений (`docs/fixes/`) не потребовалось.
