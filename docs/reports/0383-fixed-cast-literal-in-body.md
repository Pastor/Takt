# Отчёт о тестировании фичи 0383: Приведение дробного литерала к `q(m, n)` в теле

> Фича: [../features/0383-fixed-cast-literal-in-body.md](../features/0383-fixed-cast-literal-in-body.md) · тест-план: [../tests/0383-fixed-cast-literal-in-body.md](../tests/0383-fixed-cast-literal-in-body.md) · анализ: [../analyze/0383-fixed-cast-literal-in-body.md](../analyze/0383-fixed-cast-literal-in-body.md)

## Резюме

**Пройдено, фича готова к закрытию.** Одиннадцать проверок тест-плана
выполнены, обе мутации ловятся (после усиления фикстуры — см. «Выводы»),
`./scripts/precheck.sh` зелёный (код 0), вывод корпуса не изменился.

## Окружение

- macOS 25.5, `rustc` 1.97.1 (пин `rust-toolchain.toml`).
- Арбитры: Apple `cc`, `iec2c` (MatIEC) + сборка порождённого C,
  `rustc` + `clippy-driver -D warnings`, `verilator --lint-only -Wall`,
  `yosys synth`.
- Прогоны: `cargo test --all-features`, `scripts/probe.sh`, `precheck.sh`.

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Присваивание приведения | ✅ | `model->gain = (int16_t)640;` вместо `floor((2.5) * 256.0)`; трасса совпала |
| T2 | Приведение в арифметике | ✅ | `mul_v` = 2, 4, 8, 16 у эталона и цели `c` |
| T3 | Отрицательный литерал | ✅ | представление `−384` (floor к −∞); `neg_v` = 0, 2, 6, 14 |
| T4 | Нет рантайм-`floor` | ✅ | `literal_cast_needs_no_runtime_floor` |
| T5 | Пути согласованы | ✅ | `eq_v = 1` на каждом такте: тело и объявление дают одно представление |
| T6 | Инструменты целей | ✅ | все восемь целей переводят; `cc`, `iec2c` + `cc`, `rustc` + `clippy`, `verilator` + `yosys` приняли (до правки пять целей отказывали) |
| T7 | Знание одно | ✅ | счёт у `const_eval::fixed_repr`; цели зовут общую обёртку |
| T8 | Контроль без приведения | ✅ | согласие девяти потребителей |
| T9 | Граница `SE-059` | ✅ | текст по-прежнему называет `float` |
| T10 | Корпус не изменился | ✅ | `git diff --stat examples/generated/` пуст |
| T11 | Мутация ловится | ✅ | «носитель → `None`» и «искажение представления» роняют сверку |

## Результаты по функциональности

| Функциональность | Итог | Чем подтверждено |
|---|---|---|
| эталон | ✅ | трасса не менялась (правка в целях) |
| `c`, `c-hal` | ✅ | константа вместо `floor`; `cc` принял; сверка значений |
| `st`, `st-at` | ✅ | `iec2c` принял, порождённый C собран; прежде `ST-011` |
| `rust` | ✅ | `rustc` + `clippy -D warnings`; прежде `RS-011` |
| `sv`, `sv-mmio` | ✅ | `verilator` + `yosys`; прежде `SV-002` |
| `plantuml` | ✅ | принимала и прежде |

## Найденные дефекты

Фиксов (`docs/fixes/0383-YY-*`) не потребовалось. Два наблюдения устранены в
рамках задачи 0383-01 (см. ниже).

## Выводы и дальнейшие шаги

**1. Правка целей, а не семантики — решение по замеру.** Напрашивающаяся
свёртка узла в дереве ломает законный вход: проверки идут после понижения, и
`q * Number` ловит `SE-059` — то есть запись `gain * (2.0 as q(8, 8))`, которой
автор смешение **снимает**, стала бы незаконной. Проверено прогоном
(`gain * 512` отвергается именно так).

**2. Правка обнажила соседний класс у цели `rust`.** Литерал в q-арифметике до
фичи оказаться не мог, а с фичей `512 as i128` — это `clippy::unnecessary_cast`,
отказ гейта самой цели. Приведение операнда печатается теперь по нужде (класс
0263/0361). Нашла это **проба с прогоном инструментов**, а не тесты.

**3. Первая редакция фикстуры была недостаточна.** Мутация «искажение
представления на единицу» её **не роняла**: наблюдаемые `as u8` грубы —
641/256 и 640/256 дают одно и то же целое. Фикстура усилена кросс-проверкой
путей: `eq_v` сравнивает значение литерала, посчитанное в **теле**, со
значением того же литерала из **объявления** (свёртка 0061/0317). После этого
мутация ловится. Урок общий: наблюдаемая величина должна быть чувствительна к
проверяемому свойству, иначе сверка декоративна.

**4. Единичное падение чужого теста при полном прогоне.** В одном из прогонов
`cargo test --all-features` упал
`conformance_sv_tests::per_tick_trace_matches_generated_sv` (сборка тестбенча
verilator); одиночный прогон и два последующих полных — зелёные, включая
финальный перед коммитом. К предмету фичи отношения не имеет (цель `sv` в
фикстуре не участвует), но зафиксировано честно: это флейк параллельного
прогона, а не доказанная стабильность.

**5. Кандидаты.** Закрытие снимает кандидата «Приведение `<дробный литерал> as
q(m, n)` в теле переводит только `c`». Заводится новый: приведение **имени
константы** (`const K := 2.5; … K as q(8, 8)`) остаётся вне правила — значение
известно, но вопрос принадлежит константному вычислителю, а не печатнику.

**Вердикт: ГОТОВО.**
