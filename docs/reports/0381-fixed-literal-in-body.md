# Отчёт о тестировании фичи 0381: Понижение q-литерала в телах и условиях

> Фича: [../features/0381-fixed-literal-in-body.md](../features/0381-fixed-literal-in-body.md) · тест-план: [../tests/0381-fixed-literal-in-body.md](../tests/0381-fixed-literal-in-body.md) · анализ: [../analyze/0381-fixed-literal-in-body.md](../analyze/0381-fixed-literal-in-body.md)

## Резюме

**Пройдено, фича готова к закрытию.** Прогон — 3379 тестов, 0 провалов;
`precheck.sh` зелёный; вывод корпуса не изменился.

Замер после правки: эталон и **все восемь** целей дают один результат,
инструменты вывод принимают (`cc`, `iec2c` + `cc`, `rustc` + `clippy`,
`verilator` + `yosys`).

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Четыре позиции приёмника | ✅ | `fixed_literal_in_body_matches_generated_c` |
| T2 | Литерал ребра понижен | ✅ | `edge_condition_literal_is_lowered`: `> 25600` |
| T3 | Диагностика не ухудшилась | ✅ | `SE-059` называет `float` |
| T4 | Эталон исполняет сравнение | ✅ | трассы без `SIM-005` |
| T5 | Восемь целей переводят | ✅ | проба: все инструменты приняли |
| T6 | Вывод корпуса | ✅ | `git diff examples/generated/` пуст |
| M1 | Мутация: проход отключён | ✅ | упали T1 и T2 |

## Результаты по функциональности

| Функциональность | Итог |
|---|---|
| семантика | ✅ литерал понижается во всех позициях приёмника |
| эталон | ✅ сравнение `q` с целым исполняется |
| цели | ✅ значения совпали; `st` и `sv` перестали отвергать вход |

## Выводы и дальнейшие шаги

1. **Класс был Tier 1:** цель `c` печатала валидный C с **другим** значением,
   а на ребре — другой автомат. Компиляция этого не видит.
2. **Объём определил замер, а не намерение:** первая редакция понижала литерал
   и в арифметике — и ухудшала текст `SE-059`, называя тип, которого автор не
   писал. Арифметику исключил замер.
3. **Эталон пришлось расширить**, и узко: сравнение — да, арифметика — нет,
   потому что там вход отсекает `SE-059`, а «эталон умнее компилятора» — класс
   0034.
4. **Граница названа:** поле структуры q-типа в теле (`p.kp := 1.5;`) под
   правило не подпадает.
5. Исправлений не потребовалось.
