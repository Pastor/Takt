# Отчёт о тестировании фичи 0228: Предупреждение taktc compile несёт позицию

> Фича: [../features/0228-compile-warning-position.md](../features/0228-compile-warning-position.md) · тест-план: [../tests/0228-compile-warning-position.md](../tests/0228-compile-warning-position.md) · анализ: [../analyze/0228-compile-warning-position.md](../analyze/0228-compile-warning-position.md)

## Резюме

**Пройдено; фича закрыта.** Все 8 проверок тест-плана зелены, 6 критериев приёмки
ADR выполнены, `./scripts/precheck.sh` — код возврата 0. Исправлений
(`docs/fixes/0228-YY-*`) не потребовалось.

Наблюдаемая разница до и после (цель `c`, неиспользуемая переменная):

```text
до:    Предупреждение [SE-036]: переменная 'unused_var' объявлена, но нигде не используется
после: probe.takt:2:5: Предупреждение [SE-036]: переменная 'unused_var' объявлена, но нигде не используется
```

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | предупреждение своего файла | ✅ | путь, строка 2, колонка 5 |
| T2 | цель `c-hal` | ✅ | `SE-090` с позицией; копия формата устранена |
| T3 | импортированный файл | ✅ | префикс называет `lib.takt` |
| T4 | форма префикса | ✅ | у предупреждения и у ошибки одна форма |
| T5 | `--quiet` на HAL-ветви | ✅ | stderr пуст |
| T6 | фича 0081 не задета | ✅ | 4/4 |
| T7 | порождённый код не задет | ✅ | предкоммит зелён, снапшоты не двинулись |
| T8 | мутации | ✅ | три мутации, каждая валит ожидаемый тест |

### Мутации

| Мутация | Упало | Ожидалось |
|---|---|---|
| штамповать путь входа вместо `path_of` (Option B) | `imported_warning_names_the_library_file` | да |
| вернуть литеральную копию формата в `report_hal_result` | `hal_target_warning_carries_position` | да |
| потерять учёт `--quiet` | `quiet_suppresses_hal_warnings` | да |

⚠️ Первая мутация — проверка **границы решения**: «префикс появился» и «префикс
верен» суть разные утверждения, и тесты их различают.

## Результаты по функциональности

| Функциональность | Статус | Чем подтверждено |
|---|---|---|
| `compile`, цель `c` и прочие простые | ✅ | T1, T4 |
| `compile`, HAL-цели | ✅ | T2, T5 (печать общая на три цели) |
| `taktc fmt`, LSP, `takt-sim` | — | не задеты |
| Порождённый код, снапшоты | ✅ | T7 |

## Выводы и дальнейшие шаги

**Названное ограничение.** В ветви HAL-целей реестр содержит только корневой файл:
цель строит своё дерево внутри и таблицу наружу не отдаёт, поэтому предупреждение
импортированного файла там остаётся без префикса. Расширение требует смены
сигнатур `compile_to_c_hal`/`compile_to_st_at`/`compile_to_sv_mmio` — отдельная
фича, если случай встретится на практике (в корпусе таких предупреждений нет).

**Наблюдение о документе.** Второй раз подряд документ оказался впереди
инструмента: раздел «Диагностики» уже объявлял формат единым
(`путь:строка:колонка: <вид> [<код>]: <текст>`), и для предупреждений `compile`
это было неверно. Документ, написанный про **свойство**, а не про реализацию,
работает сторожем — расхождение видно при чтении.

**Оба кандидата, найденных при закрытии 0226, закрыты** (второй — фичей
[0227](../features/0227-lsp-style-warning-with-errors.md)).
