# Отчёт о тестировании 0359: Сравнение знакового с беззнаковым

- **Фича:** [../features/0359-mixed-sign-comparison.md](../features/0359-mixed-sign-comparison.md)
- **Тест-план:** [../tests/0359-mixed-sign-comparison.md](../tests/0359-mixed-sign-comparison.md)
- **Дата:** 2026-08-21

## Окружение

toolchain `1.97.1`; `cc`, `iec2c`, `clippy-driver`, `verilator`, `yosys`,
`typst`. Прогоны: `cargo test` (13 наборов), целевые наборы, полный
`./scripts/precheck.sh`.

## Сверка с тест-планом

| № | Условие | Результат |
|---|---|---|
| П1 | расширение у `rust`, `st`, `sv` | ✅ |
| П2 | раскрытие проверкой знака у четырёх целей | ✅ |
| П3 | `cc -Werror`, `clippy -D warnings` | ✅ |
| К1 | `c` на 8 битах не тронут | ✅ |
| К2 | сравнение с литералом не тронуто | ✅ |
| С1 | трасса эталона = трасса цели `c` | ✅ `[(1,2), (1,2), (2,2), (2,1), (2,1)]` |
| — | значения `rust` (оба входа) | ✅ `1`, как эталон |

Итого: 4 юнит-теста носителя, 6 тестов эмиссии и инструментов, 1 сверка.

## Найденные дефекты

Фиксов не потребовалось; три поправки **в ходе** работы:

1. **Печатников у цели по два.** Первая правка тронула только условия, и
   `if s < u { … }` в теле остался прежним — вывод не изменился. Видно было
   прогоном, а не чтением.
2. **Приведение литерала.** `n >= 3` дало `3 as i32` —
   `clippy::unnecessary_cast`, то есть **отказ** гейта цели `rust`. Поймал
   чужой тест `rust_default_impl_tests::generated_module_passes_clippy_gate`.
   Тип операнда сузили до именованного значения, знание вынесли в носитель.
3. **Слабая фикстура сверки.** Первая редакция брала `u = 200`, и ответ был
   истинным на всех тактах: сверка была бы зелена при сломанном правиле. Порог
   сдвинут так, что ответ меняется дважды.

## Мутационная проверка

| Мутация | Кто упал | Что доказано |
|---|---|---|
| `plan` всегда `AsIs` (правило отключено) | сверка С1: цель `c` дала `(1,1)` на первом такте | сверка видит именно правило, а не факт сборки |

## Редакторский слой (правило 29)

**Правок не потребовалось, ответ зафиксирован:** лексика, синтаксис, семантика
и набор диагностик не затронуты.

## Документ (правило 24)

Раздел `book/src/05-expressions/` получил правило сравнения операндов разной
знаковости — прежде документ о нём молчал, хотя расхождение затрагивало
**пять** потребителей.

## Вердикт

**ГОТОВО.** Условия тест-плана выполнены, сторож проверен мутацией, предкоммит
зелёный.
