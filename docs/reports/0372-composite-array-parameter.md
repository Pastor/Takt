# Отчёт о тестировании фичи 0372: Составной элемент массива в параметре функции

> Фича: [../features/0372-composite-array-parameter.md](../features/0372-composite-array-parameter.md) · тест-план: [../tests/0372-composite-array-parameter.md](../tests/0372-composite-array-parameter.md) · анализ: [../analyze/0372-composite-array-parameter.md](../analyze/0372-composite-array-parameter.md)

## Резюме

**Пройдено, фича готова к закрытию.** Все проверки тест-плана зелёные, три
мутации проверены и роняют ровно тех сторожей, ради которых заведены. Полный
прогон — 3399 тестов, 0 провалов; `precheck.sh` зелёный; вывод корпуса не
изменился.

Замер после правки (`scripts/probe.sh`, те же три входа, что и до неё):

| Вход | было | стало |
|---|---|---|
| `[Cell; 2]` в параметре | `sv`: yosys ERROR | `verilator принял · yosys синтезировал` |
| `[Mode; 2]` в параметре | `sv`: yosys ERROR | `verilator принял · yosys синтезировал` |
| `[[u8; 2]; 2]` в параметре | `sv`: yosys ERROR; `st`: `iec2c` ОТВЕРГ | `sv` синтезирован, `st`: `iec2c` принял · `cc` собрал |

## Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Значения цели `sv` совпадают с эталоном | ✅ | `composite_parameter_values_match_generated_sv`: трассы совпали такт в такт |
| T2 | Модуль синтезируется | ✅ | `composite_parameter_is_synthesizable`: `yosys synth` код 0 |
| T3 | `st`: `iec2c` принял, значения совпали | ✅ | `composite_parameter_st_trace_matches_reference` |
| T4 | Массив скаляров не изменился | ✅ | `flat_width_is_size_times_element_width`: ширина 24, части `[0]`, `[1]`, `[2]` |
| T5 | Элемент-структура — одна часть | ✅ | `struct_element_stays_one_part`: ширина 32, части по 16 |
| T6 | Вложенный массив — до элементов внутреннего | ✅ | `nested_array_is_split_down_to_inner_elements` |
| T7 | Ширина перечисления у `enum_width` | ✅ | `enum_element_width_comes_from_enum_facts`; неизвестное перечисление → раскладки нет |
| T8 | Имя формы следует объявлению | ✅ | `nested_array_form_name_follows_the_declaration` |
| T9 | Контроль: одномерная форма прежняя | ✅ | там же: `TAKT_ARR_2_USINT`, `TAKT_ARR_2_CELL` |
| T10 | Контроль: бит-вектор не размерность | ✅ | `bit_vector_element_is_not_a_dimension_in_the_form_name` |
| T11 | Вывод корпуса не изменился | ✅ | `git diff examples/generated/` пуст, гейт детерминизма зелён |
| M1 | Мутация: прямой порядок конкатенации | ✅ | упал T1 (значения зеркальны, инструменты молчат) |
| M2 | Мутация: спуск по полям структуры | ✅ | упал T2: «Latch inferred for signal `…a[0].lo`» |
| M3 | Мутация: имя по тексту типа элемента | ✅ | упали T3 и T8 |

## Результаты по функциональности

| Функциональность | Итог |
|---|---|
| цель `sv`/`sv-mmio` | ✅ три вида элемента переводятся и синтезируются |
| цель `st`/`st-at` | ✅ вложенный массив проходит `iec2c` → `cc`, значения верны |
| цели `c`/`c-hal`, `rust`, `plantuml` | — не затронуты (те же входы переводились и до правки) |
| эталон (`takt-sim`) | — не затронут; служит арбитром значений |

## Выводы и дальнейшие шаги

1. **Класс был шире кандидата.** Кандидат 0369 называл структуру; замер нашёл
   **три** вида элемента и **вторую** цель — вложенный массив ломал ещё и `st`,
   где негоден оказался лишь **идентификатор**, а объявление было верным.
2. **Первая редакция правки нашла второй слой.** Раскладка по полям структуры
   снимала отказ порта и порождала «Latch inferred» — тот же класс, что 0367.
   Форма выбрана прогоном **обоих** инструментов, а не рассуждением.
3. **Приведение перечисления обнаружил verilator, не yosys.** Присваивание
   `a[0] = a_flat[1:0];` без приведения — ошибка `ENUMVALUE`: половину картины
   даёт каждый инструмент, и оба обязательны (урок 0045).
4. **Корпус класс не покрывает:** функций с параметром-массивом составного типа
   в `examples/` нет ни одной, поэтому сторожа только фикстурные.
5. Исправлений (`docs/fixes/0372-YY-*`) не потребовалось.
