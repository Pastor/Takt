# Тест-план фичи 0187: Пересмотр задания адресов и доступа к портам

> Фича: [../features/0187-port-io-redesign.md](../features/0187-port-io-redesign.md) · ADR: [../adr/0187-port-io-redesign.md](../adr/0187-port-io-redesign.md) · анализ: [../analyze/0187-port-io-redesign.md](../analyze/0187-port-io-redesign.md)

## Область и цель

Фича меняет **синтаксис** объявления порта (`at` вместо `:=` для размещения),
**семантику** (`:=` — начальное значение выходного порта) и **правило языка**
(присваивание — оператор, ось 4). Правило 16 требует для таких фич проверок
корректности самого языка и компилятора, а не только «собралось».

Три класса проверок:

1. **Синтаксис** — новая форма разбирается, старая перестала означать адрес,
   объявление без `at` осталось законным (адрес приходит извне).
2. **Поведение** — начальное значение выставляется **до первого такта** и
   одинаково у эталона и целей; проверяется трассой, а не фактом компиляции.
3. **Отказ** — незаконные формы отвергаются семантикой с внятным кодом, а не
   чужим инструментом на порождённом файле.

## Проверки (условие → ожидаемый результат)

| # | Проверка | Вход | Ожидаемо | R/A |
|---|---|---|---|---|
| T1 | размещение ключевым словом | `in btn: bit at 0x40000000:0;` | адрес разрешён | R1 / A1 |
| T2 | старая форма адреса не размещает | `out led: bit := 0x40000004;` | адреса нет (значение) | R2 / A2 |
| T3 | объявление **без** `at` законно | `in temp: u8;` + `address temp = …;` | адрес разрешён | R1a / A2a |
| T4 | полнота адреса — после источников | порт без `at`, оператора и карты | `SE-052` | R1b / A2b |
| T5 | форматтер печатает новую форму | корпус | `fmt --check` зелёный, идемпотентно | R3 / A3 |
| T6 | редакторский слой знает `at` | сервер + плагин | `keyword`, автодополнение, `TaktKeywordSyncTest` | R4 / A4 |
| T7 | начальное значение в `c`/`c-hal`/`rust` | `out ready: bit at … := 1;` | запись в `_init` / `new()`+`init()` | R5 / A5 |
| T8 | начальное значение в `sv`/`sv-mmio` | то же | `ready <= 1;` в ветви сброса | R5 / A5 |
| T9 | начальное значение в `st`/`st-at` | то же | инициализатор `VAR_OUTPUT` / `VAR_GLOBAL` | R5 / A6 |
| T10 | вход с начальным значением | `in btn: bit at … := 1;` | `SE-092` | R6 / A7 |
| T11 | невычислимое значение | `out level: u8 at … := n;` | `SE-094` с названной причиной | R5 |
| T12 | порт **без** `:=` | `out ready: bit at …;` | записи нет, форма вывода прежняя | R8 |
| T13 | эталон стартует порт значением | фикстура сверки | трасса `[7, 11, 12, 13, …]` | R7 / A8 |
| T14 | сверка с целью `c` | та же фикстура | трассы совпадают, включая нулевой элемент | R7 / A8 |
| T15 | сверка с целью `sv` | та же фикстура | то же (наблюдение — порт модуля) | R7 / A8 |
| T16 | значение доезжает до исполненного ST | та же фикстура | `7` до первого вызова тела | R7 |
| T17 | запись внутри выражения | `seen := (value := 3) + 1;` | `SE-095` | R9 |
| T18 | законные позиции записи | оператор тела, шаг `for` | молчат | R9 |
| T19 | формы доступа целей | фикстура чтения/записи | совпадают с таблицей контракта | R10 / A9 |
| T20 | контракт падает при отклонении | мутация судьи/цели | тест краснеет | R11 |
| T21 | корпус и документ мигрированы | `./scripts/precheck.sh` | зелёный | R12 / A10 |

## Разбивка проверок по функциональности

| Функциональность | Проверки | Статус |
|---|---|---|
| Лексер, грамматика, АСД | T1, T2, T3 | ✅ |
| Слой адресов (приоритет источников) | T3, T4 | ✅ |
| Семантика (значение, отказы) | T10, T11, T12, T17, T18 | ✅ |
| Цели `c`, `c-hal`, `rust` | T7, T19 | ✅ |
| Цели `sv`, `sv-mmio` | T8, T19 | ✅ |
| Цели `st`, `st-at` | T9, T16, T19 | ✅ |
| Симулятор (эталон) | T13 | ✅ |
| Потактовая сверка эталон ↔ цели | T14, T15 | ✅ |
| Форматтер | T5 | ✅ |
| Редакторский слой (LSP, IntelliJ, Zed) | T6 | ✅ |
| Корпус и документ | T21 | ✅ |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

## Тестовые данные и окружение

- `takt-lang/tests/port_at_syntax_tests.rs` — T1–T5.
- `takt-lang/tests/port_init_tests.rs` — T2, T10, T12.
- `takt-lang/tests/port_initial_value_tests.rs` — T7, T11 (+ гейты `cc`, `clippy`).
- `takt-lang/tests/port_initial_value_hdl_tests.rs` — T8, T9, T12 (+ гейты
  `verilator`, `yosys`, `iec2c`).
- `takt-sim/tests/conformance_port_init_tests.rs` — T13, T14, T15, T16.
- `takt-lang/tests/port_access_contract_tests.rs` — T17, T18, T19, T20.
- `takt-lang/tests/lsp_port_io_tests.rs` + `TaktKeywordSyncTest` (Gradle) — T6.
- `./scripts/precheck.sh` — T5, T21.

Окружение: macOS (arm64), толчейн пина `1.97.1`, `cc` (clang), `verilator
5.050`, `yosys`, `iec2c` (MatIEC), JDK 17 для плагина.

## Примеры и контрпримеры (правило 16)

**Принимается:**

```takt
in  btn: bit at 0x40000000:0;          // размещение
out led: bit at 0x40000004:2 := 1;     // размещение + начальное значение
out ready: bit := 0;                   // только значение: адрес придёт извне
in  temp: u8;                          // ни того, ни другого
address temp = 0x40000002;             // …например, оператором
```

**Отвергается:**

```takt
in  btn: bit at 0x40000000:0 := 1;     // SE-092: у входа значения не бывает
out lvl: u8  at 0x40000008   := n;     // SE-094: значение неизвестно при сборке
```

```takt
start S { always { seen := (value := 3) + 1; } }   // SE-095: запись — оператор
```

⚠️ **Контрпримеры-регрессы обязательны** и включены в сторожа: порт **без**
`:=` не должен получать записи (иначе «починка», пишущая ноль всякому выходу,
прошла бы проверку и изменила поведение корпуса), а законные позиции записи —
молчать (иначе запрет присваивания сломал бы весь корпус).
