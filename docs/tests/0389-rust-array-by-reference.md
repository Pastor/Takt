# Тест-план фичи 0389: Массив в параметре функции цели `rust` — по ссылке

> Фича: [../features/0389-rust-array-by-reference.md](../features/0389-rust-array-by-reference.md) · ADR: [../adr/0389-rust-array-by-reference.md](../adr/0389-rust-array-by-reference.md) · анализ: [../analyze/0389-rust-array-by-reference.md](../analyze/0389-rust-array-by-reference.md)

## Область и цель

Проверяется, что порождённый Rust с массивом в параметре компилируется и
проходит линт, что форма согласована у сигнатуры и аргумента и что прочие цели
и корпус не затронуты.

## Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | Компиляция вывода | `fn total(a: [u8; 4])` | `rustc` и `clippy -D warnings` приняли | R1 / A1 |
| T2 | Форма сигнатуры | тот же вход | `fn total(a: &[u8; 4])` | R1 / A1 |
| T3 | Форма аргумента | тот же вход | `total(&self.data)` | R2 / A1 |
| T4 | Признак один | чтение | оба печатника зовут `rust_byref` | R2 / A2 |
| T5 | Бит-вектор | `[bit;8]` в параметре | по значению (скаляр, 0078) | R3 / A3 |
| T6 | Прочие цели | та же проба | вывод не изменился | R4 / A4 |
| T7 | Корпус | пересборка | диффа нет | R5 / A4 |
| T8 | Тесты | `cargo test --all-features` | зелено | R4 / A4 |

## Разбивка проверок по функциональности

| Функциональность | T1 | T5 | T6 | Статус |
|---|---|---|---|---|
| цель `rust` | ✅ | ✅ | — | ✅ |
| цели `c`, `st`, `sv` | — | — | ✅ | ✅ (не затронуты) |
| эталон | — | — | ✅ | ✅ |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

## Тестовые данные и окружение

- Проба (вне репозитория): функция с параметром `[u8; 4]`, вызываемая из тела.
- Существующие сторожа: тесты бит-векторов (0262), гейт цели `rust` на корпусе,
  снапшоты `examples/generated/`.
- Окружение: `rustc` 1.97.1 + `clippy-driver`, macOS 25.5.
