# Тест-план фичи 0278: Мёртвая упаковка последовательной композиции

> Фича: [../features/0278-compact-implement-dead-branch.md](../features/0278-compact-implement-dead-branch.md) · ADR: [../adr/0278-compact-implement-dead-branch.md](../adr/0278-compact-implement-dead-branch.md) · анализ: [../analyze/0278-compact-implement-dead-branch.md](../analyze/0278-compact-implement-dead-branch.md)

## Область и цель

Фича изымает неисполняемый код и сужает видимость, чтобы `dead_code` заработал.
Наблюдаемое поведение инструментов не меняется — значит проверяется **два**
свойства: что снятое действительно снято (R1–R3) и что живое не задето (R4).

⚠️ **Своего греп-сторожа фича не заводит намеренно.** Сторожем здесь работает
компилятор: после сужения видимости `dead_code` называет мёртвое сам, а
греп-тест «`compact_implement` не вернулся» сторожил бы имя, а не правило.

## Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | Снятого нет в коде | — | `grep -rn "compact_implement\|X_Sequence"` по `takt-lang/src`, `takt-sim/src` и тестам обоих крейтов — пусто | R1 / A1 |
| T2 | Наружу видны только типы | — | `takt-sim` и интеграционные тесты собираются; из модуля экспортируются `Extend` и `ParameterArgument` | R2 / A2 |
| T3 | Компилятор молчит | — | `cargo clippy --all-targets --all-features` без предупреждений | R3 / A3 |
| T4 | Реализация в дереве плоская | `start Entry = M1 + M2;` и `start E = (M1 + M2) { }` | `Extend::Concatenation` из двух элементов (тесты `implement_add_composition_resolves`, `implement_parenthesized_add_resolves`) | R5 / A4 |
| T5 | Поведение `A + B` прежнее | пример `batch_cycle` и корпус | потактовые сверки sim ≡ C ≡ SV зелёные, гейт синтеза проходит, харнесс исполняется | R4 / A4 |
| T6 | Регресс проекта | — | `./scripts/precheck.sh` — код 0 | — / A5 |

## Разбивка проверок по функциональности

| Функциональность | T1 | T2 | T3 | T4 | T5 | T6 |
|---|---|---|---|---|---|---|
| `takt-lang` — семантика | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `takt-sim` — эталон | — | ✅ | ✅ | — | ✅ | ✅ |
| Цели генерации | — | — | — | — | ✅ | ✅ |
| LSP / плагины | — | — | — | — | — | ✅ |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

## Тестовые данные и окружение

Новых фикстур фича не заводит: T4 опирается на существующие тесты
`takt-lang/tests/semantic/semantic_tests.rs`, T5 — на пример
`examples/batch_cycle.takt` и его трёх свидетелей (тестбенч SV, контракт
сценария, харнесс `batch_cycle_main.c`), заведённых фичей 0166.
