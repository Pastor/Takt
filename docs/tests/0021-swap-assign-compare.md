# Тест-план фичи 0021: `:=` присваивание, `=` сравнение

> Фича: [../features/0021-swap-assign-compare.md](../features/0021-swap-assign-compare.md) · ADR: [../adr/0021-swap-assign-compare.md](../adr/0021-swap-assign-compare.md) · анализ: [../analyze/0021-swap-assign-compare.md](../analyze/0021-swap-assign-compare.md)

## Область и цель

Проверить корректность языка и компилятора после смены операторов (Option B):
`:=` — присваивание, `=` — сравнение, `<=` — «меньше-или-равно» (сохранён),
`==` — выведен. Покрываем требования R1–R6 и критерии A1–A6 из анализа.
Формируется **параллельно** разработке (правило 17); включает примеры и
контрпримеры (правило 16).

## Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | Присваивание `:=` | `var x; x := 5;` | AST `Expression::Assign`; C: `x = 5;` | R1 / A1 |
| T2 | Сравнение `=` в выражении | `var b := (a = 5);` | AST `Expression::Equal`; C: `a == 5` | R1 / A1 |
| T3 | Реляционный `<=` | `cond C = x <= 10;` | `Condition::LessEqual`; C: `x <= 10` | R2 / A2 |
| T4 | Равенство в условии `=` | `ref Next: BTN.0 = 1;` | `Condition::Equal` (без изменений) | R1 / A2 |
| T5 | `==` выведен | `var b := (a == 5);` | Ошибка парсера (неизвестный оператор `==`) | R3 / A5 |
| T6 | Нет LR(1)-конфликтов | сборка грамматики | `cargo build` без предупреждений LALRPOP | R6 / A5 |
| T7 | Регресс кодогена | мигрированный `.lam` | C/PlantUML совпадает байт-в-байт со старым | R4 / A4 |
| T8 | Мигратор `= → :=`, `== → =` | старый `.lam` | корректный новый `.lam`, семантика сохранена | R4 / A3 |
| T9 | Разграничение определений | `type T = int; enum E { A = 0 }` | `=` как определение имени принимается | R1 / A1 |
| T10 | Версия языка повышена | сборка | мажорная версия языка увеличена | R5 / A6 |
| T11 | Doctests и README | `cargo test` doctests | зелёные, синтаксис новый | R5 / A6 |

## Примеры и контрпримеры (правило 16)

**Примеры (valid):**
- `x := 0; y := x + 1;` — присваивания.
- `cond Ready = level = 5;` — равенство в условии.
- `cond Low = temp <= 20;` — реляционный `<=`.
- `var eq := (a = b);` — сравнение в выражении.

**Контрпримеры (invalid):**
- `x = 5;` в позиции присваивания значения — ошибка (нужно `:=`).
- `a == b` — ошибка (`==` выведен, использовать `=`).
- `x <= 5;` в позиции присваивания — трактуется как реляционное выражение, а не
  присваивание (нет «немого» присваивания через `<=`).

## Разбивка проверок по функциональности

Единые условия прогоняются против каждой задеваемой функциональности (правило 11):
лексер, парсер (`Expression`/`Condition`/`LoopCond`), семантика/type_inference,
C-генератор, PlantUML, LTL/Бюхи, LSP, симуляция. Статус фиксируется в отчёте.

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

## Тестовые данные и окружение

- Фикстуры: `grammar/tests/data/semantic/{valid,invalid}/` (мигрированные +
  новые для `:=`/`=`), примеры `examples/`, симуляции `examples/simulations/`.
- Прогон: `cargo test --features lsp -- --test-threads=1`,
  `./scripts/run_simulations.sh`, `./scripts/precheck.sh`.
