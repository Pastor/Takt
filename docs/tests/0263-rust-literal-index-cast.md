# Тест-план фичи 0263: Приведение индекса к `usize` по нужде

> Фича: [../features/0263-rust-literal-index-cast.md](../features/0263-rust-literal-index-cast.md) · ADR: [../adr/0263-rust-literal-index-cast.md](../adr/0263-rust-literal-index-cast.md) · анализ: [../analyze/0263-rust-literal-index-cast.md](../analyze/0263-rust-literal-index-cast.md)

## Область и цель

Проверяется **форма вывода** цели `rust` и его приёмка настоящим
`clippy -D warnings`. Требования R1…R5 и критерии A1…A4 — в
[анализе](../analyze/0263-rust-literal-index-cast.md).

## Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | R/A |
|---|---|---|---|---|
| T1 | Литерал без приведения, переменная — с ним | `arr[1] := 7; arr[i] := 9; seen := arr[1];` | `self.arr[1]`, `self.arr[self.i as usize]`; `[1 as usize]` в выводе отсутствует | R1, R2 / A1 |
| T2 | Печатник условий подчиняется правилу | индекс-литерал в условии ребра | `self.arr[2]` без приведения | R3 / A2 |
| T3 | Приёмка настоящим `clippy -D warnings` | оба входа | сборка успешна | R4 / A3 |
| T4 | Корпус и гейты | — | `examples/generated/` не изменились, `precheck.sh` зелён | R5 / A4 |

## Разбивка проверок по функциональности

| Функциональность | Статус | Чем проверено |
|---|---|---|
| Цель `rust` | ✅ | T1–T3 |
| Прочие цели, эталон | — | не затронуты: правка в печатниках цели `rust` |
| LSP и плагины (правило 29) | — | новых токенов, форм объявления, узлов АСД и диагностик нет |
| Документ `book/` (правило 24) | — | форма вывода цели, а не язык |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

## Тестовые данные и окружение

`takt-lang/tests/targets/rust_index_cast_tests.rs`; `clippy-driver` (Rust
1.97.1), macOS 15.5 (arm64). T3 деградирует мягко: без `clippy-driver` прогон
пропускается с сообщением, слой эмиссии проверяется всегда.
