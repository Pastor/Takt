# Тест-план фичи 0189: Анонимные порты

> Фича: [../features/0189-anonymous-ports.md](../features/0189-anonymous-ports.md) · ADR: [../adr/0189-anonymous-ports.md](../adr/0189-anonymous-ports.md) · анализ: [../analyze/0189-anonymous-ports.md](../analyze/0189-anonymous-ports.md)

## Область и цель

Проверяется форма `#АДРЕС` целиком: разбор (выражения и условия), свёртка в
тройку `{адрес, бит, ширина}`, диагностики (`SE-096`…`SE-099`, `SY-007`,
`SY-008`), поведение семи целей и эталона, а также **совпадение** трасс эталона
и цели `c-hal`.

Правило 16: фича меняет синтаксис и семантику, поэтому в плане обязательны
примеры **и контрпримеры** — они ниже в каждой строке таблицы (вход, который
обязан приниматься, и вход, который обязан отвергаться).

⚠️ Главный предмет проверки — не «компилируется», а **не расходится**: пробы
стадии 2 показали, что цель `c` теряла оператор молча при рапорте об успехе.
Поэтому у каждой цели проверяется **наличие диагностики**, а у пары
эталон ↔ `c-hal` — равенство трасс.

## Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | три формы разбираются в выражении | `#0x2000 as u32`, `#0x2004:3 as u8`, `#0x2008.4` | цель `c-hal` печатает разыменование нужной ширины и сдвиг | R1 / A1 |
| T2 | битовая форма разбирается в условии ребра | `ref Done: #0x2000.3;` | условие печатается чтением бита | R2 / A2 |
| T3 | ширина не задана | `n := #0x2000;` | `SE-097`, текст называет обе замены | R7 / A4 |
| T4 | поле выходит за слово | `#0x2000:60 as u8` | `SE-098` | R9 / A6 |
| T5 | позиция бита задана дважды | `#0x2000:3.4` | `SE-098` | R9 / A6 |
| T6 | обращение в инициализаторе | `var n: u8 := #0x2000 as u8;` | `SE-099` | — / A4 |
| T7 | запись предупреждает и называет выход | `#0x2000:0 as u8 := n + 1;` | `SE-096`, текст содержит «Объявите именованный порт» | R8 / A5 |
| T8 | чтение не предупреждает | `n := #0x2000 as u8;` | `SE-096` **не** выдаётся | R8 / A5 |
| T9 | голый адресный литерал в выражении | `n := 0x2000:0;` | `SY-008`, текст называет форму `#…` | R12 / A8 |
| T10 | позиция размещения не задета | `out led: bit at 0x2000:3;` + `address btn = 0x2004:1;` | `SY-008` не выдаётся | R12 / A8 |
| T11 | оператор без эффекта | `n + 1;` | `SY-007` | R11 / A7 |
| T12 | обособленное обращение | `#0x2000.4;` | `SY-007` | R11 / A7 |
| T13 | вызов остаётся оператором | `beep();` | `SY-007` **не** выдаётся | R11 / A7 |
| T14 | цель `c` отказывает с причиной | модель с `#` | `CC-021`, текст называет `c-hal` | R18 / A13 |
| T15 | цель `rust` отказывает с причиной | то же | отказ называет цели с картой памяти | R18 / A13 |
| T16 | цель `st` отказывает, `st-at` размещает | то же | `ST-018`; в выводе `st-at` — `AT_2000_0_8 AT %MB8192` и `VAR_EXTERNAL` | R16, R18 / A12, A13 |
| T17 | цель `sv` отказывает, `sv-mmio` заводит регистр | то же | `SV-017`; в выводе `sv-mmio` — `AT_2000_0_8_next` и чтение шиной | R17, R18 / A12, A13 |
| T18 | эталон исполняет ячейку | модель пишет `n + 1` и читает обратно | трасса `1, 2, 3, 4, 5, 6` | R14 / A10 |
| T19 | эталон ≡ цель `c-hal` | та же модель, `mmap` по адресу ячейки | трассы равны такт в такт | R19 / A14 |
| T20 | контракт доступа расширен | фикстура контракта | сторож проверяет форму у `c-hal`, `st-at`, `sv-mmio` | R20 / A15 |
| T21 | вывод `st-at` принимает `iec2c` | порождённый `.st` | код возврата 0 | R16 / A12 |
| T22 | вывод `sv-mmio` принимают `verilator` и `yosys` | порождённый `.sv` | оба принимают | R17 / A12 |
| T23 | вывод `c-hal` принимает `cc -Wall -Werror` | порождённый `.c` | компилируется | R15 / A11 |
| T24 | форматтер печатает формы обратно | корпус + фикстуры | `taktc fmt --check` зелёный | R4 / A9 |
| T25 | корпус не сломан ломающими правилами | `examples/*.takt` | все цели прежние, вывод не изменился | R13 / A17 |

## Разбивка проверок по функциональности

Единые условия и ожидаемые результаты прогоняются против каждой задеваемой
обратной функциональности (правило 11); фиксируется статус.

| Задеваемое | T1–T8 (формы, диагностики) | T9–T13 (ломающие) | T14–T17 (цели) | T18–T19 (эталон) | T21–T25 (гейты) |
|---|---|---|---|---|---|
| разбор выражений | ✅ | ✅ | — | — | ✅ |
| разбор условий | ✅ | — | — | — | ✅ |
| семантика (свёртка, тип) | ✅ | — | ✅ | ✅ | — |
| цель `c` / `c-hal` | — | — | ✅ | ✅ | ✅ |
| цели `st` / `st-at` | — | — | ✅ | — | ✅ |
| цели `sv` / `sv-mmio` | — | — | ✅ | — | ✅ |
| цель `rust` | — | — | ✅ | — | ✅ |
| симулятор | — | — | — | ✅ | ✅ |
| форматтер | — | — | — | — | ✅ |
| редакторский слой | — | — | — | — | ✅ |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

## Тестовые данные и окружение

- `takt-lang/tests/targets/anon_port_tests.rs` — T1–T17 (формы, диагностики, цели).
- `takt-lang/tests/semantic/port_access_contract_tests.rs::anon_access_matches_contract` — T20.
- `takt-sim/tests/conformance/conformance_anon_tests.rs` — T18, T19 (сверка через `mmap`
  по адресу `0x1000_0000_0000`; адрес тот же, что у сторожа 0098 — macOS
  отвергает низкие).
- `takt-lang/src/semantic/anon_port.rs` (модульные тесты) — свёртка форм,
  пределы, имя ячейки.
- `takt-sim/src/anon_cell.rs` (модульные тесты) — чтение/запись ячейки,
  умолчание, ограничение «разная ширина — разные ячейки».
- `takt-lang/src/generator/c/c_anon.rs` (модульные тесты) — форма доступа,
  знаковость, отсутствие лишнего чтения при записи целого слова.
- Гейты: `cc -Wall -Werror`, `iec2c`, `verilator --lint-only -Wall`,
  `yosys synth`, `taktc fmt --check`, `./scripts/precheck.sh`.
