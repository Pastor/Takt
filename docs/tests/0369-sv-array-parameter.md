# Тест-план фичи 0369: Массив в параметре функции у цели sv

> Фича: [../features/0369-sv-array-parameter.md](../features/0369-sv-array-parameter.md) · ADR: [../adr/0369-sv-array-parameter.md](../adr/0369-sv-array-parameter.md) · анализ: [../analyze/0369-sv-array-parameter.md](../analyze/0369-sv-array-parameter.md)

## Область и цель

Проверяется функция с параметром-массивом у целей `sv`/`sv-mmio`: линт, синтез
и **совпадение значений** с эталоном.

## Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | Сигнатура | `fn head(a: [u8; 3])` | `input logic [23:0] a_flat` | R1 / A1 |
| T2 | Пролог | то же | объявление `a [0:2]` и три среза | R2 / A1 |
| T3 | Вызов | `head(data)` | `head({data[2], data[1], data[0]})` | R3 / A1 |
| T4 | Линт | порождённый модуль | verilator принимает | R1 / A1 |
| T5 | Синтез | тот же модуль | yosys синтезирует | R6 / A3 |
| T6 | Сверка значений | фикстура `conformance_sv_array_param.takt` | трасса совпадает | R5 / A2 |
| T7 | Мутация «прямой порядок» | — | трассы разошлись | A4 |
| T8 | Мутация «распакованный параметр» | — | «не синтезируется» | A4 |
| T9 | Бит-вектор в параметре | `[bit; 8]` | вывод прежний | R4 / A5 |
| T10 | Контроль корпуса | `examples/*.takt` | ноль тревог | A5 |
| T11 | Регресс | conformance, targets | зелено | — |
| T12 | Вывод корпуса не изменился | предкоммит | снапшоты без диффа | A5 |

## Разбивка проверок по функциональности

| Функциональность | Статус | Замечание |
|---|---|---|
| `sv_array` (три носителя) | ✅ | юнит-тесты формы и ширины |
| `sv_fsm` (сигнатура и пролог) | ✅ | T1, T2, T5 |
| `sv_expr` (аргумент) | ✅ | T3, T6, T7 |
| Печатники индексации | — | не трогались: в этом смысл пролога |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

## Тестовые данные и окружение

- Фикстура: `takt-sim/tests/data/eval/conformance_sv_array_param.takt`.
- Проба: `arrparam.takt`; мини-пробы форм — в scratchpad.
- Инструменты: `verilator` 5.050, `yosys`.
