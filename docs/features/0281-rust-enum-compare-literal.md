# Фича 0281: Цель rust даёт невалидный код на сравнении перечисления с числом

- **Номер:** 0281
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии 3)
- **Tier:** 2 — rustc E0308 на сравнении перечисления с числом (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** генерация
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0281-rust-enum-compare-literal.md`](../adr/0281-rust-enum-compare-literal.md) |
| Анализ | [`docs/analyze/0281-rust-enum-compare-literal.md`](../analyze/0281-rust-enum-compare-literal.md) |
| Разработка | [`0281-01`](../development/0281-01-rust-enum-compare-literal.md) |
| Тест-план | [`docs/tests/0281-rust-enum-compare-literal.md`](../tests/0281-rust-enum-compare-literal.md) |
| Отчёт о тестировании | [`docs/reports/0281-rust-enum-compare-literal.md`](../reports/0281-rust-enum-compare-literal.md) |
| Исправления | [`0301-02`](../fixes/0301-02-probe-type-declaration.md) — попутный, в инструменте замера |

## Краткое описание

**Цель `rust` порождает невалидный код на сравнении перечислимой переменной с
числом.** Замер при закрытии
[0167](0167-c-enum-constants-usage.md) (2026-08-16): вход
`enum Command { Stop = 0, Go = 1 } … ref Run: c = 1;` даёт
`if self.c == 1 {`, и `rustc` отвергает это `E0308` («expected `Command`,
found integer») — присваивание при этом печатается верно (`self.c =
Command::Go`), то есть цель восстанавливает вариант **только** в одном из двух
мест. ⚠️ Гейт цели гоняет **только корпус**, а в корпусе такого сравнения нет —
ровно класс дефектов 0148-01/0148-02, доживших по той же причине. ⚠️ Цели `sv`
и `st` ту же запись переводят валидно (`== 1` там законно), так что предмет —
именно `rust`. Кандидат: восстанавливать вариант и в сравнении (место —
`rust_expr`/печатник условий), сторож — фикстурой, а не корпусом.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Определяется на стадии анализа.** Фича затрагивает язык или наблюдаемое
поведение целей, поэтому нужный раздел `book/` называется после проработки —
до неё утверждение о документе было бы догадкой.

## Итог (что сделано)

**Сравнение перечислимой переменной с числом печатается вариантом.** Цель
восстанавливала вариант при **присваивании** и не восстанавливала при
**сравнении**: `ref Done: c = 1;` давало `self.c == 1`, и `rustc` отвечал
`E0308` при нулевом коде возврата `taktc`. Ровно тот класс, что и фикс
[0148-01](../fixes/0148-01-rust-bit-compare.md), — два пути одной идеи
разошлись молча.

**Сделано:** восстановление варианта вынесено из `coerce_to` в
`rust_expr::enum_variant_literal` — единственный носитель; печатник условий
получил ветвь `enum_comparison` по образцу `boolean_comparison`. Литерал
распознаётся с обеих сторон, `==` и `!=` — одинаково, значение вне набора
вариантов даёт честный отказ.

**Сторож** — `takt-lang/tests/targets/rust_enum_compare_tests.rs`: шесть
тестов, включая контроль (сравнение с именем варианта) и прогон настоящего
`clippy-driver -D warnings`. ⚠️ Гейт цели класс не видел: он гоняет только
корпус, где такого сравнения нет.

**Попутно исправлен инструмент замера** — фикс
[0301-02](../fixes/0301-02-probe-type-declaration.md): `scripts/probe.sh` падал
под `set -eu` на **любой** пробе с объявленным типом (форма `[ … ] && printf` в
теле цикла отдавала статус 1, когда совпадения нет). Без этого замер по правилу
30 снять было нельзя; в сторож добавлена недостающая отрицательная ветвь,
мутация её валит.

**Версия языка не поднята** (форма вывода цели, не язык); крейт `takt-lang`:
`0.46.1` → `0.46.2`.

Детали — [отчёт](../reports/0281-rust-enum-compare-literal.md), решение —
[ADR](../adr/0281-rust-enum-compare-literal.md).
