# Фича 0261: Гейт размера модуля молчит до отказа: жёлтой зоны нет

- **Номер:** 0261
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-20)
- **Tier:** proc — гейт молчит до отказа — процесс разработки (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** прочее
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0261-module-size-warning-zone.md`](../adr/0261-module-size-warning-zone.md) |
| Анализ | [`docs/analyze/0261-module-size-warning-zone.md`](../analyze/0261-module-size-warning-zone.md) |
| Разработка | [`0261-01`](../development/0261-01-module-size-warning-zone.md) |
| Тест-план | [`docs/tests/0261-module-size-warning-zone.md`](../tests/0261-module-size-warning-zone.md) |
| Отчёт о тестировании | [`docs/reports/0261-module-size-warning-zone.md`](../reports/0261-module-size-warning-zone.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0261-YY-*`) |

## Краткое описание

**Гейт размера модуля молчит до самого отказа: «жёлтой зоны» нет.** Замер
[0225](0225-statement-module-size.md) (2026-08-17): при лимите
1000 строк в зоне ≥ 970 стоят **девять** файлов — `sv_fsm.rs` (998),
`conformance_st_tests.rs` (994), `sv_expr.rs` (994), `c_header.rs` (994),
`conformance_c_tests.rs` (981), `semantic_tests.rs` (976), `rust_model.rs`
(974), `conformance_sv_tests.rs` (973). `scripts/check-module-size.sh` о них
не говорит ничего: он отвечает «зелено» вплоть до превышения, и первым
сигналом становится отказ посреди чужой правки. Кандидат: печатать
предупреждение при приближении к лимиту (порог, шумность и влияние на код
возврата — предмет отдельного решения; Option D ADR 0225).

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Не требуется.** Правило размера модуля живёт в `docs/CODE.md`; `book/`
описывает язык.

## Итог (что сделано)

**Гейт размера модулей заговорил до отказа — и впервые проверяется мутацией.**

- **Жёлтая зона** `970…1000` строк: файлы печатаются списком с остатком запаса,
  **код возврата не меняется** (правило говорит «не больше ~1000», и гейт,
  роняющий прогон за 30 строк до предела, заставлял бы дробить модуль в чужой
  правке).
- **Порог выбран замером:** при 950 в зоне 15 файлов, при 900 — 23; при 970 —
  восемь. ⚠️ Ближайший к пределу — `generator/sv/sv_expr.rs`: **1000 строк,
  запас 0** (кандидат называл 998 — файл подрос).
- **Заведён сторож** `scripts/test-module-size.sh` (шаг предкоммита): мутацией
  на временном дереве (`MS_ROOT`) проверяются все пять условий отказа плюс два
  условия зоны — печатается **и** не роняет прогон; файл вне зоны в список не
  попадает. ⚠️ Самопроверки у гейта не было с фичи 0027, и кандидат этого не
  называл: «зелено» ничем не отличалось от «не смотрит».

Детали — в [отчёте](../reports/0261-module-size-warning-zone.md); решение и
отвергнутые варианты — в [ADR](../adr/0261-module-size-warning-zone.md).
