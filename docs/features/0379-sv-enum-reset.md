# Фича 0379: Сброс перечислимого регистра у цели sv

- **Номер:** 0379
- **Статус:** ГОТОВО
- **Зависит от:** нет (0060, 0338 закрыты)
- **Связанные issue (анализ):** замер-обход 2026-08-22 (порт перечислимого типа)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0379-sv-enum-reset.md`](../adr/0379-sv-enum-reset.md) |
| Анализ | [`docs/analyze/0379-sv-enum-reset.md`](../analyze/0379-sv-enum-reset.md) |
| Разработка | [`docs/development/0379-01-sv-enum-reset.md`](../development/0379-01-sv-enum-reset.md) |
| Тест-план | [`docs/tests/0379-sv-enum-reset.md`](../tests/0379-sv-enum-reset.md) |
| Отчёт о тестировании | [`docs/reports/0379-sv-enum-reset.md`](../reports/0379-sv-enum-reset.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

Замер 2026-08-22 на `var m: Mode;` (перечислимый тип **без инициализатора**) и
на `out mode: Mode at 0;`:

| Потребитель | Ответ |
|---|---|
| эталон | исполняет (значение 0) |
| `c`, `c-hal` | `cc` принял |
| `st`, `st-at` | `iec2c` принял · `cc` собрал |
| `rust` | `rustc` принял · `clippy` принял |
| `sv`, `sv-mmio` | **verilator ОТВЕРГ:** `%Error-ENUMVALUE: Implicit conversion to enum 'mode_e' from 'bit'` |

Цель печатала `enumnoinit_m <= '0;`: перечисления SystemVerilog строго
типизированы, и гейт цели считает предупреждение ошибкой — вывод не проходил
проверку своей же цели при **нулевом** коде возврата `taktc`.

## Документирование (правило 24)

**Не требуется** — язык не меняется: запись законна и исполняется эталоном;
правится форма значения сброса у одной цели.

## Итог (что сделано)

- Регистр перечислимого типа сбрасывается **своим** значением: мнемоникой
  варианта, если вариант со значением 0 есть, и приведением `mode_e'(0)`,
  если его нет.
- ⚠️ **Значение остаётся нулём — тем же, что даёт эталон.** Подстановка
  **первого** варианта (`MODE_IDLE = 1`) прошла бы линт и изменила бы
  поведение: мутация даёт `RTL=[[1, 0], …]` против `sim=[[0, 0], …]`.
- ⚠️ Формы две, и обе нужны: мнемоники для значения, которого нет среди
  вариантов, не существует, а приведение там, где мнемоника есть, было бы
  менее читаемым выводом.
- ⚠️ Класс жил на **умолчании**: запись с инициализатором (`var m: Mode :=
  Work;`) печаталась верно с фичи 0338 — расходились ветви «есть значение» и
  «значения нет».
- Вывод корпуса не изменился: перечислимых регистров без инициализатора в
  `examples/` нет.

Детали — в [отчёте](../reports/0379-sv-enum-reset.md).
