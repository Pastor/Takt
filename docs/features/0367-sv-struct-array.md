# Фича 0367: Массив структур у цели sv синтезируется

- **Номер:** 0367
- **Статус:** ГОТОВО
- **Зависит от:** нет (0293, 0309, 0365, 0366 закрыты)
- **Связанные issue (анализ):** кандидат, заведённый закрытием
  [0366](0366-nested-aggregate-carrier.md)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0367-sv-struct-array.md`](../adr/0367-sv-struct-array.md) |
| Анализ | [`docs/analyze/0367-sv-struct-array.md`](../analyze/0367-sv-struct-array.md) |
| Разработка | [`docs/development/0367-01-sv-struct-array.md`](../development/0367-01-sv-struct-array.md) |
| Тест-план | [`docs/tests/0367-sv-struct-array.md`](../tests/0367-sv-struct-array.md) |
| Отчёт о тестировании | [`docs/reports/0367-sv-struct-array.md`](../reports/0367-sv-struct-array.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

Замер 2026-08-21: `pts[0].x := 5;` при `pts: [Cell; 2]` исполняют эталон, `c`,
`st` и `rust`, а цель `sv` печатала вывод, который **verilator принимает, а
yosys отвергает**:

| Форма в выводе | Ответ yosys |
|---|---|
| сброс `pts <= '{{8'd1, 8'd2}, …};` | «Assignment pattern is only supported for whole unpacked array assignments» |
| после починки сброса — умолчание `pts_next = pts;` | «Latch inferred for signal `pts_next[0].x`» |

Всё при **нулевом** коде возврата `taktc`. Класс невидим сверке значений (её
тестбенч собирает verilator) и линту — только синтезу.

## Документирование (правило 24)

**Не требуется** — язык не меняется: запись законна и исполняется эталоном;
правится представление у одной цели.

## Итог (что сделано)

- Сброс и умолчание регистра, внутри которого лежит структура в массиве,
  печатаются **по полям**; массив скаляров не затронут (признак —
  `sv_array::needs_leafwise`).
- ⚠️ **Дефект двухслойный:** починка сброса открывает защёлку на умолчании —
  замер снимался дважды.
- ⚠️ **Класс видит один инструмент из трёх:** линт цели и потактовая сверка
  идут через verilator, поэтому у фичи отдельный тест синтеза (`yosys`).
- Временная граница фичи 0366 (`SV-002` на агрегате с полем внутри массива)
  снята.
- Печать перечислений состояний и шагов вынесена в модуль `sv_enums.rs`:
  `sv_fsm.rs` вышел за предел размера модуля.
- Вывод корпуса не изменился: массивов структур в `examples/` нет.

Детали — в [отчёте](../reports/0367-sv-struct-array.md).
