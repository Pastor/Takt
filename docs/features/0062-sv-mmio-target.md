# Фича 0062: Цель `sv-mmio`: адреса портов → регистровый файл на шине

- **Номер:** 0062
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (новая цель генерации)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0062-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0062-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0062-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0062-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0062-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0062-YY-*`) |

## Краткое описание

Карта адресов ([0020](0020-port-address-decl.md)) целью `sv` **не потребляется**:
MMIO-адрес для RTL бессмыслен — разыменование `*(volatile*)` в аппаратуре не
существует. Но осмысленна другая трактовка: породить **регистровый файл** на
APB/AXI-Lite/Wishbone, где адрес задаёт смещение регистра. Это **самостоятельная
цель**, а не флаг `sv`.

Вынесено закрытой [0045](0045-sv-backend.md) (ADR, вопрос 5, Option B).

> **Объединены два кандидата.** В `FEATURES.md` находка была записана **дважды** —
> «Цель `sv-mmio`: адреса портов → регистровый файл на шине» и «Цель `sv-mmio`» —
> с одним и тем же содержанием; при заведении сведены в одну фичу (решение
> заказчика 2026-07-17).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
