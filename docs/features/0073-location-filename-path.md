# Фича 0073: `Location::filename()` возвращает `"0"`, а не путь

- **Номер:** 0073
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (`parser/`, `diagnostics`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0073-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0073-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0073-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0073-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0073-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0073-YY-*`) |

## Краткое описание

Метод даёт `format!("{}", file_no)` — то есть **номер строкой**, тогда как имя
обещает **путь**; `parser_tests.rs:944` это закрепляет ассертом
`loc.filename() == "0"`.

После [0053](0053-diagnostics-file-id.md) путь доступен честно
(`Diagnostic::file`), поэтому метод — **ловушка для следующего читателя**.

Лечение: переименовать/удалить, поправив тесты.

Выявлено в [ADR 0053](../adr/0053-diagnostics-file-id.md) (A1).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
