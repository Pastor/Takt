# Фича 0264: Координата судей тела указывает на объявление, а не на использование

- **Номер:** 0264
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии 3)
- **Tier:** 3 — координата судьи указывает на чужое ВЕРНОЕ место — выглядит достоверной (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** семантика
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0264-body-judge-usage-position.md`](../adr/0264-body-judge-usage-position.md) |
| Анализ | [`docs/analyze/0264-body-judge-usage-position.md`](../analyze/0264-body-judge-usage-position.md) |
| Разработка | [`0264-01`](../development/0264-01-body-judge-usage-position.md) |
| Тест-план | [`docs/tests/0264-body-judge-usage-position.md`](../tests/0264-body-judge-usage-position.md) |
| Отчёт о тестировании | [`docs/reports/0264-body-judge-usage-position.md`](../reports/0264-body-judge-usage-position.md) |
| Исправления | не потребовались |

## Краткое описание

**Координата судей тела указывает на ОБЪЯВЛЕНИЕ, а не на использование.**
Замер [0249](0249-assign-to-call-place.md) (2026-08-18):
`ExpressionNode::loc()` у переменной и функции отдаёт позицию их
**объявления** — позиции вхождений живут отдельным слоем `semantic::usages`
(фича 0131) и в понижённом дереве тела отсутствуют. Поэтому `SE-111` на
`f(n) := 1;` в строке 9 указывает на строку 4, где объявлена `f`, а на
чистом литерале (`5 := 2;`) координаты нет вовсе: сообщение печатается без
префикса пути и позиции. Ограничение общее у судей тела — соседний `SE-095`
подставляет `Location::Codegen` (`validate/assignment_position.rs::target_of`).
Кандидат: дать судьям тела позицию использования — либо протащив её через
понижение, либо спросив слой `semantic::usages`. ⚠️ Класс уже назван правилом
0130 («в пачке сообщений координата `1:1` бесполезна»), и здесь он проявляется
не в `1:1`, а в **чужой верной** координате — это хуже: она выглядит
достоверной.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Определяется на стадии анализа.** Фича затрагивает язык или наблюдаемое
поведение целей, поэтому нужный раздел `book/` называется после проработки —
до неё утверждение о документе было бы догадкой.

## Итог (что сделано)

**Понижённый оператор-выражение несёт свою позицию.** У понижённого выражения
её нет: `ExpressionNode::loc()` выводит координату из объявлений операндов, а
позиции вхождений живут отдельным слоем `semantic::usages` (0131). Отсюда
`f(n) := 1;` в строке 10 указывал на строку 3, где объявлена `f`, — **чужая
верная** координата, выглядящая достоверной, — а `5 := 2;` не имел координаты
вовсе.

**Сделано:** `StatementNode::Expression(Box<ExpressionNode>, Location)`;
позиция берётся у `ast::Statement` при понижении и передаётся судьям тела
(`SE-111`, `SE-112`, `SE-095`), которые предпочитают её. Прежний вывод из
выражения остался запасным ходом. Правка затронула ≈40 мест сопоставления в
двух крейтах — механически, каждое указал компилятор.

**Сторож** — `takt-lang/tests/semantic/body_judge_position_tests.rs`: три
координаты и контроль на законной записи; координата вычисляется из смещения по
тексту пробы, а не задана офсетом. Мутация «игнорировать позицию оператора»
валит два теста.

⚠️ **Граница:** позицию получил **только** оператор-выражение; условие `if`,
шаг `for` и инициализатор локальной переменной опираются на прежний вывод — там
судьи тела диагностик не выдают.

**Частично разблокирована** [0277](0277-expression-usage-position.md): та же
причина, но потребитель другой — цели генерации.

**Версия языка не поднята**; крейт `takt-lang`: `0.46.5` → `0.47.0` (изменён
публичный вариант перечисления).

Детали — [отчёт](../reports/0264-body-judge-usage-position.md), решение —
[ADR](../adr/0264-body-judge-usage-position.md).
