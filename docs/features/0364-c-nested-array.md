# Фича 0364: Вложенный массив у цели c

- **Номер:** 0364
- **Статус:** ГОТОВО
- **Зависит от:** нет (0029, 0078, 0340, 0343, 0353, 0363 закрыты)
- **Связанные issue (анализ):** кандидат, заведённый закрытием
  [0363](0363-st-multidim-subscript.md)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0364-c-nested-array.md`](../adr/0364-c-nested-array.md) |
| Анализ | [`docs/analyze/0364-c-nested-array.md`](../analyze/0364-c-nested-array.md) |
| Разработка | [`docs/development/0364-01-c-nested-array.md`](../development/0364-01-c-nested-array.md) |
| Тест-план | [`docs/tests/0364-c-nested-array.md`](../tests/0364-c-nested-array.md) |
| Отчёт о тестировании | [`docs/reports/0364-c-nested-array.md`](../reports/0364-c-nested-array.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

Замер 2026-08-21: `var grid: [[u8; 2]; 2] := {{1, 2}, {3, 4}};` исполняют
эталон, `rust`, `sv` и (после 0363) `st`, а цели `c`/`c-hal` отвечали
**`CC-015`** «тип не представим в C» — при том что `uint8_t grid[2][2]` в C
выразим.

Отказ был **закреплён юнит-тестом** `test_unrepresentable_element_maps_to_cc_015`,
который прямо ссылался на пробу `nested.takt`, — то есть тест фиксировал
дефект (класс 0191).

Дефектов оказалось **три**, и каждый следующий открывался снятием
предыдущего:

| # | Место | Что печаталось | Ответ `cc` |
|---|---|---|---|
| 1 | объявление (`map_typed_variable`) | — (отказ `CC-015`) | — |
| 2 | инициализация переменной (`c_model_init`) | `model->grid[0] = {1, 2};` | «expected expression» |
| 3 | присваивание агрегата в теле (`c_expr/aggregate`) | то же | «expected expression» |

⚠️ Второй и третий — **разные печатники**, и правка одного оставляла другой:
класс тот же, что у пары печатников выражений и условий (урок 0359).

## Документирование (правило 24)

**Не требуется** — язык не меняется, и раздел `book/src/03-types` о вложенном
массиве написан фичей 0363; поведение цели `c` теперь совпадает с описанным
там.

## Итог (что сделано)

- Цели `c`/`c-hal` переводят вложенный массив: объявитель печатает
  `uint8_t grid[2][2]`, агрегат раскрывается **до листьев** и в инициализации
  переменной, и в присваивании в теле.
- ⚠️ **Дефектов было три, и каждый прятался за предыдущим** — замер пришлось
  повторять после каждой правки, а не снимать один раз.
- ⚠️ **Тест, закреплявший отказ, снят** (класс 0191); на его месте три новых,
  включая **границу**: массив широких бит-векторов (`[[bit; 128]; 2]`)
  остаётся `CC-015` — там отказ по существу, а не пробел печати.
- ⚠️ **Первая редакция фикстуры сверки была декоративной:** квадрат 2 × 2 без
  агрегата в теле не ловил две мутации из трёх. Годная — стороны 2 × 3 и обе
  позиции записи.
- Спуск по размерностям останавливается на `[bit;N≤64]` (правило 0078).
- Вывод корпуса не изменился: вложенных массивов в `examples/` нет.
- **Кандидаты, найденные контрольными прогонами:** у цели `sv` — сброс массива
  без инициализатора (`'0` на распакованном массиве) и переменный индекс
  (`WIDTHTRUNC`); у `st`/`sv` — присваивание вложенного агрегата.

Детали — в [отчёте](../reports/0364-c-nested-array.md).
