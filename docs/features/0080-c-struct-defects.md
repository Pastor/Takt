# Фича 0080: Дефекты генератора C по структурам

- **Номер:** 0080
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (генератор `generator/c/`, семантика)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0080-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0080-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0080-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0080-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0080-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0080-YY-*`) |

## Краткое описание

Три дефекта, каждый подтверждён пробой:

1. `var p: Point := {1,2};` → `model->p = {1, 2};` — **невалидный C**
   (`cc: error: expected expression`; нужен составной литерал `(Point){1,2}`);
2. `const c: Coord := {0,0,0};` (`extend_complex.lam`) **молча исчезает** из C —
   остаётся только `typedef`;
3. **существование поля не проверяется** — `p.NOSUCHFIELD` компилируется молча в
   невалидный `model->p.NOSUCHFIELD` (нужна диагностика в `grammar`).

Выявлено при [0034](0034-sim-struct-types.md), три пробы.

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
