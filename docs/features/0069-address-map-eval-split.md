# Фича 0069: Вынос вычислителя адреса из `address_map.rs` (лимит размера)

- **Номер:** 0069
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (`semantic/address_map.rs`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0069-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0069-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0069-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0069-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0069-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0069-YY-*`) |

## Краткое описание

Было 587 строк, стало **1054** без тестов (1116 с ними) — вычислитель выражений
адреса добавил ~460. Лимит ~1000 (`CLAUDE.md`) превышен.

Анализ [0042](0042-address-defines.md) это **предвидел** («при росте вычислитель
выделяется в `address_map/eval.rs`»), но оценивал прирост в 150–200 строк.

Лечение — ровно то, что и планировалось: вынести вычислитель (`eval_addr_expr`,
`apply_binary`, `resolve_symbol`, `AddressEnv`) в подмодуль.

Смежно с [0027](0027-module-size-split.md) (храповик размера) и с фичей
[0088](0088-module-size-remaining.md) (остальные нарушители лимита).

Выявлено при закрытой [0042](0042-address-defines.md).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
