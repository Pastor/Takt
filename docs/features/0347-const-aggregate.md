# Фича 0347: Константа структурного и массивного типа у целей `rust` и `sv`

- **Номер:** 0347
- **Статус:** ГОТОВО
- **Зависит от:** 0293, 0309, 0346 (закрыты; проставлено 2026-08-20)
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, заведённый
  фичей 0346

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0347-const-aggregate.md`](../adr/0347-const-aggregate.md) |
| Анализ | [`docs/analyze/0347-const-aggregate.md`](../analyze/0347-const-aggregate.md) |
| Разработка | [`docs/development/0347-01-const-aggregate.md`](../development/0347-01-const-aggregate.md) |
| Тест-план | [`docs/tests/0347-const-aggregate.md`](../tests/0347-const-aggregate.md) |
| Отчёт о тестировании | [`docs/reports/0347-const-aggregate.md`](../reports/0347-const-aggregate.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

`const BASE: Cell := {3, 4};` — цель `rust` печатала `[3, 4]` в поле типа
`Cell` (`E0308`), цель `sv` отвечала «значение обязано быть известно на этапе
компиляции», хотя оно известно.

## Документирование (правило 24)

**Не требуется** — подтверждено: правится печать двух целей.

## Итог (что сделано)

**Константы пошли тем же путём, что переменные.**

- **`rust`:** значение печатается через `coerce_to` — воронку, знающую литерал
  структуры. **`sv`:** `localparam` печатает `reset_value`, знающий агрегаты;
  прежде здесь стоял `constant_value`, знавший только скаляры.
- ⚠️ **У цели `sv` дефектов оказалось два:** правка печати обнажила второй —
  verilator ответил «Reference to 'cell_t' before declaration», потому что типы
  печатались **после** констант. Порядок разделов файла исправлен (класс 0341,
  но между разделами).
- ⚠️ **Четвёртая позиция одного правила:** присваивание (0340), инициализатор
  (0343), локальное объявление (0345), константа (0347) — знание об агрегате
  каждый раз жило в одной ветви из нескольких.
- ⚠️ **Контроль обязателен:** константа-скаляр печатается как прежде.

Детали — в [отчёте](../reports/0347-const-aggregate.md).
