# Фича 0248: Встроенные функции min/max/abs/clamp/debug не исполняются эталоном

- **Номер:** 0248
- **Статус:** ГОТОВО
- **Зависит от:** <номера фич-зависимостей через запятую, напр. `0007, 0027`, или
  `нет` — параметр проставляет аналитик на стадии анализа, правило 17>
- **Связанные issue (анализ):** <T-… или «новая фича»>

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0248-sim-builtin-functions.md`](../adr/0248-sim-builtin-functions.md) |
| Анализ | [`docs/analyze/0248-sim-builtin-functions.md`](../analyze/0248-sim-builtin-functions.md) |
| Разработка | [`docs/development/0248-01-sim-builtin-functions.md`](../development/0248-01-sim-builtin-functions.md) |
| Тест-план | [`docs/tests/0248-sim-builtin-functions.md`](../tests/0248-sim-builtin-functions.md) |
| Отчёт о тестировании | [`docs/reports/0248-sim-builtin-functions.md`](../reports/0248-sim-builtin-functions.md) |
| Исправления | не потребовались |

## Краткое описание

Встроенные функции языка (`min`, `max`, `abs`, `clamp`, `debug`, `S`) не
исполняются симулятором: ветвь отвечает `SIM-020` и останавливает прогон, тогда
как цели их переводят. Найдено гейтом ветвей-заглушек фичи
[0217](0217-stub-branch-gate.md).

> Фича зарегистрирована по находке гейта 0217 (2026-08-17); далее проходит
> жизненный цикл по правилу 17.

## Что известно на входе

**Класс:** семантика/эталон (Tier 2 — расхождение эталона с целями).

Ветвь `FunctionDefinitionNode::Builtin` в `takt-sim/src/unit/statement.rs`
отвечает `SIM-020` «встроенная функция … пока не поддерживается симулятором»,
номера задачи не называя.

**Проба 2026-08-17:** `m := min(n, 3);` — эталон останавливает прогон
`SIM-020`, а цель `c` тот же вход переводит **верно**:
`model->m = (((model->n) < (3)) ? (model->n) : (3));`. То же с `debug(n)`.

Затронуты все встроенные из `semantic/builtin.rs`: `debug`, `S`, `min`, `max`,
`abs`, `clamp`.

⚠️ Это ровно тот класс, ради которого заведены потактовые сверки (уроки 0045 и
0050): цель компилируется и считает, эталон не исполняется вовсе — сверить их
на такой модели нечем.

Проработка — стадии 2–3: реализовать вычисление встроенных в эталоне (`debug` —
побочный эффект вывода; `S` пересекается с фичей 0245) и завести сверку
значений, доказывающую совпадение с целями.

## Документирование (правило 24)

**Разделы языка не затронуты:** набор встроенных функций и их смысл не
менялись — менялся эталон. Обновлено **приложение «Ошибки и предупреждения»**:
описание `SIM-020` приведено в соответствие с новым смыслом (`S(Модель)` в
позиции выражения). Версия языка не поднимается (правило 22).

⚠️ Обнаружено, что документ **не описывает встроенные функции вовсе** — заведено
кандидатом в `FEATURES.md`.

## Итог (что сделано)

Эталон считает `min`, `max`, `abs`, `clamp` и исполняет `debug`. Прежде любая
встроенная функция давала `SIM-020` и останавливала прогон, тогда как цели их
переводили: модель со встроенной была **непроверяемой** — сверить эталон с
прошивкой было нечем (класс уроков 0045 и 0050).

Вычисление живёт в новом `takt-sim/src/eval/builtin.rs` и идёт **через
существующий слой** `eval::ops`: `min` — выбор по `<`, `abs` — унарный минус.
Своей арифметики нет, поэтому q-формат, обёртка (0127) и насыщение (0170)
наследуются; сверка на `q(8, 8)` прошла без единой строки Q-кода в новом
модуле. Формы повторяют цель `c` буквально, включая краевые случаи (`min` при
равенстве берёт первый; `clamp` применяет сперва нижнюю границу).

`debug` перехватывается **до** вычисления аргументов (его аргумент — строковый
литерал, а `Value` строк не представляет) и печатает в stderr: stdout занят
трассой, которую читают сверки. `S(Модель)` в позиции выражения остаётся
отказом — её не переводит ни одна цель, и эталон не вправе их обгонять
(урок 0034); заглушка объявлена в реестре 0217 с держателем
[0247](0247-sim-bare-state-condition.md).

⚠️ **Дефект реализации поймала проба, а не чтение:** первая редакция возвращала
из `max` меньшее. Отсюда требование сверки — трасса обязана меняться по тактам.

⚠️ **Гейт 0217 сам оказался с дырой:** маркер, разорванный переносом строки, был
ему невидим. После починки открылась третья заглушка `SIM-017` и настоящий
дефект: `f(n) := 1;` семантика принимает, цель `c` печатает невалидный C —
заведена фича [0249](0249-assign-to-call-place.md).

Детали — в [отчёте](../reports/0248-sim-builtin-functions.md),
[ADR](../adr/0248-sim-builtin-functions.md) и
[анализе](../analyze/0248-sim-builtin-functions.md).
