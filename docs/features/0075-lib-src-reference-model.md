# Фича 0075: Эталонный `SRC` в `lib.rs` порождает C с 11 ошибками `cc`

- **Номер:** 0075
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (`lib.rs`, генератор `generator/c/`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0075-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0075-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0075-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0075-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0075-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0075-YY-*`) |

## Краткое описание

Модель-«всё сразу», на которой стоит тест `syntax_simple`, **никогда** не давала
компилируемый C, и это не замечалось: тест проверял лишь факт `Ok`.

Причины **не связаны** с состояниями: в порождённой структуре нет порт-колбэков
`read_numeric`/`write_numeric`, а `const`-массив `MATRIX: u8 := {0,…}` печатается
как **скаляр** (`CONST_..._MATRIX`), давая `expected ')'`. Проверено: строка
перевода `S(Ping) = End` ошибок **не даёт** (ошибки — на соседних строках,
обращения к портам).

Следствие: `syntax_simple` вынужден проверять **строку** в `.c`, а не компиляцию.

Смежно с [0034](0034-sim-struct-types.md) (структуры) и с фичей
[0083](0083-model-always-block-c.md) (`always` на уровне модели).

Выявлено при [0047](0047-c-state-of-model.md).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
