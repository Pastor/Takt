# Фича 0263: Цель rust приводит к usize даже литеральный индекс

- **Номер:** 0263
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии 3)
- **Tier:** 2 — clippy -D warnings отвергает unnecessary_cast — тот же гейт, что в precheck (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** генерация
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0263-rust-literal-index-cast.md`](../adr/0263-rust-literal-index-cast.md) |
| Анализ | [`docs/analyze/0263-rust-literal-index-cast.md`](../analyze/0263-rust-literal-index-cast.md) |
| Разработка | [`0263-01`](../development/0263-01-rust-literal-index-cast.md) |
| Тест-план | [`docs/tests/0263-rust-literal-index-cast.md`](../tests/0263-rust-literal-index-cast.md) |
| Отчёт о тестировании | [`docs/reports/0263-rust-literal-index-cast.md`](../reports/0263-rust-literal-index-cast.md) |
| Исправления | не потребовались |

## Краткое описание

**Цель `rust` приводит к `usize` даже литеральный индекс.** Замер
[0250](0250-bit-write-in-targets.md) (2026-08-18): `arr[1] := 1;`
печатается `self.arr[1 as usize] = 1;`, и настоящий `clippy -D warnings`
отвечает `unnecessary_cast` — то есть порождённый модуль не проходит тот же
гейт, что стоит в `precheck.sh`. Записи разряда дефект не касается: он даёт
ту же ошибку без неё. Гейт слеп потому, что в корпусе индексируют
**переменной** (`arr[i]`), где приведение необходимо. Кандидат: печатать
приведение по нужде — литерал в `usize` не приводить.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Определяется на стадии анализа.** Фича затрагивает язык или наблюдаемое
поведение целей, поэтому нужный раздел `book/` называется после проработки —
до неё утверждение о документе было бы догадкой.

## Итог (что сделано)

**Приведение индекса к `usize` печатается по нужде.** Литеральный индекс шёл в
вывод как `self.arr[1 as usize]`, и настоящий `clippy -D warnings` — теми же
флагами, что в гейте предкоммита, — отвечал **ошибкой** `unnecessary_cast`: то
есть порождённый модуль не проходил гейт самого проекта. Замер воспроизведён
2026-08-19 вместе с контролем (`arr[i]` приведение сохраняет и замечаний не
вызывает).

**Сделано:** правило вынесено в **одну** функцию `rust_expr::subscript`, её
зовут оба печатника индекса — выражений и условий. Прежде у каждого была своя
копия строки, и правило разъехалось бы при первой правке (класс 0084/0193/0195).

**Сторож** — `takt-lang/tests/targets/rust_index_cast_tests.rs`: обе формы в
одном тесте (мутация «снять приведение везде» валит его) плюс прогон настоящего
`clippy-driver -D warnings`. ⚠️ Гейт цели этого класса не видел: он гоняет
только корпус, где индексируют **переменной**.

**Версия языка не поднята** (форма вывода цели, не язык); крейт `takt-lang`:
`0.46.0` → `0.46.1`.

Детали — [отчёт](../reports/0263-rust-literal-index-cast.md), решение —
[ADR](../adr/0263-rust-literal-index-cast.md).
