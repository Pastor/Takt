# Фича 0063: Порт `en` (clock enable) для цели `sv`

- **Номер:** 0063
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (генератор `generator/sv/`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0063-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0063-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0063-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0063-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0063-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0063-YY-*`) |

## Краткое описание

Служебный порт `en` с умолчанием `1'b1`, дающий возможность тактировать модель
**реже** тактового домена. Аддитивен поверх принятого решения 0045 (`clk`/`rst_n`
— служебные порты, которых в языке нет).

Вынесено закрытой [0045](0045-sv-backend.md) (ADR, вопрос 1, Option C — признан
«преждевременным, но **не неверным**»).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
