# Фича 0285: Ширина выведенного типа берётся у результата

- **Номер:** 0285
- **Статус:** ГОТОВО
- **Зависит от:** нет
- **Tier:** 2 — **подтверждён замером и оказался шире**: дело не в сдвиге, а в любой арифметике (`2 * 128` = 0, `200 + 200` = 144); расхождения между потребителями нет — все девять неправы одинаково (расставлен 2026-08-18, перепроверен при закрытии)
- **Класс:** семантика
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0285-inferred-width-from-result.md`](../adr/0285-inferred-width-from-result.md) |
| Анализ | [`docs/analyze/0285-inferred-width-from-result.md`](../analyze/0285-inferred-width-from-result.md) |
| Разработка | [`0285-01`](../development/0285-01-inferred-width-from-result.md) |
| Тест-план | [`docs/tests/0285-inferred-width-from-result.md`](../tests/0285-inferred-width-from-result.md) |
| Отчёт о тестировании | [`docs/reports/0285-inferred-width-from-result.md`](../reports/0285-inferred-width-from-result.md) |
| Исправления | не потребовались |

## Краткое описание

**Ширина выведенного типа берётся у операнда, поэтому `const K := 1 << 8;`
даёт ноль.** Замер при закрытии
[0208](0208-const-evaluators-unification.md) (2026-08-16): тип
выводится от левого операнда (`1` → `[bit;8]`), а нормирование
[0207](0207-bitwise-not-unsigned-literal.md) заворачивает
вычисленные 256 в ноль. Прежде это была ошибка `SE-089`. ⚠️ Поведение следует
из двух принятых правил и в теле такое же (`u: u8` → `u := 1 << 8;` даёт 0),
но для **объявления без явного типа** неочевидно: автор не выбирал ширину, её
выбрал литерал. Смежно с кандидатом о `wider_type(Integer, Array)`. Кандидат:
либо выводить ширину по **результату** вычисления, либо предупреждать, когда
нормирование меняет вычисленное значение.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Требуется, и сделано.** Языковой раздел
[«Типы»](../../book/src/03-types/index.typ), подраздел «Вывод типов»: правило
записано с примером (`const STEP := 1 + 255;` → `256`) и контрпримером
(`var u: u8 := 200 + 100;` → `44`, ширину выбрал автор).

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->


## Итог (что сделано)

Ширина **выведенного** типа берётся у результата вычисления, а не у операндов.
`const K := 1 + 255;` даёт `256`; `const WIDE := 200 + 200;` — `400`. Явный тип,
вывод из сигнатуры функции и нормирование (0207) не тронуты.

⚠️ **Кандидат винил сдвиг — замер показал любую арифметику.** `2 * 128`,
`128 * 2`, `1 + 255`, `255 + 1` давали ноль, `200 + 200` — `144`. А `300 - 44`
и `1000 + 1` работали **случайно**: там левый операнд сам не влезал в восемь
бит. Правка по букве кандидата (только `<<`) закрыла бы восьмую часть класса.

⚠️ **Расхождения между потребителями не было:** значение сворачивается в
семантике, и все девять давали один и тот же неверный ответ. `Tier 2`
подтверждён в собственном смысле — «молча неверное значение при согласии
инструментов».

⚠️ **Правка сужалась дважды, и оба раза её сузили ЧУЖИЕ сторожа.** Первая
редакция переопределяла тип **всегда** и сломала вывод Ce6: `fn get32() ->
[bit;32]` с `var val := get32();` дал `[bit;8]`, потому что вычислитель
сворачивает вызов в `0`. Вторая смотрела и на **нижнюю** границу и сломала
`var u := ~0;` (перестал давать `255` — а это домен правила 0207). Обе редакции
выглядели верными при чтении.

Отсюда три границы, каждая закреплена сторожем: явный тип не трогать, тип из
сигнатуры не трогать, отрицательное не расширять. Границы типа берутся у
**единственного** носителя `validate::literal_range::type_range` — своя копия
разошлась бы с `SE-089`.

Версия крейта `takt-lang`: `0.43.0` → `0.44.0`. Версия языка **не растёт**:
цикл `0.10.0` открыт фичей 0243 (правило 22, накопление).

Артефакты: [ADR](../adr/0285-inferred-width-from-result.md) ·
[анализ](../analyze/0285-inferred-width-from-result.md) ·
[0285-01](../development/0285-01-inferred-width-from-result.md) ·
[тест-план](../tests/0285-inferred-width-from-result.md) ·
[отчёт](../reports/0285-inferred-width-from-result.md).
