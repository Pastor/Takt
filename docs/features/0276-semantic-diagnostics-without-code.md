# Фича 0276: Восемь безликих диагностик остались в семантике

- **Номер:** 0276
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии 3)
- **Tier:** 3 — код [?] и позиция «начало первого файла» вместо настоящих (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** семантика
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0276-semantic-diagnostics-without-code.md`](../adr/0276-semantic-diagnostics-without-code.md) |
| Анализ | [`docs/analyze/0276-semantic-diagnostics-without-code.md`](../analyze/0276-semantic-diagnostics-without-code.md) |
| Разработка | [`0276-01`](../development/0276-01-semantic-diagnostics-without-code.md) |
| Тест-план | [`docs/tests/0276-semantic-diagnostics-without-code.md`](../tests/0276-semantic-diagnostics-without-code.md) |
| Отчёт о тестировании | [`docs/reports/0276-semantic-diagnostics-without-code.md`](../reports/0276-semantic-diagnostics-without-code.md) |
| Исправления | не потребовались |

## Краткое описание

**Восемь безликих диагностик остались в семантике.** Замер при закрытии
[0212](0212-c-diagnostic-without-code.md) (2026-08-17): в цели
`c` таких мест было девятнадцать и все закрыты воронками `CC-022`/`CC-023`, а
вне генераторов их **восемь** — `semantic/function.rs` (×3), `tree.rs` (×2),
`named_block.rs`, `condition/mod.rs`, `extend.rs`. Все строят диагностику
конверсией `From<&str>`: код `None` (печатается `[?]`) и позиция
`Source(0, 0, 0)`, то есть «начало первого файла». Кандидат: дать им коды
`SE-…` с описанием в приложении (правило 25), после чего **удалить**
`impl From<&str> for Diagnostic` — тогда класс запретит тип, а не греп-сторож.
⚠️ Часть мест — внутренние инварианты семантики; для них надо решить, один
общий код или свой каждому.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Определяется на стадии анализа.** Фича затрагивает язык или наблюдаемое
поведение целей, поэтому нужный раздел `book/` называется после проработки —
до неё утверждение о документе было бы догадкой.

## Итог (что сделано)

**Безликая диагностика запрещена типом.** Конверсия `From<&str> for Diagnostic`
строила отказ без кода (`[?]` на печати) и с позицией `Source(0, 0, 0)` — то
есть «начало первого файла»: координата **ложная**, а не отсутствующая.

**Замер нашёл больше, чем называл кандидат** — двенадцать мест вместо восьми:
восемь в семантике, **два пропущенных в самой цели `c`** (фича 0212 правила там
девятнадцать) и **два с кодами**, где та же конверсия молча съедала позицию
(`SE-004`, `SE-022`).

**Сделано:** достижимые места получили свои коды и позиции (`SE-117` —
индексируется не массив, `SE-118` — локальная функция без тела); недостижимые
(их держат `SE-034`, `SE-004`, `SE-081` и запасные ходы) — общую воронку
`SE-119`, называющую вид нарушения **словом**, по образцу `CC-023`. Два места
цели `c` переведены на существующую `CC-023`. Конверсия **удалена**, на её месте
— комментарий, почему возвращать нельзя.

**Сторож двухслойный** (`takt-lang/tests/semantic/diagnostic_code_presence_tests.rs`):
поведение (код и позиция у обеих достижимых диагностик) и устройство (нет
конверсии; в `src/semantic` не осталось `Err("…".into())` — падает списком мест).

⚠️ **Урок:** удаление удобной конверсии — способ **измерить** класс. Пока она
существовала, безликое место заводилось незаметно, и именно так 0212 пропустила
два места в цели, которую правила.

**Документ:** приложение «Ошибки и предупреждения» — таблица и записи `SE-117`,
`SE-118`. **Версия языка не поднята** (цикл `0.10.0`); крейт `takt-lang`:
`0.48.0` → `0.49.0`.

Детали — [отчёт](../reports/0276-semantic-diagnostics-without-code.md), решение —
[ADR](../adr/0276-semantic-diagnostics-without-code.md).
