# Фича 0401: Вид импортированного символа неточен: Imported вместо «порт»/«функция»

- **Номер:** 0401
- **Статус:** АНАЛИЗ
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-22)
- **Tier:** proc — расставлен по признаку вреда (шкала — в блоке 1 `FEATURES.md`)
- **Класс:** прочее
- **Связанные issue (анализ):** кандидат блока 3 (процессный бэклог) `FEATURES.md`, переведён в фичу пакетно 2026-08-22

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0401-lsp-imported-symbol-kind.md`](../adr/0401-lsp-imported-symbol-kind.md) |
| Анализ | [`docs/analyze/0401-lsp-imported-symbol-kind.md`](../analyze/0401-lsp-imported-symbol-kind.md) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0401-YY-*`) |
| Тест-план | [`docs/tests/README.md`](../tests/README.md) |
| Отчёт о тестировании | [`docs/reports/README.md`](../reports/README.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0401-YY-*`) |

## Краткое описание

- **Вид импортированного символа неточен: `Imported` вместо «порт»/«функция».**
  Замер [0256](0256-lsp-import-binding-kind.md) (2026-08-19):
  имя из `import { a } from "…";` получает вид, отвечающий **любому**
  пространству имён, потому что настоящий вид записан в **чужом** файле, а
  слой `semantic::usages` однофайловый по устройству (его зовут и там, где
  рабочей области нет вовсе — подсветка, форматирование, плагины поверх
  библиотеки). Точный вид требует чтения источника, то есть либо многофайлового
  слоя, либо состояния, обязанного не устаревать, — ровно того, что ADR 0153
  отверг замером. ⚠️ Сегодня вид символа читает **одно** место
  (`lsp/rename.rs` — отказ на имени модели), поэтому цена неточности мала;
  подорожает она, если вид начнёт влиять на подсветку или на автодополнение.

**Прочее — пусто.** Последняя запись — «Переезд репозитория `github.com/Pastor/BuT`» —
снята 2026-07-29 по правилу 28: переезд **состоялся** (`origin` →
`https://github.com/Pastor/Takt.git`, проверено пушем), то есть запись описывала
как несделанное то, что уже сделано. Административная часть исполнена
заказчиком; остаточная работа по коду и докам переведена в фичу
[0179](0179-repo-url-cleanup.md) — 13 живых вхождений старого имени
в манифестах крейтов, README, doc-ссылках и манифестах обоих плагинов. Старый
адрес пока работает (`GET …/Pastor/BuT` → **301**, `git ls-remote` отдаёт
коммит), то есть это не отказ, а зависимость от редиректа GitHub.

Новые улучшения процесса записывать сюда до заведения фичи.

> Фича зарегистрирована из блока кандидатов `FEATURES.md` **без проработки**
> (решение заказчика 2026-08-22: «завести фичи из кандидатов без проработки»).
> Текст кандидата перенесён **дословно**, вместе с замером и его датой: замер
> есть свидетельство, а пересказ его теряет. Стадии 2 и 3 не пройдены —
> зависимости и объём **не оценивались**; далее фича проходит жизненный цикл по
> правилу 17.

## Документирование (правило 24)

**Не требуется.** Предмет — точность вида символа в LSP; язык не затрагивается.
Поведение `rename` описано в README (раздел об интеграции) и остаётся прежним.

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->

