# Фича 0020: Адрес порта — размещение и потребление (карта адресов)

- **Номер:** 0020
- **Статус:** РАЗРАБОТКА (архитектура пересмотрена 2026-07-14)
- **Зависит от:** нет
- **Крейт:** `grammar`

## Ссылки на артефакты (правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0020-port-address-decl.md`](../adr/0020-port-address-decl.md) |
| Анализ | [`docs/analyze/0020-port-address-decl.md`](../analyze/0020-port-address-decl.md) |
| Разработка 0020-01 | [`docs/development/0020-01-address-grammar.md`](../development/0020-01-address-grammar.md) (ВЫПОЛНЕНО) |
| Разработка 0020-02 | [`docs/development/0020-02-semantics-diagnostics.md`](../development/0020-02-semantics-diagnostics.md) (ВЫПОЛНЕНО) |
| Разработка 0020-03 | [`docs/development/0020-03-external-map.md`](../development/0020-03-external-map.md) (ВЫПОЛНЕНО) |
| Разработка 0020-04 | [`docs/development/0020-04-completeness.md`](../development/0020-04-completeness.md) (ВЫПОЛНЕНО) |
| Разработка 0020-05 | [`docs/development/0020-05-c-consumption.md`](../development/0020-05-c-consumption.md) (ВЫПОЛНЕНО) |

> **Пересмотр архитектуры 2026-07-14.** По запросу заказчика решение 0020
> пересмотрено: фича охватывает **обе оси** — *размещение* адреса и его
> *потребление* компилятором (находка: адрес сейчас **не эмитится в C**, порты
> идут через HAL-колбэки — был мёртвыми метаданными). Принят **гибрид** на базе
> единого внутреннего слоя `AddressMap`. Инлайн-форма сохранена (регресс = 0),
> `address` — жёсткое ключевое слово, внешняя карта — отдельный `.ld`-подобный
> формат. Язык расширяется → версия языка повышается (правило 22).

## Краткое описание

Подсистема привязки портов к аппаратным адресам. Источники (по возрастанию
приоритета): инлайн `:=` (сахар) → оператор `address NAME = <expr>;` в модели →
внешняя `.ld`-подобная карта (`--address-map`). Все источники наполняют единый
`AddressMap: PortId → Address`, из которого генерируется C-таблица адресов и
дефолтный HAL. `-D`/инъекция define’ов, SVD-экспорт и ST `AT %` вынесены в
отдельные фичи/кандидаты.

## Декомпозиция (см. анализ)

- **0020-01** — `.lam`-грамматика + AST оператора `address`.
- **0020-02** — семантика `AddressMap` + приоритет + диагностики конфликта/висячей.
- **0020-03** — внешняя `.ld`-подобная карта (`--address-map`, оверлей + warning).
- **0020-04** — полнота по достижимости (опора на `unused.rs`).
- **0020-05** — потребление в C: таблица адресов + дефолтный HAL (`*(volatile*)`).

## Риски

- Жёсткое ключевое слово `address` — коллизия с идентификаторами (grep + миграция).
- Новый парсер `.ld`-подобного формата (отдельный мини-язык).
- Семантика приоритета/полноты — двухпроходность; не сломать колбэк-HAL.

> Открытая фича. Источник: `STATUS.md` («пропущенные задачи», п. 11).
