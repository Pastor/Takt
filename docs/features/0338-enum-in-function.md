# Фича 0338: Перечисление внутри функции у целей `st` и `sv`

- **Номер:** 0338
- **Статус:** ГОТОВО
- **Зависит от:** 0030, 0336 (закрыты; проставлено 2026-08-20)
- **Связанные issue (анализ):** заменяет **ошибочного** кандидата, заведённого
  фичей 0336 («перечисление в параметре функции цель `sv` не переводит»)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0338-enum-in-function.md`](../adr/0338-enum-in-function.md) |
| Анализ | [`docs/analyze/0338-enum-in-function.md`](../analyze/0338-enum-in-function.md) |
| Разработка | [`docs/development/0338-01-enum-in-function.md`](../development/0338-01-enum-in-function.md) |
| Тест-план | [`docs/tests/0338-enum-in-function.md`](../tests/0338-enum-in-function.md) |
| Отчёт о тестировании | [`docs/reports/0338-enum-in-function.md`](../reports/0338-enum-in-function.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

`var res: Mode := Idle;` в теле функции: цель `sv` печатала `res = 0;` —
verilator отвечал **ошибкой** `ENUMVALUE`; цель `st` печатала имя константы,
невидимое внутри `FUNCTION`. Обе — при нулевом коде возврата `taktc`.

## Документирование (правило 24)

**Не требуется** — подтверждено: правится представление у двух целей.

## Итог (что сделано)

**Одна причина, два разных проявления.**

- **Вариант перечисления приходит печатнику числом** (узла варианта у
  `ExpressionNode` нет вовсе), и цели обошлись с ним по-своему неверно: `sv`
  напечатала число, `st` — имя константы, которую в IEC 61131-3 функция не
  видит (она замкнута).
- **`sv`:** инициализатор идёт через `Scope::coerce` — ту же воронку, что
  присваивание. **`st`:** константы, **упомянутые телом**, дублируются в
  `VAR CONSTANT` функции — доводом фичи 0030.
- ⚠️ **Отбор у `st` идёт по ТЕКСТУ тела:** имя константы строит печатник, и
  второй способ его узнать разошёлся бы с первым (класс 0084/0193/0195).
- ⚠️ **Дублируются упомянутые, а не все:** лишние объявления — шум в выводе,
  который читает наладчик ПЛК; контрольная функция это проверяет.
- ⚠️ **Кандидат, из которого выросла фича, был ОШИБОЧЕН.** Он утверждал, что
  цель `sv` не переводит перечисление в **параметре**; на деле там сработал
  досрочный возврат (`SV-002`, запрет с 0045). Проба была негодной —
  посторонняя причина не снята (правило 30).

Детали — в [отчёте](../reports/0338-enum-in-function.md).
