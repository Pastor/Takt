# Фича 0065: Изоляция пространства имён цели `st` (POU и переменные)

- **Номер:** 0065
- **Статус:** **ГОТОВО** (2026-07-18) — все три задачи закрыты, включая долг сверки
- **Зависит от:** **нет** (проверено аналитиком 2026-07-17 — см. [анализ](../analyze/0065-st-namespace-isolation.md)). Опоры **закрыты**: [0041](0041-st-backend.md) (цель `st`), [0048](0048-deterministic-codegen.md) (дифф корпуса читаем).
- **Приоритет / Tier:** **Tier 2** (кандидат утверждал «не дефект» — проработка переоценила). Фича закрывает **Tier 1**-дефект [0041-01](../fixes/0041-01-st-fn-dedup-silent.md): цель `st` **молча** склеивает одноимённые `fn` разных моделей → автомат на ПЛК стоит.
- **Крейт:** `grammar` (`generator/st/`: `st_func.rs`, имена), `simulation` (новая сверка `st`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`); закрывает [фикс 0041-01](../fixes/0041-01-st-fn-dedup-silent.md)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0065-st-namespace-isolation.md`](../adr/0065-st-namespace-isolation.md) |
| Анализ | [`docs/analyze/0065-st-namespace-isolation.md`](../analyze/0065-st-namespace-isolation.md) |
| Разработка | [`docs/development/`](../development/README.md) (задачи [`0065-01`](../development/0065-01-prefix-pou.md), [`0065-02`](../development/0065-02-st014-reserved-names.md), [`0065-03`](../development/0065-03-st-conformance.md)) |
| Тест-план | [`docs/tests/0065-st-namespace-isolation.md`](../tests/0065-st-namespace-isolation.md) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0065-*` — при реализации) |
| Исправления | [`docs/fixes/0041-01-st-fn-dedup-silent.md`](../fixes/0041-01-st-fn-dedup-silent.md) (закрывается в объёме фичи) |

## Краткое описание

Пространство имён `FUNCTION`/`FUNCTION_BLOCK` в IEC 61131-3 **плоское и общее со
стандартной библиотекой**, а генератор `st` печатает имена как есть. Три следствия
(все подтверждены гейтом `iec2c`):

1. **Одноимённые `fn` разных моделей столкнутся** — цель `c` префиксует именем
   модели (`Stacker_travel_time`), цель `st` — нет.
2. **Модель с именем `Concat`/`ABS`/`MIN`/`SEL`** даёт `invalid function block
   name`; имя корневой модели берётся **из имени файла**, поэтому ловушка
   срабатывает на `concat.lam` (поймано на фикстуре 0041-07, файл переименован).
3. ⚠️ **Капкан шире POU — бьёт и по обычным переменным.** Локальная переменная
   функции с именем `left` даёт «invalid located variable declaration» — `LEFT` в
   IEC стандартная **строковая** функция. Проверено: `left`/`LEFT`/`right`/`mid`
   отвергаются, `remaining`/`v` принимаются; идентификаторы IEC
   **регистронезависимы**, поэтому нижний регистр не спасает. Диагностика
   **обманчива** — говорит про «located variable» (`AT %…`), которых в объявлении
   нет вовсе, то есть по тексту ошибки причину не найти.

Оба класса `iec2c` ловит **громко** («duplicate»/«invalid name») — тихого
расхождения нет. Лечение общее: префиксовать имена уникальным именем модели, как в
цели `c`. Обойдено переименованием в примере (`left` → `remaining`).

В корпусе п. 3 был замаскирован тем, что до 0030 **ни одна** функция примеров не
имела локальных переменных.

Выявлено при закрытой [0041](0041-st-backend.md), расширено при
[0030](0030-comprehensive-example-fix.md).

> **Объединены два кандидата** («Имена POU цели `st` не изолированы» и его
> подпункт-уточнение про переменные): лечение у них одно — изоляция пространства
> имён цели (решение заказчика 2026-07-17).

## Итог проработки (2026-07-17)

⚠️ **Ключевая заявка кандидата ОПРОВЕРГНУТА пробой.** Кандидат утверждал: «Оба
случая `iec2c` ловит **громко**, **тихого расхождения нет** — поэтому кандидат, а
не дефект». Про случай 1 это **неправда**.

| # | Проба | Результат | Заявка |
|---|---|---|---|
| 1 | Две модели, у каждой `fn helper` с **разными** телами | **`st`: ОДНА** `FUNCTION helper` с телом первой (`x+1`); **`iec2c` ПРИНЯЛ** (rc=0). Эталон `c`: `DupA_helper` **и** `DupB_helper` | **ОПРОВЕРГНУТА** |
| 2 | `concat.lam` | `invalid function block name` — громко | подтверждена |
| 3 | `var left: u8` | `invalid located variable declaration` — громко, но **обманчиво** | подтверждена |

**Механизм** — `st_func.rs:275`, дедупликация по голому имени с комментарием:
«столкновение поймает `iec2c` («duplicate»), то есть **громко, а не молча**».
**Комментарий неверен:** дедупликация именно и означает, что дубля в тексте **нет**
— ловить нечего. Модель `B` вместо `w = 3` получает `w = 2` → переход
`ref Done2: w = 3` **не срабатывает никогда** → автомат **молча стоит**.

Заведён **[фикс 0041-01](../fixes/0041-01-st-fn-dedup-silent.md), Tier 1**.
⚠️ **Кандидат унаследовал ошибку из комментария дословно** — из-за этого дефект
Tier 1 три месяца числился «долгом Tier 3». Урок: *формулировка кандидата —
гипотеза, а не факт, даже если списана из кода*.

**Принято** ([ADR](../adr/0065-st-namespace-isolation.md), Option A) — два лечения
на две **разные** беды: (1) **POU префиксуются именем модели**, как в `c` →
дедупликация исчезает вместе с причиной, фикс закрыт **как следствие**;
(2) **`ST-014`** (код свободен) — идентификатор совпал со стандартной функцией IEC
или ключевым словом; сравнение **без учёта регистра**; текст **называет причину**,
которой нет у `iec2c`. Отклонены: только префикс (оставляет обманчивую
диагностику), молчаливое переименование (имя в ST перестаёт совпадать с `.lam` —
отладка на ПЛК становится головоломкой; прецедент `sv` — диагностика), статус-кво.

⚠️ **Долг, который отдаёт фича: сверки для цели `st` НЕ СУЩЕСТВУЕТ**
(`conformance_{c,sv,rust}_tests` есть, `st` — нет). Поведение ST не проверялось
**никогда** — ровно поэтому 0041-01 и дожил. Задача 0065-03 её заводит; ⚠️ если
объём окажется несоизмерим (нужен runtime для порождённого ST), она **выделяется в
отдельную фичу** — Tier 1 ждать не должен.

Язык не меняется → версия не растёт (правило 22).

## Итог (что сделано, 2026-07-18)

Все три задачи закрыты — включая долг сверки, который проработка допускала
вынести в отдельную фичу (не понадобилось).

- **0065-01 (Tier 1).** `st_func.rs`: локальные `FUNCTION` префиксуются
  **уникальным именем модели-владельца** (`Stacker_travel_time`,
  `DupFnA_helper`/`DupFnB_helper`) — как цель `c` (`get_function_name`). Имя
  строится из `upper` (владелец), поэтому объявление и вызов из другой модели
  дают одно имя. `extern`/`Builtin` остаются с голым именем (согласованность с
  `c`, критерий T4). Дедупликация не удалена, а **перекодирована на итоговое
  имя**: локальные больше не склеиваются (разные имена), а идентичные заглушки
  `extern` одноимённых функций разных моделей по-прежнему не задваиваются (иначе
  `iec2c` дал бы «duplicate» на корректной модели). Неверный комментарий
  исправлен. Сторож — `test_st_same_named_fns_get_distinct_prefixed_functions`
  + фикстура `dup_fn.lam`.
- **0065-02 (`ST-014`).** Новый модуль `generator/st/st_reserved.rs`:
  `check_st_name` сверяет идентификатор (без учёта регистра) со списком
  **проверенных пробой через `iec2c`** стандартных функций и ключевых слов IEC.
  Список намеренно **короткий**: ложное срабатывание (запрет имени, которое
  MatIEC принимает) хуже статус-кво. Проверка стоит **в точках эмиссии** (имя
  `FUNCTION_BLOCK`, переменные/порты/константы после фильтра использования,
  параметры и локальные `var` функций, `extern`-имена) — так отфильтрованная
  `var action: Action` из `elevator.lam` не даёт ложного отказа. Текст называет
  причину, которой нет у `iec2c`. 5 интеграционных + 3 юнит-теста.
- **0065-03 (сверка `st`).** `simulation/tests/conformance_st_tests.rs`:
  порождённый ST → `iec2c` → `POUS.c` → C-драйвер → `cc` → запуск → трасса,
  сверяемая с симулятором на фикстуре дублей (`wa = 2`, `wb = 3`). Доказывает
  фикс **поведенчески**; мутация (возврат склейки) → `wb = 2` → сверка падает
  при зелёном `iec2c`. Подробности и капкан ноль-инициализации MatIEC —
  [`docs/development/0065-03-st-conformance.md`](../development/0065-03-st-conformance.md).

**Проверки:** 1964 теста `grammar`+`simulation` зелёные, clippy чист, лимит
модулей не нарушен, корпус перегенерирован (дифф `st` — только префиксы), весь
корпус проходит `iec2c`. Язык не менялся → версия не поднята.
