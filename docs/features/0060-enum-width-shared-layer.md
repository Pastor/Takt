# Фича 0060: Расчёт ширины перечисления — вынести в общий слой

- **Номер:** 0060
- **Статус:** **ГОТОВО** (2026-07-18) — общий слой заведён, четыре цели мигрированы, фикс 0005-01 закрыт
- **Зависит от:** **нет** (проверено аналитиком 2026-07-17 — см. [анализ](../analyze/0060-enum-width-shared-layer.md)). Опора — **закрытая** [0048](0048-deterministic-codegen.md): без детерминизма критерий A2 («вывод всех целей побайтово прежний») неисполним.
- **Приоритет / Tier:** **Tier 2** (кандидат подразумевал Tier 3 — проработка переоценила). Фича закрывает **Tier 1**-дефект [0005-01](../fixes/0005-01-c-enum-signedness.md): молчаливо неверный вывод **эталонной** цели `c`, из-за которого автомат стоит.
- **Крейт:** `grammar` (`semantic/` — общий слой; `generator/{c,st,rust,sv}/` — потребители)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`); закрывает [фикс 0005-01](../fixes/0005-01-c-enum-signedness.md)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0060-enum-width-shared-layer.md`](../adr/0060-enum-width-shared-layer.md) |
| Анализ | [`docs/analyze/0060-enum-width-shared-layer.md`](../analyze/0060-enum-width-shared-layer.md) |
| Разработка | [`docs/development/`](../development/README.md) (задачи [`0060-01`](../development/0060-01-enum-facts.md), [`0060-02`](../development/0060-02-migrate-targets.md)) |
| Тест-план | [`docs/tests/0060-enum-width-shared-layer.md`](../tests/0060-enum-width-shared-layer.md) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0060-*` — при реализации) |
| Исправления | [`docs/fixes/0005-01-c-enum-signedness.md`](../fixes/0005-01-c-enum-signedness.md) (закрывается в объёме фичи) |

## Краткое описание

Верный расчёт ширины перечисления — **по диапазону значений** — сегодня реализован
в каждом бэкенде **отдельно** (`rust_type::enum_repr`, `sv_type::enum_width`).
Кандидат: вынести в `semantic/`, чтобы четвёртый бэкенд не наступил на то же.

Повод — **три бэкенда подряд**, споткнувшихся об одно и то же: ST брал `USINT`,
Rust — `#[repr(u8)]`, SV — `⌈log₂(вариантов)⌉`; все три предположения опровергнуты
одним и тем же `enum Action { Idle = 670 }` из `elevator.lam:121` (в байт не
влезает).

Выявлено при закрытой [0045](0045-sv-backend.md).

## Итог проработки (2026-07-17)

Проработка заявку кандидата **не подтвердила, а усилила** — тремя уточнениями.

**1. Копий четыре, а не две.** Кандидат называл `rust_type::enum_repr` и
`sv_type::enum_width`; греп даёт **четыре** независимых извлечения диапазона —
плюс `st_type::enum_type`/`variants_range` и ветка `Enum` в `get_c_type`
(`c/mod.rs:218`).

**2. «Четвёртый бэкенд наступит на то же» — уже наступил, и это ПЕРВЫЙ.** Цель `c`
смотрит **только на `max`** и знак **не проверяет вовсе**. Проба на
`enum Level { Low = -5, Mid = 0, High = 5 }`:

| Цель | Результат | Верно? |
|---|---|---|
| **`c`** | **`uint8_t lv;`**, `model->lv = -5;` | **НЕТ** |
| `rust` | `#[repr(i8)]` | да |
| `st` | `SINT`, `Level_Low : SINT := -5` | да |
| `sv` | `level_e` (знаковая) | да |

**Следствие проверено живым `cc`:** `lv = 251`, `(lv == -5) = 0` — условие
`if (model->lv == -5)` **тождественно ложно**, переход **мёртв**, автомат стоит.
Модель, верная в симуляторе и в трёх других целях, в C **молча стоит**. Заведён
**[фикс 0005-01](../fixes/0005-01-c-enum-signedness.md)** (Tier 1), закрывается в
объёме этой фичи — прецедент: [0056](0056-lsp-goto-exact-file.md) попутно закрыла
0053-01.

**3. Цели задают РАЗНЫЕ вопросы,** поэтому выносить надо **не «ширину»**: `sv`
нужна точная ширина (`670` → 10 бит), прочим — машинная (16) плюс своё имя типа.

**Принято** ([ADR](../adr/0060-enum-width-shared-layer.md), Option A): общий слой
считает **факт** (диапазон, знак, минимальная ширина) и **не знает ни одной цели**;
отображение факта в тип остаётся в цели. Миграция `c` на общий факт **чинит
0005-01 как следствие**, а не заплатой. Отклонены: вынос вместе с именами типов
(общий слой узнал бы про все цели), «починить только C» (четыре копии остаются),
унификация пустого перечисления (это вопрос **семантики языка**, не кодогенерации —
вынесено кандидатом; прецедент: 0029 не решала семантику `[bit;N]`).

⚠️ **Корпус фикс не докажет:** ни одно его перечисление не имеет отрицательных
вариантов (`Mode` 0…2, `Floor` 80…81, `Action` 670…671, `Command` 0…2,
`Constant` 0…2, `Point` 0…1) — снова «дыра **в покрытии**, а не в коде» (класс
[0026](0026-c-root-typedef.md)). Поэтому вывод корпуса обязан остаться **побайтово
прежним** (сильный сторож рефакторинга), а фикс доказывается **новой фикстурой** и
**потактовой сверкой**, заводимой вместе с правкой (уроки 0045/0050).

⚠️ **Гейт цели `c` дефект принимает:** `cc` **предупреждает**
(`-Wtautological-constant-out-of-range-compare`), но гейт идёт **без `-Werror`** —
сторож есть, но выключен. Отдельный кандидат.

Язык не меняется → версия не растёт (правило 22).

## Итог (что сделано, 2026-07-18)

Обе задачи закрыты; фикс 0005-01 (Tier 1) закрыт **как следствие** миграции.

- **0060-01.** Общий слой в `semantic/enum_node.rs` (рядом с `EnumNode`):
  `EnumFacts { min, max, signed, min_bits }` + `enum_facts(variants) -> Option`.
  `signed = min < 0` (единая формула), `min_bits` — **точная** ширина (знаковое —
  доп. код, минимум 2 бита; беззнаковое — по `max`, `0 → 1`), `machine_bits()`
  округляет до {8,16,32,64} для `c`/`st`/`rust`. Пустое → `None`. **Ни одного
  имени типа цели** (правило 1 ADR). 6 юнит-тестов.
- **0060-02.** Четыре цели спрашивают факт и **только отображают**:
  `c` → `int{bits}_t` при `signed` (**это и есть фикс 0005-01**), `st` →
  `integer_type(machine_bits, signed)`, `rust` → `i8`/`u16`/…, `sv` →
  `(min_bits, signed)` напрямую. `variants_range` и все локальные извлечения
  диапазона удалены — грепом в `generator/` их **ноль** (A4). Пустое перечисление
  — поведение сохранено дословно (`c`/`st`/`rust` → 8 бит, `sv` → `SV-004`).
- **0060-03.** Потактовая сверка `conformance_c_tests`
  (`neg_enum_signedness_matches_generated_c`) на модели с отрицательным
  перечислением: `lv = -5`, `reached = 1`; мутация «вернуть расчёт по `max`»
  валит сверку **и** срывается на `-Werror=tautological-*`. `CLAUDE.md` поправлен
  (T13): `Enum` доверен лишь для неотрицательных диапазонов.

**Проверки:** 1971 тест зелёный, вывод **всех** целей на корпусе побайтово
прежний (A2 — в корпусе нет отрицательных перечислений; сильный сторож
рефакторинга), `cc` не выдаёт `-Wtautological` на фикстуре (A8), гейты
`c`/`st`/`rust`/`sv` зелёные. Известные факты сохранены (`670` → `u16`/`UINT`/
`uint16_t`/**10 бит**, T11 без правки ожиданий). Язык не менялся → версия не поднята.
