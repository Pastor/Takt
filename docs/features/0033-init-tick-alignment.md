# Фича 0033: Согласование тактов симулятора и порождённого C (INIT-такты)

- **Номер:** 0033
- **Статус:** **РАЗРАБОТКА** (ADR принят, анализ и тест-план готовы; код не начат)
- **Зависит от:** **нет** — проставлено аналитиком на стадии 3 (правило 17).
  Проверены 0025, 0026, 0020, 0012, 0041; жёстких зависимостей нет,
  `ЗАБЛОКИРОВАНА` **не ставится**. Связь с [0026](0026-c-root-typedef.md)
  (`typedef` корня) — **не блокирующая**: она сужает лишь потактовое покрытие
  моделей глубины 1 (критерий A3 / проверка T4), не мешая ни правке генератора,
  ни сверке на глубинах ≥ 2. Обоснование — в
  [анализе](../analyze/0033-init-tick-alignment.md), раздел «Зависимости фичи».
- **Связанные issue (анализ):** новая фича — дефект выявлен **автосверкой**
  [`0025-07`](../development/0025-07-c-conformance-test.md) при первом же прогоне
  (в бэклоге кандидатов `FEATURES.md` заведён как кандидат по правилу 7)
- **Крейт:** `grammar` (генератор C: `generator/c/c_model.rs`,
  `generator/c/c_header.rs`) + `simulation` (только тесты:
  `tests/conformance_c_tests.rs`)
- **Приоритет:** высокий (Tier 1) — бьёт в основную ценность проекта по правилу
  12: симуляция служит проверкой модели **до** синтеза, а трассы симулятора и
  прошивки смещены, поэтому потактовые свойства (время до первого воздействия на
  выход, проверки `--sim-file`, LTL-верификация) симуляцией **не проверяются**.
  Ниже 0025 (там функциональность не работала вовсе), но выше остальных
  кандидатов: фича снимает вынужденное сужение автосверки и задаёт контракт
  такта для будущих бэкендов — её следует закрыть **раньше**
  [0041](0041-st-backend.md) (бэкенд ST), иначе новый бэкенд унаследует те же
  синтетические такты.

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0033-init-tick-alignment.md`](../adr/0033-init-tick-alignment.md) — **Accepted** (Option B: генератор C не тратит такт на `INIT`) |
| Анализ | [`docs/analyze/0033-init-tick-alignment.md`](../analyze/0033-init-tick-alignment.md) — **готов** (факты Ф1–Ф5 по пробам, зависимости, R1–R10, A1–A14, декомпозиция) |
| Разработка | [`0033-01`](../development/0033-01-init-tick-alignment.md) генератор C · [`0033-02`](../development/0033-02-per-tick-conformance.md) потактовая автосверка · [`0033-03`](../development/0033-03-language-version-docs.md) версия языка и документация — **не начаты** |
| Тест-план | [`docs/tests/0033-init-tick-alignment.md`](../tests/0033-init-tick-alignment.md) — **готов** (T1–T20, примеры и контрпримеры, правило 16) |
| Отчёт о тестировании | [`docs/reports/0033-init-tick-alignment.md`](../reports/README.md) — предстоит |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0033-YY-*`) |

## Краткое описание

Симулятор и порождённый C исполняют тело стартового состояния в **разные такты**:
генератор C проводит модель через синтетические состояния `_INIT`, тратя **по
такту на каждый уровень иерархии** (проверено пробами: глубина 1 → тело на такте
2, глубина 2 → такт 3, глубина 3 → такт 4), тогда как симулятор исполняет его на
**такте 1**. Значения совпадают — расходится **моментность**, поэтому автосверка
`0025-07` вынужденно сравнивает установившиеся значения вместо потактовой трассы.

Фича устраняет сдвиг на стороне **генератора C** (ADR 0033, Option B): вход в
стартовое состояние перестаёт расходовать такт. Эталоном моментности признана
**модель**: `INIT`-такты не написаны автором, а являются бухгалтерией бэкенда, и
их число зависит от глубины вложенности — то есть структурный рефакторинг молча
сдвигает трассу. Симулятор не меняется. Меняется наблюдаемая семантика такта 1 →
рост версии языка `0.2.0` → `0.3.0` (правило 22).

> Фича зарегистрирована из бэклога кандидатов `FEATURES.md` (находка автосверки
> `0025-07`, вынесена за объём фичи 0025); далее проходит жизненный цикл по
> правилу 17.

### Уточнение исходной формулировки

Кандидат в `FEATURES.md` описывал **сдвиг на два такта**. Пробы на ветке `v2`
показали: два такта — частный случай двухуровневой фикстуры `conformance_u8.lam`,
на которой находка сделана. Общее правило — **такт на каждый уровень иерархии**
(уровень дают вложенная модель, параллель и конкатенация). Величина сдвига — не
константа, а функция формы модели; это и есть суть дефекта.

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->
