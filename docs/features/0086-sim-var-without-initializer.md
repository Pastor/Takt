# Фича 0086: `var q: u8;` без инициализатора → `SIM-009`

- **Номер:** 0086
- **Статус:** ГОТОВО (реализовано и проверено 2026-07-20; `precheck.sh` зелёный)
- **Зависит от:** **нет** (продолжение закрытой 0034 — не блокирует)
- **Приоритет / Tier:** **Tier 3** — дефект симулятора (объявленная переменная «не существует»)
- **Крейт:** `simulation` (`unit/builder.rs`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`); гэп [0034-04](0034-sim-struct-types.md)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`0086-sim-var-without-initializer.md`](../adr/0086-sim-var-without-initializer.md) |
| Анализ | [`0086-sim-var-without-initializer.md`](../analyze/0086-sim-var-without-initializer.md) |
| Разработка | [`0086-01`](../development/0086-01-default-scalar-value.md) (нулевой дефолт по типу) |
| Тест-план | [`0086-sim-var-without-initializer.md`](../tests/0086-sim-var-without-initializer.md) |
| Отчёт о тестировании | [`0086-sim-var-without-initializer.md`](../reports/0086-sim-var-without-initializer.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не понадобились) |

## Краткое описание

Объявление переменной **без инициализатора** даёт в симуляторе `SIM-009`
(«переменная не найдена») — то есть объявленная переменная для симулятора не
существует.

**Не структурный дефект** — [0034](0034-sim-struct-types.md) его не чинит (выявлен
её **контрольной** пробой, вне её объёма).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.

## Итог (что сделано)

Объявленная переменная без инициализатора теперь **существует** со значением по
умолчанию (нулевым по типу), а не даёт `SIM-009`. Принят **Option A** ADR 0086 —
прямое продолжение политики 0034 («нет init → ноль, как C default-init»),
распространённой со структур на **все** типы.

Правка — в `ModelNodeContext::get_value` (`simulation/src/unit/builder.rs`): ветка
«нет инициализатора» вместо `default_struct` (только структура) зовёт общий
`default_field(var_type(var), model)` — он строит нулевое значение для любого типа
(bool/rational/fixed/array/struct/целое). `default_struct` удалён как частный
случай (дублирование ушло). `Unresolved` (типа нет) остаётся `SIM-009` — верно.

Проверено **значенчески** (`var_without_initializer_defaults_to_zero` на фикстуре
`var_no_init.lam`: `u8`/`bit`→0, `q(8,8)`→repr 0, чтение в теле проходит); значения
захвачены зондом. Ловушка взведена: при откате фикса `unit.variable("q")` → `None`
→ тест падает. Регресса портов/констант нет (весь корпус симуляции, включая
conformance C/SV/rust/st, зелёный). Ноль согласуется с C default-init — вывод
генераторов **байт-в-байт неизменен**.

⚠️ **Массивы — вне объёма:** их исполнение — отдельная нерабочая фича
[0076](0076-sim-arrays.md) (`arr.N` — бит-доступ, не индекс); **регистрация**
массива нулей при этом уже работает. Версия языка не поднята (правка семантики
симулятора). `precheck.sh` зелёный; отчёт —
[`0086`](../reports/0086-sim-var-without-initializer.md).
