# Фича 0291: Решение «ребро безусловно» — у одного носителя

- **Номер:** 0291
- **Статус:** ГОТОВО
- **Зависит от:** нет
- **Tier:** 4 — ловушка, не действующий дефект: вход отсекает `SE-025` до генерации (⚠️ прежде стоял `1`, выведенный из текста кандидата и замером **не подтверждённый** — исправлено при закрытии 2026-08-18)
- **Класс:** генерация
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0291-rust-sv-unresolved-condition.md`](../adr/0291-rust-sv-unresolved-condition.md) |
| Анализ | [`docs/analyze/0291-rust-sv-unresolved-condition.md`](../analyze/0291-rust-sv-unresolved-condition.md) |
| Разработка | [`0291-01`](../development/0291-01-rust-sv-unresolved-condition.md) |
| Тест-план | [`docs/tests/0291-rust-sv-unresolved-condition.md`](../tests/0291-rust-sv-unresolved-condition.md) |
| Отчёт о тестировании | [`docs/reports/0291-rust-sv-unresolved-condition.md`](../reports/0291-rust-sv-unresolved-condition.md) |
| Исправления | не потребовались |

## Краткое описание

**Цели `rust` и `sv` считают неразрешённое условие отсутствием условия.**
Найдено при закрытии [0236](0236-c-unresolved-condition-refusal.md)
(2026-08-16): ветвь `ConditionNode::None | ConditionNode::Unresolved(_)` живёт
в `takt-lang/src/generator/rust/rust_tick.rs` (решение «есть ли безусловное
ребро») и `takt-lang/src/generator/sv/sv_fsm.rs`. В цели `c` такая же ветвь
давала **валидный вывод с другим автоматом** — переход срабатывал всегда;
0236 её изъяла, но объём фичи — цель `c`. ⚠️ Печатники условий этих целей
отказывают (`RS-011`, `SV-002`), поэтому дефект возможен лишь в решении «ребро
безусловное» — то есть ровно там, где печатник не зовётся. Кандидат: снять
`Unresolved` из обеих ветвей и завести тот же сторож.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Не требуется.** Язык не меняется: принимаемое множество программ то же,
наблюдаемое поведение действующих программ то же. Меняется поведение на входе,
который компилятор не принимает.

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->


## Итог (что сделано)

Вопрос «безусловно ли ребро» решает **один** предикат —
`ConditionNode::is_unconditional()`. Все восемь потребителей зовут его; ветвь,
считавшая `ConditionNode::Unresolved` отсутствием условия, снята у целей `rust`
и `sv` (у цели `c` её изъяла 0236). Новых диагностик не заводилось: неразрешённое
условие, дойдя до печатника, получает существующие `RS-011` и `SV-002`.

⚠️ **Копий правила оказалось восемь, а не две.** Кандидат называл `rust_tick.rs`
и `sv_fsm.rs`; сторож, написанный в этой же задаче, на **первом** прогоне нашёл
ещё три — `verification/kripke.rs` и два вхождения в
`semantic/validate/nondeterminism.rs`. Они были **верны**, но оставались
отдельными копиями одного правила: та же схема, по которой `rust` и `sv` и
разошлись с `c` (класс 0084/0193/0195).

⚠️ **`Tier 1` этой записи замером НЕ подтверждён.** Уровень выводился из текста
кандидата («валидный вывод с другим автоматом»). Прогон показал: неразрешённое
условие значит «имя не найдено», и такой вход отсекает **`SE-025`** до
генерации — поведение действующих программ не затронуто. Уровень исправлен на
`4`. Урок общий: **уровень, выведенный из чужого утверждения, проверяется
прогоном, как и всё остальное**; прочие записи `Tier 1`/`2` при взятии в работу
подлежат той же перепроверке.

⚠️ **Правку это не отменяет — речь о ловушке.** Недостижимость держит **чужая**
фича: `SE-025` не давала обещания охранять генераторы и перестанет прикрывать
ветвь при первом же изменении восстановления после ошибок. Цена снятия —
предикат и восемь вызовов; цена срабатывания — молчаливо другой автомат в
прошивке. Ровно этим доводом 0236 изъяла ту же ветвь у цели `c`, чья
недостижимость точно так же держалась `SE-025`.

⚠️ **Паттерн `S(Модель) = Состояние` под предикат не подпадает** — это
`Equal(Function(S, …), Unresolved(…))`, а не голый `Unresolved`; проверено
прогоном (ребро осталось условным, цели отвечают из печатника).

Версия крейта `takt-lang`: `0.40.0` → `0.41.0`. Версия языка **не растёт**.

Артефакты: [ADR](../adr/0291-rust-sv-unresolved-condition.md) ·
[анализ](../analyze/0291-rust-sv-unresolved-condition.md) ·
[0291-01](../development/0291-01-rust-sv-unresolved-condition.md) ·
[тест-план](../tests/0291-rust-sv-unresolved-condition.md) ·
[отчёт](../reports/0291-rust-sv-unresolved-condition.md).
