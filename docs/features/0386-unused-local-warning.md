# Фича 0386: `SE-036` видит локальные объявления тел

- **Номер:** 0386
- **Статус:** ГОТОВО
- **Зависит от:** нет (0376, 0264 закрыты)
- **Связанные issue (анализ):** кандидат, заведённый закрытием
  [0376](0376-unused-local-stub.md)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0386-unused-local-warning.md`](../adr/0386-unused-local-warning.md) |
| Анализ | [`docs/analyze/0386-unused-local-warning.md`](../analyze/0386-unused-local-warning.md) |
| Разработка | [`docs/development/0386-01-unused-local-warning.md`](../development/0386-01-unused-local-warning.md) |
| Тест-план | [`docs/tests/0386-unused-local-warning.md`](../tests/0386-unused-local-warning.md) |
| Отчёт о тестировании | [`docs/reports/0386-unused-local-warning.md`](../reports/0386-unused-local-warning.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

`SE-036` («переменная объявлена, но нигде не используется») проверяло
объявления **модели**. Локальную переменную тела, ни разу не использованную, не
замечал никто: замер 2026-08-22 на `always { var spare: u8 := n + 5; … }` дал
у `taktc` нулевой код возврата и молчание — при том, что вывод целей `c` и
`rust` спасает заглушка [0376](0376-unused-local-stub.md), то есть молчаливая
правка за автора.

Признак у проверки уже был (`unused_locals`, 0376) — не хватало **места**, где
его звать, и **координаты**: у `StatementNode::Variable` позиции не было вовсе.

## Документирование (правило 24)

**Не требуется.** Язык не меняется: диагностика та же (`SE-036`, её описание в
приложении «Ошибки и предупреждения» верно и сейчас), синтаксис и семантика
прежние. Расширяется область срабатывания уже описанного предупреждения.

## Итог (что сделано)

- `SE-036` проверяет теперь и **локальные объявления тел**: блоки модели, блоки
  состояний, тела функций и вложенные блоки. Прежде о мёртвой переменной автор
  не узнавал ниоткуда — вывод целей `c` и `rust` правила заглушка 0376, то есть
  инструмент молча правил за автора.
- ⚠️ **Позиция — часть предмета:** `StatementNode::Variable` получил четвёртым
  полем `Location`, иначе предупреждение печаталось бы с `1:1` — ложью вместо
  отсутствия координаты (класс 0264). Правка задела 28 мест, все назвал
  компилятор; попутно снята граница, названная самой 0264.
- ⚠️ **Знание одно:** признак переехал в `semantic::unused::unused_locals_of_block`
  (имя + позиция), а `generator::local_stub` стал обёрткой — идиома заглушки
  осталась у целей.
- ⚠️ **Доставка правок не потребовала:** проверка входит в общую точку
  `collect_model_warnings` (0081/0232), которую зовут и CLI, и LSP.
- ⚠️ **Сторож пришлось усилить:** первая редакция покрывала одно место
  объявления из трёх, и мутация «снять обход блоков модели» её не роняла.
- Шума нет: прогон по всему корпусу дал ноль новых предупреждений.

Детали — в [отчёте](../reports/0386-unused-local-warning.md).
