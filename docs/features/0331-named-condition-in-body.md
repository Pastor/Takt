# Фича 0331: Именованное условие в теле переводят все цели

- **Номер:** 0331
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-20)
- **Связанные issue (анализ):** новая фича — класс найден чтением текстов
  отказов цели `st` и пробой

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0331-named-condition-in-body.md`](../adr/0331-named-condition-in-body.md) |
| Анализ | [`docs/analyze/0331-named-condition-in-body.md`](../analyze/0331-named-condition-in-body.md) |
| Разработка | [`docs/development/0331-01-named-condition-in-body.md`](../development/0331-01-named-condition-in-body.md) |
| Тест-план | [`docs/tests/0331-named-condition-in-body.md`](../tests/0331-named-condition-in-body.md) |
| Отчёт о тестировании | [`docs/reports/0331-named-condition-in-body.md`](../reports/0331-named-condition-in-body.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

`if ready { … }` (где `ready` — объявленное `cond`) давало **пять** разных
ответов, включая невалидный C. Теперь именованное условие в теле печатается тем
же печатником условий, что и на ребре.

## Документирование (правило 24)

**Не требуется:** форма уже описана как условие; состав диагностик прежний.

## Итог (что сделано)

**Одно имя условия ведёт себя одинаково в обеих позициях.**

- **Замер 2026-08-20:** эталон — `SIM-014` **в такте**; `c`/`c-hal` печатали
  `if (COND_…)` — идентификатор, который цель **нигде не определяет**, то есть
  файл не собирался при нулевом коде возврата; `st` — `ST-011` с текстом,
  обещавшим «часть 2 задачи 0041-04»; `rust` — `RS-011`; `sv` — `SV-002`;
  `plantuml` принимал.
- **Решение:** каждый потребитель зовёт **свой** печатник (или вычислитель)
  условий — тот же, что на ребре. Своего разбора условия не заводится.
- ⚠️ **У цели `rust` понадобилась вторая правка:** `expression_type` не знал
  `Condition`, и приведение к `bool` отвечало «тип не выводится». Именованное
  условие логическое **по построению**.
- ⚠️ **Мёртвая функция снята:** `condition_macro_name` печатала имя макроса,
  который никто не определял (урок 0278 — под `pub` мёртвое доживает).
- ⚠️ **Класс найден чтением ТЕКСТОВ отказов**, как и 0330: обещание «часть 2
  задачи 0041-04» пережило свою задачу.
- ⚠️ **Ловушка сторожа:** имя модели фикстуры содержит `COND`, и наивная
  проверка «в выводе нет `COND_`» падала на верном выводе; ищется
  использование (`if (COND`).

Детали — в [отчёте](../reports/0331-named-condition-in-body.md).
