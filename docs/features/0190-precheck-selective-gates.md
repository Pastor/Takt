# Фича 0190: Разделение предкоммита на компоненты и выборочный запуск

- **Номер:** 0190
- **Статус:** СОЗДАНА
- **Зависит от:** нет
- **Tier:** 2
- **Связанные issue (анализ):** заведена **по запросу заказчика 2026-07-31**;
  продолжает [0090](0090-ci-precheck.md) (единый источник истины гейтов) и
  [0027](0027-module-size-split.md) (гейты как храповик)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0190-precheck-selective-gates.md`](../adr/0190-precheck-selective-gates.md) |
| Анализ | [`docs/analyze/0190-precheck-selective-gates.md`](../analyze/0190-precheck-selective-gates.md) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0190-YY-*`) |
| Тест-план | [`docs/tests/0190-precheck-selective-gates.md`](../tests/README.md) |
| Отчёт о тестировании | [`docs/reports/0190-precheck-selective-gates.md`](../reports/README.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0190-YY-*`) |

## Краткое описание

`scripts/precheck.sh` — единственный вход всех проверок проекта (909 строк, ~20
гейтов) и **единственный источник истины** для CI (фича 0090). Цена этого
устройства: прогон целиком идёт **порядка двадцати минут** и запускается ровно
так же, менялся ли весь конвейер семантики или одна строка документа. Правка,
затрагивающая один слой, ждёт verilator, yosys, `iec2c`, сборку Cargo и два
прогона генерации на детерминизм.

Требуется разложить предкоммит на **именованные компоненты** с возможностью
запустить подмножество, не теряя главного свойства: **полный прогон остаётся
умолчанием и обязателен перед коммитом**, а любое сужение — громкое.

> Фича заведена **по запросу заказчика 2026-07-31** в ходе работы над
> [0187](0187-port-io-redesign.md): цикл «правка → проверка» стал заметно
> дороже самой правки.

## Что известно на входе

- **Гейты уже сгруппированы визуально, но не структурно.** В скрипте различимы
  блоки: документные проверки (`check-links.py`, глифы, `check-claude-md.py`,
  статусы фич), `fmt`/`check`/`clippy`, `cargo test --all-features`, формат
  примеров, генерация всех целей, гейт воспроизводимости (два прогона × цели),
  гейт `float`-профилей, цель `rust` (rustc + clippy + сборка Cargo + прогон
  моделей), цель `sv` (verilator + yosys + тестбенчи), `sv-mmio`, цель `c`
  (cmake + `cc -c` для `c-hal`), исполнение харнессов, гейт `iec2c`, гейт
  примеров `book/`. Каждый блок — линейный участок кода, разделяемый без
  переписывания логики.
- **Механизм выбора уже частично есть:** `PRECHECK_STRICT` (фича 0090) и
  `require_tool` дают единую точку «гейт выполняется / пропущен». Пропуск при
  этом **печатается** — тот же приём годится и для намеренного сужения.
- **Стоимость по стадиям не измерена.** Известно только суммарное время
  (порядка 20 минут на рабочей машине). Что в нём занимает `cargo test`, что —
  yosys, что — двойная генерация, **никто не мерил**; оптимизировать без замера
  значит гадать.
- **Кэша нет вовсе.** Каждый прогон повторяет всё, даже если ни один вход
  соответствующего гейта не изменился (например, `book/` не тронут, а гейт
  примеров документа отрабатывает 25 файлов).
- ⚠️ **Ровно этот класс боли фича 0187 закрывает осью 5.** Молчаливо суженное
  покрытие неотличимо от пройденного — гейт `c-hal` пропускал два примера из
  одиннадцати, и боль оси 1 прожила в корпусе незамеченной. Всякое ускорение
  через «не проверять» обязано быть **громким**, иначе фича 0190 воспроизведёт
  дефект, который 0187 устраняет.

## Направление решения (наметка, не решение)

Прорабатываются варианты по осям:

1. **Ось «состав»** — как назвать и выделить компоненты: по слою (документы /
   линты / тесты / кодоген / гейты целей), по цели генерации (`c`, `rust`, `sv`,
   `st`), по инструменту (быстрые без внешних зависимостей / медленные внешние).
2. **Ось «выбор»** — как запускать подмножество: флаги (`--only sv`,
   `--skip book`), отдельные скрипты-компоненты с тонким `precheck.sh` поверх,
   переменные окружения (как сегодняшний `PRECHECK_STRICT`).
3. **Ось «автоматика»** — выводить ли набор гейтов из изменённых путей
   (`git diff --name-only`). Соблазнительно и опасно: правка `semantic/`
   затрагивает **все** цели, и наивное сопоставление «путь → гейт» дало бы
   зелёный прогон при непроверенном изменении.
4. **Ось «кэш»** — пропуск гейта, входы которого не изменились (отпечаток
   содержимого), вместо пропуска по требованию пользователя.
5. **Ось «параллельность»** — независимые гейты (verilator, yosys, `iec2c`,
   cmake) могут идти одновременно; выигрыш без потери покрытия, но вывод
   перестаёт быть линейным и диагностика усложняется.

**Инвариант, не подлежащий торгу:** в CI (`PRECHECK_STRICT=1`) выполняется
**весь** набор, никакого выбора и никакого кэша — иначе `precheck.sh` перестаёт
быть единым источником истины (фича 0090), ради которого он и заведён.

⚠️ Первая работа стадии анализа — **замер по стадиям**. Без него неизвестно,
даст ли разделение выигрыш вообще: может оказаться, что 80 % времени съедают
две стадии (`cargo test --all-features` и сборка Cargo порождённого Rust), и
дешевле ускорить их, чем строить механизм выбора.

## Документирование (правило 24)

**Не требуется:** фича не меняет язык — ни синтаксиса, ни семантики, ни
диагностик, ни поведения целей. Затронут процесс разработки, поэтому обновляются
`docs/RULE.md` (если изменится обязательная команда перед коммитом), `CLAUDE.md`
(раздел «Команды») и `.github/workflows/ci.yml`.
