# Фича 0340: Присваивание агрегата — место записи выбирает общий носитель

- **Номер:** 0340
- **Статус:** ГОТОВО
- **Зависит от:** 0330, 0293, 0320 (закрыты; проставлено 2026-08-20)
- **Связанные issue (анализ):** найдено пробой «функция возвращает структуру»

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0340-aggregate-assign-place.md`](../adr/0340-aggregate-assign-place.md) |
| Анализ | [`docs/analyze/0340-aggregate-assign-place.md`](../analyze/0340-aggregate-assign-place.md) |
| Разработка | [`docs/development/0340-01-aggregate-assign-place.md`](../development/0340-01-aggregate-assign-place.md) |
| Тест-план | [`docs/tests/0340-aggregate-assign-place.md`](../tests/0340-aggregate-assign-place.md) |
| Отчёт о тестировании | [`docs/reports/0340-aggregate-assign-place.md`](../reports/0340-aggregate-assign-place.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

`body := {3, 4};` при `body: Point` не переводила **ни одна** цель, кроме
`rust`: `c` печатала форму, которой в C нет вовсе, `st` и `sv` адресовали
структуру как **массив**. Всё — при нулевом коде возврата `taktc`.

## Документирование (правило 24)

**Не требуется** — подтверждено: правится представление у трёх целей.

## Итог (что сделано)

**Правило «куда писать элемент агрегата» стало общим носителем.**

- **Массив — по индексу, структура — по имени поля**, в порядке **объявления**:
  агрегат позиционный (0034), и сортировка развела бы значения по чужим полям.
- **Носитель принимает поля, а не модель:** цели хранят объявления по-разному
  (`ModelNode` у `st` и `c`, снимок карты у `sv`), и он отвечает за правило
  выбора формы, а не за поиск объявления.
- **Цель `c` получила разворот в печатнике операторов:** из выражения
  поэлементная запись невыразима — нужны несколько операторов.
- ⚠️ **Комментарий фичи 0330 был неверен.** Он утверждал, что поэлементная
  форма «совпадает с тем, что печатает цель `c`»; замер показал, что цель `c`
  печатала агрегат как есть и её вывод не собирался. Утверждение о **чужом**
  коде проверяется прогоном, а не чтением (класс 0292).
- ⚠️ **Массив в C не присваивается** даже составным литералом — оттого форма
  одна на оба случая.
- ⚠️ **Контрольный вход (массив) обязателен:** без него правка читалась бы как
  «агрегат печатается по имени поля всегда».

Детали — в [отчёте](../reports/0340-aggregate-assign-place.md).
