# Фича 0341: Вложенная структура — порядок объявления и тип поля

- **Номер:** 0341
- **Статус:** ГОТОВО
- **Зависит от:** 0293, 0340 (закрыты; проставлено 2026-08-20)
- **Связанные issue (анализ):** найдено пробой «вложенная структура и массив
  структур»

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0341-nested-struct-order.md`](../adr/0341-nested-struct-order.md) |
| Анализ | [`docs/analyze/0341-nested-struct-order.md`](../analyze/0341-nested-struct-order.md) |
| Разработка | [`docs/development/0341-01-nested-struct-order.md`](../development/0341-01-nested-struct-order.md) |
| Тест-план | [`docs/tests/0341-nested-struct-order.md`](../tests/0341-nested-struct-order.md) |
| Отчёт о тестировании | [`docs/reports/0341-nested-struct-order.md`](../reports/0341-nested-struct-order.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

`struct Line { a: Point, b: Point }` не переводила **ни одна** цель: три
печатали структуры по алфавиту (а `Line` стоит раньше `Point`), четвёртая
считала доступ к полю логическим. Всё — при нулевом коде возврата `taktc`.

## Документирование (правило 24)

**Не требуется** — подтверждено: правится представление у четырёх целей.

## Итог (что сделано)

**Порядок объявления стал общим носителем; доступ к полю перестал быть bool.**

- **Правило одно на три цели:** вложенная структура объявляется раньше
  вмещающей. Носитель `generator/struct_order.rs`, обход в глубину, порядок
  детерминирован (обход по `BTreeMap`, инвариант 0048).
- **Алфавитная сортировка на выходе у `st` снята:** она разрушала собранный
  порядок.
- **У цели `rust` `expression_type` различает разряд и поле:** `x.7` → `Bool`,
  `s.field` → `None` (тип неизвестен, приведение не применяется). Прежде обе
  формы давали `bool`, и `seg.a.x := 7;` отвергался как «значение 7 не
  представимо в bool».
- ⚠️ **Тест 0148 закреплял дефект:** он утверждал, что поля структур цель `rust`
  не переводит, — а она переводит с фичи 0293; отказ приходил от неверного типа.
  Переписан (класс 0191).
- ⚠️ **Ловушка замера:** первая проба назвала переменную `ln`, и `iec2c`
  отвергал файл **по другой причине** — `LN` есть стандартная функция IEC.
  Посторонняя причина снята переименованием (правило 30), а попутно выяснилось,
  что список зарезервированных имён цели `st` **неполон** — вынесено кандидатом
  с готовым замером.
- ⚠️ **Цикл структур** носитель переживает (печатает участника один раз), но не
  диагностирует: это вопрос семантики — вынесено кандидатом.

Детали — в [отчёте](../reports/0341-nested-struct-order.md).
