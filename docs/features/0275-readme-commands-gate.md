# Фича 0275: Команды в README.md никем не проверяются

- **Номер:** 0275
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-19)
- **Tier:** 3 — README вёл читателя в тупик полтора десятка фич (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** прочее
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0275-readme-commands-gate.md`](../adr/0275-readme-commands-gate.md) |
| Анализ | [`docs/analyze/0275-readme-commands-gate.md`](../analyze/0275-readme-commands-gate.md) |
| Разработка | [`0275-01`](../development/0275-01-readme-commands-gate.md) (гейт и сторож), [`0275-02`](../development/0275-02-readme-commands-gate.md) (карта адресов получила свой номер файла) |
| Тест-план | [`docs/tests/0275-readme-commands-gate.md`](../tests/0275-readme-commands-gate.md) |
| Отчёт о тестировании | [`docs/reports/0275-readme-commands-gate.md`](../reports/0275-readme-commands-gate.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0275-YY-*`) |

## Краткое описание

**Команды в `README.md` никем не проверяются.** Замер при закрытии
[0239](0239-install-script.md) (2026-08-17): раздел сборки
предлагал `cargo install --path grammar --bin taktc` — команду, не работающую
с фичи **0100** (крейт переименован в `takt-lang`), то есть полтора десятка
фич подряд документ вёл читателя в тупик; симулятор в разделе установки не
упоминался вовсе. Гейт ссылок (правило 14) этого не ловит **по устройству**:
он проверяет пути к файлам, а не работоспособность вызовов. Кандидат: прогон
помеченных блоков `sh` из README в предкоммите — по образцу гейта примеров
документа (фича 0133). ⚠️ Прогонять можно не всё: часть команд ставит
инструменты и требует сети; помечать пригодные явным маркером.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Не требуется** (определено на стадии анализа 2026-08-19). README — предмет
проверки, а не правки: все его команды оказались верными, кроме той, что падала
по вине инструмента. Язык не затронут.

## Итог (что сделано)

**Команды README проверяются машиной, и первый же прогон нашёл отказ
инструмента.** Гейт `scripts/check-readme-commands.sh` делает три вещи:
проверяет пути, названные флагами `--path`/`--manifest-path` и вызовами
`scripts/*.sh`; сверяет подкоманду `taktc` с инструментом; **прогоняет** те
команды `taktc`, чьи входные файлы существуют (сегодня их пять из 55 строк).
Сторож `scripts/test-readme-commands.sh` доказывает мутацией на копии дерева,
что негодная команда ловится, а годная — нет.

**Найденный дефект устранён здесь же** — иначе гейт не включить:
`taktc compile -t st-at --address-map …` роняло компилятор паникой
«byte index … is not a char boundary». Карта адресов разбиралась под номером
файла **корневого `.takt`**, поэтому предупреждение `SE-050` печаталось по
тексту модели: на латинице — ложная координата (класс 0264), на кириллице —
паника. Теперь карта регистрируется в реестре и разбирается под своим номером,
а печать выдаёт `examples/elevator.plc.map:14:1`. Вторым рубежом `line_column`
приводит смещение к границе символа: печать диагностики не вправе падать
никогда.

Сделано задачами [0275-01](../development/0275-01-readme-commands-gate.md) и
[0275-02](../development/0275-02-readme-commands-gate.md); подробности — в
[отчёте](../reports/0275-readme-commands-gate.md), разбор вариантов — в
[ADR](../adr/0275-readme-commands-gate.md).

**Ключевые решения:**

- **Прогонять не всё** (Option A ADR отвергнут): команды внешних инструментов
  требуют сети и софта, а часть команд намеренно показана с плейсхолдерами.
  Граница названа в заголовке гейта, а контрольная проверка сторожа требует,
  чтобы плейсхолдер гейт **не ронял**.
- **Явная разметка блоков отвергнута** (Option C): забытый маркер молча
  выключает проверку — тот же ручной труд, что и был.
- **Симптом и корень правились раздельно:** укрепление печати не отменяет
  починки номера файла, иначе координата осталась бы чужой.

**Живая проверка:** `./scripts/precheck.sh` — код 0, оба новых шага в выводе
(«строк проверено 55, команд компилятора прогнано 5»); 3316 тестов, 0
провалов.

