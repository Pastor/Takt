# Фича 0194: Симулятор теряет model-level `always` у модели-композиции

- **Номер:** 0194
- **Статус:** ГОТОВО
- **Зависит от:** нет
- **Tier:** 1
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md` (вскрыт пробой
  задачи [0185-07](../development/0185-07-model-parameters.md)); тот же класс,
  что [0083](0083-model-always-block-c.md) (тело терялось везде) и фикс
  [0181-01](../fixes/0181-01-sim-parallel-always-twice.md) (тело исполнялось
  дважды)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0194-sim-composition-model-always.md`](../adr/0194-sim-composition-model-always.md) — принят Option A (решение заказчика) |
| Анализ | [`docs/analyze/0194-sim-composition-model-always.md`](../analyze/0194-sim-composition-model-always.md) |
| Разработка | [`0194-01`](../development/0194-01-sim-composition-model-always.md) · [`0194-02`](../development/0194-02-sim-composition-model-always.md) |
| Тест-план | [`docs/tests/0194-sim-composition-model-always.md`](../tests/0194-sim-composition-model-always.md) |
| Отчёт о тестировании | [`docs/reports/0194-sim-composition-model-always.md`](../reports/0194-sim-composition-model-always.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0194-YY-*`) |

## Краткое описание

Фича [0083](0083-model-always-block-c.md) установила: тело `always` на уровне
модели исполняется **каждый такт** — в эталоне и во всех целях. У модели, чьё
состояние есть композиция, эталон его **не исполняет вовсе**, а цель `c`
исполняет. Расхождение молчаливое: обе стороны работают и рапортуют успех.

> Фича зарегистрирована из бэклога кандидатов (решение заказчика 2026-08-02:
> Tier-1 находки переводятся в фичи вперёд Tier 2); далее проходит жизненный
> цикл по правилу 17.

## Что известно на входе

Перепроверено пробой 2026-08-02, находка жива. Вход:
`var probe: u8 := 0; model Tuner { … } always { probe := probe + 1; } start Main = Tuner | Tuner;`

| Сторона | Результат |
|---|---|
| эталон | `probe = 0` на всех тактах |
| цель `c` | `model->probe = model->probe + 1;` в теле такта |

- То же с **именованной** моделью-композицией (`start Main = Tuner \| Tuner;`
  внутри `model Root2 { … always { … } }`).
- У модели со **своими** состояниями (`always { … } start S { … }`) блок
  исполняется верно — то есть дефект в ветви, где такт модели уходит в
  композицию, и тело модели-владельца до исполнения не доезжает.
- ⚠️ Корпус класс **не покрывает**: `always` на уровне модели есть только у
  моделей со своими состояниями.
- ⚠️ Урок фикса 0181-01: тела корпуса идемпотентны (`x := <выражение>`), и на
  них лишнее либо пропущенное исполнение **неразличимо**. Сторож обязан стоять
  на **накапливающем** теле (`probe := probe + 1`).

## Итог (что сделано)

Эталон исполняет тело `always` модели-владельца, чьё состояние есть композиция,
— **ровно раз за такт**, как и все четыре цели:

| Форма | Было | Стало |
|---|---|---|
| `start Main = A \| B` | `n = 0` на всех тактах | `1, 2, 3, 4` |
| именованная модель-композиция | `0` | `1, 2, 3` |
| `start Main = A + B` | `0` | `1, 2, 3, 4` |

Принят Option A (ADR; рекомендация архитектора и решение заказчика совпали):
наполнить `executions` узла композиции блоками владельца при построении.
⚠️ Механизм исполнения менять не пришлось — он **был на месте**: `tick_body`
звал `execution("always")` для любого вида юнита, а `Unit::execution` читал поле
и у `Parallel`, и у `Sequential`. Поле было объявлено, читалось — и никогда не
наполнялось; этим и объясняется, почему дефект пережил фичу 0083 и фикс 0181-01.

Компиляция `named_blocks` **вынесена** в `takt-sim/src/unit/blocks.rs` и теперь
одна на обоих строителей: копия правила разъехалась бы при первой правке.
Контекст владельца строится **один раз** и отдаётся и ветвям, и телу — иначе
запись ушла бы в другой экземпляр `ModelNodeContext`, и починка молча не
сработала бы.

⚠️ «Ровно одно исполнение» получено **по построению** (блок кладётся в один юнит
и в детей не спускается) и доказано **значением**: рост на единицу, а не на
двойку — иначе вернулся бы дефект-близнец фикса 0181-01. Сторожа стоят на
**накапливающем** теле; на идемпотентном оба дефекта неразличимы.

Отвергнут Option B (строить композицию как `Node`): он снял бы класс целиком,
но перестраивает стык, правленный фичей 0181 совсем недавно, а трасса,
`state_io` и viewport завязаны на нынешнюю форму дерева.

Детали: [ADR](../adr/0194-sim-composition-model-always.md),
[анализ](../analyze/0194-sim-composition-model-always.md),
[тест-план](../tests/0194-sim-composition-model-always.md). Исправлений
(`docs/fixes/`) не потребовалось.

## Направление решения (наметка, не решение)

- Правка эталона (`takt-sim`), а не целей: контракт 0083 уже принят, и цель `c`
  ему следует.
- ⚠️ Осторожно с направлением ошибки: соседний фикс
  [0181-01](../fixes/0181-01-sim-parallel-always-twice.md) снял **двойное**
  исполнение `always` у композиции (`Unit::execution` в дочерние юниты не
  спускается). Починка обязана дать ровно **одно** исполнение за такт — и это
  надо доказать значением, а не фактом наличия кода.
- Сторож — значенческая сверка sim ≡ `c` на накапливающем теле, по образцу
  `takt-sim/tests/sim/sequential_composition_tests.rs`.

⚠️ Окончательный выбор — за стадиями **архитектуры** (ADR) и **анализа**.

## Документирование (правило 24)

**Не требуется.** Правило «model-level `always` исполняется каждый такт» уже
описано и не меняется — фича приводит эталон в соответствие с ним. Если анализ
покажет, что для композиции правило требует **уточнения** формулировки, раздел
о композиции придётся освежить; решение принимается на стадии анализа.
