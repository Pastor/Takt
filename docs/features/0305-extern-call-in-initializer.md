# Фича 0305: Вызов внешней функции в инициализаторе — SE-084

- **Номер:** 0305
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-20)
  `нет` — параметр проставляет аналитик на стадии анализа, правило 17>
- **Связанные issue (анализ):** <T-… или «новая фича»>

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0305-extern-call-in-initializer.md`](../adr/0305-extern-call-in-initializer.md) |
| Анализ | [`docs/analyze/0305-extern-call-in-initializer.md`](../analyze/0305-extern-call-in-initializer.md) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0305-YY-*`) |
| Тест-план | [`docs/tests/0305-extern-call-in-initializer.md`](../tests/README.md) |
| Отчёт о тестировании | [`docs/reports/0305-extern-call-in-initializer.md`](../reports/README.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0305-YY-*`) |

## Краткое описание

<2–4 предложения по сути фичи: что делаем и зачем.>

> Фича зарегистрирована <источник: из бэклога кандидатов / из процессного
> бэклога / по запросу>; далее проходит жизненный цикл по правилу 17.

## Документирование (правило 24)

**Не требуется** для языковых разделов: конструкция не вводится и не изымается,
текст `SE-084` в приложении «Ошибки» не менялся.

## Итог (что сделано)

**Молчаливая потеря инициализатора у целей `st`/`st-at` устранена.**

- **`SE-084` на вызов `extern`-функции в инициализаторе** — третий случай в
  **том же** обходе, что `SE-099` (ячейка) и `SE-113` (порт): правило одно —
  «инициализатор не читает того, чего до первого такта нет», — и обход обязан
  быть один (урок 0203). Текст называет функцию и штатный путь
  (`always { x := f(); }`).
- **Замер 2026-08-20:** до правки один вход давал `mirror = 0` **молча** у
  эталона, `CC-023`/`RS-011`/`SV-002` у четырёх целей и **код 0 с потерянным
  инициализатором** у `st`/`st-at`. После — у всех `SE-084`.
- ⚠️ **Кандидат называл форму неверно:** «вызов функции» вычисляется, если
  функция **локальная** (`var x := seed();` работает у всех) — предмет только
  внешняя.
- ⚠️ **Форм внешней функции в ячейке две:** на стадии 2 лежит
  `Unresolved(FunctionDefine { external: true, … })`, а не `External`. Первая
  редакция проверяла только вторую и не ловила ничего.
- ⚠️ **Побочное следствие названо:** причина вердикта верификации
  `InitialValueUnknown` осталась без достижимых входов и помечена **защитной**
  (как `DomainNotEnumerable` в 0258); её тест переписан так, чтобы проверять
  **причину** недостижимости.

**Ломающее изменение** (обосновано в ADR): запись, которую `st` принимала,
теряя смысл, теперь отвергается; общего поведения у неё не существует. В
корпусе таких записей нет.

Детали — в [отчёте](../reports/0305-extern-call-in-initializer.md); решение и
отвергнутые варианты — в [ADR](../adr/0305-extern-call-in-initializer.md).
