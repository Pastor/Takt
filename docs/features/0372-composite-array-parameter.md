# Фича 0372: Составной элемент массива в параметре функции

- **Номер:** 0372
- **Статус:** ГОТОВО
- **Зависит от:** нет (0348, 0363, 0367, 0369 закрыты)
- **Связанные issue (анализ):** кандидат, заведённый закрытием
  [0369](0369-sv-array-parameter.md)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0372-composite-array-parameter.md`](../adr/0372-composite-array-parameter.md) |
| Анализ | [`docs/analyze/0372-composite-array-parameter.md`](../analyze/0372-composite-array-parameter.md) |
| Разработка | [`docs/development/0372-01-composite-array-parameter.md`](../development/0372-01-composite-array-parameter.md) |
| Тест-план | [`docs/tests/0372-composite-array-parameter.md`](../tests/0372-composite-array-parameter.md) |
| Отчёт о тестировании | [`docs/reports/0372-composite-array-parameter.md`](../reports/0372-composite-array-parameter.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

Фича 0369 научила цель `sv` передавать массив **скаляров** плоским вектором, а
массив с составным элементом остался прежним. Замер 2026-08-21 (`probe.sh`)
на трёх видах элемента:

| Вход | эталон, `c`, `rust` | `st`, `st-at` | `sv`, `sv-mmio` |
|---|---|---|---|
| `fn f(a: [Cell; 2])` — структура | исполняют | `iec2c` принял · `cc` собрал | **yosys НЕ синтезировал** |
| `fn f(a: [Mode; 2])` — перечисление | исполняют | `iec2c` принял · `cc` собрал | **yosys НЕ синтезировал** |
| `fn f(a: [[u8; 2]; 2])` — массив | исполняют | **`iec2c` ОТВЕРГ** | **yosys НЕ синтезировал** |

Всё — при **нулевом** коде возврата `taktc`.

- У цели `sv` порт функции печатался распакованным (`input cell_t a [0:1]`):
  «input/output/inout ports cannot have unpacked dimensions» — тот же отказ,
  что снимала 0369, только `flat_param_width` отдавала `None`, потому что
  ширина составного элемента не скалярна.
- У цели `st` имя типа-формы (0348) строилось из **текста** типа элемента, и
  для вложенного массива получалось `TAKT_ARR_2_ARRAY_[0..1]_OF_USINT` —
  идентификатор со скобками и точками, невыразимый в IEC.

## Документирование (правило 24)

**Не требуется** — язык не меняется: записи законны и исполняются эталоном;
правится представление у двух целей.

## Итог (что сделано)

- Массив со **структурой**, **перечислением** и **вложенным массивом** в
  параметре функции у цели `sv` передаётся плоским вектором и синтезируется.
- Имя типа-формы у цели `st` строится по **размерностям и базовому типу**
  (`TAKT_ARR_2_2_USINT`), а не по тексту типа элемента; форма имени согласована
  с объявлением одним носителем.
- ⚠️ **Раскладка плоского вектора считается ПО ЛИСТЬЯМ** (общий обход 0367):
  один порядок обслуживает и сигнатуру, и пролог, и аргумент. Вывод для массива
  скаляров остался **байт-в-байт** прежним.
- ⚠️ **Класс видит один инструмент из трёх** у цели `sv` (только yosys) и
  только полный цикл `iec2c` → `cc` у цели `st`.
- Вывод корпуса не изменился.

Детали — в [отчёте](../reports/0372-composite-array-parameter.md).
