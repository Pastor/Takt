# Фича 0368: Элемент агрегата печатается по типу элемента

- **Номер:** 0368
- **Статус:** ГОТОВО
- **Зависит от:** нет (0061, 0096, 0300, 0343, 0366 закрыты)
- **Связанные issue (анализ):** класс найден пробами 2026-08-21 (инструмент
  фичи [0362](0362-probe-target-tools.md))

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0368-aggregate-element-type.md`](../adr/0368-aggregate-element-type.md) |
| Анализ | [`docs/analyze/0368-aggregate-element-type.md`](../analyze/0368-aggregate-element-type.md) |
| Разработка | [`docs/development/0368-01-aggregate-element-type.md`](../development/0368-01-aggregate-element-type.md) |
| Тест-план | [`docs/tests/0368-aggregate-element-type.md`](../tests/0368-aggregate-element-type.md) |
| Отчёт о тестировании | [`docs/reports/0368-aggregate-element-type.md`](../reports/0368-aggregate-element-type.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

Замер 2026-08-21 — две записи, у которых **скалярный** аналог работает у всех
девяти потребителей:

| Вход | Ответ |
|---|---|
| `var gains: [q(8, 8); 2] := {1.5, 2.5};` | `c`/`c-hal`: `cc -Werror` — «implicit conversion from 'double' to 'int16_t' changes value from 1.5 to 1»; `rust`: `E0308`; `sv`: `SV-002` |
| `var modes: [Mode; 2] := {Idle, Work};` плюс `modes[0] := Work;` | `rust`: `E0308` (`[0, 1]` в поле `[Mode; 2]`); `sv`: `%Error-ENUMVALUE` |

Контроль: `var gain: q(8, 8) := 1.5;` и `var mode: Mode := Idle;` переводят и
исполняют **все** потребители — то есть дело не в типе, а в том, что элемент
агрегата свой тип теряет.

Корней три, все — «тип элемента известен, но не применён»:

1. **Понижение q-литерала** (`lower_folded_fixed`, семантика) смотрело только
   на скалярный тип.
2. **Печать агрегата у цели `rust`** знала лишь один тип элемента —
   структуру (фича 0343).
3. **Тип приёмника у цели `sv`** при записи в элемент массива возвращался
   `None`.

## Документирование (правило 24)

**Не требуется** — язык не меняется: записи законны и исполняются эталоном;
правится представление у целей и место понижения литерала.

## Итог (что сделано)

- Элемент агрегата отвечает так же, как скаляр того же типа: понижение
  q-литерала идёт поэлементно, печать массива у `rust` — по типу элемента для
  **любого** типа, а тип приёмника у `sv` при записи в элемент — тип элемента.
- ⚠️ **Место понижения — свёртка инициализаторов, а не вывод типов.** Первая
  правка стояла в `lower_fixed_var`, выполнялась и **терялась**: свёртка
  подменяет значение сырым АСД, а её защита `is_literal` агрегат не покрывает.
  Показал это отладочный прогон; мёртвая ветвь снята (урок 0278).
- ⚠️ **Частный случай маскировал класс:** ветвь «массив структур» у цели
  `rust` (0343) выглядела полной, а была одной из четырёх нужных.
- ⚠️ Бит-вектор `[bit;N≤64]` исключён (правило 0078); вывод корпуса не
  изменился — массивов `q` и перечислений в `examples/` нет.
- **Кандидаты:** массив в параметре функции у цели `sv` (yosys не
  синтезирует); поле `q` внутри структуры (свёртка не видит полей).

Детали — в [отчёте](../reports/0368-aggregate-element-type.md).
