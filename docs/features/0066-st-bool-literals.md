# Фича 0066: Булевы литералы в телах цели `st` печатаются числом

- **Номер:** 0066
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (генератор `generator/st/st_expr.rs`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0066-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0066-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0066-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0066-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0066-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0066-YY-*`) |

## Краткое описание

Тела цели `st` печатают `cmd_fork := 0;` вместо `cmd_fork := FALSE;`. MatIEC
принимает `0`/`1` для `BOOL` (проверено пробой — **вопреки ожиданию плана
0041-04**), поэтому это **не дефект компилируемости**.

Беда в другом: **объявления** тот же генератор печатает канонично
(`flag : BOOL := TRUE;`), то есть вывод **сам с собой не согласован**.

Лечение: печатник выражений должен знать **целевой тип присваивания** — сейчас он
его не получает (`st_expr::print_expression` типа назначения не видит).

Выявлено при закрытой [0041](0041-st-backend.md).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
