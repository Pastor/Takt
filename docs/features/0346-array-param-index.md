# Фича 0346: Индексация параметра-массива внутри функции

- **Номер:** 0346
- **Статус:** ГОТОВО
- **Зависит от:** 0210 (закрыта; проставлено 2026-08-20)
- **Связанные issue (анализ):** найдено пробой «константа-структура, разряд
  элемента массива, массив в параметре»

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0346-array-param-index.md`](../adr/0346-array-param-index.md) |
| Анализ | [`docs/analyze/0346-array-param-index.md`](../analyze/0346-array-param-index.md) |
| Разработка | [`docs/development/0346-01-array-param-index.md`](../development/0346-01-array-param-index.md) |
| Тест-план | [`docs/tests/0346-array-param-index.md`](../tests/0346-array-param-index.md) |
| Отчёт о тестировании | [`docs/reports/0346-array-param-index.md`](../reports/0346-array-param-index.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

`fn first(a: [u8;2]) -> u8 { return a[0]; }` давал `SE-003` «Переменная 'a' не
найдена»: параметр был виден всюду, **кроме индексации**. Отказ приходил из
семантики — то есть язык принимал объявление, которое не работало.

## Документирование (правило 24)

**Не требуется** — подтверждено: правится разрешение имени, а не правило языка;
массив в параметре документ уже описывает.

## Итог (что сделано)

**Параметр стал виден и при индексации.**

- **Ветвь `ArraySubscript` спрашивала только таблицу модели** и в список
  параметров не заглядывала. Теперь имя ищется сперва среди параметров — тот же
  приоритет, что в разрешении идентификатора.
- ⚠️ **Дефектов было два:** параметры не передавались и в разбор **индекса**
  (рекурсивный вызов шёл с `vec![]`), поэтому `a[i]` с параметром-индексом
  ломался по той же причине. Срез получил ту же правку.
- ⚠️ **Отказ был семантическим**, то есть общим для эталона и всех восьми
  целей: расхождения не было, ломался **язык**.
- ⚠️ **Контроли обязательны:** неизвестное имя по-прежнему `SE-003`, индексация
  не массива — `SE-030`.
- ⚠️ **Проба маскировала три вопроса сразу:** отказ приходил раньше остальных
  предметов файла; снятие причин по одной (правило 30) отделило предмет от
  попутчиков, и те стали кандидатами с готовым замером.

Детали — в [отчёте](../reports/0346-array-param-index.md).
