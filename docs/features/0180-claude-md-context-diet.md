# Фича 0180: Сокращение живого контекста CLAUDE.md

- **Номер:** 0180
- **Статус:** ГОТОВО
- **Зависит от:** <номера фич-зависимостей через запятую, напр. `0007, 0027`, или
  `нет` — параметр проставляет аналитик на стадии анализа, правило 17>
- **Связанные issue (анализ):** <T-… или «новая фича»>

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0180-claude-md-context-diet.md`](../adr/0180-claude-md-context-diet.md) |
| Анализ | [`docs/analyze/0180-claude-md-context-diet.md`](../analyze/0180-claude-md-context-diet.md) |
| Разработка | [`0180-01`](../development/0180-01-claude-md-context-diet.md) |
| Тест-план | [`docs/tests/0180-claude-md-context-diet.md`](../tests/README.md) |
| Отчёт о тестировании | [`docs/reports/0180-claude-md-context-diet.md`](../reports/README.md) |
| Исправления | не потребовались |

## Краткое описание

<2–4 предложения по сути фичи: что делаем и зачем.>

> Фича зарегистрирована <источник: из бэклога кандидатов / из процессного
> бэклога / по запросу>; далее проходит жизненный цикл по правилу 17.

## Документирование (правило 24)

<Меняет ли фича ЯЗЫК (синтаксис/семантику/наблюдаемое поведение/диагностики/
поведение целей)? Если да — какой раздел [`book/`](../../book/) затронут и какие
изменения нужно внести (что добавить/переписать, какие примеры на Takt). Если нет
(рефакторинг/инфраструктура/процесс) — «не требуется» с кратким обоснованием.
В документ не попадают детали реализации, номера фич и процесс разработки.>

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->


## Итог (что сделано)

**Живой контекст сокращён вдвое без потери знания.** Раздел «Технические
подводные камни» переписан в формате **инвариант → цена нарушения → сторож →
ссылка** и разбит на семь тематических подразделов: **859 → 335** строк, файл
целиком **1194 → 674** (−44 %). С `RULE.md` и `CODE.md` сессия читает ~1280 строк
вместо ~1800.

⚠️ **Сокращение — перенос, а не удаление, и это проверено, а не предположено.**
Выборка из 12 инвариантов: **12 из 12** найдены в артефактах стадий. Два факта
нашлись **не в карточке** (`VariableNode` в двух представлениях — в
`development/0096-02` и отчёте; `lam_q_floordiv` — только в отчёте 0061), поэтому
слепое правило «ссылаться на карточку» их потеряло бы. Порядок работы (ADR):
найти артефакт **грепом** → при отсутствии сперва дописать → только потом
сокращать.

⚠️ **Заодно сверен остаток:** 18 символов, кодов диагностик и имён сторожевых
тестов из оставшихся утверждений найдены в дереве. Объём и недостоверность —
одна проблема: гейт 0149 показал, что самая длинная запись оказалась самой
неверной.

**Что сознательно не сокращено:** разделы «Архитектура» и «Команды» (живой
контекст в чистом виде); пункты про цели `sv`/`rust` и `float`→`q` ужаты меньше
прочих — их ловушка не сводится к одной фразе, а инвариант без механизма читался
бы как произвольный запрет.

**Механический предел строк не введён** (A-2 ADR): он вытеснял бы то, что
писалось последним, а не то, что лишнее. Формат держит врезка в самом разделе,
достоверность — гейт [0149](0149-claude-md-consistency-gate.md).

Детали: [отчёт](../reports/0180-claude-md-context-diet.md),
[ADR](../adr/0180-claude-md-context-diet.md),
[анализ](../analyze/0180-claude-md-context-diet.md).
