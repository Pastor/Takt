# Фича 0198: Форматтер выносит комментарий из тела блока наружу

- **Номер:** 0198
- **Статус:** ГОТОВО
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу 2026-08-03 (запрос заказчика)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0198-formatter-comment-in-block.md`](../adr/0198-formatter-comment-in-block.md) — принят Option A (решение заказчика) |
| Анализ | [`docs/analyze/0198-formatter-comment-in-block.md`](../analyze/0198-formatter-comment-in-block.md) |
| Разработка | [`0198-01`](../development/0198-01-formatter-comment-in-block.md) · [`0198-02`](../development/0198-02-formatter-comment-in-block.md) |
| Тест-план | [`docs/tests/0198-formatter-comment-in-block.md`](../tests/0198-formatter-comment-in-block.md) |
| Отчёт о тестировании | [`docs/reports/0198-formatter-comment-in-block.md`](../reports/0198-formatter-comment-in-block.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0198-YY-*`) |

## Краткое описание

Форматтер выносит комментарий из тела блока наружу. Подробности находки, замеры и оговорки — в разделе «Что известно на
входе»; форма решения определяется стадиями архитектуры и анализа.

> Фича зарегистрирована **из бэклога кандидатов** (запрос заказчика 2026-08-03:
> перевести кандидатов в фичи без проработки); далее проходит жизненный цикл по
> правилу 17.

## Что известно на входе

> Фича заведена **из кандидата** блока 2 `FEATURES.md` (запрос заказчика
> 2026-08-03: перевести кандидатов в фичи без проработки). Проработка —
> стадии 2–3 жизненного цикла; ниже — запись кандидата **как есть**, вместе с
> замерами и оговорками, сделанными при его заведении.
>
> ⚠️ Замеры кандидата **не перепроверялись** при переводе в фичу: их
> актуальность подтверждает пробой стадия архитектуры (урок 0195 — записи
> кандидатов устаревают, и две из них оказались неверны).

**Класс:** синтаксис языка (Tier 1 — приоритет по решению заказчика
2026-08-03: синтаксис → семантика → генерация → остальное).

**Форматтер выносит комментарий из тела блока наружу.** Вскрыто при разработке
задачи [0186-04](../development/0186-04-book-processor-example.md)
(2026-07-30). Проба в четыре строки: комментарий, стоящий **первой строкой
внутри** `always { … }`, после `taktc fmt` оказывается **после** закрывающей
скобки — то есть привязывается к следующему элементу состояния (`ref`). Текст
не теряется, теряется **место**: в примерах раздела 20 пояснение к телу стало
читаться как пояснение к переходу. ⚠️ Это не «синоним, который не
канонизируется» (`while`/`loop`, `Guard`), а перемещение авторского
комментария — форматтер обязан сохранять его позицию либо честно отказывать.
Обход в примерах: комментарий ставится **перед** `always`. Корпус класс не
ловит: в `examples/` комментарии внутри тел стоят не первой строкой.

## Документирование (правило 24)

**Не требуется.** Фича не меняет ни синтаксис, ни семантику, ни поведение целей
— она чинит инструмент. Правила расстановки комментариев в `book/` не описаны и
описываться не должны: документ описывает **язык**, а не форматтер.

## Итог (что сделано)

Комментарий сохраняет **место** при форматировании — во всех телах:

| Форма | Было | Стало |
|---|---|---|
| первой строкой в `always` | уезжал за `}`, к следующему элементу | на месте |
| между операторами тела | уезжал | на месте |
| последней строкой тела | уезжал | на месте |
| во вложенном `if` | всплывал **через два уровня** | на своей глубине |
| в теле функции | уезжал **за пределы функции** | на месте, перед своим оператором |

⚠️ **Дефект оказался шире записи находки**: она называла только «комментарий
первой строкой внутри `always`», а уезжает любой комментарий из любого тела.
Форма записи ни при чём — граница проходит там, где кончается печать элементов
и начинается печать операторов.

⚠️ **Причина — не сломанный механизм, а незадействованный.** `Out::node_line`
существует, и его док-строка называет себя «единственным путём печати
содержательных строк, так комментарий не может потеряться»; печатник операторов
звал `out.line` в обход. Комментарий **последней строкой** тела остался
отдельным случаем: ведущих ему никто не выдаст, понадобился явный вызов перед
`}` (`close_body` + `Out::comments_before`).

⚠️ **Мутация вскрыла слабый сторож — мой собственный.** Тест «тело функции»
проверял только отступ и пропускал дефект: при печати в обход привязки
комментарий остаётся внутри тела, но встаёт **после** своего оператора. Усилен
проверкой порядка строк. Урок: у форматтера отступ доказывает «внутри тела», но
не «на своём месте».

Корпус не сдвинулся ни на строку — правка не изменила форматирование
существующих файлов.

Детали: [ADR](../adr/0198-formatter-comment-in-block.md),
[анализ](../analyze/0198-formatter-comment-in-block.md),
[отчёт](../reports/0198-formatter-comment-in-block.md). Исправлений
(`docs/fixes/`) не потребовалось.

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->

