# Фича 0230: Сторож форматтера: корпус восстановлен, KNOWN_GAPS с ратчетом

- **Номер:** 0230
- **Статус:** ГОТОВО
- **Зависит от:** нет незакрытых (опирается на закрытые [0024](0024-lam-formatter.md), [0100](0100-language-rename-takt.md), [0199](0199-model-implements-brace-form.md))
- **Связанные issue (анализ):** **два** кандидата — «список известных пробелов протух» (замер [0229](0229-format-unsupported-position.md)) и «сторож форматтера съёжился до одного каталога» (замер [0202](0202-fmt-diagnostic-formatting.md)); взяты в работу заказчиком 2026-08-05

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0230-format-corpus-sentinel.md`](../adr/0230-format-corpus-sentinel.md) |
| Анализ | [`docs/analyze/0230-format-corpus-sentinel.md`](../analyze/0230-format-corpus-sentinel.md) |
| Разработка | [`0230-01`](../development/0230-01-format-corpus-sentinel.md) |
| Тест-план | [`docs/tests/0230-format-corpus-sentinel.md`](../tests/0230-format-corpus-sentinel.md) |
| Отчёт о тестировании | [`docs/reports/0230-format-corpus-sentinel.md`](../reports/0230-format-corpus-sentinel.md) |
| Исправления | [`0199-01`](../fixes/0199-01-fmt-drops-model-implement.md) — дефект печати, найденный этой фичей |

## Краткое описание

Корпусный тест форматтера обещал прогон «по всему корпусу», а видел **один
каталог из трёх**: два были переименованы фичей 0100, и обход при ошибке чтения
молча возвращался. Рядом протух список известных пробелов — он обещал пробелы,
которых давно нет. Фича восстанавливает обход, делает его громким и заводит
ратчет списка. Цена молчания измерена сразу: первый прогон нашёл дефект, при
котором форматтер **терял реализацию модели** и печатал неразбираемую форму
(фикс [0199-01](../fixes/0199-01-fmt-drops-model-implement.md)).

> Фича зарегистрирована из бэклога кандидатов (взята заказчиком 2026-08-05);
> далее проходит жизненный цикл по правилу 17.

## Документирование (правило 24)

**Не требуется.** Языка фича не касается: `book/` описывает язык, а не устройство
тестов. Наблюдаемое поведение инструмента меняет только фикс 0199-01 — но он
приводит печать к форме, которую документ уже описывает (раздел о композиции
моделей): прежняя печать выдавала запись, которой в языке нет.

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->


## Итог (что сделано)

Сторож форматтера снова сторожит: **279** файлов вместо 158.

**Тест `format_tests.rs`:** каталоги корпуса восстановлены (`examples`,
`takt-lang/tests/data`, `takt-sim/tests/data`); обход **отказывает** на
нечитаемом каталоге, называя путь; у `KNOWN_GAPS` появился **ратчет** — запись,
которой не соответствует ни один отказ на корпусе, валит тест (образец —
реестр размеров модулей). Сам список пуст: непокрытых узлов ноль.

**Найдено и исправлено:** [0199-01](../fixes/0199-01-fmt-drops-model-implement.md)
— форматтер терял реализацию модели (`model M = A | B { … }` → `model M { … }`) и
печатал «короткую форму» `model M = A;`, которой грамматика не принимает. Оба
симптома жили в той половине корпуса, которую тест не видел.

**Мутации:** возврат каждой из трёх причин (потеря реализации, протухшая запись,
пропавший каталог) валит **ожидаемую** проверку; третья воспроизводит исходный
дефект и прежде давала зелёный тест.

**Язык не изменён** (`0.8.0`); печать форматтера не расширена — объём был сторож.

**Закрыты два кандидата** `FEATURES.md` разом. Подробности — в
[отчёте](../reports/0230-format-corpus-sentinel.md).
