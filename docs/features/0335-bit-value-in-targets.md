# Фича 0335: Разряд в позиции числового значения переводят все цели

- **Номер:** 0335
- **Статус:** ГОТОВО
- **Зависит от:** 0250, 0157, 0293 (закрыты; проставлено 2026-08-20)
- **Связанные issue (анализ):** найдено пробой соседнего класса (замер остатка
  от отрицательного, 2026-08-20)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0335-bit-value-in-targets.md`](../adr/0335-bit-value-in-targets.md) |
| Анализ | [`docs/analyze/0335-bit-value-in-targets.md`](../analyze/0335-bit-value-in-targets.md) |
| Разработка | [`docs/development/0335-01-bit-value-in-targets.md`](../development/0335-01-bit-value-in-targets.md) |
| Тест-план | [`docs/tests/0335-bit-value-in-targets.md`](../tests/0335-bit-value-in-targets.md) |
| Отчёт о тестировании | [`docs/reports/0335-bit-value-in-targets.md`](../reports/0335-bit-value-in-targets.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

`high := src.7;` при `high: u8` — эталон и цель `c` дают `1`, а `rust`, `st` и
`sv` порождали вывод, который **отвергают чужие инструменты**: `E0308`,
«Incompatible data types for ':=' operation», `WIDTHEXPAND`. Код возврата
`taktc` при этом **нулевой**.

## Документирование (правило 24)

**Не требуется** — подтверждено: семантика разряда описана; правится
представление у трёх целей.

## Итог (что сделано)

**Разряд, попавший в позицию числа, приводится к типу приёмника.**

- **Причина одна на три цели:** разряд печатается **булевым** выражением —
  верным в условии и невалидным в присваивании числу. Знание о позиции есть
  только у приведения к типу приёмника, туда правило и встало.
- **Формы** — таблица в [ADR](../adr/0335-bit-value-in-targets.md); форма для
  `st` выбрана **пробой** `iec2c`, для `sv` — та же размерная, какой печатается
  широкий литерал (0157).
- ⚠️ **Битовый приёмник не трогается**, и контрольные проверки на это
  обязательны: иначе правило читалось бы как «разряд всегда число», и цель
  перестала бы собираться в другую сторону.
- ⚠️ **Скобки у `rust` снимаются:** `u8::from((…))` — это `unused_parens`, то
  есть отказ сборки порождённого кода под `-D warnings`.
- ⚠️ **Арифметика с разрядом вне объёма:** `src.7 + 1` эталон **не исполняет**
  (`SIM-005`), а цели считают молча — соседний класс, вынесен кандидатом.
- ⚠️ **Корпус класс не покрывает** — записи «разряд в числовую переменную» в
  `examples/` нет ни одной, поэтому молчали оба гейта; сторожа фикстурные, с
  прогоном настоящих `rustc`, `iec2c`, `verilator`.
- ⚠️ **У `sv` предметом остаётся линт:** `W'(…)` дополняет беззнаковое нулями по
  определению языка, а тестбенч `verilator --binary` стоил бы минут предкоммита
  (замер 0241). Граница названа, а не забыта.

Детали — в [отчёте](../reports/0335-bit-value-in-targets.md).
