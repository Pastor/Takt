# Фича 0165: Подкоманда `taktc version`

- **Номер:** 0165
- **Статус:** ГОТОВО
- **Зависит от:** нет
- **Tier:** 3
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md` (выявлено при закрытии [0085](0085-language-version-constant.md); Option B ADR 0085)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0165-taktc-version-subcommand.md`](../adr/0165-taktc-version-subcommand.md) |
| Анализ | [`docs/analyze/0165-taktc-version-subcommand.md`](../analyze/0165-taktc-version-subcommand.md) |
| Разработка | [`0165-01`](../development/0165-01-taktc-version-subcommand.md) |
| Тест-план | [`docs/tests/0165-taktc-version-subcommand.md`](../tests/0165-taktc-version-subcommand.md) |
| Отчёт о тестировании | [`docs/reports/0165-taktc-version-subcommand.md`](../reports/0165-taktc-version-subcommand.md) |
| Исправления | не потребовались |

## Краткое описание

Версия языка есть в коде (`takt_lang::LANGUAGE_VERSION`), но `taktc` её **не
печатает**. CLI-контракт был бы чище, чем гейт-греп исходника, и полезен
пользователю. Заблокировано лимитом размера модуля.

> Фича зарегистрирована из бэклога кандидатов (отобрана заказчиком 2026-07-28); далее проходит жизненный цикл по правилу 17.

## Что известно на входе

- `taktc.rs` пришпилен к baseline размера (`scripts/module-size-baseline.txt`) —
  расти ему нельзя (правило размера модулей, фича
  [0027](0027-module-size-split.md)), а диспетч подкоманды его растит.
- Прецедент выноса: `address-map` → `run_export_subcommand` в библиотеке.
- Гейт `scripts/check-language-version.sh` сегодня сверяет **греп исходника** с
  README.

## Направление решения (наметка, не решение)

- Сначала — запас в `taktc.rs`: вынести часть логики в библиотеку (как
  `address-map`).
- Затем `taktc version` (печать `LANGUAGE_VERSION` + `CARGO_PKG_VERSION`).
- Затем переключить `check-language-version.sh` на сверку **вывода CLI** вместо
  грепа исходника.
- ⚠️ Смежно с [0149](0149-claude-md-consistency-gate.md): тот же гейт версии.

⚠️ Окончательный выбор — за стадиями **архитектуры** (ADR) и **анализа**: раздел
выше фиксирует то, что уже проверено, а не предрешает реализацию.

## Документирование (правило 24)

**Требуется.** Появляется новая подкоманда CLI — затронут раздел
[`book/src/17-tools/`](../../book/src/17-tools/index.typ) (инструменты).

## Итог (что сделано)

`taktc version` (а также `--version` и `-V`) печатает две строки: версию
компилятора и версию языка. Обе читаются из кода — литералов версии в новом коде
нет, иначе появился бы второй источник истины, ровно тот, против которого
заведена константа (ADR 0085). Логика — в библиотеке
(`takt_lang::version::version_text`), бинарник остаётся диспетчером: так тестом
проверяется **вывод**, а не факт вызова, а синонимы разрешаются в одном месте.

Гейт `check-language-version.sh` **не тронут**: сверять вывод CLI (направление,
намеченное карточкой) значило бы поставить дешёвый ранний гейт в зависимость от
собранного бинарника. Контракт вывода держит тест крейта — он ближе к коду и не
двигает порядок предкоммита.

⚠️ **Блокировка из карточки оказалась устаревшей.** «Заблокировано лимитом
размера модуля; `taktc.rs` пришпилен к baseline» было верно на 2026-07-28, но к
моменту работы файл имел **784** строки при лимите 1000 и в
`module-size-baseline.txt` не значился — запас 216 строк. Предварительный вынос
логики, стоявший в карточке первым шагом, не понадобился. Урок: условия
карточки перепроверяются на стадии архитектуры, а не наследуются.

⚠️ **Пример в документе едва не устарел при рождении:** первая редакция несла
`taktc 0.38.0`, а версия крейта растёт каждой фичей — и `book/` гейтом версии не
проверяется. Показан **формат**, а не числа.

⚠️ **Гейт глифов отверг значок `⚠️`** (`U+26A0` вне шрифта Fira Code) — в книге
принят другой приём, врезка.

Артефакты: [ADR](../adr/0165-taktc-version-subcommand.md) ·
[анализ](../analyze/0165-taktc-version-subcommand.md) ·
[0165-01](../development/0165-01-taktc-version-subcommand.md) ·
[тест-план](../tests/0165-taktc-version-subcommand.md) ·
[отчёт](../reports/0165-taktc-version-subcommand.md).
