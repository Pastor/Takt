# Фича 0399: Вывод типа операнда живёт четырьмя носителями

- **Номер:** 0399
- **Статус:** АНАЛИЗ
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-22)
- **Tier:** 4 — расставлен по признаку вреда (шкала — в блоке 1 `FEATURES.md`)
- **Класс:** генерация
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-22

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0399-operand-type-single-carrier.md`](../adr/0399-operand-type-single-carrier.md) |
| Анализ | [`docs/analyze/0399-operand-type-single-carrier.md`](../analyze/0399-operand-type-single-carrier.md) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0399-YY-*`) |
| Тест-план | [`docs/tests/README.md`](../tests/README.md) |
| Отчёт о тестировании | [`docs/reports/README.md`](../reports/README.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0399-YY-*`) |

## Краткое описание

- **Вывод типа операнда живёт ЧЕТЫРЬМЯ носителями.** Замер
  [0371](0371-fixed-cast-from-field.md) (2026-08-21): тип
  операнда выводят `semantic::extract_type` (её зовёт цель `c`),
  `rust_fixed::expression_type`, `sv_fixed::fixed_format` и
  `st_operand_type::inner_expr_type` — и все четыре расходились в том, знают
  ли они поле структуры. Из-за этого одна правка чинила одну цель, а класс
  «приведение без масштабирования» жил у четырёх сразу. ⚠️ Сведение к одному
  носителю сняло бы целый класс расхождений, но потребует общего вывода типа
  выражения, доступного печатникам (у `rust` и `sv` модели нет — есть снимки).
  ⚠️ **Пятой копии больше нет:** [0382](0382-fixed-literal-in-place.md)
  свела приватный `cond_base_type` (тип места в условии) к общему носителю
  `semantic::validate::base_type` — там же живут `SE-061`, `SE-028`/`SE-030`,
  печать среза и понижение q-литерала. ⚠️ Внутри **семантики** носителей
  по-прежнему два: `base_type` (тип **места**, консервативный) и
  `extract_type` (тип **любого выражения**) — они не дублируют друг друга по
  назначению, но на цепочке `переменная(.поле | [индекс])*` отвечают одно и то
  же, и это тоже предмет сведения.

> Фича зарегистрирована из блока кандидатов `FEATURES.md` **без проработки**
> (решение заказчика 2026-08-22: «завести фичи из кандидатов без проработки»).
> Текст кандидата перенесён **дословно**, вместе с замером и его датой: замер
> есть свидетельство, а пересказ его теряет. Стадии 2 и 3 не пройдены —
> зависимости и объём **не оценивались**; далее фича проходит жизненный цикл по
> правилу 17.

## Документирование (правило 24)

**Не требуется.** Предмет — внутреннее устройство генераторов; вывод и
поведение не меняются (любое изменение вывода — признак ошибки переноса).

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->

