# Фича 0158: Запуск компилятора и симулятора из IntelliJ (Run Configurations)

- **Номер:** 0158
- **Статус:** ГОТОВО
- **Зависит от:** нет
- **Tier:** 3
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md` (вынесено закрытием [0125](0125-intellij-takt-lsp-tooling.md), Option C ADR отвергнут в объёме)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0158-intellij-run-configurations.md`](../adr/0158-intellij-run-configurations.md) |
| Анализ | [`docs/analyze/0158-intellij-run-configurations.md`](../analyze/0158-intellij-run-configurations.md) |
| Разработка | [`0158-01`](../development/0158-01-intellij-run-configurations.md) |
| Тест-план | [`docs/tests/0158-intellij-run-configurations.md`](../tests/0158-intellij-run-configurations.md) |
| Отчёт о тестировании | [`docs/reports/0158-intellij-run-configurations.md`](../reports/0158-intellij-run-configurations.md) |
| Исправления | не потребовались |

## Краткое описание

Фича 0125 завела в настройки плагина пути к `taktc`/`takt-sim`, дополнительные
параметры компилятора и выходную директорию — но **как задел**: они хранятся и
**не исполняются**. Фича-преемник — собственно запуск из IDE.

> Фича зарегистрирована из бэклога кандидатов (отобрана заказчиком 2026-07-28); далее проходит жизненный цикл по правилу 17.

## Что известно на входе

- Настройки плагина существуют и сохраняются; исполняющего кода за ними нет.
- Смешение с LSP-редактированием раздуло бы 0125 — объём вынесен сознательно.
- Плагин **вне** `precheck.sh`/CI: верифицируется только запуском IDE.

## Направление решения (наметка, не решение)

- Run Configuration API IntelliJ: конфигурации «Compile» и «Simulate»,
  использующие уже имеющиеся настройки.
- Разбор вывода инструментов в проблемы IDE (формат позиции —
  `diagnostics::position_prefix`, `путь:строка:колонка`) + консоль.
- ⚠️ Колонка в диагностике — **в символах**, не в байтах (в `.takt` есть
  кириллица): разбор обязан это учесть.

⚠️ Окончательный выбор — за стадиями **архитектуры** (ADR) и **анализа**: раздел
выше фиксирует то, что уже проверено, а не предрешает реализацию.

## Документирование (правило 24)

**Не требуется.** Инструмент-плагин; язык не меняется.

## Итог (что сделано)

В IDE появились конфигурации запуска: группа «Takt» с фабриками **Compile**
(`taktc compile`) и **Simulate** (`takt-sim`). Настройки фичи 0125 — пути к
инструментам, каталоги импортов, дополнительные флаги, выходной каталог —
начали **исполняться**; до этого они лишь хранились, и обещание 0125 оставалось
невыполненным.

**Логика вынесена из GUI.** Сборка командной строки — чистая функция
`TaktCommandLine.build` без `Project`, `Editor` и сервисов: плагин лежит вне
`precheck.sh`, и проверить Gradle-тестом можно ровно то, что не требует окна.
Тесты закрепляют порядок аргументов (файл последним, `-o` перед ним), отсутствие
пустых аргументов и повторяемый `-I` — разделитель списка у компилятора
платформозависим, и второе знание о нём в плагине заводить нельзя.

⚠️ **Ловушка, названная в карточке, подтвердилась.** Колонка диагностики Takt
считается в **символах**, а документ IDEA адресуется кодовыми единицами UTF-16:
на символе вне BMP (эмодзи в комментарии) наивное «колонка − 1» уводит ссылку не
туда. Пересчёт делает `TaktOutputFilter.charColumnToOffset`; мутация «наивный
счёт» валит сторож.

⚠️ **Две ошибки первой редакции были в ожиданиях тестов, а не в коде** — я
неверно посчитал колонки. Исправлены ожидания; поведение под тест не
подгонялось.

Тесты плагина: 84 → **97**, зелёные.

⚠️ **Одна проверка осталась за человеком** (названа в отчёте): что конфигурации
видны в IDE и действительно запускают инструменты. Юнит-тесты окна IDE не
поднимают, поэтому доказана верность командной строки, а не факт запуска.

Артефакты: [ADR](../adr/0158-intellij-run-configurations.md) ·
[анализ](../analyze/0158-intellij-run-configurations.md) ·
[0158-01](../development/0158-01-intellij-run-configurations.md) ·
[тест-план](../tests/0158-intellij-run-configurations.md) ·
[отчёт](../reports/0158-intellij-run-configurations.md).
