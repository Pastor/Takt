# Фича 0059: Общие переменные корня → структура `Shared` вместо параметров по одной

- **Номер:** 0059
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (генератор `generator/rust/`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0059-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0059-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0059-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0059-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0059-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0059-YY-*`) |

## Краткое описание

Цель `rust` передаёт общие переменные корня под-модели **по одной**
(`rust_needs::FnNeeds`, корневые переменные идут `&mut`-параметрами). У
`MovementController` (`stacker.lam`) их девять, поэтому такт получает 10
параметров и упирается в порог `clippy::too_many_arguments` (7). Арность диктует
**модель**, а не стиль, поэтому линт сегодня заглушён точечно на самом методе.
Системное лечение — свернуть общие переменные в структуру `Shared` и передавать
`&mut Shared`.

Вскрыто закрытой [0050](0050-rust-backend.md).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
