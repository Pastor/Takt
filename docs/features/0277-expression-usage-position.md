# Фича 0277: Отказ цели указывает на объявление переменной, а не на использование

- **Номер:** 0277
- **Статус:** ГОТОВО
- **Зависит от:** [0264](0264-body-judge-usage-position.md) — закрыта (проставлено на стадии 3)
- **Tier:** 3 — отказ цели указывает на объявление, а не на использование (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** семантика
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0277-expression-usage-position.md`](../adr/0277-expression-usage-position.md) |
| Анализ | [`docs/analyze/0277-expression-usage-position.md`](../analyze/0277-expression-usage-position.md) |
| Разработка | [`0277-01`](../development/0277-01-expression-usage-position.md) |
| Тест-план | [`docs/tests/0277-expression-usage-position.md`](../tests/0277-expression-usage-position.md) |
| Отчёт о тестировании | [`docs/reports/0277-expression-usage-position.md`](../reports/0277-expression-usage-position.md) |
| Исправления | не потребовались |

## Краткое описание

**Отказ цели указывает на объявление переменной, а не на использование.**
Замер там же: `res := mem[1:2];` в строке 7 даёт `CC-022` с координатой `2:1` —
местом объявления `mem`. Причина: позиции **использования** у
`ExpressionNode` нет ни у одного варианта (решение 0056 — не тащить её через
~40 вариантов ради одного потребителя), а `ArraySlice(var, …)` берёт `loc`
переменной. Ссылка на модель, тип и список параметров позиции не имеют вовсе
(`Location::Builtin`) — сообщение остаётся без префикса. Кандидат: решить,
нужна ли `ExpressionNode` позиция использования, и если да — вводить её вместе
с потребителями (цели `st`, `rust`, `sv` строят отказы с `Location::Codegen` и
выиграли бы тоже).

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Определяется на стадии анализа.** Фича затрагивает язык или наблюдаемое
поведение целей, поэтому нужный раздел `book/` называется после проработки —
до неё утверждение о документе было бы догадкой.

## Итог (что сделано)

**Отказ цели `c` указывает на строку употребления.** Прежде координата бралась у
`ExpressionNode::loc()`, а тот выводит её из **объявлений** операндов:
`res := mem[1:2];` в строке 7 давал `1:1` — строку, где объявлена `mem`.

**Сделано:** общий носитель `generator/site.rs::StatementSite` — печатник
операторов объявляет место (`enter`), построитель отказа берёт его (`at`).
Позицию оператора дала [0264](0264-body-judge-usage-position.md); фича лишь
доносит её до цели, **не меняя** ни `ExpressionNode` (решение 0056 в силе), ни
сигнатуры рекурсивных печатников — карта и так доходит до каждого.

**Правило приоритета — оператор вернее выражения**, и это следствие замера:
«своя» позиция выражения выведена из объявления и указывает не туда; вне
оператора носитель пуст, и остаётся своя — там она настоящая.

**Сторожа:** 3 юнит-теста носителя и 3 теста поведения
(`takt-lang/tests/targets/c_refusal_position_tests.rs`), включая контроль
(отказ вне оператора чужой координаты не занимает) и проверку главного риска
изменяемого состояния — координата **следует** за текущим оператором, а не
залипает на первом.

⚠️ **Границы объёма названы:** цели `rust`, `st`, `sv` печатают отказ **без**
координаты — это честная пустота, а не ложь; приём готов к переносу, случай
вынесен кандидатом. `CC-023` недостижим из корректной программы (0236).

**Версия языка не поднята**; крейт `takt-lang`: `0.49.0` → `0.49.1`.

Детали — [отчёт](../reports/0277-expression-usage-position.md), решение —
[ADR](../adr/0277-expression-usage-position.md).
