# Фича 0532: Язык сообщений компилятора и симулятора (i18n)

- **Номер:** 0532
- **Статус:** СОЗДАНА
- **Зависит от:** <проставляет аналитик на стадии анализа (правило 17)>
- **Tier:** <шкала «признак вреда» — проставляется в анализе>
- **Класс:** прочее (инструменты)
- **Связанные issue (анализ):** запрос заказчика 2026-09-04

## Стадии жизненного цикла (правило 17)

Все стадии — **разделы этой карточки** (правило 32): «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог». Раздел
заводится в момент прохождения стадии, а не заранее. Отдельным артефактом
остаются только исправления — [`docs/fixes/`](../fixes/README.md) (при
необходимости `0532-YY-*`).

## Краткое описание

Сообщения инструментов Takt — диагностики компилятора (`SE-`, `CC-`, `ST-`,
`RS-`, `SV-`, `LE-`, `SY-`, `FM-`), сообщения прогона (`SIM-`) и тексты CLI —
сегодня существуют **только по-русски** и вшиты в код строковыми литералами.
Заказчик (2026-09-04) просит завести механизм выбора языка сообщений во всём
компиляторе и симуляторе.

Предмет фичи — **механизм**, а не перевод: как сообщение отделяется от места,
где оно рождается; чем задаётся язык (переменная окружения, ключ CLI,
параметр библиотеки); что происходит с сообщением, у которого перевода нет;
как это не разъезжается с реестром диагностик и со справочником документа.

⚠️ Цена ошибки здесь выше обычной. Тексты диагностик **проверяются машиной**:
гейт качества (фича 0467) судит 2995 сообщений корпуса, гейт справочника
(0290) сверяет состав кодов с реестром, а разборы приложения прогоняются
инструментом (0520) и сверяются с его выводом. Механизм языка обязан
объяснить, что делают эти гейты, когда сообщений становится два на код.

⚠️ Второй потребитель — веб (фича 0531): интерфейс редактора и сообщения
инструментов должны говорить на одном языке, иначе получится оболочка на
одном языке вокруг диагностик на другом. Разбор i18n самого веб-интерфейса
живёт в задаче `0531-10`; эта фича отвечает за инструменты.

## Документирование (правило 24)

Предварительно: **язык не меняется** — меняется язык сообщений о нём.
Затронуты, вероятно, два места документа: раздел «Инструментарий» (как выбрать
язык) и приложение «Ошибки и предупреждения» (на каком языке приведены тексты
и что будет со вторым языком). Решение принимается на стадии анализа.

## Документирование (правило 24)

<Меняет ли фича ЯЗЫК (синтаксис/семантику/наблюдаемое поведение/диагностики/
поведение целей)? Если да — какой раздел [`book/`](../../book/) затронут и какие
изменения нужно внести (что добавить/переписать, какие примеры на Takt). Если нет
(рефакторинг/инфраструктура/процесс) — «не требуется» с кратким обоснованием.
В документ не попадают детали реализации, номера фич и процесс разработки.>

<!-- ─────────────────────────────────────────────────────────────────────────
     Разделы ниже добавляются ПО МЕРЕ прохождения стадий (правила 17 и 32).
     Порядок разделов = порядок стадий; заранее их не заводят.

## Архитектура (ADR)

- **Status:** Draft | Proposed | Accepted
- **Date:** ГГГГ-ММ-ДД
- **Authors:** <роли>

### Context
<Какую проблему решаем; замер — прогоном (правило 30), с датой.>

### Decision Drivers
### Considered Options
### Decision
### Consequences
#### Acceptance criteria

## Анализ

### Цель и контекст
### Замер (правило 30, дата)
### Зависимости фичи (правило 17/19)
### Требования и проверяемые условия
### Критерии приёмки и способ проверки
### Редакторский слой (правило 29)
### Особенности по обратной функциональности
### Риски и зависимости

## Разработка

### Задача 0532-01
#### Что было
#### Что сделано
#### Проверки

## Тест-план

### Область и цель
### Проверки (условие → ожидаемый результат)
### Разбивка проверок по функциональности
### Тестовые данные и окружение

## Отчёт о тестировании (правило 20)

### Резюме
### Фактические результаты по проверкам
### Результаты по функциональности
### Найденные дефекты
### Выводы и дальнейшие шаги

## Итог (что сделано)   ← при закрытии, правило 21
     ───────────────────────────────────────────────────────────────────────── -->
