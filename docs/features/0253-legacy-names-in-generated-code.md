# Фича 0253: Старое имя языка в порождаемом коде (lam_q_*, LAM_Q_*)

- **Номер:** 0253
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-19)
- **Tier:** 4 — имена валидны и верны — косметика наблюдаемого вывода (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** генерация
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0253-legacy-names-in-generated-code.md`](../adr/0253-legacy-names-in-generated-code.md) |
| Анализ | [`docs/analyze/0253-legacy-names-in-generated-code.md`](../analyze/0253-legacy-names-in-generated-code.md) |
| Разработка | [`0253-01`](../development/0253-01-legacy-names-in-generated-code.md) (переименование в целях), [`0253-02`](../development/0253-02-legacy-names-in-generated-code.md) (гейт и сторож) |
| Тест-план | [`docs/tests/0253-legacy-names-in-generated-code.md`](../tests/0253-legacy-names-in-generated-code.md) |
| Отчёт о тестировании | [`docs/reports/0253-legacy-names-in-generated-code.md`](../reports/0253-legacy-names-in-generated-code.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0253-YY-*`) |

## Краткое описание

**Старое имя языка живёт в ПОРОЖДАЕМОМ коде.** Замер
[0161](0161-fixture-comments-rename.md) (2026-08-18): цель `c`
печатает хелперы `lam_q_floordiv` (21 место), `lam_q_mul`, `lam_q_wrap`,
`lam_q_sat`, цель `st` — `LAM_Q_FLOORDIV`, `LAM_Q_WRAP`, `LAM_Q_SAT`,
`LAM_Q_MUL`, `LAM_Q_DIV`; имена уезжают в прошивку пользователя и видны в
снапшотах `examples/generated/`, харнессах и потактовых сверках. Гейт
`check-legacy-names.sh` их **намеренно** не проверяет (граница названа в его
заголовке): смена — ломающее изменение контракта вывода, требующее анализа
обратной совместимости (правило 11) и согласованной правки снапшотов.
⚠️ Заводя фичу, помнить: `iec2c` регистронезависим и делит пространство имён
со стандартной библиотекой IEC — новое имя проверять пробой, а не выбирать.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Не требуется** (определено на стадии анализа 2026-08-19). Документ `book/`
описывает **язык**, а хелперы фиксированной точки — деталь реализации целей:
проверено грепом, `book/` не упоминает их ни разу, `README.md` — тоже. Язык
(синтаксис, семантика, диагностики) фичей не затронут, версия языка не
поднимается.

## Итог (что сделано)

**Порождаемый код больше не несёт имени упразднённого языка.** Хелперы
фиксированной точки переименованы: цель `c` печатает `takt_q_floordiv`,
`takt_q_mul`, `takt_q_div`, `takt_q_sat`, `takt_q_wrap`; цель `st` —
`TAKT_Q_FLOORDIV`, `TAKT_Q_WRAP`, `TAKT_Q_SAT` (у неё их **три**, а не пять,
как числил заголовок гейта: умножение строится как `TAKT_Q_FLOORDIV(a * b,
2^n)`). Цели `c-hal`/`st-at` получают новое имя тем же изменением; `rust`,
`sv`, `sv-mmio`, `plantuml` хелперов не эмитят вовсе.

Сделано двумя задачами: [0253-01](../development/0253-01-legacy-names-in-generated-code.md)
(эмиссия целей `c`/`st`, тексты сверок, перегенерация снапшотов) и
[0253-02](../development/0253-02-legacy-names-in-generated-code.md) (снятие
границы гейта и сторож новой ветви). Подробности — в
[отчёте](../reports/0253-legacy-names-in-generated-code.md); решение и
отвергнутые варианты — в [ADR](../adr/0253-legacy-names-in-generated-code.md).

**Ключевые решения:**

- **Имя выбрано пробой, а не рассуждением** (правило 30): `iec2c` принимает
  `TAKT_Q_*`, слова `takt` в `ieclib` нет ни разу; контроль — тот же файл со
  старым именем тоже принимается, то есть проба измеряет **имя**. Урок 0041
  (модель `Concat` отвергается) обязывает: идентификаторы MatIEC
  регистронезависимы и делят пространство со стандартной библиотекой.
- **Граница гейта снята, а не описана заново.** `check-legacy-names.sh` ловит
  `lam_q_`/`LAM_Q_` в любом регистре, включая снапшоты `examples/generated/`;
  сторож `test-legacy-names.sh` проверяет **поимку** старого имени и **приём**
  нового вместо прежнего «исключение названо словами». Без контроля «ловится»
  означало бы лишь, что гейт ругается на любую строку с `q_`.
- **Ломающим изменение считается формально** (правило 11): контракта на эти
  имена не было (ни `book/`, ни `README.md` их не упоминают), а в цели `c`
  вызов извне невозможен по языку — все пять хелперов `static`. Версия языка
  не менялась; версия крейта `takt-lang` — `0.51.0`.
- **Rust-константы переименованы вместе с содержимым** (`const TAKT_Q_SAT`),
  хотя формально это предмет фичи [0254](0254-legacy-names-internal-identifiers.md):
  константа **называет** порождаемый хелпер и, разъехавшись с ним, врала бы
  читателю в месте правки.

**Живая проверка:** `./scripts/precheck.sh` — код 0; `cargo test
--all-features` — 3304 теста, 0 провалов; 117 потактовых сверок зелены **без
правки ожидаемых значений**; `iec2c` — 10/10 корпуса; корпус цели `c` —
`cc -Wall -Wextra -Werror` без предупреждений.

**Вынесено дальше:** процессный кандидат «комментарий шага `precheck.sh`
пересказывает правило своего гейта» (блок 3 `FEATURES.md`) — при снятии
исключения пришлось править два таких пересказа, и разошлись бы они молча.

