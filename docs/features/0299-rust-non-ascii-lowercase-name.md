# Фича 0299: Цель rust: не-ASCII имя в нижнем регистре отвергается clippy

- **Номер:** 0299
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии 3)
- **Tier:** 2 — clippy -D warnings отвергает не-ASCII имя в нижнем регистре (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** генерация
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0299-rust-non-ascii-lowercase-name.md`](../adr/0299-rust-non-ascii-lowercase-name.md) |
| Анализ | [`docs/analyze/0299-rust-non-ascii-lowercase-name.md`](../analyze/0299-rust-non-ascii-lowercase-name.md) |
| Разработка | [`0299-01`](../development/0299-01-rust-non-ascii-lowercase-name.md) |
| Тест-план | [`docs/tests/0299-rust-non-ascii-lowercase-name.md`](../tests/0299-rust-non-ascii-lowercase-name.md) |
| Отчёт о тестировании | [`docs/reports/0299-rust-non-ascii-lowercase-name.md`](../reports/0299-rust-non-ascii-lowercase-name.md) |
| Исправления | не потребовались |

## Краткое описание

**Цель `rust`: не-ASCII имя в нижнем регистре отвергается `clippy`.** Замер при
закрытии [0200](0200-non-ascii-identifier-targets.md)
(2026-08-04): `out кнопка` даёт `variant кнопка should have an upper camel case
name` под `-D warnings`, а `out Кнопка` проходит. Это правило **регистра**, а
не алфавита, и оно существует независимо от 0200 — то есть цель `rust`
принимает не-ASCII имена **не полностью**. ⚠️ В корпусе таких имён нет.
Решить надо, отвергать ли такую запись диагностикой цели (как `SV-018`) либо
печатать вариант перечисления с приведением регистра — второе заводит класс
коллизий (`кнопка`/`Кнопка` слиплись бы), от которого проект уходил в 0084,
0193 и 0195.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Определяется на стадии анализа.** Фича затрагивает язык или наблюдаемое
поведение целей, поэтому нужный раздел `book/` называется после проработки —
до неё утверждение о документе было бы догадкой.

## Итог (что сделано)

**Регистр имени приводится по Unicode, а не по ASCII.** Приведение к CamelCase
поднимало первую букву через `to_ascii_uppercase`, поэтому не-ASCII имя
оставалось строчным: `out кнопка` давало вариант перечисления `кнопка`, и
`clippy -D warnings` — флагами гейта предкоммита — отвечал **ошибкой** при
нулевом коде возврата `taktc`. Контроли замера показали, что предмет —
**регистр**, а не алфавит: `Кнопка` и `button` проходили всегда.

**Сделано:** одна строка в общем слое `semantic::naming` (`to_uppercase` вместо
`to_ascii_uppercase`). Правка стоит **не в цели**: правило зовут семь
потребителей, и копия в одной цели разошлась бы с остальными (класс
0084/0193/0195).

⚠️ **Нового класса коллизий не заведено** — опасение карточки не подтвердилось:
слипание `кнопка`/`Кнопка` ловит существующая `RS-005`, тем же механизмом, что
и `button`/`Button` до фичи. Закреплено тестом.

**Сторожа:** юнит-тесты нормализации (включая смешанный алфавит
`пуск_button` → `ПускButton`) и `takt-lang/tests/targets/rust_non_ascii_name_tests.rs`
с прогоном настоящего `clippy-driver -D warnings`. ⚠️ Корпус класс не
покрывает: не-ASCII имён в `examples/` нет ни одного.

Это **остаток работы [0200](0200-non-ascii-identifier-targets.md)**: язык принял
не-ASCII имена, а правило регистра осталось ASCII-только.

**Версия языка не поднята**; крейт `takt-lang`: `0.46.2` → `0.46.3`.

Детали — [отчёт](../reports/0299-rust-non-ascii-lowercase-name.md), решение —
[ADR](../adr/0299-rust-non-ascii-lowercase-name.md).
