# Фича 0049: Верификация модели (Model Checking) на основе LTL

- **Номер:** 0049
- **Статус:** РАЗРАБОТКА
- **Зависит от:** `0010` (LTL/Бюхи, `ГОТОВО`), `0035` (разбор LTL, `ГОТОВО`) —
  обе закрыты → фича **не блокируется**. Обоснование — в
  [анализе](../analyze/0049-model-checking-ltl.md).
- **Связанные issue (анализ):** новая фича (из блока «Кандидаты в фичи»
  `FEATURES.md`: «LTL-верификация моделей: вход `lamc verify` (расширение 0010)»,
  выявлено при 0035/0044). Затравка — модуль `grammar/src/verification/`.
- **Крейт:** `grammar` (`verification/`, `bin/lamc.rs`).
- **Приоритет / Tier:** Tier 2 — профильная возможность DSL промышленных систем
  управления (правило 12): формальная проверка свойств модели **до** синтеза.

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0049-model-checking-ltl.md`](../adr/0049-model-checking-ltl.md) |
| Анализ | [`docs/analyze/0049-model-checking-ltl.md`](../analyze/0049-model-checking-ltl.md) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0049-YY-*`) |
| Тест-план | [`docs/tests/0049-model-checking-ltl.md`](../tests/README.md) |
| Отчёт о тестировании | [`docs/reports/0049-model-checking-ltl.md`](../reports/README.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0049-YY-*`) |

## Краткое описание

Довести конвейер верификации до рабочего: проверять LTL-свойства модели (`: [LTL]
φ;`) методом model checking. Затравка (`verification/`) даёт **два звена** — тип
`Ltl` и построение автомата Бюхи `build_buchi` (GPVW). Отсутствует **середина**:
структура Крипке из `ModelNode`, произведение с автоматом `¬φ`, проверка пустоты
(достижимый принимающий цикл), контрпример-лассо и подкоманда `lamc verify`.

По решению заказчика (2026-07-16) принята **абстракция управляющего графа**: атом
LTL — **имя состояния** FSM («автомат сейчас в состоянии S»); структура Крипке —
граф переходов автомата (состояния + рёбра `ref`), данные абстрагированы.
Проверяемы порядок состояний, достижимость, живость (`G(Fault -> F Idle)`);
свойства над данными (`G(temp <= 100)`) — вне охвата, будущее расширение.

> Фича зарегистрирована из блока «Кандидаты в фичи» `FEATURES.md` (по запросу
> заказчика 2026-07-16); проходит жизненный цикл по правилу 17. Проработана до
> стадии **РАЗРАБОТКА** (ADR принят, анализ и декомпозиция готовы).

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->

