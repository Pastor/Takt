# Фича 0085: Константы `LANGUAGE_VERSION` в коде нет

- **Номер:** 0085
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar`
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0085-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0085-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0085-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0085-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0085-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0085-YY-*`) |

## Краткое описание

Версия языка живёт **только** в `README.md:1007`; рассинхрон между документацией и
фактическим поведением компилятора **ничем не ловится**.

⚠️ Обостряется решением заказчика от 2026-07-15 (правило 22: версия
**накапливается**): исполнение решения затруднено именно тем, что константы в коде
нет — пять фич пула заявляли подъём версии, а сверить их не с чем.

Выявлено при [0045](0045-sv-backend.md).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
