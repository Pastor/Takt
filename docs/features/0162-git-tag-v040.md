# Фича 0162: Пропущенные метки версий языка и сторож правила 22

- **Номер:** 0162
- **Статус:** ГОТОВО
- **Зависит от:** нет
- **Tier:** proc
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md` (вынесено закрытием [0100](0100-language-rename-takt.md))

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0162-git-tag-v040.md`](../adr/0162-git-tag-v040.md) |
| Анализ | [`docs/analyze/0162-git-tag-v040.md`](../analyze/0162-git-tag-v040.md) |
| Разработка | [`0162-01`](../development/0162-01-git-tag-v040.md) |
| Тест-план | [`docs/tests/0162-git-tag-v040.md`](../tests/0162-git-tag-v040.md) |
| Отчёт о тестировании | [`docs/reports/0162-git-tag-v040.md`](../reports/0162-git-tag-v040.md) |
| Исправления | не потребовались |

## Краткое описание

Версия языка `0.3.0 → 0.4.0` поднята фичей [0078](0078-bit-array-semantics.md),
но коммит подъёма **не помечен тегом** — нарушение правила 22 («подъём
помечается тегом»). В истории есть `v0.3.0` и `v0.5.0`, между ними разрыв.

> Фича зарегистрирована из бэклога кандидатов (отобрана заказчиком 2026-07-28); далее проходит жизненный цикл по правилу 17.

## Что известно на входе

- Тегов `v0.3.0` и `v0.5.0` в истории есть, `v0.4.0` — нет.
- Правило 22 требует тег **на тот самый коммит**, который меняет номер версии:
  только так по истории видно, где проходит граница версии.
- Коммит подъёма ищется по `git log -S '0.4.0' -- takt-lang/src/version.rs`.

## Направление решения (наметка, не решение)

- Ретроспективный **аннотированный** тег `v0.4.0` на коммит подъёма 0078.
- ⚠️ Тег на старый коммит — **решение заказчика**: это переписывание меток
  истории, и если тег уже был где-то опубликован, последствия шире репозитория.

⚠️ Окончательный выбор — за стадиями **архитектуры** (ADR) и **анализа**: раздел
выше фиксирует то, что уже проверено, а не предрешает реализацию.

## Документирование (правило 24)

**Не требуется.** Метка истории; язык не меняется.

## Итог (что сделано)

История версий языка размечена без разрывов: восемь версий — восемь
аннотированных тегов. Проставлены `v0.4.0` на `dfec3988` (подъём фичи 0078) и
`v0.10.0` на `47e5a5fb` (подъём фичи 0243). Правило 22 обрело сторожа —
`scripts/check-version-tag.sh` со сторожем `scripts/test-version-tag.sh`, оба
шагами предкоммита.

⚠️ **Пропусков оказалось два, и второй сделан накануне.** Карточка знала об
одном (`v0.4.0`). Замер показал: из восьми версий тегов не имели две, причём
`v0.10.0` — подъём фичи 0243 от 2026-08-17, то есть дыра действующая, а не
наследие давнего периода. Один пропуск закрывается тегом; два из восьми, с
последним вчерашним, закрываются **гейтом**.

⚠️ **Коммит под `v0.4.0` пришлось выбирать, а не находить.** Подъём
0.3.0 → 0.4.0 разорван: `dfec3988` (фича 0078) объявил его в `CHANGES.md`, а
строку README `**Версия языка: 0.3.0**` исправил только `8f8a03a1` (фича 0085,
заводившая константу и гейт синхронизации) — то есть документ отставал на две
фичи, и это ровно тот дефект, ради которого 0085 и появилась. Взят `dfec3988`:
правило 22 говорит, что подъём делает **первая закрытая фича цикла**, а 0085
языка не меняла. Тег на ней приписал бы подъём не той фиче.

⚠️ **Гейт обязан молчать в момент подъёма — иначе он вреден.** Тег ставится
**на** коммит, а предкоммит идёт **до** коммита: наивная проверка «тег текущей
версии существует» падала бы у каждой фичи, которая правило исполняет **верно**.
Признак незакоммиченного подъёма — расхождение константы в рабочем дереве и в
`HEAD`; в этом случае гейт печатает пропуск и подсказывает команду тега. Как
только подъём закоммичен, тег обязателен, — поэтому пропуск `v0.10.0` был бы
пойман на первом же прогоне после 0243.

⚠️ **Замер потребовал `^{commit}`.** У аннотированного тега `git rev-parse v0.5.0`
отдаёт объект **тега**, а не коммит: наивное сравнение хешей дало ложное «тег
указывает не туда» у всех шести существующих тегов и едва не увело вывод в
«историю переписывали». Разыменование — часть замера.

⚠️ **Граница названа:** прошлые пропуски сторож не ищет. Перечня версий в
дереве нет (README документирует два перехода из восьми), а восстанавливать его
из `git log -S` значит завести гейт, зависящий от формулировок старых коммитов.
Прошлое размечено один раз — этой фичей.

⚠️ **CI получил `fetch-depth: 0`** (иначе `actions/checkout` берёт один коммит
без тегов, и под `PRECHECK_STRICT=1` гейт падал бы не по делу), но **прогоном
не проверен**: Actions заблокированы по биллингу
([0175](0175-ci-verification-followup.md)).

Артефакты: [ADR](../adr/0162-git-tag-v040.md) ·
[анализ](../analyze/0162-git-tag-v040.md) ·
[0162-01](../development/0162-01-git-tag-v040.md) ·
[тест-план](../tests/0162-git-tag-v040.md) ·
[отчёт](../reports/0162-git-tag-v040.md).
