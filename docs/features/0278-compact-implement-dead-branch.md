# Фича 0278: semantic::extend::compact_implement в конвейере не вызывается

- **Номер:** 0278
- **Статус:** ГОТОВО
- **Зависит от:** нет (проверено на стадии анализа)
- **Tier:** 4 — мёртвая либо отключённая ветвь компилятора; вывода не касается (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** семантика
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0278-compact-implement-dead-branch.md`](../adr/0278-compact-implement-dead-branch.md) |
| Анализ | [`docs/analyze/0278-compact-implement-dead-branch.md`](../analyze/0278-compact-implement-dead-branch.md) |
| Разработка | [`0278-01`](../development/0278-01-compact-implement-dead-branch.md), [`0278-02`](../development/0278-02-compact-implement-dead-branch.md) |
| Тест-план | [`docs/tests/0278-compact-implement-dead-branch.md`](../tests/0278-compact-implement-dead-branch.md) |
| Отчёт о тестировании | [`docs/reports/0278-compact-implement-dead-branch.md`](../reports/0278-compact-implement-dead-branch.md) |
| Исправления | не потребовались |

## Краткое описание

**`semantic::extend::compact_implement` в конвейере не вызывается.** Замер при
закрытии [0211](0211-c-missing-start-state-diagnostic.md)
(2026-08-17): единственные вызовы функции — **рекурсивные внутри неё самой**
плюс тесты; `tree.rs` зовёт только `unroll_extend_expression`, и комментарий
соседнего теста прямо говорит «compact_implement отключён в tree.rs». То есть
синтетическая модель `X_Sequence` (упаковка `A + B` в ступени `Step0…StepN`)
сегодня не строится, а последовательную композицию потребители разбирают сами.
Кандидат: решить, что это — **мёртвый код** (удалить вместе с тестами) или
**отключённая ветвь** (тогда назвать причину отключения в коде). ⚠️ Включение
обратно потребует правки обхода `SE-106`: синтетической модели нет в `models`
родителя, рекурсия `validate_model_all` до её состояний не дойдёт, и пустая
модель внутри `A + B` снова уедет в цели молча (замечание записано в
`validate/implemented.rs`).

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения

Развилку кандидата разрешил замер: путь **отвергнут** решением
[ADR 0057](../adr/0057-sv-sequential-composition.md) (Option B — «безусловный
`next` между ступенями продвигался бы каждый такт»), а код мёртв с 7 апреля
2026. Принято удаление вместе с классом, который его прятал, — см.
[ADR 0278](../adr/0278-compact-implement-dead-branch.md).

## Документирование (правило 24)

**Не требуется.** Язык и наблюдаемое поведение целей не меняются; синтетическая
модель `X_Sequence` была внутренним представлением и в `book/` не упоминалась
ни разу (проверено грепом). Версия языка не поднимается.

## Итог (что сделано)

**`compact_implement` снята как мёртвая, и снят класс, который её прятал.**

Археология (замер 2026-08-19): функция жила с вызовом **два дня** (заведена
2026-04-05, вызов закомментирован 2026-04-07 при постороннем рефакторинге,
строка удалена фичей 0199). Путь при этом не забыт, а **отвергнут** решением
ADR 0057: безусловный `next` между ступенями продвигался бы каждый такт, и
минимап пришлось бы менять ради одной цели. Действующее устройство `A + B` —
явное и покрыто сверками (0057, 0166, 0181, 0213), а док-комментарий снятой
функции всё это время обещал вызов из stage1.

Второй результат важнее первого: мёртвое дожило четыре с половиной месяца
потому, что **`pub` в публичном модуле глушит `dead_code`**. Видимость
`semantic::extend` сужена до фактически нужной (наружу — только типы `Extend` и
`ParameterArgument`), после чего компилятор назвал ещё двоих: четыре предиката
`Extend`, которых не звал никто, кроме их же теста, и конструктор
`ModelNode::new`. Предикаты сняты, конструктор переехал в тестовый модуль
фабрикой `model_node`.

Побочно: `semantic/extend.rs` уменьшился на 137 строк, `semantic/mod.rs` —
на 39 (1279 → 1240), обе записи реестра размеров уменьшены.

Детали — в [отчёте](../reports/0278-compact-implement-dead-branch.md) и задачах
[0278-01](../development/0278-01-compact-implement-dead-branch.md),
[0278-02](../development/0278-02-compact-implement-dead-branch.md).

Живая проверка: `./scripts/precheck.sh` — код 0, 3291 тест.

