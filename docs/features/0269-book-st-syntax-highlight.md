# Фича 0269: Блоки st и pascal в документе не подсвечиваются

- **Номер:** 0269
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-20)
- **Tier:** 4 — два блока приложения печатаются без цвета (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** прочее
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0269-book-st-syntax-highlight.md`](../adr/0269-book-st-syntax-highlight.md) |
| Анализ | [`docs/analyze/0269-book-st-syntax-highlight.md`](../analyze/0269-book-st-syntax-highlight.md) |
| Разработка | [`0269-01`](../development/0269-01-book-st-syntax-highlight.md), [`0269-02`](../development/0269-02-book-st-syntax-highlight.md) |
| Тест-план | [`docs/tests/0269-book-st-syntax-highlight.md`](../tests/0269-book-st-syntax-highlight.md) |
| Отчёт о тестировании | [`docs/reports/0269-book-st-syntax-highlight.md`](../reports/0269-book-st-syntax-highlight.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0269-YY-*`) |

## Краткое описание

**Блоки `st` и `pascal` в документе не подсвечиваются.** Замер 0240: у Typst
(syntect) есть определения `c`, `rust`, `systemverilog`, `json`, `bash`, но
Structured Text среди них нет — два блока приложения печатаются без цвета
(прежний skylighting подсвечивал их как `pascal`). Кандидат: положить рядом с
`book/takt.sublime-syntax` определение ST — тем же способом, каким заведена
подсветка Takt.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Не требуется.** Содержание документа не меняется — меняется его оформление
(подсветка блоков кода). Язык, конструкции и диагностики не затронуты.

## Итог (что сделано)

**Девять блоков документа перестали печататься чёрным.**

- **Два определения подсветки** рядом с документом: `book/st.sublime-syntax`
  (Structured Text — объявления POU, управление, словесные операторы, типы,
  литералы, локации, комментарии `(* … *)`; правила нечувствительны к регистру)
  и `book/ebnf.sublime-syntax` (терминалы, `::=`, `|`, группировка). Оба
  подключены списком в `book/src/template.typ`.
- **Гейт** `scripts/check-book-code-langs.py` (шаг предкоммита, самопроверка
  первой): язык блока обязан быть объявлен — встроенным в syntect либо своим
  файлом, который существует **и подключён**. Четыре класса: не объявлен, нет
  файла, не подключён, объявлен впустую.

**Замер уточнил кандидата** (2026-08-20): блоков `pascal` в документе нет
вовсе, зато есть **восемь** блоков `ebnf`, которых он не называл; непокрытых
блоков было девять, а не два. ⚠️ Класс невидим сборке — `typst compile` на
неизвестном языке блока даёт **код 0**; замер снимался рендером страницы в PNG.

⚠️ Рендер же нашёл дефект первой редакции ST: `16#FF` выходил как `16` плюс
бесцветное `#FF`. Чтением кода это не ловится.

Детали — в [отчёте](../reports/0269-book-st-syntax-highlight.md); решение и
отвергнутые варианты — в [ADR](../adr/0269-book-st-syntax-highlight.md).
