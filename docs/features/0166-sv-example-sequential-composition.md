# Фича 0166: Корпусной SV-транслируемый пример на последовательную композицию `+`

- **Номер:** 0166
- **Статус:** ГОТОВО
- **Зависит от:** [0181](0181-sim-state-implementation-tick.md)
- **Tier:** 2
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md` (выявлено при закрытии [0057](0057-sv-sequential-composition.md))

## Стадии жизненного цикла (правило 17)

Все стадии — **разделы этой карточки** (правило 32): «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0166-YY-*`).

## Краткое описание

Последовательная композиция `+` в цели `sv` **не покрыта гейтом** `precheck.sh`:
единственный пример корпуса с `+` не SV-транслируем. Синтез на `+` прогонялся
вручную — то есть регресс в этой ветке гейт не поймает.

> Фича зарегистрирована из бэклога кандидатов (отобрана заказчиком 2026-07-28); далее проходит жизненный цикл по правилу 17.

## Что известно на входе

- Единственный пример корпуса с `+` — `extend_complex.takt` — **не
  SV-транслируем** (`extern fn has_flag` → `SV-005`, struct, битовый доступ).
- `SV_TRANSLATABLE="stacker elevator_mini"` — обе модели на `|`.
- `yosys synth` на `+` прогнан **вручную** (проба 2026-07-18) и через
  `verilator` в потактовой сверке `conformance_sv_tests`; в `precheck.sh` `+` на
  синтез **не подаётся**.
- ⚠️ Пока не закрыт [фикс 0057-01](../fixes/0057-01-sim-composition-next.md)
  (симулятор не исполняет `+` с `next`), пример на `+` не пройдёт
  `examples_scenario_tests` без исключения. **Решение заказчика 2026-07-29:**
  фикс закрывается первым — фичей
  [0181](0181-sim-state-implementation-tick.md), от которой 0166 и зависит.
  Обход исключением отвергнут: корпусной пример без контракта сценария остался
  бы без потактовой проверки, ради которой заводится.

## Направление решения (наметка, не решение)

- Маленький SV-транслируемый пример с `+` в `examples/` и в `SV_TRANSLATABLE`.
- ⚠️ Цена — рябь: новый пример требует контракта (или исключения **с причиной**)
  в `examples_scenario_tests`, канона `fmt` и генерации во все цели.
- Класс — «дыра в покрытии гейта», смежно с [0090](0090-ci-precheck.md).

⚠️ Окончательный выбор — за стадиями **архитектуры** (ADR) и **анализа**: раздел
выше фиксирует то, что уже проверено, а не предрешает реализацию.

## Документирование (правило 24)

**Не требуется.** Покрытие гейта существующей возможностью языка; сам язык не
меняется. (Пример может пригодиться разделу
[`book/src/14-targets/`](../../book/src/14-targets/index.typ) — решает аналитик.)

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-07-29
- **Authors:** Архитектор + Аналитик
- **Related issues:** [Фича 0166](0166-sv-example-sequential-composition.md); наследует [ADR 0045](0045-sv-backend.md#архитектура-adr) и [ADR 0057](0057-sv-sequential-composition.md#архитектура-adr); разблокирована фичей [0181](0181-sim-state-implementation-tick.md)

### Context

Последовательная композиция `+` в цели `sv` **не покрыта гейтом**
`precheck.sh`. Список обязательных примеров
`SV_TRANSLATABLE="stacker elevator_mini regulator pid_regulator"` целиком состоит
из моделей на `|`; единственный пример корпуса с `+` — `extend_complex.takt` —
целью `sv` не транслируется (`extern fn has_flag` → `SV-005`, плюс struct и
битовый доступ). Синтез на `+` прогонялся **вручную** (проба 2026-07-18), то
есть регресс в этой ветке гейт не поймает.

Цена такой дыры известна поимённо: у [0045](0045-sv-backend.md#архитектура-adr) дефект приняли
молча **оба** инструмента SV, у [0050](0050-rust-backend.md#архитектура-adr) сверки не было
вовсе при зелёном гейте. Правило проекта — «сверку заводить вместе с бэкендом» —
на `+` не выполнено.

### Decision Drivers

1. **Гейт обязан покрывать конструкцию, а не её отсутствие.** `+` — часть языка
   и часть цели `sv`; непокрытая ветвь ломается молча.
2. **Пример — документация языка** (правила 15, 16). Корпусный `.takt` читают,
   поэтому он обязан быть осмысленным, а не синтетическим стендом.
3. **Форма примера диктуется гейтами.** Новый `.takt` проходит канон `fmt`, гейт
   воспроизводимости, компиляцию во **все** цели, `-Wall -Werror`, `iec2c`,
   `rustc`+`clippy -D warnings` и обязан получить контракт сценария либо
   исключение с причиной.
4. **Контракт, а не исключение.** Ради потактовой проверки пример и заводится;
   исключение оставило бы его непроверенным. Возможность контракта появилась
   только после [0181](0181-sim-state-implementation-tick.md).

### Considered Options

#### Option A. Новый маленький пример, целиком посвящённый `+`

Технологический цикл из трёх фаз: дозирование → перемешивание → слив,
`start Cycle = Dose + Mix + Drain { next Done; }`.

**Pros:**
- показывает `+` в его прямом назначении — **последовательность фаз**, ровно то,
  ради чего конструкция в языке и существует;
- мал: три под-модели по одному счётчику, читается целиком;
- проходит все гейты (проверено прогоном, см. «Consequences»), в том числе
  тестбенч и контракт сценария.

**Cons:**
- корпус растёт на файл и его порождённые артефакты во всех целях;
- нужен рукописный тестбенч SV и харнесс C.

#### Option B. Сделать `extend_complex.takt` SV-транслируемым

Убрать из него `extern fn`, struct и битовый доступ.

**Pros:**
- корпус не растёт.

**Cons:**
- **уничтожает назначение файла**: это стенд **синтаксиса** (структуры, порты с
  адресами, вложенные композиции), и именно поэтому он исключён из
  `examples_scenario_tests` как «сценария не заявляющий». Урезав его до
  SV-транслируемого подмножества, мы потеряем покрытие разбора ради покрытия
  синтеза;
- `+` в нём вложена в четырёхзвенную цепочку со скобками — при отказе гейта
  причину придётся искать в стенде на 70 строк.

#### Option C. Внести `+` в существующий пример из `SV_TRANSLATABLE`

Дописать фазовую цепочку, например, в `regulator.takt`.

**Pros:**
- ни нового файла, ни нового тестбенча.

**Cons:**
- смешивает предметы: `regulator` демонстрирует fixed-point `q(8, 8)`, и
  пришитая к нему композиция сделает непонятным, что именно пример показывает;
- ломает его контракт сценария и потактовые сверки
  (`conformance_float_modes_tests`).

### Decision

Принимается **Option A**.

Единственный вариант, который закрывает дыру, **ничего не разрушая**. Цена —
файл и его обвязка — обычная стоимость корпусного примера в этом проекте.

#### Форма примера (проверена прогоном, а не выбрана на глаз)

- **три фазы по счётчику тактов**: каждая под-модель считает свои такты и уходит
  в терминальное состояние. Так цепочка наблюдаема: общая `stage` показывает,
  **какая** фаза активна, и контракт сценария проверяет, что все три
  исполнились **по порядку**;
- **выходной порт `ready`** — не украшение: модель без портов даёт
  `clippy::new_without_default` в гейте цели `rust` (открытая фича 0174);
- **завершение** (`state Done` без переходов) — обязательное условие контракта
  `must_terminate: true`;
- ⚠️ **имя порта не должно совпадать с именем состояния.** Проба показала: `out
  ready: bit` вместе с `state Ready` даёт в цели `c` **некомпилируемый** код —
  перечислитель `<ROOT>_READY` объявляется дважды (в перечислении битовых портов
  и в перечислении состояний), а enumerator'ы в C делят область видимости.
  Поэтому завершающее состояние названо `Done`. Сам дефект цели `c` шире этой
  фичи и записан находкой в `FEATURES.md`.

### Consequences

#### Положительные

- `+` попадает под **verilator + yosys**, под тестбенч и под контракт сценария —
  три независимых свидетеля вместо ручной пробы;
- корпус получает читаемый пример последовательности фаз — конструкции, которая
  до сих пор была представлена только стендом синтаксиса.

#### Отрицательные / Action items

- корпус растёт: `.takt`, тестбенч, харнесс C и порождённые артефакты в целях;
- ⚠️ гейты SV доказывают **валидность и синтезируемость**, но не верность
  (урок 0045). Верность здесь доказывают **тестбенч** (наблюдаемое поведение) и
  **контракт сценария** (цепочка состояний прогоном), а потактовое совпадение
  `+` у sim/C/SV уже закрыто тройной сверкой фичи 0181.

#### Acceptance criteria

1. Новый пример в `examples/`, использующий `+`, внесён в `SV_TRANSLATABLE`.
2. `verilator --lint-only -Wall` и `yosys synth -top <имя>` принимают порождённый
   `.sv`.
3. Парный тестбенч `examples/generated/sv/tb/<имя>_tb.sv` собирается Verilator и
   проходит (`assert` не падает).
4. У примера **контракт сценария** в `examples_scenario_tests` (цепочка фаз по
   порядку, бюджет, `must_terminate: true`) — не исключение.
5. Пример компилируется всеми целями; гейты `-Wall -Werror`, `iec2c`,
   `rustc`+`clippy -D warnings` зелены.
6. `./scripts/precheck.sh` зелёный.

## Анализ

### Цель и контекст

Закрыть дыру покрытия: последовательная композиция `+` не подаётся на гейт цели
`sv`. Реализуется **Option A** ADR 0166 — новый маленький пример
`examples/batch_cycle.takt` (технологический цикл из трёх фаз) и его обвязка.

Язык **не меняется**: используется существующая конструкция. Версия языка не
растёт (правило 22).

### Зависимости фичи (правило 17/19)

- **Зависит от:** нет (зависимость от [0181](0181-sim-state-implementation-tick.md)
  снята — та закрыта 2026-07-29). До её закрытия симулятор не исполнял `+` с
  переходом `next`, и пример мог получить только исключение вместо контракта.
- **Влияние на порядок разработки:** ничего не блокирует.

### Раскладка изменений

| Файл | Что делаем |
|---|---|
| `examples/batch_cycle.takt` | новый пример: `start Cycle = Dose + Mix + Drain { next Done; }` |
| `scripts/precheck.sh` | `batch_cycle` в `SV_TRANSLATABLE` |
| `examples/generated/sv/tb/batch_cycle_tb.sv` | тестбенч руками (образец — `regulator_tb.sv`) |
| `examples/generated/sv/tb/README.md` | строка в таблицу |
| `takt-sim/tests/sim/examples_scenario_tests.rs` | контракт сценария |
| `examples/generated/c/batch_cycle_main.c` | харнесс руками (образец — `regulator_main.c`) |
| `examples/generated/c/CMakeLists.txt` | `add_executable` |
| `examples/generated/**` | порождённые артефакты (пишет сам `precheck.sh`) |
| `README.md`, `CHANGES.md`, `CLAUDE.md`, `FEATURES.md`, реестры | правила 4, 8, 10, 21 |

### Требования и проверяемые условия

- **R1. Пример использует `+` и транслируется целью `sv`.** Обязательность —
  через `SV_TRANSLATABLE`: выпадение примера обязано ронять предкоммит громко.
- **R2. Каждая фаза исполняется, и по порядку.** Наблюдаемая — общая `stage`
  (1 → 2 → 3) и цепочка состояний фаз.
- **R3. Прогон завершается** — иначе контракт `must_terminate: true` невозможен.
- **R4. Порождённый RTL валиден и синтезируем** — `verilator --lint-only -Wall`
  и `yosys synth -top batch_cycle`.
- **R5. RTL ведёт себя как модель** — тестбенч на замкнутой среде проверяет
  `ready` и `is_done` накопителями (устойчиво к сдвигу такта на один).
- **R6. Пример компилируется всеми целями** и проходит их гейты.
- **R7. Существующие примеры не тронуты.**

### Ограничения формы (проверены прогоном 2026-07-29)

| # | Ограничение | Цена нарушения |
|---|---|---|
| О1 | без `extern fn` | `SV-005` — цель `sv` отказывает |
| О2 | без `for` | `SV-002` |
| О3 | без struct, битового доступа `x.2`, `float` | `SV-005` / `SV-003` |
| О4 | у корня есть порт | `clippy::new_without_default` (открытая фича 0174) |
| О5 | адресов портов нет | появился бы вывод `sv-mmio` и лишняя работа гейту `c-hal` |
| О6 | имена не из стандартной библиотеки IEC (`concat`, `left`, `right`, `mid`, `limit`, `step`) | `iec2c` отвергает, диагностика обманчива |
| О7 | **имя порта ≠ имя состояния** | цель `c` даёт **некомпилируемый** код: `<ROOT>_READY` объявляется и в перечислении битовых портов, и в перечислении состояний (enumerator'ы в C делят область видимости) |
| О8 | имена переменных под-моделей различны | иначе `takt-sim` предупреждает о неоднозначности голого имени |

⚠️ О7 обнаружено пробой при разработке этой фичи: первая редакция примера
называла порт `ready`, а завершающее состояние — `Ready`, и `cc` отказывал
`redefinition of enumerator`. В примере состояние переименовано в `Done`; **сам
дефект цели `c` шире фичи** и записан находкой в `FEATURES.md`.

### Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | пример транслируется целью `sv` и обязателен | `SV_TRANSLATABLE` + проверка «не оттранслирован» в `precheck.sh` |
| A2 | RTL валиден и синтезируем (R4) | `verilator --lint-only -Wall`, `yosys synth -top batch_cycle` |
| A3 | тестбенч проходит (R5) | `verilator --binary --timing` + прогон, `assert` не падает |
| A4 | контракт сценария (R2, R3) | `examples_scenario_tests`: цепочка `Fill → Full → Stir → Blended → Empty → Dry → Done`, `must_terminate: true` |
| A5 | цели принимают пример (R6) | `cmake`+`ninja` под `-Wall -Werror`, `iec2c`, `rustc`+`clippy -D warnings` |
| A6 | канон и детерминизм | `taktc fmt --check examples/`, гейт воспроизводимости |
| A7 | существующие примеры не тронуты (R7) | `git diff` по прочим `examples/*.takt` пуст |
| A8 | корпус зелёный | `./scripts/precheck.sh` |

### Особенности по обратной совместимости (правило 11)

Изменения **аддитивны**: новый пример, новая строка в `SV_TRANSLATABLE`, новая
запись контракта, новый тестбенч и харнесс. Существующие примеры, их порождённые
артефакты и контракты не трогаются. Язык и цели генерации не меняются.

### Риски

- **Риск «пример компилируется, но бессмысленен»** (прецедент — фикс 0121-03).
  Снижение: контракт сценария проверяет **цепочку фаз прогоном**, а тестбенч —
  наблюдаемое поведение RTL; ни то, ни другое не проходит на модели, которая
  никуда не приходит.
- **Риск разъезда `SV_TRANSLATABLE` и каталога.** Снижение: гейт уже
  двусторонний — обязательный пример без `.sv` роняет предкоммит, лишний `.sv`
  без записи в списке тоже.
- **Риск, что гейт SV примет неверный RTL** (урок 0045: линт и синтез доказывают
  валидность, не верность). Снижение: тестбенч и контракт сценария; потактовое
  совпадение `+` у sim/C/SV закрыто тройной сверкой фичи 0181.

## Разработка

### Задача 0166-01

#### Что было

`SV_TRANSLATABLE` состоял из моделей на `|`; последовательная композиция `+` на
гейт цели `sv` **не подавалась**. Единственный пример корпуса с `+`
(`extend_complex.takt`) целью `sv` не транслируется.

#### Что сделано

##### Пример

`examples/batch_cycle.takt` — технологический цикл из трёх фаз:
`start Cycle = Dose + Mix + Drain { next Done; }`. Каждая фаза — своя модель со
счётчиком тактов и терминальным состоянием; общая `stage` (1 → 2 → 3) делает
активную фазу наблюдаемой снаружи.

Форма продиктована гейтами, а не вкусом (ограничения О1–О8 анализа). Две из них
вскрыты **пробой**, а не чтением:

- ⚠️ **порт и состояние не могут называться одинаково**: первая редакция несла
  `out ready: bit` и `state Ready`, и цель `c` дала **некомпилируемый** код —
  `BATCH_CYCLE_READY` объявляется и в перечислении битовых портов, и в
  перечислении состояний, а enumerator'ы в C делят область видимости
  (`redefinition of enumerator`). Состояние переименовано в `Done`; сам дефект
  цели `c` шире фичи и записан находкой в `FEATURES.md`;
- ⚠️ **счётчики фаз названы по-разному** (`dosed`/`stirred`/`drained`):
  одинаковое имя в трёх моделях даёт предупреждение симулятора о неоднозначности
  голого имени.

##### Обвязка

| Файл | Что |
|---|---|
| `scripts/precheck.sh` | `batch_cycle` в `SV_TRANSLATABLE` + **прогон** харнесса C |
| `examples/generated/sv/tb/batch_cycle_tb.sv` | тестбенч руками: наблюдает `stage` иерархической ссылкой и требует порядка 1 → 2 → 3 |
| `examples/generated/sv/tb/README.md` | строка в таблицу; заодно дописаны отсутствовавшие `regulator`/`pid_regulator` (таблица отставала — в ней было 2 из 4) |
| `takt-sim/tests/sim/examples_scenario_tests.rs` | **контракт** сценария: цепочка из семи состояний, `must_terminate: true` |
| `examples/generated/c/batch_cycle_main.c` | харнесс руками: `assert`-ами проверяет, что номер фазы растёт **ровно на единицу** |
| `examples/generated/c/CMakeLists.txt` | `add_executable` |

⚠️ **Харнесс мало собрать — его надо запустить.** Прочие харнессы каталога
печатают лог, а этот **утверждает**; в незапущенном коде `assert` не срабатывает
никогда, и гейт остался бы зелёным при сломанной композиции. Поэтому в
`precheck.sh` добавлен явный прогон с проверкой кода возврата.

Обратная функциональность (правило 11): изменения **аддитивны** — существующие
примеры, их артефакты и контракты не тронуты.

#### Проверки

Все — через `./scripts/precheck.sh` (EXIT=0):

| Гейт | Результат |
|---|---|
| `verilator --lint-only -Wall` | принял |
| `yosys synth -top batch_cycle` | синтезировал |
| тестбенч (`verilator --binary --timing`) | `OK (три фазы по порядку, затем ready и is_done)` |
| контракт сценария | цепочка из семи состояний пройдена, прогон завершился |
| `cmake`+`ninja` под `-Wall -Werror` | собран; прогон харнесса: `три фазы по порядку, завершено за 11 шагов` |
| `iec2c` | валиден |
| `rustc` + `clippy -D warnings` | приняты |
| гейт воспроизводимости | воспроизводим по всем восьми целям |
| `taktc fmt --check examples/` | канон |

**Взведённость обоих новых сторожей доказана мутацией:**

- тестбенч с укороченным прогоном (4 такта вместо 40) → `Assertion failed: фаза
  перемешивания не наблюдалась`, код выхода 1;
- харнесс с `assert(phases == 4)` → `Assertion failed`, код выхода 134.

Симулятор и цель `c` завершают цикл за **11 тактов** оба — то есть свидетели
согласуются не только по факту, но и по длине трассы.

## Тест-план

### Область и цель

Доказать, что последовательная композиция `+` **подана на гейты** и что новый
пример проходит их все. Проверяются требования R1–R7 анализа и критерии
приёмки A1–A8.

⚠️ Гейт цели `sv` доказывает **валидность и синтезируемость**, но не верность
(урок 0045: молча неверная трансляция синтезируется тоже). Поэтому верность
доказывают три независимых свидетеля с **разными** наблюдаемыми: контракт
сценария (цепочка состояний в симуляторе), тестбенч SV (порядок фаз в RTL) и
харнесс C (порядок фаз в прошивке).

### Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | Пример транслируется целью `sv` | `batch_cycle` в `SV_TRANSLATABLE` | `examples/generated/sv/batch_cycle.sv` порождён; отсутствие роняет предкоммит | R1 / A1 |
| T2 | RTL валиден | `verilator --lint-only -Wall` | принят без предупреждений | R4 / A2 |
| T3 | RTL синтезируем | `yosys synth -top batch_cycle` | синтезирован | R4 / A2 |
| T4 | Тестбенч проходит | `verilator --binary --timing` | фазы 1 → 2 → 3 по порядку, затем `ready` и `is_done`; `assert` не падает | R5 / A3 |
| T5 | **Взведённость тестбенча** | мутация: прогон укорочен до 4 тактов | тестбенч ОТКАЗЫВАЕТ (код выхода ≠ 0) | R5 / A3 |
| T6 | Контракт сценария | `examples_scenario_tests` | цепочка `Cycle, Fill → Cycle, Full → Cycle, Stir → Cycle, Blended → Cycle, Empty → Cycle, Dry → Done` пройдена, прогон завершился | R2, R3 / A4 |
| T7 | Цель `c` под `-Wall -Werror` | `cmake` + `ninja` | собирается; харнесс `batch_cycle_main.c` проходит `assert`-ы (три фазы по порядку) | R6 / A5 |
| T8 | Цель `st` | `iec2c` | валиден | R6 / A5 |
| T9 | Цель `rust` | `rustc` + `clippy -D warnings` | приняты | R6 / A5 |
| T10 | Канон форматирования | `taktc fmt --check examples/` | без изменений | — / A6 |
| T11 | Детерминизм генерации | гейт воспроизводимости (два прогона × цели) | `diff -r` пуст | — / A6 |
| T12 | Существующие примеры не тронуты | `git diff` по прочим `examples/*.takt` | пусто | R7 / A7 |
| T13 | **Контрпример формы:** порт и состояние одноимённы | `out ready: bit` + `state Ready` | цель `c` даёт `redefinition of enumerator` — форма отвергается гейтом, а не принимается молча | — / О7 анализа |
| T14 | Корпус зелёный | `./scripts/precheck.sh` | код выхода 0 | — / A8 |

### Примеры и контрпримеры (правило 16)

Фича **язык не меняет** — новых конструкций и диагностик нет, поэтому языковых
примеров в `book/` не заводится (раздел «Документирование» карточки). Пример и
контрпример здесь относятся к **форме корпусного примера**:

- **пример** — `examples/batch_cycle.takt`: три фазы, идущие по порядку;
- **контрпример** — T13: одноимённые порт и состояние (первая редакция примера),
  на которой цель `c` порождает некомпилируемый код. Ценен тем, что показывает
  границу: гейт `-Wall -Werror` такую форму ловит, а не пропускает.

### Разбивка проверок по функциональности

| Функциональность | T1 | T4 | T6 | T7 | T8 | T9 |
|---|---|---|---|---|---|---|
| Цель `sv` | ⬜ | ⬜ | — | — | — | — |
| Симулятор | — | — | ⬜ | — | — | — |
| Цель `c` | — | — | — | ⬜ | — | — |
| Цель `st` | — | — | — | — | ⬜ | — |
| Цель `rust` | — | — | — | — | — | ⬜ |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

### Тестовые данные и окружение

- `examples/batch_cycle.takt` — сам пример.
- `examples/generated/sv/tb/batch_cycle_tb.sv` — тестбенч (руками).
- `examples/generated/c/batch_cycle_main.c` — харнесс (руками).
- `takt-sim/tests/sim/examples_scenario_tests.rs` — контракт сценария.
- Инструменты: `verilator`, `yosys`, `cc`, `iec2c`, `rustc`/`clippy`. При
  отсутствии `verilator`/`yosys`/`iec2c` соответствующие гейты **мягко
  пропускаются** локально и обязательны в CI (`PRECHECK_STRICT=1`).
- Прогон: `./scripts/precheck.sh`.

## Отчёт о тестировании

### Резюме

**Пройдено; фича готова к закрытию (`ГОТОВО`).** Последовательная композиция `+`
подана на гейты цели `sv` (verilator + yosys), на тестбенч, на контракт сценария
и на харнесс цели `c`. Все проверки тест-плана зелены, обе новые ловушки
**взведены** (доказано мутацией).

### Окружение

- Толчейн — пин `rust-toolchain.toml` (стабильный 1.97.1).
- `verilator` 5.050, `yosys`, `cc` (Apple clang), `iec2c` — доступны; мягких
  пропусков не было.
- Прогон: `./scripts/precheck.sh` (код выхода 0).

### Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | Транслируется целью `sv` | ✅ | `batch_cycle` в `SV_TRANSLATABLE`, `.sv` порождён |
| T2 | RTL валиден | ✅ | `verilator --lint-only -Wall` принял |
| T3 | RTL синтезируем | ✅ | `yosys synth -top batch_cycle` синтезировал |
| T4 | Тестбенч проходит | ✅ | `OK (три фазы по порядку, затем ready и is_done)` |
| T5 | **Взведённость тестбенча** | ✅ | мутация (4 такта вместо 40) → `Assertion failed: фаза перемешивания не наблюдалась`, код выхода 1 |
| T6 | Контракт сценария | ✅ | цепочка из семи состояний пройдена, прогон завершился |
| T7 | Цель `c` под `-Wall -Werror` | ✅ | собран и **запущен**: `три фазы по порядку, завершено за 11 шагов`. Взведённость: `assert(phases == 4)` → код выхода 134 |
| T8 | Цель `st` | ✅ | `iec2c` — валиден |
| T9 | Цель `rust` | ✅ | `rustc` и `clippy -D warnings` приняли |
| T10 | Канон форматирования | ✅ | `taktc fmt --check examples/` — без изменений |
| T11 | Детерминизм генерации | ✅ | воспроизводим по всем восьми целям |
| T12 | Существующие примеры не тронуты | ✅ | `git diff` по прочим `examples/*.takt` пуст |
| T13 | **Контрпример формы** | ✅ | `out ready: bit` + `state Ready` → `cc`: `redefinition of enumerator 'BATCH_CYCLE_READY'`. Форма отвергнута гейтом, а не принята молча |
| T14 | Корпус зелёный | ✅ | `./scripts/precheck.sh` — код выхода 0 |

### Примеры и контрпримеры (правило 16)

Фича язык **не меняет** (раздел «Документирование» карточки), поэтому языковых
примеров в `book/` не заводилось. Пример и контрпример относятся к **форме
корпусного примера**:

- **пример** — `examples/batch_cycle.takt`;
- **контрпример** — T13: одноимённые порт и состояние. Он не только объясняет,
  почему завершающее состояние названо `Done`, но и показывает **границу**: гейт
  цели `c` такую форму ловит.

### Результаты по функциональности

| Функциональность | T1 | T4 | T6 | T7 | T8 | T9 |
|---|---|---|---|---|---|---|
| Цель `sv` | ✅ | ✅ | — | — | — | — |
| Симулятор | — | — | ✅ | — | — | — |
| Цель `c` | — | — | — | ✅ | — | — |
| Цель `st` | — | — | — | — | ✅ | — |
| Цель `rust` | — | — | — | — | — | ✅ |

### Выводы и дальнейшие шаги

1. **Три свидетеля вместо ручной пробы.** Гейты SV доказывают валидность и
   синтезируемость, тестбенч — порядок фаз в RTL, контракт сценария — цепочку в
   симуляторе, харнесс — порядок фаз в прошивке. Наблюдаемые у них **разные**,
   поэтому одинаковая поломка всех четырёх маловероятна.
2. **Согласие по длине трассы.** Симулятор и цель `c` завершают цикл за
   **11 тактов** оба.
3. ⚠️ **Собрать харнесс мало — его надо запустить.** Первая редакция обвязки
   добавляла `batch_cycle` в `CMakeLists`, и харнесс собирался, но не
   запускался: `assert` в неисполненном коде молчит всегда. Прогон добавлен в
   `precheck.sh` явным шагом с проверкой кода возврата.
4. **Найден дефект цели `c`, шире этой фичи**: битовый выходной порт и состояние
   с одинаковым именем дают некомпилируемый код (перечислители в C делят область
   видимости). Записан находкой в `FEATURES.md` с пробой.

Исправлений по самой 0166 не требуется.

## Итог (что сделано)

Последовательная композиция `+` подана на гейты: заведён корпусный пример
`examples/batch_cycle.takt` — технологический цикл из трёх фаз
(`start Cycle = Dose + Mix + Drain { next Done; }`), внесён в `SV_TRANSLATABLE`.

Верность доказывают **три независимых свидетеля с разными наблюдаемыми**, а не
один гейт (урок 0045: линт и синтез валидность доказывают, верность — нет):

- **тестбенч** `examples/generated/sv/tb/batch_cycle_tb.sv` — порядок фаз в RTL
  (наблюдает `stage` иерархической ссылкой, требует 1 → 2 → 3);
- **контракт сценария** в `examples_scenario_tests` — цепочка из семи состояний
  в симуляторе, `must_terminate: true`;
- **харнесс** `examples/generated/c/batch_cycle_main.c` — порядок фаз в прошивке
  (`assert`: номер фазы растёт ровно на единицу). ⚠️ В `precheck.sh` добавлен
  его **прогон**: собранный, но не запущенный `assert` молчит всегда.

Обе новые ловушки **взведены** — доказано мутацией (укороченный прогон
тестбенча → код 1; `assert(phases == 4)` → код 134). Симулятор и цель `c`
завершают цикл за 11 тактов оба.

Попутно найден дефект цели `c`, шире этой фичи: битовый выходной порт и
состояние с одинаковым именем дают некомпилируемый код (перечислители в C делят
область видимости). Записан находкой в `FEATURES.md`.

Детали — [отчёт](0166-sv-example-sequential-composition.md#отчёт-о-тестировании),
[ADR](0166-sv-example-sequential-composition.md#архитектура-adr).
