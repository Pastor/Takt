# Фича 0259: Встроенные функции языка не описаны в документе book/

- **Номер:** 0259
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-19; оговорка «после 0247» снята закрытием той фичи)
- **Tier:** 3 — шесть встроенных функций не описаны в документе (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** прочее
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0259-book-builtin-functions.md`](../adr/0259-book-builtin-functions.md) |
| Анализ | [`docs/analyze/0259-book-builtin-functions.md`](../analyze/0259-book-builtin-functions.md) |
| Разработка | [`0259-01`](../development/0259-01-book-builtin-functions.md) (подраздел «Встроенные функции») |
| Тест-план | [`docs/tests/0259-book-builtin-functions.md`](../tests/0259-book-builtin-functions.md) |
| Отчёт о тестировании | [`docs/reports/0259-book-builtin-functions.md`](../reports/0259-book-builtin-functions.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0259-YY-*`) |

## Краткое описание

**Встроенные функции языка не описаны в документе `book/`.** Замер
[0248](0248-sim-builtin-functions.md) (2026-08-17): язык несёт
шесть встроенных (`debug`, `S`, `min`, `max`, `abs`, `clamp`, реестр —
`takt-lang/src/semantic/builtin.rs`), но раздел «Функции» документа о них
молчит — слово «clamp» встречается там лишь как имя **пользовательской**
функции примера. Автор узнаёт о них из исходников компилятора либо не узнаёт
вовсе. Кандидат: раздел (или подраздел «Функции») с таблицей встроенных, их
семантикой и оговоркой о том, что переводит каждая цель — по правилу 24 это
часть описания языка. ⚠️ Оговорка «заводить после 0247» снята закрытием той
фичи (2026-08-17): место `S(Модель)` определено — она вычислима **только** в
условии `S(Модель) = Состояние`, а в позиции выражения значения не имеет
(`SIM-020` у эталона, `CC-022` у цели `c`). Описывать можно сразу.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Это и есть предмет фичи.** Затронут раздел
[`book/src/07-functions/`](../../book/src/07-functions/index.typ): добавлен
подраздел «Встроенные функции» (таблица, пример, поведение целей, правило
затенения) и дополнен блок «Частые ошибки» (правило 26). Нового раздела фича
не заводит — Option A отвергнут в ADR.

## Итог (что сделано)

**Встроенные функции описаны там, где их ищут.** Раздел «Функции» молчал о
шести встроенных (`min`, `max`, `abs`, `clamp`, `debug`, `S`) — слово `clamp`
встречалось в нём лишь как имя **пользовательской** функции примера. Теперь
там подраздел с таблицей, примером вызова, поведением каждой цели и правилом
затенения; блок «Частые ошибки» дополнен кодами `CC-022`, `ST-011`, `RS-011`,
`SV-002`, `SIM-020`.

Сделано задачей [0259-01](../development/0259-01-book-builtin-functions.md);
подробности — в [отчёте](../reports/0259-book-builtin-functions.md), разбор
вариантов — в [ADR](../adr/0259-book-builtin-functions.md).

**Ключевые решения:**

- **Подраздел, а не отдельный раздел** (Option B ADR): предмет — шесть
  функций, и он естественно живёт внутри «Функций»; новый раздел по правилу 24
  требовал бы своего жизненного цикла ради одной таблицы.
- **Поведение целей названо, а не умолчано:** автор ПЛК-программы вправе
  знать, что `debug` там нет (`ST-011`), а разработчик прошивки — что вызов
  просто не доедет до C.
- **Правило затенения снято прогоном** (`fn clamp(...) { return 42; }` →
  `r = 42`) и названо в тексте: без него таблица противоречила бы примеру
  раздела на вид.
- **Формулировки привязаны к причине, а не к списку кодов** («вызов функции в
  комбинационной логике не выражается»), чтобы описание не устарело от одной
  правки цели.

**Живая проверка:** `./scripts/precheck.sh` — код 0; сборка Typst, гейт
символов (60 файлов) и гейт лексики зелены.

**Вынесено дальше** (правило 7): два дефекта поведения, найденные прогоном, —
арность встроенной не проверяется (`min(1)` даёт невалидный C при нулевом коде
возврата, а `SIM-020` говорит про чужой случай) и цель `c` выбрасывает `debug`
молча. Плюс кандидат о том, что таблица документа — второй носитель знания о
наборе встроенных, и машинной сверки с реестром нет.

