# Фича 0058: Цель `rust`: хвостовой `if/else` с `return` даёт `needless_return`

- **Номер:** 0058
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (генератор `generator/rust/`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0058-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0058-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0058-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0058-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0058-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0058-YY-*`) |

## Краткое описание

Генератор `rust` сворачивает **хвостовой** `return` в выражение (`fn increment` →
`n + 1`), но внутрь **хвостового `if/else`** эта логика не заходит:
`if c { return A; } else { return B; }` печатается дословно, и `clippy -D warnings`
валит гейт (`needless_return`). Форма `if c { return A; } return B;` тем же
генератором печатается **чисто** (`if … { return A; } B`) — то есть дефект в
развороте хвоста, а не в языке. Лечение — распространить хвостовой разворот
(`rust_live`) на ветви завершающего `if/else`. Обходится авторской правкой `.lam`,
поэтому не блокер.

⚠️ Дефект был **скрыт пропуском** `comprehensive` в гейте цели `rust` и обнажился,
когда 0030 сняла пропуск: пропуск, объявленный ради одной причины, прятал вторую —
довод против пропусков по имени файла.

Выявлено при [0030](0030-comprehensive-example-fix.md), подтверждено двумя зондами.

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
