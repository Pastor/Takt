# Фича 0207: Отрицание ~0 для беззнакового типа: два правила языка столкнулись

- **Номер:** 0207
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено аналитиком 2026-08-16, стадия 3)
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу 2026-08-03 (запрос заказчика)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0207-bitwise-not-unsigned-literal.md`](../adr/0207-bitwise-not-unsigned-literal.md) |
| Анализ | [`docs/analyze/0207-bitwise-not-unsigned-literal.md`](../analyze/0207-bitwise-not-unsigned-literal.md) |
| Разработка | [`docs/development/0207-01-bitwise-not-unsigned-literal.md`](../development/0207-01-bitwise-not-unsigned-literal.md) |
| Тест-план | [`docs/tests/0207-bitwise-not-unsigned-literal.md`](../tests/0207-bitwise-not-unsigned-literal.md) |
| Отчёт о тестировании | [`docs/reports/0207-bitwise-not-unsigned-literal.md`](../reports/0207-bitwise-not-unsigned-literal.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0207-YY-*`) |

## Краткое описание

`~0` для беззнакового типа больше не выразим: два правила языка столкнулись. Подробности находки, замеры и оговорки — в разделе «Что известно на
входе»; форма решения определяется стадиями архитектуры и анализа.

> Фича зарегистрирована **из бэклога кандидатов** (запрос заказчика 2026-08-03:
> перевести кандидатов в фичи без проработки); далее проходит жизненный цикл по
> правилу 17.

## Что известно на входе

> Фича заведена **из кандидата** блока 2 `FEATURES.md` (запрос заказчика
> 2026-08-03: перевести кандидатов в фичи без проработки). Проработка —
> стадии 2–3 жизненного цикла; ниже — запись кандидата **как есть**, вместе с
> замерами и оговорками, сделанными при его заведении.
>
> ⚠️ Замеры кандидата **не перепроверялись** при переводе в фичу: их
> актуальность подтверждает пробой стадия архитектуры (урок 0195 — записи
> кандидатов устаревают, и две из них оказались неверны).

**Класс:** семантика языка (Tier 2 — приоритет по решению заказчика
2026-08-03: синтаксис → семантика → генерация → остальное).

**`~0` для беззнакового типа больше не выразим: два правила языка
столкнулись.** Вскрыто при закрытии
[0192](0192-const-init-fold.md) (2026-08-02). Свёртка
инициализатора даёт `-1`, а правило
[0157](0157-literal-u64-representation.md) требует, чтобы литерал
помещался в тип, — `var u: u8 := ~0;` отвергается `SE-089`. Раньше запись
компилировалась, давая **разные** значения: эталон `0`, цель `c` `255`. То есть
расхождение устранено, но **через отказ**, а идиома «все единицы» потеряна.
⚠️ В корпусе и `book/` `~` не используется — срочности нет.
Варианты: оставить отказ (внятный, с позицией; автор пишет `0xFF`) либо учить
свёртку типу назначения и применять норму
[0127](0127-int-overflow-semantics.md) («беззнаковое — обёртка
`mod 2ⁿ`»), тогда `~0` даст `255` везде. Второе касается и правила 0157, то
есть это работа о **языке**, а не о свёртке.

## Документирование (правило 24)

Фича меняет **язык**, поэтому документ правится:

- [`book/src/05-expressions/`](../../book/src/05-expressions/index.typ) («Результат
  за границами типа») — правило распространено на инициализатор объявления,
  добавлены примеры `~0` и `200 + 100`;
- [`book/src/appendix-errors/`](../../book/src/appendix-errors/index.typ)
  (`SE-089`) — уточнено, что проверка смотрит на литерал **автора**, а
  вычисленное нормируется.

Версия языка поднята `0.8.0 → 0.9.0` (правило 22), коммит помечен тегом
`v0.9.0`.

## Итог (что сделано)

Идиома «все единицы» вернулась в язык: `var u: u8 := ~0;` даёт `255` — тот же
ответ, что та же запись в теле. Крейт `takt-lang` (свёртка инициализатора).

Столкновение было не в коде, а в **границе двух правил**: свёртка (0192)
считала `~0` в `i128` и получала `-1`, а проверка диапазона (0157) видела уже
свёрнутый литерал и отвергала его. Ни одно из правил не ошибалось по
отдельности — к моменту проверки вычисленное значение неотличимо от
написанного автором.

Решение заказчика (2026-08-16): **вычисленное** значение нормируется по типу
объявления правилом [0127](../adr/0127-int-overflow-semantics.md) (беззнаковое —
обёртка `mod 2ⁿ`, знаковое — ошибка), **литерал автора** не трогается и его
выход за границы остаётся `SE-089`. Разделение уже существовало в коде —
`is_literal`, заведённый ради понижения `q` (0096).

⚠️ Цена названа прямо: `var u: u8 := 200 + 100;` перестал быть ошибкой и даёт
`44`. Это не побочный эффект, а то же правило — в теле та же запись давала `44`
и раньше.

Живая проверка: `./scripts/precheck.sh` зелёный, вывод корпуса байт-в-байт
неизменен (оператор `~` в `examples/` не встречается), мутация роняет 6 из 9
тестов. Подробности — в
[отчёте](../reports/0207-bitwise-not-unsigned-literal.md); фиксов не
потребовалось.
