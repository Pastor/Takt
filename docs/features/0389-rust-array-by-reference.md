# Фича 0389: Массив в параметре функции цели `rust` — по ссылке

- **Номер:** 0389
- **Статус:** ГОТОВО
- **Зависит от:** нет (0348, 0369, 0372 закрыты)
- **Связанные issue (анализ):** кандидат, заведённый закрытием
  [0372](0372-composite-array-parameter.md)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0389-rust-array-by-reference.md`](../adr/0389-rust-array-by-reference.md) |
| Анализ | [`docs/analyze/0389-rust-array-by-reference.md`](../analyze/0389-rust-array-by-reference.md) |
| Разработка | [`docs/development/0389-01-rust-array-by-reference.md`](../development/0389-01-rust-array-by-reference.md) |
| Тест-план | [`docs/tests/0389-rust-array-by-reference.md`](../tests/0389-rust-array-by-reference.md) |
| Отчёт о тестировании | [`docs/reports/0389-rust-array-by-reference.md`](../reports/0389-rust-array-by-reference.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

Замер 2026-08-22 на `fn total(a: [u8; 4]) -> u8`:

| Цель | Передача массива |
|---|---|
| `c`, `c-hal` | `uint8_t a[4]` — **указатель** |
| `st`, `st-at` | `VAR_IN_OUT` — по ссылке (0348) |
| `sv`, `sv-mmio` | плоский вектор + пролог (0369) |
| **`rust`** | **`a: [u8; 4]` — копия на каждый вызов** |

Наблюдаемого расхождения нет — это **цена**, а не дефект: все девять
потребителей вход исполняют одинаково. Но платить её незачем, и остальные цели
её не платят.

## Документирование (правило 24)

**Не требуется** — язык не меняется; форма передачи принадлежит цели
генерации, наблюдаемое поведение прежнее.

## Итог (что сделано)

- Массив в параметре функции цель `rust` печатает теперь по ссылке
  (`a: &[u8; 4]`), а аргумент — `&self.data`.
- ⚠️ **Признак — ОДИН на два печатника** (`rust_byref::is_array_by_reference`):
  сигнатуру печатает `rust_func`, аргумент — `rust_expr`; разъедься они,
  порождённый код не собрался бы (`E0308`). Отдельный модуль делает копию
  невозможной по построению.
- ⚠️ **Упакованный бит-вектор (`[bit;N ≤ 64]`) исключён:** он **скаляр**
  (правило 0078), и печать типа у него не массивная — передача по ссылке
  развела бы форму аргумента и форму параметра.
- Вывод корпуса не изменился: функций с массивом в параметре в `examples/` нет
  ни одной (то же наблюдение, что у 0372).
