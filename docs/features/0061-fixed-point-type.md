# Фича 0061: Fixed-point Q(m.n) как тип языка

- **Номер:** 0061
- **Статус:** ГОТОВО (закрыта 2026-07-19; все задачи 0061-01…05 реализованы)
- **Зависит от:** **нет** жёстких зависимостей (проверено аналитиком 2026-07-17 — см. [анализ](../analyze/0061-fixed-point-type.md)). **Очерёдность:** [0060](0060-enum-width-shared-layer.md) идёт **первой** (Tier 2 против Tier 3) — обе правят `*_type.rs` всех целей, и после 0060 у 0061 появляется образец «факт в общем слое → отображение в цели».
- **Приоритет / Tier:** **Tier 3.** Ничего не сломано (`sv` отвергает `float` **громко** и с подсказкой), обход есть и применяется (целочисленная арифметика с ручным масштабированием — так написан **весь** корпус), **пользователя нет** (`grep float examples/*.lam` → **0**). ⚠️ **Самая дорогая фича очереди при самом низком спросе** — рекомендация аналитика: брать **после** дефектной части очереди.
- **Крейт:** `grammar` (язык: лексер/грамматика/АСД/типы/форматтер + 4 цели), `simulation` (эталонная арифметика и сверка)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`); закрывает отложенный **Option R-B** [ADR 0045](../adr/0045-sv-backend.md)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0061-fixed-point-type.md`](../adr/0061-fixed-point-type.md) |
| Анализ | [`docs/analyze/0061-fixed-point-type.md`](../analyze/0061-fixed-point-type.md) |
| Разработка | [`docs/development/`](../development/README.md) (задачи [`0061-01`](../development/0061-01-syntax-and-type.md), [`0061-02`](../development/0061-02-simulator-arithmetic.md), [`0061-03`](../development/0061-03-software-targets.md), [`0061-04`](../development/0061-04-sv-target.md), [`0061-05`](../development/0061-05-example-and-docs.md)) |
| Тест-план | [`docs/tests/0061-fixed-point-type.md`](../tests/0061-fixed-point-type.md) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0061-*` — при реализации) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0061-YY-*`) |

## Краткое описание

Цель `sv` запрещает `float` (`SV-003`): в синтезируемом RTL плавающей точки нет.
Fixed-point дал бы вещественную арифметику в аппаратуре, но **язык формата не
выражает**, и генератор был бы вынужден угадать (Q16.16), из-за чего сверка с
симулятором (f64) проваливалась бы by design. То есть это расширение **языка**, а
не бэкенда, — отдельная фича.

Вынесено закрытой [0045](0045-sv-backend.md) (ADR отверг вариант R-B).

## Итог проработки (2026-07-17)

**Заявка кандидата подтверждена дословно** и дополнена замером.

| Что | Факт (пробы 2026-07-17) |
|---|---|
| `float` сегодня | `c` → `double`, `st` → `LREAL`, `rust` → `f64`, `sv` → **`SV-003`** (громкий отказ **с подсказкой**) |
| Согласованность | три программные цели и симулятор — **все на 64 битах** (следствие `--float-width=64`, фича 0029) |
| **Пользователь** | **`grep float examples/*.lam` → 0 вхождений** |

**Принято** ([ADR](../adr/0061-fixed-point-type.md), Option A): новый примитив
`q(m, n)` — знаковый, `m` целых бит **включая знак**, `n` дробных, `W = m + n`;
`float` **не трогаем**. Ключевое: приняты **нормативные** правила арифметики
(floor к −∞ при `*`, wraparound при `+`/`−`, только явные приведения) — без них
фича бессмысленна, потому что пять реализаций (симулятор + 4 цели) разошлись бы
молча. Отклонены: формат флагом цели (CLI не должен менять логику автомата —
принцип, записанный по фиче 0042), `float<8,8>` (одно имя для двух семантик),
статус-кво.

⚠️ **Ловушка, найденная проработкой и стоящая отдельной строки: C11 6.5.7p5.**
Сдвиг `>>` **знакового отрицательного** — **implementation-defined**. Проба на
clang даёт «правильный» результат (`(-6) >> 1 = -3`; `Q8.8: -1.5*2.0 → -768`) — и
именно поэтому опасна: дефект проявится на **чужом** компиляторе, у пользователя,
**молча**. Цель `c` обязана эмитить код, определённый стандартом. Смежно: у `st`
сдвигов над числами нет вовсе (`SHL`/`SHR` — только над битовыми строками, а
арифметика над ними запрещена), а в `rust` `>>` знакового **определён** — то есть
«одинаковая трансляция сдвига» невозможна **в принципе**.

⚠️ **Гейты тут ничего не доказывают:** неверная Q-арифметика **компилируется** и
**синтезируется**. Основной критерий — **побитовая** потактовая сверка симулятора
со **всеми** целями (урок 0045: неверное чтение приняли **оба** инструмента SV).

⚠️ **Доказывать не на чем:** корпус даёт 0 `float`. Поэтому в объём включён
**пример-регулятор** со сценарным контрактом (задача 0061-05) — фикстуры
разработчика проверяют то, что он и задумал, а класс «дыра **в покрытии**, а не в
коде» уже дал дефекты [0026](0026-c-root-typedef.md) и
[0005-01](../fixes/0005-01-c-enum-signedness.md).

**Версия языка не поднимается** (правило 22, решение заказчика 2026-07-15): `0.3.0`
уже поднята фичей 0029 и **не выпущена**, поэтому 0061 вносит изменения в уже
поднятую версию. Обязанность обосновать изменение языка — исполнена в ADR.

## Итог (что сделано)

Тип `q(m, n)` реализован во **всех шести** местах (язык, симулятор, 4 цели) с
**побитово** единой нормативной арифметикой (floor к −∞, wraparound), сверенной
потактово. Задачи:

- **0061-01** — синтаксис/тип `q(m, n)`, литерал (точный перевод, `SE-058`),
  запрет смешения (`SE-059`), приведение `as q`, отображение типа в 4 цели.
- **0061-02** — эталонная Q-арифметика симулятора (`eval::fixed`, `Value::Fixed`).
- **0061-03** — Q-арифметика `c`/`rust`/`st`; ловушка C11 6.5.7p5 закрыта
  floor-делением ([отчёт](../reports/0061-03-software-targets.md)).
- **0061-04** — Q-арифметика `sv` (синтезируемо, `SV-003` для `q` снят;
  [отчёт](../reports/0061-04-sv-target.md)).
- **0061-05** — пример `examples/regulator.lam`, README, `CLAUDE.md`
  ([отчёт](../reports/0061-05-example-and-docs.md)).

**Смежная фича:** [0096](0096-fixed-point-native-float.md) (прозрачный `float`:
глобальная Q-точность + флаги `native`/embedded) — проработана, зависит от 0061.
