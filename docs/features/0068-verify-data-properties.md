# Фича 0068: Верификация свойств над данными (атом-предикат LTL)

- **Номер:** 0068
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17); конвейер даёт закрытая [0049](0049-model-checking-ltl.md)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (`verification/`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0068-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0068-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0068-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0068-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0068-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0068-YY-*`) |

## Краткое описание

Сегодня атом LTL — **имя состояния** (ADR 0049, Option A: условия рёбер и данные
абстрагированы), поэтому `G (temp <= 100)` даёт честный `Verdict::Unsupported`, а
не молчаливое «ложно».

Атом-предикат над переменными (explicit-state или символьный MC) строится **поверх
готового** конвейера `build_kripke → build_buchi(¬φ) → product → emptiness` —
меняется **разметка вершин**, а не звенья.

Сюда же остаточные ветви [ADR 0049](../adr/0049-model-checking-ltl.md):
`X`-stutter, контрпример как трасса симулятора.

Расширение закрытой [0049](0049-model-checking-ltl.md) (ADR, action item A7).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
