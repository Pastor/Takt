# Фича 0233: Правило булевости условия — одно

- **Номер:** 0233
- **Статус:** ГОТОВО
- **Зависит от:** нет незакрытых; прямое продолжение [0232](0232-implicit-bool-warning-delivery.md)
- **Связанные issue (анализ):** требование заказчика 2026-08-05, поставлено при закрытии 0232 (её отчёт оставил сведение предикатов открытым вопросом)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0233-single-boolean-predicate.md`](../adr/0233-single-boolean-predicate.md) |
| Анализ | [`docs/analyze/0233-single-boolean-predicate.md`](../analyze/0233-single-boolean-predicate.md) |
| Разработка | [`0233-01`](../development/0233-01-single-boolean-predicate.md) |
| Тест-план | [`docs/tests/0233-single-boolean-predicate.md`](../tests/0233-single-boolean-predicate.md) |
| Отчёт о тестировании | [`docs/reports/0233-single-boolean-predicate.md`](../reports/0233-single-boolean-predicate.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0233-YY-*`) |

## Краткое описание

«Что считать булевым условием» было записано **дважды** — по семантическому узлу
и по «сырому» АСД, — и правила уже разошлись на `after` (фича 0232: 16 ложных
срабатываний на примерах документа). Замер показал, что второе правило
недостижимо по устройству конвейера: неразрешённое условие есть ошибка `SE-025`,
а предупреждения считаются только при её отсутствии. Правило сведено к одному —
удалением второго, а не построением общего обхода.

> Фича зарегистрирована по требованию заказчика (2026-08-05); далее проходит
> жизненный цикл по правилу 17.

## Документирование (правило 24)

**Не требуется.** Наблюдаемое поведение не меняется вовсе: те же коды, те же
условия срабатывания, те же тексты. Фича переставляет внутреннее устройство
проверки, а устройство проверок в документ не входит.

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->


## Итог (что сделано)

Правило булевости условия существует в **одном** экземпляре.

**Сведение оказалось удалением.** Замер (433 проверки на корпусе, фикстурах и
примерах документа плюс проба на `S(Ping) = End`) показал: запасное правило по
«сырому» АСД не срабатывает **ни разу** и не может — неразрешённое условие есть
ошибка `SE-025`, а предупреждения считаются только при её отсутствии, у обоих
потребителей. Удалены `is_boolean_ast_condition`, `ast_condition_summary`,
запасная ветвь `check_one_ref` и ставший ненужным параметр: модуль сократился с
408 до 269 строк.

**Общий обход через трейт «фактов об узле» отвергнут** (Option B ADR): он
унифицировал бы **композицию** правила (`&`/`|`, скобки), тогда как разошлась
**классификация** — она осталась бы в двух адаптерах. Слой абстракции ради одной
диагностики дороже пользы.

**Решение «на неразрешённом молчим» записано в самом правиле**, рядом с прочими
классификациями: иначе оно снова стало бы ветвлением у потребителя — тем самым,
из которого выросли два правила.

**Тесты сменили уровень.** Сорок юнит-тестов удалённого правила заменены
таблицей из 14 случаев **по исходнику**: она проверяет вердикт, а не внутреннюю
функцию, и переживёт следующую перестройку проверки. Поведение подтверждено
тестами доставки 0232 (5/5).

⚠️ **Честная граница:** ветвь `Unresolved` тестом не покрыть — мутация
«предупреждать на ней» не наблюдается. Это довод в пользу удаления, а не
сохранения «на всякий случай».

**Язык не изменён** (`0.8.0`). Подробности — в
[отчёте](../reports/0233-single-boolean-predicate.md).
