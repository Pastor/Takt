# Фича 0106: Раздел документа «Управляющие конструкции»

- **Номер:** 0106
- **Статус:** СОЗДАНА
- **Зависит от:** нет (инфраструктура `book/` заведена фичей [0101](0101-language-book.md))
- **Приоритет / Tier:** **Tier 3** (документация; раздел ОПИСЫВАЕТ уже реализованные возможности языка — сам синтаксис/семантику не меняет)
- **Связанные issue (анализ):** новая фича (правило 24: каждый раздел документа — отдельная фича; запрос заказчика 2026-07-24)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0106-book-control-flow.md`](../adr/0106-book-control-flow.md) |
| Анализ | [`docs/analyze/0106-book-control-flow.md`](../analyze/0106-book-control-flow.md) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0106-YY-*`) |
| Тест-план | [`docs/tests/0106-book-control-flow.md`](../tests/README.md) |
| Отчёт о тестировании | [`docs/reports/0106-book-control-flow.md`](../reports/README.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0106-YY-*`) |

## Краткое описание

Описывает **управляющие конструкции** в телах: `if`, `while`/`loop`, `for`, `match`/`switch`; именованные блоки `enter`/`exit`/`always` состояния и уровня модели.

> Фича зарегистрирована **2026-07-24** по запросу заказчика (правило 24 — каждый раздел документа заводится отдельной фичей); далее по правилу 17.

## Документирование (правило 24)

Наполняет раздел `book/src/06-control-flow/index.md` описанием языка (см. «Краткое описание»); примеры на Takt — в подкаталоге `06-control-flow/examples/`, изображения — в `06-control-flow/images/`. Раздел **описывает** уже реализованные возможности языка, синтаксис/семантику **не меняет**. В документ не попадают детали реализации, номера фич и процесс разработки.

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->

