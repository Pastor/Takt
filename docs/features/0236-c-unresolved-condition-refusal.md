# Фича 0236: Печатник цели c печатает пустоту на неразрешённом условии

- **Номер:** 0236
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено аналитиком 2026-08-16, стадия 3); [0203](0203-validate-formulas-traversal.md) закрыта
- **Связанные issue (анализ):** находка при закрытии [0203](0203-validate-formulas-traversal.md), 2026-08-15

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0236-c-unresolved-condition-refusal.md`](../adr/0236-c-unresolved-condition-refusal.md) |
| Анализ | [`docs/analyze/0236-c-unresolved-condition-refusal.md`](../analyze/0236-c-unresolved-condition-refusal.md) |
| Разработка | [`docs/development/0236-01-c-unresolved-condition-refusal.md`](../development/0236-01-c-unresolved-condition-refusal.md) |
| Тест-план | [`docs/tests/0236-c-unresolved-condition-refusal.md`](../tests/0236-c-unresolved-condition-refusal.md) |
| Отчёт о тестировании | [`docs/reports/0236-c-unresolved-condition-refusal.md`](../reports/0236-c-unresolved-condition-refusal.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0236-YY-*`) |

## Краткое описание

Печатник цели `c` на `ConditionNode::Unresolved` печатает **пустоту**:
`assert( < 3);` — синтаксическая ошибка, о которой сообщает **чужой** инструмент
(`cc`), а не язык. После [0203](0203-validate-formulas-traversal.md) такой вход
до генератора не доходит, но защиты «в глубину» у цели нет — в отличие от цели
`rust`, которая на том же узле отвечает `RS-011`.

> Фича зарегистрирована **из находки фичи 0203** (запрос заказчика 2026-08-15);
> далее проходит жизненный цикл по правилу 17.

## Что известно на входе

**Класс:** генерация (Tier 2 — сегодня недостижимо через семантику, но
недостижимость держится **другой** фичей, а не самой целью).

Замер 0203 (2026-08-15): на входе `: [Guard] levl < 3;` цель `c` порождала
`assert( < 3);` и рапортовала об успехе; отказ приходил от `cc -Wall -Werror`
(«expected expression»). Это класс 0189: язык принимает запись, а диагностику
даёт чужой инструмент на порождённом файле.

⚠️ **Предмет фичи — не «починить формулы»** (это сделала 0203), а **закрыть
класс**: любой неразрешённый узел, дошедший до печатника цели `c`, обязан давать
`CC-…` с позицией, а не пустую строку. Сегодня таких путей не видно, но каждая
новая конструкция языка способна открыть их снова — и молча.

## Документирование (правило 24)

**Языковой раздел не требуется** — при верном входе вывод не меняется, а новый
отказ сообщает о состоянии, недостижимом из корректной программы. Заведённый
стадией архитектуры код **`CC-023`** внесён в приложение
[«Ошибки»](../../book/src/appendix-errors/index.typ) (правило 25) и в таблицу
диагностик `README.md`.

## Итог (что сделано)

Цель `c` перестала терять неразрешённый узел. Три печатника — условия,
выражения и оператора — зовут **одну** воронку
`generator/c/c_unresolved::refuse(loc, вид)` → **`CC-023`** с позицией из
АСД-узла и названием вида в тексте (образец `FM-001` фичи 0229). Крейт
`takt-lang`.

Что было и чем стало:

- условие — пустая строка (`assert( < 3);`, об ошибке сообщал `cc`) → `CC-023`;
- оператор — молчаливый пропуск → `CC-023`;
- выражение — отказ без кода и позиции → `CC-023`;
- **ребро с неразрешённым условием — безусловный переход** (валидный C, другой
  автомат) → `CC-018` с позицией ребра и причиной `CC-023` заметкой.

Ключевые решения (детали — [ADR](../adr/0236-c-unresolved-condition-refusal.md)):
отвергнут проход семантики «неразрешённых узлов не бывает» — он завёл бы второе
знание о форме `S(Модель) = Состояние`, которая приходит в цель неразрешённой
**по инварианту** (класс 0203). Отказ недостижим из корректной программы: вход
отсекает семантика, поэтому сторож проверяет **ветвь**, а не программу, и падает
списком видов узла.

Живая проверка: `./scripts/precheck.sh` зелёный, вывод корпуса
`examples/generated/` байт-в-байт неизменен; мутация (снятые правки печатников)
роняет четыре теста из шести. Подробности — в
[отчёте](../reports/0236-c-unresolved-condition-refusal.md); фиксов не
потребовалось.

Границы, названные, а не забытые: `VariableNode`, `FunctionDefinitionNode` и
`StateNode` позиции не несут — их безликие отказы остаются за фичей
[0212](0212-c-diagnostic-without-code.md); тот же класс в целях `rust` и `sv` и
фантомный код `CC-022` заведены кандидатами в `FEATURES.md`.

