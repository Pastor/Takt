# Фича 0362: Проба прогоняет инструменты целей

- **Номер:** 0362
- **Статус:** ГОТОВО
- **Зависит от:** нет (0301 закрыта)
- **Связанные issue (анализ):** процессный предмет, найденный при снятии замера
  2026-08-21

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0362-probe-target-tools.md`](../adr/0362-probe-target-tools.md) |
| Анализ | [`docs/analyze/0362-probe-target-tools.md`](../analyze/0362-probe-target-tools.md) |
| Разработка | [`docs/development/0362-01-probe-target-tools.md`](../development/0362-01-probe-target-tools.md) |
| Тест-план | [`docs/tests/0362-probe-target-tools.md`](../tests/0362-probe-target-tools.md) |
| Отчёт о тестировании | [`docs/reports/0362-probe-target-tools.md`](../reports/0362-probe-target-tools.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (не потребовались) |

## Краткое описание

`scripts/probe.sh` (фича 0301) сводит **коды возврата `taktc`** по эталону и
восьми целям. Класс «`taktc` вернул ноль, а вывод отвергает **чужой**
инструмент» такой замер **не видит** — и это не рассуждение, а записанный
вывод фичи 0335: «`probe.sh` этот класс не показывает — он сводит коды
возврата, а все три цели возвращали ноль; вердикт дал прогон целевых
инструментов».

Замер 2026-08-21 (вход `nested.takt` — переменная `[[u8; 2]; 2]`, чтение
`grid[1][0]`):

| Потребитель | `probe.sh` | Прогон инструмента цели |
|---|---|---|
| эталон | `s = 3` | — |
| `c`, `c-hal` | ОТКАЗ `CC-015` | — (до инструмента не доходит) |
| **`st`, `st-at`** | **OK** | **`iec2c` ОТВЕРГ**: «Number of subscripts/indexes does not match … (array has 0 indexes)» |
| `rust` | OK | `rustc` и `clippy -D warnings` приняли |
| `sv`, `sv-mmio` | OK | `verilator --lint-only -Wall` принял |

То есть замер, снятый **по правилу 30**, объявляет цель `st` исправной там,
где она кладёт на диск невалидный ST при нулевом коде возврата.

Предмет фичи — отдать машине ту часть проверки, которую сегодня делает
человек вручную: `probe.sh --tools` прогоняет по порождённому коду те же
инструменты, что и гейты предкоммита (`cc`, `iec2c` + `cc`, `rustc` +
`clippy-driver`, `verilator`), и печатает их вердикт рядом с кодом возврата
`taktc`.

## Документирование (правило 24)

**Не требуется** — язык не меняется: фича трогает инструмент снятия замера
(`scripts/probe.sh`), его сторожа и свод правил процесса. Ни синтаксис, ни
семантика, ни поведение целей не затрагиваются.

## Итог (что сделано)

- `scripts/probe.sh` печатает раздел **«ИНСТРУМЕНТЫ ЦЕЛЕЙ»**: порождённый код
  прогоняется `cc -Wall -Wextra -Werror`, `iec2c` (и сборкой порождённого им
  C), `rustc` + `clippy-driver -D warnings`, `verilator --lint-only -Wall` +
  `yosys synth` — теми же флагами, что у гейтов предкоммита.
- ⚠️ **Класс, который замер не видел, назван замером же:** вход `[[u8; 2]; 2]`
  показывает цели `st`/`st-at` как `OK` в разделе «ЦЕЛИ» и как «iec2c ОТВЕРГ»
  в разделе инструментов.
- ⚠️ **У цели `st` шага два** — намеренно строже гейта (урок 0348: форма,
  принятая разбором, не годна, пока не собран её вывод). Единицу трансляции
  выбирает `iec_c_unit`: у `st-at` собирается `<Имя>Config.c`.
- ⚠️ **Ложную тревогу поймал контроль:** прямая сборка `POUS.c` у `st-at`
  давала «call to undeclared function `__GET_GLOBAL_…`» на **любом** входе,
  включая проходящий гейт `examples/regulator.takt`.
- ⚠️ Отсутствующий арбитр — «пропуск», никогда не «принял»; цель без вывода
  инструмента не получает и называет код отказа `taktc`.
- Сторож `scripts/test-probe.sh` вырос с 10 до 21 проверки; три мутации
  проверены. Контроль на корпусе — ноль ложных тревог на 11 примерах.
- Замер цены: полный прогон пробы — 0.9 с (прежде 0.07 с).

Детали — в [отчёте](../reports/0362-probe-target-tools.md).
