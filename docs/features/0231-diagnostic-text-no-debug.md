# Фича 0231: Текст диагностики без внутреннего представления

- **Номер:** 0231
- **Статус:** ГОТОВО
- **Зависит от:** нет незакрытых (опирается на закрытые [0024](0024-lam-formatter.md), [0077](0077-diagnostic-code-registry.md), [0081](0081-lamc-print-warnings.md))
- **Связанные issue (анализ):** кандидат, найденный при закрытии [0152](0152-semantic-recovery-element-boundary.md); тот же класс правили [0202](0202-fmt-diagnostic-formatting.md) и [0229](0229-format-unsupported-position.md); взят в работу заказчиком 2026-08-05

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0231-diagnostic-text-no-debug.md`](../adr/0231-diagnostic-text-no-debug.md) |
| Анализ | [`docs/analyze/0231-diagnostic-text-no-debug.md`](../analyze/0231-diagnostic-text-no-debug.md) |
| Разработка | [`0231-01`](../development/0231-01-diagnostic-text-no-debug.md) |
| Тест-план | [`docs/tests/0231-diagnostic-text-no-debug.md`](../tests/0231-diagnostic-text-no-debug.md) |
| Отчёт о тестировании | [`docs/reports/0231-diagnostic-text-no-debug.md`](../reports/0231-diagnostic-text-no-debug.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0231-YY-*`) |

## Краткое описание

`SE-025` показывала автору `Debug`-дамп узла АСД вместо сообщения. Замер нашёл
**десять** таких мест — тот же класс проект правил уже трижды (0202, 0229) и
каждый раз находил глазами. Фича выводит внутреннее представление из текстов
(цитата записи автора печатается форматтером, тип — через `Display`, ошибки
ввода-вывода — своим текстом) и оставляет **машинного сторожа**: свод по
фикстурам и пробам, проверяющий сами тексты.

> Фича зарегистрирована из бэклога кандидатов (взята заказчиком 2026-08-05);
> далее проходит жизненный цикл по правилу 17.

## Документирование (правило 24)

**Не требуется.** Языка фича не касается; коды, позиции и условия срабатывания
диагностик прежние. Приложение документа описывает **смысл** кодов, а не
дословные тексты сообщений, поэтому его записи остаются верными.

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->


## Итог (что сделано)

Внутреннее представление больше не попадает в тексты диагностик, и за этим
следит машина.

**Десять мест переведены на человеческий текст:** `SE-025` цитирует запись
автора (печатает форматтер — `'qqq'` вместо `Variable(Identifier { loc: … })`),
`SE-037` печатает тип через `Display` (`[bit;8]`), защитные ветви (`CC-013`,
реализация модели, константа цели `c`) называют причину и ничего не дампят, пять
отказов ввода-вывода печатают текст ошибки вместо `Os { code: 2, … }`, симулятор
показывает имя типа и доступ к члену как в исходнике.

**Сторож:** `takt-lang/tests/diagnostic_text_tests.rs` — 203 фикстуры плюс
список проб, **222** проверенных сообщения, поиск признаков внутреннего
представления в самом тексте. Гейт грепом по исходнику отвергнут замером: `:?}`
встречается в `src/` 147 раз, и почти всё законно.

⚠️ **Список проб — часть сторожа.** Одни фикстуры дают 98 сообщений и `SE-025`
не порождают: без проб свод не заметил бы возврата дампа в то самое сообщение, с
которого начался разбор (проверено мутацией).

**Найдено попутно:** `SE-037` не доезжает до пользователя вовсе —
`implicit_bool_warnings` не включён в `collect_model_warnings`, поэтому его не
печатает ни `compile`, ни редактор. Класс фичи 0081; записано кандидатом.

**Язык не изменён** (`0.8.0`). Подробности — в
[отчёте](../reports/0231-diagnostic-text-no-debug.md).
