# Фича 0163: Исчерпывающий разбор узлов во втором вычислителе `builder.rs::eval_expr`

- **Номер:** 0163
- **Статус:** ГОТОВО
- **Зависит от:** нет
- **Tier:** 2
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md` (подтверждено при закрытии [0093](0093-wildcard-match-rule.md); граница ADR 0093)

## Стадии жизненного цикла (правило 17)

Все стадии — **разделы этой карточки** (правило 32): «Архитектура (ADR)»,
«Анализ», «Разработка», «Тест-план», «Отчёт о тестировании», «Итог».
Отдельным артефактом остаются только исправления —
[`docs/fixes/`](../fixes/README.md) (при необходимости `0163-YY-*`).

## Краткое описание

Вне модуля `eval/` и вне действия `#![deny(clippy::wildcard_enum_match_arm)]`
живёт **второй** вычислитель над `ExpressionNode` с подстановочными ветками — тот
же класс, что дал восемь дефектов фичи [0025](0025-simulator-expression-eval.md):
добавление варианта не ломает сборку, вычислитель молча вернёт `None`.

> Фича зарегистрирована из бэклога кандидатов (отобрана заказчиком 2026-07-28); далее проходит жизненный цикл по правилу 17.

## Что известно на входе

- Подстановочные ветки — `takt-sim/src/unit/builder.rs:215/227/341/503`.
- Гейт `scripts/check-exhaustive-nodes.sh` (0093) защищает пометку узла и `deny`
  в `eval/`, но `_ =>` в `builder.rs` **не ловит**.
- Второй вычислитель **намеренно не слит** с адаптерами (риск порт-регресса,
  урок 0080).
- ⚠️ Территория фичи **0034** (задача 0034-04).

## Направление решения (наметка, не решение)

- Исчерпываемость **без слияния**: item-level
  `#[deny(clippy::wildcard_enum_match_arm)]` на `eval_expr` заставит перечислить
  варианты явно (поведение то же — `_ => None` → явные варианты → `None`).
- После — расширить гейт `check-exhaustive-nodes.sh`, чтобы он сторожил и это
  место: иначе `deny` снимут так же молча.

⚠️ Окончательный выбор — за стадиями **архитектуры** (ADR) и **анализа**: раздел
выше фиксирует то, что уже проверено, а не предрешает реализацию.

## Документирование (правило 24)

**Не требуется.** Внутренняя защита от молчаливого пропуска узла; язык не
меняется.

## Архитектура (ADR)

- **Status:** Accepted
- **Date:** 2026-07-29
- **Authors:** Архитектор + Аналитик
- **Related issues:** [Фича 0163](0163-builder-eval-exhaustive.md); граница [ADR 0093](0093-wildcard-match-rule.md#архитектура-adr); класс дефекта — [0025](0025-simulator-expression-eval.md#архитектура-adr)

### Context

Правило ADR 0093 требует **исчерпывающего** разбора узлов языка
(`ExpressionNode`/`ConditionNode`/`StatementNode`) и закрепляет это
`#![deny(clippy::wildcard_enum_match_arm)]`. Гейт
`scripts/check-exhaustive-nodes.sh` сторожит две вещи: чтобы узлы не пометили
`#[non_exhaustive]` и чтобы `deny` не сняли в `takt-sim/src/eval/`.

Но вычислителей над `ExpressionNode` в проекте **два**. Второй —
`builder.rs::eval_expr`, считающий начальные значения переменных и портов **до**
первого такта, — жил под веткой `_ => None` и в область действия правила не
попадал ни компилятором, ни гейтом.

Цена известна поимённо: класс «вычислители разошлись молча» дал **восемь**
дефектов фичи 0025 при зелёных тестах. Он уже срабатывал и здесь: длительность
(`var left: duration := 1m30s;`) молча превращалась в «значения нет», пока ветвь
`Duration` не была вписана вручную — комментарий об этом стоял прямо над
`_ => None`.

⚠️ Слить вычислители нельзя: у них разные входы (у второго нет ни контекста, ни
портов) и разная обязанность, а попытка слияния грозит порт-регрессом (урок
0080). Значит, инвариант нужно распространить, а не устранить дублирование.

### Decision Drivers

1. **Правило без принуждения — не правило.** `_ => None` не ломает сборку;
   добавленный вариант языка получает умолчание вместо решения.
2. **Механизм должен быть тем же, что уже работает.** В проекте `deny` живёт на
   **уровне модуля** (`eval/mod.rs`, `lsp/semantic_tokens.rs`,
   `semantic/usages/walk.rs`), и гейт проверяет именно файл модуля.
3. **Размер модуля.** `builder.rs` — 927 строк при лимите 1000; явное
   перечисление 39 вариантов упрёт его в гейт размера.
4. **Поведение менять нельзя.** Правка — про принуждение, а не про результат.

### Considered Options

#### Option A. Вынести вычислитель в отдельный модуль с `#![deny(...)]`

`takt-sim/src/unit/initial.rs` — только `eval_expr`, модульный `deny`, гейт
проверяет второй файл.

**Pros:**
- **тот же механизм**, что у трёх существующих модулей: гейту достаточно
  проверить ещё один файл, изобретать форму проверки не нужно;
- `builder.rs` уменьшается (927 → 896) — запас под будущие правки;
- имя модуля называет **тему** («начальные значения»), а не место, откуда код
  переехал.

**Cons:**
- модуль из одной функции; оправдан тем, что несёт инвариант, а не только код.

#### Option B. Item-level `#[deny(...)]` на самой функции (наметка карточки)

**Pros:**
- ничего не двигается.

**Cons:**
- **гейту нечего проверять простым способом**: атрибут внутри файла среди
  тысячи строк, привязка к конкретной функции требует разбора кода, а гейт
  писан на POSIX `sh` без зависимостей;
- расходится с тремя существующими местами, где `deny` — модульный;
- не решает вопрос размера `builder.rs`.

#### Option C. Слить с `eval/`

**Pros:**
- один вычислитель вместо двух.

**Cons:**
- отвергнуто прежде и не этой фичей: разные входы, риск порт-регресса (урок
  0080). Фича про **инвариант**, а не про архитектуру вычислителей.

### Decision

Принимается **Option A**.

`eval_expr` переезжает в `takt-sim/src/unit/initial.rs` с
`#![deny(clippy::wildcard_enum_match_arm)]`; 39 вариантов, прежде попадавших в
`_ => None`, перечислены явно одной ветвью «не константа» с объяснением, почему
они таковы. Поведение **не менялось**: те же варианты дают тот же `None`.

Гейт `check-exhaustive-nodes.sh` проверяет теперь **оба** модуля.

#### Поиск атрибута привязан к началу строки

Мутация вскрыла, что прежняя проверка (`grep -F`) находила атрибут **внутри
док-комментария**, объясняющего, зачем он нужен: модуль без атрибута, но с
рассказом о нём, гейт проходил. Проверка привязана к началу строки
(`grep '^#!\[deny(...)\]'`) — это чинит класс, а не случай.

⚠️ Находка получена **мутацией**, а не чтением: сам гейт до неё выглядел
работающим и на «правильном» дереве был зелёным.

#### Разбор по `Value` тоже стал исчерпывающим

Модульный `deny` не отличает узлы языка от типов крейта, поэтому внутренний
`match` по `Value` в ветви `Negate` пришлось перечислить. Это **к лучшему**:
появится новое представление значения — компилятор потребует решить, отрицаемо
ли оно, вместо молчаливого `None`.

### Consequences

#### Положительные

- второй вычислитель наконец под правилом ADR 0093: новый вариант языка ломает
  сборку и требует решения;
- гейт стережёт **оба** модуля и больше не обманывается комментарием;
- `builder.rs` получил запас по размеру.

#### Отрицательные / Action items

- ветвь «не константа» длинна (39 вариантов). Это цена принуждения: короткая
  запись `_ =>` и есть дефект;
- ⚠️ прочие подстановочные ветки `builder.rs` (`TypeNode`, `StateNode`,
  `Extend`, `Value`) правилом **не** покрыты и остаются: правило ADR 0093 названо
  для трёх узлов языка, и расширять его — отдельное решение.

#### Acceptance criteria

1. `_ => None` по `ExpressionNode` во втором вычислителе невозможен —
   отказывает **компилятор**.
2. Снятие `deny` в любом из двух модулей отвергает **гейт**.
3. Гейт не обманывается упоминанием атрибута в комментарии.
4. Поведение не изменилось: те же входы дают те же значения.
5. `./scripts/precheck.sh` зелёный; `examples/generated/**` не изменился.

## Анализ

### Цель и контекст

Реализовать **Option A** ADR 0163: распространить инвариант исчерпывающего
разбора на второй вычислитель. Работа целиком в `takt-sim` плюс расширение
гейта. Язык **не меняется**, версия языка не растёт.

### Зависимости фичи (правило 17/19)

- **Зависит от:** нет.
- **Влияние на порядок разработки:** ничего не блокирует. Смежна с фичей 0034
  (её территория — структуры и инициализаторы), но пересечения нет: правится
  принуждение, а не поведение.

### Разбор: где именно правило не действовало

Подстановочные ветки `builder.rs` на входе (номера после правок фичи 0181):

| Строка | Разбор по | В области правила ADR 0093? |
|---|---|---|
| 199 | `Option<&TypeNode>` | нет — не узел языка |
| 220 | `Value` | нет — тип крейта |
| **232** | **`ExpressionNode`** | **ДА — цель фичи** |
| 262 | `TypeNode` | нет |
| 346 | `StateNode` | нет |
| 573 | `Extend` | нет |

То есть в область правила попадает **одна** ветвь — `_ => None` в `eval_expr`.
Карточка перечисляла четыре места; замер показал, что три из них разбирают не
узлы языка. Уточнение существенно: расширять правило на `TypeNode`/`Extend` —
отдельное решение, а не часть этой фичи.

**Прецедент срабатывания уже был.** Над `_ => None` стоял комментарий: ветвь
`Duration` (фича 0134) пришлось вписать вручную, потому что без неё
`var left: duration := 1m30s;` молча превращалась в «значения нет». Дефект
поймал тест — то есть правило отсутствовало, и цена уже уплачена.

### Требования и проверяемые условия

- **R1. `_ =>` по `ExpressionNode` не собирается.** Принуждение — компиляторное,
  а не соглашение.
- **R2. Снятие `deny` отвергает гейт** — в **любом** из двух модулей.
- **R3. Гейт не обманывается комментарием.** Упоминание атрибута в док-тексте не
  должно засчитываться за атрибут.
- **R4. Поведение не изменилось.** Варианты, прежде попадавшие в `_ => None`,
  дают тот же `None`; корпус и порождённый код не тронуты.
- **R5. Лимит размера соблюдён.** Ни `builder.rs`, ни новый модуль не выходят за
  1000 строк; реестр долга не пополняется.

### Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | компилятор отвергает `_ =>` (R1) | мутация: вернуть `_ => None` → `cargo clippy` отказывает |
| A2 | гейт отвергает снятие `deny` в `initial.rs` (R2) | мутация + `check-exhaustive-nodes.sh` |
| A3 | гейт отвергает снятие `deny` в `eval/` (R2) | мутация + гейт |
| A4 | гейт не обманывается комментарием (R3) | та же мутация A2 при сохранённом док-тексте |
| A5 | поведение не изменилось (R4) | `cargo test --all-features` зелёный; `git diff examples/generated` пуст |
| A6 | размеры (R5) | `scripts/check-module-size.sh`; `builder.rs` 927 → 896, `initial.rs` 128 |
| A7 | корпус зелёный | `./scripts/precheck.sh` |

### Особенности по обратной совместимости (правило 11)

Изменение **внутреннее**: публичный API крейтов не затронут, поведение
вычислителя прежнее. Порождённый код и трассы симулятора не меняются — правка
касается того, что произойдёт при добавлении **будущего** узла языка.

Прочие стеки (цели генерации, LSP, плагины) — **н/п**.

### Риски

- **Риск «перечислили и забыли».** Длинная ветвь «не константа» соблазняет
  дописывать в неё новый вариант не думая. Снижение: у ветви стоит объяснение,
  **почему** эти узлы не константы (читают состояние, зовут функции, ветвятся),
  то есть автор обязан сопоставить с ним свой узел.
- **Риск мнимого сторожа.** Гейт может выглядеть работающим и не работать —
  ровно это и вскрыла мутация (`grep -F` находил атрибут в комментарии).
  Снижение: каждая ловушка проверена мутацией, а не чтением.
- **Риск разрастания правила.** Соблазн навесить `deny` на весь `builder.rs`
  затронул бы разбор `TypeNode`/`StateNode`/`Extend`, где подстановочная ветвь
  осмысленна. Снижение: модуль вынесен **узкий**, по теме.

## Разработка

### Задача 0163-01

#### Что было

`builder.rs::eval_expr` — второй вычислитель над `ExpressionNode` — разбирал 9
вариантов и сводил остальные 39 в `_ => None`. Ни компилятор, ни гейт
`check-exhaustive-nodes.sh` этого не замечали: правило ADR 0093 действовало
только в `takt-sim/src/eval/`.

Прецедент срабатывания уже был: ветвь `Duration` (фича 0134) пришлось вписывать
вручную после того, как `var left: duration := 1m30s;` молча осталась без
значения.

#### Что сделано

##### Новый модуль

`takt-sim/src/unit/initial.rs` — `eval_expr` (`pub(super)`) под
`#![deny(clippy::wildcard_enum_match_arm)]`. Имя модуля называет **тему** —
начальные значения переменных и портов, — а не место, откуда код переехал.

39 вариантов, прежде попадавших в `_ => None`, перечислены явно **одной ветвью**
«не константа» с объяснением, почему они таковы: читают состояние, зовут
функции, ветвятся или не являются выражением-значением. Поведение прежнее — те
же варианты дают тот же `None`.

⚠️ Модульный `deny` не отличает узлы языка от типов крейта, поэтому внутренний
`match` по `Value` в ветви `Negate` тоже стал исчерпывающим. Это **к лучшему**:
новое представление значения потребует решить, отрицаемо ли оно.

##### Расширение гейта

`scripts/check-exhaustive-nodes.sh` проверяет `deny` теперь в **обоих**
модулях — `eval/mod.rs` и `unit/initial.rs`.

⚠️ **Мутация вскрыла слабость самого гейта:** прежний `grep -F` находил атрибут
внутри **док-комментария**, объясняющего, зачем он нужен, — то есть модуль без
атрибута, но с рассказом о нём, гейт проходил. Поиск привязан к началу строки
(`grep '^#!\[deny(...)\]'`). Это чинит **класс**, а не случай: тот же обман
работал и для `eval/`.

Обратная функциональность (правило 11): изменение внутреннее — публичный API,
поведение вычислителя, порождённый код и трассы прежние. Для прочих стеков —
**н/п**.

#### Проверки

| Что | Результат |
|---|---|
| `cargo clippy --all-targets --all-features -- -D warnings` | чисто |
| `cargo test --all-features -- --test-threads=1` | зелёный |
| `scripts/check-module-size.sh` | `builder.rs` 927 → **896**, `initial.rs` 128; реестр долга не пополнен |
| `git diff examples/generated` | пусто |
| `./scripts/precheck.sh` | зелёный |

**Взведённость доказана тремя мутациями** — и ловят их **разные** механизмы:

| Мутация | Кто отвергает |
|---|---|
| вернуть `_ => None` по `ExpressionNode` | **компилятор** (`wildcard match will also match any future added variants`) |
| снять `deny` в `initial.rs` (док-текст сохранён) | **гейт**, код 1 |
| снять `deny` в `eval/mod.rs` | **гейт**, код 1 |

Вторая мутация и вскрыла слабость `grep -F`: до правки гейт её **не заметил**.

## Тест-план

### Область и цель

Доказать, что второй вычислитель под правилом ADR 0093 и что **обе** ловушки —
компилятор и гейт — взведены. Проверяются R1–R5 анализа и критерии A1–A7.

⚠️ Особенность этой фичи: проверяется не поведение продукта (оно неизменно), а
**принуждение**. Обычные тесты такого не видят — сторож проверяется **мутацией**
исходника, иначе «зелёный гейт» неотличим от отсутствующего.

### Проверки (условие → ожидаемый результат)

| # | Проверка | Предусловие | Ожидаемый результат | Ссылка на R/A |
|---|---|---|---|---|
| T1 | **Мутация:** вернуть `_ => None` по `ExpressionNode` | правка `unit/initial.rs` | `cargo clippy` ОТКАЗЫВАЕТ: `wildcard match will also match any future added variants` | R1 / A1 |
| T2 | **Мутация:** снять `deny` в `initial.rs` | док-текст с упоминанием атрибута **сохранён** | гейт ОТКАЗЫВАЕТ, код ≠ 0 | R2, R3 / A2, A4 |
| T3 | **Мутация:** снять `deny` в `eval/mod.rs` | правка файла | гейт ОТКАЗЫВАЕТ, код ≠ 0 | R2 / A3 |
| T4 | Гейт зелен на неповреждённом дереве | — | `OK: узлы не #[non_exhaustive]; оба вычислителя хранят deny(...)` | R2 / A2, A3 |
| T5 | Поведение вычислителя прежнее | юнит-тесты `builder.rs` (число, bool, отрицание, `None`) | зелёные без правок | R4 / A5 |
| T6 | Корпус и порождённый код не тронуты | прогон предкоммита | `git diff examples/generated` пуст | R4 / A5 |
| T7 | Размеры модулей | после выноса | `builder.rs` < 1000, `initial.rs` < 1000, реестр долга не пополнен | R5 / A6 |
| T8 | Корпус зелёный | `./scripts/precheck.sh` | код выхода 0 | — / A7 |

### Примеры и контрпримеры (правило 16)

Фича **язык не меняет** — языковых примеров в `book/` не заводится. Пример и
контрпример здесь относятся к **сторожу**, а не к продукту:

- **пример** — неповреждённое дерево: гейт зелен, сборка проходит (T4, T5);
- **контрпримеры** — T1, T2, T3: три способа тихо снять инвариант. Каждый обязан
  быть отвергнут, причём **разными** механизмами: T1 — компилятором, T2/T3 —
  гейтом. Без них «правило» существовало бы только в документации.

⚠️ T2 намеренно **сохраняет** док-текст, объясняющий атрибут: именно так была
вскрыта слабость прежней проверки (`grep -F` находил атрибут внутри
комментария, и модуль без атрибута гейт проходил).

### Разбивка проверок по функциональности

| Функциональность | T1 | T2 | T5 | T6 |
|---|---|---|---|---|
| Симулятор (`takt-sim`) | ⬜ | ⬜ | ⬜ | — |
| Гейт `check-exhaustive-nodes.sh` | — | ⬜ | — | — |
| Цели генерации, язык, LSP | — | — | — | ⬜ |

<!-- Легенда: ✅ пройдено · ❌ провалено · ⬜ не проверялось · — не применимо -->

### Тестовые данные и окружение

- `takt-sim/src/unit/initial.rs` — вычислитель под проверкой; юнит-тесты его
  поведения остались в `takt-sim/src/unit/builder.rs` (тот же вход, публичность
  `pub(super)`).
- `scripts/check-exhaustive-nodes.sh` — гейт; вызывается из `precheck.sh`.
- Мутации выполняются вручную с восстановлением файла из копии.
- Прогон: `cargo clippy --all-targets --all-features -- -D warnings`,
  `cargo test --all-features -- --test-threads=1`, `./scripts/precheck.sh`.

## Отчёт о тестировании

### Резюме

**Пройдено; фича готова к закрытию (`ГОТОВО`).** Второй вычислитель под
правилом ADR 0093: `_ =>` по узлу языка отвергает компилятор, снятие `deny` —
гейт. Обе ловушки проверены мутацией; третья мутация вскрыла **слабость самого
гейта**, которая тоже устранена.

### Окружение

- Толчейн — пин `rust-toolchain.toml` (стабильный 1.97.1).
- Прогон: `cargo clippy --all-targets --all-features -- -D warnings`,
  `cargo test --all-features -- --test-threads=1`, `./scripts/precheck.sh`
  (код выхода 0).

### Фактические результаты по проверкам

| # | Проверка | Результат | Комментарий |
|---|---|---|---|
| T1 | **Мутация:** `_ => None` по `ExpressionNode` | ✅ | компилятор: `wildcard match will also match any future added variants`, с перечислением всех 39 вариантов в подсказке |
| T2 | **Мутация:** снят `deny` в `initial.rs` | ✅ | ⚠️ **первый прогон гейт ПРОШЁЛ** — см. вывод 1 |
| T3 | **Мутация:** снят `deny` в `eval/mod.rs` | ✅ | гейт отверг, код 1 |
| T4 | Гейт зелен на неповреждённом дереве | ✅ | «оба вычислителя хранят deny(...)» |
| T5 | Поведение вычислителя прежнее | ✅ | юнит-тесты `builder.rs` зелены без правок |
| T6 | Корпус и порождённый код не тронуты | ✅ | `git diff examples/generated` пуст |
| T7 | Размеры модулей | ✅ | `builder.rs` 927 → **896**, `initial.rs` 128; реестр долга не пополнен |
| T8 | Корпус зелёный | ✅ | `./scripts/precheck.sh` — код 0 |

### Примеры и контрпримеры (правило 16)

Язык не менялся, языковых примеров в `book/` не заводилось. Проверялся **сторож**:

- **пример** — неповреждённое дерево (T4, T5);
- **контрпримеры** — T1, T2, T3: три способа тихо снять инвариант, отвергаемые
  **разными** механизмами (компилятор / гейт). Без них правило жило бы только в
  документации.

### Выводы и дальнейшие шаги

1. **Мутация вскрыла слабость гейта, которую чтение не показывало.** Прежняя
   проверка (`grep -F "#![deny(...)]"`) находила атрибут **внутри
   док-комментария**, объясняющего, зачем он нужен. То есть модуль **без**
   атрибута, но с рассказом о нём, гейт проходил — и на «правильном» дереве
   выглядел работающим. Поиск привязан к началу строки; это чинит **класс**, а
   не случай: тот же обман работал и для `eval/`.
2. **Карточка перечисляла четыре подстановочные ветки — в области правила одна.**
   Остальные три разбирают `TypeNode`, `StateNode` и `Extend`, а правило ADR 0093
   названо для трёх узлов языка. Расширять его на прочие перечисления — отдельное
   решение, в объём не взято.
3. **Побочно исчерпывающим стал разбор по `Value`** (ветвь `Negate`): модульный
   `deny` не отличает узлы языка от типов крейта. Оставлено как есть — новое
   представление значения теперь потребует решить, отрицаемо ли оно.
4. **Прецедент, ради которого фича и заведена, уже был оплачен:** ветвь
   `Duration` (фича 0134) вписывали вручную после того, как
   `var left: duration := 1m30s;` молча осталась без значения. Теперь такой
   вариант ломает сборку, а не молчит.

Исправлений по самой 0163 не требуется.

## Итог (что сделано)

Второй вычислитель вынесен в `takt-sim/src/unit/initial.rs` под
`#![deny(clippy::wildcard_enum_match_arm)]`; 39 вариантов, прежде попадавших в
`_ => None`, перечислены явно одной ветвью «не константа» с объяснением, почему
они таковы. **Поведение не менялось** — те же входы дают те же значения; смысл
правки в том, что следующий вариант языка теперь потребует решения, а не получит
умолчание.

Гейт `scripts/check-exhaustive-nodes.sh` сторожит `deny` в **обоих** модулях.

⚠️ **Мутация вскрыла слабость самого гейта:** прежний `grep -F` находил атрибут
внутри **док-комментария**, объясняющего, зачем он нужен, — модуль без атрибута,
но с рассказом о нём, проверку проходил. Поиск привязан к началу строки; это
чинит **класс**, а не случай (тот же обман работал и для `eval/`).

Взведённость доказана **тремя** мутациями, и ловят их **разные** механизмы:
`_ =>` отвергает компилятор, снятие `deny` в любом из двух модулей — гейт.

⚠️ Уточнение к карточке: из четырёх перечисленных подстановочных веток в области
правила ADR 0093 оказалась **одна** — остальные разбирают `TypeNode`,
`StateNode` и `Extend`, то есть не узлы языка. Расширение правила на прочие
перечисления в объём не взято.

Попутно исчерпывающим стал разбор по `Value` в ветви `Negate`: модульный `deny`
типы крейта от узлов языка не отличает, и это к лучшему.

`builder.rs` уменьшился 927 → 896 строк. Детали —
[отчёт](0163-builder-eval-exhaustive.md#отчёт-о-тестировании),
[ADR](0163-builder-eval-exhaustive.md#архитектура-adr).
