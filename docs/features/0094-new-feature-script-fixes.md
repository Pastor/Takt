# Фича 0094: Доработка `scripts/new-feature.sh` (статус ADR, поздние стадии, идемпотентность)

- **Номер:** 0094
- **Статус:** ✅ ГОТОВО
- **Зависит от:** нет (инфраструктура; подтверждено анализом)
- **Приоритет / Tier:** Tier 3 (процесс/инфраструктура; боль конкретная и повторяющаяся)
- **Крейт:** инфраструктура (`scripts/new-feature.sh`, `docs/templates/`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0094-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0094-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0094-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0094-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0094-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0094-YY-*`) |

## Краткое описание

Три дефекта генератора заготовок (фича [0015](0015-stage-templates.md)):

1. **С `--register` новый ADR получает статус `Accepted`** — ещё до того, как ADR
   написан (выявлено при заведении 0025, правилось вручную). Скрипт должен
   проставлять `Draft`/`Proposed`, а `Accepted` — по **факту** принятия решения на
   стадии 2.
2. **Не умеет добавлять артефакт одной поздней стадии.** Стадии наступают по
   очереди, а генератор работает «всё сразу при заведении фичи»: `--with-dev`
   создаёт development **и** tests вместе, а повторный `--register` **дублирует**
   строки в реестрах уже заведённых стадий (`insert_row` вызывается безусловно).
   Тест-план 0025 пришлось рендерить вручную (правило 17 допускает как запасной
   путь). Нужны флаги вида `--stage tests|dev|report` и **идемпотентный**
   `--register`.
3. **Реестры не пересобираются по факту файлов** (подтверждено на масштабе при
   заведении 0026–0045): генератор создаёт ровно **одну** подзадачу `XXXX-01`, тогда
   как аналитики декомпозировали работу на **76** подзадач — реестр
   `docs/development/README.md` пришлось перестраивать скриптом. Плюс
   переименование подзадачи аналитиком оставляет в реестре **битую ссылку**
   (случай 0044-01). Нужен режим «пересобрать реестр по факту файлов».

⚠️ **Дефект 1 подтверждён на этой же партии:** карточки 0058–0094 заводились
**вручную** (правило 17 допускает как запасной путь) — в том числе потому, что
`--register` пометил бы 37 ненаписанных ADR как `Accepted`, а `--with-dev` создал
бы заготовки стадий, до которых работа не дошла.

> **Объединены два пункта** процессного бэклога (оба — дефекты
> `scripts/new-feature.sh`): чинятся одной правкой одного скрипта (решение
> заказчика 2026-07-17).

> Фича зарегистрирована **2026-07-17** переводом пункта процессного бэклога.
> Проработана и закрыта **2026-07-22**.

## Итог (что сделано)

**Закрыта 2026-07-22 (`ГОТОВО`).** Реализована **Option A**
[ADR 0094](../adr/0094-new-feature-script-fixes.md): `scripts/new-feature.sh`
доработан. **Идемпотентный `insert_row`** по ключу-номеру (`| [NNNN]`/`| NNNN |`/
`| NNNN-YY |`) — повторный `--register` больше **не дублирует** строки (боль,
обходившаяся вручную ×3 за сессию, устранена конструктивно). **ADR-заготовка —
`Draft`** (строка реестра + шаблон `adr.md`); `Accepted` по факту (стадия 2).
**`--stage NAME`** добирает одну стадию (впервые `report`), **`--subtask NN`** —
dev-подзадачи; **`NF_ROOT`** для тестируемости. Регресс-тест
`scripts/test-new-feature.sh` (A1–A5) в `precheck.sh`.

⚠️ **Дефект, пойманный своим же тестом:** `[ … ] && insert_row` под `set -e` при
`REGISTER=0` обрывал `do_stage` (ломал дефолт без `--register`) — заменён на
`if … then … fi`. **Dogfooding:** стадии самой 0094 добраны исправленным скриптом
на реальных реестрах без дублей.

**Отложено в кандидат (Option B ADR):** пересборка реестра из markdown-файлов
(`--rebuild`) — ненадёжна в POSIX sh. Инфраструктура: язык и крейты не затронуты,
версии не поднимаются.

Детали — [анализ](../analyze/0094-new-feature-script-fixes.md),
[разработка](../development/0094-01-new-feature-script-fixes.md),
[тест-план](../tests/0094-new-feature-script-fixes.md),
[отчёт](../reports/0094-new-feature-script-fixes.md).
