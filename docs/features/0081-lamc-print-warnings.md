# Фича 0081: `lamc` не печатает предупреждения вообще

- **Номер:** 0081
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `grammar` (`bin/lamc.rs`)
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0081-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0081-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0081-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0081-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0081-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0081-YY-*`) |

## Краткое описание

`unused_variable_warnings` и `nondeterministic_transition_warnings` — часть
**публичного API** (`lib.rs`), но из CLI **не вызываются**: пользователь не видит
ни Ce13, ни Ce14.

Влияет на доставку **любых новых** предупреждений (SE-053/SE-054 из 0035 и 0042):
диагностика, которую никто не печатает, равносильна её отсутствию.

Выявлено при [0035](0035-ltl-in-blocks.md).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
