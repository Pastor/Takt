# Фича 0019: Унификация грамматик Condition/Expression

- **Номер:** 0019
- **Статус:** РАЗРАБОТКА
- **Зависит от:** нет
- **Крейт:** `grammar`

## Ссылки на артефакты (правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0019-condition-expression-unification.md`](../adr/0019-condition-expression-unification.md) |
| Анализ | [`docs/analyze/0019-condition-expression-unification.md`](../analyze/0019-condition-expression-unification.md) |
| Разработка 0019-01 | [`docs/development/0019-01-loopcond-dedup.md`](../development/0019-01-loopcond-dedup.md) (ЗАПЛАНИРОВАНО) |

> Доведена до стадии «Разработка»: ADR + анализ готовы, реализация не начата.
> Ключевое решение ADR: полное слияние отвергнуто (ломает инвариант `=`/`==` и
> совместимость); объём сужен до устранения дубликата `LoopCond`.

## Краткое описание

Устранение дублирования выражений-грамматик. Язык содержит три близкие грамматики
выражений: `Condition` (условия переходов и `cond`), `Expression` (блоки кода) и
`LoopCond` (третий вариант без `{` — для разрешения LR-конфликта). Каждая
изменяется отдельно, что затрудняет поддержку.

## Мотивация / контекст

Рассмотреть унификацию в единый `Expression` с разной **семантической** валидацией
(например, запрет присваивания в позиции `cond`), вместо трёх синтаксических
грамматик.

## Риски

- Рискованный рефактор грамматики LALRPOP: возможны LR(1)-конфликты.
- Обратная совместимость (правило 11): при изменении разбора условий/выражений
  обосновать влияние на существующие `.lam`.

> Открытая фича. Источник: бывший `TODO.md` (пункт 15), `STATUS.md`
> («пропущенные задачи», п. 15).
