# Фича 0087: Режим «записать и продолжить» при нарушении инварианта

- **Номер:** 0087
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Приоритет / Tier:** не проставлен — определяется при проработке (правило 19)
- **Крейт:** `simulation`
- **Связанные issue (анализ):** новая фича (перевод кандидата из `FEATURES.md`)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/`](../adr/README.md) (`0087-*` — при проработке) |
| Анализ | [`docs/analyze/`](../analyze/README.md) (`0087-*` — при проработке) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0087-YY-*`) |
| Тест-план | [`docs/tests/`](../tests/README.md) (`0087-*`) |
| Отчёт о тестировании | [`docs/reports/`](../reports/README.md) (`0087-*`) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0087-YY-*`) |

## Краткое описание

Ядро [0044](0044-sim-assert-invariant.md) **останавливает** прогон
(`TickResult::Failed`, `SIM-025`) — иначе сверка с C сравнивает несравнимое
(`assert()` → `abort()`).

**Мягкий режим для отладки** (записать нарушение и продолжить прогон) — отдельная
фича: он осмыслен ровно там, где сверки с C нет.

Вынесено закрытой [0044](0044-sim-assert-invariant.md).

> Фича зарегистрирована **2026-07-17** переводом кандидата из `FEATURES.md`
> (решение заказчика: «завести фичи по кандидатам, пока без проработки»).
> **Проработка не проводилась:** ADR, анализ, зависимости, Tier и объём — за
> стадиями 2–3 (правило 17). Текст ниже — **перенос находки кандидата** вместе с
> подтверждающими её пробами; это описание проблемы, а **не** принятое решение.
