# Фича 0161: Остаточные старые имена языка в данных и комментариях

- **Номер:** 0161
- **Статус:** ГОТОВО
- **Зависит от:** нет
- **Tier:** proc
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md` (вынесено закрытием [0100](0100-language-rename-takt.md))

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0161-fixture-comments-rename.md`](../adr/0161-fixture-comments-rename.md) |
| Анализ | [`docs/analyze/0161-fixture-comments-rename.md`](../analyze/0161-fixture-comments-rename.md) |
| Разработка | [`0161-01`](../development/0161-01-fixture-comments-rename.md) |
| Тест-план | [`docs/tests/0161-fixture-comments-rename.md`](../tests/0161-fixture-comments-rename.md) |
| Отчёт о тестировании | [`docs/reports/0161-fixture-comments-rename.md`](../reports/0161-fixture-comments-rename.md) |
| Исправления | не потребовались |

## Краткое описание

Ренейм языка Lam→Takt сознательно **не** трогал комментарии фикстур
`tests/data/**`: часть LSP/goto-фикстур позиционно связана с офсет-ассертами
тестов. Остаточная косметика внутренних данных, не видимая пользователю.

> Фича зарегистрирована из бэклога кандидатов (отобрана заказчиком 2026-07-28); далее проходит жизненный цикл по правилу 17.

## Что известно на входе

- В комментариях фикстур остались `Lam`, `lamc`, `lam-lsp`, `grammar/…`.
- Причина отказа при 0100: `Lam`→`Takt` **длиннее на символ** → сдвиг байтовых
  смещений → падение проверок, завязанных на жёсткий офсет.
- Класс — косметика внутренних данных, **не user-facing**.

## Направление решения (наметка, не решение)

- Аккуратный проход с одновременной правкой ожидаемых позиций в тестах.
- Либо (устойчивее) перевести тесты на **поиск маркера** вместо жёсткого офсета
  — тогда фикстуры перестанут быть позиционно связанными раз и навсегда.

⚠️ Окончательный выбор — за стадиями **архитектуры** (ADR) и **анализа**: раздел
выше фиксирует то, что уже проверено, а не предрешает реализацию.

## Документирование (правило 24)

**Не требуется.** Комментарии тестовых данных; язык не меняется.

## Итог (что сделано)

Старые имена языка и инструментов (`BuT`, `Lam`, `butc`, `lamc`, `lam-lsp`,
расширения `.but`/`.lam`, пути упразднённых крейтов `grammar/…`,
`simulation/…`) убраны из **рабочих** файлов: фикстур обоих крейтов, харнессов
`examples/generated/**`, корпуса примеров, doc-комментария
`takt-lang/src/semantic/unused.rs`. В `docs/` и журналах они остались — там
старое имя есть свидетельство (правило 21). Возврат закрыт гейтом предкоммита
`scripts/check-legacy-names.sh` со сторожем `scripts/test-legacy-names.sh`.

⚠️ **Обоснование отказа из этой же карточки оказалось неверным, и это главный
результат.** Отказ 0100 («`Lam` → `Takt` длиннее на символ → сдвиг байтовых
смещений → падение проверок, завязанных на жёсткий офсет») держал работу
отложенной два месяца. Замер: **ни один** тест, читающий фикстуру с диска, не
адресует её литеральным смещением — позиция строится поиском маркера
(`source.find("= Shared") + 2` и подобные), а литеральные `Position::new(L, C)`
адресуют строковые константы внутри самих `.rs`. Маркерный приём введён фичей
0072 (`d7249482`), то есть **раньше** ренейма: предположение не выполнялось уже
в момент отказа и никем не проверялось. Прогон после правки — 3167 тестов, ноль
провалов, **без единой правки ожидаемой позиции**.

⚠️ **Область оказалась шире карточки, и вне фикстур лежала не косметика.**
`examples/graphics-configs/README.md` — единственное место документа про
настройку графики — предлагал команду, неверную **во всех** элементах: крейт
`simulation` (его нет), бинарник `simulation` (его нет), расширение `.but`
(его нет), путь `examples/gif-configs/` (его нет) и, как показал прогон, флаги
`--gif`/`--gif-config`, которых **не существует вовсе**. Правка одних имён
оставила бы команду неисполнимой — поэтому критерий приёмки проверяется
**прогоном**, а не `grep`. Там же нашлось, что симулятор представлялся в
собственном `--help` упразднённым именем крейта (`name = "simulation"`) и
требовал `<LAM_FILE>`.

⚠️ **Гейт нашёл то, чего не увидел замер.** Первый же его прогон дал **8**
мест сверх 36 измеренных: ссылки вида `simulation/tests/conformance_sv_tests.rs`
— пути, устаревшие **дважды** (крейт переименован 0100, тесты перенесены 0244).
Это и есть довод в пользу гейта против разовой правки.

⚠️ **Граница названа, а не забыта.** Имена в **порождаемом** коде
(`lam_q_floordiv`, `lam_q_mul`, `lam_q_wrap`, `lam_q_sat`, `LAM_Q_*`) гейт не
проверяет: это наблюдаемый вывод инструмента, их смена — ломающее изменение
контракта со своим жизненным циклом. Граница написана в заголовке гейта, её
наличие проверяет сторож, а сам класс вынесен кандидатом в `FEATURES.md`.

Артефакты: [ADR](../adr/0161-fixture-comments-rename.md) ·
[анализ](../analyze/0161-fixture-comments-rename.md) ·
[0161-01](../development/0161-01-fixture-comments-rename.md) ·
[тест-план](../tests/0161-fixture-comments-rename.md) ·
[отчёт](../reports/0161-fixture-comments-rename.md).
