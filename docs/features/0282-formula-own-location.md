# Фича 0282: У формулы нет собственной позиции: два invariant неразличимы

- **Номер:** 0282
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии 3)
- **Tier:** 3 — два invariant неразличимы; при дедупликации схлопнутся в один (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** семантика
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0282-formula-own-location.md`](../adr/0282-formula-own-location.md) |
| Анализ | [`docs/analyze/0282-formula-own-location.md`](../analyze/0282-formula-own-location.md) |
| Разработка | [`0282-01`](../development/0282-01-formula-own-location.md) |
| Тест-план | [`docs/tests/0282-formula-own-location.md`](../tests/0282-formula-own-location.md) |
| Отчёт о тестировании | [`docs/reports/0282-formula-own-location.md`](../reports/0282-formula-own-location.md) |
| Исправления | не потребовались |

## Краткое описание

**У формулы нет собственной позиции, поэтому два `invariant` неразличимы в
диагностике.** Замер при закрытии
[0168](0168-generator-warnings-return.md) (2026-08-16): реестр
мест объявления формул (`semantic/formula/sites.rs`) кладёт в `FormulaSite::loc`
позицию **вместилища** — модели либо состояния, — а не самой формулы, потому
что у `Formula` позиции в дереве нет вовсе. Наблюдаемо: файл с двумя
`invariant` даёт две диагностики `ST-022` с **одинаковой** координатой (`1:1`
обе), и автор не знает, о какой из формул речь. ⚠️ Док-строка поля обещала
«позицию объявления» — утверждение было неверным и исправлено при 0168, но
**причина осталась**. ⚠️ Смежно: дедупликация `diagnostics::normalize`
сравнивает `(loc, код, текст)`, то есть при первой же попытке пропустить
целевые предупреждения через неё две одинаковые записи схлопнутся в одну —
ровно та потеря, от которой фича 0235 сторожила счётом. Кандидат: завести у
формулы собственный `loc` (место — построение `Formula` из АСД).

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Определяется на стадии анализа.** Фича затрагивает язык или наблюдаемое
поведение целей, поэтому нужный раздел `book/` называется после проработки —
до неё утверждение о документе было бы догадкой.

## Итог (что сделано)

**У формулы есть собственная позиция.** Прежде её не было, и реестр мест
(`semantic/formula/sites.rs`) подставлял координату **вместилища**: два
`invariant` давали два предупреждения `ST-022` с одинаковой координатой `1:1` —
автор не знал, о какой формуле речь.

⚠️ **Это была не только неинформативность.** Дедупликация
`diagnostics::normalize` сравнивает `(позиция, код, текст)`: при равном тексте
две записи с одной координатой схлопнулись бы в одну — ровно та потеря, от
которой фича [0235](0235-guard-formula-in-st-sv.md) сторожила **счётом**, то
есть косвенно. Теперь проверка прямая (тест прогоняет `normalize`).

**Сделано:** `Formula::Guard(ConditionNode, Option<String>, Location)`; позиция
берётся у АСД при построении — у `invariant` позиция объявления, у встроенной
формулы позиция аннотации. Реестр мест предпочитает свою позицию, координата
вместилища осталась **запасным ходом** (`condition_to_formula` строит формулу
без исходного текста). Равенство формул позицию **игнорирует** — довод 0056:
иначе две записи одной формулы стали бы разными узлами.

**Сторож** — `takt-lang/tests/semantic/formula_position_tests.rs`: смотрит на
**наблюдаемое** (координаты `ST-022`), а не на внутренний реестр; включает
прямой прогон `normalize` и контроль на формуле, построенной вне разбора.

⚠️ **Граница названа:** `Formula::LTL` позиции не получила — её потребитель
(верификатор) координату не печатает, а `SE-056` живёт своим путём.

**Версия языка не поднята**; крейт `takt-lang`: `0.49.1` → `0.50.0` (изменён
публичный вариант перечисления).

Детали — [отчёт](../reports/0282-formula-own-location.md), решение —
[ADR](../adr/0282-formula-own-location.md).
