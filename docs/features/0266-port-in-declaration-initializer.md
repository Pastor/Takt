# Фича 0266: Чтение порта в инициализаторе объявления

- **Номер:** 0266
- **Статус:** ГОТОВО
- **Зависит от:** нет
- **Tier:** 1 — эталон даёт 0, цель c читает порт в _init — совпадает лишь случайно (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** семантика
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0266-port-in-declaration-initializer.md`](../adr/0266-port-in-declaration-initializer.md) |
| Анализ | [`docs/analyze/0266-port-in-declaration-initializer.md`](../analyze/0266-port-in-declaration-initializer.md) |
| Разработка | [`0266-01`](../development/0266-01-port-in-declaration-initializer.md) |
| Тест-план | [`docs/tests/0266-port-in-declaration-initializer.md`](../tests/0266-port-in-declaration-initializer.md) |
| Отчёт о тестировании | [`docs/reports/0266-port-in-declaration-initializer.md`](../reports/0266-port-in-declaration-initializer.md) |
| Исправления | не потребовались |

## Краткое описание

**Порт в инициализаторе объявления читается по-разному.** Замер
[0246](0246-init-forward-reference.md) (2026-08-17):
`in sensor: bit; var mirror: u8 := sensor;` — эталон даёт `0` (значение порта
до первого такта неизвестно), а цель `c` печатает чтение порта прямо в
`_init` (`(*model->read_bit)(…)`). При `sensor = 0` совпадает случайно.
Соседний класс к `SE-099` (обращение по адресу в инициализаторе запрещено —
«содержимое памяти до первого такта неизвестно»); кандидат: распространить то
же правило на **порты** либо нормировать поведение эталона и целей.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Требуется, и сделано.** Затронуты: языковой раздел
[`book/src/08-ports-addresses/`](../../book/src/08-ports-addresses/index.typ) —
правило записано текстом рядом с симметричным `SE-094`, с примером и
контрпримером, плюс строка в блоке «Частые ошибки» (правило 26); приложение
[«Ошибки и предупреждения»](../../book/src/appendix-errors/index.typ) — раздел
`SE-113` с разбором (правило 25).

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->


## Итог (что сделано)

Чтение порта в инициализаторе объявления отвергается диагностикой **`SE-113`**.
Правило то же, что у `SE-099` (ячейка `#АДРЕС`, фича 0189): память до первого
такта не определена. Обход инициализаторов у них **один** — модуль
`semantic/validate/anon_init.rs` переименован в `init_undefined_read.rs` и судит
чтение неопределённой памяти, чем бы она ни называлась.

⚠️ **Перепроверка замера дала больше, чем знала карточка.** Карточка называла
пару «эталон против цели `c`». Прогон по всем восьми входам генерации и эталону
дал **три** поведения у принимающих и **два** отказа у остальных: ноль у
эталона, чтение HAL-колбэка у `c`, чтение **регистра железа** у `c-hal`,
молчаливая **потеря** инициализатора у `st`/`st-at`, `RS-022` у `rust`,
`SV-002` у `sv`. Молчаливы первые три, а при `sensor = 0` ноль и чтение ещё и
совпадают — расхождение не выдаёт себя ничем.

⚠️ **Первый прогон `c-hal` вводил в заблуждение и едва не перевернул вывод.**
Он отвечал `SE-052` («порт без адреса») — отказом, приходящим раньше и к делу
не относящимся; казалось, что цель, ради которой заведён `SE-099`, эту форму
уже отвергает. Повторный замер с `at 0x100:0` показал настоящее поведение.

⚠️ **Нормировать было нечем — потому запрет.** Общего поведения у формы не
существует: ветвь сброса цели `sv` выражений не вычисляет (свойство синтеза, а
не недоделка), а у эталона до первого такта входа нет. Отказ семантики делает
ответ одинаковым у всех девяти потребителей за один шаг, и обход — однострочный
(`always { x := порт; }`), названный прямо в тексте диагностики.

⚠️ **Единственным потребителем запрещаемой формы был тест.** Фича 0246 прямо
объявляла её законной (`port_initializer_is_accepted`), утверждая «фича
исполняет принятое правило, а не ужесточает язык». Тест переписан в сторож
**границы**: порт судится кодом `SE-113`, а не `SE-109`. В корпусе и фикстурах
записи не было ни одной — предкоммит зелён без правок `examples/`.

Версия крейта `takt-lang`: `0.39.0` → `0.40.0`. Версия языка **не растёт**:
цикл `0.10.0` открыт фичей 0243 и помечен тегом `v0.10.0` (фича 0162) —
изменения накапливаются в уже поднятую версию (правило 22).

Артефакты: [ADR](../adr/0266-port-in-declaration-initializer.md) ·
[анализ](../analyze/0266-port-in-declaration-initializer.md) ·
[0266-01](../development/0266-01-port-in-declaration-initializer.md) ·
[тест-план](../tests/0266-port-in-declaration-initializer.md) ·
[отчёт](../reports/0266-port-in-declaration-initializer.md).
