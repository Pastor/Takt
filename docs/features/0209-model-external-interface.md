# Фича 0209: Внешний интерфейс модели: extern fn в симуляторе и агрегат как аргумент

- **Номер:** 0209
- **Статус:** СОЗДАНА
- **Зависит от:** не проставлено — проставит аналитик на стадии анализа (правило 17)
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу 2026-08-03 (запрос заказчика)

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | не заведена (стадия 2) |
| Анализ | не заведён (стадия 3) |
| Разработка | [`docs/development/`](../development/README.md) (задачи `0209-YY-*`) |
| Тест-план | [`docs/tests/README.md`](../tests/README.md) |
| Отчёт о тестировании | [`docs/reports/README.md`](../reports/README.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0209-YY-*`) |

## Краткое описание

Внешний интерфейс модели не выражается: `extern fn` не исполняется симулятором, агрегат не может быть аргументом параметра. Подробности находки, замеры и оговорки — в разделе «Что известно на
входе»; форма решения определяется стадиями архитектуры и анализа.

> Фича зарегистрирована **из бэклога кандидатов** (запрос заказчика 2026-08-03:
> перевести кандидатов в фичи без проработки); далее проходит жизненный цикл по
> правилу 17.

## Что известно на входе

> Фича заведена **из кандидата** блока 2 `FEATURES.md` (запрос заказчика
> 2026-08-03: перевести кандидатов в фичи без проработки). Проработка —
> стадии 2–3 жизненного цикла; ниже — запись кандидата **как есть**, вместе с
> замерами и оговорками, сделанными при его заведении.
>
> ⚠️ Замеры кандидата **не перепроверялись** при переводе в фичу: их
> актуальность подтверждает пробой стадия архитектуры (урок 0195 — записи
> кандидатов устаревают, и две из них оказались неверны).

**Класс:** семантика языка (Tier 2 — приоритет по решению заказчика
2026-08-03: синтаксис → семантика → генерация → остальное).

**Внешний интерфейс модели не выражается: `extern fn` не исполняется
симулятором, агрегат не может быть аргументом параметра.** Вскрыто фиксом
[0186-01](../fixes/0186-01-example-decomposition.md) (2026-07-30, две пробы)
при попытке сделать интерфейс памяти внешним — по предложению заказчика.
Первое: `extern fn mem_read(addr: u8) -> u8;` цель `c` принимает (метод HAL), а
симулятор останавливает прогон — «внешняя функция не имеет тела». Значит модель
с внешним интерфейсом **непроверяема эталоном**: сверка поведения невозможна, а
примеры документа (их гейт исполняет) такой формы содержать не могут. Второе:
`Memory(prog := {9, 8, 7, 6})` отвергается `SE-083` — константный вычислитель
агрегат не сворачивает, поэтому «программа как аргумент» не выражается.
Устранение первого — стенд/заглушка для `extern fn` в симуляторе (это ADR:
откуда брать значения); второго — агрегатные значения в константном
вычислителе.

## Документирование (правило 24)

<Меняет ли фича ЯЗЫК (синтаксис/семантику/наблюдаемое поведение/диагностики/
поведение целей)? Если да — какой раздел [`book/`](../../book/) затронут и какие
изменения нужно внести (что добавить/переписать, какие примеры на Takt). Если нет
(рефакторинг/инфраструктура/процесс) — «не требуется» с кратким обоснованием.
В документ не попадают детали реализации, номера фич и процесс разработки.>

<!-- При ЗАКРЫТИИ фичи (стадия 8, статус ГОТОВО) сюда добавляется раздел
     «## Итог (что сделано)» —
     ссылки на отчёт/фиксы (правило 21). Незакрытые фичи «Итога» не имеют. -->

