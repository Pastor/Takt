# Фича 0288: Фикстуры сторожатся проверкой «разбирается без ошибок»

- **Номер:** 0288
- **Статус:** ГОТОВО
- **Зависит от:** нет (проставлено на стадии анализа 2026-08-20)
- **Tier:** proc — тесты обещают не то, что проверяют (расставлен по признаку вреда 2026-08-18, см. `FEATURES.md`)
- **Класс:** прочее
- **Связанные issue (анализ):** кандидат блока 2 `FEATURES.md`, переведён в фичу пакетно 2026-08-18

## Ссылки на артефакты (жизненный цикл, правило 17)

| Стадия | Артефакт |
|---|---|
| Архитектура (ADR) | [`docs/adr/0288-fixture-guards-audit.md`](../adr/0288-fixture-guards-audit.md) |
| Анализ | [`docs/analyze/0288-fixture-guards-audit.md`](../analyze/0288-fixture-guards-audit.md) |
| Разработка | [`0288-01`](../development/0288-01-fixture-guards-audit.md) |
| Тест-план | [`docs/tests/0288-fixture-guards-audit.md`](../tests/0288-fixture-guards-audit.md) |
| Отчёт о тестировании | [`docs/reports/0288-fixture-guards-audit.md`](../reports/0288-fixture-guards-audit.md) |
| Исправления | [`docs/fixes/`](../fixes/README.md) (при необходимости `0288-YY-*`) |

## Краткое описание

**Фикстуры `Ce6`/`FE6` сторожатся проверкой «разбирается без ошибок».**
Найдено при закрытии [0204](0204-const-ref-type-inference.md)
(2026-08-16): тест `example_ce6_type_inference_chain_valid` объявлял, что
«тип `y` выводится из переменной `x`», а проверял лишь отсутствие ошибок
разбора — и проходил всё время, пока вывод не работал вовсе. Сторож заменён,
но **соседи не осмотрены**: рядом лежат `ce6_type_from_func`, `fn_array_param`
и другие с той же формой проверки. ⚠️ Класс знаком (фикс 0010-01: тесты на
структуру автомата вместо теста на язык). Кандидат: пройти по фикстурам
`valid/`, у каждой спросить «что именно она обещает» и проверять **это**.

> Фича заведена **из бэклога кандидатов пакетным переводом** (решение заказчика
> 2026-08-18: «перевести всех кандидатов в фичи без проработки, решить, делать
> или нет, позже»). Стадии 2 и 3 **не пройдены**: приоритет, зависимости и
> объём не оценивались. Далее — жизненный цикл по правилу 17.

## Что известно на входе

Всё, что проверено, — в разделе выше: это **дословный текст кандидата** со
своим замером и ссылками на фичу, при закрытии которой он был вынесен. Ничего
сверх этого не проверялось.

⚠️ Замер снят на дату, указанную в тексте. Прежде чем брать фичу в работу,
**перепроверь** его: условие карточки перепроверяется, а не наследуется — урок
фичи [0161](0161-fixture-comments-rename.md), где обоснование отказа держало
работу отложенной два месяца и оказалось неверным уже в момент отказа.

## Направление решения (наметка, не решение)

Наметка — в тексте кандидата выше (там, где он её содержит). Решение о том,
**делать ли фичу вообще**, отложено заказчиком и принимается до стадии 2.

## Документирование (правило 24)

**Не требуется.** Устройство тестов — процесс, а не язык.

## Итог (что сделано)

**Сторож фикстуры проверяет её обещание, а класс констатаций перестал расти.**

- **Пять усиленных сторожей**
  (`takt-lang/tests/semantic/fixture_promises_tests.rs`): тип из возвращаемого
  значения функции (`Bool`), локальная переменная блока (узел **и** разрешённое
  тело), варианты перечисления, узел индексации массива, три именованных блока.
- **Ратчет:** `scripts/fixture-guard-baseline.txt` (28 записей) и гейт
  `scripts/check-fixture-guards.py` — `F1` (новая констатация) и `F2`
  (протухшая запись). Обе пробы прогнаны.
- **Замер 2026-08-20:** из **62** тестов на `valid/` **28** были констатациями
  — кандидат называл двух.

⚠️ **Фикстура обещала не то, что содержит:** `ce4_enum_basic.takt`, вопреки
имени, перечислений не содержит вовсе.

⚠️ **Две ловушки поймал прогон, а не чтение:** проверка «в дереве нет
`Unresolved`» неверна по существу (условия рёбер `ref` остаются неразрешёнными
по инварианту проекта), а искать узлы надо **там, где они написаны** —
индексация в инициализаторах, перечисление во вложенной модели.

Детали — в [отчёте](../reports/0288-fixture-guards-audit.md); решение и
отвергнутые варианты — в [ADR](../adr/0288-fixture-guards-audit.md).
