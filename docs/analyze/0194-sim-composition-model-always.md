# Анализ фичи 0194: Симулятор теряет model-level `always` у модели-композиции

> Фича: [../features/0194-sim-composition-model-always.md](../features/0194-sim-composition-model-always.md) · ADR: [../adr/0194-sim-composition-model-always.md](../adr/0194-sim-composition-model-always.md) · тест-план: [../tests/README.md](../tests/README.md)

## Цель и контекст

ADR принял **Option A** (рекомендация архитектора и решение заказчика совпали):
наполнить `executions` узла композиции блоками модели-владельца при построении.
Механизм исполнения менять не нужно — он уже есть: `tick_body` зовёт
`self.execution("always")` до диспетчеризации формы, а `Unit::execution` читает
поле `executions` и у `Parallel`, и у `Sequential`. Поле объявлено, читается — и
никогда не наполняется.

## Зависимости фичи (правило 17/19)

**Нет.** Контракт 0083 принят и не меняется; механизм 0181 не трогается.

## Места правки

| # | Что | Файл | Замечание |
|---|---|---|---|
| М1 | контекст владельца строится **один раз** и отдаётся обоим потребителям | `takt-sim/src/unit/builder.rs::build_impl` | тем же способом, что в `build_node`: `new_with_parent` при наличии `shared_parent`, иначе `new` |
| М2 | компиляция `named_blocks` владельца | там же | тем же кодом, что в `build_node` (0083) — **вынести** в общую функцию, а не скопировать |
| М3 | укладка в `executions` результата | там же | у `Parallel`/`Sequential`; поле уже есть |

⚠️ **М2 — вынос, а не копия.** Наполнение `executions` из `model.named_blocks`
написано в `build_node` (фича 0083). Вторая копия разъедется при первой правке —
тот же класс, что «арифметика в одном месте» (0042) и «ключ строится одной
функцией» (0084/0193).

## Ограничения, которые определяют форму решения

- **О1. Одна ячейка значений.** Тело владельца и ветви обязаны писать в **один**
  контекст. Сегодня контекст ветвей строит `shared_context` — из «родительской
  модели первой ветви», а не из владельца. Совпадение проверяется **значением**
  (`probe` растёт), а не рассуждением: разойдясь, «починка» молча не сработает.
- **О2. Ровно одно исполнение.** Блок кладётся в **один** юнит — корневой
  композитный, — и в детей не спускается (`Unit::execution` в дочерние юниты не
  ходит, инвариант фикса 0181-01). Проверяется значением: после N тактов
  `probe = N`, не `2N`.
- **О3. Порядок соблюдается даром.** `tick_body` зовёт `execution("always")`
  **до** `tick_parallel`/`tick_sequential` — контракт 0083 «до диспетчеризации
  состояния» выполняется без правки такта.
- **О4. Достижима из текста только одна из двух делегирующих ветвей.** Проба
  2026-08-03: форма «модель без своих состояний» (`model M = A | B { … }`)
  ломается **раньше и целиком** — эталон даёт пустую трассу `[—]` и мгновенное
  завершение, цель `c` отвергает `CC-005`. Правка делается симметрично в обеих
  ветвях (она дешёвая), но **проверить значением** можно только первую; вторая
  честно отмечается непроверенной, а её собственный дефект — кандидатом.
- **О5. Корпус класс не покрывает** (`always` уровня модели есть только у
  моделей со своими состояниями), поэтому сторож обязан быть фикстурным и
  подтверждаться мутацией — иначе он не доказывает ничего.

## Требования и проверяемые условия

| # | Требование | Проверяемое условие |
|---|---|---|
| R1 | Тело `always` владельца-композиции исполняется | эталон даёт `probe = 1, 2, 3` (было `0`) |
| R2 | Ровно один раз за такт | после N тактов `probe = N`, не `2N` |
| R3 | Обе формы объявления | анонимный корень (`start Main = A \| B`) и именованная модель |
| R4 | Обе композиции | `\|` (`tick_parallel`) и `+` (`tick_sequential`) |
| R5 | Значение согласовано с целью `c` | потактовая сверка sim ≡ `c` на накапливающем теле |
| R6 | Модель со своими состояниями не задета | контрпример по-прежнему даёт `1, 2, 3` |
| R7 | Корпус не задет | дифф `examples/generated/` пуст, предкоммит зелёный |

## Критерии приёмки и способ проверки

| # | Критерий (из ADR) | Способ проверки |
|---|---|---|
| A1 | эталон даёт `1, 2, 3` | значенческий тест |
| A2 | обе формы и обе композиции | четыре фикстуры |
| A3 | ровно одно исполнение | накапливающее тело, сравнение с N |
| A4 | сверка sim ≡ `c`, мутация её роняет | снять наполнение → сверка краснеет |
| A5 | корпус и модель со своими состояниями не задеты | `precheck.sh` + контрпример |

⚠️ **A3 и A4 нельзя заменить друг другом.** Пропуск и двойное исполнение — два
разных дефекта, и оба невидимы на идемпотентном теле (`x := выражение`), которым
полон корпус. Именно поэтому 0181-01 дожил до отдельного фикса.

## Предлагаемая декомпозиция разработки

| Задача | Содержание |
|---|---|
| 0194-01 | вынос компиляции `named_blocks` в общую функцию + наполнение `executions` композиции (М1–М3) |
| 0194-02 | сторожа: значенческие тесты на четырёх фикстурах, сверка sim ≡ `c`, мутационная проверка |

## Особенности по обратной совместимости (правило 11)

- **Язык не меняется**, вывод генераторов не меняется — правка целиком в
  `takt-sim`.
- **Поведение эталона меняется** на конструкции, где он расходился со всеми
  целями: это и есть предмет фичи. Сценарий, полагавшийся на «тело не
  исполняется», сломался бы — таких в корпусе нет (О5).
- ⚠️ Контекст ветвей может измениться (М1): если владелец и «родитель первой
  ветви» — разные модели, ветви получат контекст владельца. В корпусе они
  совпадают (проверяется прогоном сверок и предкоммитом); расхождение, если
  всплывёт, — сигнал, а не шум.

## Документирование (правило 24)

**Не требуется.** Правило «model-level `always` исполняется каждый такт» уже
описано (0083) и не меняется — фича приводит эталон в соответствие с ним.
Уточнения формулировки для композиции не потребовалось: контракт сформулирован
про модель, а композиция — её состояние.

## Редакторский слой (правило 29)

**Правок не требуется.** Проверено на этой стадии:

| Что проверялось | Вывод |
|---|---|
| лексика, синтаксис, семантика языка | **не меняются** — правка целиком в `takt-sim/src/unit/builder.rs`, компилятор не задет |
| новый токен / узел АСД / форма объявления | **нет** |
| навигация, ссылки, переименование, hover, структура | **не задеты**: LSP работает над `takt-lang` и о симуляторе не знает |
| новая диагностика | **нет** — Option A отказов не заводит |

## Риски

- **Риск «починка не работает молча»** (О1: разные контексты). Снижение: сторож
  на **значении**, а не на факте наличия кода; контекст строится один раз.
- **Риск двойного исполнения** (О2, возврат дефекта 0181-01). Снижение:
  накапливающее тело и сравнение с N; блок кладётся ровно в один юнит.
- **Риск задеть форму дерева** правкой контекста ветвей (М1). Снижение:
  предкоммит и существующие сверки композиции
  (`sequential_composition_tests`, `conformance_sv_tests`).
