# Анализ фичи 0221: Панель структуры: инвариант состояния символом не становится

> Фича: [../features/0221-lsp-state-invariant-symbol.md](../features/0221-lsp-state-invariant-symbol.md) · ADR: [../adr/0221-lsp-state-invariant-symbol.md](../adr/0221-lsp-state-invariant-symbol.md) · тест-план: [../tests/0221-lsp-state-invariant-symbol.md](../tests/0221-lsp-state-invariant-symbol.md)

## Цель и контекст

Снять асимметрию `documentSymbol`: `invariant` уровня модели становится
символом, `invariant` внутри состояния — нет. Решение ADR (Option B): добавить
`StateElement::Invariant` в сбор детей состояния и сделать разбор
`StateElement` **исчерпывающим**, чтобы следующий вид элемента не выпал молча.

Язык не меняется: правится только редакторский слой (`takt-lang/src/lsp/`).
Меняется **наблюдаемое поведение LSP** — состав панели структуры.

## Зависимости фичи (правило 17/19)

- **Зависит от:** нет. Фичи 0044 (инвариант как конструкция), 0147 (тесты
  `documentSymbol`, тест-заглушка) и 0226 (канон именования, образец
  исчерпывающего разбора `StateElement`) закрыты.
- **Влияние на порядок разработки:** никого не разблокирует. Соседняя
  незакрытая работа по LSP — 0153 (индекс рабочей области) и 0154 (серверный
  `rename` в плагине) — с этой не пересекается: там ссылки и переименование, тут
  состав панели.

## Требования и проверяемые условия

- **R1. Инвариант состояния — символ.** `invariant Имя = …;` внутри состояния
  попадает в детей состояния с видом `CONSTANT`.
- **R2. Контракт диапазонов соблюдён** (0147): `selection_range` покрывает
  **имя**, `range` — всё объявление и содержит `selection_range` внутри себя.
- **R3. Симметрия достигнута, а не переставлена.** Инвариант **модели**
  остаётся символом `CONSTANT`.
- **R4. Состав панели больше ничем не изменился.** `every`, встроенные формулы
  (`: [Guard] …;`, `: [LTL] …;`), `ref`, `next` и одиночная `;` символами не
  становятся.
- **R5. Разбор `StateElement` исчерпывающий.** В `lsp/symbols.rs` нет ветви
  `_`; пропуск нового варианта АСД останавливает сборку (`E0004`).
- **R6. Тест-заглушка снята осознанно.**
  `state_level_invariant_is_not_a_symbol_yet` удалён, вместо него —
  положительная проверка (этого и требовал его текст).
- **R7. Безымянный элемент символом не становится.** У `Invariant` имя —
  `Option<Identifier>`; при `None` символ не создаётся (правило 0147: имя
  берётся через `match … { Some => …, None => continue/skip }`, а не `unwrap`,
  иначе паника сервера во время набора).

## Критерии приёмки и способ проверки

| # | Критерий | Способ проверки |
|---|---|---|
| A1 | R1 | тест: `Sane` в детях `Idle`, вид `CONSTANT` |
| A2 | R2 | тест диапазонов для нового символа (по образцу `selection_range_covers_the_name_for_children`) |
| A3 | R3 | существующий тест на `Safe` (вид `CONSTANT`) остаётся зелёным |
| A4 | R4 | отрицательные тесты: в детях `Idle` нет `every`, формул, `ref`, `next` |
| A5 | R5 | мутация: добавить вариант в `ast::StateElement` → сборка `lsp/symbols.rs` падает `E0004` |
| A6 | R6 | `grep` — теста нет; на его месте положительная проверка |
| A7 | R7 | чтение кода: имя берётся через `?`/`match`, `unwrap` отсутствует |
| A8 | Общая исправность | `./scripts/precheck.sh` — код 0 (включает `cargo test --all-features`) |

## Особенности по обратной функциональности (правило 11)

- **Язык не меняется** — ни синтаксис, ни семантика, ни диагностики; версия
  языка не поднимается (правило 22).
- **Поведение LSP меняется наблюдаемо**: в панели структуры появляется новый
  узел. Слома нет — состав панели не является контрактом, на который опираются
  инструменты; протокол `documentSymbol` допускает любой набор символов.
- **Плагин IntelliJ** зовёт `documentSymbol` через LSP4IJ (фича 0038) и
  показывает то, что вернёт сервер: правок в `extensions/` не требуется.
- Публичный API крейта (`takt_lang::lsp::document_symbols`) сохраняет
  сигнатуру.

## Редакторский слой (правило 29)

Фича **сама** живёт в редакторском слое; правило требует проверить, не нужно ли
править LSP и плагины, — ответ: LSP правится (в этом суть), плагины **нет**.
Плагин IntelliJ своего сбора символов не ведёт (панель строит LSP4IJ по ответу
сервера), плагин Zed списков не ведёт вовсе. Новых токенов, ключевых слов и
узлов АСД фича не вводит, поэтому `semantic_tokens.rs`, `keywords.rs`,
`TaktTokenTypes.KEYWORDS` и `TaktSymbolScanner` не затрагиваются.

## Документирование (правило 24)

**Не требуется.** Документ `book/` описывает язык; состав панели структуры
редактора — свойство инструментария, а не языка (правило 15 — инструментарий
описывает README). Раздел README про IntelliJ перечисляет возможности плагина, а
не состав `documentSymbol`, — правки не требует.

## Риски и зависимости

- **Риск: «заодно» расширить состав панели.** `every`, `ref`, `next` выглядят
  напрашивающимися, но у первого нет имени, а вторые — ссылки; ADR это
  отверг (Option C). Снижение — отрицательные тесты R4, фиксирующие отказ как
  решение, а не как забывчивость.
- **Риск: паника сервера на безымянном инварианте.** `name` —
  `Option<Identifier>`; в АСД он бывает `None` при незавершённом наборе.
  Снижение — R7 и тот же приём, что у соседних ветвей.
- **Риск: исчерпывающий `match` рассыплется при следующей правке АСД.** Это не
  риск, а цель: сборка обязана падать, пока автор не примет решение (A5).
