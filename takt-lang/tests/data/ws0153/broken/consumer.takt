// Потребитель, который сейчас не компилируется (незакрытая скобка). Его
// вхождения `flow` серверу не видны, поэтому переименование обязано отказать,
// а не править остальные файлы.
import "lib.takt";

start Broken {
    always {
        flow := 1;
    }
