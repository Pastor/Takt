// Контр-пример: одинаковые имена в разных категориях
// SemanticIndex должен точно различать их по позиции

// Переменная с именем "Flag"
var Flag: bit := false;

// Тип с именем "Count" (другой идентификатор, не конфликт)
type Count = [bit;8];

start S;
