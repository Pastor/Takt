// Контрпример угадыванию: имя модели (`Motor`) не совпадает с именем файла
// (`engine.lam`). Прежний код строил кандидатов из ИМЕНИ МОДЕЛИ — motor.lam —
// и не нашёл бы цель никогда. Путь обязан браться из реестра файлов (0053).
import "engine.lam" as Motor;

start Main = Motor;
