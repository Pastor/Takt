// FE2: Вывод типа из пользовательских псевдонимов.
// Функция get() возвращает Byte (псевдоним [bit;8]).
// Тип переменной x должен выводиться как Array(8, Bit).

model M {
    type Byte = [bit;8];
    fn get() -> Byte { return 0; }
    var x := get();
    start S {}
}
