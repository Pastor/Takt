// Контрпример: неизвестное имя в теле `loop` (синоним `while`).
model Probe {
    var x: u8 := 0;
    always {
        loop x < 3 {
            x := unknown_var_zzz;
        }
    }
    start A { ref A: x = 200; }
}
start Entry = Probe;
