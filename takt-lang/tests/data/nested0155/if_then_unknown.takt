// Контрпример: неизвестное имя в теле `if` (ветвь then).
model Probe {
    var x: u8 := 0;
    always {
        if x > 0 {
            x := unknown_var_zzz;
        }
    }
    start A { ref A: x = 200; }
}
start Entry = Probe;
