// Контрпример: неизвестное имя в теле `for`.
model Probe {
    var x: u8 := 0;
    always {
        for var i: u8 := 0; i < 3; i := i + 1 {
            x := unknown_var_zzz;
        }
    }
    start A { ref A: x = 200; }
}
start Entry = Probe;
