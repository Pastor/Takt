// Контрпример: неизвестное имя в теле `while`.
model Probe {
    var x: u8 := 0;
    always {
        while x < 3 {
            x := unknown_var_zzz;
        }
    }
    start A { ref A: x = 200; }
}
start Entry = Probe;
