// Контрпример: неизвестное имя в ветви `else`.
model Probe {
    var x: u8 := 0;
    always {
        if x > 0 {
            x := 1;
        }
        else {
            x := unknown_var_zzz;
        }
    }
    start A { ref A: x = 200; }
}
start Entry = Probe;
