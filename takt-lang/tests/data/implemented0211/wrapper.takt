// Подключаемый файл: состояния лежат у ВЛОЖЕННОЙ модели, а не на верхнем
// уровне. Снаружи виден корень файла — без состояний (ловушка именования).
model Helper {
    var speed: u8 := 0;

    start Idle {
        ref Idle;
    }
}
